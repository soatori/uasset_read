<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>true</ImplicitUsings>
    <RootNamespace>GoldenDump</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <!-- Dev-time only: clone https://github.com/atenfyr/UAssetAPI into external/ (gitignored).
         pytest never builds this project; golden JSON is checked in. -->
    <ProjectReference Include="../../../../external/UAssetAPI/UAssetAPI/UAssetAPI.csproj" />
  </ItemGroup>
</Project>
