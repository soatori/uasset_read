"""Kismet bytecode expression system -- EExprToken + KismetExpression class hierarchy + FKismetArchive.

Import from the submodules directly (e.g. ``uasset_read.kismet.tokens``,
``uasset_read.kismet.expressions``).

DEPRECATED since v0.6.0 (Phase 6, issue #621): the v1 pipeline that consumed this
package was removed. Kept alive only for the v2 decode-depth bridge
(``uasset_read.kismet.decompile_bridge``) and shared constants used by
serializers/parsers. Retirement tracked in a follow-up issue.
"""
