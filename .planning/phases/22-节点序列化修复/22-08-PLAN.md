---
phase: 22-节点序列化修复
plan: 08
type: execute
wave: 8
depends_on: [22-07]
files_modified: [uasset_read.py, tests/test_phase21_verification.py]
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04]
user_setup: []

must_haves:
  truths:
    - "Pin 数组可以正确读取，每个节点有正确数量的 Pin"
    - "Pin 连接关系正确构建，linked_to 数组不为空"
    - "execution_flows 包含 IA_Jump → Jump → StopJumping 链路"
    - "data_flows 包含 ActionValue_X/Y → 参数连接"
    - "function_reference.MemberName 可以正确提取"
  artifacts:
    - path: "uasset_read.py"
      provides: "回滚后的版本 + 详细调试输出"
      contains: "DEBUG_PIN_PARSING 或类似的调试标志"
    - path: "tests/test_phase21_verification.py"
      provides: "验证测试"
      exports: ["TestExecutionFlow", "TestDataFlow", "TestNodeProperties"]
  key_links:
    - from: "read_ue_graph_pin()"
      to: "read_pin_array()"
      via: "LinkedTo 数组读取"
      pattern: "linked_to = read_pin_array"
    - from: "read_ue_graph_node()"
      to: "read_ue_graph_pin()"
      via: "Pin 数组循环"
      pattern: "for i in range(pins_count)"
---

<objective>
回滚 22-06 的修改并添加详细调试输出，找出导致 Pin 解析失败的根因。

Purpose: 22-06 的修改（FText 枚举值修正 + SourceIndex 位置修正）导致 TEST-04 从 PASSED → FAILED，且 Pin 数组读取失败。需要回滚修改并添加调试输出，找出具体原因。

Output: 回滚后的代码 + 调试数据 + 根因分析报告
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-节点序列化修复/22-CONTEXT.md
@.planning/phases/22-节点序列化修复/22-06-SUMMARY.md
@.planning/phases/22-节点序列化修复/22-07-SUMMARY.md
@.planning/phases/22-节点序列化修复/22-07-RESEARCH.md
@.planning/phases/22-节点序列化修复/22-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 回滚 22-06 修改并添加详细调试输出</name>
  <files>uasset_read.py</files>
  <read_first>
    E:\Develop\uasset_read\uasset_read.py.backup_22-06
    E:\Develop\uasset_read\uasset_read.py
  </read_first>
  <action>
  回滚 22-06 的所有修改，然后添加详细调试输出。

  回滚的修改（来自 22-06-SUMMARY）：
  1. 恢复 FText ETextHistoryType 枚举值处理（line 2822-2873, 2903-2931）
     - 恢复为 original 版本（history_type=0 跳过 bHasCultureInvariantString）
  2. 恢复 SourceIndex 位置（line 3008-3011）
     - 恢复为在 PinToolTip 之后读取（而非 PinFriendlyName 之后）

  具体操作：
  1. 读取 uasset_read.py.backup_22-06 的相关部分
  2. 使用 git 检查 22-06 之前的版本，或使用 patch 文件回滚
  3. 或者使用 git checkout 恢复到 22-05 提交，然后重新应用 22-07 的修改（如果需要）
  4. 添加详细的调试输出到以下函数：
     - read_ue_graph_pin(): 每个字段读取前后的位置和值
     - read_pin_array(): 每个读取的 Pin 的 PinId 和 PinName
     - read_ue_graph_node(): pins_count 和实际读取的 Pin 数量
     - skip_ftext_editoronly(): FText 跳过尝试的结果

  调试输出格式（使用环境变量 --debug-pin 或全局标志）：
  ```python
  DEBUG_PIN_PARSING = "--debug-pin" in sys.argv

  if DEBUG_PIN_PARSING:
      print(f"[DEBUG PIN] Node: {node_export.object_name}")
      print(f"[DEBUG PIN] pins_count: {pins_count}")
      print(f"[DEBUG PIN] archive position: {archive.tell()}")
  ```

  调试输出应该包括：
  - 每个字段的偏移位置和值（hex 格式显示字节）
  - 异常发生的完整堆栈跟踪
  - Pin 读取成功/失败的数量
  </action>
  <acceptance_criteria>
  - grep "DEBUG_PIN_PARSING" uasset_read.py 找到至少 1 处定义
  - grep "22-06" uasset_read.py 无相关注释（表示 22-06 修改已回滚）
  - grep "SourceIndex.*PinFriendlyName" uasset_read.py 无匹配（表示 SourceIndex 位置已回滚）
  - python -c "from uasset_read import parse_uasset; r = parse_uasset('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/Blueprints/BP_FirstPersonCharacter.uasset'); print(f'Nodes: {len(r.blueprints[0].graphs[0].nodes) if r.blueprints and r.blueprints[0].graphs else 0}')" 输出 30
  </acceptance_criteria>
  <verify>
  <automated>
  python -c "
from uasset_read import parse_uasset
r = parse_uasset('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/Blueprints/BP_FirstPersonCharacter.uasset')
print(f'Graphs: {len(r.blueprints)}')
if r.blueprints:
    print(f'Nodes: {len(r.blueprints[0].graphs[0].nodes)}')
    for node in r.blueprints[0].graphs[0].nodes[:3]:
        print(f'  Node: {node.node_name}, Pins: {len(node.pins)}')
"
  </automated>
  </verify>
  <done>
  代码已回滚到 22-06 之前的状态 + 添加了详细的调试输出标志
  </done>
</task>

<task type="auto">
  <name>Task 2: 运行测试并收集调试数据</name>
  <files>uasset_read.py, tests/test_phase21_verification.py</files>
  <read_first>
    E:\Develop\uasset_read\tests\test_phase21_verification.py
  </read_first>
  <action>
  使用调试输出标志运行所有测试，收集详细的调试数据。

  运行命令：
  ```bash
  python -m pytest tests/test_phase21_verification.py --tb=short -v --debug-pin 2>&1 | tee test_22_08_debug.log
  ```

  分析输出：
  1. 检查 pins_count 的值是否合理（通常 1-20）
  2. 检查每个 Pin 的读取是否成功
  3. 检查 Pin 连接数量（linked_to 数组长度）
  4. 识别第一个失败的 Pin 读取位置和原因

  如果某些节点有 0 个 Pin，记录这些节点的：
  - 节点名（node_export.object_name）
  - 类名（class_name）
  - serial_offset 和 script_serial_size
  - pins_count 的值
  </action>
  <acceptance_criteria>
  - test_22_08_debug.log 文件存在
  - grep -c "DEBUG PIN" test_22_08_debug.log 返回值大于 0
  - grep "pins_count" test_22_08_debug.log 至少有 30 行（每个节点一行）
  - grep "Pin #" test_22_08_debug.log 至少有 30 行（每个节点至少一个 Pin）
  </acceptance_criteria>
  <verify>
  <automated>
  grep -c "DEBUG PIN" test_22_08_debug.log
  </automated>
  </verify>
  <done>
  调试日志已生成，包含所有 Pin 解析的详细信息
  </done>
</task>

<task type="auto">
  <name>Task 3: 根据调试数据分析根因并修复</name>
  <files>uasset_read.py</files>
  <read_first>
    E:\Develop\uasset_read\test_22_08_debug.log
    E:\Develop\uasset_read\.planning\phases\22-节点序列化修复\22-07-RESEARCH.md
  </read_first>
  <action>
  根据调试日志分析 Pin 解析失败的根因，并实施针对性修复。

  可能的根因和修复方案：

  **根因 1: FText 跳过失败**
  - 症状: FText 跳过异常，archive 位置错误
  - 修复: 改进 skip_ftext_editoronly() 的异常处理和验证逻辑

  **根因 2: SourceIndex 版本检查失败**
  - 症状: SourceIndex 读取位置错误，导致后续字段偏移
  - 修复: 改进版本检查的 fallback 逻辑，或移除 SourceIndex 读取

  **根因 3: PinToolTip 格式误解**
  - 症状: PinToolTip 读取字节数不正确（预期 4 bytes，实际 6 bytes）
  - 修复: 调整 read_fstring() 或 PinToolTip 的处理逻辑

  **根因 4: Direction 序列化格式**
  - 症状: Direction 后有额外的 2 bytes（00 00）
  - 修复: 跳过 Direction 后的 2 bytes padding

  **根因 5: pins_offset 动态扫描失败**
  - 症状: pins_count 为垃圾值（如 41984），或 Pin 数组读取失败
  - 修复: 改进动态扫描的验证条件或使用更精确的 pattern

  根据调试日志中的实际数据，选择最可能的根因并实施修复。

  修复后再次运行测试验证效果：
  ```bash
  python -m pytest tests/test_phase21_verification.py --tb=short -v
  ```
  </action>
  <acceptance_criteria>
  - python -m pytest tests/test_phase21_verification.py::TestExecutionFlow -v --tb=short 退出码为 0
  - python -m pytest tests/test_phase21_verification.py::TestDataFlow -v --tb=short 退出码为 0
  - python -m pytest tests/test_phase21_verification.py::TestNodeProperties::test_node_guid_present -v --tb=short 退出码为 0
  - python -m pytest tests/test_phase21_verification.py::TestNodeProperties::test_function_reference_member_name -v --tb=short 退出码为 0
  - python -m pytest tests/test_phase21_verification.py::TestNodeCount::test_node_count_matches_exports -v --tb=short 退出码为 0（回归验证）
  </acceptance_criteria>
  <verify>
  <automated>
  python -m pytest tests/test_phase21_verification.py::TestExecutionFlow -v --tb=short
  python -m pytest tests/test_phase21_verification.py::TestDataFlow -v --tb=short
  python -m pytest tests/test_phase21_verification.py::TestNodeProperties::test_node_guid_present -v --tb=short
  python -m pytest tests/test_phase21_verification.py::TestNodeProperties::test_function_reference_member_name -v --tb=short
  </automated>
  </verify>
  <done>
  TEST-02/03/04 全部通过，execution_flows、data_flows、function_reference 正确提取
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .uasset → parser | 不可信的二进制数据，可能包含恶意构造的值 |
| archive.tell() → offset calc | 可能的整数溢出或错误偏移计算 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08-01 | T (Tampering) | read_ue_graph_pin | mitigate | pins_count 边界检查（0-1000），防止 DoS |
| T-22-08-02 | I (Information Disclosure) | DEBUG_PIN_PARSING | accept | 调试输出仅在 --debug-pin 模式下启用，生产环境关闭 |
| T-22-08-03 | D (Denial of Service) | read_pin_array | mitigate | MAX_PINS_PER_NODE 限制，防止无限循环 |
| T-22-08-04 | E (Elevation of Privilege) | N/A | accept | 无权限提升风险 |
| T-22-08-05 | R (Repudiation) | N/A | accept | 无不可否认风险 |
| T-22-08-06 | S (Spoofing) | N/A | accept | 无伪造风险 |

</threat_model>

<verification>
## Phase 验证步骤

1. **代码回滚验证**：
   - 确认 22-06 修改已回滚
   - 确认调试输出标志已添加（--debug-pin）

2. **测试执行验证**：
   - 运行 TEST-02：execution_flows 应包含 IA_Jump → Jump → StopJumping
   - 运行 TEST-03：data_flows 应包含 ActionValue_X/Y 连接
   - 运行 TEST-04：function_reference.MemberName 应正确提取

3. **调试数据收集**：
   - 确认调试日志包含所有 Pin 解析的详细信息
   - 确认日志可以用于根因分析

4. **根因修复验证**：
   - 确认根因已识别并修复
   - 确认修复后 TEST-02/03/04 全部通过

5. **回归测试**：
   - 确认 TEST-01 仍然通过（K2Node 数量匹配）
   - 确认没有引入新的测试失败
</verification>

<success_criteria>
- [ ] 22-06 修改已回滚
- [ ] 调试输出标志已添加到关键函数
- [ ] 调试日志已生成，包含详细解析信息
- [ ] 根因已识别并修复
- [ ] TEST-02 通过（execution_flows 正确构建）
- [ ] TEST-03 通过（data_flows 正确构建）
- [ ] TEST-04 通过（function_reference.MemberName 正确提取）
- [ ] TEST-01 仍然通过（K2Node 数量匹配）
- [ ] 22-08-SUMMARY.md 已创建，包含详细分析过程
</success_criteria>

<output>
After completion, create `.planning/phases/22-节点序列化修复/22-08-SUMMARY.md`
</output>