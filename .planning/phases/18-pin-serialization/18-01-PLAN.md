---
phase: 18-pin-serialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [uasset_read.py]
autonomous: true
requirements: [PIN-01, PIN-05]

must_haves:
  truths:
    - "CustomVersion常量可用于版本条件判断"
    - "UEdGraphPin dataclass包含所有需求字段"
    - "BitField值可解析为显示属性"
  artifacts:
    - path: "uasset_read.py"
      provides: "CustomVersion常量定义"
      contains: "FFrameworkObjectVersion.*GUID"
    - path: "uasset_read.py"
      provides: "UEdGraphPin完整字段"
      contains: "pin_tooltip.*default_object.*hidden"
  key_links:
    - from: "UEdGraphPin dataclass"
      to: "read_ue_graph_pin()"
      via: "字段定义"
      pattern: "UEdGraphPin.*pin_tooltip"
---

<objective>
添加 CustomVersion 常量定义和扩展 UEdGraphPin dataclass，为后续函数重写提供类型基础。

Purpose: 按UE源码定义版本阈值常量，确保序列化顺序正确；扩展dataclass包含所有需求字段。
Output: CustomVersion常量 + 完整UEdGraphPin结构定义
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-pin-serialization/18-RESEARCH.md

<interfaces>
<!-- 从当前代码提取的关键接口 -->
From uasset_read.py L1155-1167 (FEdGraphPinType dataclass):
```python
@dataclass
class FEdGraphPinType:
    pin_category: str = ""
    pin_sub_category: str = ""
    pin_sub_category_object: int = 0
    container_type: int = 0
    is_reference: bool = False
    is_const: bool = False
    is_weak_pointer: bool = False
    is_uobject_wrapper: bool = False
```

From uasset_read.py L1208-1226 (当前UEdGraphPin):
```python
@dataclass
class UEdGraphPin:
    pin_id: str
    pin_name: str
    direction: int
    pin_type: "FEdGraphPinType"
    default_value: Optional[str] = None
    auto_default_value: Optional[str] = None
    linked_to_raw: List[str] = field(default_factory=list)
    sub_pins: List[str] = field(default_factory=list)
    parent_pin: Optional[str] = None
    flags: int = 0
```

From uasset_read.py L40-115 (现有版本常量):
```python
UE5_SCRIPT_SERIALIZATION_OFFSET = 1010
UE5_PROPERTY_TAG_EXTENSION = 1011
UE5_PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 CustomVersion 常量定义</name>
  <files>uasset_read.py</files>
  <read_first>
    - E:\Develop\uasset_read\uasset_read.py (L40-115 常量定义区)
    - E:\Develop\lib\UnrealEngine\Engine\Source\Runtime\Core\Public\UObject\FrameworkObjectVersion.h (CustomVersion GUID)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-RESEARCH.md (L50-90 版本阈值)
  </read_first>
  <action>
    在常量定义区（L40-115附近）添加以下 CustomVersion GUID 和版本阈值：

    1. FFrameworkObjectVersion GUID 和阈值：
       ```python
       # CustomVersion GUIDs (from FrameworkObjectVersion.h)
       FFRAMEWORK_OBJECT_VERSION_GUID = "0F4D4A9B-18B3-4D9F-BDE9-6A8F9A9C9D9F"  # 实际GUID需从UE源码提取
       FFRAMEWORK_VERSION_PINS_STORE_FNAME = 17  # PinsStoreFName 阈值
       FFRAMEWORK_VERSION_ED_GRAPH_PIN_CONTAINER_TYPE = 22  # EdGraphPinContainerType 阈值
       ```

    2. FUE5MainStreamObjectVersion GUID 和阈值：
       ```python
       FUE5_MAINSTREAM_VERSION_GUID = "..."
       FUE5_MAINSTREAM_VERSION_ED_GRAPH_PIN_SOURCE_INDEX = 0  # EdGraphPinSourceIndex 阈值（需确认）
       ```

    3. FReleaseObjectVersion 阈值（已有）：
       ```python
       FRELEASE_VERSION_PIN_TYPE_UOBJECT_WRAPPER = ...  # PinTypeIncludesUObjectWrapperFlag
       ```

    注意：GUID值必须从 UE源码 FrameworkObjectVersion.h 提取精确值，不可猜测。
  </action>
  <verify>
    <automated>grep -c "FFRAMEWORK.*GUID" uasset_read.py >= 1 && grep -c "PINS_STORE_FNAME" uasset_read.py >= 1</automated>
  </verify>
  <done>
    - CustomVersion GUID 常量定义存在
    - 版本阈值常量存在（PinsStoreFName、EdGraphPinContainerType、EdGraphPinSourceIndex）
    - 常量命名遵循 UE 源码命名规范
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 UEdGraphPin dataclass 字段</name>
  <files>uasset_read.py</files>
  <read_first>
    - E:\Develop\uasset_read\uasset_read.py (L1208-1226 当前UEdGraphPin)
    - E:\Develop\lib\UnrealEngine\Engine\Source\Runtime\Engine\Classes\EdGraph\EdGraphPin.h (Pin完整字段)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-RESEARCH.md (L327-347 字段缺失列表)
  </read_first>
  <action>
    扩展 UEdGraphPin dataclass（L1208-1226）添加以下字段：

    ```python
    @dataclass
    class UEdGraphPin:
        """UEdGraphPin 蓝图引脚完整结构（Phase 18 扩展）。

        来自 UE 源码 EdGraphPin.cpp L1838-1964 序列化顺序验证。
        """
        pin_id: str                          # FGuid hex（16 bytes）
        pin_name: str                        # FName 解析结果

        # PIN-01: 基础信息
        pin_tooltip: str = ""                # FString - PinToolTip (RESEARCH L335)
        direction: int = 0                   # uint8: 0=Input, 1=Output

        # PIN-02: PinType
        pin_type: "FEdGraphPinType" = None   # FEdGraphPinType结构

        # PIN-03: 默认值
        default_value: Optional[str] = None  # FString
        auto_default_value: Optional[str] = None  # FString
        default_object: Optional[int] = None  # FPackageIndex (RESEARCH L340)
        default_text_value: Optional[str] = None  # FText简化 (RESEARCH L341)

        # PIN-04: 连接引用（格式改变）
        linked_to_raw: List[dict] = field(default_factory=list)  # D-01a改为dict格式
        sub_pins: List[dict] = field(default_factory=list)       # 同linked_to格式
        parent_pin: Optional[dict] = None                       # 同linked_to格式

        # PIN-05: 显示属性（BitField解析）
        hidden: bool = False              # bit 0
        not_connectable: bool = False     # bit 1
        advanced_view: bool = False       # bit 4
        orphaned_pin: bool = False        # bit 5

        # EditorOnly/版本依赖字段（内部使用，不输出到JSON）
        owning_node_index: int = 0        # FPackageIndex (序列化起始)
        source_index: Optional[int] = None  # int32 - 版本依赖
        persistent_guid: Optional[str] = None  # FGuid hex - EditorOnly
    ```

    关键变更：
    1. linked_to_raw/sub_pins/parent_pin 从 List[str] 改为 List[dict] 格式
    2. 新增 pin_tooltip, default_object, default_text_value 字段
    3. 新增 hidden/not_connectable/advanced_view/orphaned_pin 显示属性
    4. 新增 owning_node_index/source_index/persistent_guid 内部字段
  </action>
  <verify>
    <automated>grep -c "pin_tooltip.*str" uasset_read.py >= 1 && grep -c "hidden.*bool" uasset_read.py >= 1 && grep -c "default_object.*int" uasset_read.py >= 1</automated>
  </verify>
  <done>
    - UEdGraphPin 包含 pin_tooltip 字段（PIN-01）
    - UEdGraphPin 包含 default_object/default_text_value 字段（PIN-03）
    - UEdGraphPin 包含 hidden/not_connectable/advanced_view/orphaned_pin 字段（PIN-05）
    - linked_to_raw/sub_pins 类型改为 List[dict]
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 输入数据 | .uasset二进制文件，不可信来源 |
| 解析器 | 内部处理，可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | CustomVersion值 | accept | 使用UE源码精确值，无运行时修改 |
| T-18-02 | Tampering | dataclass字段 | accept | 仅定义结构，无数据处理 |
</threat_model>

<verification>
- grep验证：CustomVersion常量存在
- grep验证：UEdGraphPin新字段存在
- 类型检查：linked_to_raw改为List[dict]
</verification>

<success_criteria>
- CustomVersion GUID常量定义完成
- 版本阈值常量（PinsStoreFName等）定义完成
- UEdGraphPin dataclass扩展包含所有需求字段
- 字段类型正确（dict而非str用于连接引用）
</success_criteria>

<output>
完成后创建 `.planning/phases/18-pin-serialization/18-01-SUMMARY.md`
</output>