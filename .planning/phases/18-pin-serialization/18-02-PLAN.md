---
phase: 18-pin-serialization
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: [uasset_read.py]
autonomous: true
requirements: [PIN-04]

must_haves:
  truths:
    - "Pin引用格式包含OwningNode和PinGuid"
    - "Pin数组格式包含count和多个引用"
    - "read_pin_reference可解析单个Pin引用"
  artifacts:
    - path: "uasset_read.py"
      provides: "Pin引用解析函数"
      contains: "def read_pin_reference"
    - path: "uasset_read.py"
      provides: "Pin数组解析函数"
      contains: "def read_pin_array"
  key_links:
    - from: "read_pin_reference()"
      to: "read_ue_graph_pin()"
      via: "LinkedTo/SubPins/ParentPin解析"
      pattern: "read_pin_reference.*archive"
---

<objective>
添加 Pin 引用解析辅助函数，为 read_ue_graph_pin() 重写提供 SerializePin 格式支持。

Purpose: 按UE源码SerializePin格式实现引用解析，包含OwningNode+PinGuid而非仅PinId。
Output: read_pin_reference() + read_pin_array() 辅助函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-pin-serialization/18-RESEARCH.md

<interfaces>
<!-- UE源码验证的SerializePin格式 -->
From EdGraphPin.cpp L2132-2296 (SerializePin格式):
```cpp
// SerializePin format:
// 1. bNullPtr (bool/uint8)
// 2. OwningNode (FPackageIndex / int32)
// 3. PinGuid (FGuid 16 bytes)
```

From EdGraphPin.cpp L2063-2098 (SerializePinArray格式):
```cpp
// SerializePinArray format:
// 1. ArrayNum (int32)
// 2. For each element: SerializePin
```

From uasset_read.py (FArchive方法):
```python
archive.read_bool()  # bool读取
archive.read_i32()   # FPackageIndex
archive.read_bytes(16)  # FGuid
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 read_pin_reference() 函数</name>
  <files>uasset_read.py</files>
  <read_first>
    - E:\Develop\uasset_read\uasset_read.py (L2577-2675 当前read_ue_graph_pin)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-RESEARCH.md (L257-314 SerializePin格式)
    - E:\Develop\lib\UnrealEngine\Engine\Source\Runtime\Engine\Private\EdGraph\EdGraphPin.cpp (L2132-2296)
  </read_first>
  <action>
    在 read_ue_graph_pin() 函数之前（约L2575位置）添加 read_pin_reference() 函数：

    ```python
    def read_pin_reference(
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        import_map: List[ObjectImport]
    ) -> Optional[dict]:
        """
        读取单个Pin引用（SerializePin格式）。

        用于LinkedTo/SubPins/ParentPin字段，格式来自UE源码EdGraphPin.cpp L2132-2296：
        1. bNullPtr (bool/uint8)
        2. OwningNode (FPackageIndex/int32)
        3. PinGuid (FGuid 16 bytes)

        Args:
            archive: FArchive positioned at pin reference start
            name_map: NameMap for FName resolution
            export_map: ExportMap for FPackageIndex resolution
            import_map: ImportMap for FPackageIndex resolution

        Returns:
            dict with "owning_node" (str) and "pin_guid" (str), or None if null
        """
        # 1. bNullPtr flag
        b_null_ptr = archive.read_bool()
        if b_null_ptr:
            return None

        # 2. OwningNode (FPackageIndex)
        owning_node_index = archive.read_i32()

        # 3. PinGuid (FGuid 16 bytes)
        pin_guid_bytes = archive.read_bytes(16)
        pin_guid = pin_guid_bytes.hex().upper()

        # Resolve OwningNode FPackageIndex to node name
        # FPackageIndex: >0 = ExportMap, <0 = ImportMap (negated index)
        owning_node_name = ""
        if owning_node_index > 0:
            node_idx = owning_node_index - 1  # FPackageIndex is 1-indexed
            if node_idx < len(export_map):
                owning_node_name = export_map[node_idx].object_name
        elif owning_node_index < 0:
            import_idx = -owning_node_index - 1
            if import_idx < len(import_map):
                owning_node_name = import_map[import_idx].object_name

        return {
            "owning_node": owning_node_name,
            "pin_guid": pin_guid
        }
    ```

    注意：
    - OwningNode使用FPackageIndex格式（正数ExportMap，负数ImportMap）
    - 返回dict而非str，包含节点名称和PinGuid
  </action>
  <verify>
    <automated>grep -c "def read_pin_reference" uasset_read.py >= 1 && grep -c "b_null_ptr.*read_bool" uasset_read.py >= 1 && grep -c "owning_node_index.*read_i32" uasset_read.py >= 1</automated>
  </verify>
  <done>
    - read_pin_reference()函数存在
    - 函数读取bNullPtr + OwningNode + PinGuid三个字段
    - OwningNode解析为节点名称
    - 返回dict格式{"owning_node": ..., "pin_guid": ...}
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 read_pin_array() 函数</name>
  <files>uasset_read.py</files>
  <read_first>
    - E:\Develop\uasset_read\uasset_read.py (L2577-2675 当前LinkedTo解析)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-RESEARCH.md (L257-272 SerializePinArray格式)
    - E:\Develop\lib\UnrealEngine\Engine\Source\Runtime\Engine\Private\EdGraph\EdGraphPin.cpp (L2063-2098)
  </read_first>
  <action>
    在 read_pin_reference() 之后添加 read_pin_array() 函数：

    ```python
    def read_pin_array(
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        import_map: List[ObjectImport]
    ) -> List[dict]:
        """
        读取Pin引用数组（SerializePinArray格式）。

        用于LinkedTo/SubPins字段，格式来自UE源码EdGraphPin.cpp L2063-2098：
        1. ArrayNum (int32)
        2. For each: read_pin_reference()

        安全边界：count <= MAX_LINKEDTO_PER_PIN (100)

        Args:
            archive: FArchive positioned at array start
            name_map: NameMap for FName resolution
            export_map: ExportMap for FPackageIndex resolution
            import_map: ImportMap for FPackageIndex resolution

        Returns:
            List of dict pin references (empty list if count=0)

        Raises:
            ParseError: if count exceeds MAX_LINKEDTO_PER_PIN or negative
        """
        # 1. ArrayNum (int32)
        array_count = archive.read_i32()

        if array_count < 0:
            raise ParseError(f"Invalid pin array count: {array_count} (negative)")
        if array_count > MAX_LINKEDTO_PER_PIN:
            raise ParseError(
                f"Pin array count {array_count} exceeds MAX_LINKEDTO_PER_PIN "
                f"{MAX_LINKEDTO_PER_PIN}"
            )

        # 2. For each element
        pins: List[dict] = []
        for _ in range(array_count):
            pin_ref = read_pin_reference(archive, name_map, export_map, import_map)
            if pin_ref is not None:
                pins.append(pin_ref)

        return pins
    ```

    注意：
    - 使用MAX_LINKEDTO_PER_PIN边界检查（已有常量）
    - 调用read_pin_reference()解析每个元素
    - 返回List[dict]而非List[str]
  </action>
  <verify>
    <automated>grep -c "def read_pin_array" uasset_read.py >= 1 && grep -c "read_pin_reference.*archive" uasset_read.py >= 2 && grep -c "MAX_LINKEDTO_PER_PIN" uasset_read.py >= 5</automated>
  </verify>
  <done>
    - read_pin_array()函数存在
    - 函数读取ArrayNum + 调用read_pin_reference循环
    - 边界检查使用MAX_LINKEDTO_PER_PIN
    - 返回List[dict]格式
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 二进制输入 | Pin引用数据来自.uasset，不可信 |
| 边界检查 | MAX_LINKEDTO_PER_PIN防止DoS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Denial of Service | array_count | mitigate | MAX_LINKEDTO_PER_PIN=100限制 |
| T-18-04 | Tampering | FPackageIndex | accept | 索引越界时返回空字符串 |
</threat_model>

<verification>
- grep验证：read_pin_reference函数存在
- grep验证：read_pin_array函数存在
- grep验证：边界检查存在
</verification>

<success_criteria>
- read_pin_reference()正确读取bNullPtr + OwningNode + PinGuid
- read_pin_array()正确读取ArrayNum + 循环调用
- 边界检查使用MAX_LINKEDTO_PER_PIN
- 返回类型为dict而非str
</success_criteria>

<output>
完成后创建 `.planning/phases/18-pin-serialization/18-02-SUMMARY.md`
</output>