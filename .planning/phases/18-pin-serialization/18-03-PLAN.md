---
phase: 18-pin-serialization
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified: [uasset_read.py]
autonomous: true
requirements: [PIN-01, PIN-02, PIN-03, PIN-04, PIN-05]

must_haves:
  truths:
    - "read_ue_graph_pin从OwningNode开始读取"
    - "所有字段按UE源码顺序读取"
    - "LinkedTo使用SerializePin格式解析"
    - "BitField解析为显示属性"
  artifacts:
    - path: "uasset_read.py"
      provides: "完整Pin序列化解析"
      contains: "def read_ue_graph_pin"
      min_lines: 100
  key_links:
    - from: "read_ue_graph_pin()"
      to: "read_pin_reference()"
      via: "LinkedTo/SubPins解析"
      pattern: "read_pin_array.*linked_to"
    - from: "read_ue_graph_pin()"
      to: "read_ed_graph_pin_type()"
      via: "PinType解析"
      pattern: "read_ed_graph_pin_type.*archive"
---

<objective>
完全重写 read_ue_graph_pin() 函数，按 UE 源码 EdGraphPin.cpp L1838-1964 序列化顺序解析所有字段。

Purpose: 解决当前实现从PinId开始跳过OwningNode导致的所有字段错位问题。
Output: 正确的Pin序列化解析，包含所有需求字段
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-pin-serialization/18-RESEARCH.md
@.planning/phases/18-pin-serialization\18-01-PLAN.md (已完成的常量和dataclass)
@.planning/phases/18-pin-serialization\18-02-PLAN.md (已完成的辅助函数)

<interfaces>
<!-- UE源码验证的完整序列化顺序 -->
From EdGraphPin.cpp L1838-1964 (UEdGraphPin::Serialize):
```cpp
// 1. OwningNode (FPackageIndex) [L1844]
// 2. PinId (FGuid 16 bytes) [L1845]
// 3. PinName (FName/FString) [L1847-1856] - version dependent
// 4. [PinFriendlyName] (FText) [L1858-1863] - EditorOnly
// 5. [SourceIndex] (int32) [L1865-1868] - version dependent
// 6. PinToolTip (FString) [L1870]
// 7. Direction (uint8) [L1871]
// 8. PinType (FEdGraphPinType) [L1872]
// 9. DefaultValue (FString) [L1873]
// 10. AutogeneratedDefaultValue (FString) [L1874]
// 11. DefaultObject (FPackageIndex) [L1875]
// 12. DefaultTextValue (FText) [L1876]
// 13. LinkedTo (SerializePinArray) [L1886]
// 14. SubPins (SerializePinArray) [L1889]
// 15. ParentPin (SerializePin) [L1891]
// 16. ReferencePassThroughConnection (SerializePin) [L1892]
// 17. [PersistentGuid] (FGuid) [L1897] - EditorOnly
// 18. [BitField] (uint32) [L1902-1942] - EditorOnly
```

From 18-RESEARCH.md L327-347 (当前问题诊断):
| 序号 | UE 源码字段 | 当前实现 | 问题 |
|------|-------------|----------|------|
| 1 | OwningNode (FPackageIndex) | 缺失 | **位置错位根因** |
| 13 | LinkedTo (SerializePinArray) | 格式错误 | 仅读 PinId，缺失 OwningNode + PinGuid |
| 18 | BitField (uint32) | 格式错误 | 当前读 u8，应读 uint32 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 read_ue_graph_pin() 序列化顺序</name>
  <files>uasset_read.py</files>
  <read_first>
    - E:\Develop\uasset_read\uasset_read.py (L2577-2687 当前实现)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-RESEARCH.md (L43-153 完整序列化顺序)
    - E:\Develop\lib\UnrealEngine\Engine\Source\Runtime\Engine\Private\EdGraph\EdGraphPin.cpp (L1838-1964)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-01-PLAN.md (dataclass扩展)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-02-PLAN.md (辅助函数)
  </read_first>
  <action>
    完全重写 read_ue_graph_pin() 函数（L2577-2687），按UE源码顺序读取：

    ```python
    def read_ue_graph_pin(
        archive: FArchive,
        name_map: List[str],
        summary: PackageFileSummary,
        export_map: List[ObjectExport],
        import_map: List[ObjectImport]
    ) -> UEdGraphPin:
        """
        读取 UEdGraphPin（完整序列化格式）。

        序列化顺序（UE源码 EdGraphPin.cpp L1838-1964 验证）：
        1. OwningNode (FPackageIndex) - 序列化起始字段
        2. PinId (FGuid 16 bytes)
        3. PinName (FName/FString) - 版本依赖
        4. [PinFriendlyName] (FText) - EditorOnly
        5. [SourceIndex] (int32) - 版本依赖
        6. PinToolTip (FString)
        7. Direction (uint8)
        8. PinType (FEdGraphPinType)
        9. DefaultValue (FString)
        10. AutogeneratedDefaultValue (FString)
        11. DefaultObject (FPackageIndex)
        12. DefaultTextValue (FText)
        13. LinkedTo (SerializePinArray)
        14. SubPins (SerializePinArray)
        15. ParentPin (SerializePin)
        16. ReferencePassThroughConnection (SerializePin) - 不输出，仅位置同步
        17. [PersistentGuid] (FGuid) - EditorOnly
        18. [BitField] (uint32) - EditorOnly，解析显示属性

        Args:
            archive: FArchive positioned at pin start
            name_map: NameMap for FName resolution
            summary: PackageFileSummary for version info
            export_map: ExportMap for FPackageIndex resolution
            import_map: ImportMap for FPackageIndex resolution

        Returns:
            UEdGraphPin dataclass with all fields populated
        """
        # 版本检查
        framework_version = summary.custom_version.get(FFRAMEWORK_OBJECT_VERSION_GUID, 0)
        mainstream_version = summary.custom_version.get(FUE5_MAINSTREAM_VERSION_GUID, 0)

        # 1. OwningNode (FPackageIndex) [L1844] - 关键：序列化起始字段
        owning_node_index = archive.read_i32()

        # 2. PinId (FGuid 16 bytes) [L1845]
        pin_id_bytes = archive.read_bytes(16)
        pin_id = pin_id_bytes.hex().upper()

        # 3. PinName (version dependent) [L1847-1856]
        if framework_version >= FFRAMEWORK_VERSION_PINS_STORE_FNAME:
            pin_name = archive.read_name(name_map)
        else:
            pin_name = archive.read_fstring()

        # 4. PinFriendlyName (FText) - EditorOnly [L1858-1863]
        # 跳过EditorOnly字段（uncooked资产可能有，但当前测试资产为cooked）
        # 若需要支持uncooked，应检查 PKG_FilterEditorOnly flag
        # 暂不读取

        # 5. SourceIndex (int32) - version dependent [L1865-1868]
        source_index = None
        if mainstream_version >= FUE5_MAINSTREAM_VERSION_ED_GRAPH_PIN_SOURCE_INDEX:
            source_index = archive.read_i32()

        # 6. PinToolTip (FString) [L1870]
        pin_tooltip = archive.read_fstring()

        # 7. Direction (uint8) [L1871]
        direction = archive.read_u8()

        # 8. PinType (FEdGraphPinType) [L1872]
        pin_type = read_ed_graph_pin_type(archive, name_map, summary)

        # 9-10. DefaultValue strings [L1873-1874]
        default_value = archive.read_fstring()
        autogenerated_default_value = archive.read_fstring()

        # 11. DefaultObject (FPackageIndex) [L1875]
        default_object_index = archive.read_i32()

        # 12. DefaultTextValue (FText) [L1876]
        # FText简化处理：跳过复杂格式，仅读取基础文本
        # 若有实现，调用read_ftext；否则跳过
        default_text_value = None  # TODO: Phase后续实现FText

        # 13. LinkedTo (SerializePinArray) [L1886]
        linked_to = read_pin_array(archive, name_map, export_map, import_map)

        # 14. SubPins (SerializePinArray) [L1889]
        sub_pins = read_pin_array(archive, name_map, export_map, import_map)

        # 15. ParentPin (SerializePin) [L1891]
        parent_pin = read_pin_reference(archive, name_map, export_map, import_map)

        # 16. ReferencePassThroughConnection (SerializePin) [L1892]
        # 不在需求范围，仅读取用于位置同步
        ref_pass_through = read_pin_reference(archive, name_map, export_map, import_map)

        # 17-18. EditorOnly fields [L1894-1948]
        persistent_guid = None
        hidden = False
        not_connectable = False
        advanced_view = False
        orphaned_pin = False

        # 若uncooked资产，应读取EditorOnly字段
        # 暂不读取，假设cooked资产跳过这些字段

        # BitField处理（如果存在）
        # 对于UE5.7 editor保存的资产，可能存在BitField
        # 尝试读取，若有解析错误则跳过
        try:
            # 18. BitField (uint32) [L1902-1942]
            bitfield = archive.read_u32()
            hidden = bool(bitfield & (1 << 0))
            not_connectable = bool(bitfield & (1 << 1))
            advanced_view = bool(bitfield & (1 << 4))
            orphaned_pin = bool(bitfield & (1 << 5))
        except:
            # cooked资产可能没有BitField，保持默认值
            pass

        return UEdGraphPin(
            pin_id=pin_id,
            pin_name=pin_name,
            pin_tooltip=pin_tooltip,
            direction=direction,
            pin_type=pin_type,
            default_value=default_value,
            auto_default_value=autogenerated_default_value,
            default_object=default_object_index,
            default_text_value=default_text_value,
            linked_to_raw=linked_to,
            sub_pins=sub_pins,
            parent_pin=parent_pin,
            hidden=hidden,
            not_connectable=not_connectable,
            advanced_view=advanced_view,
            orphaned_pin=orphaned_pin,
            owning_node_index=owning_node_index,
            source_index=source_index,
            persistent_guid=persistent_guid
        )
    ```

    关键变更：
    1. 添加 OwningNode 作为第一个读取字段（解决位置错位根因）
    2. 添加 PinToolTip 读取（PIN-01需求）
    3. 添加 DefaultObject/DefaultTextValue 读取（PIN-03需求）
    4. LinkedTo/SubPins/ParentPin 使用 read_pin_array/read_pin_reference（PIN-04需求）
    5. 添加 BitField uint32 读取和位解析（PIN-05需求）
    6. 版本条件检查（SourceIndex等）
  </action>
  <verify>
    <automated>grep -c "owning_node_index.*read_i32" uasset_read.py >= 1 && grep -c "pin_tooltip.*read_fstring" uasset_read.py >= 1 && grep -c "bitfield.*read_u32" uasset_read.py >= 1</automated>
  </verify>
  <done>
    - OwningNode作为第一个字段读取
    - PinToolTip字段解析
    - DefaultObject字段解析
    - LinkedTo使用read_pin_array解析
    - BitField读取uint32并解析位
    - 版本条件检查存在
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 二进制输入 | Pin序列化数据来自.uasset |
| 版本检查 | CustomVersion值决定字段格式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | OwningNode FPackageIndex | accept | 索引值由UE资产决定，解析失败返回空字符串 |
| T-18-06 | Denial of Service | LinkedTo数组 | mitigate | MAX_LINKEDTO_PER_PIN限制 |
| T-18-07 | Tampering | BitField值 | accept | 值超出预期时保持默认false |
</threat_model>

<verification>
- grep验证：OwningNode读取存在
- grep验证：完整序列化顺序
- grep验证：版本条件检查
</verification>

<success_criteria>
- read_ue_graph_pin()从OwningNode开始读取（解决位置错位）
- 所有需求字段正确解析（PIN-01~05）
- LinkedTo使用SerializePin格式（OwningNode+PinGuid）
- BitField读取uint32而非u8
- 版本条件检查正确
</success_criteria>

<output>
完成后创建 `.planning/phases/18-pin-serialization\18-03-SUMMARY.md`
</output>