---
phase: 68
plan: 03
type: execute
wave: 3
depends_on: [68-01, 68-02]
files_modified:
  - src/uasset_read/graph/flow_builder.py
  - src/uasset_read/n2c/node_types.py
  - tests/n2c/test_type_registry.py
  - tests/n2c/test_inheritance_chain.py
autonomous: true
requirements:
  - REGISTRY-01
  - REGISTRY-02
must_haves:
  truths:
    - "flow_builder._resolve_node_type() 使用 N2CNodeTypeRegistry 替代硬编码 dict"
    - "node_types.py 不再包含"临时"相关注释"
    - "所有现有测试通过（不破坏 Phase 69 已通过的 1200 tests）"
    - "继承链 3 层深度节点正确解析"
    - "未注册类型返回 Unknown 且不阻塞解析"
  artifacts:
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "更新后的 _resolve_node_type() 函数"
      contains: "N2CNodeTypeRegistry.get_instance().resolve()"
    - path: "src/uasset_read/n2c/node_types.py"
      provides: "126 种枚举（无临时注释）"
    - path: "tests/n2c/test_inheritance_chain.py"
      provides: "继承链集成测试"
      exports: ["TestInheritanceChain"]
  key_links:
    - from: "flow_builder._resolve_node_type()"
      to: "N2CNodeTypeRegistry.resolve()"
      via: "registry.resolve(class_name)"
      pattern: "N2CNodeTypeRegistry\\.get_instance\\(\\)\\.resolve"
    - from: "tests/n2c/test_inheritance_chain.py"
      to: "src/uasset_read/n2c/type_data.py"
      via: "验证继承链深度"
      pattern: "get_parent_chain"
---

<objective>
Wave 3: 将 flow_builder.py 的 _resolve_node_type() 替换为 N2CNodeTypeRegistry，清理临时代码，编写集成测试验证继承链正确性。

Purpose: 完成 Phase 68 目标 — 用注册表全面替代临时映射，确保 126 种类型全覆盖。
Output: 更新的 flow_builder.py + 清理后的 node_types.py + 集成测试。
</objective>

<execution_context>
@E:\Develop\uasset_read\.claude\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md
@E:\Develop\uasset_read\.planning\phases\phase-68\68-RESEARCH.md
@E:\Develop\uasset_read\src\uasset_read\graph\flow_builder.py
</context>

<interfaces>
From flow_builder.py (L85-104, to be replaced):
```python
def _resolve_node_type(class_name: str) -> N2CNodeType:
    """临时 class_name -> N2CNodeType 映射。Phase 68 N2CNodeTypeRegistry 就绪后替换。"""
    _TYPE_MAP = {
        "K2Node_CallFunction": N2CNodeType.CallFunction,
        "K2Node_Event": N2CNodeType.Event,
        "K2Node_FunctionEntry": N2CNodeType.FunctionEntry,
        "K2Node_FunctionResult": N2CNodeType.FunctionResult,
        "K2Node_IfThenElse": N2CNodeType.Branch,
        "K2Node_ExecutionSequence": N2CNodeType.Sequence,
        "K2Node_SwitchInteger": N2CNodeType.SwitchInt,
        "K2Node_SwitchString": N2CNodeType.SwitchString,
        "K2Node_SwitchEnum": N2CNodeType.SwitchEnum,
        "K2Node_DynamicCast": N2CNodeType.DynamicCast,
        "K2Node_VariableGet": N2CNodeType.VariableGet,
        "K2Node_VariableSet": N2CNodeType.VariableSet,
        "K2Node_CustomEvent": N2CNodeType.CustomEvent,
        "K2Node_EnhancedInputAction": N2CNodeType.Event,
        "K2Node_ClassDynamicCast": N2CNodeType.ClassDynamicCast,
    }
    return _TYPE_MAP.get(class_name, N2CNodeType.Unknown)
```

调用位置（flow_builder.py）：
- L176: `node_type = _resolve_node_type(node.class_name)` 在 format_node_dict() 中
- L550: `node_type = _resolve_node_type(current_node.class_name)` 在 _trace_execution_from_event() 中

替换后：
```python
def _resolve_node_type(class_name: str) -> N2CNodeType:
    """使用 N2CNodeTypeRegistry 解析节点类型（Phase 68）。"""
    return N2CNodeTypeRegistry.get_instance().resolve(class_name)
```

需要添加的导入：
```python
from uasset_read.n2c.type_registry import N2CNodeTypeRegistry
```

注意：flow_builder.py 已有 `_ensure_registry()` 初始化 Processor Registry（L27-32），TypeRegistry 初始化通过 `_ensure_initialized()` 自动完成，无需额外初始化代码。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 替换 flow_builder._resolve_node_type() 硬编码 dict 为注册表调用</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <action>修改 `src/uasset_read/graph/flow_builder.py`，替换 _resolve_node_type() 函数。

步骤：
1. 添加导入：在已有 n2c 导入块中增加 `from uasset_read.n2c.type_registry import N2CNodeTypeRegistry`
2. 重写 _resolve_node_type()（L85-104）：
   - 删除整个 _TYPE_MAP dict（15 个硬编码映射）
   - 替换为单行：`return N2CNodeTypeRegistry.get_instance().resolve(class_name)`
   - 更新 docstring 为"使用 N2CNodeTypeRegistry 解析节点类型（Phase 68）"
3. 确保 _ensure_registry() 在调用前已初始化 Processor Registry（已有，无需修改）

注意：
- 不修改 _ensure_registry()、format_node_dict()、_trace_execution_from_event() 等其他函数
- 仅替换 _resolve_node_type() 函数体
- 保留函数签名不变（参数 class_name: str，返回 N2CNodeType）</action>
  <verify>
    <automated>python -c "from uasset_read.graph.flow_builder import _resolve_node_type; r = _resolve_node_type('K2Node_CallFunction'); print(f'Resolved: {r}'); assert r.value == 'CallFunction', f'Expected CallFunction, got {r.value}'"</automated>
  </verify>
  <done>_resolve_node_type() 使用注册表，精确匹配 K2Node_CallFunction 返回 CallFunction</done>
</task>

<task type="auto">
  <name>Task 2: 清理 node_types.py 临时注释 + 编写继承链集成测试</name>
  <files>src/uasset_read/n2c/node_types.py, tests/n2c/test_inheritance_chain.py</files>
  <action>两项清理 + 测试创建：

**2a. 清理 node_types.py**：
- 确认文件顶部无"临时"、"Phase 68 就绪后替换"等注释
- 当前文件顶部注释应改为：`"""N2C 节点语义类型枚举（126 种）。"""`

**2b. 创建 tests/n2c/test_inheritance_chain.py**，继承链集成测试：

测试用例：
```python
"""继承链集成测试 — 验证 3 层深度节点正确解析。"""
import pytest
from uasset_read.n2c.type_registry import N2CNodeTypeRegistry
from uasset_read.n2c.node_types import N2CNodeType
from uasset_read.n2c.type_data import K2NODE_INHERITANCE, get_parent_chain


class TestInheritanceChain:
    """继承链集成测试。"""

    def test_call_array_function_resolves_to_call_function_via_inheritance(self):
        """K2Node_CallArrayFunction 有独立枚举值时精确匹配。"""
        registry = N2CNodeTypeRegistry.get_instance()
        result = registry.resolve("K2Node_CallArrayFunction")
        # 如果枚举有 CallArrayFunction → 精确匹配
        # 如果没有 → 继承回退到 CallFunction
        assert result in [N2CNodeType.CallArrayFunction, N2CNodeType.CallFunction]

    def test_three_level_inheritance(self):
        """3 层继承链回退测试。
        
        示例：K2Node_StructMemberSet → K2Node_StructOperation → K2Node_Variable
        应正确回退到最近有处理器的祖先类型。
        """
        registry = N2CNodeTypeRegistry.get_instance()
        chain = get_parent_chain("K2Node_StructMemberSet")
        assert len(chain) >= 3, f"Expected 3+ levels, got {len(chain)}: {chain}"

    def test_inheritance_map_no_cycles(self):
        """继承关系映射无循环。"""
        for class_name in K2NODE_INHERITANCE:
            chain = get_parent_chain(class_name)
            assert len(chain) <= 10, f"Chain too long for {class_name}: {chain}"
            # 无重复
            assert len(chain) == len(set(chain)), f"Cycle detected for {class_name}"

    def test_all_types_have_valid_inheritance(self):
        """所有类型在继承链中最终能到达 K2Node 或根类型。"""
        from uasset_read.n2c.type_data import K2NODE_TYPES
        root_types = {"K2Node", "UK2Node"}
        for class_name in K2NODE_TYPES:
            if class_name not in K2NODE_INHERITANCE:
                continue  # 根类型无父类
            chain = get_parent_chain(class_name)
            assert any(t in str(chain[-1]) for t in root_types), \
                f"{class_name} chain does not reach root: {chain}"


class TestUnknownFallback:
    """Unknown fallback 测试。"""

    def test_unregistered_type_returns_unknown(self):
        """未注册类型返回 Unknown。"""
        registry = N2CNodeTypeRegistry.get_instance()
        result = registry.resolve("K2Node_CustomPluginNode")
        assert result == N2CNodeType.Unknown

    def test_unknown_is_cached(self):
        """Unknown 结果被缓存。"""
        registry = N2CNodeTypeRegistry.get_instance()
        registry.resolve("K2Node_UnknownTest1")
        assert "K2Node_UnknownTest1" in registry._resolve_cache

    def test_empty_string_returns_unknown(self):
        """空字符串返回 Unknown。"""
        registry = N2CNodeTypeRegistry.get_instance()
        result = registry.resolve("")
        assert result == N2CNodeType.Unknown

    def test_none_like_string(self):
        """类似 None 的字符串返回 Unknown。"""
        registry = N2CNodeTypeRegistry.get_instance()
        result = registry.resolve("None")
        assert result == N2CNodeType.Unknown
```</action>
  <verify>
    <automated>python -m pytest tests/n2c/test_inheritance_chain.py -x -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>继承链集成测试通过（7 个测试），Unknown fallback 测试通过（4 个测试），node_types.py 临时注释已清理</done>
</task>

<task type="auto">
  <name>Task 3: 全量回归测试 — 验证所有现有测试通过</name>
  <files>tests/n2c/test_type_registry.py</files>
  <action>运行全量测试套件，验证 Phase 68 改动不破坏现有功能。

步骤：
1. 运行 n2c 模块测试：`python -m pytest tests/n2c/ -v --tb=short`
   - 包含 Phase 69 已有的 10 个测试文件
   - 包含 Wave 2 新增的 test_type_registry.py
   - 包含 Wave 3 新增的 test_inheritance_chain.py

2. 运行 flow_builder 相关测试：`python -m pytest tests/ -k "flow" -v --tb=short`
   - 验证 _resolve_node_type() 替换后 flow_builder 行为不变
   - 检查 test_flow_builder_integration.py

3. 运行全量测试（关键）：`python -m pytest tests/ -v --tb=short -x`
   - 不应有失败
   - Phase 69 已有 1200 tests passed，此处应保持 0 failures

4. 如果任何测试失败：
   - 分析是否因注册表替换导致
   - 检查 _resolve_node_type() 替换后是否有类型映射差异
   - 修复后重新运行全量测试</action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short -x 2>&1 | tail -5</automated>
  </verify>
  <done>全量测试通过，0 failures，Phase 68 改动未破坏现有功能</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow_builder → TypeRegistry | 替换硬编码 dict 为注册表调用，行为应等价 |
| 现有测试 → 新注册表 | 测试不应感知实现变化（透明替换） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-07 | Tampering | 替换后行为不一致 | mitigate | 全量回归测试覆盖，1200+ 测试验证等价性 |
| T-68-08 | Availability | 注册表未初始化导致首次 resolve 失败 | mitigate | _ensure_initialized() 自动填充，无需手动 init |
| T-68-09 | Information Disclosure | 日志记录未知类型名 | accept | 仅记录类型名，无敏感数据 |
| T-68-SC | Tampering | pip installs | mitigate | 无外部依赖，slopcheck 不适用 |
</threat_model>

<verification>
- flow_builder._resolve_node_type() 使用 N2CNodeTypeRegistry.resolve()
- node_types.py 无"临时"注释
- 继承链集成测试 7 个通过
- Unknown fallback 测试 4 个通过
- 全量测试通过，0 failures
- 全量测试数 >= 1200（Phase 69 基准）
</verification>

<success_criteria>
- _resolve_node_type() 不再包含硬编码 _TYPE_MAP dict
- node_types.py 顶部注释描述正式枚举（126 种）
- 继承链 3 层深度节点正确解析
- 未注册类型返回 Unknown，不阻塞解析管线
- 全量测试通过，不破坏 Phase 69 已有功能
- 注册表已集成到主管线（flow_builder → Processor Registry → N2CNodeTypeRegistry）
</success_criteria>

<output>
Create `.planning/phases/phase-68/68-03-SUMMARY.md` when done
</output>
