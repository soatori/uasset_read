---
phase: 05-optimization-security
plan: 03
type: execute
wave: 3
depends_on: ["05-02B"]
files_modified: ["uasset_read.py", "tests/test_loop_limits.py"]
autonomous: true
requirements: ["SAFE-05"]
user_setup: []
must_haves:
  truths:
    - "Property loop limited to MAX_PROPERTY_COUNT = 10000"
    - "Name table limited to MAX_NAME_COUNT = 10000000"
    - "Import/Export tables limited to 1000000 entries"
    - "Parser never hangs on infinite loops"
  artifacts:
    - path: "uasset_read.py"
      provides: "Loop count limits"
      contains: "MAX_PROPERTY_COUNT, loop counter checks"
    - path: "tests/test_loop_limits.py"
      provides: "SAFE-05 test coverage"
  key_links:
    - from: "parse_properties_from_export()"
      to: "MAX_PROPERTY_COUNT"
      via: "D-08/D-09: 循环计数限制"
      pattern: "if property_count >= MAX_PROPERTY_COUNT: raise ParseError"
---

# Phase 5 Plan 03: 循环计数限制

## Objective

Implement loop count limits to prevent parser hanging per D-08, D-09.

## Tasks

<task type="auto" tdd="true">
  <name>Create Wave 3 test scaffolding</name>
  <files>tests/test_loop_limits.py</files>
  <action>
Create tests/test_loop_limits.py with test stubs:
- test_property_loop_limit
- test_name_table_limit
- test_import_export_limit
  </action>
  <verify>
    <automated>python -m pytest tests/test_loop_limits.py --collect-only -q</automated>
  </verify>
  <done>
    - tests/test_loop_limits.py exists
    - Test stubs ready
  </done>
</task>

<task type="auto">
  <name>Add property loop counter to parse_properties_from_export()</name>
  <files>uasset_read.py</files>
  <read_first>uasset_read.py:parse_properties_from_export() lines 1535-1606</read_first>
  <action>
Add loop counter check per D-08:

```python
property_count = 0
while True:
    if property_count >= MAX_PROPERTY_COUNT:
        raise ParseError(f"Property count exceeds {MAX_PROPERTY_COUNT}")
    property_count += 1
    # ... rest of loop
```
  </action>
  <verify>
    <automated>python -c "from uasset_read import MAX_PROPERTY_COUNT; assert MAX_PROPERTY_COUNT == 10000"</automated>
  </verify>
  <done>
    - Property loop has counter
    - Exceeds limit raises ParseError
  </done>
</task>

<task type="auto">
  <name>Verify existing table limits</name>
  <files>uasset_read.py</files>
  <read_first>uasset_read.py:read_package_summary() lines 583, 620, 628</read_first>
  <action>
Verify MAX_NAME_COUNT, MAX_IMPORT_COUNT, MAX_EXPORT_COUNT checks already implemented per D-09.
  </action>
  <verify>
    <automated>python -c "from uasset_read import MAX_NAME_COUNT, MAX_IMPORT_COUNT, MAX_EXPORT_COUNT; assert MAX_NAME_COUNT == 10000000; assert MAX_IMPORT_COUNT == 1000000"</automated>
  </verify>
  <done>
    - All table limits verified
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Loop count limits implemented</what-built>
  <how-to-verify>
    1. Run tests: python -m pytest tests/ -v --tb=short -x
    2. Verify constants: python -c "from uasset_read import MAX_PROPERTY_COUNT, MAX_NAME_COUNT; print('OK')"
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 4</resume-signal>
</task>

## Success Criteria

- SAFE-05 fully implemented
- Parser never hangs
- All loops have limits
