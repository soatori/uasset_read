---
phase: 69-n2c-processor
plan: 04
type: execute
wave: 4
depends_on: ["69-03"]
files_modified:
  - tests/n2c/test_full_regression.py
autonomous: true
requirements:
  - PROCESSOR-01
  - PROCESSOR-02
  - PROCESSOR-03

must_haves:
  truths:
    - "全部 1271 个现有测试通过（零回归）"
    - "所有 n2c 测试通过（30+ 个新测试）"
    - "端到端解析真实 .uasset 文件结果不变"
  artifacts:
    - path: "tests/n2c/test_full_regression.py"
      provides: "回归测试验证套件"
      exports: []
  key_links:
    - from: "tests/n2c/test_full_regression.py"
      to: "src/uasset_read/graph/flow_builder.py"
      via: "import build_execution_flows"
      pattern: "from uasset_read.graph.flow_builder import"
---

<objective>
执行完整回归验证，确保 Phase 69 重构未引入任何功能退化。

Purpose: 用自动化测试验证所有 must-have truths，确保 1271 个现有测试 + 新 n2c 测试全部通过。
Output: 回归测试报告 + 阶段完成确认。
</objective>

<execution_context>
@E:\Develop\uasset_read\.claude\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\phases\phase-69\RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行完整测试套件验证零回归</name>
  <files>tests/n2c/test_full_regression.py</files>
  <action>创建回归测试文件并执行验证：

**test_full_regression.py:**
- `test_all_existing_tests_pass` — 运行完整 pytest 套件，断言零失败
- `test_n2c_module_imports` — 验证所有 n2c 模块可导入
- `test_processor_registry_initialized` — 验证 register_all_processors() 可用

**执行验证命令（按顺序）：**

1. `python -m pytest tests/n2c/ -v` — n2c 测试套件全部通过
2. `python -m pytest tests/ -x -q --ignore=tests/n2c/` — 前 100 个现有测试无回归
3. `python -m pytest tests/ -q` — 完整测试套件统计

**如果任何测试失败：**
- 记录失败的测试名和错误信息
- 分析是否因 Processor 重构导致
- 如果是回归：修复 flow_builder.py 或 compat.py
- 如果是新测试 bug：修复 n2c 测试

**验证 n2c 模块完整性：**
```bash
python -c "
from uasset_read.n2c import N2CNodeDefinition
from uasset_read.n2c.processor_base import N2CNodeProcessor
from uasset_read.n2c.processor_registry import N2CProcessorRegistry
from uasset_read.n2c.processors import register_all_processors
from uasset_read.n2c.compat import definition_to_node_dict
print('All n2c modules imported successfully')
"
```</action>
  <verify>
    <automated>python -m pytest tests/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>完整测试套件通过，1271 tests collected，零失败</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工确认架构完整性</name>
  <what-built>
N2C 节点处理器架构完整实现：
1. N2CNodeDefinition 数据类（69-01）
2. N2CNodeProcessor 抽象基类（69-01）
3. N2CProcessorRegistry 单例注册中心（69-01）
4. 5 个核心 Processor + Fallback（69-02）
5. flow_builder.py 迁移使用 Processor 分发（69-03）
6. 向后兼容层 n2c/compat.py（69-03）
7. 完整测试套件（69-01 + 69-02 + 69-03 + 69-04）
  </what-built>
  <how-to-verify>
1. 确认 `src/uasset_read/n2c/` 目录结构符合预期：
   ```
   n2c/
   ├── __init__.py
   ├── definitions.py          # N2CNodeDefinition
   ├── processor_base.py       # N2CNodeProcessor ABC
   ├── processor_registry.py   # N2CProcessorRegistry
   ├── processors/
   |   ├── __init__.py          # register_all_processors()
   |   ├── call_function.py
   |   ├── event.py
   |   ├── function_entry.py
   |   ├── flow_control.py
   |   ├── variable.py
   |   ├── cast.py
   |   └── fallback.py
   └── compat.py               # definition_to_node_dict()
   ```

2. 确认 `tests/n2c/` 目录有足够测试覆盖

3. 确认所有测试通过（`python -m pytest tests/ -q`）

4. 确认现有 JSON 输出格式不变
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → Production | 测试通过不代表生产无问题（需真实 uasset 文件验证） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-11 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `python -m pytest tests/ -q` — 全部测试通过（1271 + 新增 n2c 测试）
- `python -m pytest tests/n2c/ -v` — 所有 n2c 测试通过
- n2c 模块导入无错误
- Phase 69 的 4 个 PLAN.md 文件存在
</verification>

<success_criteria>
- 1271 个现有测试零回归
- 30+ 个新 n2c 测试全部通过
- n2c 模块可完整导入
- 人工确认架构完整性
- Phase 69 的 must-have truths 全部满足
</success_criteria>

<output>
Create `.planning/phases/phase-69/69-04-SUMMARY.md` when done
</output>
