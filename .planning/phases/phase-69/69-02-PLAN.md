---
phase: 69-n2c-processor
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - src/uasset_read/n2c/processors/__init__.py
  - src/uasset_read/n2c/processors/call_function.py
  - src/uasset_read/n2c/processors/event.py
  - src/uasset_read/n2c/processors/function_entry.py
  - src/uasset_read/n2c/processors/flow_control.py
  - src/uasset_read/n2c/processors/variable.py
  - src/uasset_read/n2c/processors/cast.py
  - src/uasset_read/n2c/processors/fallback.py
  - tests/n2c/test_call_function_processor.py
  - tests/n2c/test_event_processor.py
  - tests/n2c/test_flow_control_processor.py
autonomous: true
requirements:
  - PROCESSOR-02

must_haves:
  truths:
    - "CallFunctionProcessor 正确提取 member_name/member_parent/pure 到 extra_data"
    - "EventProcessor 正确提取 event_name 到 extra_data"
    - "FlowControlProcessor 正确识别分支类型到 extra_data"
    - "FallbackProcessor 对未知类型记录 warning 但不抛异常"
    - "所有 Processor 处理 None node_data 不崩溃"
  artifacts:
    - path: "src/uasset_read/n2c/processors/call_function.py"
      provides: "CallFunctionProcessor"
      exports: ["CallFunctionProcessor"]
    - path: "src/uasset_read/n2c/processors/event.py"
      provides: "EventProcessor"
      exports: ["EventProcessor"]
    - path: "src/uasset_read/n2c/processors/flow_control.py"
      provides: "FlowControlProcessor"
      exports: ["FlowControlProcessor"]
    - path: "src/uasset_read/n2c/processors/fallback.py"
      provides: "FallbackProcessor"
      exports: ["FallbackProcessor"]
    - path: "src/uasset_read/n2c/processors/__init__.py"
      provides: "批量注册所有处理器"
      exports: ["register_all_processors"]
  key_links:
    - from: "src/uasset_read/n2c/processors/__init__.py"
      to: "src/uasset_read/n2c/processor_registry.py"
      via: "import + register"
      pattern: "N2CProcessorRegistry.get_instance().register"
    - from: "src/uasset_read/n2c/processors/call_function.py"
      to: "src/uasset_read/n2c/processor_base.py"
      via: "import N2CNodeProcessor"
      pattern: "from.*processor_base import N2CNodeProcessor"
---

<objective>
实现 5 个核心 Processor 类 + Fallback 处理器，注册所有处理器到注册表。

Purpose: 覆盖 flow_builder.py 中最频繁出现的 5 种节点类型（CallFunction, Event, FunctionEntry, FlowControl, Variable），以及未知类型的 Fallback 处理。
Output: `n2c/processors/` 目录下的 6 个处理器文件 + 注册入口 + 测试。
</objective>

<execution_context>
@E:\Develop\uasset_read\.claude\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\phases\phase-69\RESEARCH.md
</context>

<interfaces>
<!-- Phase 68 dependency: N2CNodeType enum (assumed existing) -->
```python
# From Phase 68 — N2CNodeType enum (Phase 69 consumes, does not implement)
class N2CNodeType(Enum):
    CallFunction = "CallFunction"
    Event = "Event"
    CustomEvent = "CustomEvent"
    FunctionEntry = "FunctionEntry"
    FunctionResult = "FunctionResult"
    VariableGet = "VariableGet"
    VariableSet = "VariableSet"
    Branch = "Branch"          # K2Node_IfThenElse
    Sequence = "Sequence"      # K2Node_ExecutionSequence
    SwitchInt = "SwitchInt"    # K2Node_SwitchInteger
    DynamicCast = "DynamicCast"
    # ... 100+ more
```

<!-- From Phase 68 — N2CNodeTypeRegistry (assumed existing) -->
```python
class N2CNodeTypeRegistry:
    def resolve(self, class_name: str) -> N2CNodeType:
        """UE class_name -> N2CNodeType"""
```

<!-- From 69-01 — N2CNodeDefinition -->
```python
@dataclass
class N2CNodeDefinition:
    node_id: str
    node_type: N2CNodeType
    position: Tuple[int, int]
    comment: str = ""
    input_pins: List[Dict] = field(default_factory=list)
    output_pins: List[Dict] = field(default_factory=list)
    extra_data: Dict[str, Any] = field(default_factory=dict)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现 CallFunctionProcessor + EventProcessor</name>
  <files>src/uasset_read/n2c/processors/__init__.py, src/uasset_read/n2c/processors/call_function.py, src/uasset_read/n2c/processors/event.py</files>
  <action>创建 `n2c/processors/` 目录和 `__init__.py`。

**CallFunctionProcessor** (`call_function.py`):
- 继承 N2CNodeProcessor，`node_types = [N2CNodeType.CallFunction]`
- `process()` 方法：
  1. 从 `node.node_data` 提取 `function_reference`（兼容 dict 和 dataclass 两种模式）
  2. 写入 `extra_data["member_name"]`, `extra_data["member_parent"]`, `extra_data["b_self_context"]`
  3. 检测 pure 标志：遍历 `node.pins`，检查是否有 `pin_category == "exec"` 的 pin
  4. 写入 `extra_data["pure"]`
- 处理 `node_data` 为 None 的情况：跳过 reference 提取，但仍检测 pure

**EventProcessor** (`event.py`):
- 继承 N2CNodeProcessor，`node_types = [N2CNodeType.Event, N2CNodeType.CustomEvent]`
- `process()` 方法：
  1. 从 `node.node_data` 提取 `event_reference`
  2. 写入 `extra_data["event_name"]`, `extra_data["event_parent"]`
- 处理 `node_data` 为 None 的情况

`__init__.py` 定义 `register_all_processors()` 函数，导入所有 Processor 类并注册到单例。

注意：所有文件使用 `TYPE_CHECKING` 块导入 `N2CNodeType` 和 `UEdGraphNode`。</action>
  <verify>
    <automated>python -m pytest tests/n2c/test_call_function_processor.py tests/n2c/test_event_processor.py -x -q</automated>
  </verify>
  <done>CallFunctionProcessor 和 EventProcessor 测试通过，覆盖正常/None/边界场景</done>
</task>

<task type="auto">
  <name>Task 2: 实现 FlowControlProcessor + VariableProcessor + CastProcessor</name>
  <files>src/uasset_read/n2c/processors/flow_control.py, src/uasset_read/n2c/processors/variable.py, src/uasset_read/n2c/processors/cast.py</files>
  <action>**FlowControlProcessor** (`flow_control.py`):
- 继承 N2CNodeProcessor，`node_types = [N2CNodeType.Branch, N2CNodeType.Sequence, N2CNodeType.SwitchInt]`
- `process()` 方法：
  1. 从 `constants.BRANCH_TYPE_MAP` 查找 `node.class_name` 获取分支类型
  2. 写入 `extra_data["branch_type"]`
  3. 写入 `extra_data["stops_execution"] = True`（控制流节点阻断执行流追踪）
- 注意：BRANCH_TYPE_MAP 使用 class_name 字符串，此处是临时的（Phase 68 完善后改用 NodeType 枚举）

**VariableProcessor** (`variable.py`):
- 继承 N2CNodeProcessor，`node_types = [N2CNodeType.VariableGet, N2CNodeType.VariableSet]`
- `process()` 方法：
  1. 从 `node.node_data` 提取变量相关信息
  2. 写入 `extra_data["variable_name"]`
  3. 写入 `extra_data["direction"]`（"get" 或 "set"，由 node_type 推断）

**CastProcessor** (`cast.py`):
- 继承 N2CNodeProcessor，`node_types = [N2CNodeType.DynamicCast]`
- `process()` 方法：
  1. 从 `node.node_data` 提取 cast 目标类型
  2. 写入 `extra_data["target_type"]`
  3. 写入 `extra_data["b_is_safe_cast"]`

每个处理器都需处理 `node_data` 为 None 的情况，不抛异常。</action>
  <verify>
    <automated>python -m pytest tests/n2c/test_flow_control_processor.py -x -q</automated>
  </verify>
  <done>3 个处理器测试通过，每个覆盖正常/None 场景</done>
</task>

<task type="auto">
  <name>Task 3: 实现 FallbackProcessor + 注册入口</name>
  <files>src/uasset_read/n2c/processors/fallback.py, src/uasset_read/n2c/processors/__init__.py</files>
  <action>**FallbackProcessor** (`fallback.py`):
- 继承 N2CNodeProcessor，`node_types = []`（空列表，不主动注册任何类型）
- `process()` 方法：
  1. 记录 warning：`f"Fallback processing for unknown type: {node.class_name}"`
  2. 在 `extra_data["fallback"] = True`
  3. 在 `extra_data["original_class_name"] = node.class_name`
- 手动通过 `set_fallback()` 注册，不通过 `register()`

**注册入口** (`__init__.py`):
```python
def register_all_processors():
    """批量注册所有处理器到全局注册表。"""
    from uasset_read.n2c.processor_registry import N2CProcessorRegistry
    from uasset_read.n2c.processors.call_function import CallFunctionProcessor
    # ... 其他处理器

    registry = N2CProcessorRegistry.get_instance()
    for proc_cls in [CallFunctionProcessor, EventProcessor, FlowControlProcessor,
                     VariableProcessor, CastProcessor]:
        registry.register(proc_cls())

    # Fallback 手动注册
    registry.set_fallback(FallbackProcessor())
```

注意：`__init__.py` 中的导入需在函数内部执行（惰性导入），避免模块加载时的循环依赖。</action>
  <verify>
    <automated>python -c "from uasset_read.n2c.processors import register_all_processors; register_all_processors(); from uasset_read.n2c.processor_registry import N2CProcessorRegistry; r = N2CProcessorRegistry.get_instance(); print(f'Registered: {len(r._processors)} processors')"</automated>
  </verify>
  <done>register_all_processors() 成功注册 5+ 个处理器，Fallback 正确设置</done>
</task>

<task type="auto">
  <name>Task 4: 编写处理器单元测试</name>
  <files>tests/n2c/test_call_function_processor.py, tests/n2c/test_event_processor.py, tests/n2c/test_flow_control_processor.py</files>
  <action>为每个处理器编写测试文件（复用 conftest.py fixtures）：

**test_call_function_processor.py:**
- `test_extracts_function_reference` — 正常 dict node_data 提取 member_name
- `test_extracts_function_reference_dataclass` — dataclass node_data 提取
- `test_detects_pure_function` — 无 exec pin 时 pure=True
- `test_detects_non_pure` — 有 exec pin 时 pure=False 或不设置
- `test_handles_none_node_data` — node_data=None 不崩溃
- `test_handles_missing_function_reference` — node_data 存在但无 function_reference

**test_event_processor.py:**
- `test_extracts_event_reference` — 正常事件名提取
- `test_handles_none_node_data` — 不崩溃
- `test_handles_custom_event_type` — CustomEvent 类型也正确处理

**test_flow_control_processor.py:**
- `test_sets_branch_type_if_then_else` — K2Node_IfThenElse -> "if_then_else"
- `test_sets_branch_type_switch` — K2Node_SwitchInteger -> "switch_integer"
- `test_sets_stops_execution` — 总是 True

每个测试文件导入 `register_all_processors()` 并在 setup 中注册。使用 `reset_registry` fixture 确保隔离。</action>
  <verify>
    <automated>python -m pytest tests/n2c/test_call_function_processor.py tests/n2c/test_event_processor.py tests/n2c/test_flow_control_processor.py -x -q</automated>
  </verify>
  <done>所有处理器测试通过（预计 12+ 个测试）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node_data → Processor | node_data 可能为 dict/dataclass/None，Processor 必须安全处理 |
| Registry → Fallback | 未知类型调用 Fallback，不应静默失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05 | Availability | All Processors.process() | mitigate | 每个处理器防御性检查 node_data 为 None，不抛 AttributeError |
| T-69-06 | Tampering | BRANCH_TYPE_MAP lookup | mitigate | 未知 class_name 使用 "unknown" 默认值，不抛 KeyError |
| T-69-07 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `python -m pytest tests/n2c/ -x -q` — 所有 n2c 测试通过（15+ 基础 + 12+ 处理器 = 27+ 总计）
- `python -c "from uasset_read.n2c.processors import register_all_processors; register_all_processors()"` — 无错误
- 现有测试不受影响：`python -m pytest tests/test_blueprint_extraction.py -x -q` 通过
</verification>

<success_criteria>
- 5 个核心 Processor 类实现并注册（CallFunction, Event, FlowControl, Variable, Cast）
- FallbackProcessor 作为默认回退
- `register_all_processors()` 批量注册入口可用
- 12+ 处理器单元测试通过
- 所有 Processor 对 None node_data 不崩溃
- CallFunctionProcessor 正确提取 function_reference 和 pure 标志
- 现有 1271 个测试不受影响
</success_criteria>

<output>
Create `.planning/phases/phase-69/69-02-SUMMARY.md` when done
</output>
