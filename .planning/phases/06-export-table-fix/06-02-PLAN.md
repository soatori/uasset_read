---
phase: 06-export-table-fix
plan: 02
type: execute
wave: 2
depends_on: [06-01-PLAN]
files_modified: [tests/test_uasset_read.py]
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]
user_setup: []

must_haves:
  truths:
    - "OuterIndex 值对比验证偏移错位修复效果"
    - "TemplateIndex 非零验证（UE4 >= 506文件）"
    - "bool flags 读取验证"
    - "错误上下文信息验证"
    - "Lyra 资产解析成功（修复前后对比）"
  artifacts:
    - path: "tests/test_uasset_read.py"
      provides: "导出表修复测试验证"
      contains: "test_template_index, test_outer_index_fix"
      min_lines: 100
  key_links:
    - from: "tests/test_uasset_read.py"
      to: "uasset_read.py::read_export_map"
      via: "测试调用"
      pattern: "parse_uasset(file_path)"
---

<objective>
验证导出表修复的正确性，确保 TemplateIndex、OuterIndex、bool flags 等字段正确读取。

**Purpose:** 测试验证是修复的关键环节。通过单元测试、UE 编辑器文件验证、Lyra 资产对比等多角度验证，确保修复正确性。

**Output:**
- 单元测试（TemplateIndex/OuterIndex/bool flags/PackageGuid）
- Lyra 资产对比测试（修复前后验证）
- 错误上下文测试（清晰错误信息验证）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-export-table-fix/06-CONTEXT.md

# Plan 01 实现结果
@.planning/phases/06-export-table-fix/06-01-PLAN.md

# 现有测试结构（参考）
@tests/test_uasset_read.py
</context>

<interfaces>
<!-- Key types and test helpers needed for testing -->

From tests/test_uasset_read.py:
```python
# Test patterns
def test_package_summary_valid():
    # Parse real file, verify structure
    
def test_error_context_export_fields():
    # Unit test for ErrorContext new fields
```

From uasset_read.py:
```python
def parse_uasset(path: str) -> ParseResult:
    # Main entry point
    
def read_export_map(archive, summary, name_map) -> List[ObjectExport]:
    # Target function to test
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 ErrorContext 和 ObjectExport 单元测试</name>
  <files>tests/test_uasset_read.py</files>
  <behavior>
    - Test 1: test_error_context_export_fields — ErrorContext 包含 export_index, expected_offset, actual_offset, field_name, version_info 字段
    - Test 2: test_object_export_new_fields — ObjectExport 包含 template_index, b_forced_export, package_flags, b_is_inherited_instance 字段
    - Test 3: test_error_context_initialization — ErrorContext 可初始化所有字段（包括新字段）
    - Test 4: test_object_export_initialization — ObjectExport 可初始化所有字段
  </behavior>
  <action>
**实现步骤：**

1. **定位测试文件**（tests/test_uasset_read.py）

2. **添加 ErrorContext 测试（D-12/D-13/D-14）：**
```python
def test_error_context_export_fields():
    """测试 ErrorContext 导出表特定字段（Phase 6 D-12/D-13/D-14）"""
    from uasset_read import ErrorContext
    
    # 测试新字段存在
    ctx = ErrorContext(
        offset=100,
        phase="export_map",
        operation="read_i32",
        context_name="TemplateIndex",
        export_index=5,
        expected_offset=1200,
        actual_offset=1204,
        field_name="TemplateIndex",
        version_info={"file_version_ue4": 506, "threshold": 506}
    )
    
    assert ctx.export_index == 5
    assert ctx.expected_offset == 1200
    assert ctx.actual_offset == 1204
    assert ctx.field_name == "TemplateIndex"
    assert ctx.version_info["file_version_ue4"] == 506

def test_error_context_backward_compatibility():
    """测试 ErrorContext 向后兼容（现有字段不变）"""
    from uasset_read import ErrorContext
    
    # 仅使用现有字段初始化（向后兼容）
    ctx = ErrorContext(
        offset=100,
        phase="header",
        operation="read_u32",
        context_name="FileVersion"
    )
    
    assert ctx.offset == 100
    assert ctx.phase == "header"
    assert ctx.export_index is None  # 新字段默认值
    assert ctx.field_name == ""      # 新字段默认值
```

3. **添加 ObjectExport 测试（D-16/D-17）：**
```python
def test_object_export_new_fields():
    """测试 ObjectExport 新字段（Phase 6 D-16/D-17）"""
    from uasset_read import ObjectExport, PackageIndex
    
    # 测试所有新字段
    export = ObjectExport(
        class_index=PackageIndex(1),
        super_index=PackageIndex(0),
        template_index=PackageIndex(2),  # 新字段
        outer_index=PackageIndex(-1),
        object_name="TestObject",
        object_flags=0,
        serial_size=100,
        serial_offset=500,
        b_forced_export=True,           # 新字段
        b_not_for_client=False,         # 新字段
        b_not_for_server=False,         # 新字段
        b_is_inherited_instance=True,   # 新字段（条件）
        package_flags=0x200,            # 新字段
    )
    
    assert export.template_index.index == 2
    assert export.b_forced_export == True
    assert export.b_not_for_client == False
    assert export.b_is_inherited_instance == True
    assert export.package_flags == 0x200

def test_object_export_backward_compatibility():
    """测试 ObjectExport 向后兼容（现有字段不变）"""
    from uasset_read import ObjectExport, PackageIndex
    
    # 仅使用现有字段初始化（向后兼容）
    export = ObjectExport(
        class_index=PackageIndex(1),
        super_index=PackageIndex(0),
        outer_index=PackageIndex(-1),
        object_name="TestObject",
        object_flags=0,
        serial_size=100,
        serial_offset=500
    )
    
    assert export.class_index.index == 1
    assert export.template_index.index == 0  # 默认值
    assert export.b_forced_export == False   # 默认值
    assert export.package_flags == 0         # 默认值
```

4. **运行测试验证：**
```bash
python -m pytest tests/test_uasset_read.py::test_error_context_export_fields -xvs
python -m pytest tests/test_uasset_read.py::test_object_export_new_fields -xvs
```

**避免：**
- 不要修改现有测试（向后兼容验证）
- 不要跳过向后兼容测试（确保无回归）

**原因：** 单元测试直接验证 dataclass 字段；向后兼容测试确保无回归。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py::test_error_context_export_fields -xvs</automated>
    <automated>python -m pytest tests/test_uasset_read.py::test_object_export_new_fields -xvs</automated>
  </verify>
  <done>ErrorContext 和 ObjectExport 单元测试通过，所有新字段验证成功</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 read_export_map 功能测试</name>
  <files>tests/test_uasset_read.py</files>
  <behavior>
    - Test 1: test_read_export_map_template_index — UE4 >= 506 文件 TemplateIndex 正确读取
    - Test 2: test_read_export_map_outer_index_fix — OuterIndex 值对比验证偏移错位修复
    - Test 3: test_read_export_map_bool_flags — bool flags 正确读取（各 1 byte）
    - Test 4: test_read_export_map_package_guid — PackageGuid 条件读取（UE5 < 1010）
    - Test 5: test_read_export_map_serialization_order — 严格按 ObjectResource.cpp 顺序
    - Test 6: test_read_export_map_error_context — 错误时返回详细 ErrorContext
  </behavior>
  <action>
**实现步骤：**

**前置条件：** Task 1 完成（dataclass 测试）

1. **定位测试文件**（tests/test_uasset_read.py）

2. **添加 TemplateIndex 测试（D-01/D-18/D-19）：**
```python
def test_read_export_map_template_index():
    """测试 TemplateIndex 字段读取（Phase 6 D-01/D-18/D-19）"""
    from uasset_read import parse_uasset
    
    # 使用 UE4 >= 506 文件（TemplateIndex 应存在）
    result = parse_uasset("LyraStarterGame/Source/BP_Character.uasset")
    
    assert result.success
    assert len(result.export_map) > 0
    
    # 验证 TemplateIndex 读取（UE4 >= 506）
    for export in result.export_map:
        # UE5 文件的 file_version_ue4 通常 >= 522，满足 >= 506 条件
        if result.summary.file_version_ue4 >= 506:
            # TemplateIndex 应有值（可能为 0，但字段存在）
            assert hasattr(export, 'template_index')
            assert isinstance(export.template_index.index, int)

def test_template_index_ue4_version_check():
    """测试 TemplateIndex 版本条件检查（D-01）"""
    from uasset_read import VER_UE4_TemplateIndex_IN_COOKED_EXPORTS
    
    # 版本常量验证
    assert VER_UE4_TemplateIndex_IN_COOKED_EXPORTS == 506
    
    # 条件检查逻辑验证（模拟）
    file_version_ue4 = 505  # 低于阈值
    should_read_template_index = file_version_ue4 >= 506
    assert should_read_template_index == False
    
    file_version_ue4 = 506  # 达到阈值
    should_read_template_index = file_version_ue4 >= 506
    assert should_read_template_index == True
```

3. **添加 OuterIndex 修复验证（D-18）：**
```python
def test_read_export_map_outer_index_fix():
    """测试 OuterIndex 值对比验证偏移错位修复（Phase 6 D-18）"""
    from uasset_read import parse_uasset
    import json
    
    # 修复前 OuterIndex 值（v1.0 偏移错位）
    # 修复后 OuterIndex 值（v2.0 正确）
    
    result = parse_uasset("LyraStarterGame/Source/BP_Character.uasset")
    assert result.success
    
    # 验证 OuterIndex 非零值（修复前错位导致值异常）
    for export in result.export_map:
        # OuterIndex 应为有效值（可能为 -1 或导出索引）
        assert hasattr(export, 'outer_index')
        
        # 验证 OuterIndex 合理范围
        outer_idx = export.outer_index.index
        # ImportMap 索引：负数（-1 到 -import_count）
        # ExportMap 紆引：正数（0 到 export_count-1）
        # 0：空引用
        assert outer_idx <= len(result.export_map) or outer_idx >= -len(result.import_map)
    
    # D-18: OuterIndex 值对比验证（修复前后 JSON 输出对比）
    # 手动对比：修复前 OuterIndex 值异常（偏移错位）
    # 修复后 OuterIndex 值正确（非零值有效）
```

4. **添加 bool flags 测试（D-07）：**
```python
def test_read_export_map_bool_flags():
    """测试 bool flags 字段读取（Phase 6 D-07）"""
    from uasset_read import parse_uasset
    
    result = parse_uasset("LyraStarterGame/Source/BP_Character.uasset")
    assert result.success
    
    # 验证 bool flags 读取（各 1 byte）
    for export in result.export_map:
        assert hasattr(export, 'b_forced_export')
        assert hasattr(export, 'b_not_for_client')
        assert hasattr(export, 'b_not_for_server')
        
        # bool flags 应为布尔值
        assert isinstance(export.b_forced_export, bool)
        assert isinstance(export.b_not_for_client, bool)
        assert isinstance(export.b_not_for_server, bool)
```

5. **添加错误上下文测试（D-12/D-13/D-14）：**
```python
def test_read_export_map_error_context():
    """测试导出表解析失败时错误上下文信息（Phase 6 D-12/D-13/D-14）"""
    from uasset_read import parse_uasset, ParseError, ErrorContext
    import struct
    
    # 创建损坏文件（模拟导出表解析失败）
    # 省略真实文件创建，使用合成测试
    
    # 模拟错误上下文验证
    try:
        # 尝试解析损坏文件（期望失败）
        result = parse_uasset("test/damaged_export.uasset")
        assert not result.success  # 应失败
        
        # 验证错误上下文信息
        if result.error and result.error.context:
            ctx = result.error.context
            assert ctx.phase == "export_map"
            assert ctx.export_index >= 0
            assert ctx.offset >= 0
            assert ctx.field_name != ""  # 包含字段名
            assert "file_version_ue4" in ctx.version_info  # 包含版本信息
            
    except ParseError as e:
        # 验证错误信息格式
        assert "导出表解析失败" in str(e)
        assert e.context is not None
        assert e.context.export_index >= 0  # 包含导出索引
        assert e.context.field_name != ""   # 包含字段名
```

6. **运行测试验证：**
```bash
python -m pytest tests/test_uasset_read.py::test_read_export_map_template_index -xvs
python -m pytest tests/test_uasset_read.py::test_read_export_map_outer_index_fix -xvs
python -m pytest tests/test_uasset_read.py::test_read_export_map_bool_flags -xvs
python -m pytest tests/test_uasset_read.py::test_read_export_map_error_context -xvs
```

**避免：**
- 不要使用真实损坏文件（合成测试更可控）
- 不要跳过版本条件测试（D-01 核心）

**原因：** 功能测试验证实际解析行为；错误上下文测试确保 BUG-03 修复。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py -k "test_read_export_map" -xvs</automated>
    <manual>对比 Lyra 资产修复前后 JSON 输出（OuterIndex 变化）</manual>
  </verify>
  <done>read_export_map 功能测试通过，TemplateIndex/OuterIndex/bool flags 验证成功</done>
</task>

<task type="auto">
  <name>Task 3: Lyra 资产对比验证（D-18/D-19/D-20/D-21）</name>
  <files>tests/test_uasset_read.py</files>
  <action>
**实现步骤：**

**前置条件：** Task 2 完成（功能测试）

1. **定位测试文件**（tests/test_uasset_read.py）

2. **添加 Lyra 资产对比测试（D-18/D-19/D-20/D-21）：**
```python
def test_lyra_asset_outer_index_comparison():
    """
    Lyra 资产对比验证（Phase 6 D-18/D-19/D-20/D-21）。
    
    对比修复前后 OuterIndex 值变化验证偏移错位修复。
    """
    from uasset_read import parse_uasset
    import json
    
    # 解析 Lyra 资产
    result = parse_uasset("LyraStarterGame/Source/BP_Character.uasset")
    assert result.success
    
    # 输出 JSON（用于对比）
    output = {
        "file_version_ue4": result.summary.file_version_ue4,
        "export_count": len(result.export_map),
        "exports": [
            {
                "class_index": e.class_index.index,
                "super_index": e.super_index.index,
                "template_index": e.template_index.index,  # 新字段
                "outer_index": e.outer_index.index,         # 修复字段
                "object_name": e.object_name,
                "b_forced_export": e.b_forced_export,       # 新字段
                "package_flags": e.package_flags            # 新字段
            }
            for e in result.export_map[:5]  # 前 5 个导出
        ]
    }
    
    # 保存 JSON（用于手动对比）
    with open("test/output/lyra_export_comparison.json", "w") as f:
        json.dump(output, f, indent=2)
    
    # D-19: TemplateIndex 非零验证（UE4 >= 506）
    if result.summary.file_version_ue4 >= 506:
        template_nonzero_count = sum(
            1 for e in result.export_map 
            if e.template_index.index != 0
        )
        # Lyra 资产应有非零 TemplateIndex（修复前缺失）
        print(f"TemplateIndex 非零数量: {template_nonzero_count}")
    
    # D-18: OuterIndex 值对比验证
    outer_index_values = [e.outer_index.index for e in result.export_map[:5]]
    print(f"OuterIndex 前 5 值: {outer_index_values}")
    
    # 验证 OuterIndex 合理范围（修复前异常值）
    for outer_idx in outer_index_values:
        assert outer_idx != 999999  # 修复前可能的异常值

def test_ue_editor_file_validation():
    """UE 编辑器文件验证（Phase 6 D-20）"""
    from uasset_read import parse_uasset
    import os

    # 使用 Lyra 资产进行验证（如果可用）
    lyra_path = "LyraStarterGame/Content/Characters/Character_Default.uasset"

    if not os.path.exists(lyra_path):
        # 使用其他可用测试样本
        test_samples = [
            "test/samples/ue_editor_saved.uasset",
            "LyraStarterGame/Source/BP_Character.uasset"
        ]
        lyra_path = None
        for sample in test_samples:
            if os.path.exists(sample):
                lyra_path = sample
                break

        if lyra_path is None:
            # 跳过测试：无可用样本
            import pytest
            pytest.skip("No UE editor test samples available")

    result = parse_uasset(lyra_path)
    assert result.success

    # 验证所有导出字段
    for export in result.export_map:
        # TemplateIndex 应存在（UE5 文件 >= 506）
        assert hasattr(export, 'template_index')

        # OuterIndex 应正确
        assert hasattr(export, 'outer_index')

        # bool flags 应存在
        assert hasattr(export, 'b_forced_export')
```

3. **创建输出目录：**
```bash
mkdir -p test/output
mkdir -p test/samples
```

4. **运行对比验证：**
```bash
python -m pytest tests/test_uasset_read.py::test_lyra_asset_outer_index_comparison -xvs
python -m pytest tests/test_uasset_read.py::test_ue_editor_file_validation -xvs
```

5. **手动对比验证：**
   - 对比 `test/output/lyra_export_comparison.json`（修复前后）
   - 验证 OuterIndex 值变化（修复前异常 → 修复后正常）
   - 验证 TemplateIndex 非零值（修复前缺失 → 修复后存在）

**避免：**
- 不要依赖外部资产（使用项目内置 Lyra 资产）
- 不要跳过手动对比（自动化无法完全验证）

**原因：** Lyra 资产是真实验证数据；手动对比确保修复效果直观可见。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py::test_lyra_asset_outer_index_comparison -xvs</automated>
    <manual>对比 test/output/lyra_export_comparison.json（OuterIndex 变化）</manual>
  </verify>
  <done>Lyra 资产对比验证完成，OuterIndex/TemplateIndex 修复效果确认</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
已完成导出表修复实现（06-01-PLAN）和测试验证（06-02-PLAN）：

**实现内容：**
- ErrorContext 结构扩展（导出表特定字段）
- ObjectExport dataclass 扩展（TemplateIndex, bool flags, PackageFlags）
- read_export_map 函数重构（完整字段读取）

**测试内容：**
- 单元测试（ErrorContext/ObjectExport 字段验证）
- 功能测试（TemplateIndex/OuterIndex/bool flags 读取验证）
- Lyra 资产对比（修复前后 OuterIndex 值变化）
  </what-built>
  <how-to-verify>
**验证步骤（严格顺序）：**

1. **运行所有测试：**
```bash
python -m pytest tests/test_uasset_read.py -v
```
期望：所有测试通过（包括新测试）

2. **对比 Lyra 资产输出：**
```bash
python -m uasset_read LyraStarterGame/Source/BP_Character.uasset --json > test/output/lyra_v2.json
```
对比 `test/output/lyra_export_comparison.json`：
- TemplateIndex 字段是否存在？
- OuterIndex 值是否合理？（修复前异常值消失）
- bool flags 字段是否存在？

3. **验证错误上下文：**
解析损坏文件（测试用），检查错误信息是否包含：
- 导出索引（export_index）
- 偏移值（expected_offset, actual_offset）
- 字段名（field_name）
- 版本信息（version_info）

4. **回归验证：**
```bash
python -m pytest tests/test_uasset_read.py -k "not export" -v
```
期望：现有测试全部通过（无回归）
  </how-to-verify>
  <resume-signal>
Type "approved" 或描述需要修复的问题。
  </resume-signal>
  <done>Human verify checkpoint 通过，用户确认 OuterIndex/TemplateIndex 修复效果，JSON 输出对比验证完成，所有测试通过</done>
</task>

</tasks>

<verification>
## Phase-Level Verification

**功能验证：**
1. ✓ 单元测试全部通过
2. ✓ 功能测试全部通过
3. ✓ Lyra 资产对比验证成功
4. ✓ 错误上下文信息完整

**回归验证：**
- ✓ Phase 1-5 测试通过
- ✓ 无新增警告或错误

**文档验证：**
- ✓ 测试文档更新（新测试说明）
- ✓ JSON 输出格式更新（新字段）
</verification>

<success_criteria>
## Success Metrics

- [ ] 所有单元测试通过（覆盖率 >= 80%）
- [ ] TemplateIndex 非零验证成功（UE4 >= 506 文件）
- [ ] OuterIndex 值对比验证成功（修复前后对比）
- [ ] bool flags 读取验证成功
- [ ] 错误上下文信息完整（包含导出索引、偏移、字段名、版本）
- [ ] Lyra 资产解析成功（JSON 输出对比）
- [ ] 无回归（Phase 1-5 测试通过）
- [ ] Human verify checkpoint 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/06-export-table-fix/06-02-SUMMARY.md`
</output>