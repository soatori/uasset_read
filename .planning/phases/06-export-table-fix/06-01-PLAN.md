---
phase: 06-export-table-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [uasset_read.py]
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]
user_setup: []

must_haves:
  truths:
    - "解析器正确读取 TemplateIndex 字段（UE4 >= 506）"
    - "解析器正确读取 OuterIndex 字段（修复偏移错位）"
    - "导出表解析失败时返回清晰错误信息（包含文件偏移、期望值、实际值）"
    - "解析器正确读取 bool flags（bForcedExport, bNotForClient, bNotForServer）"
    - "解析器正确读取 PackageGuid（UE5 < 1010）"
    - "解析器正确读取 bIsInheritedInstance（UE5 >= 1011）"
  artifacts:
    - path: "uasset_read.py"
      provides: "完整的导出表解析实现"
      contains: "template_index, b_forced_export, package_flags"
      min_lines: 50
  key_links:
    - from: "uasset_read.py::read_export_map"
      to: "ObjectExport dataclass"
      via: "字段赋值"
      pattern: "export_map.append(ObjectExport(...))"
    - from: "read_export_map"
      to: "file_version_ue4"
      via: "版本检查"
      pattern: "if summary.file_version_ue4 >= 506"
---

<objective>
修复 v1.0 导出表解析的 FObjectExport 结构缺失字段，确保正确读取所有字段。

**Purpose:** 当前 read_export_map() 缺失 TemplateIndex、bool flags、PackageGuid 等字段，导致偏移错位影响后续蓝图图解析（Phase 7）。此修复实现完整的 FObjectExport 序列化，按 UE 源码顺序读取所有字段。

**Output:**
- ErrorContext 结构扩展（导出表特定字段）
- ObjectExport dataclass 扩展（新增字段）
- read_export_map() 函数重构（完整字段读取）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-export-table-fix/06-CONTEXT.md
@.planning/research/SUMMARY.md

# Phase 5 ErrorContext结构（D-18）
@uasset_read.py[112-131]

# 当前ObjectExport dataclass（需扩展）
@uasset_read.py[636-656]

# 当前read_export_map实现（需修复）
@uasset_read.py[1267-1330]
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From uasset_read.py:
```python
@dataclass
class ErrorContext:
    """错误上下文信息（Phase 5 D-18）"""
    offset: int           # 文件偏移位置
    phase: str            # 解析阶段
    operation: str        # 操作类型
    context_name: str = ""  # 相关对象名或属性名

@dataclass
class PackageIndex:
    """FPackageIndex 有符号索引"""
    index: int
    # Properties: is_null, is_import, is_export, to_import_index, to_export_index

@dataclass
class ObjectExport:
    """FObjectExport 导出表条目（当前版本）"""
    class_index: PackageIndex
    super_index: PackageIndex
    outer_index: PackageIndex
    object_name: str
    object_flags: int
    serial_size: int
    serial_offset: int
    script_serial_size: int = 0
    script_serial_offset: int = 0
    properties: List["PropertyValue"] = field(default_factory=list)

class FArchive:
    """二进制读取器"""
    def read_i32() -> int
    def read_i64() -> int
    def read_u32() -> int
    def read_u8() -> int
    def read_name(name_map) -> str
    def seek(offset)
```

From PackageFileSummary:
```python
class PackageFileSummary:
    file_version_ue4: int    # UE4 文件版本
    file_version_ue5: int    # UE5 文件版本
    legacy_file_version: int # Legacy 版本
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 ErrorContext 结构（导出表特定字段）</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: ErrorContext 包含 export_index 字段，默认值 None
    - Test 2: ErrorContext 包含 expected_offset 和 actual_offset 字段，默认值 None
    - Test 3: ErrorContext 包含 field_name 字段，默认值空字符串
    - Test 4: ErrorContext 包含 version_info 字段，默认值空字典
    - Test 5: ErrorContext 可初始化所有字段（包括新字段）
  </behavior>
  <action>
**实现步骤（严格顺序）：**

1. **定位 ErrorContext dataclass**（uasset_read.py 第 112-131 行）

2. **添加导出表特定字段（D-12/D-13/D-14）：**
```python
@dataclass
class ErrorContext:
    """D-15/D-18: 错误上下文信息。记录错误发生时的解析状态，帮助定位问题。"""
    offset: int           # 文件偏移位置
    phase: str            # 解析阶段：header/name_table/import_map/export_map/properties/blueprint
    operation: str        # 操作类型：read_i32/read_name/seek 等
    context_name: str = ""  # 相关对象名或属性名
    # Phase 6 新增（D-12/D-13/D-14）：导出表解析阶段信息
    export_index: Optional[int] = None    # 当前导出索引（0-based）
    expected_offset: Optional[int] = None  # 期望偏移
    actual_offset: Optional[int] = None    # 实际偏移
    field_name: str = ""                  # 字段名（如 "TemplateIndex"）
    version_info: Dict[str, int] = field(default_factory=dict)  # 版本检查失败信息
```

3. **导入 Optional 和 Dict 类型：**
   - 检查文件顶部 import（约第 1-20 行）
   - 确保 `from typing import Optional, Dict, List` 已存在

4. **验证点：**
   - ErrorContext 结构扩展完成
   - 所有新字段有默认值（向后兼容）
   - 可初始化所有字段

**避免：**
- 不要修改现有字段名或类型（向后兼容）
- 不要删除现有字段

**原因（D-12/D-13/D-14）：** 导出表解析失败时需要具体上下文信息定位问题，包括导出索引、偏移值、字段名、版本信息。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py::test_error_context_export_fields -xvs</automated>
    <manual>检查 ErrorContext dataclass 包含所有新字段</manual>
  </verify>
  <done>ErrorContext 包含 export_index, expected_offset, actual_offset, field_name, version_info 字段，所有测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 ObjectExport dataclass（新增字段）</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: ObjectExport 包含 template_index 字段，类型 PackageIndex，默认值 PackageIndex(0)
    - Test 2: ObjectExport 包含 b_forced_export, b_not_for_client, b_not_for_server 字段，类型 bool，默认值 False
    - Test 3: ObjectExport 包含 package_flags 字段，类型 int，默认值 0
    - Test 4: ObjectExport 包含 b_is_inherited_instance 字段，类型 Optional[bool]，默认值 None（条件字段）
    - Test 5: ObjectExport 包含 b_not_always_loaded_for_editor_game, b_is_asset, b_generate_public_hash 字段（条件bool flags）
    - Test 6: ObjectExport 可初始化所有字段
  </behavior>
  <action>
**实现步骤（严格顺序）：**

1. **定位 ObjectExport dataclass**（uasset_read.py 第 636-656 行）

2. **添加缺失字段（D-16/D-17）：**
```python
@dataclass
class ObjectExport:
    """
    FObjectExport 导出表条目（CORE-05/CORE-06）。
    来自 ObjectResource.h：表示包内对象定义。
    """
    class_index: PackageIndex      # 类引用（CORE-06 资产类型识别）
    super_index: PackageIndex      # 父类引用
    template_index: PackageIndex = PackageIndex(0)  # D-04/D-01: TemplateIndex（UE4 >= 506）
    outer_index: PackageIndex      # Outer 引用
    object_name: str               # 对象名
    object_flags: int              # EObjectFlags
    serial_size: int               # 序列化数据大小
    serial_offset: int             # 序列化数据偏移
    # bool flags（D-07）
    b_forced_export: bool = False
    b_not_for_client: bool = False
    b_not_for_server: bool = False
    # PackageGuid（D-10/D-11：UE5 < 1010时读取但不存储）
    # 条件 bool flags（D-08）
    b_is_inherited_instance: Optional[bool] = None  # UE5 >= 1011
    package_flags: int = 0         # D-09: PackageFlags
    # 其他条件 bool flags（D-08）
    b_not_always_loaded_for_editor_game: Optional[bool] = None
    b_is_asset: Optional[bool] = None
    b_generate_public_hash: Optional[bool] = None
    # UE5+ 字段（现有）
    script_serial_size: int = 0
    script_serial_offset: int = 0
    # 属性列表 (Phase 2 PROP-01 至 PROP-08)
    properties: List["PropertyValue"] = field(default_factory=list)
```

3. **版本常量定义（文件顶部约第 30-50 行）：**
```python
# Phase 6 新增版本常量（D-01）
VER_UE4_TemplateIndex_IN_COOKED_EXPORTS = 506  # ObjectVersion.h line 711
VER_UE4_64BIT_EXPORTOFFSETS = 508              # 已存在
# UE5 版本常量
UE5_REMOVE_OBJECT_EXPORT_PACKAGE_GUID = 1010    # FReleaseObjectVersion
UE5_TRACK_OBJECT_EXPORT_IS_INHERITED = 1011     # FReleaseObjectVersion
UE5_TRACK_OBJECT_EXPORT_IS_ASSET = ?            # 待确认
UE5_GENERATE_PUBLIC_HASH = 1015                 # FReleaseObjectVersion
```

4. **验证点：**
   - ObjectExport 包含所有缺失字段
   - 条件字段使用 Optional 或默认值
   - 向后兼容（现有字段不变）

**避免：**
- 不要删除现有字段
- 不要改变现有字段顺序（dataclass 初始化顺序）

**原因（D-16/D-17）：** dataclass 需反映完整 FObjectExport 结构；JSON 输出应包含所有解析字段；条件字段用 Optional 表示可能不存在。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py::test_object_export_new_fields -xvs</automated>
    <manual>检查 ObjectExport dataclass 包含所有新字段</manual>
  </verify>
  <done>ObjectExport 包含 template_index, bool flags, package_flags 等所有缺失字段，所有测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 重构 read_export_map 函数（完整字段读取）</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: read_export_map 正确读取 TemplateIndex（UE4 >= 506），检查版本条件
    - Test 2: read_export_map 正确读取 OuterIndex（修复偏移错位），TemplateIndex 读取后
    - Test 3: read_export_map 正确读取 bool flags（bForcedExport, bNotForClient, bNotForServer）
    - Test 4: read_export_map 正确读取 PackageGuid（UE5 < 1010），读取但不存储
    - Test 5: read_export_map 正确读取 bIsInheritedInstance（UE5 >= 1011）
    - Test 6: read_export_map 正确读取 PackageFlags
    - Test 7: read_export_map 错误时返回 ErrorContext（包含导出索引、偏移、字段名）
    - Test 8: read_export_map 严格按 ObjectResource.cpp 第 130-217 行顺序序列化
  </behavior>
  <action>
**实现步骤（严格按 ObjectResource.cpp 第 130-217 行顺序）：**

**前置条件：** Task 1 和 Task 2 完成（ErrorContext 和 ObjectExport 扩展）

1. **定位 read_export_map 函数**（uasset_read.py 第 1267-1330 行）

2. **重构导出表读取逻辑（D-05 严格顺序）：**
```python
def read_export_map(
    archive: FArchive,
    summary: PackageFileSummary,
    name_map: List[str]
) -> List[ObjectExport]:
    """
    读取导出表（CORE-05/CORE-06, BUG-01/BUG-02/BUG-03）。
    
    严格按 ObjectResource.cpp 第 130-217 行顺序：
    1. ClassIndex → 2. SuperIndex → 3. TemplateIndex(条件) → 4. OuterIndex →
    5. ObjectName → 6. ObjectFlags → 7-8. SerialSize/Offset →
    9-11. bool flags → 12. PackageGuid(条件) → 13. bIsInheritedInstance(条件) →
    14. PackageFlags → 15-17. 其他 bool flags →
    19-20. ScriptSerializationStartOffset/EndOffset(条件)
    
    Args:
        archive: FArchive 实例
        summary: PackageFileSummary 实例
        name_map: 已解析的名称表
    
    Returns:
        导出表列表（ExportMap）
    
    Raises:
        ParseError: 导出表解析失败（携带 ErrorContext）
    """
    archive.seek(summary.export_offset)
    export_map: List[ObjectExport] = []
    
    for export_idx in range(summary.export_count):
        try:
            # 1. ClassIndex
            class_index = PackageIndex(archive.read_i32())
            
            # 2. SuperIndex
            super_index = PackageIndex(archive.read_i32())
            
            # 3. TemplateIndex（D-01：条件读取 UE4 >= 506）
            template_index = PackageIndex(0)
            if summary.file_version_ue4 >= VER_UE4_TemplateIndex_IN_COOKED_EXPORTS:  # 506
                template_index = PackageIndex(archive.read_i32())
            
            # 4. OuterIndex（D-02：TemplateIndex 之后）
            outer_index = PackageIndex(archive.read_i32())
            
            # 5. ObjectName
            object_name = archive.read_name(name_map)
            
            # 6. ObjectFlags
            object_flags = archive.read_u32()
            
            # 7-8. SerialSize/Offset（UE4 >= 508 使用 i64，否则 i32）
            if summary.file_version_ue4 >= VER_UE4_64BIT_EXPORTOFFSETS:  # 508
                serial_size = archive.read_i64()
                serial_offset = archive.read_i64()
            else:
                serial_size = archive.read_i32()
                serial_offset = archive.read_i32()
            
            # 9-11. bool flags（D-07：各读取 1 byte）
            b_forced_export = bool(archive.read_u8())
            b_not_for_client = bool(archive.read_u8())
            b_not_for_server = bool(archive.read_u8())
            
            # 12. PackageGuid（D-10/D-11：UE5 < 1010时读取但不存储）
            # UE5 文件检测（legacy_file_version <= -8）
            is_ue5_file = summary.legacy_file_version <= -8
            if is_ue5_file and summary.file_version_ue5 < UE5_REMOVE_OBJECT_EXPORT_PACKAGE_GUID:  # 1010
                # 读取 16 bytes FGuid，但不存储（DummyPackageGuid）
                archive.read_bytes(16)  # skip FGuid
            
            # 13. bIsInheritedInstance（D-08：UE5 >= 1011）
            b_is_inherited_instance = None
            if is_ue5_file and summary.file_version_ue5 >= UE5_TRACK_OBJECT_EXPORT_IS_INHERITED:  # 1011
                b_is_inherited_instance = bool(archive.read_u8())
            
            # 14. PackageFlags（D-09）
            package_flags = archive.read_u32()
            
            # 15-17. 其他 bool flags（D-08：条件读取）
            b_not_always_loaded_for_editor_game = None
            b_is_asset = None
            b_generate_public_hash = None
            
            # UE4 版本条件（待确认阈值）
            # if summary.file_version_ue4 >= VER_UE4_TRACK_NOT_ALWAYS_LOADED:
            #     b_not_always_loaded_for_editor_game = bool(archive.read_u8())
            
            # UE5 版本条件
            if is_ue5_file and summary.file_version_ue5 >= UE5_GENERATE_PUBLIC_HASH:  # 1015
                b_generate_public_hash = bool(archive.read_u8())
            
            # 18. 依赖数组（D-06：推迟到 Phase 10）
            # FirstExportDependency + 4个数组 → Phase 10
            
            # 19-20. ScriptSerializationStartOffset/EndOffset（UE5+ 条件）
            script_serial_size = 0
            script_serial_offset = 0
            if is_ue5_file:
                script_serial_size = archive.read_i64()
                script_serial_offset = archive.read_i64()
            
            # 构建导出条目
            export_map.append(ObjectExport(
                class_index=class_index,
                super_index=super_index,
                template_index=template_index,
                outer_index=outer_index,
                object_name=object_name,
                object_flags=object_flags,
                serial_size=serial_size,
                serial_offset=serial_offset,
                b_forced_export=b_forced_export,
                b_not_for_client=b_not_for_client,
                b_not_for_server=b_not_for_server,
                b_is_inherited_instance=b_is_inherited_instance,
                package_flags=package_flags,
                b_not_always_loaded_for_editor_game=b_not_always_loaded_for_editor_game,
                b_is_asset=b_is_asset,
                b_generate_public_hash=b_generate_public_hash,
                script_serial_size=script_serial_size,
                script_serial_offset=script_serial_offset
            ))
            
        except Exception as e:
            # D-12/D-13/D-14：错误上下文增强
            context = ErrorContext(
                offset=archive.tell(),
                phase="export_map",
                operation="read_export",
                context_name=object_name if 'object_name' in locals() else "",
                export_index=export_idx,
                expected_offset=summary.export_offset + export_idx * get_export_size(summary),
                actual_offset=archive.tell(),
                field_name=get_current_field_name(),  # 当前读取的字段名
                version_info={
                    "file_version_ue4": summary.file_version_ue4,
                    "file_version_ue5": summary.file_version_ue5,
                    "threshold": VER_UE4_TemplateIndex_IN_COOKED_EXPORTS
                }
            )
            raise ParseError(
                f"导出表解析失败（导出 #{export_idx}）：{str(e)}",
                partial_result={"export_map": export_map},
                context=context
            )
    
    return export_map
```

3. **辅助函数：**
```python
def get_export_size(summary: PackageFileSummary) -> int:
    """估算导出条目大小（用于错误信息）"""
    # 基础大小：ClassIndex + SuperIndex + OuterIndex + ObjectName + ObjectFlags + SerialSize + SerialOffset
    base_size = 4 + 4 + 4 + 8 + 4 + 8 + 8  # 粗略估算
    # TemplateIndex（UE4 >= 506）
    if summary.file_version_ue4 >= VER_UE4_TemplateIndex_IN_COOKED_EXPORTS:
        base_size += 4
    # bool flags
    base_size += 3  # bForcedExport + bNotForClient + bNotForServer
    # UE5 额外字段
    is_ue5_file = summary.legacy_file_version <= -8
    if is_ue5_file:
        base_size += 16 + 16  # ScriptSerialSize/Offset
        if summary.file_version_ue5 < UE5_REMOVE_OBJECT_EXPORT_PACKAGE_GUID:
            base_size += 16  # PackageGuid
        if summary.file_version_ue5 >= UE5_TRACK_OBJECT_EXPORT_IS_INHERITED:
            base_size += 1  # bIsInheritedInstance
    return base_size
```

4. **验证点：**
   - TemplateIndex 条件读取正确（UE4 >= 506）
   - OuterIndex 在 TemplateIndex 之后读取
   - bool flags 各读取 1 byte
   - PackageGuid 条件读取但不存储
   - 错误时返回详细 ErrorContext

**避免：**
- 不要跳过任何字段（即使不存储也要读取）
- 不要改变读取顺序（严格按 UE 源码）
- 不要忽略版本条件（避免旧文件偏移错位）

**原因（D-05/D-01）：** UE 源码明确序列化顺序；版本条件决定字段是否存在；跳过读取导致后续字段偏移错位。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py::test_read_export_map_template_index -xvs</automated>
    <automated>python -m pytest tests/test_uasset_read.py::test_read_export_map_outer_index_fix -xvs</automated>
    <automated>python -m pytest tests/test_uasset_read.py::test_read_export_map_bool_flags -xvs</automated>
    <automated>python -m pytest tests/test_uasset_read.py::test_read_export_map_package_guid -xvs</automated>
    <manual>对比 Lyra 资产修复前后 JSON 输出（OuterIndex 变化）</manual>
  </verify>
  <done>read_export_map 完整读取所有 FObjectExport 字段，TemplateIndex/OuterIndex 正确，错误时返回清晰 ErrorContext</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 输入文件 → FArchive | 不可信的 .uasset 文件，可能损坏或恶意构造 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | read_export_map | mitigate | D-10/D-12: 偏移验证 + 错误上下文，检测越界读取 |
| T-06-02 | Tampering | PackageGuid | mitigate | D-11: 读取但不存储，避免恶意 GUID 影响 |
| T-06-03 | Denial of Service | export_count | mitigate | D-08: 循环计数限制（Phase 5 MAX_EXPORT_COUNT），防止超大导出表 |
| T-06-04 | Information Disclosure | ErrorContext | accept | D-12/D-13: 错误信息包含偏移/版本，用于诊断（无敏感数据） |

**安全说明：** Phase 6 修复导出表解析，主要风险是损坏文件导致越界读取。通过 Phase 5 的边界验证和此阶段的错误上下文增强，可以安全处理损坏文件。
</threat_model>

<verification>
## Phase-Level Verification

**功能验证：**
1. TemplateIndex 字段读取（UE4 >= 506 文件）
2. OuterIndex 字段正确（修复偏移错位）
3. bool flags 字段读取
4. PackageGuid 条件读取
5. 错误上下文信息完整

**回归验证：**
- 现有测试通过（Phase 1-5 功能不受影响）
- Lyra 资产解析成功（修复前后对比）

**文档验证：**
- ErrorContext 结构文档更新
- ObjectExport dataclass 文档更新
- read_export_map 函数文档更新（UE 源码参考）
</verification>

<success_criteria>
## Must-Haves (Goal-Backward Derivation)

**Observable Truths:**
1. ✓ 解析器正确读取 TemplateIndex 字段（UE4 >= 506）
2. ✓ 解析器正确读取 OuterIndex 字段（修复偏移错位）
3. ✓ 导出表解析失败时返回清晰错误信息
4. ✓ 解析器正确读取 bool flags
5. ✓ 解析器正确读取 PackageGuid（UE5 < 1010）
6. ✓ 解析器正确读取 bIsInheritedInstance（UE5 >= 1011）

**Required Artifacts:**
- ✓ uasset_read.py 包含 ErrorContext 扩展
- ✓ uasset_read.py 包含 ObjectExport 扩展
- ✓ uasset_read.py 包含 read_export_map 重构
- ✓ tests/test_uasset_read.py 包含新测试

**Required Wiring:**
- ✓ ErrorContext → ParseError（错误时携带上下文）
- ✓ ObjectExport → read_export_map（字段赋值）
- ✓ file_version_ue4 → TemplateIndex 条件检查
- ✓ file_version_ue5 → PackageGuid/bIsInheritedInstance 条件检查

**Key Links:**
- ✓ TemplateIndex 条件检查 → file_version_ue4 >= 506（偏移正确性关键）
- ✓ OuterIndex 读取位置 → TemplateIndex 之后（偏移错位修复关键）
- ✓ ErrorContext → 错误信息质量（用户诊断关键）

## Success Metrics

- [ ] 所有单元测试通过（覆盖率 >= 80%）
- [ ] Lyra 资产解析成功（OuterIndex 值对比验证）
- [ ] UE4 >= 506 文件 TemplateIndex 非零验证
- [ ] 错误信息包含导出索引、偏移、字段名、版本信息
- [ ] 无回归（Phase 1-5 测试通过）
</success_criteria>

<output>
完成后创建 `.planning/phases/06-export-table-fix/06-01-SUMMARY.md`
</output>