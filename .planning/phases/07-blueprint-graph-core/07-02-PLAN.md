---
phase: 07-blueprint-graph-core
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [uasset_read.py]
autonomous: true
requirements: [GRAPH-03, GRAPH-04]
user_setup: []

must_haves:
  truths:
    - "解析器能解析 UEdGraphNode 基类字段（NodeGuid、NodePosX/Y、NodeComment、Pins）"
    - "解析器能解析 UEdGraphPin 完整结构（PinId、PinName、Direction、PinType、DefaultValue、LinkedTo 原始数据）"
    - "解析器能从 EdGraph SerialOffset 读取并构建完整 Graph→Node→Pin 结构"
  artifacts:
    - path: "uasset_read.py"
      provides: "read_ue_graph_pin() 引脚解析函数"
      contains: "def read_ue_graph_pin"
      min_lines: 50
    - path: "uasset_read.py"
      provides: "read_ue_graph_node() 节点解析函数"
      contains: "def read_ue_graph_node"
      min_lines: 80
    - path: "uasset_read.py"
      provides: "read_ue_graph() 图解析函数"
      contains: "def read_ue_graph"
      min_lines: 60
  key_links:
    - from: "read_ue_graph_pin()"
      to: "read_ed_graph_pin_type()"
      via: "直接调用 Phase 3 函数"
    - from: "read_ue_graph_node()"
      to: "read_ue_graph_pin()"
      via: "遍历 Pins 数组调用"
    - from: "read_ue_graph()"
      to: "read_ue_graph_node()"
      via: "遍历 Nodes 数组调用"
---

# Phase 7 Plan 02: Node/Pin 核心解析实现

<objective>
实现 UEdGraph、UEdGraphNode、UEdGraphPin 的完整二进制解析，复用 Phase 3 FEdGraphPinType 实现。

Purpose: 构建三层解析管道，交付 GRAPH-03/GRAPH-04 核心解析能力。
Output: read_ue_graph(), read_ue_graph_node(), read_ue_graph_pin() 函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-blueprint-graph-core/07-CONTEXT.md
@.planning/phases/07-blueprint-graph-core/07-RESEARCH.md
@.planning/phases/07-blueprint-graph-core/07-01-SUMMARY.md

# Wave 1 交付（Plan 01）
- UEdGraph/UEdGraphNode/UEdGraphPin 数据类定义
- extract_blueprint_graphs() EdGraph 检测入口
- resolve_class_name() 类名解析函数
- ParseResult.graphs 字段扩展
</context>

<interfaces>
<!-- Executor 使用的现有代码接口 -->

From uasset_read.py（Wave 1 定义）:
```python
class UEdGraphPin:
    pin_id: str
    pin_name: str
    direction: int
    pin_type: FEdGraphPinType
    default_value: Optional[str] = None
    auto_default_value: Optional[str] = None
    linked_to_raw: List[str] = field(default_factory=list)
    sub_pins: List[str] = field(default_factory=list)
    parent_pin: Optional[str] = None
    flags: int = 0

class UEdGraphNode:
    node_guid: str
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List[UEdGraphPin] = field(default_factory=list)
    class_name: str = ""
    node_data: Optional[Any] = None

class UEdGraph:
    graph_name: str
    graph_class: str
    schema: Optional[str] = None
    nodes: List[UEdGraphNode] = field(default_factory=list)
    graph_guid: Optional[str] = None
    b_editable: bool = True
```

From uasset_read.py（Phase 3 已实现）:
```python
def read_ed_graph_pin_type(
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary
) -> FEdGraphPinType:
    """Parse FEdGraphPinType - Phase 3 已实现，直接复用"""
    # 函数位置：L1561-1635
    # 已处理版本感知，完整结构解析
```

From uasset_read.py（FArchive 方法）:
```python
archive.read_i32()      # int32
archive.read_u8()       # uint8
archive.read_fstring()  # FString -> str
archive.read_name(name_map)  # FName -> str
archive.read_bytes(n)   # n bytes -> bytes.hex()
archive.seek(offset)
archive.tell()
```

From RESEARCH.md L384-430（UEdGraphPin 解析顺序）:
```
1. PinId (FGuid 16 bytes)
2. PinName (FName)
3. Direction (uint8)
4. PinType (FEdGraphPinType — 调用 read_ed_graph_pin_type)
5. DefaultValue (FString)
6. AutogeneratedDefaultValue (FString)
7. LinkedTo 数组 (int32 count + PinId[])
8. SubPins/ParentPin (条件字段)
9. Flags (uint8)
```

From RESEARCH.md L270-290（UEdGraphNode 基类字段顺序）:
```
1. Pins 数组 (int32 count + loop)
2. NodePosX (int32)
3. NodePosY (int32)
4. NodeGuid (FGuid 16 bytes)
5. NodeComment (FString)
```

From RESEARCH.md L160-170（UEdGraph 字段顺序）:
```
1. Schema (FPackageIndex)
2. Nodes 数组 (int32 count + FPackageIndex[])
3. GraphGuid (FGuid 16 bytes)
4. bEditable (uint8)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现 UEdGraphPin 解析函数（GRAPH-04）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L1561-1635（read_ed_graph_pin_type 函数）
    - uasset_read.py L724-742（FEdGraphPinType 定义）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L384-430（引脚解析顺序）
    - .planning/phases/07-blueprint-graph-core/07-01-SUMMARY.md（UEdGraphPin 数据类定义）
    - .planning/research/PITFALLS.md（陷阱 3：版本依赖字段）
  </read_first>
  <acceptance_criteria>
    - grep -c "def read_ue_graph_pin" uasset_read.py == 1
    - grep "read_ed_graph_pin_type" uasset_read.py | grep -v "^#" | wc -l >= 2（至少一处调用）
    - python -c "from uasset_read import read_ue_graph_pin; print('OK')"
  </acceptance_criteria>
  <action>
    实现 read_ue_graph_pin() 函数，解析 UEdGraphPin 完整结构：

    ```python
    def read_ue_graph_pin(
        archive: FArchive,
        name_map: List[str],
        summary: PackageFileSummary
    ) -> UEdGraphPin:
        """
        读取 UEdGraphPin（GRAPH-04）。

        序列化顺序（基于 RESEARCH.md L384-430）：
        1. PinId (FGuid 16 bytes) -> hex string
        2. PinName (FName) -> str
        3. Direction (uint8) -> int
        4. PinType (FEdGraphPinType) -> 调用 read_ed_graph_pin_type()
        5. DefaultValue (FString) -> str
        6. AutogeneratedDefaultValue (FString) -> str
        7. LinkedTo 数组 (int32 count + loop) -> D-01a: 存储原始数据
        8. SubPins (int32 count + loop) -> PinId 列表
        9. ParentPin (条件：has_parent flag + PinId) -> Optional[str]
        10. Flags (uint8) -> int

        Args:
            archive: FArchive positioned at pin start
            name_map: NameMap for FName resolution
            summary: PackageFileSummary for version info

        Returns:
            UEdGraphPin dataclass
        """
        # 1. PinId (FGuid 16 bytes)
        pin_id = archive.read_bytes(16).hex()

        # 2. PinName (FName)
        pin_name = archive.read_name(name_map)

        # 3. Direction (uint8: 0=Input, 1=Output, 2=None)
        direction = archive.read_u8()

        # 4. PinType — 直接复用 Phase 3 实现
        pin_type = read_ed_graph_pin_type(archive, name_map, summary)

        # 5-6. DefaultValue strings
        default_value = archive.read_fstring()
        auto_default_value = archive.read_fstring()

        # 7. LinkedTo 数组 — D-01a: 存储为原始数据列表
        linked_to_count = archive.read_i32()
        linked_to_raw = []
        for _ in range(linked_to_count):
            # 每个 linked pin 是 PinId GUID
            linked_pin_id = archive.read_bytes(16).hex()
            linked_to_raw.append(linked_pin_id)

        # 8. SubPins 数组
        sub_pins_count = archive.read_i32()
        sub_pins = []
        for _ in range(sub_pins_count):
            sub_pin_id = archive.read_bytes(16).hex()
            sub_pins.append(sub_pin_id)

        # 9. ParentPin（条件字段）
        has_parent = archive.read_u8() != 0
        parent_pin = None
        if has_parent:
            parent_pin = archive.read_bytes(16).hex()

        # 10. Flags bitfield
        flags = archive.read_u8()

        return UEdGraphPin(
            pin_id=pin_id,
            pin_name=pin_name,
            direction=direction,
            pin_type=pin_type,
            default_value=default_value,
            auto_default_value=auto_default_value,
            linked_to_raw=linked_to_raw,
            sub_pins=sub_pins,
            parent_pin=parent_pin,
            flags=flags
        )
    ```

    关键要点：
    - 直接调用 read_ed_graph_pin_type()，不重复实现版本检查
    - LinkedTo 存储为原始 PinId 列表（D-01）
    - 边界检查：linked_to_count 和 sub_pins_count 应有上限（MAX_LINKEDTO=100）
    - 函数位置：在 read_ed_graph_pin_type() 之后

    添加到 __all__ 导出列表。
  </action>
  <verify>
    <automated>python -c "from uasset_read import read_ue_graph_pin; print('Import OK')" && python -m pytest tests/test_uasset_read.py -v -x 2>&1 | grep -E "(passed|PASSED|ERROR)"</automated>
  </verify>
  <done>
    - read_ue_graph_pin() 函数实现完成，可导入
    - 正确调用 read_ed_graph_pin_type()
    - LinkedTo 存储为原始数据（符合 D-01）
    - 现有测试无回归
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 UEdGraphNode/UEdGraph 解析函数（GRAPH-03）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L1267-1416（read_export_map 函数 - SerialOffset 使用）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L270-290（Node 基类字段顺序）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L160-170（Graph 字段顺序）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L260-316（类型分派模式）
    - .planning/phases/07-blueprint-graph-core/07-01-SUMMARY.md（resolve_class_name 函数）
  </read_first>
  <acceptance_criteria>
    - grep -c "def read_ue_graph_node" uasset_read.py == 1
    - grep -c "def read_ue_graph" uasset_read.py == 1
    - grep "read_ue_graph_pin" uasset_read.py | grep -v "^#" | wc -l >= 2
    - python -c "from uasset_read import read_ue_graph_node, read_ue_graph; print('OK')"
  </acceptance_criteria>
  <action>
    实现 read_ue_graph_node() 和 read_ue_graph() 函数：

    ```python
    def read_ue_graph_node(
        archive: FArchive,
        name_map: List[str],
        summary: PackageFileSummary,
        export_map: List[ObjectExport],
        import_map: List[ObjectImport],
        node_export: ObjectExport
    ) -> UEdGraphNode:
        """
        读取 UEdGraphNode 基类字段（GRAPH-03）。

        序列化顺序（RESEARCH.md L270-290）：
        1. Pins 数组 (int32 count + loop call read_ue_graph_pin)
        2. NodePosX (int32)
        3. NodePosY (int32)
        4. NodeGuid (FGuid 16 bytes)
        5. NodeComment (FString)

        Args:
            archive: FArchive positioned at node serial_offset
            name_map, summary: 解析上下文
            export_map, import_map: 类名解析
            node_export: 当前节点导出条目

        Returns:
            UEdGraphNode（基类字段 + class_name）
        """
        archive.seek(node_export.serial_offset)

        # 1. Pins 数组
        pins_count = archive.read_i32()
        pins = []
        for _ in range(pins_count):
            pin = read_ue_graph_pin(archive, name_map, summary)
            pins.append(pin)

        # 2-3. NodePos
        node_pos_x = archive.read_i32()
        node_pos_y = archive.read_i32()

        # 4. NodeGuid
        node_guid = archive.read_bytes(16).hex()

        # 5. NodeComment
        node_comment = archive.read_fstring()

        # 类型识别（D-02b）
        class_name = resolve_class_name(node_export.class_index, import_map, export_map)

        # Wave 3 将实现类型特定解析器，此处 node_data = None
        return UEdGraphNode(
            node_guid=node_guid,
            node_pos_x=node_pos_x,
            node_pos_y=node_pos_y,
            node_comment=node_comment,
            pins=pins,
            class_name=class_name,
            node_data=None  # Wave 3 实现
        )


    def read_ue_graph(
        archive: FArchive,
        name_map: List[str],
        summary: PackageFileSummary,
        export_map: List[ObjectExport],
        import_map: List[ObjectImport],
        graph_export: ObjectExport,
        graph_class: str
    ) -> UEdGraph:
        """
        读取 UEdGraph（GRAPH-02）。

        序列化顺序（RESEARCH.md L160-170）：
        1. Schema (FPackageIndex -> resolve)
        2. Nodes 数组 (int32 count + FPackageIndex[])
           — 需从 FPackageIndex 找到对应导出并调用 read_ue_graph_node
        3. GraphGuid (FGuid 16 bytes)
        4. bEditable (uint8)

        Args:
            archive: FArchive
            name_map, summary: 解析上下文
            export_map, import_map: 导出表/导入表
            graph_export: 图导出条目
            graph_class: 已解析的类名

        Returns:
            UEdGraph with nodes populated
        """
        archive.seek(graph_export.serial_offset)

        # 1. Schema (FPackageIndex)
        schema_index = archive.read_i32()
        schema = None
        if schema_index != 0:
            schema = resolve_class_name(PackageIndex(schema_index), import_map, export_map)

        # 2. Nodes 数组（FPackageIndex 数组）
        nodes_count = archive.read_i32()
        nodes = []

        # Nodes 是导出索引数组（FPackageIndex > 0）
        for _ in range(nodes_count):
            node_index = archive.read_i32()  # FPackageIndex
            if node_index > 0 and node_index <= len(export_map):
                node_export = export_map[node_index - 1]
                node = read_ue_graph_node(
                    archive, name_map, summary,
                    export_map, import_map, node_export
                )
                nodes.append(node)

        # 3. GraphGuid
        graph_guid = archive.read_bytes(16).hex()

        # 4. bEditable
        b_editable = archive.read_u8() != 0

        return UEdGraph(
            graph_name=graph_export.object_name,
            graph_class=graph_class,
            schema=schema,
            nodes=nodes,
            graph_guid=graph_guid,
            b_editable=b_editable
        )
    ```

    更新 extract_blueprint_graphs()：
    在检测到 EdGraph 后，调用 read_ue_graph() 完整解析：

    ```python
    # 在 extract_blueprint_graphs() 循环中
    if "EdGraph" in class_name or "Ubergraph" in class_name:
        graph = read_ue_graph(
            archive, name_map, summary,
            export_map, import_map, export, class_name
        )
        graphs.append(graph)
    ```

    添加到 __all__ 导出列表。
  </action>
  <verify>
    <automated>python -c "from uasset_read import read_ue_graph_node, read_ue_graph; print('Import OK')" && python -m pytest tests/test_uasset_read.py -v -x 2>&1 | grep -E "(passed|PASSED|ERROR)"</automated>
  </verify>
  <done>
    - read_ue_graph_node() 实现完成，解析基类字段
    - read_ue_graph() 实现完成，构建完整 Graph→Node→Pin 结构
    - extract_blueprint_graphs() 更新为调用 read_ue_graph()
    - 三层解析管道完整连接
    - 现有测试无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SerialOffset → Node data | 偏移定位可能超出文件边界 |
| Nodes array count | 大数组可能导致内存耗尽 |
| LinkedTo count | 异常大值可能影响性能 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | archive.seek(serial_offset) | mitigate | validate_offset() 检查边界 |
| T-07-02-02 | Tampering | pins_count | mitigate | MAX_PINS_PER_NODE=1000 上限 |
| T-07-02-03 | Tampering | nodes_count | mitigate | MAX_NODES_PER_GRAPH=5000 上限 |
| T-07-02-04 | Tampering | linked_to_count | mitigate | MAX_LINKEDTO_PER_PIN=100 上限 |
</threat_model>

<verification>
- read_ue_graph_pin() 函数导入测试
- read_ue_graph_node() 函数导入测试
- read_ue_graph() 函数导入测试
- 三层结构构建验证（集成测试 Wave 3）
- 回归测试：现有测试套件通过
</verification>

<success_criteria>
- read_ue_graph_pin() 正确解析 UEdGraphPin 完整结构
- read_ue_graph_node() 正确解析基类字段（NodeGuid、NodePos、Comment、Pins）
- read_ue_graph() 正确解析 UEdGraph 并构建完整 Node→Pin 层级
- extract_blueprint_graphs() 返回完整解析的 UEdGraph 列表
- 现有测试无回归
</success_criteria>

<output>
After completion, create `.planning/phases/07-blueprint-graph-core/07-02-SUMMARY.md`
</output>