---
phase: 07-blueprint-graph-core
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [uasset_read.py, tests/test_graph_parsing.py]
autonomous: true
requirements: [GRAPH-05, GRAPH-06, GRAPH-07, GRAPH-08, GRAPH-09]
user_setup: []

must_haves:
  truths:
    - "解析器能识别 K2Node_CallFunction 节点并提取 FunctionReference"
    - "解析器能识别 K2Node_Event 节点并提取 EventReference"
    - "解析器能识别 K2Node_Knot 节点（仅基类字段）"
    - "解析器能识别 EdGraphNode_Comment 节点并提取注释文本和尺寸"
    - "解析器能识别 K2Node_EnhancedInputAction 节点并提取 InputAction 路径"
    - "未知节点类型触发警告但解析继续"
  artifacts:
    - path: "uasset_read.py"
      provides: "5种节点类型特定解析器"
      contains: "def read_k2node_call_function"
      min_lines: 150
    - path: "tests/test_graph_parsing.py"
      provides: "Phase 7 单元测试"
      contains: "test_k2node_call_function"
      min_lines: 100
  key_links:
    - from: "read_ue_graph_node()"
      to: "类型特定解析器"
      via: "match/case class_name 分派"
    - from: "node_data"
      to: "K2NodeCallFunction/K2NodeEvent/etc"
      via: "多态数据存储"
    - from: "read_k2node_call_function()"
      to: "read_fmember_reference()"
      via: "直接调用获取 FunctionReference"
    - from: "read_k2node_event()"
      to: "read_fmember_reference()"
      via: "直接调用获取 EventReference"
---

# Phase 7 Plan 03: 节点类型特定解析器

<objective>
实现 5 种需求节点类型的特定解析器，集成类型分派机制，完成 Phase 7 全部核心需求。

Purpose: 交付 GRAPH-05~09 节点类型识别和特定字段提取能力。
Output: 5 种节点类型解析器 + match/case 分派 + Phase 7 单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-blueprint-graph-core/07-CONTEXT.md
@.planning/phases/07-blueprint-graph-core/07-RESEARCH.md
@.planning/phases/07-blueprint-graph-core/07-02-SUMMARY.md

# Wave 2 交付（Plan 02）
- read_ue_graph_pin() 引脚解析
- read_ue_graph_node() 节点基类解析（node_data=None）
- read_ue_graph() 图完整解析
- 三层解析管道连接
</context>

<interfaces>
<!-- Executor 使用的现有代码接口 -->

From uasset_read.py（Wave 1 定义）:
```python
class FMemberReference:
    member_parent: Optional[str] = None
    member_name: str = ""
    member_guid: Optional[str] = None
    b_self_context: bool = False

class UEdGraphNode:
    node_guid: str
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List[UEdGraphPin] = field(default_factory=list)
    class_name: str = ""
    node_data: Optional[Any] = None  # 此 Wave 填充
```

From RESEARCH.md L318-355（FMemberReference 解析）:
```
FMemberReference 序列化顺序（推断）：
1. MemberParent (FPackageIndex)
2. MemberName (FName)
3. MemberGuid (FGuid 16 bytes)
4. bSelfContext (uint8)
```

From RESEARCH.md L546-570（节点类型特有数据）:
```
K2Node_CallFunction:
  - FunctionReference (FMemberReference)
  - bDefaultsToPureFunc (uint8)

K2Node_Event:
  - EventReference (FMemberReference)
  - bOverrideFunction (uint8)

K2Node_Knot:
  - 无额外字段（仅 InputPin/OutputPin 在 Pins 数组中）

EdGraphNode_Comment:
  - CommentColor (4 floats)
  - NodeWidth (int32)
  - NodeHeight (int32)
  - FontSize (int32)
  - NodeComment (FString) — 已在基类读取

K2Node_EnhancedInputAction:
  - InputAction (FSoftObjectPath: path string)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现 5 种节点类型特定解析器（GRAPH-05~09）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L1267-1416（read_export_map - 版本检查模式）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L260-316（match/case 分派）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L318-355（FMemberReference）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L546-570（节点特有数据）
    - .planning/phases/07-blueprint-graph-core/07-02-SUMMARY.md（read_ue_graph_node 位置）
    - test/编辑器中复制出的文本.txt（编辑器导出格式验证）
  </read_first>
  <acceptance_criteria>
    - grep -c "def read_fmember_reference" uasset_read.py == 1
    - grep -c "def read_k2node_call_function" uasset_read.py == 1
    - grep -c "def read_k2node_event" uasset_read.py == 1
    - grep -c "def read_k2node_knot" uasset_read.py == 1
    - grep -c "def read_edgraph_node_comment" uasset_read.py == 1
    - grep -c "def read_k2node_enhanced_input" uasset_read.py == 1
    - grep "match class_name:" uasset_read.py | wc -l == 1
  </acceptance_criteria>
  <action>
    实现 5 种节点类型特定解析器并集成类型分派：

    1. **read_fmember_reference() 函数**（辅助函数）：
       ```python
       def read_fmember_reference(
           archive: FArchive,
           name_map: List[str],
           import_map: List[ObjectImport],
           export_map: List[ObjectExport]
       ) -> FMemberReference:
           """读取 FMemberReference（函数/事件引用）"""
           member_parent_index = archive.read_i32()
           member_parent = None
           if member_parent_index != 0:
               member_parent = resolve_class_name(
                   PackageIndex(member_parent_index), import_map, export_map
               )

           member_name = archive.read_name(name_map)
           member_guid = archive.read_bytes(16).hex()
           b_self_context = archive.read_u8() != 0

           return FMemberReference(
               member_parent=member_parent,
               member_name=member_name,
               member_guid=member_guid,
               b_self_context=b_self_context
           )
       ```

    2. **节点类型解析器**（位于 read_ue_graph_node 之后）：

       ```python
       def read_k2node_call_function(
           archive: FArchive,
           name_map: List[str],
           import_map: List[ObjectImport],
           export_map: List[ObjectExport]
       ) -> K2NodeCallFunction:
           """GRAPH-05: K2Node_CallFunction 特有字段"""
           function_reference = read_fmember_reference(
               archive, name_map, import_map, export_map
           )
           b_defaults_to_pure = archive.read_u8() != 0
           return K2NodeCallFunction(
               function_reference=function_reference,
               b_defaults_to_pure=b_defaults_to_pure
           )

       def read_k2node_event(
           archive: FArchive,
           name_map: List[str],
           import_map: List[ObjectImport],
           export_map: List[ObjectExport]
       ) -> K2NodeEvent:
           """GRAPH-06: K2Node_Event 特有字段"""
           event_reference = read_fmember_reference(
               archive, name_map, import_map, export_map
           )
           b_override_function = archive.read_u8() != 0
           return K2NodeEvent(
               event_reference=event_reference,
               b_override_function=b_override_function
           )

       def read_k2node_knot(archive: FArchive) -> K2NodeKnot:
           """GRAPH-07: K2Node_Knot（无额外字段）"""
           return K2NodeKnot()  # 仅基类字段

       def read_edgraph_node_comment(archive: FArchive) -> EdGraphNodeComment:
           """GRAPH-08: EdGraphNode_Comment 特有字段"""
           # CommentColor (4 floats RGBA)
           r = struct.unpack('<f', archive.read(4))[0]
           g = struct.unpack('<f', archive.read(4))[0]
           b = struct.unpack('<f', archive.read(4))[0]
           a = struct.unpack('<f', archive.read(4))[0]
           comment_color = (r, g, b, a)

           node_width = archive.read_i32()
           node_height = archive.read_i32()
           font_size = archive.read_i32()

           return EdGraphNodeComment(
               comment_color=comment_color,
               node_width=node_width,
               node_height=node_height,
               font_size=font_size
           )

       def read_k2node_enhanced_input(
           archive: FArchive,
           name_map: List[str]
       ) -> K2NodeEnhancedInputAction:
           """GRAPH-09: K2Node_EnhancedInputAction 特有字段"""
           # FSoftObjectPath: AssetPath (FString)
           input_action_path = archive.read_fstring()
           return K2NodeEnhancedInputAction(
               input_action_path=input_action_path
           )
       ```

    3. **节点类型数据类**（在 UEdGraphNode 之后定义）：
       ```python
       @dataclass
       class K2NodeCallFunction:
           function_reference: FMemberReference
           b_defaults_to_pure: bool = False

       @dataclass
       class K2NodeEvent:
           event_reference: FMemberReference
           b_override_function: bool = False

       @dataclass
       class K2NodeKnot:
           pass  # 无额外字段

       @dataclass
       class EdGraphNodeComment:
           comment_color: Tuple[float, float, float, float] = (0.05, 0.05, 0.05, 1.0)
           node_width: int = 0
           node_height: int = 0
           font_size: int = 14

       @dataclass
       class K2NodeEnhancedInputAction:
           input_action_path: str = ""
       ```

    4. **更新 read_ue_graph_node()**（类型分派）：
       在基类字段读取后添加 match/case：
       ```python
       # 类型分派（D-02b）
       match class_name:
           case "K2Node_CallFunction":
               node_data = read_k2node_call_function(
                   archive, name_map, import_map, export_map
               )
           case "K2Node_Event":
               node_data = read_k2node_event(
                   archive, name_map, import_map, export_map
               )
           case "K2Node_Knot":
               node_data = read_k2node_knot(archive)
           case "EdGraphNode_Comment":
               node_data = read_edgraph_node_comment(archive)
           case "K2Node_EnhancedInputAction":
               node_data = read_k2node_enhanced_input(archive, name_map)
           case _:
               # D-02a: 未知类型 — 记录警告，继续解析
               node_data = {"unknown_type": class_name}
               # 可选：添加到 warnings 列表
       ```

    添加所有新函数到 __all__ 导出列表。
  </action>
  <verify>
    <automated>python -c "from uasset_read import read_k2node_call_function, read_k2node_event, read_k2node_knot, read_edgraph_node_comment, read_k2node_enhanced_input; print('Import OK')" && python -c "from uasset_read import read_fmember_reference; print('FMemberReference OK')" && python -m pytest tests/test_uasset_read.py -v -x 2>&1 | grep -E "(passed|PASSED|ERROR)"</automated>
  </verify>
  <done>
    - 5 种节点类型解析器实现完成
    - 类型数据类定义完成
    - read_ue_graph_node() 集成 match/case 分派
    - 未知类型触发警告但解析继续
    - 现有测试无回归
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 Phase 7 单元测试并验证完整功能</name>
  <files>tests/test_graph_parsing.py, tests/conftest.py</files>
  <read_first>
    - tests/test_blueprint_extraction.py（测试模式参考）
    - tests/test_uasset_read.py（fixture 和合成数据模式）
    - tests/conftest.py（现有 fixture）
    - uasset_read.py L1561-1635（read_ed_graph_pin_type 测试参考）
    - .planning/phases/07-blueprint-graph-core/07-RESEARCH.md L676-689（测试需求）
  </read_first>
  <acceptance_criteria>
    - test -f tests/test_graph_parsing.py
    - grep -c "test_detect_edgraph_export" tests/test_graph_parsing.py == 1
    - grep -c "test_read_ue_graph_pin_complete" tests/test_graph_parsing.py == 1
    - grep -c "test_k2node_call_function" tests/test_graph_parsing.py == 1
    - python -m pytest tests/test_graph_parsing.py -v 2>&1 | grep "passed"
  </acceptance_criteria>
  <action>
    创建 Phase 7 单元测试文件：

    1. **创建 tests/test_graph_parsing.py**：
       ```python
       """Phase 7: 蓝图图核心解析测试（GRAPH-01~09）"""

       import pytest
       from uasset_read import (
           UEdGraph, UEdGraphNode, UEdGraphPin,
           FMemberReference, K2NodeCallFunction, K2NodeEvent,
           extract_blueprint_graphs, read_ue_graph_pin,
           read_ue_graph_node, read_ue_graph,
           resolve_class_name, parse_uasset, PackageIndex
       )
       from tests.conftest import create_test_archive

       # ========== GRAPH-01: EdGraph 检测 ==========

       def test_resolve_class_name_from_export():
           """ClassIndex > 0 从 ExportMap 解析"""
           export_map = [MockExport(object_name="TestClass")]
           import_map = []
           result = resolve_class_name(PackageIndex(1), import_map, export_map)
           assert result == "TestClass"

       def test_resolve_class_name_from_import():
           """ClassIndex < 0 从 ImportMap 解析"""
           export_map = []
           import_map = [MockImport(class_name="ImportedClass")]
           result = resolve_class_name(PackageIndex(-1), import_map, export_map)
           assert result == "ImportedClass"

       def test_detect_edgraph_export():
           """GRAPH-01: EdGraph 类型检测"""
           # 使用合成 .uasset 数据测试
           # 验证 extract_blueprint_graphs 能识别 EdGraph 导出
           pass  # 实现需要合成 fixture

       # ========== GRAPH-02: UEdGraph 基本信息 ==========

       def test_read_ue_graph_basic():
           """GRAPH-02: UEdGraph Schema、GraphGuid、Nodes"""
           # 测试图基本信息提取
           pass

       # ========== GRAPH-03: UEdGraphNode 基类 ==========

       def test_read_ue_graph_node_basic():
           """GRAPH-03: NodeGuid、NodePos、Comment、Pins"""
           # 测试节点基类字段
           pass

       # ========== GRAPH-04: UEdGraphPin 完整结构 ==========

       def test_read_ue_graph_pin_complete():
           """GRAPH-04: PinId、PinName、Direction、PinType、DefaultValue、LinkedTo"""
           # 使用 create_test_archive 创建合成引脚数据
           archive, name_map, summary = create_test_archive(
               pin_data=b'\x00...'  # 合成引脚二进制
           )
           pin = read_ue_graph_pin(archive, name_map, summary)
           assert pin.pin_id  # GUID hex
           assert pin.pin_name
           assert pin.direction in [0, 1, 2]
           assert pin.pin_type  # FEdGraphPinType

       # ========== GRAPH-05~09: 节点类型特定解析 ==========

       def test_k2node_call_function():
           """GRAPH-05: FunctionReference 提取"""
           pass

       def test_k2node_event():
           """GRAPH-06: EventReference 提取"""
           pass

       def test_k2node_knot():
           """GRAPH-07: Knot 节点（仅基类）"""
           pass

       def test_edgraph_node_comment():
           """GRAPH-08: Comment 节点文本和尺寸"""
           pass

       def test_k2node_enhanced_input():
           """GRAPH-09: InputAction 路径"""
           pass

       # ========== 集成测试 ==========

       def test_full_graph_parsing_integration():
           """完整 Graph→Node→Pin 三层解析"""
           # 使用 Lyra 示例资产或合成数据测试完整流程
           pass

       def test_unknown_node_type_warning():
           """D-02a: 未知类型触发警告但解析继续"""
           pass

       # ========== 边界测试 ==========

       def test_max_pins_limit():
           """T-07-02-02: pins_count 上限检查"""
           pass

       def test_cooked_asset_skip():
           """T-07-01-02: cooked 资产跳过图解析"""
           pass

       class MockExport:
           def __init__(self, object_name="Test", class_index=0):
               self.object_name = object_name
               self.class_index = PackageIndex(class_index)

       class MockImport:
           def __init__(self, class_name="Test"):
               self.class_name = class_name
       ```

    2. **扩展 tests/conftest.py**（合成图数据 fixture）：
       添加 create_test_archive 函数生成合成 .uasset 数据用于测试。
       参考 Phase 3 测试模式。

    3. **运行测试**：
       执行 `python -m pytest tests/test_graph_parsing.py -v` 验证。

    注意：
    - 测试覆盖 GRAPH-01~09（GRAPH-10 在 Phase 8）
    - 边界测试验证安全威胁缓解
    - 集成测试验证完整三层解析
  </action>
  <verify>
    <automated>python -m pytest tests/test_graph_parsing.py -v -x 2>&1 | head -30</automated>
  </verify>
  <done>
    - tests/test_graph_parsing.py 创建完成
    - 所有 GRAPH-01~09 单元测试实现
    - 测试套件通过（或跳过需要真实资产的测试）
    - Phase 7 完整功能验证
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 类型分派 → 特定解析器 | match/case 覆盖已知类型，未知类型 fallback |
| MemberReference 解析 | FPackageIndex 范围检查 |
| CommentColor 读取 | 4 float 读取边界验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | match/case fallback | mitigate | D-02a: 未知类型记录警告，node_data=dict |
| T-07-03-02 | Tampering | FMemberReference | mitigate | resolve_class_name 范围检查 |
| T-07-03-03 | Tampering | EdGraphNode_Comment | mitigate | CommentColor 读取验证（0.0-1.0 范围可选） |
</threat_model>

<verification>
- 5 种节点类型解析器导入测试
- 类型分派集成测试
- Phase 7 单元测试套件运行
- 回归测试：完整测试套件通过
- GRAPH-01~09 需求验证
</verification>

<success_criteria>
- K2Node_CallFunction 解析器提取 FunctionReference（GRAPH-05）
- K2Node_Event 解析器提取 EventReference（GRAPH-06）
- K2Node_Knot 解析器处理基类字段（GRAPH-07）
- EdGraphNode_Comment 解析器提取注释文本和尺寸（GRAPH-08）
- K2Node_EnhancedInputAction 解析器提取 InputAction 路径（GRAPH-09）
- 类型分派机制完整，未知类型 fallback
- Phase 7 单元测试套件创建并通过
- 现有测试无回归
</success_criteria>

<output>
After completion, create `.planning/phases/07-blueprint-graph-core/07-03-SUMMARY.md`
</output>