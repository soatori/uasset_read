---
phase: 14-output-format-optimization
plan: 04
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - uasset_read.py
  - tests/test_output_formatting.py
autonomous: true
requirements:
  - OUT-03
  - OUT-06
user_setup: []

must_haves:
  truths:
    - "用户通过 --summary 标志获取精简摘要，输出 token 减少 70%+"
    - "摘要模式移除 imports/soft_references/circular_deps 字段"
    - "摘要模式精简 exports（仅保留 name/class/parent_class）"
    - "摘要模式移除 properties 数组"
    - "API 冻结标注清晰（代码注释 + 版本号）"
    - "测试覆盖所有新增输出格式"
  artifacts:
    - path: "uasset_read.py"
      provides: "精简 format_json_summary + API 冻结注释"
      contains: "# API Frozen since Phase 14"
      contains: "format_json_summary.*compact"
    - path: "tests/test_output_formatting.py"
      provides: "Phase 14 输出格式测试"
      contains: "test_phase14"
  key_links:
    - from: "format_json_summary()"
      to: "format_exports_summary()"
      via: "精简 exports 格式化"
      pattern: "name.*class.*parent_class"
---

<objective>
摘要精简 + CLI 扩展完善 + 测试覆盖 + API 冻结标注（OUT-03, OUT-06）

Purpose: 精简输出减少 70%+ token，冻结 API 确保 Phase 15 skill 依赖稳定
Output: format_json_summary 精简 + 测试文件 + API 冻结注释
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-output-format-optimization/14-CONTEXT.md

# 现有代码接口（提取关键类型）

From uasset_read.py:5067-5106:
```python
def format_json_summary(result: ParseResult) -> Dict:
    """Format compact JSON summary"""
    exports_summary = []
    for exp in result.export_map:
        export_summary = {
            "name": exp.object_name,
            "class": get_asset_class(...),
            "properties": [{"name": p.name, "type": p.type, "value": p.value} ...]
        }
    return {
        "version": version_dict,
        "package_name": ...,
        "exports": exports_summary,
        "blueprint_metadata": ...,
        "errors": result.errors
    }
```

From D-14-07~09:
- 移除: imports, soft_references, circular_deps, errors 详情
- 精简 exports: 仅 name, class, parent_class
- 移除: properties 数组
</context>

<interfaces>
<!-- Executor 需要的关键接口 -->

## 精简后 format_json_summary 结构

```json
{
  "status": {"status": "success", "message": null, "code": null},
  "output_version": "3.0",
  "package_name": "/Game/Test",
  "exports": [
    {"name": "TestClass_C", "class": "BlueprintGeneratedClass", "parent_class": "ACharacter"}
  ],
  "graphs_summary": [...],
  "_schema": {...}  // 仅 --verbose/--schema
}
```

## 移除字段清单（D-14-07~09）

| 字段 | 原位置 | 移除原因 |
|------|--------|----------|
| imports | 顶层 | 依赖详情不必要 |
| soft_references | 顶层 | 软引用不必要 |
| circular_deps | 顶层 | 循环依赖不必要 |
| serial_size | exports 条目 | 低级详情 |
| outer_index | exports 条目 | 低级详情 |
| super_index | exports 条目 | 低级详情 |
| properties | exports 条目 | 摘要不含属性详情 |
| errors | 顶层 | 移除详情（status 已含状态） |
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 精简 format_json_summary() 函数实现 70%+ token 减少</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: format_json_summary 不包含 imports/soft_references/circular_deps
    - Test 2: exports 仅包含 name/class/parent_class
    - Test 3: exports 不包含 properties 数组
    - Test 4: 顶层不包含 errors 数组（status 已含状态）
    - Test 5: graphs_summary 保留（已顶层化）
  </behavior>
  <action>
    Per D-14-07~09: 摘要精简实现

    1. 重构 format_json_summary() 函数（约 line 5067）:
       ```python
       def format_json_summary(result: ParseResult, include_schema: bool = False) -> Dict:
           """
           Format compact JSON summary - 70%+ token reduction（D-14-07~09, OUT-03）。

           精简策略:
           - 移除: imports, soft_references, circular_deps, errors
           - 精简 exports: 仅 name, class, parent_class
           - 移除 properties 数组
           - 保留: status, output_version, graphs_summary

           Per D-07: 移除依赖字段
           Per D-08: 精简 exports
           Per D-09: 移除 properties 数组

           Args:
               result: ParseResult from parse_uasset()
               include_schema: 是否包含 _schema 字段

           Returns:
               Dict: 精简摘要
           """
           # Status 字段（已在 14-01 实现）
           status_info = build_status_info(result)

           # 精简 exports（D-14-08）
           exports_summary = []
           for i, exp in enumerate(result.export_map):
               parent_class = ""
               if result.blueprint and result.blueprint.is_blueprint and i == 0:
                   parent_class = result.blueprint.parent_class or ""

               exports_summary.append({
                   "name": exp.object_name,
                   "class": get_asset_class(exp, result.import_map, result.export_map),
                   "parent_class": parent_class
               })

           output = {
               "status": asdict(status_info),
               "output_version": "3.0",
               "package_name": result.summary.package_name if result.summary else "",
               "exports": exports_summary,
               "graphs_summary": build_graphs_summary(result.graphs),  # D-14-04
           }

           # blueprint_metadata 精简（仅保留核心字段）
           if result.blueprint and result.blueprint.is_blueprint:
               output["blueprint_metadata"] = {
                   "parent_class": result.blueprint.parent_class,
                   "is_blueprint": True,
               }

           # _schema 字段（可选）
           if include_schema:
               output["_schema"] = build_schema_info()

           return output
       ```

    2. 添加 API 冻结注释（在 format_json_full 和 format_json_summary 函数前）:
       ```python
       # ============================================================================
       # API Frozen Since Phase 14 (D-14-14~16, OUT-06)
       # ============================================================================
       #
       # 以下输出格式函数自 Phase 14 完成后冻结，后续 Phase 15+ 不修改核心字段结构:
       # - format_json_full(): 顶层字段固定
       # - format_json_summary(): 摘要字段固定
       # - build_status_info(): status 结构固定
       # - build_graphs_summary(): graphs_summary 结构固定
       #
       # 向后兼容承诺:
       # - 新字段可通过可选参数添加（如 include_schema）
       # - 字段语义不变（parent_class 含义保持）
       # - 废弃字段通过注释标记，不删除
       # ============================================================================

       def format_json_full(result: ParseResult, include_schema: bool = False) -> Dict:
           """..."""
       ```

    注意事项:
    - 移除 imports/soft_references/circular_deps/errors 字段
    - exports 仅保留 3 个字段（name/class/parent_class）
    - 保留 graphs_summary（已在 14-02 添加）
    - blueprint_metadata 精简为仅 parent_class + is_blueprint
    - errors 数组移除（status.status 已包含状态）
    - 添加 API 冻结注释块
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -v -k "summary" --tb=short</automated>
  </verify>
  <done>
    - format_json_summary 精简完成
    - 移除指定字段
    - exports 仅保留 name/class/parent_class
    - API 冻结注释存在
    - 测试验证精简格式正确
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 完善 CLI --summary 标志处理并验证互斥关系</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: --summary 标志输出精简 JSON
    - Test 2: --summary --schema 包含 _schema 字段
    - Test 3: --summary 与 --json/--text/--markdown 互斥
    - Test 4: CLI help 显示新标志说明
  </behavior>
  <action>
    Per D-14-17~19: CLI 扩展完善

    1. 验证 main() 中 --summary 处理逻辑正确（已在 14-03 修改）
    2. 确保 --summary 正确调用 format_json_summary(result, include_schema)
    3. 确保 args.schema 或 args.verbose 时 include_schema=True

    检查 main() 逻辑（约 line 5384-5391）:
    ```python
    elif args.summary:
        include_schema = args.schema or args.verbose
        output_str = json.dumps(format_json_summary(result, include_schema), indent=2, ensure_ascii=False)
    ```

    确保互斥组正确:
    - --json, --text, --summary, --markdown 四选一
    - --schema, --verbose 可与任何格式组合
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -v -k "cli" --tb=short</automated>
  </verify>
  <done>
    - --summary 标志正确输出精简 JSON
    - --summary --schema 包含 _schema
    - CLI 标志互斥关系正确
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 tests/test_phase14_output_formats.py 测试文件</name>
  <files>tests/test_phase14_output_formats.py</files>
  <action>
    创建 Phase 14 输出格式测试文件，覆盖 OUT-01~06 所有需求:

    ```python
    """
    tests/test_phase14_output_formats.py - Phase 14 输出格式优化测试

    测试覆盖:
    - OUT-01: status 字段（JSend 风格）
    - OUT-02: graphs_summary 顶层化
    - OUT-03: 摘要精简（70%+ token 减少）
    - OUT-04: Markdown 输出格式
    - OUT-05: _schema 字段语义注释
    - OUT-06: output_version + API 冻结
    """

    import pytest
    import json
    from uasset_read import (
        ParseResult,
        PackageFileSummary,
        ObjectExport,
        BlueprintMetadata,
        UEdGraph,
        UEdGraphNode,
        UEdGraphPin,
        FEdGraphPinType,
        PackageIndex,
        StatusInfo,
        build_status_info,
        build_graphs_summary,
        format_json_full,
        format_json_summary,
        format_markdown,
        build_schema_info,
    )


    class TestStatusField:
        """OUT-01: status 字段测试"""

        def test_status_info_success(self):
            """D-14-01: is_success=True + errors=[] → success"""
            result = ParseResult(is_success=True, errors=[])
            status = build_status_info(result)
            assert status.status == "success"
            assert status.message is None
            assert status.code is None

        def test_status_info_fail(self):
            """D-14-01: is_success=True + errors non-empty → fail"""
            result = ParseResult(is_success=True, errors=["Parse error at offset 100"])
            status = build_status_info(result)
            assert status.status == "fail"
            assert status.message == "Parse error at offset 100"
            assert status.code == "PARSE_ERROR"

        def test_status_info_error(self):
            """D-14-01: is_success=False → error"""
            result = ParseResult(is_success=False, errors=["Fatal error"])
            status = build_status_info(result)
            assert status.status == "error"
            assert status.message == "Fatal error"

        def test_format_json_full_contains_status(self):
            """D-14-03: 顶层 status 字段"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[]
            )
            output = format_json_full(result)
            assert "status" in output
            assert output["status"]["status"] == "success"
            assert output["output_version"] == "3.0"


    class TestGraphsSummary:
        """OUT-02: graphs_summary 顶层化测试"""

        def test_graphs_summary_structure(self):
            """D-14-04: graphs_summary 按图分组"""
            # 创建 mock graph
            graph = UEdGraph(
                graph_name="EventGraph",
                nodes=[]
            )
            result = ParseResult(graphs=[graph])
            summary = build_graphs_summary(result.graphs)
            assert len(summary) == 1
            assert summary[0]["graph"] == "EventGraph"

        def test_graphs_summary_empty(self):
            """空 graphs 返回空数组"""
            result = ParseResult(graphs=[])
            summary = build_graphs_summary(result.graphs)
            assert summary == []

        def test_format_json_full_contains_graphs_summary(self):
            """graphs_summary 为顶层字段"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[],
                graphs=[]
            )
            output = format_json_full(result)
            assert "graphs_summary" in output


    class TestSummaryCompact:
        """OUT-03: 摘要精简测试"""

        def test_summary_removes_dependency_fields(self):
            """D-14-07: 移除 imports/soft_references/circular_deps"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[],
                imports=[{"name": "TestClass"}],
                soft_references=[{"path": "/Game/Ref"}],
                circular_deps=[["A", "B"]]
            )
            output = format_json_summary(result)
            assert "imports" not in output
            assert "soft_references" not in output
            assert "circular_deps" not in output

        def test_summary_compact_exports(self):
            """D-14-08: exports 仅 name/class/parent_class"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[
                    ObjectExport(
                        object_name="TestClass_C",
                        class_index=PackageIndex(-1),
                        serial_size=1024,
                    )
                ],
                import_map=[]
            )
            output = format_json_summary(result)
            export = output["exports"][0]
            assert "name" in export
            assert "class" in export
            assert "serial_size" not in export
            assert "properties" not in export

        def test_summary_removes_errors_array(self):
            """D-14-07: errors 数组移除"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[],
                errors=["Warning: deprecated field"]
            )
            output = format_json_summary(result)
            assert "errors" not in output


    class TestMarkdownFormat:
        """OUT-04: Markdown 输出格式测试"""

        def test_markdown_header(self):
            """D-14-10: Markdown 以 "# Asset:" 开头"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test/TestAsset"),
                export_map=[]
            )
            md = format_markdown(result)
            assert md.startswith("# Asset: TestAsset")

        def test_markdown_table_format(self):
            """D-14-11: exports 使用表格格式"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[
                    ObjectExport(
                        object_name="TestClass_C",
                        class_index=PackageIndex(-1),
                    )
                ],
                import_map=[]
            )
            md = format_markdown(result)
            assert "| Name | Class | Parent |" in md
            assert "|------|-------|--------|" in md

        def test_markdown_mermaid(self):
            """D-14-12: execution_flows 使用 Mermaid"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[],
                graphs=[]
            )
            md = format_markdown(result)
            # 空 graphs 显示 No graphs
            assert "No graphs" in md or "```mermaid" in md


    class TestSchemaField:
        """OUT-05: _schema 字段测试"""

        def test_schema_field_structure(self):
            """D-14-13: _schema 包含字段描述"""
            schema = build_schema_info()
            assert "parent_class" in schema
            assert "variables" in schema
            assert "graphs_summary" in schema

        def test_schema_included_with_flag(self):
            """--schema 标志添加 _schema"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[]
            )
            output = format_json_full(result, include_schema=True)
            assert "_schema" in output

        def test_schema_not_included_by_default(self):
            """默认不包含 _schema"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[]
            )
            output = format_json_full(result, include_schema=False)
            assert "_schema" not in output


    class TestAPIFrozen:
        """OUT-06: API 冻结测试"""

        def test_output_version_present(self):
            """D-14-15: output_version 字段"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[]
            )
            output = format_json_full(result)
            assert output["output_version"] == "3.0"

        def test_output_version_in_summary(self):
            """摘要模式也包含 output_version"""
            result = ParseResult(
                is_success=True,
                summary=PackageFileSummary(package_name="/Game/Test"),
                export_map=[]
            )
            output = format_json_summary(result)
            assert output["output_version"] == "3.0"
    ```

    注意事项:
    - 测试文件遵循现有 tests/ 目录命名规范
    - 测试类按需求 ID 组织
    - 每个测试引用对应的决策 ID
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase14_output_formats.py -v --tb=short</automated>
  </verify>
  <done>
    - test_phase14_output_formats.py 文件存在
    - 测试覆盖 OUT-01~06 所有需求
    - 所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | 本 phase 为纯输出格式优化，无安全边界变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 本 phase 无安全相关变更 |
</threat_model>

<verification>
**Goal-backward verification:**
1. --summary 输出 token 减少 70%+（移除指定字段）
2. API 冻结注释存在
3. output_version 字段值 "3.0"
4. 测试覆盖所有新增功能

**自动化测试:**
```bash
python -m pytest tests/test_phase14_output_formats.py tests/test_output_formatting.py -v --tb=short
```
</verification>

<success_criteria>
- [ ] format_json_summary 精简完成（移除 6 个字段）
- [ ] exports 仅保留 name/class/parent_class
- [ ] API 冻结注释添加
- [ ] CLI --summary 标志正确处理
- [ ] test_phase14_output_formats.py 创建并测试通过
- [ ] 所有 OUT-01~06 需求覆盖
</success_criteria>

<output>
完成后创建 `.planning/phases/14-output-format-optimization/14-04-SUMMARY.md`
</output>