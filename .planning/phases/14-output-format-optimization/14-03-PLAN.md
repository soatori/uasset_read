---
phase: 14-output-format-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - uasset_read.py
autonomous: true
requirements:
  - OUT-04
  - OUT-05
user_setup: []

must_haves:
  truths:
    - "用户可以通过 --markdown 标志获取 Markdown 格式输出"
    - "Markdown 输出包含 Asset Overview / Blueprint Details / Graph Summary 三节"
    - "exports 和属性列表使用 Markdown 表格格式"
    - "execution_flows 使用 Mermaid 流程图语法展示调用链"
    - "用户可以通过 --schema 标志获取字段语义注释"
  artifacts:
    - path: "uasset_read.py"
      provides: "Markdown 格式化函数"
      contains: "def format_markdown"
      contains: '"_schema"'
      contains: "--markdown"
  key_links:
    - from: "format_markdown()"
      to: "build_graphs_summary()"
      via: "Mermaid 流程图生成"
      pattern: "mermaid.*graph"
    - from: "CLI argparse"
      to: "format_markdown()"
      via: "--markdown 标志"
      pattern: "--markdown.*format_markdown"
---

<objective>
实现 Markdown 输出格式和 _schema 字段语义注释（OUT-04, OUT-05）

Purpose: 同时友好人类和 AI 阅读，字段语义注释帮助理解结构
Output: format_markdown() 函数 + _schema 字段 + CLI --markdown/--schema 标志
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-output-format-optimization/14-CONTEXT.md

# 现有代码接口（提取关键类型）

From uasset_read.py:5109-5145:
```python
def format_text_full(result: ParseResult) -> str:
    """Format YAML-style text output"""
    lines = []
    if result.summary:
        package_name = result.summary.package_name or "Unknown"
        lines.append(f"Package: {package_name}")
        # YAML style hierarchy
```

From CONTEXT.md D-14-10~12:
```markdown
# Asset: BP_FirstPersonCharacter

## Asset Overview
| Field | Value |
|-------|-------|
| Package | /Game/FirstPerson/Blueprints |
| Version | UE 5.5 |

## Blueprint Details
| Field | Value |
|-------|-------|
| Parent Class | ACharacter |
| Variables | 5 (2 components, 3 regular) |

## Graph Summary
### EventGraph
```mermaid
graph LR
  EventBeginPlay --> PrintString
```
```
</context>

<interfaces>
<!-- Executor 需要的关键接口 -->

## Markdown 三节结构（D-14-10）

1. **Asset Overview**: 基本信息（package_name, version, status）
2. **Blueprint Details**: 蓝图元数据（parent_class, variables 统计）
3. **Graph Summary**: 执行流概览（Mermaid 流程图）
4. **Exports**: 导出列表（Markdown 表格）

## Mermaid 流程图格式（D-14-12）

```mermaid
graph LR
  EventBeginPlay --> PrintString
  PrintString --> SetLifeSpan
```

- 使用 LR 方向（Claude's Discretion）
- 事件节点 → 函数节点 → 函数节点 链式展示

## _schema 字段结构（D-14-13）

```json
"_schema": {
  "parent_class": "蓝图继承的父类名称",
  "variables": "蓝图变量列表（名称、类型、默认值）",
  "execution_flows": "函数调用链路径（事件 → 函数序列）",
  "graphs_summary": "顶层化的图执行流概览"
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 format_markdown() 函数实现 Markdown 输出</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: Markdown 输出以 "# Asset: {name}" 开头
    - Test 2: 包含 "## Asset Overview" 和 "## Blueprint Details" 节
    - Test 3: exports 使用 Markdown 表格格式
    - Test 4: graphs_summary 使用 mermaid 流程图语法
    - Test 5: 空 graphs 输出时 Graph Summary 节显示 "No graphs"
  </behavior>
  <action>
    Per D-14-10~12: Markdown 格式实现

    1. 创建 format_markdown() 函数（放在 format_text_full 后，约 line 5200）:
       ```python
       def format_markdown(result: ParseResult) -> str:
           """
           格式化 Markdown 输出（D-14-10~12, OUT-04）。

           三节结构 + 表格优先 + Mermaid 流程图。

           Args:
               result: ParseResult from parse_uasset()

           Returns:
               str: Markdown 格式文本
           """
           lines = []

           # 标题
           asset_name = result.summary.package_name if result.summary else "Unknown"
           asset_name = asset_name.split("/")[-1] if "/" in asset_name else asset_name
           lines.append(f"# Asset: {asset_name}")
           lines.append("")

           # === Asset Overview ===
           lines.append("## Asset Overview")
           lines.append("| Field | Value |")
           lines.append("|-------|-------|")
           if result.summary:
               lines.append(f"| Package | {result.summary.package_name} |")
               ue_version = result.summary.file_version_ue5 or result.summary.file_version_ue4
               lines.append(f"| Version | UE {ue_version} |")
           # Status
           status_info = build_status_info(result)
           lines.append(f"| Status | {status_info.status} |")
           if status_info.message:
               lines.append(f"| Message | {status_info.message} |")
           lines.append("")

           # === Blueprint Details ===
           if result.blueprint and result.blueprint.is_blueprint:
               lines.append("## Blueprint Details")
               lines.append("| Field | Value |")
               lines.append("|-------|-------|")
               lines.append(f"| Parent Class | {result.blueprint.parent_class or 'Unknown'} |")
               # Variables 统计
               var_count = len(result.blueprint.variables) if result.blueprint.variables else 0
               comp_count = sum(1 for v in result.blueprint.variables if v.is_component) if result.blueprint.variables else 0
               lines.append(f"| Variables | {var_count} ({comp_count} components, {var_count - comp_count} regular) |")
               lines.append("")

           # === Graph Summary ===
           graphs_summary = build_graphs_summary(result.graphs)
           if graphs_summary:
               lines.append("## Graph Summary")
               for graph_summary in graphs_summary:
                   graph_name = graph_summary.get("graph", "Unknown")
                   lines.append(f"### {graph_name}")

                   # Mermaid 流程图
                   flows = graph_summary.get("execution_flows", [])
                   if flows:
                       lines.append("```mermaid")
                       lines.append("graph LR")
                       for flow in flows:
                           event = flow.get("event", "Unknown")
                           calls = flow.get("calls", [])
                           if calls:
                               # 第一个节点
                               lines.append(f"  {event} --> {calls[0].split('(')[0]}")
                               # 链式连接
                               for i in range(len(calls) - 1):
                                   fn1 = calls[i].split('(')[0]
                                   fn2 = calls[i+1].split('(')[0]
                                   lines.append(f"  {fn1} --> {fn2}")
                       lines.append("```")
                       lines.append("")
           else:
               lines.append("## Graph Summary")
               lines.append("No graphs in this asset.")
               lines.append("")

           # === Exports ===
           if result.export_map:
               lines.append("## Exports")
               lines.append("| Name | Class | Parent |")
               lines.append("|------|-------|--------|")
               for i, exp in enumerate(result.export_map):
                   name = exp.object_name
                   cls = get_asset_class(exp, result.import_map, result.export_map)
                   parent = ""
                   if result.blueprint and i == 0:
                       parent = result.blueprint.parent_class or ""
                   lines.append(f"| {name} | {cls} | {parent} |")
               lines.append("")

           return "\n".join(lines)
       ```

    2. 在 __all__ 导出列表中添加 format_markdown

    注意事项:
    - 资产名称从 package_name 提取（取最后一段）
    - Mermaid 使用 graph LR 方向（Claude's Discretion）
    - 函数调用链用 --> 连接
    - 空 graphs 显示 "No graphs" 文本
    - 表格列: Name | Class | Parent（D-14-11）
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -v -k "markdown" --tb=short</automated>
  </verify>
  <done>
    - format_markdown() 函数存在
    - 输出格式符合三节结构
    - Mermaid 流程图语法正确
    - format_markdown 在 __all__ 中导出
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 build_schema_info() 函数并添加 CLI --markdown/--schema 标志</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: --schema 标志输出 _schema 字段
    - Test 2: --markdown 标志输出 Markdown 格式
    - Test 3: --markdown 与 --json 互斥
    - Test 4: _schema 字段包含 parent_class/variables 等字段描述
  </behavior>
  <action>
    Per D-14-13, D-14-17~19: Schema 字段和 CLI 扩展

    1. 创建 build_schema_info() 函数（约 line 4912）:
       ```python
       def build_schema_info() -> Dict[str, str]:
           """
           构建字段语义注释（D-14-13, OUT-05）。

           仅在 --verbose 或 --schema 标志时输出。

           Returns:
               Dict[str, str]: 字段描述映射
           """
           return {
               "status": "解析结果状态（success/fail/error）",
               "output_version": "输出格式 API 版本标识",
               "summary": "资产基本信息（版本、包名）",
               "exports": "导出对象列表（蓝图、组件等）",
               "blueprint_metadata": "蓝图元数据（父类、变量、图）",
               "parent_class": "蓝图继承的父类名称",
               "variables": "蓝图变量列表（名称、类型、默认值、元数据）",
               "is_component": "变量是否为组件类型（SkeletalMeshComponent 等）",
               "graphs": "蓝图执行图数据（完整节点/引脚信息）",
               "graphs_summary": "顶层化的图执行流概览（事件→函数调用链）",
               "execution_flows": "函数调用链路径",
               "imports": "ImportMap 依赖列表（外部对象引用）",
               "soft_references": "SoftObjectPaths 软引用列表",
               "circular_deps": "检测到的循环依赖路径",
           }
       ```

    2. 修改 format_json_full() 添加可选 _schema 字段:
       - 仅在 verbose 模式时添加
       - 由于 main() 中 args.verbose 可用，需要参数传递

       暂时简化：format_json_full 添加可选参数 include_schema=False:
       ```python
       def format_json_full(result: ParseResult, include_schema: bool = False) -> Dict:
           output = {
               "status": asdict(build_status_info(result)),
               "output_version": "3.0",
               ...existing fields...
           }
           if include_schema:
               output["_schema"] = build_schema_info()
           return output
       ```

    3. 修改 create_parser() CLI（约 line 5317）添加新标志:
       ```python
       # 修改互斥组添加 --markdown
       group = parser.add_mutually_exclusive_group(required=False)
       group.add_argument('--json', action='store_true', help='Output full JSON structure')
       group.add_argument('--text', action='store_true', help='Output YAML-style text (default)')
       group.add_argument('--summary', action='store_true', help='Output compact summary format')
       group.add_argument('--markdown', action='store_true', help='Output Markdown format')  # D-14-17

       # 添加 --schema 标志（D-14-19）
       parser.add_argument('--schema', action='store_true', help='Include field semantic annotations (_schema)')
       ```

    4. 修改 main() 函数处理新标志（约 line 5370）:
       ```python
       # 在 --graph 逻辑后添加 --markdown 处理
       if args.markdown:
           output_str = format_markdown(result)
       elif args.json:
           include_schema = args.schema or args.verbose
           output_str = json.dumps(format_json_full(result, include_schema), indent=2, ensure_ascii=False)
       elif args.summary:
           include_schema = args.schema or args.verbose
           output_str = json.dumps(format_json_summary(result, include_schema), indent=2, ensure_ascii=False)
       else:
           # Default: --text
           output_str = format_text_full(result)
       ```

    注意事项:
    - --markdown 与 --json/--text/--summary 互斥
    - --schema 可与任何格式组合（但主要用于 JSON）
    - _schema 字段仅在 include_schema=True 时添加
    - 修改 format_json_summary 同样添加 include_schema 参数
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -v -k "schema or markdown" --tb=short</automated>
  </verify>
  <done>
    - build_schema_info() 函数存在
    - --markdown 标志可用
    - --schema 标志可用
    - format_json_full/format_json_summary 支持 include_schema 参数
    - CLI 正确处理新标志组合
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | 本 phase 为纯输出格式优化，无安全边界变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 本 phase 无安全相关变更 |
</threat_model>

<verification>
**Goal-backward verification:**
1. --markdown 标志输出 Markdown 格式
2. Markdown 包含三节结构 + Mermaid 流程图
3. --schema 标志添加 _schema 字段
4. 标志互斥关系正确

**自动化测试:**
```bash
python -m pytest tests/test_output_formatting.py -v --tb=short
```
</verification>

<success_criteria>
- [ ] format_markdown() 函数创建完成
- [ ] build_schema_info() 函数创建
- [ ] --markdown 标志添加
- [ ] --schema 标志添加
- [ ] format_json_* 支持 include_schema 参数
- [ ] 测试覆盖 Markdown 和 Schema 输出
</success_criteria>

<output>
完成后创建 `.planning/phases/14-output-format-optimization/14-03-SUMMARY.md`
</output>