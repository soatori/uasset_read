---
phase: 14-output-format-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - uasset_read.py
autonomous: true
requirements:
  - OUT-01
  - OUT-06
user_setup: []

must_haves:
  truths:
    - "用户可以在 JSON 输出中看到顶层 status 字段"
    - "status 字段包含 status/message/code 三元结构"
    - "用户可以看到 output_version 字段标识 API 版本"
    - "success/fail/error 三元分类正确区分解析状态"
  artifacts:
    - path: "uasset_read.py"
      provides: "StatusInfo dataclass + format_json_* 函数修改"
      contains: "class StatusInfo"
      contains: "output_version"
  key_links:
    - from: "format_json_full()"
      to: "StatusInfo"
      via: "顶层 status 字段组装"
      pattern: '"status": build_status_info'
---

<objective>
为 JSON 输出添加 JSend 风格 status 字段和 output_version 标识（OUT-01, OUT-06 部分）

Purpose: 让 AI 一眼判断解析结果状态，API 版本标识供 skill 依赖
Output: StatusInfo dataclass + format_json_full/format_json_summary 修改
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-output-format-optimization/14-CONTEXT.md

# 现有代码接口（提取关键类型）

From uasset_read.py:
```python
class ParseResult:
    summary: Optional[PackageFileSummary] = None
    errors: List[str] = field(default_factory=list)
    is_success: bool = False
    warnings: List[str] = field(default_factory=list)
```

From uasset_read.py:4916-4955:
```python
def format_json_full(result: ParseResult) -> Dict:
    return {
        "summary": summary_dict,
        "exports": format_exports_list(result),
        "blueprint_metadata": ...,
        "graphs": format_graphs_json(result.graphs),
        "imports": result.imports,
        "soft_references": result.soft_references,
        "circular_deps": result.circular_deps,
        "errors": result.errors
    }
```
</context>

<interfaces>
<!-- Executor 需要的关键接口 -->

## StatusInfo 结构（新建）

```python
@dataclass
class StatusInfo:
    """JSend 风格 status 字段（D-14-02）"""
    status: str      # "success" | "fail" | "error"
    message: Optional[str] = None
    code: Optional[str] = None
```

## 三元分类逻辑（D-14-01）

| is_success | errors | warnings | status |
|------------|--------|----------|--------|
| True | [] | any | "success" |
| True | non-empty | any | "fail" |
| False | any | any | "error" |

## 错误码枚举（Claude's Discretion）

```python
# error.code 值
PARSE_ERROR = "PARSE_ERROR"
FILE_NOT_FOUND = "FILE_NOT_FOUND"
VERSION_ERROR = "VERSION_ERROR"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 StatusInfo dataclass 并集成到 format_json_* 函数</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: format_json_full 返回顶层 status 对象
    - Test 2: is_success=True + errors=[] → status="success"
    - Test 3: is_success=True + errors non-empty → status="fail"
    - Test 4: is_success=False → status="error"
    - Test 5: output_version 字段存在且值为 "3.0"
  </behavior>
  <action>
    Per D-14-01~03: 三元分类 status 字段实现

    1. 在 dataclass 定义区域（约 line 1300-1360）添加 StatusInfo dataclass:
       ```python
       @dataclass
       class StatusInfo:
           """JSend 风格 status 字段（D-14-02, OUT-01）"""
           status: str      # "success" | "fail" | "error"
           message: Optional[str] = None
           code: Optional[str] = None
       ```

    2. 创建 build_status_info() 函数（放在 format 函数区域前，约 line 4910）:
       ```python
       def build_status_info(result: ParseResult) -> StatusInfo:
           """
           构建 status 字段（D-14-01）。

           三元分类:
           - success: is_success=True, errors=[]
           - fail: is_success=True, errors non-empty（部分结果可用）
           - error: is_success=False（严重错误）

           Args:
               result: ParseResult 对象

           Returns:
               StatusInfo: status 对象
           """
           if result.is_success:
               if not result.errors:
                   return StatusInfo(status="success")
               else:
                   # D-14-01: 有错误但部分结果可用 → fail
                   message = result.errors[0] if result.errors else None
                   return StatusInfo(status="fail", message=message, code="PARSE_ERROR")
           else:
               # is_success=False → error
               message = result.errors[0] if result.errors else "Unknown error"
               return StatusInfo(status="error", message=message, code="PARSE_ERROR")
       ```

    3. 修改 format_json_full() 函数（约 line 4945）添加 status 和 output_version:
       - 在 return 语句开头添加 status 字段
       - 添加 output_version: "3.0" 顶层字段
       ```python
       return {
           "status": asdict(build_status_info(result)),  # D-14-03: 顶层位置
           "output_version": "3.0",  # D-14-15: API 版本标识
           "summary": summary_dict,
           ...existing fields...
       }
       ```

    4. 同样修改 format_json_summary() 函数（约 line 5100）添加 status 和 output_version

    5. 在 __all__ 导出列表中添加 StatusInfo（约 line 70-90）

    注意事项:
    - status 字段必须放在 return dict 的第一个位置（顶层显眼）
    - asdict(StatusInfo) 会自动处理 None → null
    - 不要修改 errors 字段的现有位置（保持向后兼容）
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -v -k "status" --tb=short</automated>
  </verify>
  <done>
    - StatusInfo dataclass 存在于 uasset_read.py
    - format_json_full/format_json_summary 返回包含 status 和 output_version 的 dict
    - 测试验证三元分类逻辑正确
    - StatusInfo 在 __all__ 中导出
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | 本 phase 为纯输出格式优化，无安全边界变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 本 phase 无安全相关变更 |
</threat_model>

<verification>
**Goal-backward verification:**
1. 运行解析后 JSON 输出包含顶层 status 字段
2. status 字段结构符合 JSend 规范（status/message/code）
3. output_version 字段值为 "3.0"

**自动化测试:**
```bash
python -m pytest tests/test_output_formatting.py -v --tb=short
```
</verification>

<success_criteria>
- [ ] StatusInfo dataclass 创建完成
- [ ] build_status_info() 函数实现三元分类
- [ ] format_json_full() 包含 status + output_version 字段
- [ ] format_json_summary() 包含 status + output_version 字段
- [ ] __all__ 导出 StatusInfo
- [ ] 测试覆盖 status 三元分类逻辑
</success_criteria>

<output>
完成后创建 `.planning/phases/14-output-format-optimization/14-01-SUMMARY.md`
</output>