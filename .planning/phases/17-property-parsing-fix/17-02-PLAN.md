---
phase: 17-property-parsing-fix
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified: [uasset_read.py]
autonomous: true
requirements: [FIX-04, FIX-05]
user_setup: []

must_haves:
  truths:
    - "属性数据前正确读取 SerializationControlExtensions 头部"
    - "PropertyTag 读取不再因头部跳过而位置错位"
    - "exceeds_remaining 错误消失（Size 字段正确读取）"
  artifacts:
    - path: "uasset_read.py"
      function: "parse_properties_from_export"
      contains: "serialization_control"
      pattern: "read_u8.*serialization"
  key_links:
    - from: "parse_properties_from_export()"
      to: "Class.cpp SerializationControlExtensions"
      via: "头部读取"
      pattern: "UE5_PROPERTY_TAG_EXTENSION"
---

<objective>
添加 SerializationControlExtensions 头部处理（D-02），使属性标签读取位置正确。

Purpose: UE 5.11+ 在属性数据前添加 SerializationControlExtensions 头部，当前代码跳过导致 PropertyTag 位置错位。
Output: parse_properties_from_export() 正确读取头部，PropertyTag 从正确位置开始。

根因验证（RESEARCH.md D-02）:
- UE 源码 Class.cpp 第 1627-1654 行：当 UE5 >= 1011 时序列化头部
- EClassSerializationControlExtension (u8): 控制扩展标志
- 条件字段：OverriddenPropertyOperation (u8) — 如果标志包含 0x02
- 版本阈值: UE5_PROPERTY_TAG_EXTENSION = 1011
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-property-parsing-fix/17-CONTEXT.md
@.planning/phases/17-property-parsing-fix/17-RESEARCH.md
@.planning/phases/17-property-parsing-fix/17-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From uasset_read.py (第 79-80 行):
```python
UE5_SCRIPT_SERIALIZATION_OFFSET = 1010        # SCRIPT_SERIALIZATION_OFFSET
UE5_PROPERTY_TAG_EXTENSION = 1011             # PROPERTY_TAG_EXTENSION_AND_OVERRIDABLE_SERIALIZATION
```

From uasset_read.py (FArchive 类):
```python
def read_u8(self) -> int:
    """读取 unsigned 8-bit integer（字节序无关）"""
    return struct.unpack('<B', self.read(1))[0]
```

From Class.cpp (UE 源码参考):
```cpp
// 第 1559-1571 行：EClassSerializationControlExtension 定义
enum class EClassSerializationControlExtension : uint8
{
    NoExtension                 = 0x00,
    ReserveForFutureUse        = 0x01,
    OverridableSerializationInformation = 0x02,
};

// 第 1627-1654 行：序列化逻辑
if (UnderlyingArchive.UEVer() >= EUnrealEngineObjectUE5Version::PROPERTY_TAG_EXTENSION_AND_OVERRIDABLE_SERIALIZATION)
{
    Slot << SA_ATTRIBUTE(TEXT("SerializationControlExtensions"), SerializationControl);
    if (EnumHasAnyFlags(SerializationControl, EClassSerializationControlExtension::OverridableSerializationInformation))
    {
        Slot << SA_ATTRIBUTE(TEXT("OverridableOperation"), Operation);
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 SerializationControlExtensions 头部读取</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py (第 4343-4420 行 parse_properties_from_export 函数)
    - uasset_read.py (第 79-80 行 UE5 版本常量)
    - uasset_read.py (第 340-343 行 FArchive.read_u8)
    - .planning/phases/17-property-parsing-fix/17-RESEARCH.md (D-02 根因验证)
  </read_first>
  <action>
    在 parse_properties_from_export() 中，偏移计算后、属性循环前添加头部读取：

    在第 4343 行 seek 之后、while True 循环之前添加：

    ```python
    # D-02: SerializationControlExtensions 头部处理
    # 参考: Class.cpp 第 1627-1654 行
    # 当 UE5 >= PROPERTY_TAG_EXTENSION (1011) 时，属性数据前有额外头部
    if summary.file_version_ue5 >= UE5_PROPERTY_TAG_EXTENSION:
        # EClassSerializationControlExtension (u8)
        serialization_control = archive.read_u8()

        # OverridableSerializationInformation 标志 (0x02)
        if serialization_control & 0x02:
            # EOverriddenPropertyOperation (u8) — 仅读取用于位置同步
            overridden_operation = archive.read_u8()
            # 注意：具体语义不解析，仅跳过字节
    ```

    关键点：
    - UE5_PROPERTY_TAG_EXTENSION = 1011 已在第 80 行定义
    - 头部在属性循环（while True）之前读取
    - OverridableSerializationInformation 标志值为 0x02（来自 Class.cpp 第 1559-1571 行）
    - 仅读取字节用于位置同步，不解析具体语义
  </action>
  <acceptance_criteria>
    - uasset_read.py parse_properties_from_export 函数包含 "serialization_control"
    - uasset_read.py 包含版本条件 "file_version_ue5 >= UE5_PROPERTY_TAG_EXTENSION"
    - uasset_read.py 包含标志检查 "serialization_control & 0x02"
    - 头部读取代码位于 while True 循环之前
  </acceptance_criteria>
  <verify>
    <automated>python -c "from uasset_read import parse_properties_from_export; import inspect; src = inspect.getsource(parse_properties_from_export); assert 'serialization_control' in src, '头部读取未添加'"</automated>
  </verify>
  <done>
    - parse_properties_from_export() 在属性循环前正确读取头部
    - 版本条件检查正确（UE5 >= 1011）
    - OverridableSerializationInformation 标志处理正确
    - 代码无语法错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加头部读取单元测试</name>
  <files>tests/test_property_parsing.py</files>
  <read_first>
    - tests/test_property_parsing.py (现有测试结构)
    - uasset_read.py (第 340-343 行 FArchive.read_u8)
    - uasset_read.py (第 79-80 行 UE5 版本常量)
  </read_first>
  <behavior>
    - Test 1: UE5 version >= 1011 时，头部读取消耗 1-2 bytes
    - Test 2: serialization_control = 0x00 时，仅读取 1 byte
    - Test 3: serialization_control = 0x02 时，读取 2 bytes
  </behavior>
  <action>
    在 tests/test_property_parsing.py 中添加头部读取测试：

    ```python
    # ============================================================================
    # Phase 17 D-02: SerializationControlExtensions 头部测试
    # ============================================================================

    def test_serialization_control_extensions_no_extension():
        """D-02: serialization_control = 0x00 时，仅读取 1 byte"""
        # 构造 mock 数据：serialization_control = 0x00
        data = bytes([0x00])  # NoExtension
        archive = create_mock_archive_with_data(data)

        serialization_control = archive.read_u8()
        assert serialization_control == 0x00
        assert not (serialization_control & 0x02)  # 无 OverridableSerializationInformation
        # 仅读取 1 byte，位置正确
        assert archive.tell() == 1

    def test_serialization_control_extensions_with_overridable():
        """D-02: serialization_control = 0x02 时，读取 2 bytes"""
        # 构造 mock 数据：serialization_control = 0x02 + overridden_operation
        data = bytes([0x02, 0x00])  # OverridableSerializationInformation + operation
        archive = create_mock_archive_with_data(data)

        serialization_control = archive.read_u8()
        assert serialization_control == 0x02
        assert (serialization_control & 0x02)  # 有 OverridableSerializationInformation

        # 需要读取 overridden_operation
        overridden_operation = archive.read_u8()
        assert archive.tell() == 2  # 总共读取 2 bytes

    def test_serialization_control_extensions_version_threshold():
        """D-02: UE5_PROPERTY_TAG_EXTENSION = 1011 版本阈值验证"""
        from uasset_read import UE5_PROPERTY_TAG_EXTENSION
        assert UE5_PROPERTY_TAG_EXTENSION == 1011
    ```

    注意：
    - 使用 create_mock_archive_with_data 辅助函数（已存在）
    - 测试验证字节消耗，确保位置正确
  </action>
  <acceptance_criteria>
    - tests/test_property_parsing.py 包含函数 test_serialization_control_extensions_no_extension
    - tests/test_property_parsing.py 包含函数 test_serialization_control_extensions_with_overridable
    - tests/test_property_parsing.py 包含函数 test_serialization_control_extensions_version_threshold
    - 测试文件包含注释 "# Phase 17 D-02"
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_property_parsing.py::test_serialization_control_extensions_no_extension -x -v</automated>
  </verify>
  <done>
    - 三个头部读取测试函数添加完成
    - 所有测试通过
    - 测试覆盖 NoExtension (0x00) 和 OverridableSerializationInformation (0x02) 两种情况
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件数据 → 解析器 | 二进制数据从文件读取，头部字节需在有效范围内 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | serialization_control | mitigate | FArchive.read_u8 已有边界验证 |
| T-17-04 | Denial of Service | 头部读取 | accept | 仅读取 1-2 bytes，无风险 |
</threat_model>

<verification>
1. 头部读取代码添加完成
2. 单元测试添加并通过
3. 与 Plan 01 偏移修复配合工作
</verification>

<success_criteria>
- parse_properties_from_export() 正确读取 SerializationControlExtensions 头部
- 版本条件检查正确（UE5 >= 1011）
- OverridableSerializationInformation 标志处理正确
- 单元测试通过
- 解决 FIX-04 (negative_size) 和 FIX-05 (exceeds_remaining) 的头部错位原因
</success_criteria>

<output>
完成后创建 `.planning/phases/17-property-parsing-fix/17-02-SUMMARY.md`
</output>