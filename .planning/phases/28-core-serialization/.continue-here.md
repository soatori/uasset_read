---
context: phase
phase: 28-core-serialization
task: 5
total_tasks: 6
status: in_progress
last_updated: 2026-05-11T22:45:00.000Z
---

# Work Handoff — Phase 28a 测试基线修复

**Date:** 2026-05-11
**Branch:** v2.0-dev
**Last Commit:** 1c6b4e8 fix(phase-28a): FunctionReference 解析 — StructProperty 嵌套 PropertyTags

## Critical Analysis Discovery

### Pin解析位置验证结果

**手动测试验证（正确的位置流程）：**
```
Position 0x16fa1: DefaultValue (空字符串)
  After: 0x16fa5 (读取4字节: length=0)
Position 0x16fa5: AutogeneratedDefaultValue ("0.0")
  After: 0x16fad (读取8字节: length=4 + UTF-8 content)
Position 0x16fad: [错误! 应该从0x16fb5开始]

Position 0x16fb5: DefaultObject (手动测试正确)
  Result: 0
  After: 0x16fb9
Position 0x16fb9: DefaultTextValue flags
  Flags: 0
```

**调试输出显示的位置（有差异）：**
```
DefaultValue: '', offset now: 0x16fa5  ✓ 正确
AutogeneratedDefaultValue: '0.0', offset now: 0x16fad ✓ 正确
DefaultObject: 4, offset now: 0x16fb1  ✗ 差异! 手动测试是0x16fb5
DefaultTextValue: flags=3157552, start=0x16fb1 ✗ 导致UTF-16错误
```

### 关键发现

**位置差异4字节（调试输出 vs 手动测试）：**
- 调试显示 DefaultObject 结束于 0x16fb1
- 手动测试 DefaultObject 从 0x16fb5 开始
- 差异原因：可能是 Pin #3 之前的某个 Pin 解析位置偏差累积

**Pin #3 (Val pin) 解析流程：**
- PinType 正确结束于 0x16fa1
- DefaultValue 是空字符串（正确）
- AutogeneratedDefaultValue 是 "0.0"（正确）
- **问题点：DefaultObject 位置错误**

### 已修复问题

1. **StructProperty 嵌套 PropertyTags** — FunctionReference value 包含嵌套 PropertyTags
2. **"None" PropertyTag 特殊处理** — 仅 Name (8 bytes)
3. **BoolProperty size=0 处理** — 值在 flags.bool_val

### 测试状态

**当前**: 13 failed, 398 passed, 47 skipped (从 17 failed 降到 13)

### 剩余失败分类

| 测试 | 根因 | 状态 |
|------|------|------|
| test_phase21_verification.py::test_function_reference_member_name | Pin fallback导致node_data=dict | 需修复位置 |
| test_skill_integration.py::test_execution_flows_contains_function_name | function_name为空 | 同上 |
| test_property_parsing.py (11个) | 测试数据格式错误(FString vs FPropertyTypeName) | 需更新测试数据 |

## 下一步

1. **验证调试输出 vs 实际解析位置**
   - 对比 read_ue_graph_pin 函数中的位置计算
   - 确认是否某个字段多读或少读了字节

2. **定位 4 字节差异来源**
   - 检查 AutogeneratedDefaultValue 和 DefaultObject 之间的字段
   - 可能是某个条件分支导致额外读取

3. **修复并验证**
   - 目标: 0 failed tests

## UE 源码参考

| 文件 | 内容 |
|------|------|
| EdGraphPin.cpp | Pin 序列化顺序 |
| MemberReference.h | FMemberReference USTRUCT |
| PropertyTag.cpp | "None" PropertyTag 特殊格式 |

---
*Resume with: `/gsd-resume-work`*