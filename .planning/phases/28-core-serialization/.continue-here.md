---
context: phase
phase: 28-core-serialization
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-05-11T21:30:00.000Z
---

# Work Handoff — Phase 28a 测试基线修复

**Date:** 2026-05-11
**Branch:** v2.0-dev
**Last Commit:** 1c6b4e8 fix(phase-28a): FunctionReference 解析 — StructProperty 嵌套 PropertyTags

## 已修复问题

### 核心修复 (Commit 1c6b4e8)
1. **StructProperty 嵌套 PropertyTags** — FunctionReference value 包含嵌套 PropertyTags (MemberName, bSelfContext 等)，而非直接 struct bytes
2. **"None" PropertyTag 特殊处理** — UE 的 "None" PropertyTag 仅包含 Name (8 bytes)，无 TypeName/Size/Flags
3. **BoolProperty size=0 处理** — UE5 格式中 bool 值存储在 flags.bool_val 中

### 之前的修复
1. **CustomVersion GUID 字节序** — 大端序 → 小端序
2. **pins_offset 计算公式** — 固定公式 `script_serial_offset + script_serial_size`
3. **FText bHasCultureInvariantString** — uint8 → UBOOL (4 bytes)
4. **FText flags=0 处理** — 添加特殊跳过逻辑

## 测试进展

**当前**: 13 failed, 398 passed, 47 skipped (从 17 failed 降到 13)

### 已通过的测试 (之前失败)
- test_jump_started_flow ✓
- test_jump_completed_flow ✓
- test_execution_flows_exist ✓
- test_node_guid_present ✓
- test_event_reference_present ✓

### 剩余失败 (2 个与 FunctionReference 相关)
- test_function_reference_member_name — node_data 为 dict (fallback)，因 Pin/FText 解析失败
- test_execution_flows_contains_function_name — function_name 为空，同上

### PropertyTag 测试失败 (11 个)
这些测试使用错误的测试数据格式 (FString 而非 FPropertyTypeName nodes)。需要更新测试数据以匹配 UE5 >= 1012 格式。

## 根因分析

### FunctionReference 仍失败的原因
节点解析在 **Pin 阶段失败**（FText UTF-16 string length too large），导致进入 fallback 模式。Fallback 创建的 node_data 是 dict `{"node_name": "xxx"}`，而非 K2NodeCallFunction dataclass。

### Pin/FText 解析问题
- `UTF-16 string length 1011044606 too large` 错误
- 发生在读取 PinFriendlyName (FText) 时
- 可能是位置错误导致读取了错误数据

## 下一步

1. **修复 Pin/FText 解析** — 解决位置管理问题，让更多节点成功解析
2. **更新 PropertyTag 测试数据** — 使用 FPropertyTypeName nodes 格式
3. **验证测试基线** — 目标: 0 failed

## UE 源码参考

| 文件 | 内容 |
|------|------|
| MemberReference.h | FMemberReference 是 USTRUCT，字段为 UPROPERTY(SaveGame) |
| PropertyTag.cpp | "None" PropertyTag 仅 Name，无后续字段 |
| PropertyTypeName.cpp | FPropertyTypeName 节点格式 (FName + inner_count) |
| EdGraphNode.cpp:212-261 | 序列化顺序: Super::Serialize → Pins |

---
*Resume with: `/gsd-resume-work`*