---
context: phase
phase: 28-core-serialization
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-05-11T14:30:00.000Z
---

# Work Handoff — Phase 28a 测试基线修复

**Date:** 2026-05-11
**Branch:** v2.0-dev
**Last Commit:** 5ce03dd fix(phase-28a): resolve merge conflicts and API key inconsistencies

## Current Position

**Milestone:** v6.0 模块化重构 (Phases 27-34)
**Phase Status:**
- Phase 27: 项目结构初始化 — Complete
- Phase 28: 核心序列化模块 — Complete (ce110d2)
- **Phase 28a: 测试基线修复 — In Progress (9→4 failures)**
- Phase 29-34: Not started

## What Was Done This Session

1. **Phase 28提交** — 核心序列化模块 (archive.py, serializers/)
2. **Stash合并** — pin解析容错修复、SourceIndex fallback、FText处理
3. **API键名统一** — `graph` → `graph_name`
4. **测试修复进展**:
   - test_resolve_class_name_from_import ✓ (测试bug)
   - test_graphs_summary_contains_eventgraph ✓ (API键名)
   - test_function_reference_member_name ✓ (stash修复)
   - test_output_formatting/test_phase14 ✓ (API键名)

## Test Baseline Status

`python -m pytest tests/ --tb=short`: **4 failed, 406 passed, 48 skipped**

4 failures (all root cause: pin parsing):
1. `test_jump_started_flow` — Jump节点未找到
2. `test_jump_completed_flow` — StopJumping节点未找到
3. `test_actionvalue_x_to_right` — Move数据流缺失
4. `test_execution_flows_contains_function_name` — function_name为空

## Pin解析问题分析

**根因:** 扫描器无法找到正确的 `pins_offset`

**已发现的问题:**
1. MemberReference字段无条件读取，但UE只在 `UEVer() >= VER_UE4_MEMBERREFERENCE_IN_PINTYPE` 时序列化
2. `read_ed_graph_pin_type` 在Direction之后尝试读取大量字节失败
3. 版本检查逻辑不一致 (`use_fname_format` 在两处计算方式不同)

**UE源码参考:**
- `EdGraphPin.cpp:SerializePinArray` — pins数组序列化
- `EdGraphPin.cpp:SerializePin` — 单个pin序列化 (bNullPtr + OwningNode + PinGuid)
- `EdGraphNode.cpp:Serialize` — 节点基类序列化
- `K2Node_CallFunction.cpp:Serialize` — CallFunction节点特定序列化

## Working Tree State

- Modified: `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `CLAUDE.md`
- Untracked: `reference/UnrealEditor_uasset加载流程.md`
- All code changes committed in 5ce03dd

## Next Steps (Recommended Order)

1. **研究UE序列化顺序** — 深入分析K2Node_CallFunction::Serialize和UEdGraphNode::Serialize
2. **修复pins_offset定位** — 基于UE源码确定正确的扫描逻辑
3. **完成Phase 28a** — 达到测试基线0失败
4. **启动Phase 29** — 核心数据模型模块

## Key Decisions/Notes

- API键名统一为 `graph_name` (更常用)
- `function_name` 添加到execution_flows (calls数组的第一个)
- Stash合并解决: SourceIndex fallback, FText handling, pin tolerance

## Resume Commands

```bash
# Check current state
cd E:\Develop\uasset_read && git status && python -m pytest tests/ --tb=short -q

# Resume pin parsing research
# Option A: Continue debugging pin scanner
# Option B: Skip to Phase 29, accept 4 test failures as known issues

# Key files to read:
# - uasset_read.py: read_ue_graph_node (line ~3846)
# - uasset_read.py: read_ue_graph_pin (line ~3542)
# - uasset_read.py: read_ed_graph_pin_type (line ~3168)
```

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| 版本检查不一致 | `use_fname_format`在两处计算方式不同导致读取错位 | advisory | 统一版本检查逻辑 |
| 无条件读取MemberReference | UE只在特定版本序列化此字段 | blocking | 添加版本条件判断 |

---
*Resume with: `/gsd-resume-work`*