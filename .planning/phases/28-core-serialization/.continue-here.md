---
context: phase
phase: 28-core-serialization
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-05-11T19:30:00.000Z
---

# Work Handoff — Phase 28a 测试基线修复

**Date:** 2026-05-11
**Branch:** v2.0-dev
**Last Commit:** 5ce03dd fix(phase-28a): resolve merge conflicts and API key inconsistencies

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| FText bHasCultureInvariantString 作为 uint8 | UE bool 序列化为 UBOOL (4 bytes)，而非 uint8 (1 byte) | blocking | 使用 archive.read_i32() 或 read_bool() |
| FunctionReference 在 Pins 之后读取 | UE 序列化顺序：Super::Serialize (tagged properties) → Pins，FunctionReference 在前者 | blocking | 在 script_serial 中解析 tagged properties |
| FText flags=0 时忽略后续字节 | 空 FText 有特殊的序列化格式，需跳过中间 4 bytes | advisory | 检测 flags=0 并跳过 9 bytes |

## Current Position

**Milestone:** v6.0 模块化重构 (Phases 27-34)
**Phase Status:**
- Phase 27: 项目结构初始化 — Complete
- Phase 28: 核心序列化模块 — Complete (ce110d2)
- **Phase 28a: 测试基线修复 — In Progress (从5失败降到4失败)**

## What Was Done This Session

### 已修复问题
1. **CustomVersion GUID 字节序** — 大端序 → 小端序
2. **pins_offset 计算公式** — 固定公式 `script_serial_offset + script_serial_size`
3. **FText bHasCultureInvariantString** — uint8 → UBOOL (4 bytes)
4. **FText flags=0 处理** — 添加特殊跳过逻辑 (跳过中间 4 bytes + history_type + bHasCulture)

### 测试进展
- **之前**: 5 failed
- **现在**: 4 failed, 406 passed, 48 skipped
- **新通过**: test_actionvalue_x_to_right (数据流连接)

## Test Baseline Status

`python -m pytest tests/ --tb=short -q`: **4 failed, 406 passed, 48 skipped**

4 failures (all related to FunctionReference):
1. `test_jump_started_flow` — Jump节点未找到
2. `test_jump_completed_flow` — StopJumping节点未找到
3. `test_function_reference_member_name` — function_reference = None
4. `test_execution_flows_contains_function_name` — function_name为空

## 核心问题分析

**根因**: FunctionReference 读取位置错误

**UE 序列化顺序** (EdGraphNode.cpp L212-261):
```cpp
UEdGraphNode::Serialize(FArchive& Ar) {
    Super::Serialize(Ar);  // UObject::Serialize → tagged properties (FunctionReference 在这里)
    // ...
    UEdGraphPin::SerializeAsOwningNode(Ar, Pins);  // Pins 数组在最后
}
```

**当前代码问题**:
- `read_ue_graph_node` 在 Pins 之后调用 `read_k2node_call_function`
- FunctionReference 应在 script_serial (tagged properties) 中读取

**UE 源码参考**:
- EdGraphNode.cpp:212-261 — 节点序列化顺序
- K2Node_CallFunction.cpp:2304-2360 — CallFunction 特定序列化
- Class.cpp:1514-1600 — Tagged property serialization (SerializeTaggedProperties)

## Working Tree State

- Modified: `uasset_read.py` (FText 处理修复)
- Modified: `.planning/HANDOFF.json`
- Modified: `.planning/debug/pin-parsing-pins-offset.md`

## Next Steps (Recommended Order)

1. **研究 UE tagged property serialization** — 理解 script_serial 格式
2. **定位 FunctionReference** — 在 script_serial 中找到正确的读取位置
3. **修改 read_ue_graph_node 流程** — 在读取 script_serial 时提取 FunctionReference
4. **验证测试** — 确保 4 个剩余测试通过
5. **提交修复** — 完成 Phase 28a

## Key Decisions/Notes

- FText `bHasCultureInvariantString` 使用 UBOOL (int32, 4 bytes)
- 当 FText flags=0 时，跳过 9 bytes (flags + 4 + history_type)
- pins_offset = `script_serial_offset + script_serial_size`
- BlueprintsObjectVersion = 10 (>= EdGraphPinOptimized=4)

## Resume Commands

```bash
# Check current state
cd E:\Develop\uasset_read && git status && python -m pytest tests/ --tb=short -q

# Resume pin parsing research
# Key files to read:
# - uasset_read.py: read_ue_graph_node (line ~3980)
# - UE: Engine/Source/Runtime/CoreUObject/Private/UObject/Class.cpp (SerializeTaggedProperties)
```

---
*Resume with: `/gsd-resume-work`*