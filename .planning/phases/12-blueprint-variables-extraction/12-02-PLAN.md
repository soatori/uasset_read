---
phase: 12-blueprint-variables-extraction
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: [uasset_read.py]
autonomous: true
requirements: [EXTR-02, EXTR-03, EXTR-05]

must_haves:
  truths:
    - "read_blueprint_variable()存储MetaDataArray到metadata字典"
    - "read_blueprint_variable()计算is_component标志"
    - "read_blueprint_variable()调用parse_property_flags_to_labels()填充flags_labels"
    - "变量默认值正确解析多种类型（数值、字符串、布尔、向量、对象引用）"
    - "组件变量通过类型名和CPF_InstancedReference双重验证识别"
  artifacts:
    - path: "uasset_read.py"
      provides: "enhanced read_blueprint_variable"
      contains: "var.metadata ="
    - path: "uasset_read.py"
      provides: "is_component logic"
      pattern: "is_component.*=.*(CPF_InstancedReference|Component)"
  key_links:
    - from: "read_blueprint_variable"
      to: "parse_property_flags_to_labels"
      via: "flags字段解析"
      pattern: "flags_labels.*parse_property_flags_to_labels"
    - from: "read_blueprint_variable"
      to: "parse_default_value"
      via: "default_value解析"
      pattern: "parse_default_value"
---

<objective>
增强read_blueprint_variable()函数，存储MetaDataArray元数据、计算is_component标志、生成flags_labels列表，并修复extract_blueprint_metadata()的定位逻辑问题。

Purpose: 完整解析FBPVariableDescription结构，实现D-02/D-03/D-05决策
Output: 增强的read_blueprint_variable()函数 + 修复后的变量提取逻辑
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-blueprint-variables-extraction/12-CONTEXT.md
@.planning/phases/12-blueprint-variables-extraction/12-RESEARCH.md
@.planning/phases/12-blueprint-variables-extraction/12-01-PLAN.md

<interfaces>
<!-- Key interfaces from existing code -->

From uasset_read.py lines 2861-2928 (read_blueprint_variable function):
```python
def read_blueprint_variable(
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary
) -> BlueprintVariable:
    # Current implementation skips MetaDataArray:
    # meta_count = archive.read_i32()
    # for _ in range(meta_count):
    #     archive.read_name(name_map)  # DataKey
    #     archive.read_fstring()       # DataValue
```

From 12-RESEARCH.md lines 275-328 (FBPVariableDescription完整解析示例):
```python
# MetaDataArray (TArray<FBPVariableMetaDataEntry>) - Phase 12新增
meta_count = archive.read_i32()
var.metadata = {}
for _ in range(meta_count):
    key = archive.read_name(name_map)
    value = archive.read_fstring()
    var.metadata[key] = value

# 组件变量识别（Phase 12新增）
is_component_by_name = isinstance(type_str, str) and "Component" in type_str
is_component_by_flag = (var.property_flags & 0x0000000000080000) != 0
var.is_component = is_component_by_flag or is_component_by_name
```

From uasset_read.py lines 2073-2095 (detect_blueprint function):
```python
def detect_blueprint(
    export: ObjectExport,
    import_map: List[ObjectImport],
    export_map: List[ObjectExport]
) -> bool:
    class_name = get_asset_class(export, import_map, export_map)
    if class_name and "Blueprint" in class_name:
        return True
    return False
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强read_blueprint_variable存储MetaDataArray</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 2861-2928 (read_blueprint_variable函数)
    12-RESEARCH.md lines 275-328 (完整解析示例)
    12-01-PLAN.md (BlueprintVariable新增字段定义)
  </read_first>
  <action>
    修改read_blueprint_variable()函数（lines 2861-2928附近），增强MetaDataArray解析：

当前代码（Phase 3）跳过MetaDataArray：
```python
# MetaDataArray count + entries - skip for Phase 3 (deferred)
meta_count = archive.read_i32()
for _ in range(meta_count):
    archive.read_name(name_map)  # DataKey
    archive.read_fstring()       # DataValue
```

修改为存储MetaDataArray：
```python
# MetaDataArray count + entries - Phase 12: store metadata
meta_count = archive.read_i32()
var.metadata = {}
for _ in range(meta_count):
    key = archive.read_name(name_map)  # DataKey
    value = archive.read_fstring()       # DataValue
    if key:  # 避免None key
        var.metadata[key] = value
```

在函数末尾（return var之前），添加flags_labels解析：
```python
# Phase 12: 解析PropertyFlags为可读标签
var.flags_labels = parse_property_flags_to_labels(var.property_flags)
```

确保函数使用12-01中增强的BlueprintVariable dataclass。
  </action>
  <verify>
    <automated>grep -c "var.metadata = {}" uasset_read.py && grep -c "var.metadata\[key\]" uasset_read.py && grep -c "var.flags_labels = parse_property_flags_to_labels" uasset_read.py</automated>
    Expected: 三个grep各返回>=1
  </verify>
  <done>
    read_blueprint_variable()正确存储MetaDataArray到var.metadata字典
    read_blueprint_variable()调用parse_property_flags_to_labels()填充flags_labels
    测试运行通过：python -c "from uasset_read import read_blueprint_variable; print('OK')"
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加is_component组件变量识别逻辑</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 2861-2928 (read_blueprint_variable函数)
    12-RESEARCH.md lines 321-326 (组件变量识别逻辑)
  </read_first>
  <action>
    在read_blueprint_variable()函数末尾（return var之前），添加is_component计算逻辑：

```python
# Phase 12: 组件变量识别（per D-02）
# 双重验证：类型名contains("Component") + CPF_InstancedReference标志位
# CPF_InstancedReference = 0x0000000000080000
CPF_INSTANCED_REFERENCE = 0x0000000000080000

# 从var_type获取类型字符串
type_str = ""
if var.var_type:
    # 优先使用pin_sub_category（更具体的类型）
    if var.var_type.pin_sub_category and var.var_type.pin_sub_category != "None":
        type_str = var.var_type.pin_sub_category
    elif var.var_type.pin_category:
        type_str = var.var_type.pin_category

# 类型名判断：包含"Component"
is_component_by_name = isinstance(type_str, str) and "Component" in type_str

# CPF_InstancedReference标志位判断
is_component_by_flag = (var.property_flags & CPF_INSTANCED_REFERENCE) != 0

# 双重验证：任一满足即视为组件变量
var.is_component = is_component_by_flag or is_component_by_name
```

注意：CPF_INSTANCED_REFERENCE常量已在parse_property_flags_to_labels中定义，可复用或重新定义。
  </action>
  <verify>
    <automated>grep -c "is_component_by_name" uasset_read.py && grep -c "CPF_INSTANCED_REFERENCE" uasset_read.py && grep -c "var.is_component = " uasset_read.py</automated>
    Expected: 三个grep各返回>=1
  </verify>
  <done>
    is_component字段通过双重验证正确计算
    CPF_InstancedReference标志位识别组件引用
    类型名包含"Component"识别组件类型
  </done>
</task>

<task type="auto">
  <name>Task 3: 增强parse_default_value函数类型覆盖</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 2804-2858 (parse_default_value函数)
    uasset_read.py lines 3945-4005 (parse_property_value函数，Phase 11)
  </read_first>
  <action>
    检查parse_default_value()函数（lines 2804-2858）是否覆盖EXTR-05需求类型：
- 数值类型：int、float（已覆盖）
- 字符串类型：str（已覆盖）
- 布尔类型：bool（已覆盖）
- 向量类型：Vector（当前保持为字符串"(X=...,Y=...,Z=...)"）

若需要增强，考虑：
1. 向量类型解析为Dict：{"x": float, "y": float, "z": float}
2. 对象引用解析（ObjectProperty类型）- 需要FPackageIndex解析

当前parse_default_value处理字符串格式默认值，而Phase 11的parse_property_value处理二进制属性值。
两者职责不同：
- parse_default_value：解析FBPVariableDescription的DefaultValue FString
- parse_property_value：解析PropertyTag二进制属性值

对于向量类型，增强parse_default_value解析"(X=...,Y=...,Z=...)"格式为Dict：
```python
# Vector parsing enhancement (per EXTR-05)
if value_str.startswith("(") and value_str.endswith(")"):
    # 尝试解析向量格式
    import re
    match = re.match(r'\(X=(-?\d+\.?\d*),Y=(-?\d+\.?\d*),Z=(-?\d+\.?\d*)\)', value_str)
    if match:
        return {"x": float(match.group(1)), "y": float(match.group(2)), "z": float(match.group(3))}
    # 其他结构体格式保持为字符串
    return value_str
```

注意：此增强可选，不影响核心功能。若测试验证发现需要，再实施。
  </action>
  <verify>
    <automated>python -m pytest tests/test_blueprint_extraction.py -x -v 2>&1 | head -20</automated>
    Expected: 测试通过或提供需要修复的具体信息
  </verify>
  <done>
    parse_default_value覆盖EXTR-05需求的所有类型
    向量类型可选择解析为Dict或保持字符串
    函数不破坏现有测试
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MetaDataArray解析 → metadata字典 | 输入来自二进制，需验证count边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Denial of Service | MetaDataArray count过大 | mitigate | 已有sanity check（var_count>1000） |
| T-12-04 | Tampering | is_component标志计算 | accept | 双重验证降低误判风险 |
</threat_model>

<verification>
Wave 2验证：
- read_blueprint_variable()正确存储metadata
- is_component字段通过双重验证计算
- flags_labels正确生成
- parse_default_value覆盖EXTR-05类型需求
- 所有现有测试通过
</verification>

<success_criteria>
- read_blueprint_variable()返回的BlueprintVariable包含填充的metadata字典
- BlueprintVariable.is_component正确识别组件变量（类型名或标志位）
- BlueprintVariable.flags_labels包含可读标签（EditAnywhere、BlueprintReadWrite等）
- 默认值正确解析int/float/bool/string/vector类型
</success_criteria>

<output>
完成后创建`.planning/phases/12-blueprint-variables-extraction/12-02-SUMMARY.md`
</output>