---
phase: 13-component-transform-properties
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - uasset_read.py
autonomous: true
requirements:
  - EXTR-04

must_haves:
  truths:
    - "用户可以从ParseResult中的组件export.properties读取RelativeLocation/RelativeRotation/RelativeScale3D属性"
    - "用户可以通过VectorValue/RotatorValue/ScaleValue dataclass获取结构化变换值"
    - "UE度数格式保持不变（RotatorValue.unit='degrees'标注）"
    - "精度处理正确（Location整数优先/3位，Rotation 3位，Scale 4位）"
  artifacts:
    - path: "uasset_read.py"
      provides: "VectorValue dataclass继承AdvancedPropertyValue"
      contains: "class VectorValue"
    - path: "uasset_read.py"
      provides: "RotatorValue dataclass带unit='degrees'字段"
      contains: "class RotatorValue"
    - path: "uasset_read.py"
      provides: "ScaleValue dataclass继承AdvancedPropertyValue"
      contains: "class ScaleValue"
    - path: "uasset_read.py"
      provides: "format_transform_value精度处理函数"
      pattern: "def format_transform_value"
  key_links:
    - from: "VectorValue"
      to: "AdvancedPropertyValue"
      via: "继承关系"
      pattern: "class VectorValue\(AdvancedPropertyValue\)"
    - from: "RotatorValue"
      to: "AdvancedPropertyValue"
      via: "继承关系"
      pattern: "class RotatorValue\(AdvancedPropertyValue\)"
    - from: "parse_struct_property"
      to: "format_transform_value"
      via: "StructValue分派到专用值类型"
      pattern: "format_transform_value.*location|rotation|scale"
---

<objective>
创建VectorValue/RotatorValue/ScaleValue专用dataclass，继承AdvancedPropertyValue基类，提供X/Y/Z/Roll/Pitch/Yaw语义化字段和类型自适应精度处理函数。

Purpose: 为Phase 13组件变换属性解析提供专用数据结构（per D-04, D-04a, D-03a）
Output: VectorValue/RotatorValue/ScaleValue dataclass + format_transform_value()函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-transform-properties/13-CONTEXT.md
@.planning/phases/13-component-transform-properties/13-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing code -->

From uasset_read.py lines 830-855 (AdvancedPropertyValue基类 + StructValue定义):
```python
@dataclass
class AdvancedPropertyValue:
    """高级属性值基类（D-07a）。统一基类设计，包含property_type字段用于类型识别。"""
    property_type: str  # 属性类型名（如 "StructProperty", "MapProperty")

@dataclass
class StructValue(AdvancedPropertyValue):
    """StructProperty值容器（D-01a）。格式：{struct_type: str, fields: dict}"""
    struct_type: str              # 结构体类型名
    fields: Dict[str, Any]        # 嵌套字段（递归解析）
```

From uasset_read.py lines 3593-3623 (_extract_struct_type_from_tag函数):
```python
def _extract_struct_type_from_tag(tag: PropertyTag) -> str:
    """从PropertyTag提取结构体类型名（D-08）。UE5格式: "StructProperty(/Script/CoreUObject.Vector)" """
    type_str = tag.type
    if "(" in type_str:
        start = type_str.find("(")
        end = type_str.find(")")
        if start != -1 and end != -1:
            struct_path = type_str[start+1:end]
            if "." in struct_path:
                return struct_path.split(".")[-1]
            return struct_path
    return "UnknownStruct"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建VectorValue/RotatorValue/ScaleValue dataclass</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 830-880 (AdvancedPropertyValue基类和现有StructValue/MapValue/SetValue定义)
    uasset_read.py lines 4930-4950 (__all__导出列表)
  </read_first>
  <action>
    在uasset_read.py的AdvancedPropertyValue基类定义之后（lines 830-855附近），添加三个新的dataclass：

1. VectorValue dataclass（location/position缩放值）:
```python
@dataclass
class VectorValue(AdvancedPropertyValue):
    """Vector struct property value (Phase 13)。X/Y/Z坐标值"""
    x: float
    y: float
    z: float
```

2. RotatorValue dataclass（rotation角度值，UE使用度数）:
```python
@dataclass
class RotatorValue(AdvancedPropertyValue):
    """Rotator struct property value (Phase 13)。Roll/Pitch/Yaw角度值（UE使用度数）"""
    roll: float
    pitch: float
    yaw: float
    unit: str = 'degrees'  # D-02a: 标注单位为度数
```

3. ScaleValue dataclass（Scale3D缩放值）:
```python
@dataclass
class ScaleValue(AdvancedPropertyValue):
    """Scale3D struct property value (Phase 13)。X/Y/Z缩放因子"""
    x: float
    y: float
    z: float
```

确保导入Dict和Any类型提示（from typing import Dict, Any已存在）。
更新__all__导出列表，添加VectorValue, RotatorValue, ScaleValue。
    </action>
  <verify>
    <automated>grep -c "class VectorValue" uasset_read.py && grep -c "class RotatorValue" uasset_read.py && grep -c "class ScaleValue" uasset_read.py && grep -c "unit: str = 'degrees'" uasset_read.py</automated>
    Expected: 四个grep各返回>=1
  </verify>
  <done>
    VectorValue/RotatorValue/ScaleValue dataclass已定义并继承AdvancedPropertyValue
    RotatorValue包含unit='degrees'字段（per D-02a）
    所有dataclass已添加到__all__导出列表
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建format_transform_value精度处理函数</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 855-880 (现有AdvancedPropertyValue子类定义位置)
    13-RESEARCH.md lines 345-360 (精度处理函数示例)
  </read_first>
  <action>
    创建format_transform_value(value: float, precision_type: str) -> Union[int, float]函数，位置在ScaleValue定义之后（约line 880附近）。

函数功能（per D-03, D-03a）：
1. Input: value (float), precision_type ('location'|'rotation'|'scale')
2. Location: 整数检查 - 若value == int(value)返回int(value)，否则round(value, 3)
3. Rotation: round(value, 3) - 角度精度需求
4. Scale: round(value, 4) - 缩放精度需求

实现（per CONTEXT.md D-03a示例）：
```python
def format_transform_value(value: float, precision_type: str) -> Union[int, float]:
    """Format transform value with adaptive precision."""
    if precision_type == 'location':
        # Integer check - prefer integer if whole number
        if value == int(value):
            return int(value)
        return round(value, 3)
    elif precision_type == 'rotation':
        return round(value, 3)
    elif precision_type == 'scale':
        return round(value, 4)
    return value
```

从typing导入Union类型（from typing import Dict, Any, Union）。
更新__all__导出列表添加format_transform_value。
    </action>
  <verify>
    <automated>grep -c "def format_transform_value" uasset_read.py && grep -c "from typing import" uasset_read.py | head -1</automated>
    Expected: format_transform_value函数存在，Union类型导入存在
  </verify>
  <done>
    format_transform_value函数正确实现类型自适应精度处理
    Location整数优先：format_transform_value(10.0, 'location')返回10
    Rotation 3位：format_transform_value(1.234567, 'rotation')返回1.235
    Scale 4位：format_transform_value(1.234567, 'scale')返回1.2346
    Union已导入，__all__已更新
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 精度处理输入value | float来自StructValue.fields解析，需边界验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | format_transform_value输入 | accept | float值来自已验证的 StructProperty 解析，范围 IEEE 754 double |
| T-13-02 | Information Disclosure | 精度处理输出 | accept | 无敏感信息泄露风险 |
</threat_model>

<verification>
整体Wave 1验证：
- VectorValue/RotatorValue/ScaleValue dataclass定义正确
- RotatorValue.unit='degrees'字段存在
- format_transform_value函数实现类型自适应精度处理
- 所有新增类型和函数已添加到__all__导出列表
</verification>

<success_criteria>
- VectorValue(x, y, z)可构造并继承AdvancedPropertyValue
- RotatorValue(roll, pitch, yaw)可构造且unit='degrees'默认值
- ScaleValue(x, y, z)可构造并继承AdvancedPropertyValue
- format_transform_value(10.0, 'location')返回10（整数优先）
- format_transform_value(1.234567, 'rotation')返回1.235
- format_transform_value(1.234567, 'scale')返回1.2346
</success_criteria>

<output>
完成后创建`.planning/phases/13-component-transform-properties/13-01-SUMMARY.md`
</output>
