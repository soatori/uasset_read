---
phase: 52
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/models/node_types.py
  - src/uasset_read/serializers/graph.py
  - src/uasset_read/models/__init__.py
autonomous: true
requirements:
  - FUNC-01
  - FUNC-02

must_haves:
  truths:
    - "K2Node_FunctionEntry 节点被识别为独立节点类型（非 unknown type）"
    - "FunctionReference（函数名如 'Move', 'Aim'）从 PropertyTag 正确提取并存入 node_data"
    - "BP_FirstPersonCharacter 的 Move/Aim/UserConstructionScript 图中的 FunctionEntry 节点有非空 node_data.function_reference"
  artifacts:
    - path: "src/uasset_read/models/node_types.py"
      provides: "K2NodeFunctionEntry dataclass"
      contains: "class K2NodeFunctionEntry"
    - path: "src/uasset_read/serializers/graph.py"
      provides: "read_k2node_functionentry() + create_node_from_archive 分派"
      exports: ["read_k2node_functionentry"]
    - path: "src/uasset_read/models/__init__.py"
      provides: "导出 K2NodeFunctionEntry"
      contains: "K2NodeFunctionEntry"
  key_links:
    - from: "src/uasset_read/serializers/graph.py:create_node_from_archive"
      to: "read_k2node_functionentry"
      via: "elif class_name == 'K2Node_FunctionEntry' 分派"
      pattern: "K2Node_FunctionEntry"
    - from: "src/uasset_read/serializers/graph.py:read_ue_graph_node"
      to: "create_node_from_archive"
      via: "function_reference 变量传入 node_refs"
      pattern: "node_refs.*function_reference"

threat_model:
  Trust Boundaries:

  | Boundary | Description |
  |----------|-------------|
  | .uasset binary → FArchive | 不受信任的二进制输入，需边界检查 |
  | PropertyTag 解析 → node_data | 恶意构造的 PropertyTag 可能导致越界读取 |

  STRIDE Threat Register:

  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-52-01 | Tampering | read_ue_graph_node PropertyTag 解析 | mitigate | 已有 script_end 边界检查 + tag.size 验证，read_k2node_functionentry 复用此保护 |
  | T-52-02 | Information Exposure | node_data 中的 _raw_properties | accept | 仅记录偏移/大小，不读取内容，无敏感信息暴露 |
  | T-52-SC | Tampering | npm/pip/cargo installs | mitigate | 本阶段零外部依赖，无包安装 |
---

<objective>
为 K2Node_FunctionEntry 添加完整的数据模型和序列化支持，修复 function_reference 解析但未使用的 bug。

Purpose: FUNC-02 要求能识别 FunctionEntry 节点作为函数入口。当前代码中 function_reference 已解析（graph.py 第 748、773-808 行）但未传递给下游，导致 FunctionEntry 落入 unknown type 分支。

Output: K2NodeFunctionEntry dataclass、read_k2node_functionentry 函数、create_node_from_archive 新增分派分支、修复 function_reference 传递
</objective>

<execution_context>
@E:\Develop\uasset_read\.gsd\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.gsd\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\PROJECT.md
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md
@E:\Develop\uasset_read\.planning\phases\52-struct-offset-alignment\52-CONTEXT.md
@E:\Develop\uasset_read\.planning\phases\52-struct-offset-alignment\52-RESEARCH.md

<read_first>
<!-- 核心文件 — 必须读取后再修改 -->
- src/uasset_read/serializers/graph.py — 完整读取，理解 read_ue_graph_node、create_node_from_archive、现有 read_k2node_* 模式
- src/uasset_read/models/node_types.py — 完整读取，理解 dataclass 定义模式和 from_archive 延迟导入模式
- src/uasset_read/models/__init__.py — 完整读取，理解导出模式
- src/uasset_read/models/core.py — 读取 UEdGraphNode 基类和 FMemberReference 定义
</read_first>

<interfaces>
<!-- 关键类型契约 — 执行者直接使用 -->

From src/uasset_read/models/core.py — FMemberReference:
```python
@dataclass
class FMemberReference:
    member_parent: Optional[str] = None
    member_name: str = ""
    member_guid: str = ""
    b_self_context: bool = False
```

From src/uasset_read/models/core.py — UEdGraphNode:
```python
@dataclass
class UEdGraphNode:
    node_guid: str
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List["UEdGraphPin"] = field(default_factory=list)
    class_name: str = ""
    node_data: Optional[Any] = None
```

From src/uasset_read/serializers/graph.py — read_k2node_event 模式（复用此模式）:
```python
def read_k2node_event(archive, name_map, import_map, export_map, linker=None) -> Dict[str, Any]:
    event_reference = read_fmember_reference(archive, name_map, import_map, export_map, linker)
    b_override_function = archive.read_bool()
    return {"event_reference": event_reference, "b_override_function": b_override_function}
```

From src/uasset_read/serializers/graph.py — create_node_from_archive 分派模式:
```python
if class_name == "K2Node_CallFunction":
    base_node.node_data = read_k2node_call_function(...)
elif class_name == "K2Node_Event":
    base_node.node_data = read_k2node_event(...)
# 需要添加: elif class_name == "K2Node_FunctionEntry":
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 K2NodeFunctionEntry dataclass</name>
  <files>src/uasset_read/models/node_types.py, src/uasset_read/models/__init__.py</files>
  <read_first>
    - src/uasset_read/models/node_types.py — 完整文件（现有 5 种 K2Node dataclass 定义模式）
    - src/uasset_read/models/core.py — UEdGraphNode 基类定义
    - src/uasset_read/models/__init__.py — 导出列表
  </read_first>
  <behavior>
    - Test 1: from uasset_read.models import K2NodeFunctionEntry — 导入成功
    - Test 2: K2NodeFunctionEntry() 创建实例，默认值 function_reference=None
    - Test 3: K2NodeFunctionEntry 继承 UEdGraphNode（isinstance 检查）
    - Test 4: from uasset_read import K2NodeFunctionEntry — 顶层导出可用
  </behavior>
  <action>
在 node_types.py 中，参照现有 K2NodeEvent 和 K2NodeCallFunction 的模式（第 21-55 行），添加 K2NodeFunctionEntry dataclass：

1. 添加 dataclass 定义，继承 UEdGraphNode，包含字段：
   - `function_reference: Optional[FMemberReference] = None` — 函数引用（从 FunctionTerminator 继承）
   - `extra_flags: int = 0` — 函数标志（FUNC_* 组合）
   - `b_is_editable: bool = False` — 是否可编辑

2. 添加 `@classmethod from_archive(...)` 方法，延迟导入避免循环依赖：
   ```python
   from uasset_read.serializers.graph import read_k2node_functionentry
   ```
   签名与 K2NodeEvent 一致（archive, name_map, import_map, export_map）

3. 在 models/__init__.py 的 node_types import 块中添加 K2NodeFunctionEntry，在 __all__ 列表中添加 "K2NodeFunctionEntry"（在 K2NodeEnhancedInputAction 之后）

注意：不要修改 core.py 或任何其他文件。仅修改 node_types.py 和 models/__init__.py。
  </action>
  <verify>
    <automated>python -c "from uasset_read.models import K2NodeFunctionEntry; from uasset_read import K2NodeFunctionEntry as K; print('OK')"</automated>
  </verify>
  <done>K2NodeFunctionEntry 可从 uasset_read 和 uasset_read.models 导入，继承 UEdGraphNode，包含 function_reference/extra_flags/b_is_editable 字段</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加 read_k2node_functionentry 和修复 function_reference 传递</name>
  <files>src/uasset_read/serializers/graph.py</files>
  <read_first>
    - src/uasset_read/serializers/graph.py — 完整文件，特别关注：
      - 第 580-617 行: read_k2node_call_function / read_k2node_event 模式
      - 第 687-726 行: create_node_from_archive 分派逻辑
      - 第 733-910 行: read_ue_graph_node — function_reference 变量声明（第 748 行）、解析（第 773-808 行）、未使用问题
  </read_first>
  <behavior>
    - Test 1: read_k2node_functionentry(archive, name_map, import_map, export_map, linker, function_reference=fr) 返回 {"function_reference": fr}
    - Test 2: create_node_from_archive 遇到 class_name="K2Node_FunctionEntry" 时调用 read_k2node_functionentry 并设置 base_node.node_data
    - Test 3: read_ue_graph_node 对 K2Node_FunctionEntry 节点返回的 node_data 包含 function_reference.member_name
  </behavior>
  <action>
在 graph.py 中进行三处修改：

**修改 1 — 添加 read_k2node_functionentry 函数（在 read_k2node_enhanced_input 之后，约第 681 行后）：**
参考 read_k2node_event 模式（第 600-617 行），但关键区别：FunctionReference 已在 read_ue_graph_node 的 PropertyTag 中解析，不需要从 archive 重新读取。函数签名接受 function_reference 参数：
```python
def read_k2node_functionentry(
    archive: FArchive,
    name_map: List[str],
    import_map: List[ObjectImport],
    export_map: List[ObjectExport],
    linker: Optional["PackageLinker"] = None,
    function_reference: Optional[FMemberReference] = None,
) -> Dict[str, Any]:
    """读取 K2Node_FunctionEntry 特有字段。
    FunctionReference 已在 read_ue_graph_node() 中从 PropertyTag 解析。
    """
    return {"function_reference": function_reference}
```

**修改 2 — create_node_from_archive 添加分派分支（约第 717 行，K2Node_EnhancedInputAction 之后 raw_properties 之前）：**
```python
elif class_name == "K2Node_FunctionEntry":
    fr = node_refs.get('function_reference')
    base_node.node_data = read_k2node_functionentry(
        archive, name_map, import_map, export_map, linker,
        function_reference=fr,
    )
```
注意：create_node_from_archive 当前签名不接受 node_refs 参数。需要在函数签名中添加 `node_refs: Optional[Dict[str, Any]] = None` 参数。

**修改 3 — read_ue_graph_node 传递 node_refs（约第 906 行，create_node_from_archive 调用处）：**
在调用 create_node_from_archive 之前，构建 node_refs 字典：
```python
node_refs = {
    'function_reference': function_reference,
    'event_reference': event_reference,
}
```
传入 create_node_from_archive 调用中作为 node_refs=node_refs 参数。

关键：这三处修改必须协同工作。先添加函数定义，再修改 create_node_from_archive 签名，最后修改 read_ue_graph_node 调用。
  </action>
  <verify>
    <automated>python -c "from uasset_read.serializers.graph import read_k2node_functionentry; print(read_k2node_functionentry.__doc__)"</automated>
  </verify>
  <done>read_k2node_functionentry 函数存在并可导入，create_node_from_archive 对 K2Node_FunctionEntry 正确分派，read_ue_graph_node 传递 function_reference 到下游</done>
</task>

<task type="auto">
  <name>Task 3: 添加导出和修复 raw_properties 中 ExtraFlags 处理</name>
  <files>src/uasset_read/serializers/graph.py, src/uasset_read/__init__.py</files>
  <read_first>
    - src/uasset_read/__init__.py — serializers import 块（约第 89-109 行）和 __all__ 列表
    - src/uasset_read/serializers/graph.py — 第 852-856 行 raw_properties 收集逻辑
  </read_first>
  <action>
1. 在 __init__.py 的 serializers import 块中添加 read_k2node_functionentry（在 read_k2node_enhanced_input 之后）
2. 在 __init__.py 的 __all__ 列表中添加 "read_k2node_functionentry"（在 "read_k2node_enhanced_input" 之后）
3. 在 graph.py 的 read_ue_graph_node PropertyTag 解析中（第 852 行 elif tag.size > 0 分支），为 ExtraFlags 添加显式处理：
   在 `elif tag.size > 0:` 之前添加：
   ```python
   elif tag.name == "ExtraFlags":
       raw_properties[tag.name] = archive.read_i32()
   ```
   这样 ExtraFlags 存储实际值而非仅偏移量，供 read_k2node_functionentry 后续使用。
  </action>
  <verify>
    <automated>python -c "from uasset_read import read_k2node_functionentry; print('OK')"</automated>
  </verify>
  <done>read_k2node_functionentry 可从 uasset_read 顶层导入，ExtraFlags 在 read_ue_graph_node 中被显式读取为整数值</done>
</task>

</tasks>

<verification>
- `python -c "from uasset_read import K2NodeFunctionEntry, read_k2node_functionentry"` — 无 ImportError
- `python -c "from uasset_read.models.node_types import K2NodeFunctionEntry; n = K2NodeFunctionEntry(); assert n.function_reference is None; print('OK')"` — dataclass 默认值正确
- `pytest tests/test_graph_parsing.py -v -k "function_entry"` — 新增测试通过（Wave 02 创建）
</verification>

<success_criteria>
- K2NodeFunctionEntry dataclass 存在且可从 uasset_read 导入
- read_k2node_functionentry 函数存在且接受 function_reference 参数
- create_node_from_archive 对 K2Node_FunctionEntry 正确分派
- read_ue_graph_node 传递 function_reference 和 event_reference 到 create_node_from_archive
- BP_FirstPersonCharacter 解析时 Move/Aim/UserConstructionScript 图的 FunctionEntry 节点 node_data 不为 None
</success_criteria>

<output>
Create `.planning/phases/52-struct-offset-alignment/52-01-SUMMARY.md` when done
</output>
