---
phase: "059"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/cpp_gen/cpp_constructor_ir_builder.py
  - src/uasset_read/cpp_gen/extract_cpp_skeleton.py
  - src/uasset_read/cpp_gen/__init__.py
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "CppClassIR.constructor['component_creations'] contains one entry per component property with type and name"
    - "CppClassIR.constructor['component_assignments'] contains SetupAttachment entries for components with attach parents"
    - "CppClassIR.constructor['default_values'] contains property assignment entries from blueprint variables"
    - "InputAction type variables generate LoadObject or skip, NOT CreateDefaultSubobject"
  artifacts:
    - path: "src/uasset_read/cpp_gen/cpp_constructor_ir_builder.py"
      provides: "IR builder functions: build_component_creations, build_component_assignments, build_default_values"
      exports: ["build_component_creations", "build_component_assignments", "build_default_values", "CppComponentCreation", "CppComponentAssignment", "CppDefaultValue"]
    - path: "src/uasset_read/cpp_gen/extract_cpp_skeleton.py"
      provides: "Integration of IR builders into extract_cpp_class_skeleton"
      contains: "ir.constructor = build_..."
  key_links:
    - from: "src/uasset_read/cpp_gen/cpp_constructor_ir_builder.py"
      to: "src/uasset_read/cpp_gen/formatters/cpp_json_ir.py"
      via: "consumes CppClassIR.properties and CppProperty data"
      pattern: "CppClassIR\\.properties|CppProperty"
    - from: "src/uasset_read/cpp_gen/extract_cpp_skeleton.py"
      to: "src/uasset_read/cpp_gen/cpp_constructor_ir_builder.py"
      via: "imports and calls builder functions"
      pattern: "from.*cpp_constructor_ir_builder import"
---

<objective>
创建构造函数 IR 构建器模块，从 CppClassIR.properties、component 数据和 blueprint.variables 提取数据，填充 CppClassIR.constructor 字典的 component_creations、component_assignments、default_values 三个列表。

Purpose: 将蓝图组件层次结构和属性数据转换为结构化的构造函数 IR，供后续 formatter 生成 .cpp 文本。
Output: cpp_constructor_ir_builder.py 模块，包含 3 个数据模型和 3 个构建函数；extract_cpp_skeleton.py 集成调用。
</objective>

<execution_context>
@E:\Develop\uasset_read\.planning\phases\059-component-initialization\59-CONTEXT.md
@E:\Develop\uasset_read\.planning\phases\056-cpp-class-skeleton\056-CONTEXT.md
</execution_context>

<context>
@E:\Develop\uasset_read\src\uasset_read\cpp_gen\formatters\cpp_json_ir.py
@E:\Develop\uasset_read\src\uasset_read\cpp_gen\extract_cpp_skeleton.py
@E:\Develop\uasset_read\src\uasset_read\cpp_gen\cpp_type_mapper.py
@E:\Develop\uasset_read\src\uasset_read\models\blueprint.py
@E:\Develop\uasset_read\src\uasset_read\blueprint\component_extractor.py
@E:\Develop\uasset_read\src\uasset_read\blueprint\variable_extractor.py

<interfaces>
<!-- Key types from cpp_json_ir.py — used directly by this plan -->

CppClassIR.constructor 结构:
```python
constructor: Dict[str, List] = field(default_factory=lambda: {
    "component_creations": [],   # Plan 01 填充
    "component_assignments": [], # Plan 01 填充
    "default_values": [],        # Plan 01 填充
})
```

CppProperty 数据模型:
```python
@dataclass
class CppProperty:
    cpp_type: str       # e.g., "USkeletalMeshComponent*", "float", "UInputAction*"
    name: str           # e.g., "FirstPersonMesh", "MoveSpeed"
    uproperty_marks: List[str]
    category: str       # "component" | "variable"
    default_value: Any  # None for components, actual value for variables
    cpp_comment: str = ""
```

extract_components 返回的字典结构:
```python
{
    "name": str,           # 组件名
    "class": str,          # UE 类名 (如 "SkeletalMeshComponent")
    "properties": dict,    # 标量属性
    "transforms": dict,    # {"relative_location": VectorValue, ...}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建数据模型和组件创建 IR 构建器（D-59-01）</name>
  <files>src/uasset_read/cpp_gen/cpp_constructor_ir_builder.py</files>
  <action>
创建新模块 `cpp_constructor_ir_builder.py`，包含三个数据模型和对应的构建函数。

**数据模型（dataclass）：**

1. `CppComponentCreation` — CreateDefaultSubobject 调用：
   - `variable_name: str` — 变量名（如 "FirstPersonMesh"）
   - `cpp_type: str` — 去指针的 C++ 类型（如 "USkeletalMeshComponent"）
   - `component_name: str` — TEXT() 参数（如 "FirstPersonMesh"）

2. `CppComponentAssignment` — SetupAttachment 调用：
   - `child_name: str` — 子组件变量名
   - `parent_name: str` — 父组件变量名（如 "FirstPersonMesh" 或 "RootComponent"）
   - `socket_name: str` — 插槽名（可为空字符串 ""）

3. `CppDefaultValue` — 属性赋值：
   - `target: str` — 赋值目标（如 "FirstPersonCameraComponent->bUsePawnControlRotation" 或 "TargetArmLength"）
   - `value: str` — 格式化后的值字符串（如 "400.0f", "true", "TEXT(\"value\")"）
   - `cpp_type: str` — C++ 类型（如 "float", "bool", "FString"）
   - `is_method_call: bool` — 是否为方法调用式赋值（如 `GetCapsuleComponent()->InitCapsuleSize(55.f, 96.0f)`）

**构建函数：**

`build_component_creations(ir: CppClassIR) -> List[CppComponentCreation]`：
- 遍历 `ir.properties`，筛选 `category == "component"`
- 从 `prop.cpp_type` 去掉尾部的 `*` 获取模板类型
- 使用 `prop.name` 作为变量名和组件名称
- **特殊处理 InputAction**（D-59-06 补充）：检测 `prop.cpp_type` 是否为 `"UInputAction*"` — 如果是，**不**生成 CreateDefaultSubobject，而是跳过（标记为 `skip=True` 或不在 creations 中），因为 InputAction 通常作为数据资产存在
- 返回 `List[CppComponentCreation]`

关键逻辑示例：
```python
def build_component_creations(ir: CppClassIR) -> List[CppComponentCreation]:
    creations = []
    for prop in ir.properties:
        if prop.category != "component":
            continue
        # D-59-06: InputAction 特殊处理 — 跳过 CreateDefaultSubobject
        if prop.cpp_type == "UInputAction*":
            continue
        cpp_type = prop.cpp_type.rstrip("*").strip()
        creations.append(CppComponentCreation(
            variable_name=prop.name,
            cpp_type=cpp_type,
            component_name=prop.name,
        ))
    return creations
```

验证：`pytest tests/test_cpp_gen/test_cpp_constructor_ir_builder.py -x`
</action>
<verify>
  <automated>pytest tests/test_cpp_gen/test_cpp_constructor_ir_builder.py::test_build_component_creations -x</automated>
</verify>
<done>CppComponentCreation 数据模型存在；build_component_creations 从 CppClassIR.properties 提取组件创建信息，UInputAction* 类型被跳过</done>
</task>

<task type="auto">
  <name>Task 2: Attach 关系和默认值 IR 构建器（D-59-02, D-59-03）</name>
  <files>src/uasset_read/cpp_gen/cpp_constructor_ir_builder.py</files>
  <action>
在同一模块中追加两个构建函数。

**`build_component_assignments(components: List[Dict]) -> List[CppComponentAssignment]`：**

- 遍历 `components` 列表（来自 `extract_components` 的输出）
- 每个 component 字典检查是否存在 `attach_parent` 或 `AttachParent` 字段
- 如果存在 attach_parent：
  - `child_name` = component 的 `name`
  - `parent_name` = `attach_parent`（如果 parent 是组件名，直接使用；如果是 "Root" 或 "RootComponent"，映射为 "RootComponent"）
  - `socket_name` = 从 `attach_socket_name` 或 `AttachSocketName` 提取（如 "head"），不存在则为空字符串
- 如果不存在 attach_parent，跳过该组件（无 attach 关系）
- 返回 `List[CppComponentAssignment]`

注意：当前 `extract_components` 返回的字典不包含 attach_parent。需要在函数中检查组件 properties 中是否有 `AttachParent` 或 `attach_parent` 键。如果有，使用它；否则返回空列表（attach 关系需要后续扩展 extract_components 才能获取）。

**`build_default_values(ir: CppClassIR, blueprint_vars: Optional[List[BlueprintVariable]] = None) -> List[CppDefaultValue]`：**

- 遍历 `ir.properties` 中 `category == "variable"` 的条目
- 对于每个变量属性：
  - 如果 `prop.default_value` 为 None，跳过
  - 否则创建 `CppDefaultValue`：
    - `target` = `prop.name`
    - `cpp_type` = `prop.cpp_type`
    - `value` = 暂存为 `str(prop.default_value)`（**不在此处格式化**，格式化由 Plan 02 的 formatter 负责）
- 如果提供了 `blueprint_vars` 参数，也从其中提取 `is_component=False` 的变量，使用 `var.default_value` 和从 `var.var_type` 推导的 cpp_type
- 返回 `List[CppDefaultValue]`

注意：`build_default_values` 中的 `value` 字段暂存原始值字符串，Plan 02 的 constructor formatter 会使用 `format_cpp_default_value()` 进行类型正确的格式化（float→55.f, string→TEXT(), bool→true/false）。

**集成到 extract_cpp_skeleton.py：**

在 `extract_cpp_class_skeleton()` 函数中，第 103-108 行的 constructor 初始化处，替换为空字典创建后调用构建函数填充：

```python
# 在 return CppClassIR(...) 之前
from uasset_read.cpp_gen.cpp_constructor_ir_builder import (
    build_component_creations,
    build_component_assignments,
    build_default_values,
)

constructor_data = {
    "component_creations": build_component_creations(ir),
    "component_assignments": build_component_assignments(components),
    "default_values": build_default_values(ir, blueprint.variables),
}
```

其中 `ir` 是即将创建的 CppClassIR 实例（需要先创建临时实例，或重构为先创建 IR 再填充 constructor）。

推荐实现方式：先创建 CppClassIR（constructor 留空），然后调用构建函数填充 `ir.constructor` 字典，最后返回。
</action>
<verify>
  <automated>pytest tests/test_cpp_gen/test_cpp_constructor_ir_builder.py::test_build_component_assignments -x</automated>
</verify>
<done>build_component_assignments 从组件数据提取 attach 关系；build_default_values 从变量属性提取默认值赋值；extract_cpp_class_skeleton 调用所有三个构建函数填充 constructor 字典</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blueprint data → C++ code | Untrusted blueprint variable values used as C++ literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-01 | Tampering | build_default_values | mitigate | Validate default_value types match cpp_type before inclusion; reject non-scalar types |
| T-059-02 | Injection | CppDefaultValue.value | mitigate | Sanitize value strings — reject values containing `;`, `{`, `}`, or `//` to prevent code injection |
| T-059-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pytest tests/test_cpp_gen/test_cpp_constructor_ir_builder.py` — 所有新测试通过
- `pytest tests/test_cpp_gen/` — 现有测试不受影响
</verification>

<success_criteria>
- CppComponentCreation, CppComponentAssignment, CppDefaultValue 三个数据模型在 cpp_constructor_ir_builder.py 中定义并导出
- build_component_creations 从 CppClassIR.properties 中提取所有 category="component" 条目（排除 UInputAction*），返回 CppComponentCreation 列表
- build_component_assignments 从 components 数据中提取 attach 关系，返回 CppComponentAssignment 列表
- build_default_values 从 CppClassIR.properties（variable category）和 blueprint.variables 中提取默认值，返回 CppDefaultValue 列表
- extract_cpp_class_skeleton 调用三个构建函数填充 constructor 字典
- 所有函数有对应的单元测试覆盖
</success_criteria>

<output>
Create `.planning/phases/059-component-initialization/59-01-SUMMARY.md` when done
</output>
