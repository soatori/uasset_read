---
phase: 15
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - .claude/skills/uasset-read/examples/basic-usage.md
  - .claude/skills/uasset-read/examples/blueprint-analysis.md
  - .claude/skills/uasset-read/examples/cpp-conversion.md
  - .claude/skills/uasset-read/examples/troubleshooting.md
  - tests/test_skill_integration.py
autonomous: true
requirements: [SKILL-03, SKILL-04]
must_haves:
  truths:
    - "用户可以在examples/目录中找到4个示例文件"
    - "示例文件使用D-15-04锁定的FirstPerson测试资产"
    - "示例代码可直接执行（使用parse_uasset API）"
    - "集成测试验证skill触发、API调用、输出解读三个环节"
  artifacts:
    - path: ".claude/skills/uasset-read/examples/basic-usage.md"
      provides: "CLI和Python API基础调用"
      min_lines: 400
    - path: ".claude/skills/uasset-read/examples/blueprint-analysis.md"
      provides: "EventGraph分析流程"
      min_lines: 500
    - path: ".claude/skills/uasset-read/examples/cpp-conversion.md"
      provides: "蓝图→C++转换示例"
      min_lines: 500
    - path: ".claude/skills/uasset-read/examples/troubleshooting.md"
      provides: "错误处理场景"
      min_lines: 300
    - path: "tests/test_skill_integration.py"
      provides: "skill集成测试"
      contains: "def test_"
      min_lines: 100
  key_links:
    - from: "示例代码"
      to: "parse_uasset() API"
      via: "from uasset_read import parse_uasset"
      pattern: "parse_uasset"
    - from: "示例资产路径"
      to: "FirstPerson测试资产"
      via: "D-15-04锁定路径"
      pattern: "FirstPerson"
---

# 15-03: 示例文件创建与集成测试

<objective>
创建4个示例文件（D-15-05锁定）和集成测试，验证skill触发、API调用、输出解读三个环节正确工作。

Purpose: 用户可快速上手，skill功能可验证。
Output: 4个examples/*.md文件 + tests/test_skill_integration.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-claude-code-skill-packaging/15-CONTEXT.md
@.planning/phases/15-claude-code-skill-packaging/15-RESEARCH.md
@.claude/skills/uasset-read/SKILL.md
@.claude/skills/uasset-read/knowledge/*.md
@uasset_read.py:4472-4621
@.planning/phases/14-output-format-optimization/14-VERIFICATION.md
</context>

<interfaces>
<!-- 示例资产路径（D-15-04锁定） -->
Test Asset Path:
```
E:\Develop\lib\UnrealEngine\Samples\FirstPerson\Content\FirstPerson\Blueprints\BP_FirstPersonCharacter.uasset
```

<!-- API调用模式 -->
From uasset_read.py:
```python
from uasset_read import parse_uasset, format_json_full, format_json_summary, format_markdown

# 基础调用
result = parse_uasset("path/to/file.uasset")

# 状态检查
if result.is_success:
    print(f"解析成功: {result.summary.package_name}")

# 输出格式选择
json_full = format_json_full(result)
json_summary = format_json_summary(result)  # 70%+ token减少
markdown = format_markdown(result)
```

<!-- CLI命令 -->
```bash
python uasset_read.py "path/to/file.uasset" --json
python uasset_read.py "path/to/file.uasset" --summary
python uasset_read.py "path/to/file.uasset" --markdown
```
</interfaces>

<tasks>

## Task 1: 创建basic-usage.md

<read_first>
- uasset_read.py:5630-5700 (CLI入口)
- uasset_read.py:4472-4500 (parse_uasset签名)
- .planning/phases/15-claude-code-skill-packaging/15-CONTEXT.md (D-15-04测试资产路径)
</read_first>

<action>
创建basic-usage.md，内容包括：

**章节结构：**

1. **CLI基础用法** (100-150行)
   - 基础解析命令：
     ```bash
     python uasset_read.py "E:\Develop\lib\UnrealEngine\Samples\FirstPerson\Content\FirstPerson\Blueprints\BP_FirstPersonCharacter.uasset"
     ```
   - --json标志：完整JSON输出
   - --summary标志：精简摘要（70%+ token减少）
   - --markdown标志：Markdown格式

2. **Python API基础调用** (150-200行)
   - 基础导入和调用：
     ```python
     from uasset_read import parse_uasset

     result = parse_uasset("E:\Develop\lib\UnrealEngine\Samples\FirstPerson\Content\FirstPerson\Blueprints\BP_FirstPersonCharacter.uasset")

     if result.is_success:
         print(f"解析成功: {result.summary.package_name}")
     else:
         for error in result.errors:
             print(f"错误: {error}")
     ```
   - 结果结构说明

3. **输出格式选择** (100-150行)
   - format_json_full：完整输出
   - format_json_summary：精简输出
   - format_markdown：Markdown输出
   - 各格式适用场景

4. **字段快速参考** (50-100行)
   - status字段：success/fail/error
   - output_version: "3.0"
   - graphs_summary：执行流程概览
   - exports：导出对象列表

**测试资产路径per D-15-04：** 使用FirstPerson模板资产，路径为：
`E:\Develop\lib\UnrealEngine\Samples\FirstPerson\Content\FirstPerson\Blueprints\BP_FirstPersonCharacter.uasset`

**行数目标：** 400-600行
</action>

<verify>
wc -l .claude/skills/uasset-read/examples/basic-usage.md && grep -c "parse_uasset" .claude/skills/uasset-read/examples/basic-usage.md && grep -c "FirstPerson" .claude/skills/uasset-read/examples/basic-usage.md && grep -c "is_success" .claude/skills/uasset-read/examples/basic-usage.md
</verify>

<acceptance_criteria>
- basic-usage.md文件存在
- 文件行数 >= 400
- 包含parse_uasset调用示例
- 包含FirstPerson测试资产路径（D-15-04）
- 包含is_success状态检查示例
- 包含CLI命令示例
</acceptance_criteria>

## Task 2: 创建blueprint-analysis.md

<read_first>
- uasset_read.py:4800-5000 (graphs_summary构建)
- .claude/skills/uasset-read/knowledge/blueprint-semantics.md (EventGraph概念)
- .claude/skills/uasset-read/knowledge/node-types.md (节点类型)
- .planning/phases/14-output-format-optimization/14-VERIFICATION.md (graphs_summary格式)
</read_first>

<action>
创建blueprint-analysis.md，内容包括：

**章节结构：**

1. **EventGraph分析流程** (150-200行)
   - 步骤1：解析蓝图获取graphs字段
   - 步骤2：遍历graphs_summary查看执行流程
   - 步骤3：深入graphs[].nodes查看节点详情

   示例代码：
   ```python
   from uasset_read import parse_uasset

   result = parse_uasset("path/to/BP_FirstPersonCharacter.uasset")

   # 查看执行流程概览
   for flow in result.graphs_summary:
       print(f"图: {flow['graph_name']}")
       for exec_flow in flow['execution_flows']:
           print(f"  函数: {exec_flow['function_name']}")
           print(f"  参数: {exec_flow['params']}")
   ```

2. **节点分析示例** (150-200行)
   - 分析K2Node_Event节点
   - 分析K2Node_CallFunction节点
   - 节点连接关系解析

   示例代码：
   ```python
   # 深入分析节点
   for graph in result.graphs:
       for node in graph.nodes:
           if node.node_type == "K2Node_Event":
               print(f"事件节点: {node.node_name}")
           elif node.node_type == "K2Node_CallFunction":
               print(f"函数调用: {node.node_name}")
   ```

3. **变量提取示例** (100-150行)
   - 从exports提取蓝图变量
   - 区分组件变量和普通变量
   - 读取变量默认值

4. **组件分析示例** (100-150行)
   - 查找组件导出对象
   - 解析组件transforms
   - SkeletalMesh/Camera等组件类型

**行数目标：** 500-700行
</action>

<verify>
wc -l .claude/skills/uasset-read/examples/blueprint-analysis.md && grep -c "graphs_summary" .claude/skills/uasset-read/examples/blueprint-analysis.md && grep -c "execution_flows" .claude/skills/uasset-read/examples/blueprint-analysis.md && grep -c "K2Node" .claude/skills/uasset-read/examples/blueprint-analysis.md
</verify>

<acceptance_criteria>
- blueprint-analysis.md文件存在
- 文件行数 >= 500
- 包含graphs_summary字段使用示例
- 包含execution_flows字段使用示例
- 包含K2Node节点类型识别示例
- 包含完整Python代码片段（带注释）
</acceptance_criteria>

## Task 3: 创建cpp-conversion.md

<read_first>
- .claude/skills/uasset-read/knowledge/cpp-conversion.md (C++映射理论)
- E:\Develop\lib\UnrealEngine\Samples\FirstPersonC (C++对照代码)
- uasset_read.py:4800-5000 (function_name提取)
</read_first>

<action>
创建cpp-conversion.md，内容包括：

**章节结构：**

1. **蓝图→C++转换流程** (100-150行)
   - 步骤1：解析蓝图获取graphs_summary
   - 步骤2：从function_name推导C++函数名
   - 步骤3：从params推导函数签名
   - 步骤4：从variables推导成员变量

2. **EventGraph→C++函数对照** (150-200行)
   - ReceiveBeginPlay → BeginPlay() override
   - ReceiveTick → Tick(float DeltaTime) override
   - Event_Destroyed → OnDestroyed()

   示例代码：
   ```python
   # 从execution_flows推导C++函数
   for flow in result.graphs_summary[0]['execution_flows']:
       func_name = flow['function_name']
       params = flow['params']

       # 映射到C++
       if func_name == "ReceiveBeginPlay":
           cpp_func = "void BeginPlay() override"
       elif func_name == "ReceiveTick":
           cpp_func = "void Tick(float DeltaTime) override"
   ```

3. **变量→UPROPERTY映射** (100-150行)
   - 蓝图变量 → C++ UPROPERTY
   - 类型映射表
   - is_component → UActorComponent*

4. **完整转换示例** (150-200行)
   - BP_FirstPersonCharacter → FirstPersonCharacter C++对照
   - 对比分析：蓝图JSON vs C++代码
   - 关键差异说明

5. **转换限制说明** (50-100行)
   - skill提供参考级别信息
   - 不生成完整C++代码
   - 需要手动补充细节

**行数目标：** 500-700行
</action>

<verify>
wc -l .claude/skills/uasset-read/examples/cpp-conversion.md && grep -c "function_name" .claude/skills/uasset-read/examples/cpp-conversion.md && grep -c "BeginPlay" .claude/skills/uasset-read/examples/cpp-conversion.md && grep -c "UPROPERTY" .claude/skills/uasset-read/examples/cpp-conversion.md
</verify>

<acceptance_criteria>
- cpp-conversion.md文件存在
- 文件行数 >= 500
- 包含function_name→C++函数名映射示例
- 包含"BeginPlay"示例
- 包含UPROPERTY映射说明
- 包含完整转换对照示例
</acceptance_criteria>

## Task 4: 创建troubleshooting.md

<read_first>
- .claude/skills/uasset-read/knowledge/troubleshooting.md (故障排除知识)
- uasset_read.py:100-200 (UAssetError错误类)
- uasset_read.py:1983-2041 (错误处理)
</read_first>

<action>
创建troubleshooting.md，内容包括：

**章节结构：**

1. **错误处理基础** (100-150行)
   - 检查is_success字段
   - 读取errors数组
   - status字段含义

   示例代码：
   ```python
   result = parse_uasset("path/to/file.uasset")

   if not result.is_success:
       print("解析失败！")
       for error in result.errors:
           print(f"错误: {error}")
       # 部分结果可能仍然可用
       if result.summary:
           print(f"文件头解析成功: {result.summary.package_name}")
   ```

2. **Cooked资产问题** (100-150行)
   - Cooked资产识别方法
   - 为什么看不到EventGraph
   - 解决方案：使用Uncooked资产

3. **常见解析错误** (50-100行)
   - VersionError：版本不兼容
   - ParseError：数据损坏
   - 文件不存在错误

4. **部分结果处理** (50-100行)
   - 即使失败也可能有部分数据
   - summary字段通常可用
   - name_map通常可用

**行数目标：** 300-500行
</action>

<verify>
wc -l .claude/skills/uasset-read/examples/troubleshooting.md && grep -c "is_success" .claude/skills/uasset-read/examples/troubleshooting.md && grep -c "Cooked" .claude/skills/uasset-read/examples/troubleshooting.md && grep -c "errors" .claude/skills/uasset-read/examples/troubleshooting.md
</verify>

<acceptance_criteria>
- troubleshooting.md文件存在
- 文件行数 >= 300
- 包含is_success错误检查示例
- 包含Cooked资产问题说明
- 包含errors数组读取示例
</acceptance_criteria>

## Task 5: 创建集成测试

<read_first>
- tests/test_phase14_output_formats.py (测试结构参考)
- uasset_read.py:4472-4621 (parse_uasset API)
- .planning/phases/15-claude-code-skill-packaging/15-CONTEXT.md (D-15-04测试资产)
</read_first>

<action>
创建tests/test_skill_integration.py，验证skill集成三个环节：

**测试用例设计：**

1. **test_skill_files_exist** — 验证skill文件结构完整
   ```python
   def test_skill_files_exist():
       """SKILL-04: 验证skill文件结构"""
       skill_dir = Path(".claude/skills/uasset-read")
       assert skill_dir.exists()
       assert (skill_dir / "SKILL.md").exists()
       assert len(list((skill_dir / "knowledge").glob("*.md"))) >= 6
       assert len(list((skill_dir / "examples").glob("*.md"))) >= 4
   ```

2. **test_skill_triggers_defined** — 验证触发词定义（D-15-01）
   ```python
   def test_skill_triggers_defined():
       """SKILL-04: 验证触发词包含D-15-01锁定的关键词"""
       skill_md = Path(".claude/skills/uasset-read/SKILL.md").read_text()
       triggers = ["uasset", ".uasset", "蓝图解析", "蓝图图", "parse_uasset", "uasset_read"]
       for trigger in triggers:
           assert trigger in skill_md, f"触发词 {trigger} 未在SKILL.md中定义"
   ```

3. **test_parse_uasset_api_call** — 验证API调用正确
   ```python
   def test_parse_uasset_api_call():
       """SKILL-04: 验证parse_uasset API可调用"""
       from uasset_read import parse_uasset
       test_asset = "E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset"
       if Path(test_asset).exists():
           result = parse_uasset(test_asset)
           assert result.is_success
           assert result.summary.package_name != ""
   ```

4. **test_output_interpretation** — 验证输出解读正确
   ```python
   def test_output_interpretation():
       """SKILL-04: 验证输出字段可正确解读"""
       from uasset_read import parse_uasset, format_json_summary
       test_asset = "E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset"
       if Path(test_asset).exists():
           result = parse_uasset(test_asset)
           output = format_json_summary(result)
           # 验证status字段
           assert output["status"]["status"] in ["success", "fail", "error"]
           # 验证output_version字段
           assert output["output_version"] == "3.0"
           # 验证graphs_summary字段
           if "graphs_summary" in output:
               assert isinstance(output["graphs_summary"], list)
   ```

**文件导入：**
```python
import pytest
from pathlib import Path
from uasset_read import parse_uasset, format_json_full, format_json_summary, format_markdown
```

**行数目标：** 100-150行
</action>

<verify>
python -m pytest tests/test_skill_integration.py -v --tb=short
</verify>

<acceptance_criteria>
- test_skill_integration.py文件存在
- 文件包含至少4个test_*函数
- pytest运行测试文件无语法错误
- test_skill_files_exist测试验证skill结构
- test_skill_triggers_defined验证D-15-01触发词
- test_parse_uasset_api_call验证API调用
- test_output_interpretation验证输出解读
</acceptance_criteria>

</tasks>

<verification>
## Verification Steps

1. 检查所有示例文件存在：
   ```bash
   ls .claude/skills/uasset-read/examples/*.md
   ```
   预期：4个文件（basic-usage、blueprint-analysis、cpp-conversion、troubleshooting）

2. 检查示例文件使用测试资产（D-15-04）：
   ```bash
   grep "FirstPerson" .claude/skills/uasset-read/examples/*.md
   ```
   预期：至少basic-usage.md包含FirstPerson路径

3. 检查示例代码可执行性：
   ```bash
   grep "parse_uasset" .claude/skills/uasset-read/examples/*.md
   grep "is_success" .claude/skills/uasset-read/examples/*.md
   ```
   预期：所有示例文件包含parse_uasset调用和is_success检查

4. 运行集成测试：
   ```bash
   python -m pytest tests/test_skill_integration.py -v
   ```
   预期：4个测试通过（如果测试资产可用）

5. 检查skill文件完整性：
   ```bash
   find .claude/skills/uasset-read -name "*.md" | wc -l
   ```
   预期：>= 11个文件（1 SKILL.md + 6 knowledge + 4 examples）
</verification>

<success_criteria>
## Must-Haves (Goal-Backward)

| Truth | Artifact | Verification |
|-------|----------|--------------|
| 用户可以在examples/目录中找到4个示例文件 | 4个.md文件存在 | `ls`验证 |
| 示例文件使用D-15-04锁定的FirstPerson测试资产 | 文件包含FirstPerson路径 | `grep`验证 |
| 示例代码可直接执行 | 包含parse_uasset调用 | `grep`验证 |
| 集成测试验证skill三个环节 | test_skill_integration.py存在并运行 | `pytest`验证 |

**Acceptance:** 所有4项must-haves达成 → Plan完成
</success_criteria>

<output>
完成后创建 `.planning/phases/15-claude-code-skill-packaging/15-03-SUMMARY.md`
</output>