---
phase: "35"
plan: 03
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - src/uasset_read/serializers/graph.py
  - src/uasset_read/graph/flow_builder.py
autonomous: true
requirements:
  - REQ-35-03
user_setup: []

must_haves:
  truths:
    - "EventGraph 的执行流非空（start_event 非 Unknown 且有 nodes）"
    - "连接映射（connections）非空，包含有效的 from/to 引用"
    - "数据流（data_flows）非空（如果有非 exec pin 连接）"
  artifacts:
    - path: "src/uasset_read/serializers/graph.py"
      provides: "Pin linked_to_raw 解析修复"
      contains: "read_ue_graph_pin linked_to_raw population"
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "执行流起点匹配和追踪修复"
      contains: "build_execution_flows"
  key_links:
    - from: "read_ue_graph_pin"
      to: "pin.linked_to_raw"
      via: "Pin LinkedTo 数组解析"
      pattern: "linked_to_raw"
---

<objective>
修复执行流和连接数据全部为空的问题（P1 严重 bug）。根因是 Pin 的 `linked_to_raw` 为空列表，导致 `build_execution_flows` 和 `build_connections_map` 无法找到任何连接。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-json-serialization-fix/35-CONTEXT.md
@src/uasset_read/serializers/graph.py
@src/uasset_read/graph/flow_builder.py
@src/uasset_read/constants.py
</context>

<interfaces>
<!-- Pin 读取关键逻辑，来自 serializers/graph.py -->

从 read_ue_graph_pin 函数中 linked_to_raw 的解析逻辑（需要检查 read_pin_array 实现）：

```python
# read_ue_graph_pin 中读取 linked_to 的关键路径：
# Line ~349: linked_to_raw 来自 read_pin_array 或类似函数
# 如果 read_pin_array 返回 []，则 linked_to_raw 为空

# UEdGraphPin dataclass:
@dataclass
class UEdGraphPin:
    pin_name: str
    pin_id: str
    pin_type: Optional[FEdGraphPinType]
    direction: int  # 0=Input, 1=Output
    linked_to_raw: List[Any] = field(default_factory=list)  # ← 这里为空导致问题
```

从 flow_builder.py:
```python
def build_execution_flows(graph: UEdGraph) -> List[Dict]:
    # Line 324: start_nodes = [n for n in graph.nodes if n.class_name in START_EVENT_TYPES]
    # START_EVENT_TYPES = {"K2Node_Event", "K2Node_EnhancedInputAction", "K2Node_VariableSet", "K2Node_CustomEvent"}
    # Line 304: 从 START_EVENT_TYPES 节点开始追踪
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 调查并修复 Pin linked_to_raw 为空的问题</name>
  <read_first>
    - src/uasset_read/serializers/graph.py (完整 read_ue_graph_pin 函数)
    - src/uasset_read/serializers/graph.py (read_pin_array 相关函数)
    - src/uasset_read/graph/flow_builder.py (_find_next_exec_node, build_execution_flows)
  </read_first>
  <files>src/uasset_read/serializers/graph.py</files>
  <action>
在 `read_ue_graph_pin` 函数中定位 linked_to_raw 的读取逻辑。报告指出 "pins确实存在，只是 linked_to_raw 为空列表 []"，说明 `read_pin_array` 返回空列表。

调查步骤：
1. 找到 read_ue_graph_pin 中 linked_to_raw 的赋值位置
2. 检查 `read_pin_array` 或类似函数的实现
3. 验证 LinkedTo 数组的偏移读取是否正确（UE5 格式可能与 UE4 不同）
4. 添加调试日志：在 linked_to_raw 赋值后 print(pin_name, len(linked_to_raw))

修复方向（根据调查结果选择）：
- 如果 LinkedTo 数组的序列化偏移计算错误：修正偏移公式
- 如果 UE5 格式中 LinkedTo 使用了不同的序列化方式（如 PackageIndex 数组而非 GUID）：添加 UE5 格式分支
- 如果 `read_pin_array` 本身有 bug（如提前 return [] 的条件过于激进）：修复条件判断

**注意事项**：不要静默跳过问题。修复后需要确认 linked_to_raw 包含非空数据。
  </action>
  <verify>
    <automated>python -c "from uasset_read import parse_uasset; r = parse_uasset('BP_FirstPersonCharacter.uasset'); pins_with_links = sum(1 for g in r.graphs for n in g.nodes for p in n.pins if p.linked_to_raw); total_pins = sum(1 for g in r.graphs for n in g.nodes for p in n.pins); print(f'Pins with links: {pins_with_links}/{total_pins}'); assert pins_with_links > 0, 'No pins have linked_to_raw'"</automated>
  </verify>
  <acceptance_criteria>
    - linked_to_raw 对至少部分 pin 非空（BP_FirstPersonCharacter.uasset 中有连接的 pin）
    - linked_to_raw 中的条目包含有效的 pin_guid 信息（dict 格式或 hex string）
    - read_ue_graph_pin 在遇到 LinkedTo 数组时不静默返回空列表
  </acceptance_criteria>
  <done>linked_to_raw 包含非空数据，至少部分 pin 有连接信息</done>
</task>

<task type="auto">
  <name>Task 2: 验证执行流和数据流构建正确性</name>
  <read_first>
    - src/uasset_read/graph/flow_builder.py (build_execution_flows, build_connections_map, build_data_flows)
    - src/uasset_read/graph/flow_builder.py (_find_next_exec_node, _trace_execution_from_event)
    - src/uasset_read/constants.py (START_EVENT_TYPES 定义)
  </read_first>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <action>
在 linked_to_raw 修复后，验证执行流构建：

1. 运行 `build_execution_flows` 对 BP_FirstPersonCharacter.uasset 的 EventGraph
2. 检查 start_nodes 是否正确识别（n.class_name in START_EVENT_TYPES）
3. 检查 _find_next_exec_node 能否找到下一个节点（pin.pin_type.pin_category == "exec" 且 linked_to_raw 非空）
4. 检查 build_connections_map 能否构建非空的 connections 列表

如果 linked_to_raw 已修复但执行流仍然为空：
- 检查 pin.pin_type 是否正确设置（pin_type 可能在 UE5 格式中读取不同）
- 检查 pin.direction 是否正确（0=Input, 1=Output）
- 检查 _get_start_event_name 对 K2Node_Event 能否正确提取 event_reference.member_name

如果 event_reference 为空导致 "Unknown" 事件名：
- 检查 create_node_from_archive 是否成功调用 read_k2node_event
- 检查 event_reference 的序列化读取是否正确（MemberName, MemberParent, MemberGuid）
  </action>
  <verify>
    <automated>python -c "from uasset_read import parse_uasset; from uasset_read.graph.flow_builder import build_execution_flows, build_connections_map; r = parse_uasset('BP_FirstPersonCharacter.uasset'); flows = [f for g in r.graphs for f in build_execution_flows(g)]; conns = [c for g in r.graphs for c, _ in [build_connections_map(g)]]; assert len(flows) > 0 or len(conns) > 0, f'All empty: flows={len(flows)}, conns={len(conns)}'; print(f'OK: flows={len(flows)}, conns={len(conns)}')"</automated>
  </verify>
  <acceptance_criteria>
    - build_execution_flows 对至少一个 graph 返回非空列表，或 build_connections_map 返回非空 connections
    - start_event 名称非 "Unknown"（对已知的 K2Node_Event 节点）
    - connections 列表包含有效的 from/to 引用
  </acceptance_criteria>
  <done>执行流和连接数据非空，至少能识别出已知的节点连接关系</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binary pin data → flow builder | 恶意 .uasset 可能构造循环引用或无效 pin GUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Integrity | build_execution_flows | mitigate | _trace_execution_from_event 已有 visited 集合防止无限循环（line 196-201 cycle_detected） |
| T-35-06 | Availability | read_pin_array | mitigate | 现有 try/except 保护单个 pin 解析失败不影响整个图 |
</threat_model>

<verification>
- `uasset-read BP_FirstPersonCharacter.uasset --json` 输出中 graphs_summary 包含非空的 execution_flows 或 connections
- EventGraph 的 start_event 非 "Unknown"
- connections 列表包含至少一条 from → to 连接
</verification>

<success_criteria>
- EventGraph 的执行流和连接数据非空
- 至少能识别出已知的节点连接关系（如 BeginPlay → SetVisibility）
- 所有现有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/35-json-serialization-fix/35-03-SUMMARY.md`
</output>
