---
phase: 08-blueprint-graph-output
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [uasset_read.py, tests/test_output_formatting.py]
autonomous: true
requirements: [OUT2-03]
user_setup: []

must_haves:
  truths:
    - "文本输出包含 Graphs 区块"
    - "每个 Graph 显示名称、节点数、连接数"
    - "执行流概览显示每条流的起点和节点数量"
    - "YAML 风格缩进（2 空格）"
    - "无图数据时不输出 Graphs 区块"
  artifacts:
    - path: "uasset_read.py"
      provides: "format_text_full() 扩展"
      contains: "if result.graphs:"
      pattern: "lines.append\\(\"Graphs:\"\\)"
    - path: "tests/test_output_formatting.py"
      provides: "文本输出测试"
      contains: "test_format_text_full_contains_graph_summary"
  key_links:
    - from: "format_text_full()"
      to: "build_connections_map()"
      via: "获取连接数量"
      pattern: "build_connections_map\\(graph\\)"
    - from: "format_text_full()"
      to: "build_execution_flows()"
      via: "获取执行流数据"
      pattern: "build_execution_flows\\(graph\\)"
---

<objective>
扩展文本输出格式，添加图结构摘要区块，显示节点数、连接数和执行流概览。

Purpose: 满足 OUT2-03（文本输出包含图结构摘要）
Output: format_text_full() 扩展，添加 Graphs 区块
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-blueprint-graph-output/08-CONTEXT.md
@.planning/phases/08-blueprint-graph-output/08-RESEARCH.md

<interfaces>
<!-- Phase 4 已实现的文本输出格式 -->
<!-- Phase 8 Wave 1/2 已实现的连接映射和执行流 -->

From uasset_read.py L3366-3444 (format_text_full 当前实现):
```python
def format_text_full(result: ParseResult) -> str:
    """
    Format YAML-style text output with full detail (OUT-02).
    Per D-17: YAML style hierarchy with 2-space indentation
    """
    lines = []

    # Package header (L3384-3398)
    if result.summary:
        package_name = result.summary.package_name or "Unknown"
        lines.append(f"Package: {package_name}")
        lines.append(f"  Version: UE4={...}, UE5={...}")
        lines.append(f"  Flags: 0x{...:08X}")
        lines.append(f"  Imports: {len(result.import_map)}")
        lines.append(f"  Exports: {len(result.export_map)}")
        lines.append("")

    # Exports section (L3400-3416)
    lines.append("Exports:")
    # ... exports formatting ...

    # Blueprint section (L3418-3433)
    if result.blueprint and result.blueprint.is_blueprint:
        lines.append("Blueprint:")
        # ... blueprint formatting ...

    # ERRORS block (L3435-3442)
    if result.errors:
        lines.append("ERRORS:")
        # ... errors formatting ...

    return "\n".join(lines)
    # Phase 8: 需在 ERRORS block 之前添加 Graphs 区块
```

From uasset_read.py (build_connections_map, build_execution_flows - Phase 8 Wave 1/2):
```python
# 已实现，返回：
# connections, warnings = build_connections_map(graph)
# execution_flows = build_execution_flows(graph)
``>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 format_text_full() 添加 Graphs 区块</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L3366-3444 (format_text_full 当前实现)
    - uasset_read.py (build_connections_map, build_execution_flows 函数)
    - .planning/phases/08-blueprint-graph-output/08-CONTEXT.md (Claude's Discretion: 文本格式)
    - .planning/phases/04-output-and-cli/04-CONTEXT.md (D-17: YAML 风格缩进)
  </read_first>
  <behavior>
    - 在 Blueprint section 之后、ERRORS block 之前添加 Graphs 区块
    - 每个 Graph 显示：名称、类名、节点数、连接数、执行流概览
    - 使用 2 空格缩进（YAML 风格）
    - 无图数据时跳过 Graphs 区块
  </behavior>
  <action>
    修改 format_text_full() 函数，在 ERRORS block 之前添加 Graphs 区块：

    找到 format_text_full() 函数中的 ERRORS block 部分（约 L3435），在其之前插入 Graphs 区块：

    ```python
    def format_text_full(result: ParseResult) -> str:
        """
        Format YAML-style text output with full detail (OUT-02, OUT2-03).

        Per D-17: YAML style hierarchy with 2-space indentation
        Per D-19: ERRORS block at end
        Per D-21: Blueprint metadata embedded
        Per D-22: Nested YAML indentation
        Phase 8: Graphs section with summary (OUT2-03)
        """
        lines = []

        # Package header (existing code unchanged)
        if result.summary:
            package_name = result.summary.package_name or "Unknown"
            lines.append(f"Package: {package_name}")
            lines.append(f"  Version: UE4={result.summary.file_version_ue4}, UE5={result.summary.file_version_ue5}")
            lines.append(f"  Flags: 0x{result.summary.package_flags:08X}")
            lines.append(f"  Imports: {len(result.import_map)}")
            lines.append(f"  Exports: {len(result.export_map)}")
            lines.append("")
        else:
            lines.append("Package: Unknown")
            lines.append("  Version: Unknown")
            lines.append("  Flags: Unknown")
            lines.append("  Imports: 0")
            lines.append("  Exports: 0")
            lines.append("")

        # Exports section (existing code unchanged)
        lines.append("Exports:")
        for i, exp in enumerate(result.export_map):
            asset_class = get_asset_class(exp, result.import_map, result.export_map)
            lines.append(f"  - Name: {exp.object_name}")
            lines.append(f"    Class: {asset_class}")
            lines.append(f"    SerialSize: {exp.serial_size}")

            if exp.properties:
                lines.append(f"    Properties:")
                for prop in exp.properties:
                    lines.append(f"      - Name: {prop.name}")
                    lines.append(f"        Type: {prop.type}")
                    value_str = str(prop.value) if prop.value is not None else "null"
                    lines.append(f"        Value: {value_str}")

            lines.append("")

        # Blueprint section (existing code unchanged)
        if result.blueprint and result.blueprint.is_blueprint:
            lines.append("Blueprint:")
            parent = result.blueprint.parent_class or "Unknown"
            lines.append(f"  ParentClass: {parent}")
            lines.append(f"  Variables: {len(result.blueprint.variables)}")

            for var in result.blueprint.variables:
                lines.append(f"  - Name: {var.var_name}")
                lines.append(f"    Type: {var.var_type.pin_category}")
                default = var.default_value or "None"
                lines.append(f"    Default: {default}")
                category = var.category or "Default"
                lines.append(f"    Category: {category}")

            lines.append("")

        # Phase 8: Graphs section (OUT2-03)
        if result.graphs:
            lines.append("Graphs:")
            for graph in result.graphs:
                # 获取连接数量
                connections, _ = build_connections_map(graph)

                # 获取执行流数据
                execution_flows = build_execution_flows(graph)

                lines.append(f"  - Name: {graph.graph_name}")
                lines.append(f"    Class: {graph.graph_class}")
                lines.append(f"    Nodes: {len(graph.nodes)}")
                lines.append(f"    Connections: {len(connections)}")

                # 执行流概览
                lines.append(f"    ExecutionFlows: {len(execution_flows)}")
                for flow in execution_flows:
                    start_event = flow.get("start_event", "Unknown")
                    node_count = len(flow.get("nodes", []))
                    lines.append(f"      - {start_event}: {node_count} nodes")

            lines.append("")  # Graphs 区块后的空行

        # ERRORS block (existing code unchanged)
        if result.errors:
            lines.append("ERRORS:")
            for err in result.errors:
                lines.append(f"  - {err}")
        else:
            lines.append("ERRORS:")
            lines.append("  (none)")

        return "\n".join(lines)
    ```

    关键点：
    - Graphs 区块在 Blueprint 之后、ERRORS 之前
    - 使用 2 空格缩进（符合 D-17 YAML 风格）
    - 调用 build_connections_map() 获取连接数量
    - 调用 build_execution_flows() 获取执行流数据
    - 无图数据时跳过 Graphs 区块（if result.graphs）
    - 执行流概览显示每条流的起点和节点数量
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_text_full; print('Import OK')"</automated>
    <grep>grep 'lines.append("Graphs:")' uasset_read.py | wc -l == 1</grep>
    <grep>grep 'if result.graphs:' uasset_read.py | wc -l == 1</grep>
  </verify>
  <acceptance_criteria>
    - format_text_full() 函数包含 Graphs 区块
    - Graphs 区块在 Blueprint 之后、ERRORS 之前
    - 使用 if result.graphs: 条件判断
    - 调用 build_connections_map() 获取连接数量
    - 调用 build_execution_flows() 获取执行流数据
    - 使用 2 空格缩进（YAML 风格）
    - 显示每个 Graph 的名称、类名、节点数、连接数
    - 显示执行流概览（起点 + 节点数量）
  </acceptance_criteria>
  <done>format_text_full() 扩展完成，Graphs 区块添加成功</done>
</task>

<task type="auto">
  <name>Task 2: 创建文本输出图摘要测试</name>
  <files>tests/test_output_formatting.py</files>
  <read_first>
    - tests/test_output_formatting.py (Phase 8 Wave 1/2 已创建的测试和 fixture)
    - uasset_read.py L3366-3444 (format_text_full 实现)
    - .planning/phases/04-output-and-cli/04-CONTEXT.md (D-17: YAML 风格)
  </read_first>
  <behavior>
    - 测试 format_text_full() 包含 Graphs 区块
    - 测试 Graphs 区块显示节点数、连接数
    - 测试执行流概览显示起点和节点数量
    - 测试无图数据时不输出 Graphs 区块
  </behavior>
  <action>
    在 tests/test_output_formatting.py 中添加文本输出测试：

    ```python
    # ============================================================================
    # Phase 8: OUT2-03 - 文本输出图结构摘要
    # ============================================================================

    def test_format_text_full_contains_graph_summary():
        """
        OUT2-03: 验证 format_text_full() 包含图结构摘要。
        """
        # 创建带有图的 ParseResult
        result = create_mock_parse_result()
        result.graphs = [sample_graph_with_execution_flow()]

        text = format_text_full(result)

        assert "Graphs:" in text
        assert "EventGraph" in text  # graph_name
        assert "Nodes:" in text
        assert "Connections:" in text

    def test_format_text_full_graph_details():
        """
        OUT2-03: 验证 Graphs 区块显示正确的详细信息。
        """
        result = create_mock_parse_result()
        result.graphs = [sample_graph_with_execution_flow()]

        text = format_text_full(result)

        # 验证 YAML 风格缩进（2 空格）
        assert "  - Name: EventGraph" in text
        assert "    Class: UberEdGraph" in text
        assert "    Nodes: 2" in text  # 2 个节点
        assert "    Connections: 1" in text  # 1 个连接

    def test_format_text_full_execution_flow_summary():
        """
        OUT2-03: 验证执行流概览显示。
        """
        result = create_mock_parse_result()
        result.graphs = [sample_graph_with_execution_flow()]

        text = format_text_full(result)

        assert "ExecutionFlows:" in text
        assert "BeginPlay" in text  # start_event
        assert "nodes" in text.lower()  # 节点数量

    def test_format_text_full_no_graphs():
        """
        OUT2-03: 验证无图数据时不输出 Graphs 区块。
        """
        result = create_mock_parse_result()
        result.graphs = []  # 空

        text = format_text_full(result)

        assert "Graphs:" not in text

    def test_format_text_full_graph_position():
        """
        验证 Graphs 区块位置正确（Blueprint 之后、ERRORS 之前）。
        """
        result = create_mock_parse_result()
        result.graphs = [sample_graph_with_execution_flow()]
        result.blueprint = sample_graph_blueprint_metadata()

        text = format_text_full(result)

        # 验证顺序：Blueprint → Graphs → ERRORS
        blueprint_pos = text.find("Blueprint:")
        graphs_pos = text.find("Graphs:")
        errors_pos = text.find("ERRORS:")

        assert blueprint_pos < graphs_pos, "Blueprint 应在 Graphs 之前"
        assert graphs_pos < errors_pos, "Graphs 应在 ERRORS 之前"
    ```

    注意：需要确保 create_mock_parse_result fixture 可以设置 graphs 字段，sample_graph_blueprint_metadata fixture 在 Wave 1 已定义（如未定义，可使用 sample_mock_blueprint_metadata）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_text_full_contains_graph_summary -x -v</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_text_full_no_graphs -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_output_formatting.py 包含 test_format_text_full_contains_graph_summary 测试
    - tests/test_output_formatting.py 包含 test_format_text_full_graph_details 测试
    - tests/test_output_formatting.py 包含 test_format_text_full_execution_flow_summary 测试
    - tests/test_output_formatting.py 包含 test_format_text_full_no_graphs 测试
    - tests/test_output_formatting.py 包含 test_format_text_full_graph_position 测试
    - 所有新增测试 pytest 执行通过（exit code 0）
  </acceptance_criteria>
  <done>文本输出图摘要测试创建完成，pytest 验证通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 输入 → 文本输出 | UEdGraph 数据来自 Phase 7 解析，已验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Denial of Service | format_text_full() Graphs 区块 | accept | 简单字符串拼接，Phase 7 已限制图大小 |

**Phase 8 文本输出无安全敏感操作（纯字符串格式化）。**
</threat_model>

<verification>
```bash
# 导入验证
python -c "from uasset_read import format_text_full; print('Import OK')"

# grep 检查
grep 'lines.append("Graphs:")' uasset_read.py | wc -l
grep 'if result.graphs:' uasset_read.py | wc -l

# 单元测试
python -m pytest tests/test_output_formatting.py::test_format_text_full_contains_graph_summary -x -v
python -m pytest tests/test_output_formatting.py::test_format_text_full_no_graphs -x -v

# 完整测试套件无回归
python -m pytest tests/ -v --ignore=tests/test_output_formatting.py -k "not test_json_full_structure"
```
</verification>

<success_criteria>
- format_text_full() 包含 Graphs 区块
- Graphs 区块显示节点数、连接数、执行流概览
- 使用 2 空格缩进（YAML 风格）
- 无图数据时跳过 Graphs 区块
- 文本输出测试创建并通过
- 无回归：现有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/08-blueprint-graph-output/08-03-SUMMARY.md`
</output>