---
phase: 10-dependency-analysis
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [uasset_read.py]
autonomous: true
requirements: [DEPS-01, DEPS-02]
user_setup: []

must_haves:
  truths:
    - "build_imports_list() 函数能将 ObjectImport 转换为 {class, package, object} dict"
    - "相同 {class, package, object} 的条目合并为单一条目"
    - "read_soft_object_paths() 函数能解析 SoftObjectPaths 数组"
    - "UE4 文件返回空 soft_references 数组"
    - "UE5 >= 1008 文件解析 SoftObjectPaths 数据"
  artifacts:
    - path: "uasset_read.py"
      contains: "def build_imports_list"
      exports: ["build_imports_list"]
    - path: "uasset_read.py"
      contains: "def read_soft_object_paths"
      exports: ["read_soft_object_paths"]
    - path: "uasset_read.py"
      contains: '"class": imp.class_name'
      pattern: '"class".*:'
    - path: "uasset_read.py"
      contains: '"asset_path": asset_path'
      pattern: '"asset_path".*:'
  key_links:
    - from: "build_imports_list"
      to: "ObjectImport"
      via: "import_map 参数"
      pattern: "imp\\.class_name|imp\\.class_package|imp\\.object_name"
    - from: "read_soft_object_paths"
      to: "PackageFileSummary"
      via: "soft_object_paths_count/soft_object_paths_offset"
      pattern: "soft_object_paths_count|soft_object_paths_offset"
    - from: "read_soft_object_paths"
      to: "FArchive"
      via: "read_name(), read_fstring()"
      pattern: "read_name|read_fstring"
---

<objective>
实现两个依赖解析核心函数：build_imports_list() 和 read_soft_object_paths()。

Purpose: 将 ImportMap 转换为结构化 imports 输出（DEPS-01），解析 SoftObjectPaths 软引用数据（DEPS-02）。
Output: build_imports_list() 和 read_soft_object_paths() 函数实现。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dependency-analysis/10-CONTEXT.md
@.planning/phases/10-dependency-analysis/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From uasset_read.py L1525-1564 - read_import_map() (已实现):
```python
def read_import_map(
    archive: FArchive,
    summary: PackageFileSummary,
    name_map: List[str]
) -> List[ObjectImport]:
    """读取导入表（CORE-04）。"""
    archive.seek(summary.import_offset)
    import_map: List[ObjectImport] = []
    for _ in range(summary.import_count):
        class_package = archive.read_name(name_map)
        class_name = archive.read_name(name_map)
        outer_index = PackageIndex(archive.read_i32())
        object_name = archive.read_name(name_map)
        import_map.append(ObjectImport(...))
    return import_map
```

From uasset_read.py L646-657 - ObjectImport dataclass:
```python
@dataclass
class ObjectImport:
    class_package: str      # 来源包名（如 "/Script/Engine"）
    class_name: str         # 类名（如 "Class"）
    outer_index: PackageIndex
    object_name: str        # 对象名（如 "Blueprint"）
```

From uasset_read.py L564-565 - PackageFileSummary soft_object_paths fields:
```python
soft_object_paths_count: int = 0
soft_object_paths_offset: int = 0
```

From uasset_read.py L74 - Version constant:
```python
UE5_ADD_SOFTOBJECTPATH_LIST = 1008
```

From uasset_read.py L1171-1173 - Version check pattern:
```python
if not is_ue4_file and file_version_ue5 >= UE5_ADD_SOFTOBJECTPATH_LIST:
    soft_object_paths_count = archive.read_i32()
    soft_object_paths_offset = archive.read_i32()
```

From uasset_read.py L153-520 - FArchive methods:
```python
read_name(name_map: List[str]) -> str  # FName 解析
read_fstring() -> str  # FString 解析
seek(offset: int)  # 定位到偏移
validate_offset(offset: int, context: str)  # 边界验证
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 build_imports_list() 函数</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L646-657 — ObjectImport dataclass 定义
    - uasset_read.py L1525-1564 — read_import_map() 返回 ObjectImport 列表
    - 10-CONTEXT.md D-10-01~05 — ImportMap 依赖格式决策
    - 10-RESEARCH.md Pattern 2 — build_imports_list 实现示例
  </read_first>
  <action>
    在 read_import_map() 函数之后添加新函数（约 L1565 附近）：

    ```python
    def build_imports_list(import_map: List[ObjectImport]) -> List[Dict]:
        """
        构建 imports 依赖列表（DEPS-01）。

        Per D-10-01: {class, package, object} 格式
        Per D-10-03: 保持原始顺序（首次出现）
        Per D-10-04: 合并重复（相同三元组）

        Args:
            import_map: read_import_map() 返回的导入表

        Returns:
            List[Dict]: [{"class": str, "package": str, "object": str}]
        """
        # D-10-03: 保持原始顺序（首次出现）
        seen = set()
        imports = []

        for imp in import_map:
            # D-10-01: 三元组格式
            key = (imp.class_name, imp.class_package, imp.object_name)

            # D-10-04: 合并重复
            if key not in seen:
                seen.add(key)
                imports.append({
                    "class": imp.class_name,
                    "package": imp.class_package,
                    "object": imp.object_name
                })

        return imports
    ```

    函数位置：放在 read_import_map() 之后、read_export_map() 之前（约 L1565）
    使用 Dict 类型需确保 typing 模块已导入（应已导入）
  </action>
  <verify>
    <automated>grep -n "def build_imports_list" uasset_read.py</automated>
    <automated>grep -c '"class": imp.class_name' uasset_read.py</automated>
    <automated>grep -c '"package": imp.class_package' uasset_read.py</automated>
    <automated>grep -c '"object": imp.object_name' uasset_read.py</automated>
  </verify>
  <acceptance_criteria>
    - uasset_read.py 包含 "def build_imports_list(import_map: List[ObjectImport]) -> List[Dict]" 函数签名
    - 函数位于 read_import_map() 之后
    - 函数体包含 seen = set() 去重逻辑
    - 函数体包含 "class": imp.class_name 输出
    - 函数体包含 "package": imp.class_package 输出
    - 函数体包含 "object": imp.object_name 输出
    - 函数返回 imports 列表
    - Docstring 包含 D-10-01/03/04 决策引用
  </acceptance_criteria>
  <done>build_imports_list() 函数实现完成，能将 ObjectImport 列表转换为结构化 dict 列表，合并重复条目。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 read_soft_object_paths() 函数</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L564-565 — PackageFileSummary soft_object_paths_count/offset 字段
    - uasset_read.py L74 — UE5_ADD_SOFTOBJECTPATH_LIST = 1008 常量
    - uasset_read.py L1171-1173 — 版本条件检查模式
    - uasset_read.py L153-520 — FArchive.read_name(), read_fstring() 方法
    - 10-CONTEXT.md D-10-06~09 — SoftObjectPaths 解析决策
    - 10-RESEARCH.md Pattern 1 — read_soft_object_paths 实现示例
  </read_first>
  <action>
    在 build_imports_list() 函数之后添加新函数（约 L1595 附近）：

    ```python
    def read_soft_object_paths(
        archive: FArchive,
        summary: PackageFileSummary,
        name_map: List[str]
    ) -> List[Dict]:
        """
        读取 SoftObjectPaths 数组（DEPS-02）。

        Per D-10-06: 实现完整解析
        Per D-10-07: {asset_path, sub_path} 格式
        Per D-10-09: 仅 UE5 >= 1008 时解析

        FSoftObjectPath 序列化格式（PackageFileSummary.cpp L282-285）：
        - AssetPath (FName: u32 index + u32 number)
        - SubPathString (FString: int32 length + UTF-8 bytes)

        Args:
            archive: FArchive 实例
            summary: PackageFileSummary 实例
            name_map: 已解析的名称表

        Returns:
            List[Dict]: [{"asset_path": str, "sub_path": str}]
        """
        # D-10-09: 版本条件检查
        is_ue5_file = summary.legacy_file_version <= -8
        if not is_ue5_file or summary.file_version_ue5 < UE5_ADD_SOFTOBJECTPATH_LIST:
            return []  # UE4 文件或 UE5 < 1008 无 SoftObjectPaths

        # 边界检查
        if summary.soft_object_paths_count <= 0:
            return []

        if summary.soft_object_paths_offset <= 0:
            return []

        archive.seek(summary.soft_object_paths_offset)

        soft_refs = []
        for _ in range(summary.soft_object_paths_count):
            # AssetPath: FName (index + number)
            asset_path = archive.read_name(name_map)

            # SubPathString: FString (length + UTF-8)
            sub_path = archive.read_fstring()

            # D-10-07: {asset_path, sub_path} 格式
            soft_refs.append({
                "asset_path": asset_path,
                "sub_path": sub_path
            })

        return soft_refs
    ```

    函数位置：放在 build_imports_list() 之后、read_export_map() 之前（约 L1595）
    确保正确使用 archive.read_name() 和 archive.read_fstring()
  </action>
  <verify>
    <automated>grep -n "def read_soft_object_paths" uasset_read.py</automated>
    <automated>grep -c '"asset_path": asset_path' uasset_read.py</automated>
    <automated>grep -c '"sub_path": sub_path' uasset_read.py</automated>
    <automated>grep -c 'UE5_ADD_SOFTOBJECTPATH_LIST' uasset_read.py</automated>
  </verify>
  <acceptance_criteria>
    - uasset_read.py 包含 "def read_soft_object_paths(archive: FArchive, summary: PackageFileSummary, name_map: List[str]) -> List[Dict]" 函数签名
    - 函数位于 build_imports_list() 之后
    - 函数体包含版本条件检查（is_ue5_file, file_version_ue5 >= UE5_ADD_SOFTOBJECTPATH_LIST）
    - 函数体包含边界检查（count > 0, offset > 0）
    - 函数体包含 archive.seek(summary.soft_object_paths_offset)
    - 函数体包含 archive.read_name(name_map) 调用
    - 函数体包含 archive.read_fstring() 调用
    - 函数体包含 "asset_path": asset_path 输出
    - 函数体包含 "sub_path": sub_path 输出
    - UE4 文件返回空数组
    - Docstring 包含 D-10-06/07/09 决策引用
  </acceptance_criteria>
  <done>read_soft_object_paths() 函数实现完成，能从 soft_object_paths_offset 解析 SoftObjectPaths 数组，UE4 文件返回空数组。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FArchive → 解析函数 | 二进制数据从文件读取后进入解析函数，需边界验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | soft_object_paths_count | mitigate | 边界检查：count <= 0 返回空数组 |
| T-10-03 | Tampering | soft_object_paths_offset | mitigate | 边界检查：offset <= 0 返回空数组 |
| T-10-04 | Tampering | AssetPath FName index | mitigate | archive.read_name() 已有边界验证 |
| T-10-05 | Tampering | SubPathString length | mitigate | archive.read_fstring() 已有边界验证 |
</threat_model>

<verification>
- grep 验证两个函数签名存在
- grep 验证输出字段格式正确
- grep 验证版本条件检查存在
- grep 验证边界检查存在
</verification>

<success_criteria>
- build_imports_list() 函数实现完成，能转换 ObjectImport 为 dict 格式
- read_soft_object_paths() 函数实现完成，能解析 SoftObjectPaths 数组
- 两个函数包含正确的决策 ID 引用
- UE4 文件返回空 soft_references
- 合并重复逻辑正确（使用 set 去重）
</success_criteria>

<output>
完成后创建 `.planning/phases/10-dependency-analysis/10-02-SUMMARY.md`
</output>