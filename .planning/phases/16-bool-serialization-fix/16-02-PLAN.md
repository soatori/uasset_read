---
phase: 16-bool-serialization-fix
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .claude/skills/uasset-read/scripts/uasset_read.py
autonomous: true
requirements: [FIX-02]
user_setup: []
must_haves:
  truths:
    - "ImportMap 解析的 b_import_optional 字段使用 4-byte 读取"
    - "FEdGraphPinType 的 4 个 bool 字段使用 read_bool()"
    - "其他蓝图图结构的 4 个 bool 字段使用 read_bool()"
    - "所有 16 个 bool 字段修复完成"
  artifacts:
    - path: ".claude/skills/uasset-read/scripts/uasset_read.py"
      provides: "ImportMap + 蓝图图结构 bool 字段修复"
      pattern: "archive.read_bool()"
  key_links:
    - from: "read_import_map()"
      to: "read_bool()"
      via: "line 1912 b_import_optional"
      pattern: "archive.read_bool()"
    - from: "read_ed_graph_pin_type()"
      to: "read_bool()"
      via: "4 个 bool 字段"
      pattern: "archive.read_bool()"
---

# Plan 16-02: ImportMap + 蓝图图结构修复

## Objective

修复剩余 9 个 bool 字段（ImportMap 1 个 + 蓝图图结构 8 个），完成所有 16 个 bool 序列化位置的修复。

**Purpose:** Plan 01 修复了 ExportMap 的 7 个 bool 字段，本计划修复剩余的 ImportMap 和蓝图图结构中的 bool 字段，确保整个解析器正确处理 UE bool 序列化。

**Output:** 所有 16 个 bool 字段使用 read_bool()

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-bool-serialization-fix/16-01-SUMMARY.md
@.planning/phases/16-bool-serialization-fix/16-RESEARCH.md

### 依赖说明

Plan 16-01 已添加 FArchive.read_bool() 方法，本计划使用该方法替换剩余的 bool 字段。

<interfaces>
<!-- Key positions from RESEARCH.md -->
剩余 9 个 bool 字段位置：

**ImportMap (line 1912):**
```python
b_import_optional = bool(archive.read_u8())  # 当前错误
```

**FEdGraphPinType (lines 2538-2555):**
```python
pin_type.is_reference = archive.read_u8() != 0  # line 2538
pin_type.is_weak_pointer = archive.read_u8() != 0  # line 2539
pin_type.is_const = archive.read_u8() != 0  # line 2550
pin_type.is_uobject_wrapper = archive.read_u8() != 0  # line 2555
```

**其他蓝图结构 (lines 2650, 2823, 2864, 2903, 3078):**
```python
has_parent = archive.read_u8() != 0  # UEdGraphPin, line 2650
b_self_context = archive.read_u8() != 0  # FMemberReference, line 2823
b_defaults_to_pure = archive.read_u8() != 0  # K2NodeCallFunction, line 2864
b_override_function = archive.read_u8() != 0  # K2NodeEvent, line 2903
b_editable = archive.read_u8() != 0  # UEdGraph, line 3078
```
</interfaces>

## Tasks

<task type="auto">
<name>Task 1: 修复 ImportMap 的 b_import_optional 字段</name>
<files>.claude/skills/uasset-read/scripts/uasset_read.py</files>
<read_first>
- .claude/skills/uasset-read/scripts/uasset_read.py (lines 1905-1920, read_import_map function)
</read_first>
<action>
替换 read_import_map() 函数中的 b_import_optional 字段（line 1912）：

当前实现（错误）：
```python
b_import_optional = bool(archive.read_u8())
```

修正实现：
```python
b_import_optional = archive.read_bool()
```

位置：line 1912，在 ObjectImport 结构解析中。

注意：保持其他字段不变，仅替换 bool 字段的读取方法。
</action>
<verify>
<automated>grep -c "bool(archive.read_u8())" .claude/skills/uasset-read/scripts/uasset_read.py</automated>
</verify>
<done>
read_import_map() 中无 bool(archive.read_u8()) 调用，grep 结果应为 8（剩余蓝图图结构）。
</done>
</task>

<task type="auto">
<name>Task 2: 修复 FEdGraphPinType 的 4 个 bool 字段</name>
<files>.claude/skills/uasset-read/scripts/uasset_read.py</files>
<read_first>
- .claude/skills/uasset-read/scripts/uasset_read.py (lines 2530-2560, read_ed_graph_pin_type function)
</read_first>
<action>
替换 read_ed_graph_pin_type() 函数中的 4 个 bool 字段（lines 2538, 2539, 2550, 2555）：

**替换清单：**

1. Line 2538：
   ```python
   # 当前：pin_type.is_reference = archive.read_u8() != 0
   pin_type.is_reference = archive.read_bool()
   ```

2. Line 2539：
   ```python
   # 当前：pin_type.is_weak_pointer = archive.read_u8() != 0
   pin_type.is_weak_pointer = archive.read_bool()
   ```

3. Line 2550（版本条件内）：
   ```python
   # 当前：pin_type.is_const = archive.read_u8() != 0
   pin_type.is_const = archive.read_bool()
   ```

4. Line 2555（版本条件内）：
   ```python
   # 当前：pin_type.is_uobject_wrapper = archive.read_u8() != 0
   pin_type.is_uobject_wrapper = archive.read_bool()
   ```

关键注意事项：
- 保持版本检查条件不变
- 位域字段（uint8 bIsReference:1）在序列化时转换为 4-byte bool（UE 源码验证）
- 仅替换读取方法，不修改赋值逻辑
</action>
<verify>
<automated>grep -c "archive.read_u8() != 0" .claude/skills/uasset-read/scripts/uasset_read.py</automated>
</verify>
<done>
read_ed_graph_pin_type() 中 4 个 bool 字段已替换，grep read_u8() != 0 结果应为 4（剩余其他蓝图结构）。
</done>
</task>

<task type="auto">
<name>Task 3: 修复其他蓝图图结构的 4 个 bool 字段</name>
<files>.claude/skills/uasset-read/scripts/uasset_read.py</files>
<read_first>
- .claude/skills/uasset-read/scripts/uasset_read.py (lines 2640-2660, read_ue_graph_pin)
- .claude/skills/uasset-read/scripts/uasset_read.py (lines 2815-2830, read_member_reference)
- .claude/skills/uasset-read/scripts/uasset_read.py (lines 2855-2870, read_k2node_call_function)
- .claude/skills/uasset-read/scripts/uasset_read.py (lines 2895-2910, read_k2node_event)
- .claude/skills/uasset-read/scripts/uasset_read.py (lines 3070-3085, read_ue_graph)
</read_first>
<action>
替换 4 个不同函数中的 bool 字段：

**1. read_ue_graph_pin() (line 2650):**
```python
# 当前：has_parent = archive.read_u8() != 0
has_parent = archive.read_bool()
```

**2. read_member_reference() (line 2823):**
```python
# 当前：b_self_context = archive.read_u8() != 0
b_self_context = archive.read_bool()
```

**3. read_k2node_call_function() (line 2864):**
```python
# 当前：b_defaults_to_pure = archive.read_u8() != 0
b_defaults_to_pure = archive.read_bool()
```

**4. read_k2node_event() (line 2903):**
```python
# 当前：b_override_function = archive.read_u8() != 0
b_override_function = archive.read_bool()
```

**5. read_ue_graph() (line 3078):**
```python
# 当前：b_editable = archive.read_u8() != 0
b_editable = archive.read_bool()
```

注意：每个位置都要保持周围代码不变，仅替换读取方法。
</action>
<verify>
<automated>grep -c "archive.read_u8() != 0" .claude/skills/uasset-read/scripts/uasset_read.py</automated>
</verify>
<done>
所有 archive.read_u8() != 0 已替换为 archive.read_bool()，grep 结果应为 0。
</done>
</task>

## Verification

### must_haves

- [ ] ImportMap 的 b_import_optional 使用 read_bool()
- [ ] FEdGraphPinType 的 4 个 bool 字段使用 read_bool()
- [ ] 其他蓝图图结构的 4 个 bool 字段使用 read_bool()
- [ ] grep -c "bool(archive.read_u8())" 返回 0
- [ ] grep -c "archive.read_u8() != 0" 返回 0

### truths

- 所有 16 个 bool 字段使用 read_bool()
- 修复完整，无遗漏位置
- 蓝图图解析正确工作

## Success Criteria

1. grep -c "bool(archive.read_u8())" 返回 0（所有替换完成）
2. grep -c "archive.read_u8() != 0" 返回 0（所有替换完成）
3. Python 语法检查通过：python -c "from uasset_read import FArchive; print('OK')"

## Output

After completion, create `.planning/phases/16-bool-serialization-fix/16-02-SUMMARY.md`