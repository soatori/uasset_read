# v8.0 — BP-to-CPP 翻译能力

**目标**: 使解析器能够输出足够的结构化 JSON，支持人工将 Blueprint 翻译为 C++ 代码。
**范围**: Phase 47–50 聚焦于补全 JSON 中的结构性缺口，不生成 C++ 代码。
JSON 输出需达到"人工可读、可对照 C++ 构造函数/头文件逐行翻译"的程度。

## Gap 分析

对比 `BP_FirstPersonCharacter.uasset` 解析结果与等价的 C++ 实现 (`FirstPersonCCharacter.cpp/h`)，
识别出以下无法自动化翻译的部分：

| # | Gap | 影响 | 严重程度 |
|---|-----|------|---------|
| G1 | `linked_to_raw` 全为空数组 | 执行流/数据流/连接全部为 0 | P0 | ✅ Phase 47 已修复 |
| G2 | 组件属性值未提取（位置/旋转/缩放/布尔标志） | C++ 构造函数数值不可见 | P0 |
| G3 | K2Node_CallFunction 的引脚参数类型缺失 | 函数签名无法推断 | P1 |
| G4 | EnhancedInputAction 触发事件类型（Started/Completed/Triggered）不可见 | BindAction 无法区分 | P1 |
| G5 | 中间 Knot 节点数据流链路断裂 | 输入转发逻辑丢失 | P1 |
| G6 | UPROPERTY/UFUNCTION 元数据（Category/可见性）不可提取 | C++ 宏修饰符无法推断 | P2 |

## Phase 分解

| Phase | 名称 | 目标 | 依赖 |
|-------|------|------|------|
| 47 | Pin LinkedTo 修复 | linked_to_raw 非空，connections > 0 | 无 | ✅ |
| 48 | 组件属性递归解析 | 输出组件的数值型属性 | 无 | 🔴 |
| 49 | 函数调用引脚解析 | CallFunction 节点的参数引脚完整 | 47 | 🔴 |
| 50 | EnhancedInput 语义增强 | TriggerEvent 类型、Action 路径 | 47 | 🔴 |

## Phase 47: Pin LinkedTo 修复 ✅ 完成

**修复内容:** `FEdGraphPinType` 中 5 个布尔字段 `read_bool_1byte()` → `read_bool()` (1B → 4B)，
修复约 15 字节偏移，使 `linked_to` 数组读取位置正确。

**结果:** `linked_to_raw` 从 0/30 pins 变为 16/43 pins，connections 非空 (EventGraph: 2, Aim: 1, Move: 1)，
execution_flows 有 7 条 flow 含 nodes 数组。

**测试:** 6/6 passed，0 新回归 (438 passed, 21 pre-existing failed, 67 skipped)。

## Phase 48: 组件属性递归解析

**Goal:** 输出组件的数值型属性（位置、旋转、缩放、布尔标志），与 C++ 构造函数中的配置对应。

**具体任务:**
- 在 `blueprint/` 中新增 `component_parser.py`，遍历 ExportMap 中的组件类型对象
- 解析组件的 PropertyTag 序列，提取嵌套结构体值：
  - `RelativeLocation` / `RelativeRotation` / `RelativeScale3D` → FVector/FRotator
  - `CapsuleRadius` / `CapsuleHalfHeight` → float
  - `bOnlyOwnerSee` / `bUsePawnControlRotation` → bool
  - `FieldOfView` / `FirstPersonScale` → float
- 在 JSON 输出的 `blueprint` 部分新增 `components` 数组
- 每个组件包含：名称、类型、父组件、属性值字典

**验证:**
- JSON 输出包含 `components` 数组，至少 2 个组件（FirstPersonMesh, FirstPersonCamera）
- `FirstPersonCameraComponent` 包含 `RelativeLocation=(-2.8, 5.89, 0.0)` 等数值
- `CharacterMovementComponent` 包含 `AirControl=0.5` 等数值

## Phase 49: 函数调用引脚解析

**Goal:** K2Node_CallFunction 节点的功能签名（参数名、类型、方向）完整提取。

**具体任务:**
- 在 `read_ue_graph_node()` 中，确保 CallFunction 节点的 pins 数组正确解析
- 从 pin 的 `pin_type.pin_category` 和 `pin_subcategory` 推断参数类型：
  - `exec` → 执行引脚（无类型）
  - `real` → float
  - `struct` + `Vector` → FVector
  - `object` + `Class` → UObject 派生
  - `wildcard` → 泛型/可变参数
- 区分 input pin（direction=0, exec 除外）和 output pin（direction=1）
- 在 `format_node_dict()` 中输出 `parameters` 数组
- 利用 linker 解析 `function_reference.member_parent` 为具体类名

**验证:**
- CallFunction 节点输出类似 `"parameters": [{"name": "Yaw", "type": "float", "direction": "input"}]`
- `DoAim(Yaw:float, Pitch:float)` 函数签名可识别
- `DoMove(Right:float, Forward:float)` 函数签名可识别

## Phase 50: EnhancedInput 语义增强

**Goal:** K2Node_EnhancedInputAction 节点的触发事件类型（Started/Completed/Triggered）可识别。

**具体任务:**
- 扩展 `read_k2node_enhanced_input()` 读取额外字段：
  - `InputTriggerType` 枚举值（Started/Ongoing/Completed/Triggered）
  - `bIsTriggered` 布尔标志
- 在 pin 级别，识别 exec output pin 的触发事件标签
  - UE5 EnhancedInput 节点有多个 exec output（Started, Triggered, Completed）
  - 每个 exec pin 需要关联到对应的 ETriggerEvent 枚举值
- 在 JSON 输出中标注每个 EnhancedInputAction 的触发类型映射
- 新增 `input_bindings` 数组，格式类似 C++ 的 `BindAction` 调用

**验证:**
- JSON 输出中 `input_bindings` 包含：
  - `JumpAction → Started → DoJumpStart`
  - `JumpAction → Completed → DoJumpEnd`
  - `MoveAction → Triggered → MoveInput`
  - `LookAction → Triggered → LookInput`
- 与 C++ `SetupPlayerInputComponent()` 中的 BindAction 一一对应

## 验证标准 — JSON 可翻译性

373+ 测试 0 回归。`BP_FirstPersonCharacter.uasset` 解析输出需覆盖 C++ 文件中以下结构：

### C++ 头文件可对照项

| C++ 声明 | JSON 来源 | Phase |
|----------|-----------|-------|
| `UPROPERTY(VisibleAnywhere) USkeletalMeshComponent*` | `blueprint.components[].type` | 48 |
| `UPROPERTY(EditAnywhere, Category="Camera") float FieldOfView` | `blueprint.variables[]` + `components[].properties` | 48 |
| `UFUNCTION(BlueprintCallable) void DoAim(float Yaw, float Pitch)` | `graphs[].nodes[].parameters[]` | 49 |
| `void SetupPlayerInputComponent(UInputComponent*)` | `input_bindings[]` | 50 |
| `virtual void BeginPlay()` | `graphs[].execution_flows[]` (Event BeginPlay 起点) | 47 |

### C++ 构造函数可对照项

| C++ 代码 | JSON 来源 | Phase |
|----------|-----------|-------|
| `FirstPersonCamera = CreateDefaultSubobject<UCameraComponent>("FirstPersonCamera")` | `components[].name + .type` | 48 |
| `FirstPersonCamera->RelativeLocation = FVector(-2.8f, 5.89f, 0.0f)` | `components[].properties.RelativeLocation` | 48 |
| `FirstPersonCamera->FieldOfView = 70.0f` | `components[].properties.FieldOfView` | 48 |
| `MovementComponent->AirControl = 0.5f` | `components[].properties.AirControl` | 48 |
| `EnhancedInput->BindAction(JumpAction, ETriggerEvent::Started, this, &DoJumpStart)` | `input_bindings[].action + .trigger + .target` | 50 |

### C++ 函数体可对照项

| C++ 逻辑 | JSON 来源 | Phase |
|----------|-----------|-------|
| `Super::BeginPlay()` → `SetInputMode(GameOnly)` | `execution_flows[].nodes[]` 执行链 | 47 |
| `AddControllerYawInput(Yaw)` | `CallFunction` 节点 + `parameters` | 49 |
| `if (bCanMove) MoveRight(Value)` | 条件分支 + `Knot` 转发链 | 47, 49 |

*Updated: 2026-05-15*
