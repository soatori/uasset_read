# v8.0 — BP-to-CPP 翻译能力

**目标**: 使解析器能够输出足够的结构化 JSON，支持人工将 Blueprint 翻译为 C++ 代码。
**范围**: Phase 47–50 聚焦于补全 JSON 中的结构性缺口，不生成 C++ 代码。
JSON 输出需达到"人工可读、可对照 C++ 构造函数/头文件逐行翻译"的程度。

## Gap 分析

对比 `BP_FirstPersonCharacter.uasset` 解析结果与等价的 C++ 实现 (`FirstPersonCCharacter.cpp/h`)，
识别出以下无法自动化翻译的部分：

| # | Gap | 影响 | 严重程度 | 状态 |
|---|-----|------|---------|------|
| G1 | `linked_to_raw` 全为空数组 | 执行流/数据流/连接全部为 0 | P0 | ✅ Phase 47 |
| G2 | 组件属性值未提取（位置/旋转/缩放/布尔标志） | C++ 构造函数数值不可见 | P0 | ✅ Phase 48 |
| G3 | K2Node_CallFunction 的引脚参数类型缺失 | 函数签名无法推断 | P1 | 🔴 Phase 49 |
| G4 | EnhancedInputAction 触发事件类型不可见 | BindAction 无法区分 | P1 | ✅ Phase 50 |
| G5 | 中间 Knot 节点数据流链路断裂 | 输入转发逻辑丢失 | P1 | 🔴 Phase 49 |
| G6 | UPROPERTY/UFUNCTION 元数据不可提取 | C++ 宏修饰符无法推断 | P2 | 待规划 |

## Phase 分解

| Phase | 名称 | 目标 | 依赖 | 状态 |
|-------|------|------|------|------|
| 47 | Pin LinkedTo 修复 | linked_to_raw 非空，connections > 0 | 无 | ✅ 完成 |
| 48 | 组件属性递归解析 | 输出组件的数值型属性 | 无 | ✅ 完成 |
| 49 | 函数调用引脚解析 | CallFunction 节点的参数引脚完整 | 47 | 🔴 未开始 |
| 50 | EnhancedInput 语义增强 | TriggerEvent 类型、Action 路径 | 47 | ✅ 完成 |

## Phase 47: Pin LinkedTo 修复 ✅

**修复:** `FEdGraphPinType` 5 个布尔字段 `read_bool_1byte()` → `read_bool()` (1B → 4B)，
修复约 15 字节偏移，使 `linked_to` 数组读取位置正确。

**结果:** `linked_to_raw` 从 0/30 pins → 16/43 pins，connections 非空，
execution_flows 有 7 条 flow 含 nodes 数组。

**测试:** 6/6 passed，0 新回归。

## Phase 48: 组件属性递归解析 ✅

**实现:** `component_extractor.py` — 从 ExportMap 发现 SCS 组件，提取变换
(Loc/Rot/Scale) 和标量属性(Float/Int/Bool/Byte/Enum/Struct)。
`_post_process()` 集成到解析管线，`ParseResult.components` 字段。

**JSON 序列化:** `_format_components()` 将 dataclass 变换值转换为 dict，
`format_json_summary()` 添加 `components_count`。

**验证:**
- JSON 输出包含 `components` 数组
- `FirstPersonCameraComponent` 包含 `RelativeLocation=(-2.8, 5.89, 0.0)` 等数值
- `CharacterMovementComponent` 包含 `AirControl=0.5` 等数值

## Phase 49: 函数调用引脚解析

**Goal:** K2Node_CallFunction 节点的功能签名（参数名、类型、方向）完整提取。

详见 `.planning/phases/49-function-call-pins/PLAN.md`

## Phase 50: EnhancedInput 语义增强 ✅

**实现:** `ETRIGGER_EVENT_PIN_MAP` 常量 + `K2NodeEnhancedInputAction.trigger_events` 字段。
从 exec 输出 pins 映射为 ETriggerEvent 枚举值（Started/Ongoing/Completed/Canceled）。

**变更文件:**
- `constants.py` — 新增 `ETRIGGER_EVENT_PIN_MAP`
- `models/node_types.py` — `K2NodeEnhancedInputAction` 添加 `trigger_events: List[str]`
- `serializers/graph.py` — `_extract_trigger_events()` + `create_node_from_archive()` 集成

**验证:**
- `K2NodeEnhancedInputAction` 节点输出 `trigger_events` 数组
- 与 C++ `SetupPlayerInputComponent()` 中的 BindAction 对应

## Phase 51: 二进制输出清理 🔴

**目标**: 消除 `pin_tooltip`、`default_value`、`auto_default_value` 中的二进制/null 字节泄漏。

**根因**: FText 解析对无效 history_type（如 -121）消费 0 字节，导致后续 FString 读取错位。

**变更文件:**
- `archive.py` — `read_fstring()` 输出验证（null 比例检测）
- `serializers/graph.py` — `read_ftext_with_history()` 无效类型处理 + pin_tooltip 验证
- `formatters/json_formatter.py` — `_sanitize_string()` 防御性过滤

**验证:** JSON

554 测试 0 新回归。`BP_FirstPersonCharacter.uasset` 解析输出需覆盖 C++ 文件中以下结构：

### C++ 头文件可对照项

| C++ 声明 | JSON 来源 | Phase | 状态 |
|----------|-----------|-------|------|
| `UPROPERTY(VisibleAnywhere) USkeletalMeshComponent*` | `blueprint.components[].type` | 48 | ✅ |
| `UPROPERTY(EditAnywhere, Category="Camera") float FieldOfView` | `blueprint.variables[]` + `components[].properties` | 48 | ✅ |
| `UFUNCTION(BlueprintCallable) void DoAim(float Yaw, float Pitch)` | `graphs[].nodes[].parameters[]` | 49 | 🔴 |
| `void SetupPlayerInputComponent(UInputComponent*)` | `input_bindings[]` | 50 | ✅ |
| `virtual void BeginPlay()` | `graphs[].execution_flows[]` (Event BeginPlay 起点) | 47 | ✅ |

### C++ 构造函数可对照项

| C++ 代码 | JSON 来源 | Phase | 状态 |
|----------|-----------|-------|------|
| `FirstPersonCamera = CreateDefaultSubobject<UCameraComponent>("FirstPersonCamera")` | `components[].name + .type` | 48 | ✅ |
| `FirstPersonCamera->RelativeLocation = FVector(-2.8f, 5.89f, 0.0f)` | `components[].properties.RelativeLocation` | 48 | ✅ |
| `FirstPersonCamera->FieldOfView = 70.0f` | `components[].properties.FieldOfView` | 48 | ✅ |
| `MovementComponent->AirControl = 0.5f` | `components[].properties.AirControl` | 48 | ✅ |
| `EnhancedInput->BindAction(JumpAction, ETriggerEvent::Started, this, &DoJumpStart)` | `input_bindings[].action + .trigger + .target` | 50 | ✅ |

### C++ 函数体可对照项

| C++ 逻辑 | JSON 来源 | Phase | 状态 |
|----------|-----------|-------|------|
| `Super::BeginPlay()` → `SetInputMode(GameOnly)` | `execution_flows[].nodes[]` 执行链 | 47 | ✅ |
| `AddControllerYawInput(Yaw)` | `CallFunction` 节点 + `parameters` | 49 | 🔴 |
| `if (bCanMove) MoveRight(Value)` | 条件分支 + `Knot` 转发链 | 47, 49 | 🔴 |

*Updated: 2026-05-16*
