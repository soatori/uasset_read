# Milestone v8.0: BP-to-CPP 翻译能力

**Status:** ✅ SHIPPED 2026-05-17
**Phases:** 47-51
**Total Plans:** 5

## Overview

对比 `BP_FirstPersonCharacter.uasset` 与等价 C++ 实现 (`FirstPersonCCharacter.cpp/h`)，
识别出 6 个结构性 gap，分 5 个 phase 解决。最终实现 JSON 输出可对照 C++ 逐行翻译。

## Phases

### Phase 47: Pin LinkedTo 修复

**Goal**: linked_to_raw 非空，connections > 0，execution_flows[].nodes 非空
**Depends on**: Phase 46 (v7.0 测试验证)
**Plans**: 1 plan

Plans:

- [x] 47-01: Fix Pin LinkedTo serialization

**Details:**

linked_to_raw 从 0/30 pins → 16/43 pins。connections 非空 (EventGraph: 2, Aim: 1, Move: 1)。execution_flows 有 7 条 flow。根因: read_bool_1byte() → read_bool() 序列化偏移修正。

### Phase 48: 组件属性递归解析

**Goal**: 输出组件数值型属性（位置/旋转/缩放/标志）
**Depends on**: Phase 47
**Plans**: 1 plan

Plans:

- [x] 48-01: extract_components() + JSON 序列化

**Details:**

components[] 数组包含位置/旋转/缩放/标志等数值属性。git commit `217257c`。Phase 目录已清理，仅保留 git 历史作为证据。

### Phase 49: 函数调用引脚解析

**Goal**: CallFunction 参数引脚完整，JSON 提供 parameters 数组
**Depends on**: Phase 47
**Plans**: 1 plan

Plans:

- [x] 49-01: _extract_call_function_parameters() + JSON parameters field

**Details:**

K2Node_CallFunction 节点输出 parameters 数组（input_params + output_params）。7/7 tests passed。最小侵入方案 — formatter 层提取，不修改序列化层。

### Phase 50: EnhancedInput 语义增强

**Goal**: TriggerEvent 类型可识别，input_bindings 与 C++ BindAction 对应
**Depends on**: Phase 47
**Plans**: 1 plan

Plans:

- [x] 50-01: ETRIGGER_EVENT_PIN_MAP + K2Node trigger_events extraction

**Details:**

trigger_events 字段从 pins 提取，3/4 nodes 成功。ETRIGGER_EVENT_PIN_MAP 正确映射 pin 名到 ETriggerEvent 值。execution_flows 包含 EnhancedInput 流。

### Phase 51: 二进制输出清理

**Goal**: 消除 JSON 中 binary/null 泄漏
**Depends on**: Phase 47-50
**Plans**: 1 plan

Plans:

- [x] 51-01: Binary output sanitization — null char detection + string cleaning

**Details:**

read_fstring() null_ratio > 0.3 检测 + FText history_type 范围校验 + pin_tooltip 二进制检测 + JSON formatter 防御性清理。ZERO \x00 escapes in JSON output。484 passed, 0 new failures。

---

## Milestone Summary

**Key Decisions:**

- Decision: 按 gap 分 phase（Rationale: 每个 gap 独立可验证，便于增量交付）
- Decision: UAT 优先于 VERIFICATION.md（Rationale: BP 解析质量更适合端到端验证）
- Decision: Phase 48 目录清理（Rationale: 所有 artifacts 已完成，仅保留 git 历史）

**Issues Resolved:**

- linked_to_raw 全为空导致执行流追踪断裂
- 组件数值属性不可见（位置/旋转/缩放）
- CallFunction 参数签名不可提取
- EnhancedInput 触发事件不可区分
- JSON 输出泄漏 binary/null 字符

**Technical Debt Incurred:**

- Phase 50: input_action_path 为空 — 可能需要进一步调查蓝图序列化
- Phase 50: 1/4 EnhancedInput 节点有 _parse_error: True
- 预存在 26 个测试失败（资产版本问题，与 v8.0 无关）

---

_For current project status, see .planning/ROADMAP.md_
