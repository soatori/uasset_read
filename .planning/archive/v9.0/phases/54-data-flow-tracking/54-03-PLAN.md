---
phase: 54-data-flow-tracking
plan: 03
type: execute
wave: 2
depends_on: [54-02]
files_modified: [src/uasset_read/graph/flow_builder.py, src/uasset_read/formatters/json_formatter.py, tests/test_output_formatting.py]
autonomous: true
requirements: [DATA-01, DATA-03]
user_setup: []

must_haves:
  truths:
    - "CallFunction 参数能标注 data_source（Pure 函数/FunctionEntry/Knot）"
    - "Pure 函数节点能标注 data_providers（数据去向）"
    - "SubPin 第一级展开能标注字段级数据来源"
    - "Move 函数完整数据流追踪验证通过"
  artifacts:
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "_trace_data_source 函数"
      contains: "def _trace_data_source"
    - path: "src/uasset_read/formatters/json_formatter.py"
      provides: "_extract_call_function_parameters 增强"
      contains: "data_source"
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "_trace_execution_from_event Pure 函数标注"
      contains: "data_providers"
  key_links:
    - from: "_trace_data_source"
      to: "_extract_call_function_parameters"
      via: "CallFunction input 参数数据源标注"
      pattern: "input_params.*data_source"
    - from: "_trace_execution_from_event"
      to: "Pure 函数节点"
      via: "正向 data_providers 标注"
      pattern: "data_providers.*pure"
---

<objective>
集成数据标注到执行流输出 — Wave 2

Purpose: 将数据流追踪能力集成到现有执行流和数据提取函数，实现双向数据标注。
Output: `_trace_data_source` + `_extract_call_function_parameters 增强` + Pure 函数 data_providers 标注
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-data-flow-tracking/54-RESEARCH.md

# Wave 1 已完成
@.planning/phases/54-data-flow-tracking/54-02-SUMMARY.md

# 现有函数结构
@src/uasset_read/graph/flow_builder.py L338-380
@src/uasset_read/formatters/json_formatter.py L474-503
</context>

<interfaces>
<!-- 需要修改的现有接口 -->

From src/uasset_read/graph/flow_builder.py L338-356:
```python
if current_node.class_name == "K2Node_CallFunction":
    # Phase 49: simplified params for execution flow
    node_info["params"] = [
        {"name": pin.pin_name, "type": pin.pin_type.pin_category if pin.pin_type else ""}
        for pin in current_node.pins
        if pin.pin_type and pin.pin_type.pin_category != "exec" and pin.direction == 0
    ]
    # Phase 53: mark pure functions with "pure": true in flow
    has_exec_pin = any(pin.pin_type and pin.pin_type.pin_category == "exec" for pin in current_node.pins)
    if not has_exec_pin:
        node_info["pure"] = True
```

From src/uasset_read/formatters/json_formatter.py L474-503:
```python
def _extract_call_function_parameters(node: Any) -> Dict[str, List[Dict]]:
    """从 K2Node_CallFunction 节点的 pins 中提取函数参数（Phase 49）。"""
    input_params: List[Dict] = []
    output_params: List[Dict] = []

    for pin in node.pins:
        if pin.pin_type and pin.pin_type.pin_category == "exec":
            continue

        param: Dict[str, Any] = {
            "name": pin.pin_name,
            "pin_category": pin.pin_type.pin_category if pin.pin_type else "",
        }
        # ... 其他字段 ...
        
        if pin.direction == 0:
            input_params.append(param)
        else:
            output_params.append(param)

    return {"input_params": input_params, "output_params": output_params}
```

From src/uasset_read/models/core.py:
```python
@dataclass
class UEdGraphPin:
    sub_pins: List[dict] = field(default_factory=list)  # SubPin 字段
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 _trace_data_source 函数</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <behavior>
    - Test 1: CallFunction input pin 返回 data_source dict
    - Test 2: Pure 函数 ReturnValue 返回 source_type == "pure_function"
    - Test 3: FunctionEntry 输出返回 source_type == "function_parameter"
    - Test 4: self pin 返回 source_type == "self_reference"
    - Test 5: 无连接 pin 返回 None 或 source_type == "default_value"
    - Test 6: Knot 链穿透后到达终端节点
  </behavior>
  <action>
在 `src/uasset_read/graph/flow_builder.py` 中实现 `_trace_data_source` 函数。

**位置：** 在 `_resolve_knot_chain` 函数之后

**函数签名：**
```python
def _trace_data_source(
    pin: UEdGraphPin,
    pin_lookup: Dict[str, Tuple[str, str]],
    node_lookup: Dict[str, UEdGraphNode],
    node_name_lookup: Dict[str, str]
) -> Optional[Dict]:
    """追踪单个参数的数据来源（Phase 54）。

    Args:
        pin: 目标 pin（CallFunction input pin）
        pin_lookup: pin_id → (node_guid, pin_name) 查找表
        node_lookup: node_guid → node 查找表
        node_name_lookup: node_guid → node_name 查找表

    Returns:
        Optional[Dict]: 数据来源标注，或 None（默认值/无连接）
    """
```

**实现要点：**
- 检查 `pin.linked_to_raw` 是否为空
- 调用 `_resolve_knot_chain` 穿透 Knot 链
- 调用 `is_boundary_node` 检测边界
- 根据 source_type 分类：function_parameter, pure_function, self_reference, boundary, default_value

**实现逻辑（参考 RESEARCH.md L469-560）：**
```python
# 检查是否有连接
if not pin.linked_to_raw:
    if pin.default_value is not None and pin.default_value != "":
        return {"source_type": "default_value", "value": pin.default_value}
    return None

sources: List[Dict] = []
for linked_ref in pin.linked_to_raw:
    target_pin_guid = linked_ref.get("pin_guid") if isinstance(linked_ref, dict) else linked_ref
    
    # Knot 穿透
    terminal_pin_guid, success = _resolve_knot_chain(target_pin_guid, pin_lookup, node_lookup)
    if not success:
        sources.append({"source_type": "knot_chain_broken", "pin_guid": terminal_pin_guid})
        continue
    
    # 获取终端节点
    terminal_node_guid, terminal_pin_name = pin_lookup.get(terminal_pin_guid, (None, None))
    if not terminal_node_guid:
        sources.append({"source_type": "pin_not_found", "pin_guid": terminal_pin_guid})
        continue
    
    terminal_node = node_lookup.get(terminal_node_guid)
    if not terminal_node:
        sources.append({"source_type": "node_not_found", "node_guid": terminal_node_guid})
        continue
    
    # 边界检测
    if is_boundary_node(terminal_node, terminal_pin_name):
        if terminal_node.class_name == "K2Node_FunctionEntry":
            node_name = node_name_lookup.get(terminal_node_guid, terminal_node_guid)
            sources.append({
                "source_type": "function_parameter",
                "node": node_name,
                "pin": terminal_pin_name
            })
        elif terminal_pin_name.lower() == "self":
            sources.append({"source_type": "self_reference"})
        else:
            node_name = node_name_lookup.get(terminal_node_guid, terminal_node_guid)
            sources.append({
                "source_type": "boundary",
                "node": node_name,
                "pin": terminal_pin_name
            })
    else:
        # Pure function output
        if terminal_node.class_name == "K2Node_CallFunction":
            has_exec_pin = any(p.pin_type and p.pin_type.pin_category == "exec" for p in terminal_node.pins)
            node_name = node_name_lookup.get(terminal_node_guid, terminal_node_guid)
            
            # 获取函数名
            func_name = None
            nd = terminal_node.node_data
            if nd:
                fr = nd.get("function_reference") if isinstance(nd, dict) else getattr(nd, 'function_reference', None)
                if fr:
                    func_name = getattr(fr, 'member_name', None)
            
            sources.append({
                "source_type": "pure_function" if not has_exec_pin else "function_output",
                "node": node_name,
                "function_name": func_name,
                "pin": terminal_pin_name
            })

return {"data_sources": sources} if sources else None
```

**注意：**
- 返回值包含 `data_sources` 数组（支持多个数据源）
- source_type 分类清晰（便于后续翻译）

添加函数后，确保能被导入：
```python
from uasset_read.graph.flow_builder import _trace_data_source
```
  </action>
  <verify>
    <automated>grep -c "def _trace_data_source" src/uasset_read/graph/flow_builder.py && python -c "from uasset_read.graph.flow_builder import _trace_data_source; print('imported')" | grep "imported"</automated>
  </verify>
  <done>_trace_data_source 函数实现成功，能追踪数据来源并分类返回，能被导入使用</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增强 _extract_call_function_parameters 添加 data_source 字段</name>
  <files>src/uasset_read/formatters/json_formatter.py</files>
  <behavior>
    - Test 1: input_params 中每个参数包含 data_source 字段
    - Test 2: Pure 函数 ReturnValue 的 data_source 标注正确
    - Test 3: Knot 链穿透后的 data_source 标注正确
    - Test 4: FunctionEntry 参数的 data_source 标注正确
  </behavior>
  <action>
修改 `src/uasset_read/formatters/json_formatter.py` 中的 `_extract_call_function_parameters` 函数。

**修改位置：** L474-503

**修改要点：**
- 为 input_params 中每个参数添加 `data_source` 字段
- 需要传入 `pin_lookup`, `node_lookup`, `node_name_lookup` 参数
- 调用 `_trace_data_source` 函数

**函数签名修改：**
```python
def _extract_call_function_parameters(
    node: Any,
    pin_lookup: Optional[Dict[str, Tuple[str, str]]] = None,
    node_lookup: Optional[Dict[str, UEdGraphNode]] = None,
    node_name_lookup: Optional[Dict[str, str]] = None
) -> Dict[str, List[Dict]]:
    """从 K2Node_CallFunction 节点的 pins 中提取函数参数（Phase 49 + Phase 54）。

    Phase 54: 增强 input_params 的 data_source 字段。
    """
```

**实现逻辑：**
```python
input_params: List[Dict] = []
output_params: List[Dict] = []

for pin in node.pins:
    if pin.pin_type and pin.pin_type.pin_category == "exec":
        continue

    param: Dict[str, Any] = {
        "name": pin.pin_name,
        "pin_category": pin.pin_type.pin_category if pin.pin_type else "",
    }
    # ... 其他字段（pin_subcategory, is_reference, default_value）...

    # Phase 54: 添加 data_source 字段（仅 input params）
    if pin.direction == 0:  # Input
        if pin_lookup and node_lookup and node_name_lookup:
            from uasset_read.graph.flow_builder import _trace_data_source
            data_source = _trace_data_source(pin, pin_lookup, node_lookup, node_name_lookup)
            if data_source:
                param["data_source"] = data_source
        
        input_params.append(param)
    else:  # Output
        output_params.append(param)

return {"input_params": input_params, "output_params": output_params}
```

**注意：**
- `pin_lookup`, `node_lookup`, `node_name_lookup` 参数可选（向后兼容）
- 仅 input_params 添加 data_source（output_params 不需要）
- 使用 try-except 或条件检查确保向后兼容

修改后，确保现有测试不失败：
```bash
python -m pytest tests/test_output_formatting.py -x -k "call_function"
```
  </action>
  <verify>
    <automated>grep -c "data_source" src/uasset_read/formatters/json_formatter.py && python -m pytest tests/test_output_formatting.py -x -k "call_function" 2>&1 | grep -E "passed|PASSED"</automated>
  </verify>
  <done>_extract_call_function_parameters 增强 data_source 字段成功，向后兼容，现有测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 增强 _trace_execution_from_event 传递 lookup 并标注 Pure 函数 data_providers</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <behavior>
    - Test 1: CallFunction 节点在 execution_flow 中包含完整参数信息
    - Test 2: Pure 函数节点在 execution_flow 中包含 data_providers 字段
    - Test 3: Move 函数完整数据流验证通过
  </behavior>
  <action>
修改 `src/uasset_read/graph/flow_builder.py` 中的 `_trace_execution_from_event` 函数和 `build_execution_flows` 函数。

**修改位置：**
- `_trace_execution_from_event` L292-380
- `build_execution_flows` L456-495

**修改要点：**
1. 在 `build_execution_flows` 中构建 `node_name_lookup` 并传递给 `_trace_execution_from_event`
2. 在 `_trace_execution_from_event` 中调用增强的 `_extract_call_function_parameters`
3. 在 `_trace_execution_from_event` 中为 Pure 函数添加 `data_providers` 标注

**build_execution_flows 修改：**
```python
def build_execution_flows(graph: UEdGraph) -> List[Dict]:
    """构建执行流路径（D-08-07~11, D-19-10~12, Phase 54）。"""
    pin_lookup: Dict[str, Tuple[str, str]] = {}
    node_lookup: Dict[str, UEdGraphNode] = {}
    node_name_lookup: Dict[str, str] = {}  # Phase 54: 新增

    for node in graph.nodes:
        node_lookup[node.node_guid] = node
        for pin in node.pins:
            pin_lookup[pin.pin_id] = (node.node_guid, pin.pin_name)
    
    # Phase 54: 构建 node_name_lookup
    for idx, node in enumerate(graph.nodes):
        node_name_lookup[node.node_guid] = _derive_node_name(node, idx)

    execution_flows: List[Dict] = []
    start_nodes = [n for n in graph.nodes if n.class_name in START_EVENT_TYPES]

    for start_node in start_nodes:
        # ... EnhancedInputAction 处理 ...
        flow = _trace_execution_from_event(start_node, pin_lookup, node_lookup, node_name_lookup)  # Phase 54: 传递 node_name_lookup
        # ...
    
    return execution_flows
```

**_trace_execution_from_event 修改：**
```python
def _trace_execution_from_event(
    start_node: UEdGraphNode,
    pin_lookup: Dict[str, Tuple[str, str]],
    node_lookup: Dict[str, UEdGraphNode],
    node_name_lookup: Dict[str, str] = {}  # Phase 54: 新增参数
) -> List[Dict]:
    """追踪单条执行流（D-08-07~11, D-19-13~14, Phase 54）。"""
    # ... 现有逻辑 ...

    if current_node.class_name == "K2Node_CallFunction":
        # Phase 54: 使用增强的 _extract_call_function_parameters
        from uasset_read.formatters.json_formatter import _extract_call_function_parameters
        node_info["parameters"] = _extract_call_function_parameters(
            current_node, pin_lookup, node_lookup, node_name_lookup
        )
        
        # Phase 53: mark pure functions with "pure": true
        has_exec_pin = any(pin.pin_type and pin.pin_type.pin_category == "exec" for pin in current_node.pins)
        if not has_exec_pin:
            node_info["pure"] = True
            
            # Phase 54: Pure 函数 data_providers 标注
            data_providers: List[Dict] = []
            for pin in current_node.pins:
                if pin.direction == 1 and pin.pin_type and pin.pin_type.pin_category != "exec":
                    # 找到 output pin 的连接目标
                    for linked_ref in (pin.linked_to_raw or []):
                        target_pin_guid = linked_ref.get("pin_guid") if isinstance(linked_ref, dict) else linked_ref
                        if target_pin_guid in pin_lookup:
                            target_node_guid, target_pin_name = pin_lookup[target_pin_guid]
                            target_node_name = node_name_lookup.get(target_node_guid, target_node_guid)
                            data_providers.append({
                                "output_pin": pin.pin_name,
                                "target_node": target_node_name,
                                "target_pin": target_pin_name
                            })
            
            if data_providers:
                node_info["data_providers"] = data_providers
```

**注意：**
- `_extract_call_function_parameters` 替换现有的 `node_info["params"]`
- Pure 函数 data_providers 仅标注 output pin 的去向
- 保持向后兼容（node_name_lookup 默认 {}）

修改后，确保现有测试不失败：
```bash
python -m pytest tests/test_output_formatting.py -x -k "execution_flow"
```
  </action>
  <verify>
    <automated>grep -c "node_name_lookup" src/uasset_read/graph/flow_builder.py && grep -c "data_providers" src/uasset_read/graph/flow_builder.py && python -m pytest tests/test_output_formatting.py -x -k "execution_flow" 2>&1 | grep -E "passed|PASSED"</automated>
  </verify>
  <done>_trace_execution_from_event 增强成功，能传递 lookup 参数，能标注 Pure 函数 data_providers，现有测试通过</done>
</task>

<task type="auto">
  <name>Task 4: 启用剩余测试并验证完整数据流追踪</name>
  <files>tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: test_trace_data_source_pure_function 取消 skip 并通过
    - Test 2: test_trace_data_source_self_reference 取消 skip 并通过
    - Test 3: test_sub_pin_first_level_expand 取消 skip 并通过
    - Test 4: test_data_providers_pure_function 取消 skip 并通过
    - Test 5: Move 函数完整数据流集成测试通过
  </behavior>
  <action>
在 `tests/test_output_formatting.py` 中取消剩余测试的 skip 标记，添加完整集成测试。

**取消 skip：**
移除以下测试的 `@pytest.mark.skip` 装饰器：
- test_trace_data_source_pure_function
- test_trace_data_source_self_reference
- test_sub_pin_first_level_expand
- test_data_providers_pure_function

**实现测试断言：**

1. **test_trace_data_source_pure_function** — 验证 Pure 函数数据源
```python
def test_trace_data_source_pure_function(sample_function_graph_with_data_flow):
    """验证 Pure 函数 ReturnValue 作为数据源"""
    graph = sample_function_graph_with_data_flow
    
    from uasset_read.graph import build_execution_flows
    
    flows = build_execution_flows(graph)
    
    # 找到 CallFunction_7445 的 flow
    flow = next(f for f in flows if "FunctionEntry" in f.get("start_event", ""))
    
    # 找到 CallFunction_7445 节点
    call_node = next(n for n in flow["nodes"] if "7445" in n.get("node_guid", ""))
    
    # 验证 WorldDirection 参数的 data_source
    params = call_node.get("parameters", {})
    input_params = params.get("input_params", [])
    
    world_dir_param = next(p for p in input_params if p["name"] == "WorldDirection")
    assert "data_source" in world_dir_param, "WorldDirection有data_source"
    
    data_sources = world_dir_param["data_source"].get("data_sources", [])
    assert len(data_sources) > 0, "有数据源"
    
    # 验证来自 Pure 函数
    pure_source = next(s for s in data_sources if s["source_type"] == "pure_function")
    assert pure_source["function_name"] == "GetActorRightVector", "数据来自GetActorRightVector"
```

2. **test_data_providers_pure_function** — 验证 Pure 函数正向标注
```python
def test_data_providers_pure_function(sample_function_graph_with_data_flow):
    """验证 Pure 函数 data_providers 字段"""
    graph = sample_function_graph_with_data_flow
    
    from uasset_read.graph import build_execution_flows
    
    flows = build_execution_flows(graph)
    
    # 找到包含 Pure 函数的 flow
    flow = next(f for f in flows if "FunctionEntry" in f.get("start_event", ""))
    
    # 找到 Pure 函数节点（GetActorRightVector）
    pure_node = next(
        (n for n in flow["nodes"] 
         if n.get("pure") == True and "GetActorRightVector" in str(n.get("function_name", ""))),
        None
    )
    
    if pure_node:
        # 验证 data_providers 字段
        assert "data_providers" in pure_node, "Pure函数有data_providers"
        
        providers = pure_node["data_providers"]
        assert len(providers) > 0, "有数据去向标注"
        
        # 验证去向为 CallFunction.WorldDirection
        provider = next(p for p in providers if p["output_pin"] == "ReturnValue")
        assert "CallFunction" in provider["target_node"], "数据去向为CallFunction"
```

3. **test_move_function_complete_data_flow** — Move 函数完整集成测试
```python
def test_move_function_complete_data_flow(sample_function_graph_with_data_flow):
    """Move 函数完整数据流追踪验证"""
    graph = sample_function_graph_with_data_flow
    
    from uasset_read.graph import build_execution_flows
    
    flows = build_execution_flows(graph)
    
    # 验证至少有一个 flow
    assert len(flows) > 0, "有执行流"
    
    # 验证 FunctionEntry flow
    func_flow = next(f for f in flows if "FunctionEntry.Move" in f.get("start_event", ""))
    assert func_flow, "FunctionEntry flow存在"
    
    # 验证 CallFunction 节点有 data_source
    call_nodes = [n for n in func_flow["nodes"] if n.get("node_type") == "K2Node_CallFunction"]
    assert len(call_nodes) > 0, "有CallFunction节点"
    
    for node in call_nodes:
        params = node.get("parameters", {})
        input_params = params.get("input_params", [])
        
        # 至少有一个参数有 data_source
        params_with_source = [p for p in input_params if "data_source" in p]
        assert len(params_with_source) > 0, f"{node.get('function_name')}有data_source标注"
```

**运行完整测试验证：**
```bash
python -m pytest tests/test_output_formatting.py -x -k "trace_data or data_provider or move_function"
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -x -k "trace_data or data_provider or move_function" 2>&1 | grep -E "passed|PASSED" | head -10</automated>
  </verify>
  <done>所有数据流追踪测试通过，Move 函数完整数据流验证成功，Phase 54 完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 数据流追踪输出 | 内部数据结构标注，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03-01 | Information Disclosure | data_source 输出 | accept | 输出为内部蓝图结构，无敏感信息 |
| T-54-SC | Tampering | pytest 测试 | accept | 测试代码无外部输入风险 |

数据标注为纯内部处理，无安全风险。
</threat_model>

<verification>
- CallFunction input_params 包含 data_source 字段
- Pure 函数节点包含 data_providers 字段
- Move 函数完整数据流追踪通过
- 所有测试通过（pytest -k "trace_data"）
</verification>

<success_criteria>
- `_trace_data_source` 函数实现并测试通过
- `_extract_call_function_parameters` 增强 data_source 字段
- `_trace_execution_from_event` 增强 Pure 函数 data_providers 标注
- 6 个数据流追踪测试通过
- Move 函数完整集成测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/54-data-flow-tracking/54-03-SUMMARY.md`
并更新 `.planning/ROADMAP.md` 标记 Phase 54 完成
</output>