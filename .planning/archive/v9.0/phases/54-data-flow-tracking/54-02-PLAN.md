---
phase: 54-data-flow-tracking
plan: 02
type: execute
wave: 1
depends_on: [54-01]
files_modified: [src/uasset_read/constants.py, src/uasset_read/graph/flow_builder.py, tests/test_output_formatting.py]
autonomous: true
requirements: [DATA-01, DATA-02]
user_setup: []

must_haves:
  truths:
    - "_resolve_knot_chain 能穿透多级 Knot 链到达终端节点"
    - "is_boundary_node 能正确识别 FunctionEntry/self/VariableSet 边界"
    - "Knot 链穿透测试通过（无循环检测失败）"
    - "边界检测测试通过（FunctionEntry 参数正确停止追踪）"
  artifacts:
    - path: "src/uasset_read/constants.py"
      provides: "DATA_BOUNDARY_NODES 常量"
      contains: "DATA_BOUNDARY_NODES = frozenset"
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "_resolve_knot_chain + is_boundary_node 函数"
      min_lines: 30
  key_links:
    - from: "_resolve_knot_chain"
      to: "is_boundary_node"
      via: "递归穿透后边界检测"
      pattern: "if is_boundary_node.*return.*boundary"
---

<objective>
实现数据流核心追踪函数 — Wave 1

Purpose: 构建 Knot 穿透和边界检测能力，为数据源追踪提供基础。
Output: `_resolve_knot_chain` + `is_boundary_node` + `DATA_BOUNDARY_NODES` 常量
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-data-flow-tracking/54-RESEARCH.md

# Wave 0 已完成
@.planning/phases/54-data-flow-tracking/54-01-SUMMARY.md

# 现有 flow_builder.py 结构
@src/uasset_read/graph/flow_builder.py L250-380

# 现有 constants.py 结构
@src/uasset_read/constants.py L130-155
</context>

<interfaces>
<!-- 需要使用的现有接口 -->

From src/uasset_read/graph/flow_builder.py:
```python
# 现有的 lookup 结构（Phase 53 已建立）
pin_lookup: Dict[str, Tuple[str, str]] = {}  # pin_id → (node_guid, pin_name)
node_lookup: Dict[str, UEdGraphNode] = {}     # node_guid → node

# 现有的 node_data 解析
def _get(key):
    if isinstance(nd, dict):
        return nd.get(key)
    return getattr(nd, key, None)
```

From src/uasset_read/models/core.py:
```python
@dataclass
class UEdGraphNode:
    node_guid: str
    class_name: str
    pins: List[UEdGraphPin]
    node_data: Optional[Any] = None
```

From src/uasset_read/models/node_types.py:
```python
# Knot 类型判断
if node.class_name == "K2Node_Knot":
    # Knot 透明穿透
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 DATA_BOUNDARY_NODES 常量</name>
  <files>src/uasset_read/constants.py</files>
  <behavior>
    - Test 1: DATA_BOUNDARY_NODES 包含 K2Node_FunctionEntry
    - Test 2: DATA_BOUNDARY_NODES 包含 K2Node_VariableSet
    - Test 3: 常量能被 flow_builder.py 导入使用
  </behavior>
  <action>
在 `src/uasset_read/constants.py` 中添加 `DATA_BOUNDARY_NODES` frozenset 常量。

**位置：** 在 `START_EVENT_TYPES` 定义之后（约 L155）

**内容：**
```python
# ============================================================================
# 数据流边界节点集合（Phase 54）
# ============================================================================

DATA_BOUNDARY_NODES = frozenset({
    "K2Node_FunctionEntry",  # 函数参数输出作为数据流起点
    "K2Node_VariableSet",    # 本地变量定义（边界）
})
```

**实现要点：**
- 使用 frozenset（不可变集合）
- 包含 FunctionEntry 和 VariableSet
- **不包含 VariableGet**（VariableGet 是数据读取，应继续追踪）
- Self 引用通过 pin_name 检测，不在此集合中

添加后，确保常量能被导入：
```python
from uasset_read.constants import DATA_BOUNDARY_NODES
```
  </action>
  <verify>
    <automated>grep -c "DATA_BOUNDARY_NODES" src/uasset_read/constants.py && python -c "from uasset_read.constants import DATA_BOUNDARY_NODES; print(len(DATA_BOUNDARY_NODES))" | grep "2"</automated>
  </verify>
  <done>DATA_BOUNDARY_NODES 常量定义成功，包含 FunctionEntry 和 VariableSet，能被导入使用</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 is_boundary_node 函数</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <behavior>
    - Test 1: FunctionEntry 节点返回 True（边界）
    - Test 2: VariableSet 节点返回 True（边界）
    - Test 3: CallFunction 节点返回 False（非边界）
    - Test 4: pin_name == "self" 返回 True（self 引用边界）
    - Test 5: Knot 节点返回 False（需穿透）
  </behavior>
  <action>
在 `src/uasset_read/graph/flow_builder.py` 中实现 `is_boundary_node` 函数。

**位置：** 在 `_find_next_exec_node` 函数之前（约 L265）或在文件末尾辅助函数区域

**函数签名：**
```python
def is_boundary_node(node: UEdGraphNode, pin_name: str) -> bool:
    """判断是否为数据流边界节点（Phase 54）。

    Args:
        node: 目标节点
        pin_name: pin 名称（用于 self 检测）

    Returns:
        bool: True=边界（停止追踪），False=继续追踪
    """
```

**实现要点：**
- 导入 `DATA_BOUNDARY_NODES`：`from uasset_read.constants import DATA_BOUNDARY_NODES`
- 检查 `node.class_name in DATA_BOUNDARY_NODES`
- 检查 `pin_name.lower() == "self"`（self 引用）
- 返回 True（停止追踪）或 False（继续追踪）

**实现逻辑：**
```python
if node.class_name in DATA_BOUNDARY_NODES:
    return True
if pin_name.lower() == "self":
    return True
return False
```

**注意：**
- VariableGet 不在边界集合中（应继续追踪）
- Knot 不是边界（需要穿透）
- Pure CallFunction 不是边界（是数据源）

添加函数后，确保能被调用：
```python
from uasset_read.graph.flow_builder import is_boundary_node
```
  </action>
  <verify>
    <automated>grep -c "def is_boundary_node" src/uasset_read/graph/flow_builder.py && python -c "from uasset_read.graph.flow_builder import is_boundary_node; print('imported')" | grep "imported"</automated>
  </verify>
  <done>is_boundary_node 函数实现成功，能正确识别 FunctionEntry/self/VariableSet 边界，能被导入使用</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 _resolve_knot_chain 函数</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <behavior>
    - Test 1: 单级 Knot 穿透返回终端节点 pin_guid
    - Test 2: 多级 Knot 链穿透（Knot_2 → Knot_1）返回终端节点
    - Test 3: 循环 Knot 链返回 success=False
    - Test 4: 超过 max_depth（20）返回 success=False
    - Test 5: 非 Knot 节点直接返回原 pin_guid
  </behavior>
  <action>
在 `src/uasset_read/graph/flow_builder.py` 中实现 `_resolve_knot_chain` 函数。

**位置：** 在 `is_boundary_node` 函数之后

**函数签名：**
```python
def _resolve_knot_chain(
    pin_guid: str,
    pin_lookup: Dict[str, Tuple[str, str]],
    node_lookup: Dict[str, UEdGraphNode],
    max_depth: int = 20
) -> Tuple[str, bool]:
    """递归穿透 Knot 链直到到达非 Knot 节点（Phase 54）。

    Args:
        pin_guid: 起始 pin GUID
        pin_lookup: pin_id → (node_guid, pin_name) 查找表
        node_lookup: node_guid → node 查找表
        max_depth: 最大穿透深度（防止无限循环）

    Returns:
        Tuple[str, bool]: (terminal_pin_guid, success)
        - success=True: 找到非 Knot 终端节点
        - success=False: 链断裂或循环检测
    """
```

**实现要点：**
- 使用 `visited: Set[str]` 防止循环
- 检查 `node.class_name != "K2Node_Knot"` 判断终端
- Knot 穿透：找 OutputPin，获取 linked_to_raw 中的下一个 pin_guid
- 使用 for 循环 + max_depth 限制（非递归）
- linked_to_raw 解析：`linked_ref.get("pin_guid") if isinstance(linked_ref, dict) else linked_ref`

**实现逻辑（参考 RESEARCH.md L386-438）：**
```python
visited: Set[str] = set()
current_pin_guid = pin_guid

for _ in range(max_depth):
    if current_pin_guid in visited:
        return (current_pin_guid, False)  # 循环检测

    visited.add(current_pin_guid)

    # Get target node
    target_node_guid, _ = pin_lookup.get(current_pin_guid, (None, None))
    if not target_node_guid:
        return (current_pin_guid, False)  # Pin 不存在

    target_node = node_lookup.get(target_node_guid)
    if not target_node:
        return (current_pin_guid, False)  # Node 不存在

    # Check if Knot
    if target_node.class_name != "K2Node_Knot":
        return (current_pin_guid, True)  # 到达非 Knot 节点

    # Knot: Find OutputPin
    for pin in target_node.pins:
        if pin.pin_name == "OutputPin" and pin.direction == 1:
            # OutputPin 的 linked_to_raw 是下一个 pin
            for linked_ref in (pin.linked_to_raw or []):
                next_pin_guid = linked_ref.get("pin_guid") if isinstance(linked_ref, dict) else linked_ref
                current_pin_guid = next_pin_guid
                break
            break

return (current_pin_guid, False)  # 超过深度限制
```

**注意：**
- Knot 的 InputPin（direction=0）接收数据，OutputPin（direction=1）传递数据
- 穿透方向：InputPin → OutputPin → 下一个节点的 InputPin
- 最大深度 20 防止恶意循环

添加函数后，确保能被调用：
```python
from uasset_read.graph.flow_builder import _resolve_knot_chain
```
  </action>
  <verify>
    <automated>grep -c "def _resolve_knot_chain" src/uasset_read/graph/flow_builder.py && python -c "from uasset_read.graph.flow_builder import _resolve_knot_chain; print('imported')" | grep "imported"</automated>
  </verify>
  <done>_resolve_knot_chain 函数实现成功，能穿透多级 Knot 链，能处理循环和深度限制，能被导入使用</done>
</task>

<task type="auto">
  <name>Task 4: 启用 Wave 0 测试并验证核心函数</name>
  <files>tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: test_trace_data_source_knot_chain 取消 skip 并通过
    - Test 2: test_trace_data_source_function_entry 取消 skip 并通过
    - Test 3: is_boundary_node 单元测试通过
    - Test 4: _resolve_knot_chain 单元测试通过
  </behavior>
  <action>
在 `tests/test_output_formatting.py` 中取消 Wave 0 测试的 skip 标记，添加核心函数的单元测试。

**取消 skip：**
移除 `test_trace_data_source_knot_chain` 和 `test_trace_data_source_function_entry` 的 `@pytest.mark.skip` 装饰器。

**实现测试断言：**

1. **test_trace_data_source_knot_chain** — 验证 Knot 穿透
```python
def test_trace_data_source_knot_chain(sample_function_graph_with_data_flow):
    """验证 Knot 链穿透：FunctionEntry → Knot_2 → Knot_1 → CallFunction"""
    graph = sample_function_graph_with_data_flow
    
    # 构建 lookup
    pin_lookup = {}
    node_lookup = {}
    for node in graph.nodes:
        node_lookup[node.node_guid] = node
        for pin in node.pins:
            pin_lookup[pin.pin_id] = (node.node_guid, pin.pin_name)
    
    # 找到 CallFunction_7445 的 ScaleValue pin
    call_func = next(n for n in graph.nodes if "7445" in n.node_guid)
    scale_pin = next(p for p in call_func.pins if p.pin_name == "ScaleValue")
    
    # 追踪数据源（穿透 Knot 链）
    from uasset_read.graph.flow_builder import _resolve_knot_chain
    
    linked_pin = scale_pin.linked_to_raw[0]
    target_pin_guid = linked_pin.get("pin_guid") if isinstance(linked_pin, dict) else linked_pin
    
    terminal_pin_guid, success = _resolve_knot_chain(target_pin_guid, pin_lookup, node_lookup)
    
    assert success == True, "Knot chain穿透成功"
    
    # 验证终端节点为 FunctionEntry
    terminal_node_guid, _ = pin_lookup.get(terminal_pin_guid, (None, None))
    terminal_node = node_lookup.get(terminal_node_guid)
    assert terminal_node.class_name == "K2Node_FunctionEntry", "终端节点为FunctionEntry"
```

2. **test_trace_data_source_function_entry** — 验证边界检测
```python
def test_trace_data_source_function_entry(sample_function_graph_with_data_flow):
    """验证 FunctionEntry 参数作为数据流边界"""
    graph = sample_function_graph_with_data_flow
    
    from uasset_read.graph.flow_builder import is_boundary_node
    
    # 找到 FunctionEntry 节点
    func_entry = next(n for n in graph.nodes if n.class_name == "K2Node_FunctionEntry")
    
    # 验证边界检测
    assert is_boundary_node(func_entry, "Left / Right") == True, "FunctionEntry为边界"
    
    # 验证 CallFunction 不是边界
    call_func = next(n for n in graph.nodes if "7445" in n.node_guid)
    assert is_boundary_node(call_func, "WorldDirection") == False, "CallFunction非边界"
```

**添加核心函数单元测试：**

3. **test_is_boundary_node_self_reference** — 验证 self 边界
```python
def test_is_boundary_node_self_reference():
    """验证 self pin 作为边界"""
    from uasset_read.graph.flow_builder import is_boundary_node
    from uasset_read import UEdGraphNode
    
    # Mock node
    node = UEdGraphNode(node_guid="test", class_name="K2Node_CallFunction", pins=[])
    
    # self pin 应为边界
    assert is_boundary_node(node, "self") == True
    assert is_boundary_node(node, "Target") == True  # self 别名
    assert is_boundary_node(node, "WorldDirection") == False
```

4. **test_resolve_knot_chain_cycle_detection** — 验证循环检测
```python
def test_resolve_knot_chain_cycle_detection():
    """验证 Knot 链循环检测"""
    from uasset_read.graph.flow_builder import _resolve_knot_chain
    from uasset_read import UEdGraphNode, UEdGraphPin, FEdGraphPinType
    
    # 构造循环 Knot 链（A → B → A）
    knot_a = UEdGraphNode(
        node_guid="knot_a",
        class_name="K2Node_Knot",
        pins=[
            UEdGraphPin(pin_id="input_a", pin_name="InputPin", direction=0, pin_type=None, linked_to_raw=[]),
            UEdGraphPin(pin_id="output_a", pin_name="OutputPin", direction=1, pin_type=None, linked_to_raw=[{"pin_guid": "input_b"}])
        ]
    )
    knot_b = UEdGraphNode(
        node_guid="knot_b",
        class_name="K2Node_Knot",
        pins=[
            UEdGraphPin(pin_id="input_b", pin_name="InputPin", direction=0, pin_type=None, linked_to_raw=[{"pin_guid": "output_a"}]),
            UEdGraphPin(pin_id="output_b", pin_name="OutputPin", direction=1, pin_type=None, linked_to_raw=[{"pin_guid": "input_a"}])
        ]
    )
    
    pin_lookup = {
        "input_a": ("knot_a", "InputPin"),
        "output_a": ("knot_a", "OutputPin"),
        "input_b": ("knot_b", "InputPin"),
        "output_b": ("knot_b", "OutputPin"),
    }
    node_lookup = {"knot_a": knot_a, "knot_b": knot_b}
    
    # 循环检测应返回 False
    terminal_guid, success = _resolve_knot_chain("input_a", pin_lookup, node_lookup)
    assert success == False, "循环Knot链检测成功"
```

**运行测试验证：**
```bash
python -m pytest tests/test_output_formatting.py -x -k "trace_data or boundary or knot_chain"
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -x -k "trace_data or boundary or knot_chain" 2>&1 | grep -E "passed|PASSED"</automated>
  </verify>
  <done>4 个测试通过，is_boundary_node 和 _resolve_knot_chain 功能验证成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Knot 链穿透 | 内部数据结构遍历，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02-01 | Tampering | Knot 链循环检测 | mitigate | max_depth=20 + visited set 防止无限循环 |
| T-54-SC | Tampering | pytest 测试 | accept | 测试代码无外部输入风险 |

核心函数为纯数据结构遍历，无安全风险。
</threat_model>

<verification>
- is_boundary_node 正确识别 FunctionEntry/self 边界
- _resolve_knot_chain 能穿透多级 Knot 链
- 循环 Knot 链正确返回 success=False
- 测试通过（pytest -k "trace_data"）
</verification>

<success_criteria>
- `DATA_BOUNDARY_NODES` 常量在 constants.py 中定义
- `is_boundary_node` 函数在 flow_builder.py 中实现
- `_resolve_knot_chain` 函数在 flow_builder.py 中实现
- 4 个测试通过（Knot 穿透 + 边界检测 + 循环检测）
</success_criteria>

<output>
完成后创建 `.planning/phases/54-data-flow-tracking/54-02-SUMMARY.md`
</output>