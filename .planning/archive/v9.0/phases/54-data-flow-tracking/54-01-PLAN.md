---
phase: 54-data-flow-tracking
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_output_formatting.py, tests/fixtures/data_flow_fixture.py]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
user_setup: []

must_haves:
  truths:
    - "测试 fixture 能模拟 FunctionEntry + Knot + Pure + CallFunction 链"
    - "基础数据流追踪测试能验证数据来源标注"
    - "Knot 穿透测试能验证多级 Knot 链穿透"
  artifacts:
    - path: "tests/fixtures/data_flow_fixture.py"
      provides: "Move 函数图数据流 fixture"
      min_lines: 50
    - path: "tests/test_output_formatting.py"
      provides: "数据流追踪测试函数"
      contains: "test_trace_data_source"
  key_links:
    - from: "tests/fixtures/data_flow_fixture.py"
      to: "tests/test_output_formatting.py"
      via: "pytest fixture import"
      pattern: "@pytest.fixture.*sample_function_graph_with_data_flow"
---

<objective>
创建数据流追踪测试基础设施 — Wave 0

Purpose: 为 Phase 54 数据流追踪功能建立测试基础，确保后续实现有明确的验收标准。
Output: 测试 fixture + 6 个数据流追踪测试函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-data-flow-tracking/54-RESEARCH.md

# Phase 53 已完成的工作
- `_get_start_event_name` 统一前缀格式（FunctionEntry.{name}）
- `_trace_execution_from_event` 添加 Pure 函数标记（`"pure": true`）
- 4 个新测试覆盖 FunctionEntry 前缀/执行流/pure/Knot 透明性

# 参考文件：Move 函数数据流
@reference/蓝图节点文本参考.md L228-340

# 现有测试结构
@tests/test_output_formatting.py L1-100
</context>

<interfaces>
<!-- 测试 fixture 需要创建的数据结构 -->

From src/uasset_read/models/core.py:
```python
@dataclass
class UEdGraphPin:
    pin_id: str
    pin_name: str
    direction: int  # 0=input, 1=output
    pin_type: Optional[FEdGraphPinType]
    linked_to_raw: List[Union[str, Dict]] = field(default_factory=list)
    default_value: Optional[str] = None
    sub_pins: List[dict] = field(default_factory=list)
```

From src/uasset_read/models/node_types.py:
```python
@dataclass
class K2NodeKnot:
    pass  # Knot 类型标记

@dataclass
class K2NodeFunctionEntry:
    function_reference: Optional[FMemberReference] = None
```

From src/uasset_read/models/core.py:
```python
@dataclass
class UEdGraphNode:
    node_guid: str
    class_name: str
    pins: List[UEdGraphPin]
    node_data: Optional[Any] = None
```

From src/uasset_read/constants.py:
```python
START_EVENT_TYPES = frozenset({
    "K2Node_Event",
    "K2Node_EnhancedInputAction",
    "K2Node_VariableSet",
    "K2Node_CustomEvent",
    "K2Node_FunctionEntry",  # Phase 52: 函数图执行流起点
})
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建数据流测试 fixture</name>
  <files>tests/fixtures/data_flow_fixture.py</files>
  <behavior>
    - Test 1: fixture 创建完整的 Move 函数图结构（FunctionEntry + Knot_2 + Knot_1 + CallFunction_7445 + Pure CallFunction）
    - Test 2: fixture 包含正确的 pin 连接关系（linked_to_raw）
    - Test 3: fixture 能被 pytest 导入和使用
  </behavior>
  <action>
创建 `tests/fixtures/data_flow_fixture.py` 文件，提供 `sample_function_graph_with_data_flow` fixture。

该 fixture 模拟 Move 函数的数据流结构（基于 reference/蓝图节点文本参考.md L228-340）：

**节点结构：**
1. K2Node_FunctionEntry_0 — 函数入口，输出参数 "Left / Right" 和 "Forward / Backward"
2. K2Node_Knot_2 → K2Node_Knot_1 — Knot 链（穿透测试）
3. K2Node_CallFunction_7445 — AddMovementInput（接收 ScaleValue 来自 Knot_1）
4. K2Node_CallFunction_8520 — GetActorRightVector（Pure 函数）
5. K2Node_CallFunction_7346 — AddMovementInput（另一个调用）

**Pin 连接关系（关键）：**
- FunctionEntry_0 "Left / Right" → Knot_2 InputPin → Knot_2 OutputPin → Knot_1 InputPin → Knot_1 OutputPin → CallFunction_7445 "ScaleValue"
- CallFunction_8520 "ReturnValue" → CallFunction_7445 "WorldDirection"

**实现要点：**
- 使用真实 GUID（从参考文本提取）或生成测试 GUID
- linked_to_raw 使用 dict 格式：`{"pin_guid": str, "owning_node": str}`
- Knot 节点需要有 InputPin（direction=0）和 OutputPin（direction=1）
- Pure 函数标记：GetActorRightVector 无 exec pin

文件创建后，在 `tests/test_output_formatting.py` 中导入 fixture：
```python
from tests.fixtures.data_flow_fixture import sample_function_graph_with_data_flow
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -k "sample_function_graph" --collect-only | grep -c "sample_function_graph_with_data_flow"</automated>
  </verify>
  <done>fixture 文件创建成功，pytest 能发现并导入 fixture，fixture 返回包含至少 5 个节点的 UEdGraph 对象</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建基础数据流追踪测试骨架</name>
  <files>tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: test_trace_data_source_knot_chain — 验证 Knot 链穿透（FunctionEntry → Knot_2 → Knot_1 → CallFunction）
    - Test 2: test_trace_data_source_function_entry — 验证 FunctionEntry 参数作为边界
    - Test 3: test_trace_data_source_pure_function — 验证 Pure 函数 ReturnValue 作为数据源
    - Test 4: test_trace_data_source_self_reference — 验证 self pin 作为边界
    - Test 5: test_sub_pin_first_level_expand — 验证 SubPin 第一级展开
    - Test 6: test_data_providers_pure_function — 验证 Pure 函数 data_providers 字段
  </behavior>
  <action>
在 `tests/test_output_formatting.py` 中添加 6 个测试函数骨架。

**测试函数结构（每测试暂时标记为 `@pytest.mark.skip(reason="Wave 1 implementation pending")`）：**

1. `test_trace_data_source_knot_chain(sample_function_graph_with_data_flow)` — 验证 Knot 链穿透
   - 检查 CallFunction_7445 的 "ScaleValue" 参数的 data_source
   - 验证穿透 Knot_1 和 Knot_2 后到达 FunctionEntry_0
   - 验证 source_type == "function_parameter"

2. `test_trace_data_source_function_entry(sample_function_graph_with_data_flow)` — 验证 FunctionEntry 参数边界
   - 检查 FunctionEntry 输出 pin 作为数据流起点
   - 验证不继续追踪到其他节点

3. `test_trace_data_source_pure_function(sample_function_graph_with_data_flow)` — 验证 Pure 函数数据源
   - 检查 CallFunction_7445 的 "WorldDirection" 参数
   - 验证 data_source 来自 GetActorRightVector（Pure）
   - 验证 source_type == "pure_function"

4. `test_trace_data_source_self_reference(sample_function_graph_with_data_flow)` — 验证 self 引用边界
   - 检查 CallFunction 的 "self" pin
   - 验证 source_type == "self_reference"

5. `test_sub_pin_first_level_expand(sample_function_graph_with_data_flow)` — 验证 SubPin 展开
   - 检查 struct pin（如 Vector）的 sub_pins 展开
   - 验证仅第一级展开，不递归

6. `test_data_providers_pure_function(sample_function_graph_with_data_flow)` — 验证正向标注
   - 检查 Pure 函数节点的 data_providers 字段
   - 验证标注了数据去向（CallFunction.WorldDirection）

**实现要点：**
- 每个测试使用 fixture 作为参数
- 测试骨架包含断言占位符（Wave 1 实现后取消 skip）
- 测试命名遵循 Phase 53 的命名风格

添加测试后，确保 pytest 能收集到这 6 个测试：
```bash
python -m pytest tests/test_output_formatting.py -k "trace_data" --collect-only
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -k "trace_data" --collect-only | grep -c "test_trace_data"</automated>
  </verify>
  <done>6 个测试函数骨架添加成功，pytest 能收集到所有测试，测试标记为 skip（等待 Wave 1 实现）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 fixture → 测试函数 | 内部测试代码，无信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-SC | Tampering | pytest fixture | accept | 测试 fixture 为内部代码，无外部输入风险 |

本 Phase 为纯测试代码，无安全风险。
</threat_model>

<verification>
- pytest 能收集到 fixture 和 6 个测试
- fixture 能被测试函数导入使用
- 测试骨架结构正确（有断言占位符）
</verification>

<success_criteria>
- `tests/fixtures/data_flow_fixture.py` 文件存在且包含 `sample_function_graph_with_data_flow` fixture
- `tests/test_output_formatting.py` 包含 6 个 `test_trace_data_*` 测试函数
- pytest 收集命令返回 6 个测试
</success_criteria>

<output>
完成后创建 `.planning/phases/54-data-flow-tracking/54-01-SUMMARY.md`
</output>