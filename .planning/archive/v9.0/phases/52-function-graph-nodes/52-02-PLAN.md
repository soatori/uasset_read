---
phase: 52
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/uasset_read/constants.py
  - src/uasset_read/graph/flow_builder.py
  - src/uasset_read/__init__.py
autonomous: true
requirements:
  - FUNC-01
  - FUNC-02

must_haves:
  truths:
    - "build_execution_flows() 能从 K2Node_FunctionEntry 开始追踪函数图执行流"
    - "函数图（Move/Aim）的 execution_flows 中 start_event 为函数名（'Move'、'Aim'）"
    - "EventGraph 的执行流输出不变（向后兼容）"
  artifacts:
    - path: "src/uasset_read/constants.py"
      provides: "START_EVENT_TYPES 包含 K2Node_FunctionEntry"
      contains: "'K2Node_FunctionEntry'"
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "_get_start_event_name 处理 FunctionEntry"
      pattern: "K2Node_FunctionEntry"
  key_links:
    - from: "src/uasset_read/graph/flow_builder.py:build_execution_flows"
      to: "src/uasset_read/constants.py:START_EVENT_TYPES"
      via: "n.class_name in START_EVENT_TYPES 过滤"
      pattern: "START_EVENT_TYPES"
    - from: "src/uasset_read/graph/flow_builder.py:_get_start_event_name"
      to: "node.node_data.function_reference"
      via: "FunctionEntry 分支提取 member_name"
      pattern: "function_reference.*member_name"

threat_model:
  Trust Boundaries:

  | Boundary | Description |
  |----------|-------------|
  | START_EVENT_TYPES → build_execution_flows | 全局集合扩展，需确保不破坏 EventGraph 行为 |
  | _get_start_event_name → node_data | node_data 可能为 None 或 dict/dataclass，需防御性编码 |

  STRIDE Threat Register:

  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-52-03 | Integrity | START_EVENT_TYPES 扩展影响 EventGraph | mitigate | EventGraph 不含 K2Node_FunctionEntry，但需在测试中验证 EventGraph 执行流输出不变 |
  | T-52-04 | Denial of Service | 恶意 .uasset 构造空 node_data 导致 AttributeError | mitigate | _get_start_event_name 已有 nd = node.node_data + isinstance 检查模式，FunctionEntry 分支沿用相同防御 |
  | T-52-SC | Tampering | npm/pip/cargo installs | mitigate | 本阶段零外部依赖，无包安装 |
---

<objective>
将 K2Node_FunctionEntry 集成到执行流构建系统中，使 build_execution_flows() 能从函数入口开始追踪调用链，并添加 is_function_graph 判断函数。

Purpose: FUNC-01 要求能区分 EventGraph 和 Function Graph，FUNC-02 要求 FunctionEntry 作为执行流起点。D-03 明确复用现有 build_execution_flows() 通过扩展 START_EVENT_TYPES。

Output: START_EVENT_TYPES 扩展、_get_start_event_name 新增 FunctionEntry 分支、is_function_graph 辅助函数、导出链更新
</objective>

<execution_context>
@E:\Develop\uasset_read\.gsd\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.gsd\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\PROJECT.md
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md
@E:\Develop\uasset_read\.planning\phases\52-struct-offset-alignment\52-CONTEXT.md
@E:\Develop\uasset_read\.planning\phases\52-struct-offset-alignment\52-RESEARCH.md

<read_first>
<!-- 核心文件 — 必须读取后再修改 -->
- src/uasset_read/constants.py — 完整文件，START_EVENT_TYPES 定义（第 147-152 行）
- src/uasset_read/graph/flow_builder.py — 完整文件，特别关注：
  - 第 171-226 行: _get_start_event_name 现有 4 种类型处理
  - 第 406-445 行: build_execution_flows 入口函数
  - 第 486-524 行: build_graphs_summary
- src/uasset_read/__init__.py — 顶层导出和 __all__
</read_first>

<interfaces>
<!-- 关键类型契约 — 执行者直接使用 -->

From src/uasset_read/constants.py — START_EVENT_TYPES（现有）:
```python
START_EVENT_TYPES = frozenset({
    "K2Node_Event",
    "K2Node_EnhancedInputAction",
    "K2Node_VariableSet",
    "K2Node_CustomEvent"
})
```

From src/uasset_read/graph/flow_builder.py — _get_start_event_name 模式（第 171-226 行）:
```python
def _get_start_event_name(node: UEdGraphNode) -> str:
    nd = node.node_data
    if node.class_name == "K2Node_Event":
        if not nd: return node.class_name
        if isinstance(nd, dict): er = nd.get("event_reference")
        else: er = getattr(nd, 'event_reference', None)
        # ... 提取 member_name
```

From src/uasset_read/models/node_types.py — K2NodeFunctionEntry（Plan 01 创建）:
```python
@dataclass
class K2NodeFunctionEntry(UEdGraphNode):
    function_reference: Optional[FMemberReference] = None
    extra_flags: int = 0
    b_is_editable: bool = False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 START_EVENT_TYPES 添加 K2Node_FunctionEntry</name>
  <files>src/uasset_read/constants.py</files>
  <read_first>
    - src/uasset_read/constants.py — START_EVENT_TYPES 定义（第 147-152 行）
    - tests/test_output_formatting.py — test_start_event_types_contains_four_types（第 2307-2319 行），需要更新此测试
  </read_first>
  <behavior>
    - Test 1: "K2Node_FunctionEntry" in START_EVENT_TYPES — True
    - Test 2: len(START_EVENT_TYPES) == 5（原有 4 种 + 1 种）
    - Test 3: 原有 4 种类型仍在集合中（向后兼容）
  </behavior>
  <action>
在 constants.py 的 START_EVENT_TYPES frozenset 中添加 "K2Node_FunctionEntry"：
```python
START_EVENT_TYPES = frozenset({
    "K2Node_Event",
    "K2Node_EnhancedInputAction",
    "K2Node_VariableSet",
    "K2Node_CustomEvent",
    "K2Node_FunctionEntry",  # Phase 52: 函数图执行流起点
})
```
同时更新 tests/test_output_formatting.py 第 2319 行：将 `assert len(START_EVENT_TYPES) == 4` 改为 `assert len(START_EVENT_TYPES) == 5`。
  </action>
  <verify>
    <automated>python -c "from uasset_read import START_EVENT_TYPES; assert 'K2Node_FunctionEntry' in START_EVENT_TYPES; assert len(START_EVENT_TYPES) == 5; print('OK')"</automated>
  </verify>
  <done>START_EVENT_TYPES 包含 5 种类型，原有 4 种不变，新增 K2Node_FunctionEntry</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _get_start_event_name 添加 FunctionEntry 分支 + is_function_graph 函数</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <read_first>
    - src/uasset_read/graph/flow_builder.py — 完整文件
    - 第 171-226 行: _get_start_event_name 现有实现（K2Node_Event / EnhancedInputAction / VariableSet / CustomEvent）
    - 第 486-524 行: build_graphs_summary 使用 GRAPH_TYPE_MAP
  </read_first>
  <behavior>
    - Test 1: FunctionEntry 节点的 _get_start_event_name 返回 function_reference.member_name（如 'Move'）
    - Test 2: FunctionEntry 节点 node_data=None 时 fallback 到 class_name
    - Test 3: is_function_graph 对含 K2Node_FunctionEntry 的图返回 True
    - Test 4: is_function_graph 对含 K2Node_Event 的图返回 False
    - Test 5: is_function_graph 对名为 'EventGraph' 的无特征图返回 False（fallback）
  </behavior>
  <action>
**修改 1 — _get_start_event_name 添加 FunctionEntry 分支（第 224 行 `return node.class_name` 之前）：**

参照 K2Node_Event 分支的防御性编码模式（isinstance dict/dataclass 检查），添加 K2Node_FunctionEntry 处理：
```python
elif node.class_name == "K2Node_FunctionEntry":
    if not nd:
        return node.class_name
    if isinstance(nd, dict):
        fr = nd.get("function_reference")
    else:
        fr = getattr(nd, 'function_reference', None)
    if fr:
        mn = getattr(fr, 'member_name', None) if not isinstance(fr, dict) else fr.get("member_name")
        if mn and mn != "None":
            return mn
    return node.class_name
```

**修改 2 — 添加 is_function_graph 函数（在 _get_start_event_name 之后，_find_next_exec_node 之前）：**

实现 D-01 组合判断策略：
```python
def is_function_graph(graph: UEdGraph) -> bool:
    """判断图是否为函数图（非事件图）。

    组合判断（D-01）：
    1. 含 K2Node_FunctionEntry → Function Graph
    2. 含 K2Node_Event → EventGraph
    3. Fallback: graph_name 模式
    """
    node_types = {n.class_name for n in graph.nodes}
    if "K2Node_FunctionEntry" in node_types:
        return True
    if "K2Node_Event" in node_types:
        return False
    return graph.graph_name.lower() != "eventgraph"
```

**修改 3 — flow_builder.py 的 import 中添加 UEdGraph（如果尚未导入）：**

检查第 14 行 `from uasset_read.models.core import UEdGraph, UEdGraphNode, UEdGraphPin`，确保 UEdGraph 已导入。
  </action>
  <verify>
    <automated>python -c "from uasset_read.graph.flow_builder import is_function_graph; print('OK')"</automated>
  </verify>
  <done>_get_start_event_name 能提取 FunctionEntry 的函数名（如 'Move'），is_function_graph 可导入并正确区分函数图和事件图</done>
</task>

<task type="auto">
  <name>Task 3: 验证 EventGraph 向后兼容 + format_node_dict 添加 function_entry_reference 提取</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <read_first>
    - src/uasset_read/graph/flow_builder.py — format_node_dict 函数（第 107-168 行）
    - 第 406-445 行: build_execution_flows — EventGraph 执行流构建逻辑
  </read_first>
  <action>
1. 在 format_node_dict 中（第 161 行 Knot/Comment 注释之前），添加 function_entry_reference 的提取逻辑，参照现有的 function_reference/event_reference 提取模式：
```python
elif _get('function_reference') is not None:
    # FunctionEntry: 与 CallFunction 同用 function_reference，但来源不同
    fr = _get('function_reference')
    result["function_entry_reference"] = {
        "member_name": getattr(fr, 'member_name', None),
        "member_parent": getattr(fr, 'member_parent', None),
        "self_context": getattr(fr, 'b_self_context', None)
    }
```
注意使用不同的键名 `function_entry_reference` 以避免与 CallFunction 的 `function_reference` 冲突。

2. 在 _trace_execution_from_pin 中（第 334-352 行），添加对 K2Node_FunctionEntry 的处理：当追踪到 FunctionEntry 节点时，应将其作为起点而非跳过。由于 FunctionEntry 已加入 START_EVENT_TYPES，build_execution_flows 会自动从它开始追踪，但需要在 _trace_execution_from_event 中处理 FunctionEntry 的节点信息记录：
在 _trace_execution_from_event 的 while 循环中，添加 FunctionEntry 的信息提取（类似 K2Node_Event 处理，第 314-319 行之后）：
```python
if current_node.class_name == "K2Node_FunctionEntry":
    nd = current_node.node_data
    if nd:
        fr = nd.get("function_reference") if isinstance(nd, dict) else getattr(nd, 'function_reference', None)
        if fr:
            node_info["function_name"] = getattr(fr, 'member_name', None)
```
  </action>
  <verify>
    <automated>pytest tests/test_output_formatting.py::test_start_event_types_contains_four_types -v</automated>
  </verify>
  <done>format_node_dict 对 FunctionEntry 节点输出 function_entry_reference 字段，_trace_execution_from_event 记录 FunctionEntry 的 function_name</done>
</task>

</tasks>

<verification>
- `pytest tests/test_output_formatting.py::test_start_event_types_contains_four_types -v` — 断言 len==5 通过
- `python -c "from uasset_read.graph.flow_builder import is_function_graph; print('OK')"` — 无 ImportError
- `python -c "from uasset_read import parse_uasset; r = parse_uasset('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/Blueprints/BP_FirstPersonCharacter.uasset', graph=True); graphs = {g.graph_name: g for g in r.graphs}; print([n.class_name for g in graphs.values() for n in g.nodes if 'FunctionEntry' in n.class_name])"` — 确认 FunctionEntry 节点存在
</verification>

<success_criteria>
- START_EVENT_TYPES 包含 K2Node_FunctionEntry（5 种类型）
- _get_start_event_name 对 FunctionEntry 返回函数名（如 'Move'、'Aim'）
- is_function_graph 正确区分 BP_FirstPersonCharacter 中的 EventGraph（False）和 Move/Aim/UserConstructionScript（True）
- BP_FirstPersonCharacter 的 Move 图 execution_flows 中 start_event = "Move"
- EventGraph 的执行流输出与修改前完全一致（无回归）
- format_node_dict 对 FunctionEntry 节点输出 function_entry_reference
</success_criteria>

<output>
Create `.planning/phases/52-struct-offset-alignment/52-02-SUMMARY.md` when done
</output>
