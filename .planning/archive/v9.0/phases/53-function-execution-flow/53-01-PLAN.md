---
phase: 53
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/graph/flow_builder.py
autonomous: true
requirements:
  - FUNC-01
  - FUNC-02

must_haves:
  truths:
    - "FunctionEntry 起点的 start_event 格式为 'FunctionEntry.{name}'（如 'FunctionEntry.Move'），而非裸 'Move'"
    - "所有起点类型统一前缀：'Event.{name}', 'FunctionEntry.{name}', 'CustomEvent.{name}', 'InputAction.{name}', 'VariableSet'"
    - "CallFunction 节点若无 exec pins（纯函数），在 execution flow 中标记 'pure': true"
    - "Knot 节点不出现在 execution flow 输出中"
  artifacts:
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "_get_start_event_name 统一前缀 + _trace_execution_from_event pure 标记"
      contains: "FunctionEntry."
      exports: ["_get_start_event_name", "_trace_execution_from_event"]
  key_links:
    - from: "src/uasset_read/graph/flow_builder.py:_get_start_event_name"
      to: "src/uasset_read/graph/flow_builder.py:build_execution_flows"
      via: "start_event_name 生成后被 build_execution_flows 使用"
      pattern: "_get_start_event_name\\(start_node\\)"
    - from: "src/uasset_read/graph/flow_builder.py:_trace_execution_from_event"
      to: "src/uasset_read/graph/flow_builder.py:build_execution_flows"
      via: "flow 节点列表返回，含 pure 标记"
      pattern: "flow\\.append\\(node_info\\)"

threat_model:
  Trust Boundaries:

  | Boundary | Description |
  |----------|-------------|
  | .uasset binary → flow_builder | 不受信任的二进制输入，node_data 可能缺失或结构异常 |
  | Pin category 检查 → pure 判定 | 恶意构造的 pin 数据可能导致误判 |

  STRIDE Threat Register:

  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-53-01 | Information Exposure | _trace_execution_from_event node_info | mitigate | 已有 node_data None 检查（第 337、350、357 行），pure 标记同样防御 None |
  | T-53-02 | Tampering | _get_start_event_name prefix 逻辑 | mitigate | 每个分支有 member_name None/"None" fallback 到 class_name，无崩溃路径 |
  | T-53-SC | Tampering | npm/pip/cargo installs | mitigate | 本阶段零外部依赖，无包安装 |
---

<objective>
修复 _get_start_event_name 前缀一致性并为 execution flow 添加 pure function 标记。

Purpose: 当前 FunctionEntry 返回裸函数名（如 "Move"），与 Event 的 "BeginPlay" 格式无法区分来源类型。统一添加 "FunctionEntry." 前缀。同时 per discussion log 决策，CallFunction 无 exec pins 时标记 "pure": true 而非跳过。

Output: flow_builder.py 两处修改（_get_start_event_name + _trace_execution_from_event）
</objective>

<execution_context>
@E:\Develop\uasset_read\.gsd\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.gsd\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\PROJECT.md
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md

<read_first>
- src/uasset_read/graph/flow_builder.py — 完整文件（已读），特别关注：
  - 第 178-245 行: _get_start_event_name — 当前 FunctionEntry 返回 mn 而非 "FunctionEntry.{mn}"
  - 第 336-347 行: _trace_execution_from_event 中 CallFunction 处理 — 需要添加 pure 标记
  - 第 264-287 行: _find_next_exec_node — 理解 exec pin 检测逻辑
</read_first>

<interfaces>
<!-- 关键类型契约 — 执行者直接使用 -->

From src/uasset_read/models/core.py — FMemberReference:
```python
@dataclass
class FMemberReference:
    member_parent: Optional[str] = None
    member_name: str = ""
    member_guid: str = ""
    b_self_context: bool = False
```

From src/uasset_read/models/node_types.py — K2NodeCallFunction:
```python
@dataclass
class K2NodeCallFunction(UEdGraphNode):
    function_reference: Optional[FMemberReference] = None
    b_defaults_to_pure: bool = False
```

From src/uasset_read/graph/flow_builder.py — _get_start_event_name 当前返回值：
- K2Node_Event → member_name（无前缀）
- K2Node_EnhancedInputAction → path last segment
- K2Node_VariableSet → "VariableSet"
- K2Node_CustomEvent → "CustomEvent"
- K2Node_FunctionEntry → member_name（无前缀）

修改后应统一为：
- K2Node_Event → "Event.{member_name}"
- K2Node_EnhancedInputAction → "InputAction.{name}"
- K2Node_VariableSet → "VariableSet"（不变，无具体名称可提取）
- K2Node_CustomEvent → "CustomEvent"（不变，无具体名称可提取）
- K2Node_FunctionEntry → "FunctionEntry.{member_name}"

From src/uasset_read/graph/flow_builder.py — _trace_execution_from_event CallFunction 段（第 336-347 行）：
```python
if current_node.class_name == "K2Node_CallFunction":
    nd = current_node.node_data
    if nd:
        fr = nd.get("function_reference") if isinstance(nd, dict) else getattr(nd, 'function_reference', None)
        if fr:
            node_info["function_name"] = getattr(fr, 'member_name', None)
    node_info["params"] = [...]
# 需要在 node_info["params"] 之后添加 pure 检测
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一 _get_start_event_name 前缀格式</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <read_first>
    - src/uasset_read/graph/flow_builder.py 第 178-245 行 — _get_start_event_name 完整函数
  </read_first>
  <action>
修改 src/uasset_read/graph/flow_builder.py 的 _get_start_event_name 函数（第 178-245 行），统一添加类型前缀：

1. **K2Node_Event 分支（第 191-217 行）：** 所有 return mn 改为 return f"Event.{mn}"。保持 fallback 到 node.class_name 不变（当无法提取 member_name 时）。注意第 216 行的路径分割后也要加前缀：return f"Event.{mn.split('/')[-1]}"。

2. **K2Node_EnhancedInputAction 分支（第 219-227 行）：** return path.split('/')[-1] 改为 return f"InputAction.{path.split('/')[-1]}"；return node.class_name 改为 return f"InputAction.{node.class_name}"。

3. **K2Node_VariableSet 和 K2Node_CustomEvent（第 228-231 行）：** 保持不变（"VariableSet" 和 "CustomEvent" 本身就是前缀格式）。

4. **K2Node_FunctionEntry 分支（第 232-243 行）：** return mn 改为 return f"FunctionEntry.{mn}"；路径分割后也要加前缀。fallback return node.class_name 保持不变。

最终效果：
- Event.BeginPlay, Event.Tick
- FunctionEntry.Move, FunctionEntry.Aim
- InputAction.IA_Jump.Started
- VariableSet
- CustomEvent

注意：修改前先用 grep 确认所有 return 位置，确保不遗漏任何分支。
  </action>
  <verify>
    <automated>python -c "
from uasset_read.graph.flow_builder import _get_start_event_name
from uasset_read.models.core import UEdGraphNode, FMemberReference
from uasset_read.models.node_types import K2NodeFunctionEntry, K2NodeEvent

# Test FunctionEntry prefix
fe_node = UEdGraphNode(node_guid='test', class_name='K2Node_FunctionEntry',
    node_data=K2NodeFunctionEntry(node_guid='test', function_reference=FMemberReference(member_name='Move')))
assert _get_start_event_name(fe_node) == 'FunctionEntry.Move', f'got {_get_start_event_name(fe_node)}'

# Test Event prefix
ev_node = UEdGraphNode(node_guid='test', class_name='K2Node_Event',
    node_data=K2NodeEvent(node_guid='test', event_reference=FMemberReference(member_name='BeginPlay'), b_override_function=False))
assert _get_start_event_name(ev_node) == 'Event.BeginPlay', f'got {_get_start_event_name(ev_node)}'

print('OK')
"</automated>
  </verify>
  <done>_get_start_event_name 对所有类型返回统一前缀格式，FunctionEntry 返回 "FunctionEntry.{name}"，Event 返回 "Event.{name}"</done>
</task>

<task type="auto">
  <name>Task 2: _trace_execution_from_event 添加 pure function 标记</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <read_first>
    - src/uasset_read/graph/flow_builder.py 第 290-373 行 — _trace_execution_from_event 完整函数，特别关注第 336-347 行 CallFunction 处理段
    - src/uasset_read/graph/flow_builder.py 第 264-287 行 — _find_next_exec_node，理解 exec pin 检测
  </read_first>
  <action>
在 src/uasset_read/graph/flow_builder.py 的 _trace_execution_from_event 函数中，为 CallFunction 节点添加 pure 检测（在第 347 行 params 赋值之后，第 349 行 Event 检查之前插入）：

```python
# Per discussion log: mark pure functions with "pure": true in flow
has_exec_pin = any(pin.pin_type and pin.pin_type.pin_category == "exec" for pin in current_node.pins)
if not has_exec_pin:
    nd = current_node.node_data
    node_info["pure"] = True
elif nd and hasattr(nd, 'b_defaults_to_pure') and nd.b_defaults_to_pure:
    node_info["pure"] = True
```

检测逻辑：
1. 主检测：遍历节点所有 pins，检查是否有任何 pin 的 pin_category == "exec"。如果完全没有 exec pins，则为纯函数。
2. Fallback：如果 node_data.b_defaults_to_pure 为 True，也标记为 pure。

注意：pure 标记只适用于 K2Node_CallFunction 节点。其他节点类型不需要此检测。

确保 node_info["pure"] = True 在 flow.append(node_info) 之前设置（当前第 370 行）。
  </action>
  <verify>
    <automated>python -c "
from uasset_read.graph.flow_builder import _trace_execution_from_event, build_execution_flows
from uasset_read.models.core import UEdGraphNode, UEdGraphPin, FEdGraphPinType, FMemberReference
from uasset_read.models.node_types import K2NodeCallFunction

# 纯函数：无 exec pins
struct_pin = FEdGraphPinType(pin_category='struct', pin_subcategory='Vector', container_type=0)
pure_output_pin = UEdGraphPin(pin_id='pure_out', pin_name='ReturnValue', direction=1, pin_type=struct_pin, linked_to_raw=[])
pure_self_pin = UEdGraphPin(pin_id='pure_self', pin_name='self', direction=0, pin_type=FEdGraphPinType(pin_category='object', pin_subcategory='', container_type=0), linked_to_raw=[])
pure_node = UEdGraphNode(node_guid='pure_guid', pins=[pure_output_pin, pure_self_pin], class_name='K2Node_CallFunction', node_data=K2NodeCallFunction(node_guid='test', function_reference=FMemberReference(member_name='GetActorForwardVector'), b_defaults_to_pure=True))

lookup = {}
node_lookup = {}
for pin in pure_node.pins:
    lookup[pin.pin_id] = (pure_node.node_guid, pin.pin_name)
node_lookup[pure_node.node_guid] = pure_node

flow = _trace_execution_from_event(pure_node, lookup, node_lookup)
assert len(flow) == 1
assert flow[0].get('pure') == True, f'expected pure=True, got {flow[0]}'
assert flow[0].get('function_name') == 'GetActorForwardVector'
print('OK')
"</automated>
  </verify>
  <done>CallFunction 节点无 exec pins 时，node_info 包含 "pure": True；b_defaults_to_pure fallback 也生效</done>
</task>

</tasks>

<verification>
- `python -c "from uasset_read.graph.flow_builder import _get_start_event_name"` — 无 ImportError
- `python -c "from uasset_read.graph.flow_builder import _trace_execution_from_event"` — 无 ImportError
- `pytest tests/test_output_formatting.py -v -k "function_entry or pure_function or knot_transparent"` — Wave 2 新增测试通过
- 对 BP_FirstPersonCharacter 解析后，execution_flows 中起点为 "FunctionEntry.Move" 而非 "Move"
</verification>

<success_criteria>
- _get_start_event_name 对 K2Node_FunctionEntry 返回 "FunctionEntry.{member_name}"
- _get_start_event_name 对 K2Node_Event 返回 "Event.{member_name}"
- _get_start_event_name 对 K2Node_EnhancedInputAction 返回 "InputAction.{name}"
- _trace_execution_from_event 对无 exec pins 的 CallFunction 节点添加 "pure": true
- _trace_execution_from_event 对 b_defaults_to_pure=True 的 CallFunction 节点也添加 "pure": true
- 现有测试不受影响（前缀变更仅影响 start_event 字符串，不影响 flow nodes 内容）
- execution_flows 输出结构不变（D-01：复用现有 execution_flows，不创建新数组）
- CallFunction 在 flow 中仅包含 {function_name, params, pure}（D-02：reference only，不递归展开）
</success_criteria>

<output>
Create `.planning/phases/53-function-execution-flow/53-01-SUMMARY.md` when done
</output>
