---
phase: 53
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - tests/test_output_formatting.py
autonomous: true
requirements:
  - FUNC-01
  - FUNC-02

must_haves:
  truths:
    - "test_get_start_event_name_function_entry_prefix 验证 'FunctionEntry.Move' 格式"
    - "test_build_execution_flows_function_entry 验证 FunctionEntry → CallFunction → CallFunction 链"
    - "test_build_execution_flows_pure_function_marking 验证纯函数标记 pure=true"
    - "test_execution_flow_knot_transparent 验证 Knot 节点不出现在 flow 输出中"
  artifacts:
    - path: "tests/test_output_formatting.py"
      provides: "4 个新测试函数 + 必要的 test fixtures"
      contains: "def test_get_start_event_name_function_entry_prefix"
  key_links:
    - from: "tests/test_output_formatting.py:test_get_start_event_name_function_entry_prefix"
      to: "src/uasset_read/graph/flow_builder.py:_get_start_event_name"
      via: "直接调用函数验证前缀格式"
      pattern: "_get_start_event_name"
    - from: "tests/test_output_formatting.py:test_build_execution_flows_function_entry"
      to: "src/uasset_read/graph/flow_builder.py:build_execution_flows"
      via: "合成 FunctionEntry 图验证执行流构建"
      pattern: "build_execution_flows"

threat_model:
  Trust Boundaries:

  | Boundary | Description |
  |----------|-------------|
  | 测试 fixture → production code | 测试数据可能不覆盖边界条件（None node_data, missing pins） |

  STRIDE Threat Register:

  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-53-03 | Tampering | 测试 fixture 构造 | mitigate | 每个测试覆盖正常路径 + 至少一个边界条件（如 node_data=None fallback） |
  | T-53-SC | Tampering | npm/pip/cargo installs | mitigate | 本阶段零外部依赖，无包安装 |
---

<objective>
为 Phase 53 的改动添加 4 个测试，遵循已有的 synthetic fixture 模式。

Purpose: 验证 _get_start_event_name 前缀格式、FunctionEntry 执行流、pure function 标记、Knot 透明性。确保改动不破坏现有行为。

Output: tests/test_output_formatting.py 新增 4 个测试函数
</objective>

<execution_context>
@E:\Develop\uasset_read\.gsd\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.gsd\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\PROJECT.md
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md

<read_first>
- tests/test_output_formatting.py 第 274-344 行 — sample_graph_with_execution_flow fixture 模式（synthetic 构造）
- tests/test_output_formatting.py 第 2440-2503 行 — test_build_execution_flows_enhanced_input_action / test_build_execution_flows_variable_set_start 模式（独立测试函数，内联构造图）
- tests/test_output_formatting.py 第 17-47 行 — 现有 import 块，确认需要添加 K2NodeFunctionEntry 和 K2NodeKnot
- reference/蓝图节点文本参考.md 第 228-341 行 — Move 函数的真实节点链（FunctionEntry → CallFunction_7445 → CallFunction_7346，Knot 节点仅数据流）
</read_first>

<interfaces>
<!-- 关键类型契约 — 执行者直接使用 -->

From tests/test_output_formatting.py 第 17-47 行 — 现有 imports，需要追加：
```python
# Phase 53 imports
K2NodeFunctionEntry,
K2NodeKnot,
```

From src/uasset_read/models/node_types.py — K2NodeKnot:
```python
@dataclass
class K2NodeKnot(UEdGraphNode):
    """K2Node_Knot 节点。"""
    pass  # 无额外字段
```

测试构造模式（参照第 2440-2503 行模式）：
- 在测试函数内 import 所需类型
- 构造 FEdGraphPinType（pin_category 决定 exec/data）
- 构造 UEdGraphPin（pin_id, pin_name, direction, pin_type, linked_to_raw）
- 构造 UEdGraphNode（node_guid, pins, class_name, node_data）
- 构造 UEdGraph（graph_name, graph_class, nodes）
- 调用 build_execution_flows(graph) 验证结果
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 FunctionEntry 前缀和执行流测试</name>
  <files>tests/test_output_formatting.py</files>
  <read_first>
    - tests/test_output_formatting.py 第 17-47 行 — import 块
    - tests/test_output_formatting.py 第 2440-2446 行 — test_build_execution_flows_enhanced_input_action 模式
    - tests/test_output_formatting.py 第 2340-2445 行 — EnhancedInputAction 测试完整模式
  </read_first>
  <action>
在 tests/test_output_formatting.py 末尾（第 3394 行之后）添加两个测试：

**测试 1 — test_get_start_event_name_function_entry_prefix：**
直接测试 _get_start_event_name 函数。构造一个 K2Node_FunctionEntry 节点（node_data=K2NodeFunctionEntry，function_reference.member_name="Move"），调用 _get_start_event_name，断言返回 "FunctionEntry.Move"。

同时测试 Event 前缀：构造 K2Node_Event 节点（node_data=K2NodeEvent，event_reference.member_name="BeginPlay"），断言返回 "Event.BeginPlay"。

**测试 2 — test_build_execution_flows_function_entry：**
构造合成图模拟 Move 函数的执行流（基于 reference/蓝图节点文本参考.md 第 228-266 行的真实链）：

节点顺序：
1. K2Node_FunctionEntry_0 (FunctionEntry.Move) — exec output pin linked_to=CallFunction_7445.execute
2. K2Node_CallFunction_7445 (AddMovementInput) — exec input + exec output "then" linked_to=CallFunction_7346.execute
3. K2Node_CallFunction_7346 (AddMovementInput) — exec input only（链路结束）

验证：
- len(flows) == 1
- flows[0]["start_event"] == "FunctionEntry.Move"
- flows[0]["nodes"] 包含 3 个节点
- nodes[0]["node_type"] == "K2Node_FunctionEntry"，nodes[0]["function_name"] == "Move"
- nodes[1]["node_type"] == "K2Node_CallFunction"，nodes[1]["function_name"] == "AddMovementInput"
- nodes[2]["node_type"] == "K2Node_CallFunction"，nodes[2]["function_name"] == "AddMovementInput"

使用 synthetic fixture 模式（内联构造，不使用 @pytest.fixture）。pin_id 使用有意义的字符串（如 "fe_exec_out", "cf7445_exec_in", "cf7445_exec_out", "cf7346_exec_in"）。
  </action>
  <verify>
    <automated>pytest tests/test_output_formatting.py -v -k "test_get_start_event_name_function_entry_prefix or test_build_execution_flows_function_entry" -x</automated>
  </verify>
  <done>两个测试通过：FunctionEntry 前缀正确，执行流链 FunctionEntry → CallFunction → CallFunction 完整</done>
</task>

<task type="auto">
  <name>Task 2: 添加 pure function 和 Knot 透明性测试</name>
  <files>tests/test_output_formatting.py</files>
  <read_first>
    - tests/test_output_formatting.py 第 2448-2503 行 — VariableSet start 测试模式
    - reference/蓝图节点文本参考.md 第 323-340 行 — GetActorForwardVector/GetActorRightVector（bDefaultsToPure=True，无 exec pins）
  </read_first>
  <action>
在 tests/test_output_formatting.py 继续添加两个测试：

**测试 3 — test_build_execution_flows_pure_function_marking：**
构造合成图模拟 FunctionEntry → CallFunction(pure) → CallFunction(impure) 链：

1. K2Node_FunctionEntry — exec output linked_to=CallA.execute
   node_data=K2NodeFunctionEntry(function_reference=FMemberReference(member_name="TestFunc"))
2. K2Node_CallFunction A — 有 exec input + exec output，但 b_defaults_to_pure=True（如某些特殊函数）
3. K2Node_CallFunction B — exec input（链路结束）

验证 nodes[1]["pure"] == True。

同时添加 b_defaults_to_pure=False 的对照测试，确保 "pure" 键不存在。

**测试 4 — test_execution_flow_knot_transparent：**
构造合成图包含 Knot 节点在数据流中但不在执行流中：

1. K2Node_FunctionEntry — exec output linked_to=CallFunc.execute，data output linked_to=Knot1.input
2. K2Node_Knot — 只有 real input pin 和 real output pin（无 exec pins）
3. K2Node_CallFunction — exec input（链路结束），data input linked_from=Knot

验证：
- len(flows) == 1
- flows[0]["nodes"] 只包含 FunctionEntry 和 CallFunction（2 个节点）
- K2Node_Knot 不出现在 flow nodes 中（因为 _find_next_exec_node 只追踪 exec pins）
- 这已经是现有行为（Knot 无 exec pins），测试确保不被 Phase 53 改动破坏
  </action>
  <verify>
    <automated>pytest tests/test_output_formatting.py -v -k "test_build_execution_flows_pure_function_marking or test_execution_flow_knot_transparent" -x</automated>
  </verify>
  <done>pure function 标记正确（b_defaults_to_pure=True 时 pure=true），Knot 节点不出现在 execution flow 中</done>
</task>

</tasks>

<verification>
- `pytest tests/test_output_formatting.py -v -k "function_entry or pure_function or knot_transparent" -x` — 4 个新测试全部通过
- `pytest tests/test_output_formatting.py -v -x` — 全部现有测试不受影响
- `pytest tests/ -v --tb=short 2>&1 | tail -5` — 整体测试无新增失败
</verification>

<success_criteria>
- test_get_start_event_name_function_entry_prefix 断言 "FunctionEntry.Move" 和 "Event.BeginPlay"
- test_build_execution_flows_function_entry 验证 FunctionEntry → CallFunction → CallFunction 链，start_event 为 "FunctionEntry.Move"
- test_build_execution_flows_pure_function_marking 验证 b_defaults_to_pure=True 的 CallFunction 在 flow 中 "pure": true
- test_execution_flow_knot_transparent 验证 Knot 不在 flow nodes 中
- 所有 554 现有测试通过（无回归）
</success_criteria>

<output>
Create `.planning/phases/53-function-execution-flow/53-02-SUMMARY.md` when done
</output>
