# v6.0 归档 — Phase 27-29

本文件归档已完成的早期阶段详情，保留记录但不在 `STATE.md` 中重复加载。

---

## Phase 27: 项目结构初始化 ✓ Complete

**目标:** 创建 src 目录结构、`constants.py`、`exceptions.py`

**完成内容:**
- 创建 `src/uasset_read/` 目录结构符合 Python src layout
- 创建 `src/uasset_read/constants.py` — 版本号、属性类型阈值、边界常量
- 创建 `src/uasset_read/exceptions.py` — `UAssetError`, `VersionError`, `ParseError`, `ErrorContext`
- 配置 `pyproject.toml` 零依赖安装

---

## Phase 28: 核心序列化模块 ✓ Complete

**目标:** 拆分 `FArchive`, `PackageFileSummary`, `Import/ExportMap`

**完成内容:**
- `src/uasset_read/archive.py` — `FArchive` 二进制读取器，所有读写方法、字节交换、mmap 支持
- `src/uasset_read/serializers/package_summary.py` — `PackageFileSummary`, `GenerationInfo`, `EngineVersion`, `CustomVersion`
- `src/uasset_read/serializers/object_resources.py` — `ObjectImport`, `ObjectExport`, `PackageIndex`, resolve 函数

依赖: 仅 `constants.py`, `exceptions.py`，依赖单向无循环。

---

## Phase 28a: 测试基线修复 ✓ Complete

**目标:** 修复 7 个已知测试失败，合并 stash 关键修复

**修复内容:**
- 修复 `peek_i32()` 偏移计算
- 修复 `ETextHistoryType` 枚举支持 0xFF (None)
- 修复 `FText` 跳过逻辑影响后续字段位置
- 添加 pin 读取容错处理

**结果:** 380 passed, 62 skipped, 0 failed 基线建立。

---

## Phase 29: 核心数据模型 ✓ Complete

**目标:** 定义 `UEdGraph`, `Node`, `Pin`, `ParseResult` 等 dataclasses

**完成内容:**
- `src/uasset_read/models/core.py` — `ParseResult`, `StatusInfo`
- `src/uasset_read/models/node_types.py` — `UEdGraph`, `UEdGraphNode`, `UEdGraphPin`, `FEdGraphPinType`, `FMemberReference` + 各种 K2Node 子类
- `src/uasset_read/models/blueprint.py` — `BlueprintMetadata`, `BlueprintVariable`, `BlueprintFunction`, `BlueprintEvent`
- `src/uasset_read/models/result.py` — 结果状态结构

使用 dataclass，零依赖。
