---
phase: 31-graph-parsing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - uasset_read_legacy.py
  - tests/test_phase12_blueprint_variables.py
  - tests/test_property_parsing.py
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "format_variable_type in legacy shim uses pin_subcategory (not pin_sub_category)"
    - "format_variable_type uses getattr for is_const/is_weak_pointer (compatibility)"
    - "test_phase12 container_type uses int (0/1/2) not string ('None')"
    - "test_property_parsing expects parse_object_property to return int not dict"
  artifacts:
    - path: "uasset_read_legacy.py"
      provides: "Fixed format_variable_type to work with new FEdGraphPinType"
    - path: "tests/test_phase12_blueprint_variables.py"
      provides: "Fixed container_type and is_const test data"
    - path: "tests/test_property_parsing.py"
      provides: "Fixed parse_object_property return type expectations"
  key_links:
    - from: "uasset_read_legacy.py:format_variable_type"
      to: "src/uasset_read/models/core.py"
      via: "FEdGraphPinType field names"
      pattern: "pin_subcategory|container_type"
    - from: "tests/test_phase12_blueprint_variables.py"
      to: "uasset_read_legacy.py"
      via: "format_variable_type function"
      pattern: "def format_variable_type"
---

<objective>
Fix 9 test failures by fixing legacy shim compatibility AND test data construction:
- uasset_read_legacy.py: format_variable_type uses old field names incompatible with new FEdGraphPinType (causes all 7 test_phase12 failures)
- tests/test_phase12_blueprint_variables.py: container_type="None" should be int (contributes to failures)
- tests/test_property_parsing.py: expects dict return from parse_object_property but gets int (2 failures)

Purpose: Make legacy shim functions compatible with new dataclass field names AND fix test data construction.
Output: 3 files modified, 9 failures eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-graph-parsing/31-UAT.md

# New FEdGraphPinType (src/uasset_read/models/core.py lines 22-30)
- pin_category: str = ""
- pin_subcategory: str = ""  (NOT pin_sub_category)
- pin_subcategory_object: Optional[str] = None  (NOT int)
- container_type: int = 0    (NOT str "None")
- is_map_key: bool = False
- is_map_value: bool = False
- NO is_const field
- NO is_weak_pointer field

# Legacy format_variable_type (uasset_read_legacy.py lines 4861-4939)
- Line 4889: pin_type.pin_sub_category.lower() -> AttributeError (new dataclass has pin_subcategory)
- Line 4907: pin_type.pin_sub_category_object != 0 -> wrong type (new is Optional[str], old was int)
- Line 4917: pin_type.is_weak_pointer -> AttributeError (field doesn't exist in new dataclass)
- Line 4936: pin_type.is_const -> AttributeError (field doesn't exist in new dataclass)

# Fix strategy for legacy shim: use getattr() for optional fields, rename pin_sub_category
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix format_variable_type in uasset_read_legacy.py for new FEdGraphPinType compatibility</name>
  <files>uasset_read_legacy.py</files>
  <action>
Fix format_variable_type function in uasset_read_legacy.py (lines 4861-4939) to work with the NEW FEdGraphPinType dataclass from src/uasset_read/models/core.py.

**Changes required (6 lines):**

1. Line 4889: `pin_type.pin_sub_category.lower()` -> `getattr(pin_type, 'pin_subcategory', getattr(pin_type, 'pin_sub_category', '')).lower()`
   - Primary: use pin_subcategory (new name)
   - Fallback: pin_sub_category (legacy name for backwards compat)
   - Default: empty string

2. Line 4907: `pin_type.pin_sub_category_object != 0` -> `getattr(pin_type, 'pin_subcategory_object', None) is not None`
   - New field is Optional[str], old was int
   - Check for non-None instead of != 0

3. Line 4917: `if not pin_type.is_weak_pointer:` -> `if not getattr(pin_type, 'is_weak_pointer', True):`
   - New dataclass doesn't have is_weak_pointer
   - Default to True (add * suffix) for backwards compat with old behavior

4. Line 4936: `if pin_type.is_const:` -> `if getattr(pin_type, 'is_const', False):`
   - New dataclass doesn't have is_const
   - Default to False (no const prefix)

5. Line 4873 (docstring): `pin_sub_category_object` -> `pin_subcategory_object`

6. Line 4923 (reference type check): also uses sub_category variable -- this is fine since it's the local var from line 4889

**Verification after changes:**
python -c "
from uasset_read import FEdGraphPinType, format_variable_type
assert format_variable_type(FEdGraphPinType(pin_category='float')) == 'float'
assert format_variable_type(FEdGraphPinType(pin_category='int')) == 'int'
assert format_variable_type(FEdGraphPinType(pin_category='bool')) == 'bool'
assert format_variable_type(FEdGraphPinType(pin_category='float', container_type=1)) == 'TArray<float>'
assert format_variable_type(FEdGraphPinType(pin_category='int', container_type=2)) == 'TSet<int>'
assert format_variable_type(FEdGraphPinType(pin_category='object')).endswith('*')
assert format_variable_type(FEdGraphPinType(pin_category='string')) == 'FString'
assert format_variable_type(FEdGraphPinType(pin_category='name')) == 'FName'
print('All format_variable_type tests pass')
"

read_first:
  - uasset_read_legacy.py (lines 4861-4939, format_variable_type function)
  - src/uasset_read/models/core.py (FEdGraphPinType definition, lines 22-30)

acceptance_criteria:
  - grep "pin_type.pin_sub_category" uasset_read_legacy.py | grep -v "getattr" | wc -l == 0 (all bare accesses replaced)
  - grep "pin_type.is_const" uasset_read_legacy.py | grep -v "getattr" | wc -l == 0 (bare is_const replaced)
  - grep "pin_type.is_weak_pointer" uasset_read_legacy.py | grep -v "getattr" | wc -l == 0 (bare is_weak_pointer replaced)
  - python -c "from uasset_read import FEdGraphPinType, format_variable_type; format_variable_type(FEdGraphPinType(pin_category='float'))" runs without error
  - python -m pytest tests/test_phase12_blueprint_variables.py::TestVariableTypeFormatting -x --tb=short passes (0 failures)
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase12_blueprint_variables.py::TestVariableTypeFormatting -x --tb=short</automated>
  </verify>
  <done>format_variable_type in uasset_read_legacy.py works with new FEdGraphPinType. All TestVariableTypeFormatting tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_phase12_blueprint_variables.py container_type and is_const test data</name>
  <files>tests/test_phase12_blueprint_variables.py</files>
  <action>
Fix remaining test_phase12_blueprint_variables.py issues after format_variable_type shim fix:

1. **container_type="None" -> container_type=0** (it's an int field):
   Find ALL occurrences with: grep -n 'container_type="None"' tests/test_phase12_blueprint_variables.py
   Replace each: container_type="None" -> container_type=0
   This affects TestVariableTypeFormatting class tests.

2. **is_const=True test** (line 188, test_format_const_type_adds_const_prefix):
   The test creates FEdGraphPinType(pin_category="float", is_const=True) -- FEdGraphPinType has no is_const field.
   Since format_variable_type now uses getattr(pin_type, 'is_const', False), passing is_const=True as a kwarg will raise TypeError.
   - Change the test to: pytest.skip("is_const removed in v6.0 -- const prefix no longer supported")

3. **Note**: After Task 1 (legacy shim fix), tests that use format_variable_type with correct field names should pass. But tests that pass invalid kwargs (like is_const=True) will still fail with TypeError. So this task is still needed.

read_first:
  - tests/test_phase12_blueprint_variables.py (full file)
  - src/uasset_read/models/core.py (FEdGraphPinType fields)

acceptance_criteria:
  - grep -c 'container_type="None"' tests/test_phase12_blueprint_variables.py == 0
  - grep -c 'is_const=True' tests/test_phase12_blueprint_variables.py == 0
  - python -m pytest tests/test_phase12_blueprint_variables.py -x --tb=short passes (0 failures)
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase12_blueprint_variables.py -x --tb=short</automated>
  </verify>
  <done>test_phase12_blueprint_variables.py: container_type uses int, is_const test skipped. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fix test_property_parsing.py parse_object_property return type</name>
  <files>tests/test_property_parsing.py</files>
  <action>
Fix test_property_parsing.py where tests expect parse_object_property to return enhanced dict but it now returns int (FPackageIndex raw value):

1. **test_object_property_in_parse_properties** (lines ~841-923):
   The test expects prop.value to be a dict with raw_index and resolved keys.
   - Change assertion at line ~917-923:
     - Remove: assert isinstance(prop.value, dict), assert "raw_index" in prop.value, assert "resolved" in prop.value
     - Replace with: assert isinstance(prop.value, int), assert prop.value == -1

2. **test_object_property_null_in_parse_properties** (lines ~926-983):
   - Change assertion at line ~982-983:
     - Remove: assert prop.value["raw_index"] == 0, assert prop.value["resolved"] is None
     - Replace with: assert prop.value == 0

3. Keep test_parse_object_property (line 438-445) and test_parse_object_property_import_reference (line 448-455) -- they already expect int return.

4. Keep test_object_property_resolved_import/export (lines 709-838) -- they test resolve_package_index_to_reference, not parse_object_property.

read_first:
  - tests/test_property_parsing.py (lines 438-455, 709-983)
  - src/uasset_read/parsers/property_types.py (parse_object_property, line 79-81)

acceptance_criteria:
  - grep "raw_index" tests/test_property_parsing.py returns 0 matches
  - python -m pytest tests/test_property_parsing.py -x --tb=short passes (0 failures)
  </action>
  <verify>
    <automated>python -m pytest tests/test_property_parsing.py -x --tb=short</automated>
  </verify>
  <done>test_property_parsing.py: parse_object_property tests expect int return. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy shim -> new dataclass | format_variable_type receives NEW FEdGraphPinType from test imports |
| test -> production code | Tests verify behavior, don't change production logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | Integrity | uasset_read_legacy.py | mitigate | Use getattr() with safe defaults -- never crash on missing fields |
| T-31-04-02 | Integrity | tests/test_phase12_blueprint_variables.py | mitigate | Verify only field name changes, no test logic removal |
| T-31-04-03 | Integrity | tests/test_property_parsing.py | mitigate | Preserve int-returning tests as correctness baseline |
</threat_model>

<verification>
1. python -m pytest tests/test_phase12_blueprint_variables.py tests/test_property_parsing.py -v --tb=short
2. All tests should pass (0 failures)
3. No new failures introduced in other test files
</verification>

<success_criteria>
- [ ] uasset_read_legacy.py format_variable_type works with new FEdGraphPinType
- [ ] test_phase12_blueprint_variables.py: 0 failures (was 7)
- [ ] test_property_parsing.py: 0 failures (was 2)
- [ ] No regressions in other test files
- [ ] grep confirms bare pin_sub_category accesses replaced with getattr
- [ ] grep confirms container_type="None" replaced with container_type=0
- [ ] grep confirms raw_index/resolved dict expectations removed
</success_criteria>

<output>
After completion, create `.planning/phases/31-graph-parsing/31-04-SUMMARY.md`
</output>
