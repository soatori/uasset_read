---
phase: 31-graph-parsing
plan: 05
type: execute
wave: 2
depends_on: ["31-04"]
files_modified:
  - tests/test_phase13_transform.py
  - tests/test_phase26_blueprint_metadata_enhancement.py
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "test_phase13 imports parse_component_transform (not extract_component_transforms)"
    - "test_phase13 adapts to dict return type (not dataclass with x/y/z attributes)"
    - "test_phase26 removes _parse_property_flags calls (method doesn't exist on new FArchive)"
  artifacts:
    - path: "tests/test_phase13_transform.py"
      provides: "Updated to use parse_component_transform with dict return type"
    - path: "tests/test_phase26_blueprint_metadata_enhancement.py"
      provides: "Flag parsing tests use parse_property_flags_to_labels instead of archive method"
  key_links:
    - from: "tests/test_phase13_transform.py"
      to: "src/uasset_read/blueprint/variable_extractor.py"
      via: "parse_component_transform function"
      pattern: "def parse_component_transform"
    - from: "tests/test_phase26_blueprint_metadata_enhancement.py"
      to: "src/uasset_read/__init__.py"
      via: "parse_property_flags_to_labels shim"
      pattern: "parse_property_flags_to_labels"
---

<objective>
Fix 2 test files with function name/return type mismatches: test_phase13_transform.py (5 failures) and test_phase26_blueprint_metadata_enhancement.py (2 failures).

Purpose: Align tests with renamed functions and removed FArchive methods. Requires understanding new API behavior, not just find/replace.
Output: 2 fixed test files, both passing pytest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-graph-parsing/31-UAT.md

# parse_component_transform (new function, src/uasset_read/blueprint/variable_extractor.py line 227)
- Signature: def parse_component_transform(properties: List[PropertyValue]) -> Dict[str, Any]
- Returns dict with keys like "relative_location", "relative_rotation", "relative_scale3d"
- Values are dicts (not dataclasses): _extract_vector returns {"X": val, "Y": val, "Z": val}
- _extract_rotator returns {"Roll": val, "Pitch": val, "Yaw": val}

# Old extract_component_transforms (legacy in uasset_read.py)
- Was exported as: extract_component_transforms
- Old shim still exports it via __init__.py line 255: extract_component_transforms = _legacy.extract_component_transforms
- But the shim depends on legacy file existing — if deleted in Phase 33, this breaks

# test_phase13_transform.py current imports (lines 16-28):
# from uasset_read import (
#     VectorValue, RotatorValue, ScaleValue,  # legacy shim only
#     StructValue, PropertyValue,
#     parse_vector_value, parse_rotator_value, parse_scale_value,  # legacy only
#     format_transform_value,  # legacy only
#     extract_component_transforms,  # legacy only
#     parse_uasset,
# )

# _parse_property_flags does NOT exist on new FArchive
# FArchive (archive.py) has: read_u8, read_i32, read_fstring, read_name, etc.
# NO _parse_property_flags method
# The flag parsing logic is in parse_property_flags_to_labels (legacy shim)
# Phase 26 tests call archive._parse_property_flags(flags) expecting dict of boolean flags

# BlueprintVariable dataclass HAS all Phase 26 fields already (models/blueprint.py):
# is_edit_anywhere, is_edit_instance_only, is_visible_anywhere, is_blueprint_read_only,
# is_blueprint_readable, is_blueprint_writable, is_transient, is_save_game, is_rep_notify,
# is_interp, is_expose_on_spawn, is_net, is_replicated, is_blueprint_assignable,
# is_blueprint_callable, is_non_pi_ed_duplicate_transient, meta_data, edit_condition,
# meta_class, edit_category, edit_widget
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_phase13_transform.py function name and return type</name>
  <files>tests/test_phase13_transform.py</files>
  <action>
Rewrite test_phase13_transform.py to use new API:

**Import changes (lines 16-28):**
- REMOVE: `VectorValue, RotatorValue, ScaleValue` (legacy-only)
- REMOVE: `parse_vector_value, parse_rotator_value, parse_scale_value` (legacy-only)
- REMOVE: `format_transform_value` (legacy-only)
- REPLACE: `extract_component_transforms` → `parse_component_transform`
- KEEP: `StructValue, PropertyValue, parse_uasset`

**TestTransformValuesConstructor class (lines 42-91) — REMOVE entirely:**
- All VectorValue/RotatorValue/ScaleValue constructor tests are legacy-only
- These classes only exist in the legacy shim
- Replace with a single test verifying parse_component_transform returns dict with expected keys

**TestStructValueConversion class (lines 94-144) — REMOVE entirely:**
- parse_vector_value/parse_rotator_value/parse_scale_value are legacy functions
- These tested StructValue → specialized dataclass conversion
- Replace with test verifying parse_component_transform handles PropertyValue with StructValue correctly

**TestPrecisionHandling class (lines 147-181) — REMOVE entirely:**
- format_transform_value is legacy-only
- Precision formatting is handled internally by the new module
- Replace with test that dict values preserve numeric precision

**TestComponentTransforms class (lines 184-296) — REWRITE:**
- Replace `extract_component_transforms(props)` with `parse_component_transform(props)`
- Change assertions from dataclass attribute access to dict key access:
  - `result["relative_location"].x == 100` → `result["relative_location"]["X"] == 100.0`
  - `result["relative_rotation"].yaw == 90.0` → `result["relative_rotation"]["Yaw"] == 90.0`
  - `result["relative_scale"].x == 1.5` → `result["relative_scale3d"]["X"] == 1.5`
- Note: The new function uses key "relative_scale3d" not "relative_scale" (see variable_extractor.py line 254)
- Empty test: `parse_component_transform([])` should return `{}`
- Single property tests: create PropertyValue with StructValue, pass to parse_component_transform
- All three test: create 3 PropertyValues, verify all 3 keys present
- Ignores non-transform properties: verify only transform keys in result

**TestIntegration class (lines 299-338) — REWRITE:**
- parse_uasset is a legacy shim that works
- Replace `exp.transforms` access with appropriate new access pattern
- Since transforms is set on exports by the legacy pipeline, verify the dict structure

**New tests to add (replacing removed functionality):**
```python
class TestParseComponentTransform:
    def test_empty_returns_empty_dict(self):
        result = parse_component_transform([])
        assert result == {}

    def test_relative_location_returns_dict(self):
        props = [PropertyValue(name="RelativeLocation", type="StructProperty", value=StructValue(
            property_type="StructProperty", struct_type="Vector",
            fields={"X": 100.0, "Y": 200.0, "Z": 0.0}
        ))]
        result = parse_component_transform(props)
        assert "relative_location" in result
        assert result["relative_location"]["X"] == 100.0

    def test_relative_rotation_returns_dict(self):
        props = [PropertyValue(name="RelativeRotation", type="StructProperty", value=StructValue(
            property_type="StructProperty", struct_type="Rotator",
            fields={"Roll": 0.0, "Pitch": 0.0, "Yaw": 90.0}
        ))]
        result = parse_component_transform(props)
        assert "relative_rotation" in result
        assert result["relative_rotation"]["Yaw"] == 90.0

    def test_relative_scale3d_returns_dict(self):
        props = [PropertyValue(name="RelativeScale3D", type="StructProperty", value=StructValue(
            property_type="StructProperty", struct_type="Vector",
            fields={"X": 1.5, "Y": 1.5, "Z": 1.5}
        ))]
        result = parse_component_transform(props)
        assert "relative_scale3d" in result  # NOTE: "relative_scale3d" not "relative_scale"
        assert result["relative_scale3d"]["X"] == 1.5
```

read_first:
  - tests/test_phase13_transform.py (full file)
  - src/uasset_read/blueprint/variable_extractor.py (parse_component_transform, lines 227-270, plus _extract_vector/_extract_rotator helpers)
  - src/uasset_read/models/core.py (PropertyValue, StructValue definitions)

acceptance_criteria:
  - grep -c "extract_component_transforms" tests/test_phase13_transform.py == 0 (old name eliminated)
  - grep -c "parse_component_transform" tests/test_phase13_transform.py >= 3 (new name in use)
  - grep -c "VectorValue\|RotatorValue\|ScaleValue" tests/test_phase13_transform.py == 0 (legacy dataclasses removed)
  - grep "relative_scale3d" tests/test_phase13_transform.py returns at least 1 match (new key name)
  - python -m pytest tests/test_phase13_transform.py -x --tb=short passes (0 failures)
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase13_transform.py -x --tb=short</automated>
  </verify>
  <done>test_phase13_transform.py: extract_component_transforms replaced with parse_component_transform, dict access patterns replace dataclass attributes, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_phase26_blueprint_metadata_enhancement.py _parse_property_flags removal</name>
  <files>tests/test_phase26_blueprint_metadata_enhancement.py</files>
  <action>
Fix test_phase26_blueprint_metadata_enhancement.py where tests call archive._parse_property_flags() which doesn't exist on new FArchive:

**test_parse_property_flags_returns_correct_flags (lines 67-102):**
- CURRENT: Creates `FArchive.__new__(FArchive)`, sets `_byte_swapping`, calls `archive._parse_property_flags(flags)`
- PROBLEM: _parse_property_flags never existed on the new FArchive class
- FIX: Replace with calls to `parse_property_flags_to_labels(flags)` from uasset_read (legacy shim)
- The legacy function returns a list of string labels (e.g., ["EditAnywhere", "BlueprintReadWrite"])
- Rewrite assertions to check labels list instead of dict:
  - `flags['is_edit_anywhere'] == True` → `"EditAnywhere" in labels`
  - `flags['is_edit_instance_only'] == True` → `"EditInstanceOnly" in labels`
  - `flags['is_blueprint_readable'] == True` → `"BlueprintVisible" in labels`
  - `flags['is_blueprint_writable'] == True` → `"BlueprintReadWrite" in labels` or similar
  - `flags['is_transient'] == True` → `"Transient" in labels`
  - `flags['is_save_game'] == True` → `"SaveGame" in labels`
  - `flags['is_expose_on_spawn'] == True` → `"ExposeOnSpawn" in labels`

**test_parse_property_flags_combined_flags (lines 104-116):**
- Same approach: use parse_property_flags_to_labels with combined flags
- Check that multiple labels are present in the returned list

**Import changes (lines 9-22):**
- ADD: `parse_property_flags_to_labels` to the import from uasset_read
- REMOVE: `FArchive` from imports (no longer needed for flag tests)

**Other tests (lines 28-66, 118-197) — NO CHANGES NEEDED:**
- test_blueprint_variable_has_phase26_fields: BlueprintVariable dataclass already has all fields
- test_meta_data_initialized_as_dict: BlueprintVariable meta_data defaults to empty dict
- test_blueprint_variable_can_set_phase26_fields: BlueprintVariable accepts these kwargs
- test_metadata_field_stores_meta_data: BlueprintVariable has metadata dict
- test_all_phase26_boolean_flags_default_to_false: BlueprintVariable defaults are all False

read_first:
  - tests/test_phase26_blueprint_metadata_enhancement.py (full file)
  - src/uasset_read/__init__.py (parse_property_flags_to_labels export, line 227/451)
  - uasset_read.py (parse_property_flags_to_labels implementation — search for "def parse_property_flags_to_labels")
  - src/uasset_read/models/blueprint.py (BlueprintVariable definition to confirm fields exist)

acceptance_criteria:
  - grep -c "_parse_property_flags" tests/test_phase26_blueprint_metadata_enhancement.py == 0 (old method eliminated)
  - grep -c "parse_property_flags_to_labels" tests/test_phase26_blueprint_metadata_enhancement.py >= 2 (new function in use)
  - grep "FArchive" tests/test_phase26_blueprint_metadata_enhancement.py == 0 (no longer needed)
  - python -m pytest tests/test_phase26_blueprint_metadata_enhancement.py -x --tb=short passes (0 failures)
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase26_blueprint_metadata_enhancement.py -x --tb=short</automated>
  </verify>
  <done>test_phase26_blueprint_metadata_enhancement.py: _parse_property_flags calls replaced with parse_property_flags_to_labels, assertions use list membership checks, all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Tests verify behavior, don't change production logic |
| legacy shim → new code | parse_property_flags_to_labels is a legacy shim — ensure it handles all flag combinations tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05-01 | Integrity | tests/test_phase13_transform.py | mitigate | Verify dict key names match _extract_vector/_extract_rotator output (uppercase X/Y/Z, Roll/Pitch/Yaw) |
| T-31-05-02 | Integrity | tests/test_phase26_blueprint_metadata_enhancement.py | mitigate | Cross-reference parse_property_flags_to_labels output labels with test assertions — ensure label names match actual output |
</threat_model>

<verification>
1. python -m pytest tests/test_phase13_transform.py tests/test_phase26_blueprint_metadata_enhancement.py -v --tb=short
2. All tests should pass (0 failures)
3. No regressions in other test files
</verification>

<success_criteria>
- [ ] test_phase13_transform.py: 0 failures (was 5)
- [ ] test_phase26_blueprint_metadata_enhancement.py: 0 failures (was 2)
- [ ] No regressions in other test files
- [ ] grep confirms extract_component_transforms replaced with parse_component_transform
- [ ] grep confirms _parse_property_flags replaced with parse_property_flags_to_labels
- [ ] grep confirms VectorValue/RotatorValue/ScaleValue imports removed from test_phase13
</success_criteria>

<output>
After completion, create `.planning/phases/31-graph-parsing/31-05-SUMMARY.md`
</output>
