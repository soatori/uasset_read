---
phase: 31
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/uasset_read/models/core.py
  - src/uasset_read/models/node_types.py
  - src/uasset_read/__init__.py
autonomous: true
requirements: [MOD-08, MOD-09, TEST-01]

must_haves:
  truths:
    - "模型类的 from_archive 方法可正确调用 serializers/graph.py 中的对应函数"
    - "从 uasset_read 包导入图解析函数无循环导入错误"
    - "现有测试的导入路径仍然有效"
  artifacts:
    - path: "src/uasset_read/models/core.py"
      provides: "from_archive 实现（委托到 serializers/graph.py）"
      contains: "def from_archive"
    - path: "src/uasset_read/models/node_types.py"
      provides: "节点类型 from_archive 实现"
      contains: "def from_archive"
    - path: "src/uasset_read/__init__.py"
      provides: "图解析公共 API 导出"
      exports: ["extract_blueprint_graphs", "build_execution_flows", "build_data_flows"]
  key_links:
    - from: "src/uasset_read/models/core.py"
      to: "src/uasset_read/serializers/graph.py"
      via: "from_archive 方法内延迟导入"
      pattern: "from uasset_read.serializers.graph import read_ue_graph_pin"
    - from: "src/uasset_read/__init__.py"
      to: "src/uasset_read/graph/"
      via: "公共 API 导出"
      pattern: "from uasset_read.graph import"
---

<objective>
实现 models/core.py 和 models/node_types.py 的 from_archive 委托，更新顶层 __init__.py 导出图解析公共 API。

Purpose: 连接 Phase 29 数据模型 stubs 与 Phase 31 序列化函数，建立完整的数据解析链路。确保现有测试的导入路径兼容性。
Output: 更新后的 models/core.py, models/node_types.py, __init__.py
</objective>

<execution_context>
@E:\Develop\uasset_read\.planning\workflows\execute-plan.md
@E:\Develop\uasset_read\.planning\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\PROJECT.md
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\phases\31-graph-parsing\31-CONTEXT.md
@E:\Develop\uasset_read\.planning\phases\31-graph-parsing\31-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/uasset_read/serializers/graph.py (Plan 01 产出):
```python
def read_ue_graph_pin(archive, name_map, summary, export_map, import_map) -> UEdGraphPin
def read_ue_graph_node(archive, name_map, summary, export_map, import_map, node_export) -> UEdGraphNode
def read_ue_graph(archive, name_map, summary, export_map, import_map, graph_export, graph_class, graph_export_idx=0) -> UEdGraph
def read_ed_graph_pin_type(archive, name_map, summary) -> FEdGraphPinType
def read_fmember_reference(archive, name_map, import_map, export_map) -> FMemberReference
def create_node_from_archive(archive, name_map, summary, export_map, import_map, node_export, base_node) -> UEdGraphNode
```

From src/uasset_read/archive.py:
```python
class FArchive:
    def read_i32(self) -> int
    def read_u8(self) -> int
    def read_bytes(self, n: int) -> bytes
```

From src/uasset_read/serializers/package_summary.py:
```python
@dataclass
class PackageFileSummary:
    legacy_file_version: int
    file_version_ue4: int
    file_version_ue5: int
    package_flags: int
    def get_custom_version(self, guid: str, default: int) -> int
```

From src/uasset_read/serializers/object_resources.py:
```python
@dataclass
class ObjectExport:
    object_name: str
    class_index: PackageIndex
    serial_offset: int
    serial_size: int
    script_serial_offset: int
    script_serial_size: int
    outer_index: PackageIndex
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task: 实现 models/core.py 和 models/node_types.py 的 from_archive 委托</name>
  <files>src/uasset_read/models/core.py, src/uasset_read/models/node_types.py</files>
  <read_first>
    - src/uasset_read/models/core.py (当前 from_archive stub)
    - src/uasset_read/models/node_types.py (当前 from_archive stub)
    - src/uasset_read/serializers/graph.py (Plan 01 产出)
  </read_first>
  <action>
将 models/core.py 中 5 个数据类的 `raise NotImplementedError("Phase 31")` stubs 替换为实际委托调用：

**FEdGraphPinType.from_archive(archive, name_map, summary) -> Self:**
```python
from uasset_read.serializers.graph import read_ed_graph_pin_type
return read_ed_graph_pin_type(archive, name_map, summary)
```

**UEdGraphPin.from_archive(archive, name_map, summary, export_map, import_map) -> Self:**
```python
from uasset_read.serializers.graph import read_ue_graph_pin
return read_ue_graph_pin(archive, name_map, summary, export_map, import_map)
```

**UEdGraph.from_archive(archive, name_map, summary, export_map, import_map, graph_export, graph_class, graph_export_idx=0) -> Self:**
```python
from uasset_read.serializers.graph import read_ue_graph
return read_ue_graph(archive, name_map, summary, export_map, import_map, graph_export, graph_class, graph_export_idx)
```

**UEdGraphNode.from_archive(archive, name_map, summary, export_map, import_map, node_export) -> Self:**
```python
from uasset_read.serializers.graph import read_ue_graph_node
return read_ue_graph_node(archive, name_map, summary, export_map, import_map, node_export)
```

**FMemberReference.from_archive(archive, name_map, import_map, export_map) -> Self:**
```python
from uasset_read.serializers.graph import read_fmember_reference
return read_fmember_reference(archive, name_map, import_map, export_map)
```

**关键：** 所有 import 语句必须在 from_archive 方法体内（延迟导入），不能在模块顶部，避免循环导入。

将 models/node_types.py 中 5 个节点类型的 `raise NotImplementedError("Phase 31")` stubs 替换为：

**K2NodeCallFunction.from_archive(archive, name_map, import_map, export_map) -> Self:**
```python
from uasset_read.serializers.graph import read_k2node_call_function
return read_k2node_call_function(archive, name_map, import_map, export_map)
```

**K2NodeEvent.from_archive(archive, name_map, import_map, export_map) -> Self:**
```python
from uasset_read.serializers.graph import read_k2node_event
return read_k2node_event(archive, name_map, import_map, export_map)
```

**K2NodeKnot.from_archive(archive) -> Self:**
```python
from uasset_read.serializers.graph import read_k2node_knot
return read_k2node_knot(archive)
```

**EdGraphNodeComment.from_archive(archive) -> Self:**
```python
from uasset_read.serializers.graph import read_edgraph_node_comment
return read_edgraph_node_comment(archive)
```

**K2NodeEnhancedInputAction.from_archive(archive, name_map) -> Self:**
```python
from uasset_read.serializers.graph import read_k2node_enhanced_input
return read_k2node_enhanced_input(archive, name_map)
```

保留 TYPE_CHECKING guard 保护 archive 类型注解：
```python
if TYPE_CHECKING:
    from uasset_read.archive import FArchive
```
  </action>
  <verify>
    <automated>python -c "from uasset_read.models.core import FEdGraphPinType, UEdGraphPin, UEdGraphNode, UEdGraph, FMemberReference; print('models OK')"</automated>
    <automated>python -c "from uasset_read.models.node_types import K2NodeCallFunction, K2NodeEvent, K2NodeKnot, EdGraphNodeComment, K2NodeEnhancedInputAction; print('node_types OK')"</automated>
    <automated>grep -v "^#" src/uasset_read/models/core.py | grep -c "NotImplementedError"</automated>
    <automated>grep -v "^#" src/uasset_read/models/node_types.py | grep -c "NotImplementedError"</automated>
  </verify>
  <acceptance_criteria>
    - models/core.py 中 0 个 NotImplementedError（grep 返回 0）
    - models/node_types.py 中 0 个 NotImplementedError（grep 返回 0）
    - models/core.py 中 5 个 from_archive 方法体包含 `from uasset_read.serializers.graph import`
    - models/node_types.py 中 5 个 from_archive 方法体包含 `from uasset_read.serializers.graph import`
    - models/core.py 顶部 import 语句不包含 `from uasset_read.serializers.graph`（验证延迟导入）
    - `python -c "from uasset_read.models.core import UEdGraph; UEdGraph.from_archive"` 不抛出 ImportError
  </acceptance_criteria>
  <done>所有 10 个 from_archive stub 替换为实际委托调用，延迟导入避免循环导入</done>
</task>

<task type="auto">
  <name>Task: 更新顶层 __init__.py 导出图解析公共 API</name>
  <files>src/uasset_read/__init__.py</files>
  <read_first>
    - src/uasset_read/__init__.py (当前导出列表)
    - tests/test_graph_parsing.py (测试导入需求)
    - uasset_read.py L3095-3143 (extract_blueprint_graphs 签名)
  </read_first>
  <action>
更新 `src/uasset_read/__init__.py`，添加图解析模块的公共 API 导出。

需要导出的函数和常量（基于 tests/test_graph_parsing.py 的导入需求）：

```python
# 图解析模块（Phase 31）
# 注意：graph 模块尚未创建（Plan 03），此处先导出 serializers 中的函数
from .serializers.graph import (
    read_ue_graph,
    read_ue_graph_node,
    read_ue_graph_pin,
    read_ed_graph_pin_type,
    read_fmember_reference,
    create_node_from_archive,
)
# extract_blueprint_graphs 在 Plan 03 创建，此处用 TODO 占位，
# Plan 03 将添加 from .graph import extract_blueprint_graphs 等

# 图解析常量（已在 constants.py 中）
from .constants import (
    MAX_PINS_PER_NODE,
    MAX_NODES_PER_GRAPH,
    MAX_LINKEDTO_PER_PIN,
    START_EVENT_TYPES,
    CONTROL_FLOW_NODES,
    BRANCH_TYPE_MAP,
)
```

更新 __all__ 列表，添加：
- "read_ue_graph", "read_ue_graph_node", "read_ue_graph_pin"
- "read_ed_graph_pin_type", "read_fmember_reference"
- "create_node_from_archive"
- "K2NodeCallFunction", "K2NodeEvent", "K2NodeKnot", "EdGraphNodeComment", "K2NodeEnhancedInputAction"
- "read_k2node_call_function", "read_k2node_event", "read_k2node_knot", "read_edgraph_node_comment", "read_k2node_enhanced_input"
- "MAX_PINS_PER_NODE", "MAX_NODES_PER_GRAPH", "MAX_LINKEDTO_PER_PIN"
- "START_EVENT_TYPES", "CONTROL_FLOW_NODES", "BRANCH_TYPE_MAP"

注意：extract_blueprint_graphs, build_execution_flows, build_data_flows, build_connections_map 在 Plan 03 创建后添加到 __init__.py。
  </action>
  <verify>
    <automated>python -c "from uasset_read import read_ue_graph, read_ue_graph_node, read_ue_graph_pin, read_ed_graph_pin_type, read_fmember_reference; print('graph serializers OK')"</automated>
    <automated>python -c "from uasset_read import MAX_PINS_PER_NODE, MAX_NODES_PER_GRAPH, MAX_LINKEDTO_PER_PIN, START_EVENT_TYPES; print('graph constants OK')"</automated>
    <automated>python -c "from uasset_read import K2NodeCallFunction, K2NodeEvent, K2NodeKnot, EdGraphNodeComment, K2NodeEnhancedInputAction; print('node types OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from uasset_read import read_ue_graph"` 成功执行
    - `python -c "from uasset_read import MAX_PINS_PER_NODE, MAX_NODES_PER_GRAPH, MAX_LINKEDTO_PER_PIN, START_EVENT_TYPES"` 成功执行
    - `python -c "from uasset_read import K2NodeCallFunction, K2NodeEvent"` 成功执行
    - src/uasset_read/__init__.py 包含 `from .serializers.graph import` 导入行
    - __all__ 包含 "read_ue_graph", "read_ue_graph_node", "read_ue_graph_pin"
  </acceptance_criteria>
  <done>顶层 __init__.py 正确导出图序列化函数、节点类型类和相关常量，测试所需导入路径兼容</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models → serializers import | 延迟导入防止循环，错误时抛出 ImportError 而非 RuntimeError |
| __init__.py 扁平导出 | 大量导出项增加启动时间，使用懒加载可优化（但非安全必须） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08 | Denial of Service | models/core.py from_archive | mitigate | 延迟导入（方法体内 import）防止循环导入导致的启动时 ImportError，方法调用失败时抛出清晰错误而非崩溃 |
| T-31-09 | Information Disclosure | __init__.py __all__ | accept | 公共 API 导出列表不包含内部实现细节（如 _trace_execution_from_pin），仅暴露设计为公共的函数 |
</threat_model>

<verification>
- 所有 models from_archive 方法可调用（无 NotImplementedError）
- `from uasset_read import ...` 图解析相关导入无报错
- 无循环导入（启动 `python -c "import uasset_read"` 无错误）
- tests/test_graph_parsing.py 中所有 from uasset_read import ... 语句成功执行（不运行测试，仅检查导入）
</verification>

<success_criteria>
- models/core.py 中 5 个 from_archive stub 全部实现为委托调用
- models/node_types.py 中 5 个 from_archive stub 全部实现为委托调用
- 所有 import 在方法体内（延迟导入），无循环导入
- 顶层 __init__.py 导出图解析相关函数、类、常量
- `python -c "import uasset_read"` 无错误
- tests/test_graph_parsing.py 的 import 语句全部兼容
</success_criteria>

<output>
After completion, create `.planning/phases/31-graph-parsing/31-02-SUMMARY.md`
</output>
