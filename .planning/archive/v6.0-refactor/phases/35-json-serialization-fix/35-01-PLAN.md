---
phase: "35"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/formatters/json_formatter.py
autonomous: true
requirements:
  - REQ-35-01
user_setup: []

must_haves:
  truths:
    - "uasset-read file.uasset --json 输出完整合法 JSON，exit code 0"
    - "StructProperty 在 JSON 中显示为 {struct_type, fields} 而非 Python dataclass repr"
    - "MapProperty 在 JSON 中显示为 {key_type, value_type, entries} 而非 Python dataclass repr"
    - "EnumProperty/TextProperty/DelegateProperty/SetProperty 在 JSON 中均可正常输出"
  artifacts:
    - path: "src/uasset_read/formatters/json_formatter.py"
      provides: "JSON 序列化 helper 和 format_properties_list 修复"
      contains: "def serialize_property_value"
  key_links:
    - from: "format_properties_list"
      to: "serialize_property_value"
      via: "prop.value = serialize_property_value(prop.value)"
      pattern: "serialize_property_value\\(prop\\.value\\)"
---

<objective>
修复 `--json` 模式下 StructValue/MapValue/EnumValue/TextValue/DelegateValue/SetValue 序列化崩溃（P0 阻塞级 bug）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-json-serialization-fix/35-CONTEXT.md
@src/uasset_read/formatters/json_formatter.py
@src/uasset_read/models/properties.py
</context>

<interfaces>
<!-- 关键类型契约，来自 models/properties.py -->

```python
@dataclass
class PropertyValue:
    name: str
    type: str
    value: Any = None
    array_index: int = 0

@dataclass
class StructValue(AdvancedPropertyValue):
    property_type: str
    struct_type: str
    fields: Dict[str, Any] = field(default_factory=dict)

@dataclass
class MapValue(AdvancedPropertyValue):
    property_type: str
    key_type: str
    value_type: str
    entries: List[Dict[str, Any]] = field(default_factory=list)

@dataclass
class SetValue(AdvancedPropertyValue):
    property_type: str
    element_type: str
    elements: List[Any] = field(default_factory=list)

@dataclass
class EnumValue(AdvancedPropertyValue):
    property_type: str
    enum_type: str
    value_name: str

@dataclass
class TextValue(AdvancedPropertyValue):
    property_type: str
    namespace: str = ""
    key: str = ""
    source_string: str = ""

@dataclass
class DelegateValue(AdvancedPropertyValue):
    property_type: str
    object_ref: int
    function_name: str
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 添加 serialize_property_value helper 并修复 format_properties_list</name>
  <read_first>
    - src/uasset_read/formatters/json_formatter.py
    - src/uasset_read/models/properties.py
  </read_first>
  <files>src/uasset_read/formatters/json_formatter.py</files>
  <action>
在 `format_properties_list` 之前（约 L133 之前）添加 `serialize_property_value` helper 函数，将高级 dataclass 转换为 JSON 兼容 dict：

```python
def serialize_property_value(value: Any) -> Any:
    """将高级属性值 dataclass 转换为 JSON 兼容 dict。

    处理 StructValue, MapValue, SetValue, EnumValue, TextValue, DelegateValue。
    原始 dict/str/int/float/None 等 JSON 原生类型直接返回。
    """
    if value is None or isinstance(value, (str, int, float, bool, list, dict)):
        return value

    if hasattr(value, "struct_type"):  # StructValue
        return {
            "struct_type": value.struct_type,
            "fields": {k: serialize_property_value(v) for k, v in value.fields.items()}
        }
    if hasattr(value, "entries") and hasattr(value, "key_type"):  # MapValue
        return {
            "key_type": value.key_type,
            "value_type": value.value_type,
            "entries": value.entries
        }
    if hasattr(value, "elements") and hasattr(value, "element_type"):  # SetValue
        return {
            "element_type": value.element_type,
            "elements": value.elements
        }
    if hasattr(value, "value_name"):  # EnumValue
        return {
            "enum_type": value.enum_type,
            "value": value.value_name
        }
    if hasattr(value, "source_string"):  # TextValue
        return {
            "namespace": value.namespace,
            "key": value.key,
            "source_string": value.source_string
        }
    if hasattr(value, "function_name"):  # DelegateValue
        return {
            "object_ref": value.object_ref,
            "function_name": value.function_name
        }

    # Fallback: 尝试 asdict 或 str
    from dataclasses import asdict, is_dataclass
    if is_dataclass(value):
        return asdict(value)
    return str(value)
```

**重要**: 添加递归深度保护以防止循环引用导致无限递归。修改 `serialize_property_value` 签名为 `serialize_property_value(value: Any, depth: int = 0, max_depth: int = 10) -> Any`，在函数开头添加：
```python
    if depth > max_depth:
        return "[deep nesting truncated]"
```
并在递归调用时传入 `depth + 1`（嵌套 dict 的 fields 序列化处）。

修改 `format_properties_list` 函数中 L151 的 `"value": prop.value` 行，改为 `"value": serialize_property_value(prop.value)`。

添加必要的 import: `from uasset_read.models.properties import StructValue, MapValue, SetValue, EnumValue, TextValue, DelegateValue`（在 TYPE_CHECKING 块外，因为需要在运行时进行 isinstance 检查）。
  </action>
  <verify>
    <automated>python -c "from uasset_read.formatters.json_formatter import serialize_property_value; from uasset_read.models.properties import StructValue; s = StructValue(property_type='StructProperty', struct_type='Vector', fields={'X': 1.0, 'Y': 2.0, 'Z': 3.0}); r = serialize_property_value(s); assert r == {'struct_type': 'Vector', 'fields': {'X': 1.0, 'Y': 2.0, 'Z': 3.0}}, f'Got: {r}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - serialize_property_value 函数存在，处理 StructValue/MapValue/SetValue/EnumValue/TextValue/DelegateValue 六种类型
    - serialize_property_value 有 depth/max_depth 参数，超过 max_depth=10 时返回 "[deep nesting truncated]"
    - format_properties_list 中调用 serialize_property_value(prop.value) 而非直接使用 prop.value
    - 递归嵌套 StructValue fields 也被序列化（带深度保护）
    - dict/str/int/float/bool/list/None 等原生类型直接返回不变
  </acceptance_criteria>
  <done>serialize_property_value helper 存在并被 format_properties_list 正确调用；对 6 种高级类型的单元测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → json.dumps | 用户传入的 .uasset 文件可能包含恶意构造的 property 数据，serialize_property_value 需要避免无限递归 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Integrity | serialize_property_value | mitigate | 对嵌套 dict 使用递归深度保护，默认最大深度 10，超过则返回 "[deep nesting truncated]" |
| T-35-02 | Availability | format_properties_list | accept | JSON 输出为只读格式化，不涉及系统调用或文件写入 |
</threat_model>

<verification>
- 运行 `uasset-read BP_FirstPersonCharacter.uasset --json | python -m json.tool > /dev/null` 返回 exit code 0
- JSON 中包含 StructProperty 的 struct_type + fields，非 Python dataclass repr
- JSON 中包含 MapProperty 的 key_type + value_type + entries
</verification>

<success_criteria>
- `--json` 模式对 BP_FirstPersonCharacter.uasset 输出完整 JSON，无异常
- `python -c "import json; json.loads(open('output.json').read())"` 验证为合法 JSON
- 所有现有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/35-json-serialization-fix/35-01-SUMMARY.md`
</output>
