---
phase: 20-整合输出
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [uasset_read.py]
autonomous: true
requirements: [OUT-01, OUT-02]

must_haves:
  truths:
    - "用户可以在JSON中看到每个节点的node_name（K2Node_CallFunction_1193格式）"
    - "用户可以在JSON中看到每个节点的node_type字段（而非class_name）"
    - "用户可以在JSON中看到每个节点的position结构（而非node_pos_x/node_pos_y）"
    - "CallFunction节点用户可以在JSON顶层看到function_reference字段"
    - "Event节点用户可以在JSON顶层看到event_reference字段"
    - "用户可以在JSON中看到每个Graph的graph_type字段（event/uber而非EdGraph/UberEdGraph）"
  artifacts:
    - path: "uasset_read.py"
      function: "format_node_dict()"
      purpose: "格式化单个节点为OUT-01规范结构"
      min_lines: 30
    - path: "uasset_read.py"
      function: "format_graphs_json()"
      modification: "使用format_node_dict + graph_type映射"
      contains: "graph_type"
  key_links:
    - from: "format_node_dict()"
      to: "_derive_node_name()"
      via: "调用派生节点名"
      pattern: "_derive_node_name\\(node, idx\\)"
    - from: "format_graphs_json()"
      to: "format_node_dict()"
      via: "替换asdict(node)调用"
      pattern: "format_node_dict\\(node, idx\\)"
    - from: "format_graphs_json()"
      to: "GRAPH_TYPE_MAP"
      via: "graph_class语义化映射"
      pattern: "GRAPH_TYPE_MAP"
---

<objective>
实现节点和Graph输出结构重组，遵循OUT-01和OUT-02规范。

Purpose: 规范化字段命名，提升常用字段到顶层，语义化Graph类型。
Output: format_node_dict()函数、修改后的format_graphs_json()。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-整合输出/20-CONTEXT.md

## 现有代码结构

**UEdGraphNode dataclass (第1305-1321行):**
```python
class UEdGraphNode:
    node_guid: str                       # FGuid hex
    node_pos_x: int = 0                  # 需改为 position: {x, y}
    node_pos_y: int = 0                  # 需改为 position: {x, y}
    node_comment: str = ""
    pins: List["UEdGraphPin"] = field(default_factory=list)
    class_name: str = ""                 # 需改为 node_type
    node_data: Optional[any] = None      # function_reference/event_reference源
```

**K2NodeCallFunction (第1360-1372行):**
```python
@dataclass
class K2NodeCallFunction:
    function_reference: FMemberReference
    b_defaults_to_pure: bool = False
```

**K2NodeEvent (第1374-1386行):**
```python
@dataclass
class K2NodeEvent:
    event_reference: FMemberReference
    b_override_function: bool = False
```

**FMemberReference (第1341-1354行):**
```python
@dataclass
class FMemberReference:
    member_parent: Optional[str] = None
    member_name: str = ""
    member_guid: Optional[str] = None
    b_self_context: bool = False
```

**format_graphs_json (第5144-5193行):**
```python
def format_graphs_json(graphs: List[UEdGraph]) -> List[Dict]:
    # 当前使用 asdict(node) 输出原始字段
    graph_dict = {
        "graph_name": graph.graph_name,
        "graph_class": graph.graph_class,  # 需改为 graph_type
        "nodes": [asdict(node) for node in graph.nodes],  # 需用 format_node_dict
        ...
    }
```

**_derive_node_name (第5018-5032行):**
```python
def _derive_node_name(node: UEdGraphNode, idx: int) -> str:
    return f"{node.class_name}_{idx}"
```

</context>

<interfaces>

## Phase 18/19 Output Format（需兼容）

**UEdGraphPin输出（Phase 18规范）：**
```python
# Pin dict包含：
{
    "pin_id": "13FD260E4EE18FD0AA5F7085F9B509D6",
    "pin_name": "execute",
    "direction": 0,  # 0=input, 1=output
    "pin_type": {...},
    "default_value": "",
    "linked_to": [...],
    "linked_to_raw": [...],  # Phase 18 dict格式
    ...
}
```

**build_connections_map/build_execution_flows/build_data_flows输出（Phase 19规范）：**
- 已正确实现，无需修改

</interfaces>

<tasks>

<task type="auto">
<name>Task 1: 创建format_node_dict()节点格式化函数</name>
<files>uasset_read.py</files>
<read_first>
uasset_read.py 第1305-1321行 (UEdGraphNode dataclass)
uasset_read.py 第1360-1372行 (K2NodeCallFunction)
uasset_read.py 第1374-1386行 (K2NodeEvent)
uasset_read.py 第1341-1354行 (FMemberReference)
uasset_read.py 第5018-5032行 (_derive_node_name函数)
uasset_read.py 第5144-5193行 (format_graphs_json当前实现)
</read_first>
<behavior>
- format_node_dict(node, idx)返回Dict符合OUT-01规范
- 输出字段：node_name, node_type, node_guid, position:{x,y}, node_comment, pins, node_data（可选）
- CallFunction类型：function_reference提升到顶层
- Event类型：event_reference提升到顶层
- 其他类型：node_data保持嵌套或省略
</behavior>
<action>
在 format_graphs_json 函数之前（约第5140行位置）创建新函数 format_node_dict(node: UEdGraphNode, idx: int) -> Dict：

1. **派生node_name（D-20-01）：**
   - 调用 `_derive_node_name(node, idx)` 获取节点名
   - 返回格式如 `"K2Node_CallFunction_1193"`

2. **字段名规范化（D-20-02）：**
   - `node_type = node.class_name`（使用node_type而非class_name）
   - `position = {"x": node.node_pos_x, "y": node.node_pos_y}`（嵌套结构）

3. **嵌套结构展开（D-20-03）：**
   - 检查 node.node_data 类型：
     - 若为 K2NodeCallFunction：提取 `function_reference` 到顶层
       ```python
       if isinstance(node.node_data, K2NodeCallFunction):
           result["function_reference"] = {
               "member_name": node.node_data.function_reference.member_name,
               "member_parent": node.node_data.function_reference.member_parent,
               "self_context": node.node_data.function_reference.b_self_context
           }
       ```
     - 若为 K2NodeEvent：提取 `event_reference` 到顶层
       ```python
       if isinstance(node.node_data, K2NodeEvent):
           result["event_reference"] = {
               "member_name": node.node_data.event_reference.member_name,
               "member_parent": node.node_data.event_reference.member_parent,
               "member_guid": node.node_data.event_reference.member_guid
           }
       ```
     - 其他类型：保持 node_data 嵌套（若有）

4. **完整输出结构：**
   ```python
   result = {
       "node_name": _derive_node_name(node, idx),
       "node_type": node.class_name,
       "node_guid": node.node_guid,
       "position": {"x": node.node_pos_x, "y": node.node_pos_y},
       "node_comment": node.node_comment,
       "pins": [asdict(pin) for pin in node.pins]  # Pin格式保持Phase 18规范
   }
   # 添加 function_reference/event_reference（按类型）
   # 若有其他node_data，添加 "node_data": {...}
   ```

5. **添加函数docstring：**
   ```python
   """
   格式化单个节点为 OUT-01 规范 JSON 结构。

   Per D-20-01: node_name 使用 _derive_node_name() 派生
   Per D-20-02: 字段名规范化（node_type, position:{x,y})
   Per D-20-03: function_reference/event_reference 提升到顶层

   Args:
       node: UEdGraphNode 节点对象
       idx: 节点在图中的索引

   Returns:
       Dict: OUT-01 规范节点结构
   """
   ```

**禁止：** 不修改UEdGraphNode dataclass定义（保持原始字段），仅在此函数中转换输出格式。
</action>
<verify>
<automated>python -c "from uasset_read import format_node_dict; print('format_node_dict imported')" 2>&1</automated>
</verify>
<acceptance_criteria>
- format_node_dict函数存在，可导入
- 函数签名：format_node_dict(node: UEdGraphNode, idx: int) -> Dict
- docstring包含D-20-01/02/03引用
- 输出包含node_name字段（值为class_name_idx格式）
- 输出包含node_type字段（而非class_name）
- 输出包含position结构（而非node_pos_x/node_pos_y）
- CallFunction节点输出包含顶层function_reference
- Event节点输出包含顶层event_reference
</acceptance_criteria>
<done>format_node_dict函数实现完成，遵循OUT-01规范。</done>
</task>

<task type="auto">
<name>Task 2: 修改format_graphs_json()使用新节点格式和graph_type映射</name>
<files>uasset_read.py</files>
<read_first>
uasset_read.py 第5144-5193行 (format_graphs_json当前实现)
uasset_read.py 第1330-1338行 (UEdGraph dataclass - graph_class字段)
Task 1创建的format_node_dict函数
</read_first>
<behavior>
- format_graphs_json输出graph_type而非graph_class
- nodes数组使用format_node_dict而非asdict
- graph_type映射：EdGraph→event, UberEdGraph→uber
</behavior>
<action>
修改 format_graphs_json 函数（第5144-5193行）：

1. **在函数前定义GRAPH_TYPE_MAP常量（D-20-07）：**
   ```python
   # D-20-07: Graph类型语义化映射
   GRAPH_TYPE_MAP = {
       "EdGraph": "event",
       "UberEdGraph": "uber",
   }
   ```

2. **修改nodes输出（使用format_node_dict）：**
   ```python
   # 原代码：
   "nodes": [asdict(node) for node in graph.nodes]
   # 改为：
   "nodes": [format_node_dict(node, idx) for idx, node in enumerate(graph.nodes)]
   ```

3. **修改graph_class为graph_type：**
   ```python
   # 原代码：
   "graph_class": graph.graph_class,
   # 改为：
   "graph_type": GRAPH_TYPE_MAP.get(graph.graph_class, graph.graph_class),
   ```

4. **更新函数docstring：**
   ```python
   """
   格式化蓝图图数据为 JSON 输出（OUT-02）。

   Per D-20-07: graph_type 语义化映射（EdGraph→event, UberEdGraph→uber）
   Per OUT-01: nodes 使用 format_node_dict 格式化
   ...
   """
   ```

5. **保持其他字段不变：**
   - graph_name, connections, execution_flows, data_flows, warnings, graph_guid, schema
   - 这些字段已在Phase 8/19正确实现

**禁止：** 不修改connections/execution_flows/data_flows构建逻辑（Phase 19已实现）。
</action>
<verify>
<automated>python -m pytest tests/test_output_formatting.py -x -v --tb=short 2>&1 | head -50</automated>
</verify>
<acceptance_criteria>
- format_graphs_json输出包含graph_type字段
- graph_type值为event或uber（对应EdGraph/UberEdGraph）
- graph_class字段不再出现在输出中
- nodes数组元素包含node_name字段
- nodes数组元素包含node_type字段
- nodes数组元素包含position结构（含x和y）
- 测试全部通过
</acceptance_criteria>
<done>format_graphs_json修改完成，遵循OUT-02规范，兼容OUT-01节点格式。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 解析数据→JSON输出 | 数据转换边界，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | format_node_dict | accept | 仅数据转换，无持久化存储 |
| T-20-02 | Information Disclosure | JSON输出 | accept | 输出为本地文件，无敏感数据 |

**说明：** 本阶段仅重组输出格式，无安全边界变化。
</threat_model>

<verification>
**自动化验证：**
```bash
# 验证format_node_dict存在
python -c "from uasset_read import format_node_dict; print('OK')"

# 验证GRAPH_TYPE_MAP存在
python -c "from uasset_read import GRAPH_TYPE_MAP; print(GRAPH_TYPE_MAP)"

# 运行现有测试确保兼容
python -m pytest tests/test_output_formatting.py -v
```

**手动验证：**
- 解析BP_FirstPersonCharacter.uasset，检查节点输出结构
- 验证node_name格式为K2Node_CallFunction_1193样式
- 验证position为{x, y}结构
</verification>

<success_criteria>
- format_node_dict函数创建完成，可导入
- format_graphs_json修改完成，输出graph_type字段
- nodes数组使用新格式（node_name, node_type, position）
- CallFunction节点输出包含function_reference
- Event节点输出包含event_reference
- 所有现有测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/20-整合输出/20-01-SUMMARY.md`
</output>