---
phase: 20-整合输出
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified: [uasset_read.py]
autonomous: true
requirements: [OUT-03]

must_haves:
  truths:
    - "用户可以在JSON顶层看到单一blueprint对象"
    - "用户可以在blueprint对象内看到graphs数组"
    - "用户可以在blueprint对象内看到blueprint_name字段"
    - "用户可以在blueprint对象内看到parent_class字段"
    - "用户可以在blueprint对象内看到variables数组"
    - "用户可以看到output_version为4.0"
  artifacts:
    - path: "uasset_read.py"
      function: "format_json_full()"
      modification: "重组为blueprint对象结构"
      contains: "blueprint"
    - path: "uasset_read.py"
      field: "output_version"
      value: "4.0"
      pattern: '"output_version": "4.0"'
  key_links:
    - from: "format_json_full()"
      to: "format_graphs_json()"
      via: "blueprint.graphs字段调用"
      pattern: '"graphs": format_graphs_json'
    - from: "format_json_full()"
      to: "format_blueprint_dict()"
      via: "blueprint对象构建"
      pattern: "format_blueprint_dict"
    - from: "format_json_full()"
      to: "output_version"
      via: "版本标识"
      pattern: '"output_version": "4.0"'
---

<objective>
重组JSON输出为单一蓝图对象结构，升级版本标识。

Purpose: 遵循OUT-03规范，将graphs移入blueprint对象内部，升级output_version。
Output: 修改后的format_json_full()、output_version升级到4.0。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-整合输出/20-CONTEXT.md
@.planning/phases/20-整合输出/20-01-SUMMARY.md

## 现有代码结构

**format_json_full (第5639-5688行):**
```python
def format_json_full(result: ParseResult, include_schema: bool = False) -> Dict:
    output = {
        "status": asdict(build_status_info(result)),
        "output_version": "3.0",  # D-20-05: 需升级到 "4.0"
        "summary": summary_dict,
        "exports": format_exports_list(result),
        "blueprint_metadata": format_blueprint_dict(result.blueprint) if result.blueprint else None,
        "graphs": format_graphs_json(result.graphs),  # D-20-04: 需移入blueprint
        "graphs_summary": build_graphs_summary(result.graphs),
        "imports": result.imports,
        "soft_references": result.soft_references,
        "circular_deps": result.circular_deps,
        "errors": result.errors
    }
```

**format_blueprint_dict (第6110-6142行):**
```python
def format_blueprint_dict(blueprint: BlueprintMetadata) -> Dict:
    variables_list = []
    for v in blueprint.variables:
        var_dict = {
            "name": v.var_name,
            "type": {...},
            "category": v.category,
            "property_flags": v.property_flags,
            "default_value": v.default_value,
            "friendly_name": v.friendly_name
        }
    return {
        "parent_class": blueprint.parent_class,
        "variables": variables_list,
        "detection_warning": blueprint.detection_warning
    }
```

**BlueprintMetadata (需确认):**
- blueprint_name字段需添加（或从summary.package_name提取）
- D-20-06: variables扩展信息已在Phase 12实现

</context>

<interfaces>

## 依赖Plan 20-01的输出格式

**format_graphs_json输出（Plan 20-01实现）：**
```python
[
    {
        "graph_name": "EventGraph",
        "graph_type": "event",  # D-20-07语义化映射
        "nodes": [
            {
                "node_name": "K2Node_CallFunction_1193",
                "node_type": "CallFunction",
                "node_guid": "...",
                "position": {"x": 3136, "y": -1040},
                "function_reference": {"member_name": "Jump", ...},
                "pins": [...]
            }
        ],
        "connections": [...],
        "execution_flows": [...],
        "data_flows": [...]
    }
]
```

</interfaces>

<tasks>

<task type="auto">
<name>Task 1: 修改format_json_full()为单一蓝图对象结构</name>
<files>uasset_read.py</files>
<read_first>
uasset_read.py 第5639-5688行 (format_json_full当前实现)
uasset_read.py 第6110-6142行 (format_blueprint_dict当前实现)
uasset_read.py 第665-680行 (ParseResult - blueprint字段)
Plan 20-01的format_graphs_json输出格式
</read_first>
<behavior>
- format_json_full输出单一blueprint对象（含graphs）
- 移除顶层graphs字段
- output_version升级到4.0
- blueprint_name从summary.package_name或导出名提取
</behavior>
<action>
修改 format_json_full 函数（第5639-5688行）：

1. **升级output_version（D-20-05）：**
   ```python
   # 原代码：
   "output_version": "3.0",
   # 改为：
   "output_version": "4.0",  # D-20-05: 反映输出结构重大变化
   ```

2. **创建blueprint对象结构（D-20-04）：**
   ```python
   # 构建blueprint对象（包含graphs）
   blueprint_obj = None
   if result.blueprint:
       blueprint_obj = format_blueprint_dict(result.blueprint)
       blueprint_obj["graphs"] = format_graphs_json(result.graphs)  # D-20-04: graphs移入blueprint
       # D-20-06: blueprint_name添加
       blueprint_obj["blueprint_name"] = result.summary.package_name if result.summary else None
   ```

3. **修改output结构：**
   ```python
   output = {
       "status": asdict(build_status_info(result)),
       "output_version": "4.0",
       "summary": summary_dict,
       "exports": format_exports_list(result),
       "blueprint": blueprint_obj,  # D-20-04: 单一blueprint对象
       # 移除顶层 graphs（已移入blueprint）
       # 移除 blueprint_metadata（改用blueprint）
       "graphs_summary": build_graphs_summary(result.graphs),  # 保持顶层摘要
       "imports": result.imports,
       "soft_references": result.soft_references,
       "circular_deps": result.circular_deps,
       "errors": result.errors
   }
   ```

4. **更新函数docstring：**
   ```python
   """
   Format full JSON output with complete asset data (OUT-03).

   Per D-20-04: 单一blueprint对象结构（graphs移入blueprint内部）
   Per D-20-05: output_version升级到 "4.0"
   Per D-20-06: blueprint_name从package_name提取

   Args:
       result: ParseResult from parse_uasset()
       include_schema: bool, whether to include _schema field

   Returns:
       Dict with keys: status, output_version, summary, exports, blueprint, graphs_summary, imports, soft_references, circular_deps, errors
   """
   ```

**禁止：**
- 不修改graphs_summary构建（保持顶层摘要功能）
- 不修改非蓝图资产的输出逻辑（blueprint=None时正常处理）

**注意：**
- 若blueprint为None，blueprint字段输出为null
- graphs_summary保持顶层位置（D-14-04规范）
</action>
<verify>
<automated>python -c "
from uasset_read import parse_uasset, format_json_full
r = parse_uasset('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/Blueprints/BP_FirstPersonCharacter.uasset')
j = format_json_full(r)
assert 'blueprint' in j, 'blueprint field missing'
assert 'graphs' not in j, 'graphs should be inside blueprint'
print('OK: blueprint structure')
" 2>&1</automated>
</verify>
<acceptance_criteria>
- format_json_full输出包含blueprint字段（而非blueprint_metadata）
- blueprint对象包含graphs数组
- blueprint对象包含blueprint_name字段
- blueprint对象包含parent_class字段
- blueprint对象包含variables字段
- 顶层不再有graphs字段（已移入blueprint）
- output_version值为"4.0"
</acceptance_criteria>
<done>format_json_full重组完成，遵循OUT-03规范。</done>
</task>

<task type="auto">
<name>Task 2: 更新format_blueprint_dict()添加blueprint_name</name>
<files>uasset_read.py</files>
<read_first>
uasset_read.py 第6110-6142行 (format_blueprint_dict当前实现)
Task 1修改后的format_json_full调用方式
</read_first>
<behavior>
- format_blueprint_dict返回blueprint_name字段
- blueprint_name参数传入函数
- 其他字段保持不变
</behavior>
<action>
修改 format_blueprint_dict 函数（第6110-6142行）：

1. **添加blueprint_name参数：**
   ```python
   def format_blueprint_dict(blueprint: BlueprintMetadata, blueprint_name: str = None) -> Dict:
       """
       Format BlueprintMetadata for JSON output (D-04, D-20-06).

       Per D-20-06: blueprint_name从package_name或导出名提取

       Args:
           blueprint: BlueprintMetadata object
           blueprint_name: 资产名称（可选）

       Returns:
           Dict with keys: blueprint_name, parent_class, variables, detection_warning
       """
   ```

2. **添加blueprint_name到输出：**
   ```python
   return {
       "blueprint_name": blueprint_name,  # D-20-06
       "parent_class": blueprint.parent_class,
       "variables": variables_list,
       "detection_warning": blueprint.detection_warning
   }
   ```

3. **更新Task 1中的调用：**
   ```python
   # Task 1中需相应修改：
   blueprint_obj = format_blueprint_dict(
       result.blueprint,
       blueprint_name=result.summary.package_name if result.summary else None
   )
   ```

**禁止：** 不修改variables_list构建逻辑（Phase 12已正确实现）。
</action>
<verify>
<automated>python -c "
from uasset_read import format_blueprint_dict, BlueprintMetadata
b = BlueprintMetadata(parent_class='TestClass', variables=[], detection_warning=None)
d = format_blueprint_dict(b, blueprint_name='TestBP')
assert 'blueprint_name' in d, 'blueprint_name missing'
assert d['blueprint_name'] == 'TestBP', 'blueprint_name wrong value'
print('OK: blueprint_name field')
" 2>&1</automated>
</verify>
<acceptance_criteria>
- format_blueprint_dict包含blueprint_name参数
- 输出包含blueprint_name字段
- blueprint_name值为传入参数
- variables字段输出保持Phase 12规范
</acceptance_criteria>
<done>format_blueprint_dict修改完成，支持blueprint_name字段。</done>
</task>

<task type="auto">
<name>Task 3: 更新测试和验证完整输出结构</name>
<files>tests/test_output_formatting.py</files>
<read_first>
tests/test_output_formatting.py (现有测试结构)
uasset_read.py Task 1/2修改后的输出格式
</read_first>
<behavior>
- 测试验证output_version为4.0
- 测试验证blueprint对象结构
- 测试验证graphs在blueprint内部
- 现有测试兼容新格式
</behavior>
<action>
更新 tests/test_output_formatting.py：

1. **添加或更新output_version测试：**
   ```python
   def test_output_version_4_0():
       """验证output_version为4.0（D-20-05）"""
       r = parse_uasset(BP_TEST_FILE)
       j = format_json_full(r)
       assert j["output_version"] == "4.0"
   ```

2. **添加blueprint结构测试：**
   ```python
   def test_blueprint_object_structure():
       """验证blueprint对象结构（D-20-04, OUT-03）"""
       r = parse_uasset(BP_TEST_FILE)
       j = format_json_full(r)
       assert "blueprint" in j
       assert "blueprint_metadata" not in j  # 已改名
       bp = j["blueprint"]
       assert "blueprint_name" in bp
       assert "parent_class" in bp
       assert "graphs" in bp  # graphs在blueprint内部
       assert "variables" in bp
   ```

3. **添加graphs不在顶层测试：**
   ```python
   def test_graphs_inside_blueprint():
       """验证graphs移入blueprint对象（D-20-04）"""
       r = parse_uasset(BP_TEST_FILE)
       j = format_json_full(r)
       assert "graphs" not in j  # 顶层无graphs
       assert "graphs" in j["blueprint"]  # graphs在blueprint内
   ```

**禁止：** 不破坏现有测试，确保兼容性。
</action>
<verify>
<automated>python -m pytest tests/test_output_formatting.py -v --tb=short 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria>
- test_output_version_4_0测试通过
- test_blueprint_object_structure测试通过
- test_graphs_inside_blueprint测试通过
- 所有现有测试通过（无破坏）
</acceptance_criteria>
<done>测试更新完成，验证OUT-03规范实现。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 解析数据→JSON输出 | 数据转换边界，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | format_json_full | accept | 仅数据重组，无安全边界变化 |
| T-20-04 | Information Disclosure | JSON输出 | accept | 本地输出，无敏感数据 |

**说明：** 本阶段仅重组JSON结构，无安全边界变化。
</threat_model>

<verification>
**自动化验证：**
```bash
# 验证output_version升级
python -c "from uasset_read import format_json_full; import json; r=parse_uasset('...'); j=format_json_full(r); print(j['output_version'])"

# 验证blueprint结构
python -m pytest tests/test_output_formatting.py -v
```

**手动验证：**
- 解析BP_FirstPersonCharacter.uasset
- 检查JSON结构：blueprint对象、graphs位置、output_version
</verification>

<success_criteria>
- output_version为"4.0"
- blueprint对象存在且包含graphs
- blueprint_name字段存在
- graphs不在顶层（在blueprint内部）
- 所有测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/20-整合输出/20-02-SUMMARY.md`
</output>