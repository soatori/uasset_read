---
phase: 21-验证测试
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_phase21_verification.py]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
user_setup: []
must_haves:
  truths:
    - "测试可以验证JSON中的节点数量与导出表一致"
    - "测试可以验证Jump执行流程（IA_Jump → Jump → StopJumping）正确构建"
    - "测试可以验证数据流（ActionValue_X/Y → 参数）正确解析"
    - "测试可以验证节点属性（FunctionReference.MemberName、NodeGuid）正确提取"
  artifacts:
    - path: "tests/test_phase21_verification.py"
      provides: "Phase 21 验证测试"
      contains: "test_"
      min_lines: 150
  key_links:
    - from: "tests/test_phase21_verification.py"
      to: "BP_FirstPersonCharacter.uasset"
      via: "parse_uasset"
      pattern: "parse_uasset.*FIRST_PERSON"
---

<objective>
创建 Phase 21 验证测试文件，验证 JSON 输出与 UE 编辑器信息一致。

Purpose: 确保 v4.0 节点属性解析的正确性，使用真实资产（BP_FirstPersonCharacter.uasset）进行集成测试
Output: tests/test_phase21_verification.py，包含 TEST-01~04 四个测试类
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From uasset_read.py exports:
```python
parse_uasset(path: str) -> ParseResult
format_json_full(result: ParseResult) -> Dict

# ParseResult fields
result.graphs: List[UEdGraph]
result.export_map: List[ObjectExport]
result.blueprint: BlueprintMetadata
result.is_success: bool
result.name_map: List[str]
```

From tests/test_exportmap_properties.py pattern:
```python
FIRST_PERSON_CHARACTER_PATH = "E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset"

def get_test_asset_path():
    if os.path.exists(FIRST_PERSON_CHARACTER_PATH):
        return FIRST_PERSON_CHARACTER_PATH
    return None

@pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
def test_...():
    result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
    ...
```

From C++ source (FirstPersonCCharacter.cpp) expected behavior:
```cpp
// Jump execution flow
EnhancedInputComponent->BindAction(JumpAction, ETriggerEvent::Started, this, &AFirstPersonCCharacter::DoJumpStart);
EnhancedInputComponent->BindAction(JumpAction, ETriggerEvent::Completed, this, &AFirstPersonCCharacter::DoJumpEnd);

void AFirstPersonCCharacter::DoJumpStart() { Jump(); }
void AFirstPersonCCharacter::DoJumpEnd() { StopJumping(); }

// Move data flow
void AFirstPersonCCharacter::MoveInput(const FInputActionValue& Value) {
    FVector2D MovementVector = Value.Get<FVector2D>();
    DoMove(MovementVector.X, MovementVector.Y);  // X→Right, Y→Forward
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建测试文件结构和资产路径常量</name>
  <files>tests/test_phase21_verification.py</files>
  <read_first>
    - tests/test_phase21_verification.py (不存在，创建新文件)
    - tests/test_exportmap_properties.py (参考资产路径和测试模式)
    - E:/Develop/lib/UnrealEngine/Samples/FirstPersonC/Source/FirstPersonC/FirstPersonCCharacter.cpp (expected 数据参考)
  </read_first>
  <action>
    创建 tests/test_phase21_verification.py 文件，包含：

    1. 文件头注释（Phase 21 验证测试，TEST-01~04）
    2. 导入必要模块（pytest, os, from uasset_read import parse_uasset, format_json_full）
    3. 定义资产路径常量：
       ```python
       FIRST_PERSON_CHARACTER_PATH = "E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset"
       ```
    4. 定义 get_test_asset_path() 辅助函数
    5. 定义 pytest skipif 标记（当资产不存在时跳过）

    Per D-21-03: 测试资产路径已锁定
    Per D-21-07: 新建专用测试文件
    Per D-21-09: C++源码作为 expected 数据参考
  </action>
  <verify>
    <automated>python -c "import tests.test_phase21_verification; print('OK')" 2>&1 || echo "File check"</automated>
    grep -c "FIRST_PERSON_CHARACTER_PATH" tests/test_phase21_verification.py
  </verify>
  <done>
    文件存在且可导入
    FIRST_PERSON_CHARACTER_PATH 常量已定义
    get_test_asset_path() 函数已定义
  </done>
</task>

<task type="auto">
  <name>Task 2: TEST-01 节点数量验证测试</name>
  <files>tests/test_phase21_verification.py</files>
  <read_first>
    - tests/test_phase21_verification.py (上一任务创建的文件)
    - uasset_read.py (理解 ParseResult 结构)
  </read_first>
  <behavior>
    - Test 1: 解析真实资产，验证 graphs 存在且非空
    - Test 2: 验证节点数量与导出表中 K2Node 类条目数量一致
    - Test 3: 验证 export_map 包含蓝图节点导出（EventGraph、K2Node_*）
  </behavior>
  <action>
    在 test_phase21_verification.py 添加 TestNodeCount 类，实现 TEST-01：

    ```python
    class TestNodeCount:
        """TEST-01: 节点数量匹配验证"""

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_graphs_exist(self):
            """验证解析结果包含 graphs 且非空"""
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            assert result.is_success, f"解析失败: {result.errors}"
            assert result.graphs is not None, "graphs 为 None"
            assert len(result.graphs) > 0, "graphs 为空列表"

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_node_count_matches_exports(self):
            """验证节点数量与导出表中 K2Node 条目数量匹配"""
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            # 从 export_map 统计 K2Node 类导出（object_name 以 K2Node 开头）
            k2node_exports = [e for e in result.export_map
                              if e.object_name and e.object_name.startswith("K2Node")]
            export_node_count = len(k2node_exports)

            # 从 graphs 统计解析出的节点
            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                parsed_node_count = len(json_output["blueprint"]["graphs"][0].get("nodes", []))
            else:
                parsed_node_count = 0

            # Per D-21-05: 精确匹配标准
            assert parsed_node_count == export_node_count, \
                f"节点数量不匹配: 解析={parsed_node_count}, 导出表={export_node_count}"
    ```

    Per TEST-01: JSON中节点数与导出表一致
    Per D-21-05: 精确匹配标准
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase21_verification.py::TestNodeCount -v 2>&1 | grep -E "(PASSED|FAILED|SKIPPED|test_)"</automated>
    grep -c "class TestNodeCount" tests/test_phase21_verification.py
    grep -c "test_node_count" tests/test_phase21_verification.py
  </verify>
  <done>
    TestNodeCount 类已定义
    test_graphs_exist 和 test_node_count_matches_exports 方法已实现
    测试可运行（PASS、FAIL 或 SKIP）
  </done>
</task>

<task type="auto">
  <name>Task 3: TEST-02 执行流程验证测试</name>
  <files>tests/test_phase21_verification.py</files>
  <read_first>
    - tests/test_phase21_verification.py (上一任务添加的文件)
    - E:/Develop/lib/UnrealEngine/Samples/FirstPersonC/Source/FirstPersonC/FirstPersonCCharacter.cpp (expected 执行流程)
  </read_first>
  <behavior>
    - Test 1: 验证 IA_Jump(Started) → Jump 执行流程存在
    - Test 2: 验证 IA_Jump(Completed) → StopJumping 执行流程存在
    - Test 3: 验证 execution_flows 包含 EnhancedInputAction 起点节点
  </behavior>
  <action>
    在 test_phase21_verification.py 添加 TestExecutionFlow 类，实现 TEST-02：

    ```python
    class TestExecutionFlow:
        """TEST-02: 执行流程验证（IA_Jump → Jump → StopJumping）"""

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_execution_flows_exist(self):
            """验证 execution_flows 存在"""
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                graph = json_output["blueprint"]["graphs"][0]
                assert "execution_flows" in graph, "execution_flows 字段不存在"
                assert len(graph["execution_flows"]) > 0, "execution_flows 为空"
            else:
                pytest.fail("blueprint.graphs 不存在，无法验证执行流程")

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_jump_started_flow(self):
            """
            验证 IA_Jump(Started) → Jump 执行流程。

            Per D-21-09 C++对照:
            EnhancedInputComponent->BindAction(JumpAction, ETriggerEvent::Started, this, &DoJumpStart)
            DoJumpStart() { Jump(); }
            """
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            # 查找包含 IA_Jump Started 事件的 execution_flow
            # Expected: entry 节点包含 "IA_Jump"，chain 包含调用 Jump 的节点
            found_jump_flow = False

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                for flow in json_output["blueprint"]["graphs"][0].get("execution_flows", []):
                    entry = flow.get("entry", "")
                    # IA_Jump 的 EnhancedInputAction 节点
                    if "IA_Jump" in entry and "Started" in entry:
                        chain = flow.get("chain", [])
                        # 验证 chain 包含调用 Jump 函数的节点
                        for node_name in chain:
                            if "CallFunction" in node_name:
                                # 查找该节点的 function_reference.member_name
                                nodes = json_output["blueprint"]["graphs"][0].get("nodes", [])
                                for node in nodes:
                                    if node.get("node_name") == node_name:
                                        func_ref = node.get("function_reference", {})
                                        if func_ref.get("member_name") == "Jump":
                                            found_jump_flow = True
                                            break

            assert found_jump_flow, "未找到 IA_Jump(Started) → Jump 执行流程"

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_jump_completed_flow(self):
            """
            验证 IA_Jump(Completed) → StopJumping 执行流程。

            Per D-21-09 C++对照:
            EnhancedInputComponent->BindAction(JumpAction, ETriggerEvent::Completed, this, &DoJumpEnd)
            DoJumpEnd() { StopJumping(); }
            """
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            found_stop_flow = False

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                for flow in json_output["blueprint"]["graphs"][0].get("execution_flows", []):
                    entry = flow.get("entry", "")
                    if "IA_Jump" in entry and "Completed" in entry:
                        chain = flow.get("chain", [])
                        for node_name in chain:
                            if "CallFunction" in node_name:
                                nodes = json_output["blueprint"]["graphs"][0].get("nodes", [])
                                for node in nodes:
                                    if node.get("node_name") == node_name:
                                        func_ref = node.get("function_reference", {})
                                        if func_ref.get("member_name") == "StopJumping":
                                            found_stop_flow = True
                                            break

            assert found_stop_flow, "未找到 IA_Jump(Completed) → StopJumping 执行流程"
    ```

    Per TEST-02: Jump执行流程正确构建
    Per D-21-09: C++源码对照（DoJumpStart→Jump, DoJumpEnd→StopJumping）
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase21_verification.py::TestExecutionFlow -v 2>&1 | grep -E "(PASSED|FAILED|SKIPPED|test_)"</automated>
    grep -c "class TestExecutionFlow" tests/test_phase21_verification.py
    grep -c "IA_Jump" tests/test_phase21_verification.py
  </verify>
  <done>
    TestExecutionFlow 类已定义
    test_execution_flows_exist、test_jump_started_flow、test_jump_completed_flow 方法已实现
    测试包含 IA_Jump 和 Jump/StopJumping 验证逻辑
  </done>
</task>

<task type="auto">
  <name>Task 4: TEST-03 数据流验证测试</name>
  <files>tests/test_phase21_verification.py</files>
  <read_first>
    - tests/test_phase21_verification.py (上一任务添加的文件)
    - E:/Develop/lib/UnrealEngine/Samples/FirstPersonC/Source/FirstPersonC/FirstPersonCCharacter.cpp (expected 数据流)
  </read_first>
  <behavior>
    - Test 1: 验证 data_flows 存在且非空
    - Test 2: 验证 ActionValue_X → Right 参数数据流
    - Test 3: 验证 ActionValue_Y → Forward 参数数据流
  </behavior>
  <action>
    在 test_phase21_verification.py 添加 TestDataFlow 类，实现 TEST-03：

    ```python
    class TestDataFlow:
        """TEST-03: 数据流验证（ActionValue_X/Y → 参数）"""

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_data_flows_exist(self):
            """验证 data_flows 存在"""
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                graph = json_output["blueprint"]["graphs"][0]
                assert "data_flows" in graph, "data_flows 字段不存在"

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_actionvalue_x_to_right(self):
            """
            验证 ActionValue_X → Right 参数数据流。

            Per D-21-09 C++对照:
            MoveInput(Value) { MovementVector.X → DoMove(Right, Forward) }
            MovementVector.X 对应 Right 参数
            """
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            found_x_flow = False

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                for flow in json_output["blueprint"]["graphs"][0].get("data_flows", []):
                    source = flow.get("source", {})
                    source_pin = source.get("pin", "")
                    # ActionValue_X 输出 pin
                    if "ActionValue_X" in source_pin or source_pin == "X":
                        target = flow.get("target", {})
                        target_pin = target.get("pin", "")
                        # Right 参数（AddMovementInput 的 Right/Left 参数名）
                        if "Right" in target_pin or "Left" in target_pin:
                            found_x_flow = True
                            break

            assert found_x_flow, "未找到 ActionValue_X → Right 数据流"

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_actionvalue_y_to_forward(self):
            """
            验证 ActionValue_Y → Forward 参数数据流。

            Per D-21-09 C++对照:
            MoveInput(Value) { MovementVector.Y → DoMove(Right, Forward) }
            MovementVector.Y 对应 Forward 参数
            """
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            found_y_flow = False

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                for flow in json_output["blueprint"]["graphs"][0].get("data_flows", []):
                    source = flow.get("source", {})
                    source_pin = source.get("pin", "")
                    # ActionValue_Y 输出 pin
                    if "ActionValue_Y" in source_pin or source_pin == "Y":
                        target = flow.get("target", {})
                        target_pin = target.get("pin", "")
                        # Forward 参数
                        if "Forward" in target_pin:
                            found_y_flow = True
                            break

            assert found_y_flow, "未找到 ActionValue_Y → Forward 数据流"
    ```

    Per TEST-03: 数据流正确解析
    Per D-21-09: C++源码对照（ActionValue.X→Right, ActionValue.Y→Forward）
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase21_verification.py::TestDataFlow -v 2>&1 | grep -E "(PASSED|FAILED|SKIPPED|test_)"</automated>
    grep -c "class TestDataFlow" tests/test_phase21_verification.py
    grep -c "ActionValue" tests/test_phase21_verification.py
  </verify>
  <done>
    TestDataFlow 类已定义
    test_data_flows_exist、test_actionvalue_x_to_right、test_actionvalue_y_to_forward 方法已实现
    测试包含 ActionValue_X/Y 和 Right/Forward 验证逻辑
  </done>
</task>

<task type="auto">
  <name>Task 5: TEST-04 节点属性验证测试</name>
  <files>tests/test_phase21_verification.py</files>
  <read_first>
    - tests/test_phase21_verification.py (上一任务添加的文件)
    - uasset_read.py (理解 function_reference 和 node_guid 结构)
  </read_first>
  <behavior>
    - Test 1: 验证节点包含 node_guid 字段（非空 GUID 格式）
    - Test 2: 验证 CallFunction 节点包含 function_reference.member_name 字段
    - Test 3: 验证 Event 节点包含 event_reference 字段
  </behavior>
  <action>
    在 test_phase21_verification.py 添加 TestNodeProperties 类，实现 TEST-04：

    ```python
    class TestNodeProperties:
        """TEST-04: 节点属性验证（FunctionReference.MemberName、NodeGuid）"""

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_node_guid_present(self):
            """验证节点包含 node_guid 字段（非空 GUID 格式）"""
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                nodes = json_output["blueprint"]["graphs"][0].get("nodes", [])
                assert len(nodes) > 0, "nodes 为空"

                # 验证每个节点有 node_guid
                for node in nodes:
                    node_guid = node.get("node_guid")
                    assert node_guid is not None, f"节点 {node.get('node_name')} 缺少 node_guid"
                    # GUID 应为 32 字符十六进制字符串
                    if node_guid:
                        assert len(node_guid) >= 32, f"node_guid 格式异常: {node_guid}"

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_function_reference_member_name(self):
            """验证 CallFunction 节点包含 function_reference.member_name"""
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            found_call_function = False

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                nodes = json_output["blueprint"]["graphs"][0].get("nodes", [])

                for node in nodes:
                    node_type = node.get("node_type", "")
                    if node_type == "K2Node_CallFunction" or "CallFunction" in node_type:
                        found_call_function = True
                        func_ref = node.get("function_reference")
                        assert func_ref is not None, \
                            f"CallFunction 节点 {node.get('node_name')} 缺少 function_reference"

                        member_name = func_ref.get("member_name")
                        assert member_name is not None, \
                            f"function_reference 缺少 member_name: {func_ref}"

                        # 验证 member_name 是有效的函数名
                        assert len(member_name) > 0, f"member_name 为空"
                        # UE 函数名应不包含空格
                        assert " " not in member_name, f"member_name 包含空格: {member_name}"

            if not found_call_function:
                pytest.skip("未找到 CallFunction 节点（可能 graphs 为空）")

        @pytest.mark.skipif(not os.path.exists(FIRST_PERSON_CHARACTER_PATH), reason="Test asset not found")
        def test_event_reference_present(self):
            """验证 Event 节点包含 event_reference 字段"""
            result = parse_uasset(FIRST_PERSON_CHARACTER_PATH)
            json_output = format_json_full(result)

            found_event_node = False

            if json_output.get("blueprint") and json_output["blueprint"].get("graphs"):
                nodes = json_output["blueprint"]["graphs"][0].get("nodes", [])

                for node in nodes:
                    node_type = node.get("node_type", "")
                    if node_type == "K2Node_Event" or "Event" in node_type:
                        found_event_node = True
                        event_ref = node.get("event_reference")
                        # event_reference 应存在（或节点有其他识别事件的方式）
                        # Phase 20 实现了 event_reference 顶层字段

            if not found_event_node:
                pytest.skip("未找到 Event 节点（可能 graphs 为空）")
    ```

    Per TEST-04: 节点属性正确提取
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase21_verification.py::TestNodeProperties -v 2>&1 | grep -E "(PASSED|FAILED|SKIPPED|test_)"</automated>
    grep -c "class TestNodeProperties" tests/test_phase21_verification.py
    grep -c "node_guid" tests/test_phase21_verification.py
    grep -c "function_reference" tests/test_phase21_verification.py
  </verify>
  <done>
    TestNodeProperties 类已定义
    test_node_guid_present、test_function_reference_member_name、test_event_reference_present 方法已实现
    测试包含 node_guid、function_reference、event_reference 验证逻辑
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试文件 → 解析器 | 测试调用 parse_uasset() 解析真实资产 |
| 解析结果 → JSON | format_json_full() 输出需验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | S | 测试断言 | accept | 测试失败即发现问题，无需额外验证 |
| T-21-02 | T | parse_uasset 输出 | mitigate | 使用真实资产测试，不信任 mock 数据 |
| T-21-03 | I | JSON 输出结构 | mitigate | 验证所有关键字段存在且格式正确 |

</threat_model>

<verification>
## 完整测试运行

```bash
python -m pytest tests/test_phase21_verification.py -v
```

## 验证项

1. 文件存在且可导入
2. 4个测试类已定义（TestNodeCount, TestExecutionFlow, TestDataFlow, TestNodeProperties）
3. 每个测试类包含至少1个测试方法
4. 测试使用真实资产路径 FIRST_PERSON_CHARACTER_PATH
5. 测试包含 pytest.mark.skipif 跳过逻辑（资产不存在时）
</verification>

<success_criteria>
## 可验证的完成条件

1. tests/test_phase21_verification.py 文件存在，行数 >= 150
2. 包含 4 个测试类（TestNodeCount, TestExecutionFlow, TestDataFlow, TestNodeProperties）
3. 包含 FIRST_PERSON_CHARACTER_PATH 常量定义
4. 包含 pytest skipif 标记
5. 测试可运行（pytest 命令成功执行）
6. 所有测试通过或跳过（无 import 错误）
</success_criteria>

<output>
After completion, create `.planning/phases/21-验证测试/21-01-SUMMARY.md`
</output>