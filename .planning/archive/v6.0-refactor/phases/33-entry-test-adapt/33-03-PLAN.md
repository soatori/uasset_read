---
phase: 33
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - tests/test_graph_parsing.py
  - tests/test_output_formatting.py
  - tests/test_phase14_output_formats.py
  - tests/test_blueprint_extraction.py
  - tests/test_uasset_read.py
  - tests/test_phase13_transform.py
  - tests/test_phase26_blueprint_metadata_enhancement.py
  - tests/test_property_parsing.py
  - tests/test_exportmap_properties.py
  - tests/test_advanced_properties.py
  - tests/test_phase12_blueprint_variables.py
  - tests/test_phase21_verification.py
  - tests/test_skill_integration.py
  - tests/test_dependency_analysis.py
  - tests/test_loop_limits.py
  - tests/test_mmap_behavior.py
  - tests/test_boundary_validation.py
  - tests/test_partial_results.py
  - CLAUDE.md
autonomous: true
requirements: [TEST-02]
user_setup: []

must_haves:
  truths:
    - "所有 18 个测试文件的导入路径更新为新版模块路径"
    - "pytest tests/ --tb=short 运行结果与基线一致（411 passed, 47 skipped, 0 failed）"
    - "旧版 uasset_read.py 已从仓库中删除"
    - "CLAUDE.md 中关于旧版的描述已更新"
  artifacts:
    - path: "tests/test_*.py"
      provides: "更新后的测试导入路径"
      contains: "from uasset_read import"
    - path: "CLAUDE.md"
      provides: "更新后的项目架构描述"
      contains: "src/uasset_read/"
  key_links:
    - from: "tests/*.py"
      to: "src/uasset_read/__init__.py"
      via: "from uasset_read import X"
      pattern: "from uasset_read import"
    - from: "CLAUDE.md"
      to: "src/uasset_read/"
      via: "项目架构描述"
      pattern: "新版模块化包"
---

<objective>
更新所有 18 个测试文件的导入路径以适配新版模块路径，运行测试确认通过，删除旧版 `uasset_read.py`（如果存在），更新 CLAUDE.md 文档。

Purpose: 完成 v6.0 模块化重构的"最后一公里"，确保所有测试指向新版模块，旧版文件安全删除。
Output: 更新的测试文件 + 删除的旧文件 + 更新的 CLAUDE.md
</objective>

<execution_context>
@E:\Develop\uasset_read\.planning\workflows\execute-plan.md
@E:\Develop\uasset_read\.planning\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\PROJECT.md
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\phases\33-entry-test-adapt\33-CONTEXT.md
@E:\Develop\uasset_read\.planning\phases\33-entry-test-adapt\33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行基线测试确认当前状态</name>
  <files></files>
  <read_first>
    - CLAUDE.md — 当前测试基线描述
  </read_first>
  <action>
在执行任何修改前，先运行测试确认基线状态：

```bash
python -m pytest tests/ --tb=short -q
```

记录结果：
- Passed 数量（预期：411）
- Skipped 数量（预期：47）
- Failed 数量（预期：0）

如果基线不是 411 passed / 47 skipped / 0 failed，记录当前状态并在后续修复时对比。

然后运行 `python -m uasset_read --help` 确认 CLI 入口可用（Plan 02 的产出）。

最后运行 `python -c "from uasset_read import parse_uasset; print('OK')"` 确认 parse_uasset 可从包导入（Plan 01 的产出）。
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 测试运行完成，记录 passed/skipped/failed 数量
    - 如果 failed > 0，记录失败测试文件名和失败原因（供后续修复参考）
    - `python -m uasset_read --help` 退出码为 0
    - `python -c "from uasset_read import parse_uasset"` 退出码为 0
  </acceptance_criteria>
  <done>基线测试已运行，CLI 和 parse_uasset 导入已确认可用</done>
</task>

<task type="auto">
  <name>Task 2: 分类更新 18 个测试文件的导入路径</name>
  <files>tests/test_graph_parsing.py, tests/test_output_formatting.py, tests/test_phase14_output_formats.py, tests/test_blueprint_extraction.py, tests/test_uasset_read.py, tests/test_phase13_transform.py, tests/test_phase26_blueprint_metadata_enhancement.py, tests/test_property_parsing.py, tests/test_exportmap_properties.py, tests/test_advanced_properties.py, tests/test_phase12_blueprint_variables.py, tests/test_phase21_verification.py, tests/test_skill_integration.py, tests/test_dependency_analysis.py, tests/test_loop_limits.py, tests/test_mmap_behavior.py, tests/test_boundary_validation.py, tests/test_partial_results.py</files>
  <read_first>
    - 每个测试文件的当前 import 语句（grep "from uasset_read import" tests/test_*.py）
    - src/uasset_read/__init__.py — 当前公共 API 导出列表
    - .planning/phases/33-entry-test-adapt/33-RESEARCH.md — 迁移缺口汇总和测试分类
  </read_first>
  <action>
根据 RESEARCH.md 的测试分类（类别 A-D），按顺序处理 18 个测试文件的导入路径。

**原则（D-11, D-12, D-13）：**
- `from uasset_read import X` 保持不变（因为 __init__.py 会重新导出所有公共 API）
- 仅当 X 尚未在 __init__.py 中导出时才需要修改导入路径
- 不要修改测试断言（D-13：功能性修复留给 Phase 34）
- 仅修导入路径

**类别 A（4 个文件 — 导入应自动工作，无需修改）：**
| 文件 | 导入项 | 已导出？ | 操作 |
|------|--------|---------|------|
| test_loop_limits.py | MAX_PROPERTY_COUNT 等 | 是 | 无需修改 |
| test_partial_results.py | ErrorContext, ParseResult | 是 | 无需修改 |
| test_boundary_validation.py | FArchive, ParseError, PackageIndex | 是 | 无需修改 |
| test_mmap_behavior.py | FArchive, ParseError | 是 | 无需修改 |

**类别 B（3 个文件 — 需 __init__.py 更新后自动工作）：**
| 文件 | 关键导入 | 已在 __init__.py？ | 操作 |
|------|---------|-------------------|------|
| test_phase21_verification.py | parse_uasset, format_json_full | parse_uasset 新加, format_json_full 需 Phase 32 | 无需修改（依赖 Plan 01/Phase 32 产出） |
| test_phase12_blueprint_variables.py | parse_property_flags_to_labels, format_variable_type | 需从旧版迁移到 __init__.py | 需检查是否已导出 |
| test_skill_integration.py | parse_uasset, format_json_full 等 | 同上 | 无需修改 |

**类别 C（3 个文件 — 需 Phase 31/32 产出）：**
| 文件 | 关键导入 | 依赖 | 操作 |
|------|---------|------|------|
| test_graph_parsing.py | read_ue_graph, CONTROL_FLOW_NODES 等 | Phase 31 | 无需修改（假设 Phase 31 已完成） |
| test_output_formatting.py | format_json_full, build_connections_map 等 | Phase 32 | 无需修改（假设 Phase 32 已完成） |
| test_phase14_output_formats.py | build_status_info, format_markdown 等 | Phase 32 | 无需修改 |

**类别 D（需确认缺口函数是否已迁移）：**

以下测试文件需要确认导入项是否已在新版 __init__.py 中导出：

1. **test_blueprint_extraction.py：** 导入 resolve_parent_class, parse_default_value, read_blueprint_variable（TYPE_CHECKING 块内）。resolve_parent_class 需检查是否在 serializers 中导出。parse_default_value 和 read_blueprint_variable 需检查是否在 blueprint 模块中。

2. **test_uasset_read.py：** 导入 CustomVersion, get_asset_class。CustomVersion 需检查是否在 serializers 中导出。get_asset_class 已在 serializers。

3. **test_phase13_transform.py：** 导入 VectorValue, RotatorValue, ScaleValue, parse_vector_value 等。这些在 Plan 01 中已补齐到 __init__.py。

4. **test_phase26_blueprint_metadata_enhancement.py：** 导入 read_blueprint_variable 和 CPF_* 常量。需确认是否已导出。

5. **test_property_parsing.py：** 导入 use_complete_type_name, read_property_tag, resolve_package_index_to_reference。需确认是否已导出。

6. **test_exportmap_properties.py：** 导入 use_complete_type_name, read_property_tag。同上。

7. **test_advanced_properties.py：** 导入 _extract_struct_type_from_tag 等内部函数。需确认新版 parsers 是否有等价实现。

**具体操作步骤：**

对每个类别 D 文件，执行以下检查：
```bash
python -c "from uasset_read import <导入项>; print('OK')"
```

如果导入失败，检查对应模块是否有该函数/类，然后更新 __init__.py 添加导出。如果函数/类在新版模块中不存在（纯旧版内部函数），保持测试文件中的 TYPE_CHECKING 导入块不变，跳过测试（标记为 pytest.mark.skip(reason="Phase 34: 内部函数待迁移")）。

**已知可能需要跳过的导入（TYPE_CHECKING 块内）：**
- resolve_parent_class — 检查 serializers 中是否存在
- parse_default_value — 检查 blueprint 中是否存在
- read_blueprint_variable — 检查 blueprint/variable_extractor.py 中是否存在
- read_ed_graph_pin_type — 检查 serializers/graph.py 中是否存在
- use_complete_type_name — 检查 serializers/property_tags.py 中是否存在
- read_property_tag — 检查 serializers/property_tags.py 中是否存在
- resolve_package_index_to_reference — 检查 serializers/object_resources.py 中是否存在
- _extract_struct_type_from_tag 等 — 检查 parsers 中是否存在

**对每个失败导入的处理：**
1. 如果函数存在于新版模块但 __init__.py 未导出 → 更新 __init__.py 添加导出
2. 如果函数不存在于新版模块（旧版内部函数）→ 在测试中添加 pytest.mark.skip

**验证命令（对每个类别 D 文件）：**
```bash
python -m pytest tests/<文件名>.py --tb=short -q --collect-only 2>&1 | tail -5
```

只检查导入不运行测试（使用 --collect-only）。
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=short -q --collect-only 2>&1 | tail -5</automated>
    <automated>python -c "import uasset_read; missing = [x for x in ['VectorValue', 'RotatorValue', 'ScaleValue', 'parse_vector_value', 'parse_rotator_value', 'parse_scale_value', 'extract_component_transforms', 'find_main_blueprint_generated_class', 'parse_uasset'] if not hasattr(uasset_read, x)]; print('Missing:', missing if missing else 'None')"</automated>
  </verify>
  <acceptance_criteria>
    - 所有 18 个测试文件可被 pytest --collect-only 收集（无 ImportError）
    - 每个类别 D 文件中 TYPE_CHECKING 块内的导入已通过 `hasattr(uasset_read, X)` 验证
    - 如存在无法导入的内部函数，对应测试已添加 skip 标记
    - __init__.py 包含所有测试需要的公共 API 导出
    - 没有测试断言被修改（仅允许修导入路径和添加 skip 标记）
  </acceptance_criteria>
  <done>所有 18 个测试文件的导入路径已更新，无 ImportError，无法导入的内部函数已添加 skip 标记</done>
</task>

<task type="auto">
  <name>Task 3: 运行完整测试确认通过</name>
  <files></files>
  <read_first>
    - CLAUDE.md — 基线测试预期结果
  </read_first>
  <action>
运行完整测试套件：

```bash
python -m pytest tests/ -v --tb=short
```

对比基线结果（411 passed, 47 skipped, 0 failed）。记录任何变化：
- 如果 passed 减少 → 分析原因（可能是 __init__.py 导出缺失或导入路径问题）
- 如果 skipped 增加 → 分析原因（可能是 skip 标记添加过多）
- 如果 failed > 0 → 记录失败的测试名称和原因

**如果存在失败的测试：**
1. 确认失败原因是导入路径问题还是功能性问题
2. 导入路径问题 → 修 __init__.py 或测试导入
3. 功能性问题 → 记录为已知问题，留给 Phase 34 等价验证（D-13）

**成功标准：** 0 failed（与基线一致），skipped 数量不超过基线 + 新增的 skip 标记数量。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 测试运行完成，failed = 0
    - passed 数量 >= 400（允许少量功能修复导致的跳过）
    - 所有失败的测试已记录原因
    - 如果存在非 0 failed，每个失败已标注 "Phase 34 修复" 或已添加 skip 标记
  </acceptance_criteria>
  <done>完整测试套件运行完成，0 failed（或与基线一致的已知失败），所有问题已记录</done>
</task>

<task type="auto">
  <name>Task 4: 删除旧版 uasset_read.py + 更新 CLAUDE.md（D-15, D-16）</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md — 当前关于旧版的描述
    - src/uasset_read/__init__.py — 确认 shim 块已删除（Phase 33 Plan 01 Task 3 已处理）
  </read_first>
  <action>
**删除旧文件（D-15）：**

在执行此任务前确认以下条件：
1. 所有测试通过（Task 3 完成且 failed = 0）
2. `python -m uasset_read` 能正常解析测试资产
3. CLI 入口可用（Task 2 完成）

检查旧文件是否存在：
```bash
ls E:/Develop/uasset_read/uasset_read.py 2>/dev/null
```

如果存在：
```bash
rm E:/Develop/uasset_read/uasset_read.py
```

如果旧文件已被删除（当前 git status 显示不在跟踪列表中），跳过此步骤。

**更新 CLAUDE.md：**

移除或更新关于旧版的描述：
- 删除 "uasset_read.py — 旧版单文件（~7958 行），当前 CLI 入口" 相关描述
- 更新 "常用命令" 部分：将 `python uasset_read.py` 改为 `python -m uasset_read` 或 `uasset-read`
- 更新 "架构" 部分：移除旧版单文件的引用
- 更新 "注意事项" 部分：移除 `uasset_read.py` 相关的备注
- 更新 "文件组织" 部分：移除 uasset_read.py 条目
- 确认 `src/uasset_read/__init__.py` 的兼容 shim 块（第 183-299 行）已被 Plan 01 Task 3 删除并替换为新版模块导入。如果 shim 仍存在，先执行 Plan 01 Task 3 的步骤 C-1 删除它。

具体更新内容：
1. **当前状态** 部分：更新为 "v6.0 模块化重构已完成 — 旧版 uasset_read.py 已删除"
2. **常用命令** 部分：
   - 删除 `python uasset_read.py path/to/file.uasset`
   - 添加 `python -m uasset_read path/to/file.uasset`
   - 添加 `uasset-read path/to/file.uasset`
3. **文件组织** 部分：删除 `uasset_read.py` 行
4. **API 导出** 部分：移除 "完整解析入口仍在旧版 uasset_read.py" 的注释
5. **注意事项** 部分：移除关于 `uasset_read.py` 的 CLI 入口备注
  </action>
  <verify>
    <automated>ls E:/Develop/uasset_read/uasset_read.py 2>/dev/null && echo "STILL EXISTS" || echo "DELETED"</automated>
    <automated>python -m uasset_read --help 2>&1 | head -3</automated>
    <automated>grep -c "uasset_read.py" CLAUDE.md</automated>
    <automated>grep "python -m uasset_read" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - E:/Develop/uasset_read/uasset_read.py 不存在（已删除或本来就不存在）
    - CLAUDE.md 中对旧版单文件的引用数 <= 0（grep "uasset_read.py" 返回 0 或仅在历史描述中）
    - CLAUDE.md 包含 `python -m uasset_read` 或 `uasset-read` 作为 CLI 入口
    - CLAUDE.md 的"常用命令"部分不再引用旧版 uasset_read.py
    - `python -m uasset_read --help` 正常运行
    - 测试仍在运行且 failed = 0
  </acceptance_criteria>
  <done>旧版 uasset_read.py 已删除（或确认已不存在），CLAUDE.md 已更新反映 v6.0 模块化重构完成状态</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试文件导入 → __init__.py | 导入路径变更可能导致 ImportError，--collect-only 验证 |
| skip 标记添加 → 测试覆盖率 | 过多 skip 降低测试覆盖率，但 D-13 允许已知失败留给 Phase 34 |
| 旧文件删除 → 数据丢失 | 旧版 uasset_read.py.bak 保留作为参考 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | DoS | Test import paths (circular import) | mitigate | --collect-only 验证所有导入路径后再运行完整测试 |
| T-33-10 | Integrity | Old file deletion | accept | 保留 uasset_read.py.bak 作为源码参考，删除后仍可通过 git 历史恢复 |
| T-33-11 | Tampering | Skip markers hiding failures | mitigate | 仅对 TYPE_CHECKING 块内的旧版内部函数添加 skip，不跳过功能测试 |
</threat_model>

<verification>
- 所有 18 个测试文件可通过 `pytest --collect-only` 收集
- 测试运行结果：0 failed
- `python -m uasset_read --help` 正常运行
- 旧版 uasset_read.py 已不存在
- CLAUDE.md 已更新，不再引用旧版单文件
</verification>

<success_criteria>
- 18 个测试文件导入路径全部适配
- pytest tests/ 运行结果：0 failed（与基线一致）
- 旧版 uasset_read.py 已删除
- CLAUDE.md 已更新反映 v6.0 完成状态
- `python -m uasset_read` 正常工作
- `uasset-read` CLI 入口可用（pip install -e . 后）
</success_criteria>

<output>
After completion, create `.planning/phases/33-entry-test-adapt/33-03-SUMMARY.md`
</output>
