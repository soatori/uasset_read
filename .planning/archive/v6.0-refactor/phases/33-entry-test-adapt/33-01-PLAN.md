---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/parse_uasset.py
  - src/uasset_read/models/transforms.py
  - src/uasset_read/models/__init__.py
  - src/uasset_read/blueprint/transform_parser.py
  - src/uasset_read/blueprint/__init__.py
  - src/uasset_read/blueprint/variable_extractor.py
  - src/uasset_read/serializers/object_resources.py
  - src/uasset_read/serializers/__init__.py
  - src/uasset_read/serializers/property_tags.py
  - src/uasset_read/serializers/graph.py
  - src/uasset_read/parsers/property_types.py
  - src/uasset_read/constants.py
  - src/uasset_read/__init__.py
autonomous: true
requirements: [MOD-12, TEST-02]
user_setup: []

must_haves:
  truths:
    - "from uasset_read import parse_uasset 成功导入且函数可调用"
    - "parse_uasset 能正常解析测试 .uasset 文件并返回 ParseResult"
    - "缺失的转换数据类（VectorValue/RotatorValue/ScaleValue）可从 uasset_read 导入"
    - "find_main_blueprint_generated_class 可在 parse_uasset 管线中被调用"
  artifacts:
    - path: "src/uasset_read/parse_uasset.py"
      provides: "parse_uasset 主解析管线函数"
      exports: ["parse_uasset"]
      min_lines: 80
    - path: "src/uasset_read/models/transforms.py"
      provides: "变换数据类（VectorValue, RotatorValue, ScaleValue）"
      exports: ["VectorValue", "RotatorValue", "ScaleValue"]
      min_lines: 30
    - path: "src/uasset_read/blueprint/transform_parser.py"
      provides: "变换解析函数（extract_component_transforms, parse_vector_value, parse_rotator_value, parse_scale_value, format_transform_value）"
      exports: ["extract_component_transforms", "parse_vector_value", "parse_rotator_value", "parse_scale_value", "format_transform_value"]
      min_lines: 60
    - path: "src/uasset_read/serializers/object_resources.py"
      provides: "find_main_blueprint_generated_class 函数"
      contains: "def find_main_blueprint_generated_class"
  key_links:
    - from: "src/uasset_read/parse_uasset.py"
      to: "src/uasset_read/serializers/object_resources.py"
      via: "find_main_blueprint_generated_class import"
      pattern: "from uasset_read.serializers.object_resources import find_main_blueprint_generated_class"
    - from: "src/uasset_read/parse_uasset.py"
      to: "src/uasset_read/blueprint/transform_parser.py"
      via: "extract_component_transforms import"
      pattern: "from uasset_read.blueprint.transform_parser import extract_component_transforms"
    - from: "src/uasset_read/parse_uasset.py"
      to: "src/uasset_read/graph/__init__.py"
      via: "extract_blueprint_graphs import"
      pattern: "from uasset_read.graph import extract_blueprint_graphs"
    - from: "src/uasset_read/__init__.py"
      to: "src/uasset_read/parse_uasset.py"
      via: "parse_uasset import"
      pattern: "from .parse_uasset import parse_uasset"
---

<objective>
创建 `src/uasset_read/parse_uasset.py` 主解析管线函数，补齐所有迁移缺口函数（转换数据类、变换解析、BPGC 查找），确保 `parse_uasset` 可被正常导入和调用。

Purpose: 提供完整的解析管线入口，等价迁移旧版 `uasset_read.py` §6223-6412 的 parse_uasset 函数，补齐 Phase 31/32 尚未实现的依赖函数。
Output: parse_uasset.py + models/transforms.py + blueprint/transform_parser.py + 更新的 serializers/__init__.py + __init__.py
</objective>

<execution_context>
@E:\Develop\uasset_read\.planning\workflows\execute-plan.md
@E:\Develop\uasset_read\.planning\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\PROJECT.md
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md
@E:\Develop\uasset_read\.planning\phases\33-entry-test-adapt\33-CONTEXT.md
@E:\Develop\uasset_read\.planning\phases\33-entry-test-adapt\33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 models/transforms.py 包含 VectorValue/RotatorValue/ScaleValue 数据类</name>
  <files>src/uasset_read/models/transforms.py, src/uasset_read/models/__init__.py</files>
  <read_first>
    - uasset_read.py.bak §1435-1480 — VectorValue, RotatorValue, ScaleValue 定义
    - uasset_read.py.bak §1483-1512 — format_transform_value 函数
    - src/uasset_read/models/properties.py — AdvancedPropertyValue 基类
    - src/uasset_read/models/__init__.py — 当前导出列表
  </read_first>
  <action>
创建 `src/uasset_read/models/transforms.py`，包含以下 dataclass（等价迁移旧版定义）：

```python
"""变换属性数据类 — VectorValue, RotatorValue, ScaleValue。

等价迁移 uasset_read.py §1435-1480。
Phase 33: 入口与测试适配。
"""
from __future__ import annotations

from dataclasses import dataclass, field


@dataclass(kw_only=True)
class VectorValue:
    """Vector struct property value。X/Y/Z 坐标值，用于 RelativeLocation 等。"""
    x: float
    y: float
    z: float
    property_type: str = field(default='StructProperty')


@dataclass(kw_only=True)
class RotatorValue:
    """Rotator struct property value。Roll/Pitch/Yaw 角度值（度数格式）。"""
    roll: float
    pitch: float
    yaw: float
    unit: str = 'degrees'
    property_type: str = field(default='StructProperty')


@dataclass(kw_only=True)
class ScaleValue:
    """Scale3D struct property value。X/Y/Z 缩放因子。"""
    x: float
    y: float
    z: float
    property_type: str = field(default='StructProperty')


def format_transform_value(value: float, precision_type: str) -> float | int:
    """
    格式化变换属性值，应用类型自适应精度处理。
    Location: 整数优先（is_integer 时返回 int），否则 3 位小数。
    Rotation: 3 位小数。
    Scale: 4 位小数。
    """
    if precision_type == 'location':
        if value == int(value):
            return int(value)
        return round(value, 3)
    elif precision_type == 'rotation':
        return round(value, 3)
    elif precision_type == 'scale':
        return round(value, 4)
    return value
```

更新 `src/uasset_read/models/__init__.py`，追加导出：
```python
from .transforms import VectorValue, RotatorValue, ScaleValue, format_transform_value
```

**注意：** 测试中 `VectorValue(x=1.0, y=2.0, z=3.0)` 使用位置参数调用，但 dataclass 使用了 `kw_only=True`。旧版也是 `kw_only=True`，测试使用的是关键字参数（`VectorValue(x=1.0, y=2.0, z=3.0)`），所以 kw_only 是兼容的。
  </action>
  <verify>
    <automated>python -c "from uasset_read.models.transforms import VectorValue, RotatorValue, ScaleValue, format_transform_value; v = VectorValue(x=1.0, y=2.0, z=3.0); print('OK:', v.x, v.y, v.z)"</automated>
    <automated>python -c "from uasset_read.models.transforms import RotatorValue; r = RotatorValue(roll=100.0, pitch=200.0, yaw=300.0); assert r.unit == 'degrees'; print('OK')"</automated>
    <automated>grep -c "def format_transform_value" src/uasset_read/models/transforms.py</automated>
  </verify>
  <acceptance_criteria>
    - src/uasset_read/models/transforms.py 文件存在且 >= 25 行
    - 包含 VectorValue, RotatorValue, ScaleValue 三个 dataclass 定义
    - 包含 format_transform_value 函数定义
    - VectorValue 有 x, y, z 字段（float 类型）+ property_type='StructProperty'
    - RotatorValue 有 roll, pitch, yaw 字段（float 类型）+ unit='degrees' + property_type='StructProperty'
    - ScaleValue 有 x, y, z 字段（float 类型）+ property_type='StructProperty'
    - 所有 dataclass 使用 kw_only=True
    - format_transform_value 对 location 类型使用整数优先+3位小数，rotation 使用 3 位小数，scale 使用 4 位小数
    - models/__init__.py 导出 VectorValue, RotatorValue, ScaleValue, format_transform_value
    - `python -c "from uasset_read.models.transforms import VectorValue"` 成功执行
  </acceptance_criteria>
  <done>transforms.py 创建完成，包含 3 个 dataclass 和 format_transform_value，models/__init__.py 更新导出</done>
</task>

<task type="auto">
  <name>Task 2: 创建 blueprint/transform_parser.py 补齐变换解析函数</name>
  <files>src/uasset_read/blueprint/transform_parser.py, src/uasset_read/blueprint/__init__.py</files>
  <read_first>
    - uasset_read.py.bak §1514-1587 — parse_vector_value, parse_rotator_value, parse_scale_value
    - uasset_read.py.bak §1590-1630 — extract_component_transforms
    - src/uasset_read/models/transforms.py（Task 1 产出）
    - src/uasset_read/models/properties.py — StructValue 定义
    - src/uasset_read/blueprint/__init__.py — 当前导出
  </read_first>
  <action>
创建 `src/uasset_read/blueprint/transform_parser.py`，包含以下函数（等价迁移旧版）：

```python
"""组件变换解析函数 — extract_component_transforms 及值解析辅助函数。

等价迁移 uasset_read.py §1514-1630。
Phase 33: 入口与测试适配。
"""
from __future__ import annotations

from typing import Any, Dict, List, Optional

from uasset_read.models.properties import PropertyValue, StructValue
from uasset_read.models.transforms import (
    VectorValue, RotatorValue, ScaleValue, format_transform_value,
)


def parse_vector_value(struct_value: StructValue, precision_type: str = 'location') -> VectorValue:
    """解析 Vector struct property 到 VectorValue。从 fields 提取 X/Y/Z 字段。"""
    fields = struct_value.fields
    x = format_transform_value(fields["X"], precision_type)
    y = format_transform_value(fields["Y"], precision_type)
    z = format_transform_value(fields["Z"], precision_type)
    return VectorValue(x=x, y=y, z=z)


def parse_rotator_value(struct_value: StructValue) -> RotatorValue:
    """解析 Rotator struct property 到 RotatorValue。从 fields 提取 Roll/Pitch/Yaw 字段。"""
    fields = struct_value.fields
    roll = format_transform_value(fields["Roll"], 'rotation')
    pitch = format_transform_value(fields["Pitch"], 'rotation')
    yaw = format_transform_value(fields["Yaw"], 'rotation')
    return RotatorValue(roll=roll, pitch=pitch, yaw=yaw)


def parse_scale_value(struct_value: StructValue) -> ScaleValue:
    """解析 Scale3D struct property 到 ScaleValue。从 fields 提取 X/Y/Z 字段。"""
    fields = struct_value.fields
    x = format_transform_value(fields["X"], 'scale')
    y = format_transform_value(fields["Y"], 'scale')
    z = format_transform_value(fields["Z"], 'scale')
    return ScaleValue(x=x, y=y, z=z)


def extract_component_transforms(
    export_properties: List[PropertyValue],
    component_name: Optional[str] = None,
) -> Dict[str, Any]:
    """
    从组件 export 的 properties 中提取变换属性。
    筛选 RelativeLocation/RelativeRotation/RelativeScale3D 属性，
    分派到对应解析函数转换为 VectorValue/RotatorValue/ScaleValue。

    Returns:
        Dict 包含 relative_location/relative_rotation/relative_scale 键
    """
    transforms: Dict[str, Any] = {}
    for prop in export_properties:
        if prop.type != "StructProperty" or not prop.value:
            continue
        struct_val = prop.value
        if not isinstance(struct_val, StructValue):
            continue
        prop_name = prop.name
        if prop_name == "RelativeLocation" and struct_val.struct_type == "Vector":
            transforms["relative_location"] = parse_vector_value(struct_val, 'location')
        elif prop_name == "RelativeRotation" and struct_val.struct_type == "Rotator":
            transforms["relative_rotation"] = parse_rotator_value(struct_val)
        elif prop_name == "RelativeScale3D" and struct_val.struct_type == "Vector":
            transforms["relative_scale"] = parse_scale_value(struct_val)
    return transforms
```

更新 `src/uasset_read/blueprint/__init__.py`，追加导出：
```python
from uasset_read.blueprint.transform_parser import (
    extract_component_transforms,
    parse_vector_value,
    parse_rotator_value,
    parse_scale_value,
    format_transform_value,
)
```

在 `__all__` 中追加 `"extract_component_transforms"`, `"parse_vector_value"`, `"parse_rotator_value"`, `"parse_scale_value"`, `"format_transform_value"`。
  </action>
  <verify>
    <automated>python -c "from uasset_read.blueprint.transform_parser import extract_component_transforms, parse_vector_value, parse_rotator_value, parse_scale_value; print('OK')"</automated>
    <automated>python -c "from uasset_read.blueprint import extract_component_transforms, parse_vector_value; print('OK')"</automated>
    <automated>grep -c "def extract_component_transforms\|def parse_vector_value\|def parse_rotator_value\|def parse_scale_value" src/uasset_read/blueprint/transform_parser.py</automated>
  </verify>
  <acceptance_criteria>
    - src/uasset_read/blueprint/transform_parser.py 文件存在且 >= 55 行
    - 包含 parse_vector_value, parse_rotator_value, parse_scale_value, extract_component_transforms 4 个函数定义
    - parse_vector_value 接受 struct_value 和 precision_type 参数，返回 VectorValue
    - parse_rotator_value 接受 struct_value 参数，返回 RotatorValue
    - parse_scale_value 接受 struct_value 参数，返回 ScaleValue
    - extract_component_transforms 接受 export_properties 和可选 component_name，返回 Dict
    - extract_component_transforms 筛选 RelativeLocation(Rotator)/RelativeRotation(Rotator)/RelativeScale3D(Vector)
    - blueprint/__init__.py 导出上述 4 个函数
    - `python -c "from uasset_read.blueprint import extract_component_transforms"` 成功执行
  </acceptance_criteria>
  <done>transform_parser.py 创建完成，包含所有变换解析函数，blueprint/__init__.py 更新导出</done>
</task>

<task type="auto">
  <name>Task 3: 创建 parse_uasset.py 主解析管线 + 补齐 find_main_blueprint_generated_class</name>
  <files>src/uasset_read/parse_uasset.py, src/uasset_read/serializers/object_resources.py, src/uasset_read/serializers/__init__.py, src/uasset_read/__init__.py</files>
  <read_first>
    - uasset_read.py.bak §6223-6412 — parse_uasset() 完整实现
    - uasset_read.py.bak §3035-3092 — detect_blueprint_generated_class + find_main_blueprint_generated_class
    - src/uasset_read/archive.py — FArchive 类
    - src/uasset_read/exceptions.py — VersionError, ParseError
    - src/uasset_read/serializers/package_summary.py — read_package_summary
    - src/uasset_read/serializers/object_resources.py — read_import_map, read_export_map, read_name_table, detect_blueprint_generated_class
    - src/uasset_read/parsers/property_parser.py — parse_properties_from_export
    - src/uasset_read/blueprint/__init__.py — extract_blueprint_metadata, extract_component_transforms
    - src/uasset_read/models/result.py — ParseResult
  </read_first>
  <action>
**步骤 A: 在 serializers/object_resources.py 中追加 find_main_blueprint_generated_class**

在文件末尾追加：
```python
def find_main_blueprint_generated_class(
    export_map: List[ObjectExport],
    import_map: List[ObjectImport],
    asset_name: str,
) -> Optional[ObjectExport]:
    """
    查找主 BlueprintGeneratedClass 导出（等价迁移 uasset_read.py §3063-3092）。

    使用 object_name 匹配 + serial_size 最大原则。
    主 BPGC 的 object_name 通常为 asset_name + "_C"。
    """
    candidates = []
    for export in export_map:
        if detect_blueprint_generated_class(export, import_map, export_map):
            if export.object_name and export.object_name.startswith(asset_name):
                candidates.append(export)
    if candidates:
        return max(candidates, key=lambda e: e.serial_size)
    return None
```

更新 `src/uasset_read/serializers/__init__.py`，追加导出 `find_main_blueprint_generated_class`。

**步骤 B: 创建 src/uasset_read/parse_uasset.py**

等价迁移旧版 `parse_uasset()` §6223-6412，完整管线：

```python
"""主解析管线入口 — parse_uasset() 函数。

等价迁移 uasset_read.py §6223-6412。
Phase 33: 入口与测试适配。
"""
from __future__ import annotations

from typing import TYPE_CHECKING, Optional, List

from uasset_read.archive import FArchive
from uasset_read.exceptions import VersionError, ParseError
from uasset_read.serializers.package_summary import read_package_summary
from uasset_read.serializers.object_resources import (
    read_name_table, read_import_map, read_export_map,
    find_main_blueprint_generated_class, detect_blueprint,
    build_imports_list, read_soft_object_paths, detect_circular_deps,
)
from uasset_read.parsers.property_parser import parse_properties_from_export
from uasset_read.blueprint import (
    extract_blueprint_metadata,
    extract_component_transforms,
)
from uasset_read.models.result import ParseResult


def parse_uasset(path: str) -> ParseResult:
    """
    主入口：解析 .uasset 文件（D-08 优雅降级）。

    流程：
    1. 创建 FArchive
    2. 读取 PackageFileSummary
    3. 读取 NameMap
    4. 读取 ImportMap
    5. 读取 ExportMap
    6. 对每个 export 解析属性 + 提取组件变换
    7. 提取 Blueprint 元数据（BPGC 优先 → UBlueprint 回退）
    8. 提取 Blueprint Graphs（Phase 31）
    9. 依赖分析（imports, soft_references, circular_deps）

    错误处理：
    - VersionError: 返回 result.errors, result.is_success = False
    - ParseError: 返回部分结果 + 错误信息
    - Exception: 返回 Unexpected error

    Args:
        path: .uasset 文件路径

    Returns:
        ParseResult 实例（含解析数据和错误信息）
    """
    result = ParseResult()
    archive = None

    try:
        archive = FArchive(path)

        # Extract mmap info
        mmap_info = archive.get_mmap_info()
        result.mmap_used = mmap_info["used"]
        result.mmap_warning = mmap_info["warning"]

        # 读取文件头
        result.summary = read_package_summary(archive)

        # 读取名称表
        result.name_map = read_name_table(archive, result.summary)

        # 读取导入表
        result.import_map = read_import_map(archive, result.summary, result.name_map)

        # 读取导出表
        result.export_map = read_export_map(archive, result.summary, result.name_map)

        # 解析 ExportMap 属性
        for export in result.export_map:
            if export.serial_size > 0:
                try:
                    export.properties = parse_properties_from_export(
                        export, archive, result.summary, result.name_map,
                        result.export_map, result.import_map,
                    )
                except Exception as e:
                    result.errors.append(f"Property parse error in {export.object_name}: {e}")
                    export.properties = []

                # 提取组件变换属性
                if export.properties:
                    export.transforms = extract_component_transforms(export.properties)

        result.is_success = True

        # Blueprint 元数据提取
        blueprint_metadata = None
        asset_name = result.name_map[0] if result.name_map else None

        if asset_name:
            main_bpgc = find_main_blueprint_generated_class(
                result.export_map, result.import_map, asset_name
            )
            if main_bpgc:
                temp_archive = FArchive(path)
                temp_archive.set_byte_swapping(archive._byte_swapping)
                try:
                    meta, warn = extract_blueprint_metadata(
                        main_bpgc, temp_archive, result.import_map,
                        result.export_map, result.name_map, result.summary,
                    )
                    if meta:
                        blueprint_metadata = meta
                        if warn:
                            result.errors.append(f"blueprint parent warning: {warn}")
                except ParseError as e:
                    result.errors.append(f"blueprint extraction error (BPGC): {e}")
                finally:
                    temp_archive.close()

        # UBlueprint 回退
        if not blueprint_metadata:
            for export in result.export_map:
                if detect_blueprint(export, result.import_map, result.export_map):
                    temp_archive = FArchive(path)
                    temp_archive.set_byte_swapping(archive._byte_swapping)
                    try:
                        meta, warn = extract_blueprint_metadata(
                            export, temp_archive, result.import_map,
                            result.export_map, result.name_map, result.summary,
                        )
                        if meta:
                            blueprint_metadata = meta
                            if warn:
                                result.errors.append(f"blueprint parent warning: {warn}")
                    except ParseError as e:
                        result.errors.append(f"blueprint extraction error: {e}")
                    finally:
                        temp_archive.close()
                    break

        result.blueprint = blueprint_metadata

        # Blueprint Graph 提取（Phase 31 产出）
        try:
            from uasset_read.graph import extract_blueprint_graphs
            result.graphs = extract_blueprint_graphs(
                archive, result.summary, result.name_map,
                result.import_map, result.export_map,
            )
        except ImportError:
            result.graphs = []  # graph 模块不存在时静默跳过
        except ParseError as e:
            result.errors.append(f"graph extraction error: {e}")

        # 依赖分析（Phase 10）
        try:
            result.imports = build_imports_list(result.import_map)
            result.soft_references = read_soft_object_paths(
                archive, result.summary, result.name_map,
            )
            result.circular_deps = detect_circular_deps(result.import_map)
        except ParseError as e:
            result.errors.append(f"dependency analysis error: {e}")

    except VersionError as e:
        result.errors.append(str(e))
        result.is_success = False

    except ParseError as e:
        result.errors.append(str(e))
        if e.partial_result:
            for key, value in e.partial_result.items():
                if hasattr(result, key):
                    setattr(result, key, value)
        result.is_success = False

    except Exception as e:
        result.errors.append(f"Unexpected error: {str(e)}")
        result.is_success = False

    finally:
        if archive:
            archive.close()

    return result
```

**步骤 C: 处理 src/uasset_read/__init__.py — 删除 shim 块 + 替换导入**

> **BLOCKER 注意：** 当前 `__init__.py` 第 183-299 行存在动态兼容 shim 块（`importlib.util` 从 `uasset_read.py.bak` 加载函数）。此 shim 必须在 Phase 33 中删除并替换为新版模块的直接导入。

**步骤 C-1: 删除兼容 shim 块（第 183-299 行）**

删除以下完整区块（从 `# ============` 分隔线到 `read_k2node_enhanced_input = None`）：
- 第 183-194 行：shim 注释头
- 第 196-299 行：`import sys as _sys` 到 `read_k2node_enhanced_input = None` 的 if/else 动态加载逻辑

替换为以下导入。**注意**：对 Phase 31/32 产出的模块（graph/、formatters/）使用 try/except ImportError 容错，因为这些目录可能尚未创建。

```python
# ============================================================================
# 主解析管线（Phase 33）
# ============================================================================
from .parse_uasset import parse_uasset

# 图解析辅助函数（Phase 31 产出，容错导入）
try:
    from .graph import (
        extract_blueprint_graphs,
        build_execution_flows,
        build_connections_map,
        build_graphs_summary,
    )
except ImportError:
    extract_blueprint_graphs = None
    build_execution_flows = None
    build_connections_map = None
    build_graphs_summary = None

# 格式化函数（Phase 32 产出，容错导入）
try:
    from .formatters import (
        format_json_full,
        format_json_summary,
        format_text_summary,
        format_text_full,
        format_markdown,
        format_variable_type,
        format_graphs_json,
        build_schema_info,
    )
except ImportError:
    format_json_full = None
    format_json_summary = None
    format_text_summary = None
    format_text_full = None
    format_markdown = None
    format_variable_type = None
    format_graphs_json = None
    build_schema_info = None

# 辅助函数（已迁移到新版模块）
from .serializers.object_resources import (
    find_main_blueprint_generated_class,
    resolve_parent_class,
    resolve_package_index_to_reference,
    read_soft_object_paths,
)
from .blueprint.transform_parser import (
    extract_component_transforms,
    parse_vector_value,
    parse_rotator_value,
    parse_scale_value,
    format_transform_value,
)

# 以下函数仍在 shim 中，需在 Task 4 中补齐：
# read_property_tag, _derive_node_name, read_blueprint_variable,
# parse_property_flags_to_labels, parse_default_value, read_k2node_*
# 暂保留为 None 占位，Task 4 完成后替换
read_property_tag = None
_derive_node_name = None
read_blueprint_variable = None
parse_property_flags_to_labels = None
parse_default_value = None
read_k2node_call_function = None
read_k2node_event = None
read_k2node_knot = None
read_edgraph_node_comment = None
read_k2node_enhanced_input = None
```

**步骤 C-2: 更新 __all__**

删除旧 `__all__` 中的 `# 兼容 shim（临时导出，Phase 31-34 完成后移除）` 注释及其后所有 shim 导出项，替换为：

```python
    # 主解析管线（Phase 33）
    "parse_uasset",
    # 图解析辅助函数（Phase 33 — 依赖 Phase 31）
    "extract_blueprint_graphs",
    "build_execution_flows",
    "build_connections_map",
    "build_graphs_summary",
    # 格式化函数（Phase 33 — 依赖 Phase 32）
    "format_json_full",
    "format_json_summary",
    "format_text_summary",
    "format_text_full",
    "format_markdown",
    "format_variable_type",
    "format_graphs_json",
    "build_schema_info",
    # 辅助函数（Phase 33）
    "find_main_blueprint_generated_class",
    "resolve_parent_class",
    "resolve_package_index_to_reference",
    "read_soft_object_paths",
    "extract_component_transforms",
    "parse_vector_value",
    "parse_rotator_value",
    "parse_scale_value",
    "format_transform_value",
    # 以下项在 Task 4 完成后追加到 __all__：
    # "read_property_tag", "_derive_node_name", "read_blueprint_variable",
    # "parse_property_flags_to_labels", "parse_default_value",
    # "read_k2node_call_function", "read_k2node_event", "read_k2node_knot",
    # "read_edgraph_node_comment", "read_k2node_enhanced_input",
    # 变换数据类（Phase 33）
    "VectorValue",
    "RotatorValue",
    "ScaleValue",
    "TransformValue",
```

> `TransformValue` 在 Task 1 中一并定义（与 VectorValue/RotatorValue/ScaleValue 同级）。

**步骤 C-3: 更新版本号**

将第 8 行 `__version__ = "5.1.0"` 更新为 `__version__ = "6.0.0"`。

**步骤 C-4: 更新模块文档字符串**

将第 4 行 `模块化重构版本 v5.1` 更新为 `模块化重构版本 v6.0 — 完整解析管线，零依赖，分层架构`。
  </action>
  <verify>
    <automated>python -c "from uasset_read import parse_uasset; print('OK:', parse_uasset.__name__)"</automated>
    <automated>python -c "from uasset_read import VectorValue, RotatorValue, ScaleValue, extract_component_transforms; print('OK')"</automated>
    <automated>python -c "from uasset_read import find_main_blueprint_generated_class, detect_blueprint_generated_class; print('OK')"</automated>
    <automated>grep -c "def parse_uasset" src/uasset_read/parse_uasset.py</automated>
    <automated>grep -c "def find_main_blueprint_generated_class" src/uasset_read/serializers/object_resources.py</automated>
    <automated>grep -c "parse_uasset\|VectorValue\|RotatorValue\|ScaleValue\|extract_component_transforms\|find_main_blueprint_generated_class" src/uasset_read/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - src/uasset_read/parse_uasset.py 文件存在且 >= 80 行
    - parse_uasset.py 包含 `def parse_uasset(path: str) -> ParseResult` 函数定义
    - parse_uasset.py 导入 FArchive, VersionError, ParseError, read_package_summary, read_name_table, read_import_map, read_export_map, parse_properties_from_export, extract_component_transforms, extract_blueprint_metadata, detect_blueprint, ParseResult
    - parse_uasset 函数包含 try/except VersionError/ParseError/Exception 三层错误处理
    - parse_uasset 函数包含 finally 块调用 archive.close()
    - parse_uasset 函数包含 Blueprint 元数据提取（BPGC 优先 + UBlueprint 回退）
    - parse_uasset 函数包含 graph 模块 import（使用 try/except ImportError 容错）
    - parse_uasset 函数包含依赖分析（build_imports_list, read_soft_object_paths, detect_circular_deps）
    - src/uasset_read/serializers/object_resources.py 包含 `def find_main_blueprint_generated_class`
    - src/uasset_read/__init__.py 包含 `from .parse_uasset import parse_uasset`
    - `python -c "from uasset_read import parse_uasset"` 成功执行无报错
    - `python -c "from uasset_read import VectorValue, RotatorValue, ScaleValue"` 成功执行无报错
    - `python -c "from uasset_read import extract_component_transforms, find_main_blueprint_generated_class"` 成功执行无报错
  </acceptance_criteria>
  <done>parse_uasset.py 创建完成，包含完整解析管线；transforms.py 和 transform_parser.py 补齐缺口函数；__init__.py 更新所有新导出项</done>
</task>

<task type="auto">
  <name>Task 4: 补齐剩余 shim 函数（read_property_tag, _derive_node_name, read_blueprint_variable, CPF 常量, parse_default_value, read_k2node_*）</name>
  <files>
    src/uasset_read/serializers/property_tags.py
    src/uasset_read/parsers/property_types.py
    src/uasset_read/blueprint/variable_extractor.py
    src/uasset_read/constants.py
    src/uasset_read/serializers/graph.py
    src/uasset_read/__init__.py
  </files>
  <read_first>
    - uasset_read.py.bak §5167-5286 — read_property_tag 函数
    - uasset_read.py.bak §64-68 — CPF_* 常量定义
    - uasset_read.py.bak §1513-1560 — parse_default_value 函数
    - uasset_read.py.bak §3146 — resolve_parent_class（已在 Task 3 中处理，确认）
    - src/uasset_read/serializers/property_tags.py — 现有内容
    - src/uasset_read/parsers/property_types.py — 现有内容
    - src/uasset_read/blueprint/variable_extractor.py — 现有内容
    - src/uasset_read/serializers/graph.py — Phase 31 创建的节点读取器
  </read_first>
  <action>
等价迁移以下 shim 中仍存在的函数/常量到新版模块：

### 4a. read_property_tag → serializers/property_tags.py

迁移旧版 `read_property_tag(archive, name_map, ...)` 到 `src/uasset_read/serializers/property_tags.py` 末尾。函数签名和逻辑等价迁移旧版 §5167-5286。

### 4b. _derive_node_name → parsers/property_types.py

迁移旧版 `_derive_node_name(node)` 辅助函数到 `src/uasset_read/parsers/property_types.py`。这是一个简单的名称推导函数。

### 4c. read_blueprint_variable → blueprint/variable_extractor.py

迁移旧版 `read_blueprint_variable(export, archive, ...)` 到 `src/uasset_read/blueprint/variable_extractor.py` 末尾。

### 4d. CPF_* 常量 → constants.py

在 `src/uasset_read/constants.py` 末尾追加以下常量（等价迁移旧版定义）：
```python
# 属性标志位（CPF = Class Property Flags）
CPF_InstancedReference = 0x0000000008000000
CPF_EditAnywhere = 0x0000000000000003
CPF_EditInstanceOnly = 0x0000000000000004
CPF_BlueprintReadWrite = 0x0000000000000040
CPF_BlueprintReadOnly = 0x0000000000000080
```

### 4e. parse_default_value → constants.py 或新建 parsers/helpers.py

迁移旧版 `parse_default_value(property_tag, archive, ...)` 函数。如果 parsers/helpers.py 不存在，追加到 `parsers/property_types.py` 末尾。

### 4f. read_k2node_* 函数确认

确认 Phase 31 的 `serializers/graph.py` 是否已包含以下函数：
- `read_k2node_call_function`
- `read_k2node_event`
- `read_k2node_knot`
- `read_edgraph_node_comment`
- `read_k2node_enhanced_input`

如果不存在（Phase 31 未创建这些函数），在 `serializers/graph.py` 末尾追加等价迁移实现。参考旧版 uasset_read.py 中对应区段。

### 4g. 更新 __init__.py

将 Task 3 Step C-1 中的 None 占位替换为实际导入：
```python
from .serializers.property_tags import read_property_tag
from .parsers.property_types import _derive_node_name, parse_default_value
from .blueprint.variable_extractor import read_blueprint_variable
from .constants import (
    CPF_InstancedReference, CPF_EditAnywhere, CPF_EditInstanceOnly,
    CPF_BlueprintReadWrite, CPF_BlueprintReadOnly,
)
```

将 `serializers/graph.py` 中的 read_k2node_* 函数导入：
```python
from .serializers.graph import (
    read_k2node_call_function, read_k2node_event, read_k2node_knot,
    read_edgraph_node_comment, read_k2node_enhanced_input,
)
```

取消注释 Task 3 Step C-2 中被注释的 __all__ 项。

### 4h. 更新 constants.py __all__ / serializers/property_tags.py __all__ 等

确保各模块 __init__.py 正确导出新函数。
  </action>
  <verify>
    <automated>python -c "from uasset_read import read_property_tag; print('OK:', read_property_tag)"</automated>
    <automated>python -c "from uasset_read import _derive_node_name, parse_default_value; print('OK')"</automated>
    <automated>python -c "from uasset_read import read_blueprint_variable; print('OK')"</automated>
    <automated>python -c "from uasset_read import CPF_InstancedReference, CPF_EditAnywhere, CPF_BlueprintReadWrite; print('OK')"</automated>
    <automated>python -c "from uasset_read import read_k2node_call_function; print('OK')"</automated>
    <automated>grep -c "def read_property_tag" src/uasset_read/serializers/property_tags.py</automated>
    <automated>grep -c "CPF_InstancedReference" src/uasset_read/constants.py</automated>
  </verify>
  <acceptance_criteria>
    - src/uasset_read/serializers/property_tags.py 包含 `def read_property_tag`
    - src/uasset_read/parsers/property_types.py 包含 `def _derive_node_name` 和 `def parse_default_value`
    - src/uasset_read/blueprint/variable_extractor.py 包含 `def read_blueprint_variable`
    - src/uasset_read/constants.py 包含 CPF_InstancedReference, CPF_EditAnywhere, CPF_EditInstanceOnly, CPF_BlueprintReadWrite, CPF_BlueprintReadOnly
    - src/uasset_read/serializers/graph.py 包含 read_k2node_call_function, read_k2node_event, read_k2node_knot, read_edgraph_node_comment, read_k2node_enhanced_input
    - src/uasset_read/__init__.py 中 read_property_tag 等不再是 None
    - `python -c "from uasset_read import read_property_tag, CPF_InstancedReference"` 成功执行
    - `python -c "from uasset_read import _derive_node_name, parse_default_value"` 成功执行
    - `python -c "from uasset_read import read_blueprint_variable"` 成功执行
  </acceptance_criteria>
  <done>所有 shim 函数已迁移到新版模块，__init__.py 无 None 占位</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .uasset file → FArchive | 不可信二进制输入，parse_uasset 通过 try/except 捕获所有异常 |
| extract_blueprint_graphs → archive | graph 模块可能不存在（Phase 31 未完成），使用 ImportError 容错 |
| parse_properties_from_export → export | 单个 export 解析失败不影响其他 export（per-export try/except） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | DoS | parse_uasset (malicious .uasset) | mitigate | 三层 try/except（VersionError/ParseError/Exception），所有错误捕获到 result.errors 不崩溃 |
| T-33-02 | DoS | extract_blueprint_graphs (missing module) | mitigate | ImportError 捕获返回空图列表，不中断解析管线 |
| T-33-03 | DoS | extract_component_transforms (malicious properties) | mitigate | 仅筛选 RelativeLocation/RelativeRotation/RelativeScale3D 属性名，未知属性跳过 |
| T-33-04 | Information Disclosure | parse_uasset (error messages) | accept | 错误信息包含文件路径和解析上下文，CLI 输出到 stderr 而非 stdout |
</threat_model>

<verification>
- `python -c "from uasset_read import parse_uasset"` 成功执行
- `python -c "from uasset_read import VectorValue, RotatorValue, ScaleValue"` 成功执行
- `python -c "from uasset_read import extract_component_transforms"` 成功执行
- `python -c "from uasset_read import find_main_blueprint_generated_class"` 成功执行
- `python -m pytest tests/test_phase13_transform.py -v --tb=short` 中关于导入的测试通过（不要求全部通过，因为需要真实 .uasset 文件）
- 无循环导入：`python -c "import uasset_read"` 无报错
</verification>

<success_criteria>
- parse_uasset.py 创建完成，包含完整解析管线（13 步骤）
- models/transforms.py 创建完成，包含 3 个 dataclass + format_transform_value
- blueprint/transform_parser.py 创建完成，包含 4 个函数
- find_main_blueprint_generated_class 追加到 serializers/object_resources.py
- __init__.py 追加所有新导出项（parse_uasset + transforms + transform_parser + BPGC 函数）
- `from uasset_read import parse_uasset` 不从旧版重导出
- 无循环导入，所有导入路径可用
</success_criteria>

<output>
After completion, create `.planning/phases/33-entry-test-adapt/33-01-SUMMARY.md`
</output>
