---
phase: "34"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_equivalence.py
autonomous: true
requirements:
  - 等价-01
  - 等价-02
  - 等价-03
  - 等价-04
  - 等价-05
  - 等价-06
  - 等价-07
user_setup: []

must_haves:
  truths:
    - "tests/test_equivalence.py exists and is importable by pytest"
    - "DiffRecorder class collects diffs without interrupting test flow"
    - "deep_compare recursively compares dict/list structures and returns diff list"
    - "CLI runners work for both legacy (uasset_read_legacy.py) and new (python -m uasset_read) commands"
    - "VERIFICATION.md report generation function produces valid markdown with diff categories"
    - "Test cases cover all 4 formats (JSON Full, JSON Summary, Text, Markdown) x synthetic+real assets"
  artifacts:
    - path: "tests/test_equivalence.py"
      provides: "All equivalence verification test infrastructure and test cases"
      exports:
        - "DiffRecorder class"
        - "deep_compare function"
        - "run_old_cli / run_new_cli functions"
        - "extract_mermaid_blocks function"
        - "build_verification_report function"
        - "6+ test functions covering all formats and assets"
  key_links:
    - from: "tests/test_equivalence.py"
      to: "uasset_read_legacy.py"
      via: "subprocess.run(['python', 'uasset_read_legacy.py', ...])"
      pattern: "subprocess\\.run.*uasset_read_legacy"
    - from: "tests/test_equivalence.py"
      to: "src/uasset_read/"
      via: "subprocess.run(['python', '-m', 'uasset_read', ...])"
      pattern: "subprocess\\.run.*-m.*uasset_read"
    - from: "tests/test_equivalence.py"
      to: ".planning/phases/34-equivalence-verification/VERIFICATION.md"
      via: "build_verification_report() file write"
      pattern: "VERIFICATION\\.md"
---

<objective>
创建等价验证测试基础设施，包含 DiffRecorder、deep_compare、CLI runners、测试用例和报告生成函数。

Purpose: 为 Phase 34 提供完整的自动化验证能力，覆盖 7 个需求 ID（等价-01 至 等价-07）
Output: tests/test_equivalence.py（单一测试文件，包含所有 helpers 和测试用例）
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-equivalence-verification/34-CONTEXT.md
@.planning/phases/34-equivalence-verification/34-RESEARCH.md
@.planning/phases/34-equivalence-verification/34-VALIDATION.md

# Locked decisions from CONTEXT.md
- D-01: 整体 diff + 逐字段对比（两策略结合）
- D-02: 全部四种格式 — JSON Full, JSON Summary, Text, Markdown
- D-03: 合成资产 + 真实资产结合
- D-04: 记录并继续 — 不中断验证流程
- D-05: VERIFICATION.md 报告
- D-06: 测试文件形式 (tests/test_equivalence.py)
- D-07: 验证函数在测试文件中，不放在 src/
- D-08: Phase 33 完成后才能执行

# Real asset paths (per 34-RESEARCH.md §Environment Availability)
- BP_FirstPersonCharacter: E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset
- BP_FirstPersonCameraManager: E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCameraManager.uasset
- BP_FirstPersonGameMode: E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonGameMode.uasset
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建测试基础设施 — DiffRecorder、deep_compare、CLI runners、报告生成</name>
  <files>tests/test_equivalence.py</files>
  <read_first>
    - tests/test_equivalence.py (will be created)
    - .planning/phases/34-equivalence-verification/34-RESEARCH.md (§Validation Architecture, §Pattern 1-2, §Code Examples)
    - uasset_read_legacy.py (line 7814-7938: legacy CLI main() function)
    - src/uasset_read/cli.py (new CLI entry point)
  </read_first>
  <action>
创建 tests/test_equivalence.py，包含以下 helper 类和函数（全部在测试文件中，不放在 src/，per D-07）：

**1. DiffRecorder 类**（per 34-RESEARCH.md Pattern 1）:
```python
class DiffRecorder:
    """收集所有差异，不中断验证流程（D-04）"""
    def __init__(self):
        self.diffs: list[dict] = []

    def record(self, asset_name, format_name, field_path, old_value=None,
               new_value=None, severity="diff", category="unknown", note=""):
        self.diffs.append({...})

    def get_by_severity(self, severity): ...
    def get_by_category(self, category): ...
    def get_known_diff_categories(self): ...  # 返回已知 9 类差异的分类名
```
severity 取值: "known"（已知设计变更）/ "improvement"（有意改进）/ "bug"（待修复）/ "diff"（需审查）
category 取值参考: "top_level_keys", "status", "graphs_summary_keys", "ObjectProperty_value", "execution_flows_format", "execution_flows_count", "mermaid_missing", "parent_class_str", "json_full_crash"

**2. deep_compare 函数**（per 34-RESEARCH.md Pattern 2）:
```python
def deep_compare(old, new, path=""):
    """递归对比任意 Python 对象，返回差异列表（D-01 逐字段对比）"""
    # 处理类型变化、dict 键增减/值变化、list 增减/元素变化、标量值变化
    # 返回 [{"path": ..., "type": "added/removed/value_changed/type_changed", ...}, ...]
```

**3. CLI 运行函数**:
```python
def run_old_cli(format_flag, asset_path):
    """运行旧版 CLI: python uasset_read_legacy.py {format_flag} {asset_path}"""
    # 使用 subprocess.run([...], capture_output=True, text=True, timeout=30)
    # asset_path 使用 as_posix() 转换 Windows 路径（per Pitfall 2）
    # 返回 (stdout, returncode)

def run_new_cli(format_flag, asset_path):
    """运行新版 CLI: python -m uasset_read {format_flag} {asset_path}"""
    # 同上
    # 返回 (stdout, returncode)
```
format_flag 取值: '--json', '--summary', '--text', '--markdown'

**4. compare_outputs 函数**（D-01 两策略结合）:
```python
def compare_outputs(old_output, new_output, format_name, asset_name, recorder):
    """对比新旧输出，使用两策略（D-01）：
    1. 整体 diff: 字符串完全相等则快速通过
    2. 逐字段对比: 不相等时递归对比 dict/list 结构

    对 JSON 格式：先 json.loads() 反序列化，再用 json.dumps(sort_keys=True, indent=2) 规范化
    对 Text/Markdown 格式：直接字符串对比 + 结构化解析
    """
```

**5. extract_mermaid_blocks 函数**（per Pitfall 4）:
```python
def extract_mermaid_blocks(markdown_output):
    """从 Markdown 输出提取所有 ```mermaid 代码块"""
    return re.findall(r'```mermaid\n(.*?)```', markdown_output, re.DOTALL)
```

**6. build_verification_report 函数**（per 34-RESEARCH.md Code Examples）:
```python
def build_verification_report(recorder, asset_count, test_date):
    """生成 VERIFICATION.md 报告"""
    # 路径: .planning/phases/34-equivalence-verification/VERIFICATION.md
    # 结构: 日期、资产数、总差异数、按 severity 分类的差异清单、结论
    # 已知差异表（9 类差异的固定描述）
```

**关键实现要求:**
- 使用 `json.dumps(obj, sort_keys=True, indent=2, ensure_ascii=False)` 确保 JSON 序列化一致性（per Pitfall 1）
- Windows 路径使用 `pathlib.Path.as_posix()` 避免转义问题（per Pitfall 2）
- 所有差异记录包含 severity、category、field_path、old_value、new_value
- 不抛出 AssertionError —— 所有测试使用 recorder 记录差异后 assert True（per D-04 记录并继续）
- 全局 `recorder = DiffRecorder()` 模块级变量，所有测试共享
- 使用 `atexit` 或 `conftest.py` 风格的 session 钩子在测试结束后写 VERIFICATION.md
  - 推荐方案：在测试文件中使用 `pytest_sessionfinish` hook —— 但 conftest.py 中更合适
  - 实际方案：在测试文件末尾使用 `atexit.register(lambda: _write_report())` 确保进程退出时写入
  - 注意：每个测试函数独立运行 recorder，最终统一写入

**已知 9 类差异自动分类规则（在 compare_outputs 中实现）:**
- #1 top_level_keys: imports/soft_references/circular_deps 移除 → severity="known", category="top_level_keys"
- #2 status: fail→success → severity="improvement", category="status"
- #3 graphs_summary_keys: 2键→8键扩展 → severity="improvement", category="graphs_summary_keys"
- #4 ObjectProperty_value: dict→int → severity="diff", category="ObjectProperty_value" (需人工审查)
- #5 execution_flows_format: event型→node型 → severity="diff", category="execution_flows_format"
- #6 execution_flows_count: 7→4 → severity="diff", category="execution_flows_count"
- #7 mermaid_missing: mermaid 图表缺失 → severity="bug", category="mermaid_missing"
- #8 parent_class_str: str(dict) bug → severity="bug", category="parent_class_str"
- #9 json_full_crash: 两版都崩溃 → severity="known", category="json_full_crash"
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'tests'); from test_equivalence import DiffRecorder, deep_compare, run_old_cli, run_new_cli, extract_mermaid_blocks, build_verification_report; print('All helpers importable')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_equivalence.py 文件存在
    - grep -c "class DiffRecorder" tests/test_equivalence.py == 1
    - grep -c "def deep_compare" tests/test_equivalence.py == 1
    - grep -c "def run_old_cli" tests/test_equivalence.py == 1
    - grep -c "def run_new_cli" tests/test_equivalence.py == 1
    - grep -c "def extract_mermaid_blocks" tests/test_equivalence.py == 1
    - grep -c "def build_verification_report" tests/test_equivalence.py == 1
    - grep -c "subprocess" tests/test_equivalence.py >= 2
    - grep -c "json.dumps.*sort_keys" tests/test_equivalence.py >= 1
    - grep -c "as_posix" tests/test_equivalence.py >= 1
    - 文件可被 pytest 导入且无语法错误: python -m py_compile tests/test_equivalence.py 返回 0
  </acceptance_criteria>
  <done>
    tests/test_equivalence.py 包含所有 6 个 helper 类和函数，可被 pytest 导入，无语法错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建等价验证测试用例 — 覆盖全部格式与资产组合</name>
  <files>tests/test_equivalence.py</files>
  <read_first>
    - tests/test_equivalence.py (Task 1 已创建的版本)
    - tests/test_uasset_read.py (create_test_uasset 函数参考)
    - .planning/phases/34-equivalence-verification/34-RESEARCH.md §Verified Difference Inventory
    - E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset (真实资产路径确认)
  </read_first>
  <action>
在 tests/test_equivalence.py 中追加以下测试函数（复用 Task 1 的 helpers）：

**全局变量:**
```python
import atexit
recorder = DiffRecorder()

# 真实资产路径（使用 pathlib.Path.as_posix() 兼容 Windows）
REAL_ASSETS = {
    "BP_FirstPersonCharacter": Path("E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset").as_posix(),
    "BP_FirstPersonCameraManager": Path("E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCameraManager.uasset").as_posix(),
}
```

**atexit 报告写入:**
```python
def _write_verification_report():
    """测试进程退出时写 VERIFICATION.md（D-05）"""
    import os
    report_dir = ".planning/phases/34-equivalence-verification"
    os.makedirs(report_dir, exist_ok=True)
    report_path = os.path.join(report_dir, "VERIFICATION.md")
    report = build_verification_report(recorder, asset_count=3, test_date=datetime.now().strftime('%Y-%m-%d'))
    with open(report_path, 'w', encoding='utf-8') as f:
        f.write(report)

atexit.register(_write_verification_report)
```

**测试 1: test_json_full_synthetic (等价-01, 等价-05)**
```python
def test_json_full_synthetic():
    """JSON Full 格式 — 合成资产等价验证"""
    # 1. 用 create_test_uasset() 创建合成资产
    # 2. run_old_cli('--json', path) 和 run_new_cli('--json', path)
    # 3. 如果两版都返回 exit code 1 → 记录为 known (json_full_crash)，pass
    # 4. 如果仅新版崩溃 → 记录为 bug，pass (per D-04)
    # 5. 如果都成功 → compare_outputs(old_stdout, new_stdout, 'json_full', 'synthetic', recorder)
    # 6. assert True (per D-04: 记录并继续)
```

**测试 2: test_json_summary_equivalence (等价-02)**
```python
def test_json_summary_equivalence():
    """JSON Summary 格式 — 合成+真实资产等价验证"""
    # 对每个资产（合成+BP_FirstPersonCharacter+BP_FirstPersonCameraManager）:
    #   1. run_old_cli('--summary', path) 和 run_new_cli('--summary', path)
    #   2. 解析 JSON: json.loads(old_stdout), json.loads(new_stdout)
    #   3. compare_outputs 自动对比
    #   4. 预期差异: #1 (top_level_keys), #2 (status), #3 (graphs_summary_keys), #5 (execution_flows_format), #6 (execution_flows_count)
    #   5. 记录到 recorder，assert True
```

**测试 3: test_text_equivalence (等价-03)**
```python
def test_text_equivalence():
    """Text 格式 — 合成+真实资产等价验证"""
    # 对每个资产:
    #   1. run_old_cli('--text', path) 和 run_new_cli('--text', path)
    #   2. compare_outputs(old_stdout, new_stdout, 'text', asset_name, recorder)
    #   3. 预期差异: #4 (ObjectProperty_value, 227 行 diff), #8 (parent_class_str, 18 行 diff)
    #   4. assert True
```

**测试 4: test_markdown_equivalence (等价-04)**
```python
def test_markdown_equivalence():
    """Markdown 格式 — 合成+真实资产等价验证"""
    # 对每个资产:
    #   1. run_old_cli('--markdown', path) 和 run_new_cli('--markdown', path)
    #   2. 检查 mermaid 块: extract_mermaid_blocks(old) vs extract_mermaid_blocks(new)
    #   3. compare_outputs 对比整体输出
    #   4. 预期差异: #7 (mermaid_missing), #8 (parent_class_str)
    #   5. 显式记录 mermaid 块存在性差异（有/无）
    #   6. assert True
```

**测试 5: test_synthetic_all_formats (等价-05)**
```python
def test_synthetic_all_formats():
    """合成资产 — 全部四种格式快速验证"""
    # 对 4 种格式各运行一次对比
    # 合成资产应仅有 #1 (top_level_keys) 差异
    # 确认合成资产验证通过（near-equivalent）
    # assert True
```

**测试 6: test_real_assets_all_formats (等价-06)**
```python
def test_real_assets_all_formats():
    """真实资产 — 全部格式验证（BP_FirstPersonCharacter 为主）"""
    # 使用 REAL_ASSETS 中的每个真实资产
    # 跳过不存在的资产（pytest.skip）
    # 对 4 种格式各运行一次对比
    # 预期所有 9 类差异可能出现
    # assert True
```

**测试 7: test_verification_report_generated (等价-07)**
```python
def test_verification_report_generated():
    """验证 VERIFICATION.md 报告在测试结束后生成"""
    # 此测试检查 atexit 注册是否正常工作
    # 实际报告由 atexit hook 在进程退出时写入
    # 这里仅确认 _write_verification_report 函数存在
    assert callable(_write_verification_report)
```

**关键实现要求:**
- 每个测试函数内部使用 try/except 包裹 CLI 调用，确保异常被捕获并记录到 recorder（per D-04）
- 使用 `pytest.mark.parametrize` 可简化多资产测试（如 `@pytest.mark.parametrize('asset_name,asset_path', [...])`)
- 测试函数命名规范: `test_{format}_{asset_type}_equivalence`
- 对真实资产，先用 `os.path.exists(path)` 检查，不存在则 `pytest.skip`
- 合成资产测试使用 `try/finally` 确保 `cleanup_test_file(path)` 被调用
- 所有测试函数末尾使用 `assert True` 确保 pytest 认为通过（差异在 recorder 中记录）
  </action>
  <verify>
    <automated>python -m pytest tests/test_equivalence.py --collect-only -q 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_equivalence.py 包含至少 7 个 test_ 函数
    - grep -c "def test_" tests/test_equivalence.py >= 7
    - grep -c "recorder.record" tests/test_equivalence.py >= 1
    - grep -c "atexit.register" tests/test_equivalence.py == 1
    - grep -c "assert True" tests/test_equivalence.py >= 5
    - grep -c "create_test_uasset" tests/test_equivalence.py >= 1
    - grep -c "as_posix" tests/test_equivalence.py >= 2
    - python -m py_compile tests/test_equivalence.py 返回 0
    - python -m pytest tests/test_equivalence.py --collect-only -q 输出包含至少 7 个测试用例名
  </acceptance_criteria>
  <done>
    tests/test_equivalence.py 包含 7+ 个测试用例，覆盖等价-01 至 等价-07 全部需求，可被 pytest 收集和运行
  </done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess → old CLI | 旧版代码可能包含未捕获的异常，需要 timeout 和 error handling |
| subprocess → new CLI | 新版代码可能崩溃（已知 json_full_crash），需要 graceful handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | subprocess.run() | mitigate | 设置 timeout=30 防止无限挂起，捕获 subprocess.TimeoutExpired |
| T-34-02 | Information Disclosure | diff output | accept | 测试输出仅包含 diff 信息，无敏感数据 |
| T-34-03 | Elevation of Privilege | file path handling | mitigate | 使用 pathlib.Path 规范化路径，避免路径注入 |
</threat_model>

<verification>
- Task 1: `python -c "from test_equivalence import ..."` 导入所有 6 个 helpers 无错误
- Task 2: `python -m pytest tests/test_equivalence.py --collect-only -q` 列出 7+ 测试用例
- Task 2: `python -m py_compile tests/test_equivalence.py` 无语法错误
- Phase gate: `python -m pytest tests/test_equivalence.py -v --tb=short` 全部通过（或已记录差异）
</verification>

<success_criteria>
- tests/test_equivalence.py 存在且包含 DiffRecorder、deep_compare、CLI runners、report generation
- 7+ 测试用例覆盖 等价-01 至 等价-07 全部需求
- 测试可被 pytest 收集和执行
- 执行后生成 VERIFICATION.md 报告
- 使用 Python 标准库（difflib, json, subprocess, tempfile, re, atexit），零额外依赖
</success_criteria>

<output>
After completion, create `.planning/phases/34-equivalence-verification/34-01-SUMMARY.md`
</output>
