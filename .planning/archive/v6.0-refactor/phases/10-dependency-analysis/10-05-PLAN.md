---
phase: 10-dependency-analysis
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified: [uasset_read.py, tests/test_dependency_analysis.py]
autonomous: true
gap_closure: true
requirements: [DEPS-02]

must_haves:
  truths:
    - "read_soft_object_paths() 正确处理 UE5 >= 1007 的 FTopLevelAssetPath 格式"
    - "UE5 >= 1007 时读取 PackageName(FName) + AssetName(FName) + SubPathString"
    - "UE5 < 1007 时读取 AssetPathName(FName) + SubPathWide(FWideString)"
    - "asset_path 字段格式为 PackageName.AssetName 或 PackageName（当 AssetName 为空）"
  artifacts:
    - path: "uasset_read.py"
      contains: "FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES"
      exports: ["FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES"]
    - path: "uasset_read.py"
      contains: "package_name = archive.read_name(name_map)"
      pattern: "package_name.*read_name"
    - path: "uasset_read.py"
      contains: "asset_name = archive.read_name(name_map)"
      pattern: "asset_name.*read_name"
    - path: "tests/test_dependency_analysis.py"
      contains: "test_read_soft_object_paths_ue5_1007"
      exports: ["test_read_soft_object_paths_ue5_1007"]
  key_links:
    - from: "read_soft_object_paths"
      to: "PackageFileSummary.file_version_ue5"
      via: "版本条件判断"
      pattern: "file_version_ue5"
    - from: "read_soft_object_paths"
      to: "FTopLevelAssetPath"
      via: "PackageName + AssetName 双 FName 序列化"
      pattern: "read_name.*read_name"
---

<objective>
修复 read_soft_object_paths() 函数，正确处理 UE5 >= 1007 的 FTopLevelAssetPath 格式。

Purpose: 修复 ALL-V2-UAT.md Gap #1 - SoftObjectPath UE5 >= 1007 格式错误
Output: read_soft_object_paths() 正确读取 FTopLevelAssetPath（两个 FName）格式
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dependency-analysis/10-CONTEXT.md
@.planning/phases/ALL-V2-UAT.md

<gap_source>
**Gap from ALL-V2-UAT.md:**
- truth: "SoftObjectPath UE5 >= 1007 格式正确解析"
- status: failed
- reason: "read_soft_object_paths 只读取一个 FName，应读取两个 FName（PackageName + AssetName）"
- severity: major
- root_cause: "FTopLevelAssetPath::operator<< 序列化为 Ar << PackageName << AssetName，需两个 FName"
</gap_source>

<ue_source_reference>
**UE 5.7 源码 - ObjectVersion.h:**
```cpp
// UE5 版本枚举（从 1000 开始）
INITIAL_VERSION = 1000,
NAMES_REFERENCED_FROM_EXPORT_DATA = 1001,
PAYLOAD_TOC = 1002,
OPTIONAL_RESOURCES = 1003,
LARGE_WORLD_COORDINATES = 1004,
REMOVE_OBJECT_EXPORT_PACKAGE_GUID = 1005,
TRACK_OBJECT_EXPORT_IS_INHERITED = 1006,
FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES = 1007,  // 关键版本
ADD_SOFTOBJECTPATH_LIST = 1008,
```

**UE 5.7 源码 - SoftObjectPath.cpp L555-591:**
```cpp
else if (Ar.IsLoading() && Ar.UEVer() < EUnrealEngineObjectUE5Version::FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES)
{
    // UE5 < 1007: 单 FName + FWideString 格式
    FName AssetPathName;
    Ar << AssetPathName;
    AssetPath = WriteToString<FName::StringBufferSize>(AssetPathName).ToView();

    FWideString SubPathWide(SubPathString);
    Ar << SubPathWide;
    SubPathString = FUtf8String(SubPathWide);
}
else
{
    // UE5 >= 1007: FTopLevelAssetPath (两个 FName) + FUtf8String 格式
    Ar << AssetPath;  // FTopLevelAssetPath = PackageName + AssetName
    Ar << SubPathString;  // FUtf8String
}
```

**FTopLevelAssetPath 序列化（TopLevelAssetPath.h:144）:**
```cpp
friend FArchive& operator<<(FArchive& Ar, FTopLevelAssetPath& Path)
{
    return Ar << Path.PackageName << Path.AssetName;  // 两个 FName
}
```
</ue_source_reference>

<interfaces>
From uasset_read.py L74 - 现有版本常量:
```python
UE5_ADD_SOFTOBJECTPATH_LIST = 1008  # 需添加 FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES = 1007
```

From uasset_read.py L1633-1676 - 现有 read_soft_object_paths():
```python
def read_soft_object_paths(...) -> List[Dict]:
    # 当前实现只读取单个 FName
    asset_path = archive.read_name(name_map)  # 错误：只读一个
    sub_path = archive.read_fstring()
```

From uasset_read.py L153-520 - FArchive 方法:
```python
read_name(name_map: List[str]) -> str  # FName 解析
read_fstring() -> str  # FString 解析
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES 常量</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L70-80 — 现有 UE5 版本常量定义
    - UE ObjectVersion.h L68 — FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES = 1007
  </read_first>
  <action>
    在 uasset_read.py 版本常量区域（约 L74）添加：

    ```python
    # UE5 SoftObjectPath 格式版本（ObjectVersion.h L68）
    UE5_FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES = 1007
    ```

    位置：在 UE5_ADD_SOFTOBJECTPATH_LIST 常量之前（便于理解版本演进）
  </action>
  <verify>
    <automated>grep -n "UE5_FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES = 1007" uasset_read.py</automated>
  </verify>
  <acceptance_criteria>
    - uasset_read.py 包含 "UE5_FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES = 1007" 常量定义
    - 常量位于 UE5_ADD_SOFTOBJECTPATH_LIST 之前
    - 常量名称与 UE 源码命名一致（便于对照）
  </acceptance_criteria>
  <done>版本常量添加完成，值为 1007，对应 UE5 FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES 版本。</done>
</task>

<task type="auto">
  <name>Task 2: 重构 read_soft_object_paths() 函数</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L1633-1676 — 现有 read_soft_object_paths() 实现
    - UE SoftObjectPath.cpp L555-591 — 版本分支逻辑
    - UE TopLevelAssetPath.h L144 — FTopLevelAssetPath 序列化
    - 10-CONTEXT.md D-10-06~09 — 现有设计决策
  </read_first>
  <action>
    重构 read_soft_object_paths() 函数，添加版本分支：

    ```python
    def read_soft_object_paths(
        archive: FArchive,
        summary: PackageFileSummary,
        name_map: List[str]
    ) -> List[Dict]:
        """
        读取 SoftObjectPaths 数组（DEPS-02）。

        Per D-10-06: 实现完整解析
        Per D-10-07: {asset_path, sub_path} 格式
        Per D-10-09: 仅 UE5 >= 1008 时解析

        UE5 版本格式变化（SoftObjectPath.cpp L555-591）：
        - UE5 < 1007: AssetPathName(FName) + SubPathWide(FWideString)
        - UE5 >= 1007: PackageName(FName) + AssetName(FName) + SubPathString(FUtf8String)

        Args:
            archive: FArchive 实例
            summary: PackageFileSummary 实例
            name_map: 已解析的名称表

        Returns:
            List[Dict]: [{"asset_path": str, "sub_path": str}]
        """
        is_ue5_file = summary.legacy_file_version <= -8
        if not is_ue5_file or summary.file_version_ue5 < UE5_ADD_SOFTOBJECTPATH_LIST:
            return []  # UE4 文件或 UE5 < 1008 无 SoftObjectPaths

        if summary.soft_object_paths_count <= 0 or summary.soft_object_paths_offset <= 0:
            return []

        archive.seek(summary.soft_object_paths_offset)

        soft_refs = []
        for _ in range(summary.soft_object_paths_count):
            # UE5 >= 1007: FTopLevelAssetPath 格式（两个 FName）
            if summary.file_version_ue5 >= UE5_FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES:
                package_name = archive.read_name(name_map)
                asset_name = archive.read_name(name_map)
                # 组合为完整 asset_path：PackageName.AssetName 或 PackageName
                if asset_name:
                    asset_path = f"{package_name}.{asset_name}"
                else:
                    asset_path = package_name
                # SubPathString: FUtf8String（与 FString 格式相同）
                sub_path = archive.read_fstring()
            else:
                # UE5 < 1007: 单 FName + FWideString 格式
                asset_path = archive.read_name(name_map)
                # FWideString 格式与 FString 相同（UTF-16 LE，但实际存储常为 UTF-8）
                sub_path = archive.read_fstring()

            soft_refs.append({
                "asset_path": asset_path,
                "sub_path": sub_path
            })

        return soft_refs
    ```

    关键修改：
    1. 添加 UE5_FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES 版本检查
    2. >= 1007 时读取两个 FName（PackageName + AssetName）
    3. 组合 asset_path 为 "PackageName.AssetName" 格式
    4. < 1007 时保持原逻辑（单 FName + FString）
  </action>
  <verify>
    <automated>grep -n "UE5_FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES" uasset_read.py</automated>
    <automated>grep -c "package_name = archive.read_name(name_map)" uasset_read.py</automated>
    <automated>grep -c "asset_name = archive.read_name(name_map)" uasset_read.py</automated>
  </verify>
  <acceptance_criteria>
    - read_soft_object_paths() 包含 UE5 >= 1007 版本分支
    - >= 1007 分支读取两个 FName（package_name, asset_name）
    - asset_path 组合格式正确（PackageName.AssetName）
    - < 1007 分支保持原逻辑（单 FName）
    - 函数 Docstring 包含版本格式说明
  </acceptance_criteria>
  <done>read_soft_object_paths() 函数重构完成，正确处理 UE5 >= 1007 的 FTopLevelAssetPath 格式。</done>
</task>

<task type="auto">
  <name>Task 3: 添加 UE5 >= 1007 格式单元测试</name>
  <files>tests/test_dependency_analysis.py</files>
  <read_first>
    - tests/test_dependency_analysis.py — 现有 read_soft_object_paths 测试
    - uasset_read.py L1633-1700 — 新的 read_soft_object_paths 实现
  </read_first>
  <action>
    在 test_dependency_analysis.py 中添加新测试：

    ```python
    def test_read_soft_object_paths_ue5_1007():
        """测试 UE5 >= 1007 的 FTopLevelAssetPath 格式解析。"""
        # 创建 UE5 >= 1007 测试数据
        name_map = ["/Game/Maps/TestMap", "TestMapAsset", "SubObject"]
        summary = create_test_summary(
            legacy_file_version=-8,
            file_version_ue5=1007,
            soft_object_paths_count=1,
            soft_object_paths_offset=100
        )

        # 构造二进制数据：PackageName(FName) + AssetName(FName) + SubPathString(FString)
        data = bytearray()
        # PackageName: index=0, number=0
        data.extend(struct.pack('<I', 0))  # index
        data.extend(struct.pack('<I', 0))  # number
        # AssetName: index=1, number=0
        data.extend(struct.pack('<I', 1))  # index
        data.extend(struct.pack('<I', 0))  # number
        # SubPathString: FString (length + UTF-8)
        sub_path = "SubObject"
        data.extend(struct.pack('<i', len(sub_path) + 1))
        data.extend(sub_path.encode('utf-8'))
        data.extend(b'\x00')  # null terminator

        archive = FArchive(io.BytesIO(data))
        archive.name_map = name_map

        result = read_soft_object_paths(archive, summary, name_map)

        assert len(result) == 1
        # 验证 FTopLevelAssetPath 组合格式
        assert result[0]["asset_path"] == "/Game/Maps/TestMap.TestMapAsset"
        assert result[0]["sub_path"] == "SubObject"

    def test_read_soft_object_paths_ue5_1006():
        """测试 UE5 < 1007 的旧格式（单 FName + FString）。"""
        name_map = ["/Game/Maps/TestMap"]
        summary = create_test_summary(
            legacy_file_version=-8,
            file_version_ue5=1006,
            soft_object_paths_count=1,
            soft_object_paths_offset=100
        )

        # 构造二进制数据：AssetPathName(FName) + SubPathWide(FString)
        data = bytearray()
        # AssetPathName: index=0, number=0
        data.extend(struct.pack('<I', 0))
        data.extend(struct.pack('<I', 0))
        # SubPathWide: FString 格式
        sub_path = "SubObject"
        data.extend(struct.pack('<i', len(sub_path) + 1))
        data.extend(sub_path.encode('utf-8'))
        data.extend(b'\x00')

        archive = FArchive(io.BytesIO(data))
        archive.name_map = name_map

        result = read_soft_object_paths(archive, summary, name_map)

        assert len(result) == 1
        # 验证旧格式：asset_path 直接从 FName 读取
        assert result[0]["asset_path"] == "/Game/Maps/TestMap"
        assert result[0]["sub_path"] == "SubObject"
    ```

    确保测试覆盖：
    - UE5 >= 1007 的新格式（双 FName）
    - UE5 < 1007 的旧格式（单 FName）
    - asset_path 组合逻辑
  </action>
  <verify>
    <automated>grep -n "test_read_soft_object_paths_ue5_1007" tests/test_dependency_analysis.py</automated>
    <automated>grep -n "test_read_soft_object_paths_ue5_1006" tests/test_dependency_analysis.py</automated>
    <automated>python -m pytest tests/test_dependency_analysis.py::test_read_soft_object_paths_ue5_1007 -v</automated>
    <automated>python -m pytest tests/test_dependency_analysis.py::test_read_soft_object_paths_ue5_1006 -v</automated>
  </verify>
  <acceptance_criteria>
    - test_dependency_analysis.py 包含 test_read_soft_object_paths_ue5_1007 测试
    - test_dependency_analysis.py 包含 test_read_soft_object_paths_ue5_1006 测试
    - 两个测试都通过（pytest exit code 0）
    - 测试验证 FTopLevelAssetPath 组合格式
  </acceptance_criteria>
  <done>UE5 >= 1007 格式单元测试添加完成，覆盖新旧两种格式。</done>
</task>

<task type="auto">
  <name>Task 4: 运行完整测试套件验证</name>
  <files>tests/test_dependency_analysis.py, tests/test_uasset_read.py</files>
  <read_first>
    - tests/test_dependency_analysis.py — Phase 10 测试文件
  </read_first>
  <action>
    运行完整测试套件：
    ```bash
    python -m pytest tests/test_dependency_analysis.py -v
    python -m pytest tests/test_uasset_read.py -v --tb=short
    ```

    验证：
    1. 所有 Phase 10 测试通过（包括新测试）
    2. 无回归（现有测试仍通过）
    3. 测试覆盖率保持或提升
  </action>
  <verify>
    <automated>python -m pytest tests/test_dependency_analysis.py -v --tb=short 2>&1 | tail -20</automated>
    <automated>python -m pytest tests/test_uasset_read.py -x -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_dependency_analysis.py 所有测试通过
    - test_uasset_read.py 无回归（至少 160+ passed）
    - 无新增 failed 测试
  </acceptance_criteria>
  <done>测试套件验证完成，SoftObjectPath 格式修复无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FArchive → 解析函数 | 二进制数据从文件读取后进入解析函数，需边界验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | soft_object_paths_count | mitigate | 边界检查：count <= 0 返回空数组（已有） |
| T-10-05-02 | Tampering | soft_object_paths_offset | mitigate | 边界检查：offset <= 0 返回空数组（已有） |
| T-10-05-03 | Tampering | PackageName FName index | mitigate | archive.read_name() 已有边界验证（已有） |
| T-10-05-04 | Tampering | AssetName FName index | mitigate | archive.read_name() 已有边界验证（已有） |
</threat_model>

<verification>
- UE 源码对照验证版本常量值（1007）
- UE 源码对照验证序列化格式（双 FName）
- 单元测试覆盖 UE5 >= 1007 和 < 1007 两种格式
- pytest 验证无回归
</verification>

<success_criteria>
- read_soft_object_paths() 正确处理 UE5 >= 1007 FTopLevelAssetPath 格式
- asset_path 组合格式正确（PackageName.AssetName）
- 单元测试覆盖两种版本格式
- 所有测试通过，无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/10-dependency-analysis/10-05-SUMMARY.md`
</output>