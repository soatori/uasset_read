---
phase: 10-dependency-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [uasset_read.py]
autonomous: true
requirements: [DEPS-01, DEPS-02, DEPS-03, DEPS-04]
user_setup: []

must_haves:
  truths:
    - "ParseResult 包含 imports 字段"
    - "ParseResult 包含 soft_references 字段"
    - "ParseResult 包含 circular_deps 字段"
    - "imports 字段默认为空数组"
    - "soft_references 字段默认为空数组"
    - "circular_deps 字段默认为空数组"
  artifacts:
    - path: "uasset_read.py"
      contains: "imports: List[Dict] = field(default_factory=list)"
      min_lines: 1
    - path: "uasset_read.py"
      contains: "soft_references: List[Dict] = field(default_factory=list)"
      min_lines: 1
    - path: "uasset_read.py"
      contains: "circular_deps: List[List[str]] = field(default_factory=list)"
      min_lines: 1
  key_links:
    - from: "ParseResult"
      to: "format_json_full"
      via: "imports/soft_references/circular_deps 字段"
      pattern: "imports|soft_references|circular_deps"
---

<objective>
扩展 ParseResult dataclass，添加依赖分析相关的三个顶层字段。

Purpose: 为 Phase 10 依赖分析提供数据容器，支持 imports/soft_references/circular_deps 三种依赖信息的存储和输出（D-10-05/08/13）。
Output: ParseResult dataclass 扩展，包含三个新字段。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dependency-analysis/10-CONTEXT.md
@.planning/phases/10-dependency-analysis/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From uasset_read.py L1035-1055 - ParseResult dataclass:
```python
@dataclass
class ParseResult:
    """解析结果（D-15 部分结果）。"""
    summary: Optional[PackageFileSummary] = None
    name_map: List[str] = field(default_factory=list)
    import_map: List[ObjectImport] = field(default_factory=list)
    export_map: List[ObjectExport] = field(default_factory=list)
    errors: List[str] = field(default_factory=list)
    blueprint: Optional["BlueprintMetadata"] = None
    graphs: List["UEdGraph"] = field(default_factory=list)  # Phase 7
    is_success: bool = False
    mmap_used: bool = False
    mmap_warning: Optional[str] = None
    warnings: List[str] = field(default_factory=list)
```

From uasset_read.py L646-657 - ObjectImport dataclass:
```python
@dataclass
class ObjectImport:
    """FObjectImport 导入表条目（CORE-04）。"""
    class_package: str      # 来源包名
    class_name: str         # 类名
    outer_index: PackageIndex  # Outer 引用
    object_name: str        # 对象名
```

From typing module (standard):
```python
List[Dict]  # imports 和 soft_references 字段类型
List[List[str]]  # circular_deps 字段类型（循环路径数组）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 ParseResult dataclass</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L1035-1055 — ParseResult dataclass 当前定义
    - uasset_read.py L1049 — graphs 字段模式（Phase 7 扩展示例）
    - 10-CONTEXT.md D-10-05/08/13 — 顶层字段设计决策
  </read_first>
  <action>
    在 ParseResult dataclass 中添加三个新字段，位于 graphs 字段之后、is_success 字段之前：

    位置：uasset_read.py L1055 附近（warnings 字段之后）

    添加以下三行（D-10-05/08/13 锁定）：
    ```python
    # Phase 10: 依赖分析字段（D-10-05/08/13）
    imports: List[Dict] = field(default_factory=list)           # D-10-05: ImportMap 依赖列表
    soft_references: List[Dict] = field(default_factory=list)   # D-10-08: SoftObjectPaths 软引用列表
    circular_deps: List[List[str]] = field(default_factory=list) # D-10-13: 循环依赖路径
    ```

    注意：
    - 字段顺序遵循现有模式（功能相关字段分组）
    - 使用 List[Dict] 类型（而非 List[ObjectImport]），因为输出格式为 dict
    - 使用 field(default_factory=list) 确保默认值为空数组而非共享列表
    - 添加注释标注 Phase 和决策 ID
  </action>
  <verify>
    <automated>grep -n "imports: List\[Dict\]" uasset_read.py</automated>
    <automated>grep -n "soft_references: List\[Dict\]" uasset_read.py</automated>
    <automated>grep -n "circular_deps: List\[List\[str\]\]" uasset_read.py</automated>
  </verify>
  <acceptance_criteria>
    - uasset_read.py 包含 "imports: List[Dict] = field(default_factory=list)" 行
    - uasset_read.py 包含 "soft_references: List[Dict] = field(default_factory=list)" 行
    - uasset_read.py 包含 "circular_deps: List[List[str]] = field(default_factory=list)" 行
    - 字段位于 ParseResult dataclass 内部，graphs 字段之后
    - 每行包含 Phase 10 注释标注
  </acceptance_criteria>
  <done>ParseResult dataclass 扩展完成，包含三个依赖分析字段，所有 grep 验证通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增边界 | 本任务仅扩展 dataclass 字段，不涉及数据解析或外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | N/A | ParseResult fields | accept | 仅数据容器扩展，无安全风险 |
</threat_model>

<verification>
- grep 验证三个新字段存在
- 字段类型正确（List[Dict], List[Dict], List[List[str]]）
- 字段位置正确（graphs 之后）
- 注释标注 Phase 10 和决策 ID
</verification>

<success_criteria>
- ParseResult 包含 imports/soft_references/circular_deps 三个新字段
- 字段默认值为空数组
- 字段类型符合 D-10-05/08/13 定义
- grep 验证命令返回正确行号
</success_criteria>

<output>
完成后创建 `.planning/phases/10-dependency-analysis/10-01-SUMMARY.md`
</output>