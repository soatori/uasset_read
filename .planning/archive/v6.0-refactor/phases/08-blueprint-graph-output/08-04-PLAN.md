---
phase: 08-blueprint-graph-output
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified: [uasset_read.py, tests/test_output_formatting.py]
autonomous: true
requirements: [OUT2-04]
user_setup: []

must_haves:
  truths:
    - "CLI 支持 --graph 标志"
    - "--graph 不与 --json/--text/--summary 互斥（可组合使用）"
    - "--graph 默认仅输出 graphs 字段（JSON 格式）"
    - "--graph --verbose 输出完整结构 + graphs"
    - "--graph --json 输出完整 JSON + graphs"
  artifacts:
    - path: "uasset_read.py"
      provides: "create_parser() 添加 --graph 标志"
      contains: "add_argument('--graph'"
      pattern: "action='store_true'"
    - path: "uasset_read.py"
      provides: "main() 添加 --graph 分支逻辑"
      contains: "if args.graph:"
      pattern: "format_graphs_json"
    - path: "tests/test_output_formatting.py"
      provides: "CLI --graph 测试"
      contains: "test_cli_graph_flag"
  key_links:
    - from: "main()"
      to: "args.graph"
      via: "检查 --graph 标志是否设置"
      pattern: "args\\.graph"
    - from: "main()"
      to: "format_graphs_json()"
      via: "输出图数据"
      pattern: "format_graphs_json\\(result\\.graphs\\)"
    - from: "create_parser()"
      to: "add_argument('--graph')"
      via: "定义 CLI 标志"
      pattern: "parser\\.add_argument\\('--graph'"
---

<objective>
扩展 CLI 支持 --graph 标志，允许用户仅输出蓝图图数据，满足 OUT2-04 需求。

Purpose: 满足 OUT2-04（CLI 支持 --graph 标志仅输出蓝图图数据）
Output: create_parser() 扩展添加 --graph 标志，main() 扩展添加 --graph 分支逻辑
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-blueprint-graph-output/08-CONTEXT.md
@.planning/phases/08-blueprint-graph-output/08-RESEARCH.md

<interfaces>
<!-- Phase 4 已实现的 CLI 结构 -->
<!-- Phase 8 Wave 1/2 已实现的图数据格式化 -->

From uasset_read.py L3530-3560 (create_parser 当前实现):
```python
def create_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(
        prog='uasset_read',
        description='Parse Unreal Engine .uasset files and output structured data'
    )

    # Positional: file path
    parser.add_argument('file', help='Path to .uasset file to parse')

    # Mutually exclusive output flags (D-24)
    group = parser.add_mutually_exclusive_group(required=False)
    group.add_argument('--json', action='store_true', help='Output full JSON structure')
    group.add_argument('--text', action='store_true', help='Output YAML-style text (default)')
    group.add_argument('--summary', action='store_true', help='Output compact summary format')

    # Optional flags (D-27)
    parser.add_argument('--verbose', action='store_true', help='Include extra detail fields')
    parser.add_argument('--output', metavar='FILE', help='Write output to file instead of stdout')
    parser.add_argument('--export', metavar='INDEX', type=int, help='Output only specific export by index')

    return parser
    # Phase 8: 需添加 --graph 标志（不加入互斥组）
```

From uasset_read.py L3563-3609 (main 当前实现):
```python
def main():
    parser = create_parser()
    args = parser.parse_args()

    # ... file check and parse ...

    # Select formatter (D-24)
    if args.json:
        output_str = json.dumps(format_json_full(result), indent=2, ensure_ascii=False)
    elif args.summary:
        output_str = json.dumps(format_json_summary(result), indent=2, ensure_ascii=False)
    else:
        output_str = format_text_full(result)  # Default --text

    # ... output routing ...
    # Phase 8: 需添加 --graph 分支
```

From uasset_read.py (format_graphs_json - Phase 8 Wave 1):
```python
def format_graphs_json(graphs: List[UEdGraph]) -> List[Dict]:
    # 已实现，返回 graphs JSON 数据
``>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 create_parser() 添加 --graph 标志</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L3530-3560 (create_parser 当前实现)
    - .planning/phases/08-blueprint-graph-output/08-CONTEXT.md (D-08-12~13 决策)
    - .planning/phases/04-output-and-cli/04-CONTEXT.md (D-24: 互斥输出标志, D-27: 可选标志)
  </read_first>
  <behavior>
    - 添加 --graph 标志（不加入互斥组）
    - help 文本说明可组合使用
    - action='store_true'（布尔标志）
  </behavior>
  <action>
    修改 create_parser() 函数，添加 --graph 标志：

    在 Optional flags 部分（约 L3555-3558）添加 --graph 标志：

    ```python
    def create_parser() -> argparse.ArgumentParser:
        """
        Create argparse parser for CLI (CLI-01 to CLI-04, OUT2-04).

        Per D-23: Double entry point support
        Per D-24: Mutually exclusive --json/--text/--summary flags
        Per D-27: Optional flags: --verbose, --output FILE, --export INDEX
        Per D-08-12: --graph is independent, not in mutually_exclusive_group

        Returns:
            argparse.ArgumentParser: Configured parser
        """
        parser = argparse.ArgumentParser(
            prog='uasset_read',
            description='Parse Unreal Engine .uasset files and output structured data'
        )

        # Positional: file path (CLI-01)
        parser.add_argument('file', help='Path to .uasset file to parse')

        # Mutually exclusive output flags (D-24)
        group = parser.add_mutually_exclusive_group(required=False)
        group.add_argument('--json', action='store_true', help='Output full JSON structure')
        group.add_argument('--text', action='store_true', help='Output YAML-style text (default)')
        group.add_argument('--summary', action='store_true', help='Output compact summary format')

        # Phase 8: --graph flag (D-08-12: NOT in mutually_exclusive_group)
        parser.add_argument('--graph', action='store_true',
                            help='Output blueprint graph data (composable with --json/--text/--verbose)')

        # Optional flags (D-27)
        parser.add_argument('--verbose', action='store_true', help='Include extra detail fields')
        parser.add_argument('--output', metavar='FILE', help='Write output to file instead of stdout')
        parser.add_argument('--export', metavar='INDEX', type=int, help='Output only specific export by index')

        return parser
    ```

    关键点：
    - --graph 标志不加入互斥组（D-08-12 独立可组合）
    - 放在互斥组之后、其他可选标志之前
    - help 文本说明可组合使用
  </action>
  <verify>
    <automated>python -c "from uasset_read import create_parser; p = create_parser(); args = p.parse_args(['test.uasset', '--graph']); print(args.graph)"</automated>
    <grep>grep "add_argument('--graph'" uasset_read.py | wc -l == 1</grep>
  </verify>
  <acceptance_criteria>
    - uasset_read.py create_parser() 包含 add_argument('--graph')
    - --graph 标志使用 action='store_true'
    - --graph 标志不在互斥组中（不在 add_mutually_exclusive_group 之后）
    - help 文本说明可组合使用
  </acceptance_criteria>
  <done>create_parser() 添加 --graph 标志完成</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 main() 添加 --graph 分支逻辑</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L3563-3609 (main 当前实现)
    - uasset_read.py (format_graphs_json 函数)
    - .planning/phases/08-blueprint-graph-output/08-CONTEXT.md (D-08-12~13 决策)
    - .planning/phases/08-blueprint-graph-output/08-RESEARCH.md (§ CLI Design)
  </read_first>
  <behavior>
    - --graph 默认仅输出 graphs 字段（JSON 格式）
    - --graph --json 输出完整 JSON（包含 graphs）
    - --graph --verbose 输出完整 JSON（包含 graphs）
    - --graph --text 输出文本格式（包含 Graphs 区块）
    - 输出逻辑优先级：--graph 检查在最前
  </behavior>
  <action>
    修改 main() 函数，添加 --graph 分支逻辑：

    找到输出选择部分（约 L3602-3609），修改为：

    ```python
    def main():
        """
        Main CLI entry point (CLI-05, OUT2-04).

        Per D-23: Double entry point (also __main__.py)
        Per D-25: stdout for data, stderr for errors
        Per D-26: Exit codes 0/1/2/3
        Per D-28: UTF-8 encoding for file output
        Per D-08-13: --graph output depends on --verbose/--json

        Exit codes:
        - 0: Success
        - 1: Parse error
        - 2: File not found
        - 3: Argument error
        """
        parser = create_parser()

        try:
            args = parser.parse_args()
        except SystemExit as e:
            sys.exit(EXIT_ARGUMENT_ERROR)

        # D-26: file not found check
        file_path = Path(args.file)
        if not file_path.exists():
            print(f"Error: File not found: {args.file}", file=sys.stderr)
            sys.exit(EXIT_FILE_NOT_FOUND)

        # Parse the file
        result = parse_uasset(args.file)

        # D-26: parse error handling
        if not result.is_success:
            print("Parse errors:", file=sys.stderr)
            for err in result.errors:
                print(f"  - {err}", file=sys.stderr)
            sys.exit(EXIT_PARSE_ERROR)

        # Phase 8: --graph flag handling (D-08-12/13)
        # 优先级：--graph 检查在最前
        if args.graph:
            # D-08-13: --graph + --json/--verbose = full output with graphs
            if args.json or args.verbose:
                output_str = json.dumps(format_json_full(result), indent=2, ensure_ascii=False)
            elif args.text:
                # --graph --text = text output with Graphs section
                output_str = format_text_full(result)
            else:
                # D-08-13: --graph alone = only graphs in JSON format
                output_str = json.dumps({"graphs": format_graphs_json(result.graphs)},
                                        indent=2, ensure_ascii=False)
        elif args.json:
            output_str = json.dumps(format_json_full(result), indent=2, ensure_ascii=False)
        elif args.summary:
            output_str = json.dumps(format_json_summary(result), indent=2, ensure_ascii=False)
        else:
            # Default: --text or no flag
            output_str = format_text_full(result)

        # Output routing (existing code unchanged)
        if args.output:
            output_path = Path(args.output)
            try:
                output_path.write_text(output_str, encoding='utf-8')
            except IOError as e:
                print(f"Error: Cannot write to {args.output}: {e}", file=sys.stderr)
                sys.exit(EXIT_PARSE_ERROR)
        else:
            print(output_str)

        sys.exit(EXIT_SUCCESS)
    ```

    关键点：
    - --graph 分支在最前（优先级最高）
    - --graph --json/--verbose：输出完整 JSON（format_json_full 已包含 graphs）
    - --graph --text：输出文本（format_text_full 已包含 Graphs 区块）
    - --graph alone：仅输出 {"graphs": ...}（D-08-13 默认行为）
  </action>
  <verify>
    <automated>python -c "from uasset_read import create_parser; p = create_parser(); args = p.parse_args(['test.uasset', '--graph', '--json']); print(f'graph={args.graph}, json={args.json}')"</automated>
    <grep>grep "if args.graph:" uasset_read.py | wc -l == 1</grep>
    <grep>grep '"graphs": format_graphs_json' uasset_read.py | wc -l == 1</grep>
  </verify>
  <acceptance_criteria>
    - main() 函数包含 if args.graph: 分支
    - --graph 分支在最前（在 args.json 检查之前）
    - --graph alone 输出 {"graphs": format_graphs_json(result.graphs)}
    - --graph --json/--verbose 输出完整 JSON
    - --graph --text 输出文本格式
    - 使用 json.dumps 格式化输出
  </acceptance_criteria>
  <done>main() 添加 --graph 分支逻辑完成</done>
</task>

<task type="auto">
  <name>Task 3: 创建 CLI --graph 标志测试</name>
  <files>tests/test_output_formatting.py</files>
  <read_first>
    - tests/test_output_formatting.py (Phase 4 CLI 测试参考)
    - uasset_read.py L3530-3560 (create_parser 实现)
    - uasset_read.py L3563-3609 (main 实现)
    - .planning/phases/08-blueprint-graph-output/08-VALIDATION.md (验证策略)
  </read_first>
  <behavior>
    - 测试 --graph 标志存在并可解析
    - 测试 --graph 不与 --json/--text/--summary 互斥
    - 测试 --graph alone 输出仅 graphs 字段
    - 测试 --graph --json 输出完整 JSON
    - 测试 --graph --verbose 输出完整结构
  </behavior>
  <action>
    在 tests/test_output_formatting.py 中添加 CLI --graph 测试：

    ```python
    # ============================================================================
    # Phase 8: OUT2-04 - CLI --graph 标志
    # ============================================================================

    def test_cli_graph_flag():
        """
        OUT2-04: 验证 --graph 标志存在并可解析。
        """
        from uasset_read import create_parser

        parser = create_parser()
        args = parser.parse_args(['test.uasset', '--graph'])

        assert args.graph is True

    def test_cli_graph_json_composable():
        """
        D-08-12: 验证 --graph 不与 --json 互斥。
        """
        from uasset_read import create_parser

        parser = create_parser()
        args = parser.parse_args(['test.uasset', '--graph', '--json'])

        assert args.graph is True
        assert args.json is True

    def test_cli_graph_text_composable():
        """
        D-08-12: 验证 --graph 不与 --text 互斥。
        """
        from uasset_read import create_parser

        parser = create_parser()
        args = parser.parse_args(['test.uasset', '--graph', '--text'])

        assert args.graph is True
        assert args.text is True

    def test_cli_graph_summary_composable():
        """
        D-08-12: 验证 --graph 不与 --summary 互斥。
        """
        from uasset_read import create_parser

        parser = create_parser()
        args = parser.parse_args(['test.uasset', '--graph', '--summary'])

        assert args.graph is True
        assert args.summary is True

    def test_cli_graph_verbose_composable():
        """
        D-08-12: 验证 --graph 与 --verbose 可组合。
        """
        from uasset_read import create_parser

        parser = create_parser()
        args = parser.parse_args(['test.uasset', '--graph', '--verbose'])

        assert args.graph is True
        assert args.verbose is True

    def test_cli_graph_output_alone():
        """
        D-08-13: 验证 --graph alone 输出仅 graphs 字段。
        """
        # 使用 mock 测试输出内容
        result = create_mock_parse_result()
        result.graphs = [sample_graph_with_connections()]

        # --graph alone 应输出 {"graphs": [...]}
        output_str = json.dumps({"graphs": format_graphs_json(result.graphs)},
                                indent=2, ensure_ascii=False)

        assert '"graphs"' in output_str
        # 不应包含其他字段（如 exports）
        assert '"exports"' not in output_str

    def test_cli_graph_json_output_full():
        """
        D-08-13: 验证 --graph --json 输出完整 JSON。
        """
        result = create_mock_parse_result()
        result.graphs = [sample_graph_with_connections()]

        # --graph --json 应输出完整 JSON
        output_str = json.dumps(format_json_full(result), indent=2, ensure_ascii=False)

        assert '"graphs"' in output_str
        assert '"exports"' in output_str  # 包含其他字段
        assert '"summary"' in output_str
    ```

    需要在导入部分添加 format_json_summary（如未导入）：
    ```python
    from uasset_read import (
        # ... existing imports ...
        format_json_summary,
    )
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_cli_graph_flag -x -v</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_cli_graph_json_composable -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_output_formatting.py 包含 test_cli_graph_flag 测试
    - tests/test_output_formatting.py 包含 test_cli_graph_json_composable 测试
    - tests/test_output_formatting.py 包含 test_cli_graph_text_composable 测试
    - tests/test_output_formatting.py 包含 test_cli_graph_verbose_composable 测试
    - tests/test_output_formatting.py 包含 test_cli_graph_output_alone 测试
    - tests/test_output_formatting.py 包含 test_cli_graph_json_output_full 测试
    - 所有新增测试 pytest 执行通过（exit code 0）
  </acceptance_criteria>
  <done>CLI --graph 标志测试创建完成，pytest 验证通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 输入 → 输出 | 用户输入通过 argparse 验证，输出为数据格式化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | main() --graph 分支 | accept | 简单条件分支，无复杂计算 |

**Phase 8 CLI 扩展无安全敏感操作（argparse 配置 + 条件分支）。**
</threat_model>

<verification>
```bash
# 导入验证
python -c "from uasset_read import create_parser; p = create_parser(); args = p.parse_args(['test.uasset', '--graph']); print(args.graph)"

# grep 检查
grep "add_argument('--graph'" uasset_read.py | wc -l
grep "if args.graph:" uasset_read.py | wc -l

# CLI 组合测试
python -c "from uasset_read import create_parser; p = create_parser(); args = p.parse_args(['test.uasset', '--graph', '--json']); print(f'graph={args.graph}, json={args.json}')"

# 单元测试
python -m pytest tests/test_output_formatting.py::test_cli_graph_flag -x -v
python -m pytest tests/test_output_formatting.py::test_cli_graph_json_composable -x -v

# 完整测试套件无回归
python -m pytest tests/ -v --ignore=tests/test_output_formatting.py -k "not test_json_full_structure"
```
</verification>

<success_criteria>
- create_parser() 包含 --graph 标志
- --graph 标志不与 --json/--text/--summary 互斥
- main() 包含 --graph 分支逻辑
- --graph alone 输出仅 graphs 字段
- --graph --json/--verbose 输出完整 JSON
- CLI --graph 测试创建并通过
- 无回归：现有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/08-blueprint-graph-output/08-04-SUMMARY.md`
</output>