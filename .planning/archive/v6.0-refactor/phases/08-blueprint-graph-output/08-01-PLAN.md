---
phase: 08-blueprint-graph-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [uasset_read.py, tests/test_output_formatting.py]
autonomous: true
requirements: [GRAPH-11, OUT2-01]
user_setup: []

must_haves:
  truths:
    - "JSON 输出包含 graphs 字段（与 blueprint_metadata 同级）"
    - "每个 Graph 包含 graph_name、nodes 数组、connections 数组"
    - "每个连接使用 {from: {node_guid, pin_name}, to: {node_guid, pin_name}} 结构"
    - "连接映射仅从 Output pins 出发（单向表示）"
    - "查找失败时连接包含 warning 字段"
  artifacts:
    - path: "uasset_read.py"
      provides: "build_connections_map() 和 format_graphs_json() 函数"
      contains: "def build_connections_map"
    - path: "uasset_read.py"
      provides: "format_json_full() 扩展包含 graphs 字段"
      pattern: "\"graphs\": format_graphs_json"
    - path: "tests/test_output_formatting.py"
      provides: "Phase 8 Wave 1 单元测试"
      contains: "test_format_json_full_contains_graphs"
  key_links:
    - from: "format_json_full()"
      to: "format_graphs_json()"
      via: "调用并添加到返回字典"
      pattern: "format_graphs_json\\(result\\.graphs\\)"
    - from: "format_graphs_json()"
      to: "build_connections_map()"
      via: "为每个 graph 构建连接"
      pattern: "build_connections_map\\(graph\\)"
    - from: "build_connections_map()"
      to: "UEdGraphPin.linked_to_raw"
      via: "读取 PinId GUID hex 查找目标"
      pattern: "pin\\.linked_to_raw"
---

<objective>
实现连接映射构建和 JSON 输出扩展，将 Phase 7 存储的 linked_to_raw 原始数据转换为可读的 {node_guid, pin_name} 连接表示。

Purpose: 满足 GRAPH-11（JSON 输出包含蓝图图层级结构）和 OUT2-01（graphs 与 blueprint 同级）
Output: build_connections_map()、format_graphs_json() 函数，format_json_full() 扩展
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-blueprint-graph-output/08-CONTEXT.md
@.planning/phases/08-blueprint-graph-output/08-RESEARCH.md

<interfaces>
<!-- Phase 7 已实现的数据结构 -->
<!-- Executor 应使用这些直接 - 无需探索代码库 -->

From uasset_read.py L783-800 (UEdGraphPin):
```python
@dataclass
class UEdGraphPin:
    pin_id: str                          # FGuid hex（16 bytes）
    pin_name: str                        # FName 解析结果
    direction: int                       # uint8: 0=Input, 1=Output, 2=None
    pin_type: "FEdGraphPinType"
    default_value: Optional[str] = None
    auto_default_value: Optional[str] = None
    linked_to_raw: List[str] = field(default_factory=list)  # PinId GUID hex 列表
    sub_pins: List[str] = field(default_factory=list)
    parent_pin: Optional[str] = None
    flags: int = 0
```

From uasset_read.py L804-818 (UEdGraphNode):
```python
@dataclass
class UEdGraphNode:
    node_guid: str                       # FGuid hex（16 bytes）
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List["UEdGraphPin"] = field(default_factory=list)
    class_name: str = ""                 # K2Node_CallFunction 等
    node_data: Optional[any] = None      # 类型特定数据（多态）
```

From uasset_read.py L823-837 (UEdGraph):
```python
@dataclass
class UEdGraph:
    graph_name: str
    graph_class: str                     # EdGraph/UberEdGraph
    schema: Optional[str] = None
    nodes: List["UEdGraphNode"] = field(default_factory=list)
    graph_guid: Optional[str] = None
    b_editable: bool = True
```

From uasset_read.py L946 (ParseResult.graphs):
```python
graphs: List["UEdGraph"] = field(default_factory=list)  # Phase 7: 蓝图图数据
```

From uasset_read.py L3178-3212 (format_json_full - 当前实现):
```python
def format_json_full(result: ParseResult) -> Dict:
    # ... existing code ...
    return {
        "summary": summary_dict,
        "exports": format_exports_list(result),
        "blueprint_metadata": format_blueprint_dict(result.blueprint) if result.blueprint else None,
        "errors": result.errors
    }
    # Phase 8: 需添加 "graphs": format_graphs_json(result.graphs)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 build_connections_map() 函数</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L783-837 (UEdGraphPin/UEdGraphNode/UEdGraph 数据结构)
    - uasset_read.py L946 (ParseResult.graphs 字段)
    - uasset_read.py L1945-1950 (linked_to_raw 存储: PinId GUID hex)
    - .planning/phases/08-blueprint-graph-output/08-CONTEXT.md (D-08-01~06 决策)
  </read_first>
  <behavior>
    - 输入: UEdGraph 对象
    - 输出: Tuple[List[Dict], List[str]] - (connections, warnings)
    - 连接结构: {from: {node_guid, pin_name}, to: {node_guid, pin_name}}
    - 仅从 Output pins (direction=1) 出发构建连接（D-08-05）
    - 查找失败时返回 warning + raw_pin_id（D-08-04）
    - 每个 PinId 在 pin_lookup 中查找对应 node_guid + pin_name
  </behavior>
  <action>
    在 uasset_read.py 中 format_json_full() 函数之前添加 build_connections_map() 函数：

    ```python
    def build_connections_map(graph: UEdGraph) -> Tuple[List[Dict], List[str]]:
        """
        构建引脚连接映射（D-08-01~06）。

        将 linked_to_raw（PinId GUID hex）转换为 {node_guid, pin_name} 表示。

        算法：
        1. 构建 PinId → (node_guid, pin_name) 查找表
        2. 遍历所有 Output pins (direction=1)
        3. 对每个 linked_to_raw 中的 PinId，查找目标 pin
        4. 构建 {from, to} 连接对象
        5. 处理查找失败（warning + 原始数据）

        Args:
            graph: UEdGraph 对象

        Returns:
            Tuple[List[Dict], List[str]]: (connections 列表, warnings 列表)
        """
        # Step 1: Build pin_lookup: pin_id → (node_guid, pin_name)
        pin_lookup: Dict[str, Tuple[str, str]] = {}
        for node in graph.nodes:
            for pin in node.pins:
                pin_lookup[pin.pin_id] = (node.node_guid, pin.pin_name)

        # Step 2-4: Build connections (only from Output pins, D-08-05)
        connections: List[Dict] = []
        warnings: List[str] = []

        for node in graph.nodes:
            for pin in node.pins:
                if pin.direction == 1:  # EGPD_Output
                    for linked_pin_id in pin.linked_to_raw:
                        if linked_pin_id in pin_lookup:
                            target_node_guid, target_pin_name = pin_lookup[linked_pin_id]
                            # D-08-06: {from, to} 对象结构
                            connections.append({
                                "from": {"node_guid": node.node_guid, "pin_name": pin.pin_name},
                                "to": {"node_guid": target_node_guid, "pin_name": target_pin_name}
                            })
                        else:
                            # D-08-04: Warning + raw data
                            warnings.append(f"PinId {linked_pin_id} not found in graph")
                            connections.append({
                                "from": {"node_guid": node.node_guid, "pin_name": pin.pin_name},
                                "to": {"raw_pin_id": linked_pin_id},
                                "warning": "target pin not found"
                            })

        return connections, warnings
    ```

    函数签名：使用 `from typing import Dict, List, Tuple, Optional`（已在文件顶部导入）

    关键点：
    - pin_lookup 使用 dict 存储所有 pin 的 pin_id → (node_guid, pin_name) 映射
    - 仅遍历 direction == 1 (Output) 的引脚（D-08-05 单向表示）
    - 连接对象使用 {from: {...}, to: {...}} 结构（D-08-06）
    - 查找失败时添加 warning 字段和 raw_pin_id（D-08-04）
  </action>
  <verify>
    <automated>python -c "from uasset_read import build_connections_map; print('Import OK')"</automated>
    <grep>grep -c "def build_connections_map" uasset_read.py == 1</grep>
    <grep>grep "pin_lookup: Dict\[str, Tuple\[str, str\]\]" uasset_read.py | wc -l == 1</grep>
  </verify>
  <acceptance_criteria>
    - uasset_read.py 包含 def build_connections_map 函数定义
    - 函数签名接受 UEdGraph 参数，返回 Tuple[List[Dict], List[str]]
    - 函数实现包含 pin_lookup 查找表构建
    - 函数仅从 direction == 1 的引脚出发构建连接
    - 连接结构为 {from: {node_guid, pin_name}, to: {node_guid, pin_name}}
    - 查找失败时包含 warning 字段和 raw_pin_id
  </acceptance_criteria>
  <done>build_connections_map() 函数实现完成，导入验证通过，grep 检查确认实现存在</done>
</task>

<task type="auto">
  <name>Task 2: 实现 format_graphs_json() 函数并扩展 format_json_full()</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L3178-3212 (format_json_full 当前实现)
    - uasset_read.py L783-837 (UEdGraph/UEdGraphNode/UEdGraphPin 数据结构)
    - .planning/phases/08-blueprint-graph-output/08-CONTEXT.md (D-08-03: Graph.connections 位置)
    - .planning/phases/04-output-and-cli/04-CONTEXT.md (D-04: blueprint_metadata 顶层字段)
  </read_first>
  <behavior>
    - format_graphs_json(graphs: List[UEdGraph]) → List[Dict]
    - 每个 graph dict 包含: graph_name, graph_class, nodes (asdict), connections, warnings
    - format_json_full() 返回添加 graphs 字段（与 blueprint_metadata 同级）
    - graphs 为空列表时返回空数组 []
  </behavior>
  <action>
    在 build_connections_map() 函数之后添加 format_graphs_json() 函数：

    ```python
    def format_graphs_json(graphs: List[UEdGraph]) -> List[Dict]:
        """
        格式化蓝图图数据为 JSON 输出（GRAPH-11, OUT2-01）。

        Per D-08-03: connections 放在 graph 层级
        Per D-04: graphs 与 blueprint_metadata 同级

        Args:
            graphs: List[UEdGraph] from ParseResult.graphs

        Returns:
            List[Dict]: 每个 graph 的 JSON 表示
        """
        from dataclasses import asdict

        formatted = []
        for graph in graphs:
            # 构建连接映射
            connections, warnings = build_connections_map(graph)

            graph_dict = {
                "graph_name": graph.graph_name,
                "graph_class": graph.graph_class,
                "nodes": [asdict(node) for node in graph.nodes],
                "connections": connections,
            }

            # D-08-04: 添加 warnings（如果有）
            if warnings:
                graph_dict["warnings"] = warnings

            # 可选字段
            if graph.graph_guid:
                graph_dict["graph_guid"] = graph.graph_guid
            if graph.schema:
                graph_dict["schema"] = graph.schema

            formatted.append(graph_dict)

        return formatted
    ```

    然后修改 format_json_full() 函数，在返回字典中添加 graphs 字段：

    将 uasset_read.py L3207-3212 的 return 语句修改为：

    ```python
    return {
        "summary": summary_dict,
        "exports": format_exports_list(result),
        "blueprint_metadata": format_blueprint_dict(result.blueprint) if result.blueprint else None,
        "graphs": format_graphs_json(result.graphs),  # Phase 8: OUT2-01
        "errors": result.errors
    }
    ```

    关键点：
    - format_graphs_json() 调用 build_connections_map() 获取连接
    - nodes 使用 asdict() 转换为 dict
    - graphs 字段添加到顶层返回字典（与 blueprint_metadata 同级）
    - 空 graphs 列表返回空数组 []
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_graphs_json; print('Import OK')"</automated>
    <grep>grep -c "def format_graphs_json" uasset_read.py == 1</grep>
    <grep>grep '"graphs": format_graphs_json' uasset_read.py | wc -l == 1</grep>
  </verify>
  <acceptance_criteria>
    - uasset_read.py 包含 def format_graphs_json 函数定义
    - format_graphs_json 接受 List[UEdGraph] 参数，返回 List[Dict]
    - 函数调用 build_connections_map() 构建连接
    - nodes 使用 asdict() 转换
    - format_json_full() 返回字典包含 graphs 字段
    - graphs 字段位置在 blueprint_metadata 之后、errors 之前
  </acceptance_criteria>
  <done>format_graphs_json() 实现完成，format_json_full() 扩展完成，导入验证通过</done>
</task>

<task type="auto">
  <name>Task 3: 创建 Phase 8 Wave 1 单元测试</name>
  <files>tests/test_output_formatting.py</files>
  <read_first>
    - tests/test_output_formatting.py (现有测试结构)
    - tests/test_graph_parsing.py (Phase 7 测试参考)
    - uasset_read.py L783-837 (数据结构定义)
  </read_first>
  <behavior>
    - 测试 format_json_full() 返回包含 graphs 字段
    - 测试 graphs 字段与 blueprint_metadata 同级
    - 测试 build_connections_map() 正确构建连接
    - 测试连接结构符合 {from, to} 格式
    - 测试查找失败时包含 warning
  </behavior>
  <action>
    在 tests/test_output_formatting.py 中添加 Phase 8 Wave 1 测试函数：

    1. 添加必要的导入（在文件顶部导入区域添加）：
    ```python
    from uasset_read import (
        # ... existing imports ...
        UEdGraph,
        UEdGraphNode,
        UEdGraphPin,
        FEdGraphPinType,
        build_connections_map,
        format_graphs_json,
        format_json_full,
    )
    ```

    2. 创建测试 fixture（在现有 fixture 之后添加）：
    ```python
    @pytest.fixture
    def sample_graph_with_connections():
        """
        创建测试用的 UEdGraph fixture，包含连接数据。

        Returns:
            UEdGraph: 包含 2 个节点和 1 个连接的测试图
        """
        pin_type = FEdGraphPinType(
            pin_category="exec",
            pin_sub_category="exec",
            container_type="None",
            is_reference=False,
            is_const=False,
        )

        # Node 1: Output pin
        output_pin = UEdGraphPin(
            pin_id="a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6",  # GUID hex
            pin_name="then",
            direction=1,  # Output
            pin_type=pin_type,
            linked_to_raw=["f1e2d3c4b5a697880910111213141516"],  # 指向 Node 2 的 input pin
        )

        node1 = UEdGraphNode(
            node_guid="11111111111111111111111111111111",
            node_pos_x=0,
            node_pos_y=0,
            pins=[output_pin],
            class_name="K2Node_Event",
        )

        # Node 2: Input pin
        input_pin = UEdGraphPin(
            pin_id="f1e2d3c4b5a697880910111213141516",  # linked_to_raw 目标
            pin_name="execute",
            direction=0,  # Input
            pin_type=pin_type,
            linked_to_raw=[],
        )

        node2 = UEdGraphNode(
            node_guid="22222222222222222222222222222222",
            node_pos_x=100,
            node_pos_y=0,
            pins=[input_pin],
            class_name="K2Node_CallFunction",
        )

        return UEdGraph(
            graph_name="EventGraph",
            graph_class="UberEdGraph",
            nodes=[node1, node2],
        )

    @pytest.fixture
    def sample_graph_with_missing_pin():
        """
        创建测试用的 UEdGraph fixture，包含查找失败的连接。

        Returns:
            UEdGraph: 包含无法找到的目标 pin
        """
        pin_type = FEdGraphPinType(
            pin_category="exec",
            pin_sub_category="exec",
            container_type="None",
            is_reference=False,
            is_const=False,
        )

        # Output pin 指向不存在的 pin
        output_pin = UEdGraphPin(
            pin_id="a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6",
            pin_name="then",
            direction=1,
            pin_type=pin_type,
            linked_to_raw=["00000000000000000000000000000000"],  # 不存在的 pin
        )

        node = UEdGraphNode(
            node_guid="11111111111111111111111111111111",
            pins=[output_pin],
            class_name="K2Node_Event",
        )

        return UEdGraph(
            graph_name="TestGraph",
            graph_class="EdGraph",
            nodes=[node],
        )
    ```

    3. 替换现有的 TODO 测试函数（L172-195）并添加新测试：
    ```python
    # ============================================================================
    # Phase 8: GRAPH-11 - JSON 输出包含 graphs 层级结构
    # ============================================================================

    def test_format_json_full_contains_graphs():
        """
        GRAPH-11: 验证 format_json_full() 返回包含 graphs 字段。
        """
        result = create_mock_parse_result()
        result.graphs = [sample_graph_with_connections()]

        json_dict = format_json_full(result)

        assert 'graphs' in json_dict
        assert isinstance(json_dict['graphs'], list)
        assert len(json_dict['graphs']) == 1

    def test_graphs_field_top_level():
        """
        OUT2-01: 验证 graphs 字段与 blueprint_metadata 同级。
        """
        result = create_mock_parse_result()
        result.graphs = []

        json_dict = format_json_full(result)

        # graphs 与 blueprint_metadata 同级
        assert 'graphs' in json_dict
        assert 'blueprint_metadata' in json_dict
        assert 'exports' in json_dict
        assert 'errors' in json_dict

    def test_format_graphs_json_structure():
        """
        GRAPH-11: 验证 format_graphs_json() 返回正确的 graph 结构。
        """
        graph = sample_graph_with_connections()
        formatted = format_graphs_json([graph])

        assert len(formatted) == 1
        graph_dict = formatted[0]

        assert 'graph_name' in graph_dict
        assert 'graph_class' in graph_dict
        assert 'nodes' in graph_dict
        assert 'connections' in graph_dict

        assert graph_dict['graph_name'] == "EventGraph"
        assert graph_dict['graph_class'] == "UberEdGraph"

    def test_build_connections_map_basic():
        """
        验证 build_connections_map() 正确构建连接。
        """
        graph = sample_graph_with_connections()
        connections, warnings = build_connections_map(graph)

        assert len(connections) == 1
        assert len(warnings) == 0

        conn = connections[0]
        assert 'from' in conn
        assert 'to' in conn

        # D-08-06: {from, to} 对象结构
        assert 'node_guid' in conn['from']
        assert 'pin_name' in conn['from']
        assert conn['from']['pin_name'] == "then"

    def test_build_connections_map_warning():
        """
        D-08-04: 验证查找失败时包含 warning 和原始数据。
        """
        graph = sample_graph_with_missing_pin()
        connections, warnings = build_connections_map(graph)

        assert len(connections) == 1
        assert len(warnings) == 1

        conn = connections[0]
        assert 'warning' in conn
        assert conn['warning'] == "target pin not found"
        assert 'raw_pin_id' in conn['to']
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_json_full_contains_graphs -x -v</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_connections_map_basic -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_output_formatting.py 包含 test_format_json_full_contains_graphs 测试
    - tests/test_output_formatting.py 包含 test_graphs_field_top_level 测试
    - tests/test_output_formatting.py 包含 test_build_connections_map_basic 测试
    - tests/test_output_formatting.py 包含 sample_graph_with_connections fixture
    - 所有新增测试 pytest 执行通过（exit code 0）
  </acceptance_criteria>
  <done>Phase 8 Wave 1 单元测试创建完成，pytest 验证通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 输入 → 输出格式化 | UEdGraph 数据来自 Phase 7 解析，已在解析阶段验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | build_connections_map() | accept | 简单 dict 遍历，O(n*m) 复杂度可控；Phase 7 已限制 MAX_NODES_PER_GRAPH=5000 |
| T-08-02 | Information Disclosure | format_graphs_json() | accept | 无敏感数据，仅蓝图结构 |

**Phase 8 无安全敏感操作（纯数据格式化）。Phase 7 安全边界已覆盖输入数据。**
</threat_model>

<verification>
```bash
# 导入验证
python -c "from uasset_read import build_connections_map, format_graphs_json; print('Import OK')"

# grep 检查
grep -c "def build_connections_map" uasset_read.py
grep -c "def format_graphs_json" uasset_read.py
grep '"graphs": format_graphs_json' uasset_read.py | wc -l

# 单元测试
python -m pytest tests/test_output_formatting.py::test_format_json_full_contains_graphs -x -v
python -m pytest tests/test_output_formatting.py::test_build_connections_map_basic -x -v

# 完整测试套件无回归
python -m pytest tests/ -v --ignore=tests/test_output_formatting.py -k "not test_json_full_structure"
```
</verification>

<success_criteria>
- build_connections_map() 函数实现完成，导入验证通过
- format_graphs_json() 函数实现完成，导入验证通过
- format_json_full() 扩展包含 graphs 字段
- Phase 8 Wave 1 单元测试创建并通过
- 无回归：现有 105 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-blueprint-graph-output/08-01-SUMMARY.md`
</output>