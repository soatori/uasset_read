---
phase: 14-output-format-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - uasset_read.py
autonomous: true
requirements:
  - OUT-02
user_setup: []

must_haves:
  truths:
    - "用户可以在顶层 graphs_summary 字段中看到 execution_flows 概览"
    - "graphs_summary 按图分组（EventGraph, ConstructionScript 等）"
    - "函数调用链格式为函数名+参数类型（如 PrintString(InStr:String)）"
  artifacts:
    - path: "uasset_read.py"
      provides: "graphs_summary 提取函数"
      contains: "def build_graphs_summary"
      contains: '"graphs_summary"'
  key_links:
    - from: "build_graphs_summary()"
      to: "format_graphs_json()"
      via: "复用 execution_flows 数据"
      pattern: "execution_flows.*graph"
---

<objective>
将 execution_flows 从 graphs[] 内提升至顶层 graphs_summary 字段（OUT-02）

Purpose: AI 无需深入 graphs 数组即可获取函数调用链概览
Output: build_graphs_summary() 函数 + format_json_* 集成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-output-format-optimization/14-CONTEXT.md

# 现有代码接口（提取关键类型）

From uasset_read.py:4716-4808:
```python
def format_graphs_json(graphs: List[UEdGraph]) -> List[Dict]:
    """格式化蓝图图数据为 JSON 输出"""
    formatted = []
    for graph in graphs:
        execution_flows = build_execution_flows(graph)
        graph_dict = {
            "graph_name": graph.graph_name,
            "execution_flows": execution_flows,  # 已有数据
            ...
        }
    return formatted

def build_execution_flows(graph: UEdGraph) -> List[Dict]:
    """构建执行流路径（D-08-07~11）"""
    # 返回格式: [{"start_event": "EventBeginPlay", "nodes": [...]}]
```

From CONTEXT.md D-14-04:
```json
"graphs_summary": [
  {
    "graph": "EventGraph",
    "execution_flows": [
      {"event": "EventBeginPlay", "calls": ["PrintString(InStr:String)", "SetLifeSpan(Lifespan:Float)"]}
    ]
  }
]
```
</context>

<interfaces>
<!-- Executor 需要的关键接口 -->

## 现有 execution_flows 数据结构

```python
# build_execution_flows() 返回的格式
execution_flows = [
    {
        "start_event": "EventBeginPlay",
        "nodes": [
            {"node_guid": "...", "node_type": "K2Node_Event", "event_name": "EventBeginPlay"},
            {"node_guid": "...", "node_type": "K2Node_CallFunction", "function_name": "PrintString"},
        ]
    }
]
```

## 目标 graphs_summary 格式（D-14-04~06）

```python
graphs_summary = [
    {
        "graph": "EventGraph",
        "execution_flows": [
            {"event": "EventBeginPlay", "calls": ["PrintString(InStr:String)"]}
        ]
    }
]
```

## 参数类型提取（D-14-06）

从 UEdGraphNode.pins 中提取:
- pin.pin_name → 参数名
- pin.pin_type.pin_category → 类型类别（如 "string", "float"）
- 组合为 "ParamName:TypeCategory" 格式
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 build_graphs_summary() 函数并集成到 format_json_full()</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: graphs_summary 字段存在且为数组
    - Test 2: 每个 graphs_summary 条目包含 graph 和 execution_flows
    - Test 3: execution_flows.calls 格式为 ["FuncName(Param:Type)"]
    - Test 4: 空 graphs 输入返回空 graphs_summary
  </behavior>
  <action>
    Per D-14-04~06: graphs_summary 结构实现

    1. 创建 build_graphs_summary() 函数（放在 format_graphs_json 后，约 line 4760）:
       ```python
       def build_graphs_summary(graphs: List[UEdGraph]) -> List[Dict]:
           """
           构建顶层 graphs_summary 字段（D-14-04~06, OUT-02）。

           将 execution_flows 从 graphs[] 内提升至顶层，按图分组。
           函数调用格式: FunctionName(ParamName:TypeCategory)

           Args:
               graphs: List[UEdGraph] from ParseResult.graphs

           Returns:
               List[Dict]: graphs_summary 数组
           """
           summary = []
           for graph in graphs:
               # 复用现有 build_execution_flows 数据
               flows = build_execution_flows(graph)

               # 转换为目标格式
               execution_flows_summary = []
               for flow in flows:
                   event_name = flow.get("start_event", "Unknown")
                   # 提取函数调用链
                   calls = []
                   for node in flow.get("nodes", []):
                       if node.get("node_type") == "K2Node_CallFunction":
                           func_name = node.get("function_name", "Unknown")
                           # D-14-06: 提取参数类型（从 graph.nodes 中查找）
                           param_str = _extract_function_params(graph, node.get("node_guid"))
                           calls.append(f"{func_name}({param_str})")

                   execution_flows_summary.append({
                       "event": event_name,
                       "calls": calls
                   })

               summary.append({
                   "graph": graph.graph_name,
                   "execution_flows": execution_flows_summary
               })

           return summary


       def _extract_function_params(graph: UEdGraph, node_guid: str) -> str:
           """
           提取函数参数类型（D-14-06）。

           从节点 pins 中提取非 exec pin 的类型信息。
           格式: "Param1:Type1, Param2:Type2"

           Args:
               graph: UEdGraph 对象
               node_guid: 节点 GUID

           Returns:
               str: 参数类型字符串
           """
           # 查找节点
           node = None
           for n in graph.nodes:
               if n.node_guid == node_guid:
                   node = n
                   break

           if not node:
               return ""

           # 提取 input pins（非 exec 类型）
           params = []
           for pin in node.pins:
               if pin.direction == 0:  # Input
                   if pin.pin_type and pin.pin_type.pin_category != "exec":
                       pin_type = pin.pin_type.pin_category
                       # 常见类型映射
                       type_map = {
                           "string": "String",
                           "float": "Float",
                           "int": "Int",
                           "bool": "Bool",
                           "object": "Object",
                           "struct": "Struct",
                           "delegate": "Delegate",
                           "class": "Class",
                       }
                       type_name = type_map.get(pin_type, pin_type.capitalize())
                       params.append(f"{pin.pin_name}:{type_name}")

           return ", ".join(params[:3])  # 最多显示 3 个参数
       ```

    2. 修改 format_json_full() 函数（约 line 4945）添加 graphs_summary 字段:
       - 在 return dict 中添加 graphs_summary（在 graphs 字段后）
       ```python
       return {
           "status": asdict(build_status_info(result)),
           "output_version": "3.0",
           "summary": summary_dict,
           "exports": format_exports_list(result),
           "blueprint_metadata": ...,
           "graphs": format_graphs_json(result.graphs),
           "graphs_summary": build_graphs_summary(result.graphs),  # D-14-04: 顶层化
           ...existing fields...
       }
       ```

    3. 同样修改 format_json_summary() 函数添加 graphs_summary

    注意事项:
    - graphs_summary 和 graphs 字段共存（graphs 保留完整数据）
    - 参数类型最多显示 3 个，避免过长
    - 空 graphs 返回空数组 []，不是 null
    - 复用现有 build_execution_flows() 逻辑，不重复实现
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py -v -k "graphs_summary" --tb=short</automated>
  </verify>
  <done>
    - build_graphs_summary() 函数存在
    - _extract_function_params() 辅助函数存在
    - format_json_full/format_json_summary 包含 graphs_summary 字段
    - 测试验证 graphs_summary 格式正确
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | 本 phase 为纯输出格式优化，无安全边界变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 本 phase 无安全相关变更 |
</threat_model>

<verification>
**Goal-backward verification:**
1. graphs_summary 字段为顶层字段
2. 每个条目包含 graph 名称和 execution_flows 数组
3. calls 格式为函数名+参数类型

**自动化测试:**
```bash
python -m pytest tests/test_output_formatting.py -v --tb=short
```
</verification>

<success_criteria>
- [ ] build_graphs_summary() 函数创建完成
- [ ] _extract_function_params() 辅助函数创建
- [ ] format_json_full() 包含 graphs_summary 字段
- [ ] format_json_summary() 包含 graphs_summary 字段
- [ ] 测试覆盖 graphs_summary 格式验证
</success_criteria>

<output>
完成后创建 `.planning/phases/14-output-format-optimization/14-02-SUMMARY.md`
</output>