---
phase: 27
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/uasset_read/constants.py
  - src/uasset_read/exceptions.py
  - src/uasset_read/__init__.py
autonomous: true
requirements: [MOD-02, MOD-03]
user_setup: []

must_haves:
  truths:
    - "常量模块包含所有版本号、属性类型阈值、边界常量（如PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012）"
    - "异常模块包含所有异常类（UAssetError, VersionError, ParseError, ErrorContext）"
    - "src/uasset_read/__init__.py导出常量和异常类（填充__all__）"
  artifacts:
    - path: "src/uasset_read/constants.py"
      provides: "常量定义（版本号、阈值、边界常量）"
      min_lines: 100
      contains: "PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012"
    - path: "src/uasset_read/exceptions.py"
      provides: "异常类定义（UAssetError, VersionError, ParseError, ErrorContext）"
      min_lines: 30
      contains: "class UAssetError"
    - path: "src/uasset_read/__init__.py"
      provides: "公共API导出（常量和异常）"
      min_lines: 15
      contains: "from .constants import"
  key_links:
    - from: "src/uasset_read/__init__.py"
      to: "constants.py, exceptions.py"
      via: "导入语句"
      pattern: "from \\.constants import|from \\.exceptions import"
    - from: "后续模块（archive.py, parsers等）"
      to: "constants.py, exceptions.py"
      via: "导入常量和异常类"
      pattern: "from \\. import constants|from \\. import exceptions"
---

# 阶段 27 计划 02：提取常量和异常到独立模块

## 目标

将现有uasset_read.py中的所有常量定义提取到constants.py，所有异常类提取到exceptions.py，更新__init__.py导出这些基础模块。

**目的：** 为后续模块化重构提供可复用的常量和异常定义，所有后续阶段都依赖于此两个模块。

**输出：** constants.py（完整常量定义）、exceptions.py（完整异常类）、__init__.py（导出常量和异常）。

## 执行上下文

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-project-structure-init/27-CONTEXT.md
@.planning/phases/27-project-structure-init/27-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@CLAUDE.md
@uasset_read.py

<interfaces>
<!-- 从uasset_read.py提取的常量和异常定义 -->

**常量定义（来自uasset_read.py第28-152行）：**
- PACKAGE_FILE_TAG = 0x9E2A83C1
- PACKAGE_FILE_TAG_SWAPPED = 0xC1832A9E
- UE5_VERSION_MIN = 0
- LEGACY_FILE_VERSION_MIN = -9
- LEGACY_FILE_VERSION_MAX = -2
- MAX_NAME_COUNT = 10_000_000
- MAX_IMPORT_COUNT = 1_000_000
- MAX_EXPORT_COUNT = 1_000_000
- MAX_CUSTOM_VERSIONS = 10_000
- MMAP_THRESHOLD = 50 * 1024 * 1024
- MAX_PROPERTY_COUNT = 10_000
- PROP_TAG_* 系列常量（PROP_TAG_NONE, PROP_TAG_HAS_ARRAY_INDEX等）
- PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012
- VER_UE4_* 系列常量
- UE5_* 系列常量
- FFRAMEWORK_* 系列常量
- FUE5_* 系列常量
- FRELEASE_* 系列常量
- CustomVersion GUIDs
- CONTROL_FLOW_NODES（frozenset）
- START_EVENT_TYPES（frozenset）
- BRANCH_TYPE_MAP（dict）
- FORMAT_CONFIG（dict）
- GRAPH_TYPE_MAP（dict）
- EXIT_SUCCESS, EXIT_PARSE_ERROR, EXIT_FILE_NOT_FOUND, EXIT_ARGUMENT_ERROR

**异常类定义（来自uasset_read.py第158-196行）：**
- class UAssetError(Exception)
- class VersionError(UAssetError)
- @dataclass class ErrorContext
- class ParseError(UAssetError)
</interfaces>

## 任务

### 任务 1：创建constants.py并提取所有常量

<read_first>
- uasset_read.py — 读取第28-152行（常量定义区域）
- .planning/research/STACK.md — 常量组织建议（D-10/D-11）
- .planning/phases/27-project-structure-init/27-CONTEXT.md — D-10/D-11决策
</read_first>

<action>
创建src/uasset_read/constants.py，从uasset_read.py提取所有常量定义（per D-10/D-11）：

1. **读取uasset_read.py提取所有常量**：
   - 读取文件开头到第152行（常量定义区域）
   - 提取所有大写常量定义（^ *[A-Z_][A-Z0-9_]*\s*=）

2. **创建src/uasset_read/constants.py**：
   ```python
   """
   uasset_read常量定义

   包含所有版本号、属性类型阈值、边界常量。
   从uasset_read.py提取（per D-11）。
   """

   # ============================================================================
   # Package文件标签（来自UE源码）
   # ============================================================================

   PACKAGE_FILE_TAG = 0x9E2A83C1       # 正确字节序魔术标签
   PACKAGE_FILE_TAG_SWAPPED = 0xC1832A9E  # 交换字节序魔术标签

   # ============================================================================
   # 版本范围常量
   # ============================================================================

   UE5_VERSION_MIN = 0                # UE5 版本最低值（接受任何 UE5 文件）
   LEGACY_FILE_VERSION_MIN = -9       # LegacyFileVersion 范围下限
   LEGACY_FILE_VERSION_MAX = -2       # LegacyFileVersion 范围上限

   # ============================================================================
   # 边界验证常量（防御性编程）
   # ============================================================================

   MAX_NAME_COUNT = 10_000_000        # Maximum name table entries
   MAX_IMPORT_COUNT = 1_000_000       # Maximum import table entries
   MAX_EXPORT_COUNT = 1_000_000       # Maximum export table entries
   MAX_CUSTOM_VERSIONS = 10_000       # Maximum custom version entries
   MMAP_THRESHOLD = 50 * 1024 * 1024  # 50MB - switch to mmap above this
   MAX_PROPERTY_COUNT = 10_000        # Property loop limit

   # ============================================================================
   # PropertyTag标志
   # ============================================================================

   PROP_TAG_NONE = 0x00
   PROP_TAG_HAS_ARRAY_INDEX = 0x01      # ArrayIndex field present
   PROP_TAG_HAS_PROPERTY_GUID = 0x02    # PropertyGuid field present
   PROP_TAG_HAS_EXTENSIONS = 0x04       # Extension data
   PROP_TAG_HAS_BINARY_OR_NATIVE = 0x08 # Binary/native serialize
   PROP_TAG_BOOL_TRUE = 0x10            # Bool value is true
   PROP_TAG_SKIPPED_SERIALIZE = 0x20    # Skipped serialize

   # ============================================================================
   # PropertyTag版本阈值
   # ============================================================================

   PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012  # UE5 format switch threshold
   VER_UE4_STRUCT_GUID_IN_PROPERTY_TAG = 500
   VER_UE4_PROPERTY_GUID_IN_PROPERTY_TAG = 510

   # ============================================================================
   # Package Flags
   # ============================================================================

   PKG_Cooked = 0x200                     # Package is cooked
   PKG_UnversionedProperties = 0x2000     # Uses unversioned property serialization
   PKG_FilterEditorOnly = 0x00000080      # Filter editor-only objects

   # ============================================================================
   # 蓝图图解析安全常量
   # ============================================================================

   MAX_PINS_PER_NODE = 1000               # 单节点最大引脚数
   MAX_NODES_PER_GRAPH = 5000             # 单图最大节点数
   MAX_LINKEDTO_PER_PIN = 100             # 单引脚最大连接数

   # ============================================================================
   # UE5版本常量（EUnrealEngineObjectUE5Version）
   # ============================================================================

   UE5_NAMES_REFERENCED_FROM_EXPORT_DATA = 1001
   UE5_PAYLOAD_TOC = 1002
   UE5_LARGE_WORLD_COORDINATES = 1004
   UE5_FSOFTOBJECTPATH_REMOVE_ASSET_PATH_FNAMES = 1007
   UE5_ADD_SOFTOBJECTPATH_LIST = 1008
   UE5_DATA_RESOURCES = 1009
   UE5_SCRIPT_SERIALIZATION_OFFSET = 1010
   UE5_PROPERTY_TAG_EXTENSION = 1011
   UE5_PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012
   UE5_ASSETREGISTRY_PACKAGEBUILDDEPENDENCIES = 1013
   UE5_METADATA_SERIALIZATION_OFFSET = 1014
   UE5_VERSE_CELLS = 1015
   UE5_PACKAGE_SAVED_HASH = 1016
   UE5_OS_SUB_OBJECT_SHADOW_SERIALIZATION = 1017
   UE5_IMPORT_TYPE_HIERARCHIES = 1018

   # ============================================================================
   # UE4版本常量（EUnrealEngineObjectUE4Version）
   # ============================================================================

   UE4_ADDED_PACKAGE_SUMMARY_LOCALIZATION_ID = 385
   UE4_SERIALIZE_TEXT_IN_PACKAGES = 401
   UE4_WORLD_LEVEL_INFO = 223
   UE4_ADDED_CHUNKID = 277
   UE4_CHANGED_CHUNKID_TO_ARRAY = 341
   UE4_ENGINE_VERSION_OBJECT = 334
   UE4_ADD_STRING_ASSET_REFERENCES_MAP = 382
   UE4_PACKAGE_SUMMARY_HAS_COMPATIBLE_ENGINE_VERSION = 442
   UE4_LOAD_FOR_EDITOR_GAME = 365
   UE4_COOKED_ASSETS_IN_EDITOR_SUPPORT = 485
   UE4_PRELOAD_DEPENDENCIES_IN_COOKED_EXPORTS = 507
   VER_UE4_TemplateIndex_IN_COOKED_EXPORTS = 508
   UE4_ADDED_SEARCHABLE_NAMES = 510
   VER_UE4_64BIT_EXPORTOFFSETS = 511
   UE4_ADDED_PACKAGE_OWNER = 518
   UE4_NON_OUTER_PACKAGE_IMPORT = 520

   # ============================================================================
   # CustomVersion GUIDs
   # ============================================================================

   FFRAMEWORK_OBJECT_VERSION_GUID = "CFFC743F-43B04480-939114DF-171D2073"
   FUE5_MAINSTREAM_VERSION_GUID = "697DD581-E64F41AB-AA4A51EC-BEB7B628"
   FRELEASE_OBJECT_VERSION_GUID = "9C54D522-A8264FBE-94210746-61B482D0"

   # ============================================================================
   # FrameworkObjectVersion阈值
   # ============================================================================

   FFRAMEWORK_VERSION_ED_GRAPH_PIN_CONTAINER_TYPE = 15
   FFRAMEWORK_VERSION_PINS_STORE_FNAME = 19

   # ============================================================================
   # FUE5MainStreamObjectVersion阈值
   # ============================================================================

   FUE5_MAINSTREAM_VERSION_ED_GRAPH_PIN_SOURCE_INDEX = 50

   # ============================================================================
   # FReleaseObjectVersion阈值
   # ============================================================================

   FRELEASE_VERSION_PIN_TYPE_UOBJECT_WRAPPER = 10

   # ============================================================================
   # 调试标志
   # ============================================================================

   import sys
   DEBUG_PIN_PARSING = "--debug-pin" in sys.argv or "--debug-pins" in sys.argv

   # ============================================================================
   # 控制流节点集合（用于蓝图图解析）
   # ============================================================================

   CONTROL_FLOW_NODES = frozenset({
       "K2Node_IfThenElse",
       "K2Node_Switch",
       "K2Node_ForLoop",
       "K2Node_WhileLoop",
       "K2Node_DoWhileLoop",
       "K2Node_ForEachLoop",
       "K2Node_Break",
       "K2Node_Continue",
       "K2Node_CommutativeAssociativeBinaryOperator",
   })

   # ============================================================================
   # 开始事件类型集合
   # ============================================================================

   START_EVENT_TYPES = frozenset({
       "K2Node_Event",
       "K2Node_FunctionEntry",
       "K2Node_CustomEvent",
   })

   # ============================================================================
   # 分支类型映射
   # ============================================================================

   BRANCH_TYPE_MAP = {
       "K2Node_IfThenElse": "if-else",
       "K2Node_Switch": "switch",
       "K2Node_ForLoop": "for-loop",
       "K2Node_WhileLoop": "while-loop",
       "K2Node_DoWhileLoop": "do-while-loop",
       "K2Node_ForEachLoop": "for-each-loop",
   }

   # ============================================================================
   # 输出格式配置
   # ============================================================================

   FORMAT_CONFIG = {
       "indent_size": 2,
       "max_line_length": 100,
       "show_hidden": False,
       "show_private": False,
   }

   # ============================================================================
   # 图类型映射
   # ============================================================================

   GRAPH_TYPE_MAP = {
       "UEdGraph": "default",
       "UberGraph": "execution",
       "FunctionGraph": "function",
       "MacroGraph": "macro",
       "AnimationGraph": "animation",
   }

   # ============================================================================
   # CLI退出代码
   # ============================================================================

   EXIT_SUCCESS = 0
   EXIT_PARSE_ERROR = 1
   EXIT_FILE_NOT_FOUND = 2
   EXIT_ARGUMENT_ERROR = 3
   ```

3. **确保完整性**：
   - 所有常量从uasset_read.py提取，无遗漏
   - 保持原始值和注释不变
   - 按功能分组（package标签、版本、边界、PropertyTag等）
   - 包含所有集合和字典常量（CONTROL_FLOW_NODES, BRANCH_TYPE_MAP等）

**为什么这个组织方式：**
- D-10要求按扁平分组（所有常量在单一文件）
- D-11要求从现有代码提取（保持原有值和注释）
- 按功能分组便于后续查找和维护
- 集合和字典常量也需要提取（后续解析器会用到）
</action>

<verify>
<automated>
test -f src/uasset_read/constants.py && grep -q "PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012" src/uasset_read/constants.py && grep -q "PACKAGE_FILE_TAG = 0x9E2A83C1" src/uasset_read/constants.py && grep -q "CONTROL_FLOW_NODES = frozenset" src/uasset_read/constants.py
</automated>
</verify>

<done>
constants.py存在，包含所有常量定义（版本号、阈值、边界常量、集合、字典），保持原有值和注释，符合D-10/D-11决策。
</done>

---

### 任务 2：创建exceptions.py并提取所有异常类

<read_first>
- uasset_read.py — 读取第158-196行（异常类定义区域）
- .planning/research/STACK.md — 异常类组织建议（D-12/D-13）
- .planning/phases/27-project-structure-init/27-CONTEXT.md — D-12/D-13决策
</read_first>

<action>
创建src/uasset_read/exceptions.py，从uasset_read.py提取所有异常类定义（per D-12/D-13）：

1. **读取uasset_read.py提取所有异常类**：
   - 读取第158-196行（异常类定义区域）
   - 提取所有异常类：UAssetError, VersionError, ErrorContext, ParseError

2. **创建src/uasset_read/exceptions.py**：
   ```python
   """
   uasset_read异常类定义

   包含所有异常类，用于错误处理和优雅降级。
   从uasset_read.py提取（per D-13）。
   """

   from dataclasses import dataclass, field
   from typing import Optional, Dict


   # ============================================================================
   # 自定义异常（优雅降级）
   # ============================================================================

   class UAssetError(Exception):
       """uasset解析错误基类"""
       pass


   class VersionError(UAssetError):
       """版本不支持错误"""
       pass


   @dataclass
   class ErrorContext:
       """
       错误上下文信息。

       记录错误发生时的解析状态，帮助定位问题。
       Phase 6新增：导出表解析阶段信息。
       """

       offset: int           # 文件偏移位置
       phase: str            # 解析阶段：header/name_table/import_map/export_map/properties/blueprint
       operation: str        # 操作类型：read_i32/read_name/seek 等
       context_name: str = ""  # 相关对象名或属性名
       # Phase 6新增：导出表解析阶段信息
       export_index: Optional[int] = None    # 当前导出索引（0-based）
       expected_offset: Optional[int] = None  # 期望偏移
       actual_offset: Optional[int] = None    # 实际偏移
       field_name: str = ""                  # 字段名（如 "TemplateIndex"）
       version_info: Dict[str, int] = field(default_factory=dict)  # 版本检查失败信息


   class ParseError(UAssetError):
       """解析错误（可携带部分结果和上下文）"""

       def __init__(self, message: str, partial_result: Optional[Dict] = None, context: Optional[ErrorContext] = None):
           super().__init__(message)
           self.partial_result = partial_result
           self.context = context
   ```

3. **确保完整性**：
   - 所有异常类从uasset_read.py提取，无遗漏
   - 保持原始类结构和功能不变
   - ErrorContext保留所有字段（包括Phase 6新增字段）
   - ParseError保留__init__方法的三个参数

**为什么这个组织方式：**
- D-12要求单一模块包含所有异常类
- D-13要求从现有代码提取（保持原有类结构和功能）
- 保持原有的异常层次结构（UAssetError → VersionError/ParseError）
- ErrorContext是dataclass，需要导入dataclasses
</action>

<verify>
<automated>
test -f src/uasset_read/exceptions.py && grep -q "class UAssetError" src/uasset_read/exceptions.py && grep -q "class VersionError" src/uasset_read/exceptions.py && grep -q "class ErrorContext" src/uasset_read/exceptions.py && grep -q "class ParseError" src/uasset_read/exceptions.py
</automated>
</verify>

<done>
exceptions.py存在，包含所有异常类（UAssetError, VersionError, ErrorContext, ParseError），保持原有类结构和功能，符合D-12/D-13决策。
</done>

---

### 任务 3：更新__init__.py导出常量和异常

<read_first>
- src/uasset_read/__init__.py — 读取当前内容
- .planning/research/STACK.md — __init__.py API控制策略
- .planning/phases/27-project-structure-init/27-CONTEXT.md — D-08/D-09决策
</read_first>

<action>
更新src/uasset_read/__init__.py，导出常量和异常类（per D-08/D-09）：

1. **读取当前__init__.py内容**（如果存在）
2. **修改__init__.py**：
   ```python
   """
   uasset_read - Unreal Engine .uasset 文件解析器

   模块化重构版本 v5.1 — src layout，零依赖，分层架构

   公共API通过__all__控制，初始阶段导出常量和异常类。
   """
   __version__ = "5.1.0"

   # 导出常量模块
   from .constants import (
       PACKAGE_FILE_TAG,
       PACKAGE_FILE_TAG_SWAPPED,
       UE5_VERSION_MIN,
       LEGACY_FILE_VERSION_MIN,
       LEGACY_FILE_VERSION_MAX,
       MAX_NAME_COUNT,
       MAX_IMPORT_COUNT,
       MAX_EXPORT_COUNT,
       MAX_CUSTOM_VERSIONS,
       MMAP_THRESHOLD,
       MAX_PROPERTY_COUNT,
       PROPERTY_TAG_COMPLETE_TYPE_NAME,
       # 导出其他常用常量（后续阶段按需添加）
   )

   # 导出异常类
   from .exceptions import (
       UAssetError,
       VersionError,
       ErrorContext,
       ParseError,
   )

   # 公共API导出控制（per D-09）
   __all__ = [
       # 版本号
       "__version__",
       # 常量
       "PACKAGE_FILE_TAG",
       "PACKAGE_FILE_TAG_SWAPPED",
       "UE5_VERSION_MIN",
       "LEGACY_FILE_VERSION_MIN",
       "LEGACY_FILE_VERSION_MAX",
       "MAX_NAME_COUNT",
       "MAX_IMPORT_COUNT",
       "MAX_EXPORT_COUNT",
       "MAX_CUSTOM_VERSIONS",
       "MMAP_THRESHOLD",
       "MAX_PROPERTY_COUNT",
       "PROPERTY_TAG_COMPLETE_TYPE_NAME",
       # 异常类
       "UAssetError",
       "VersionError",
       "ErrorContext",
       "ParseError",
   ]
   ```

3. **为什么初始导出这些**：
   - D-08要求初始阶段不导出API（现在开始导出基础模块）
   - D-09要求使用__all__控制公共接口
   - 常量和异常是所有后续模块的基础依赖
   - 导出常用常量，后续阶段可按需添加更多
   - 所有异常类都需要导出（用于错误处理）

**注意：**
- 这是首次填充__all__，从空列表变为包含常量和异常
- 后续阶段（28-32）会继续添加更多导出（FArchive, models, parsers等）
- 仅导出常用常量，所有常量可通过`import uasset_read.constants as const`访问
</action>

<verify>
<automated>
test -f src/uasset_read/__init__.py && grep -q "from \.constants import" src/uasset_read/__init__.py && grep -q "from \.exceptions import" src/uasset_read/__init__.py && grep -q "PACKAGE_FILE_TAG" src/uasset_read/__init__.py && grep -q "UAssetError" src/uasset_read/__init__.py
</automated>
</verify>

<done>
__init__.py更新完成，导出常量模块（PACKAGE_FILE_TAG, UE5_VERSION_MIN等）和异常类（UAssetError, VersionError, ErrorContext, ParseError），__all__填充完整，符合D-08/D-09决策。
</done>

## 威胁模型

## 信任边界

| 边界 | 描述 |
|----------|-------------|
| 导入代码 → constants.py | 常量值必须正确，否则影响解析逻辑 |
| 导入代码 → exceptions.py | 异常类必须正确，否则影响错误处理 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Tampering | constants.py | mitigate | 保持原有值和注释，验证提取完整性 |
| T-27-05 | Tampering | exceptions.py | mitigate | 保持原有类结构和功能，验证提取完整性 |
| T-27-06 | Information Disclosure | __all__ | accept | 导出的API是公共接口，不应包含敏感信息 |

## 验证

验证Phase 27-02成功：

1. **constants.py验证**：
   ```bash
   test -f src/uasset_read/constants.py && grep -c "^[A-Z_][A-Z0-9_]*\s*=" src/uasset_read/constants.py
   ```
   应返回大于50（包含所有常量）。

2. **exceptions.py验证**：
   ```bash
   test -f src/uasset_read/exceptions.py && grep -E "class (UAssetError|VersionError|ErrorContext|ParseError)" src/uasset_read/exceptions.py | wc -l
   ```
   应返回4（4个异常类）。

3. **__init__.py验证**：
   ```bash
   python -c "from src.uasset_read import PACKAGE_FILE_TAG, UAssetError, __version__; print(__version__)"
   ```
   应输出 "5.1.0" 且无错误。

4. **导入验证**：
   ```bash
   python -c "from src.uasset_read.constants import MAX_NAME_COUNT; print(MAX_NAME_COUNT)"
   ```
   应输出 "10000000" 且无错误。

## 成功标准

Phase 27-02成功标准：

1. **给定**uasset_read.py中的常量定义，**当**提取到constants.py，**则**所有常量值和注释保持不变（如PROPERTY_TAG_COMPLETE_TYPE_NAME = 1012）。

2. **给定**uasset_read.py中的异常类，**当**提取到exceptions.py，**则**所有异常类结构和功能保持不变（如UAssetError基类、ParseError带partial_result参数）。

3. **给定**新创建的constants.py和exceptions.py，**当**更新__init__.py，**则**公共API导出包含常量和异常类，可通过`from src.uasset_read import *`访问。

## 输出

完成后，创建`.planning/phases/27-project-structure-init/27-02-SUMMARY.md`，记录：
- 提取的常量列表和值
- 提取的异常类列表和功能
- __init__.py更新的导出列表
- 验证结果