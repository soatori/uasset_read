---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/uasset-read/SKILL.md
autonomous: true
requirements: [SKILL-01]
must_haves:
  truths:
    - "用户可以在.claude/skills/uasset-read/目录中找到SKILL.md主文件"
    - "SKILL.md包含触发词字段：uasset、.uasset、蓝图解析、蓝图图、parse_uasset、uasset_read"
    - "SKILL.md包含'能做什么'和'不能做什么'章节"
    - "SKILL.md包含知识库索引和示例索引表格"
  artifacts:
    - path: ".claude/skills/uasset-read/SKILL.md"
      provides: "skill主入口文件"
      contains: "| Skill 名称 | uasset-read |"
      min_lines: 50
  key_links:
    - from: "SKILL.md触发词"
      to: "Claude Code skill系统"
      via: "表格frontmatter格式"
      pattern: "| 触发词 |"
---

# 15-01: SKILL.md 主文件创建

<objective>
创建uasset-read skill的主入口文件SKILL.md，定义触发词、能力范围、知识库索引和示例索引。

Purpose: Claude Code通过触发词自动识别并加载skill，用户无需记忆API名称。
Output: .claude/skills/uasset-read/SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-claude-code-skill-packaging/15-CONTEXT.md
@.planning/phases/15-claude-code-skill-packaging/15-RESEARCH.md
@.claude/skills/lyra-course/SKILL.md
@uasset_read.py:4472-4621
@.planning/phases/14-output-format-optimization/14-VERIFICATION.md
</context>

<interfaces>
<!-- 关键API签名供executor参考 -->
From uasset_read.py:
```python
def parse_uasset(path: str) -> ParseResult:
    """主入口：解析 .uasset 文件"""

# 输出格式（output_version: "3.0"）
format_json_full(result) -> dict  # 完整JSON
format_json_summary(result) -> dict  # 精简JSON（70%+ token减少）
format_markdown(result) -> str  # Markdown格式
```

From Phase 14 VERIFICATION.md:
```json
{
  "status": { "status": "success/fail/error" },
  "output_version": "3.0",
  "graphs_summary": [{ "graph_name": "...", "execution_flows": [...] }],
  "exports": [{ "name": "...", "class": "...", "parent_class": "..." }]
}
```
</interfaces>

<tasks>

## Task 1: 创建skill目录结构

<read_first>
- .claude/skills/lyra-course/SKILL.md (参考目录结构和frontmatter格式)
- .planning/phases/15-claude-code-skill-packaging/15-CONTEXT.md (锁定决策D-15-02)
</read_first>

<action>
创建.claude/skills/uasset-read/目录结构，包含knowledge/和examples/子目录。

使用Bash工具执行：
```bash
mkdir -p .claude/skills/uasset-read/knowledge
mkdir -p .claude/skills/uasset-read/examples
```

目录结构：
```
.claude/skills/uasset-read/
├── SKILL.md          # 本plan创建
├── knowledge/        # 知识库目录（Plan 15-02填充）
└── examples/         # 示例目录（Plan 15-03填充）
```

Per D-15-02: 安装位置锁定为 .claude/skills/uasset-read/
</action>

<verify>
ls -la .claude/skills/uasset-read/ && ls .claude/skills/uasset-read/knowledge/ && ls .claude/skills/uasset-read/examples/
</verify>

<acceptance_criteria>
- .claude/skills/uasset-read/ 目录存在
- .claude/skills/uasset-read/knowledge/ 目录存在
- .claude/skills/uasset-read/examples/ 目录存在
</acceptance_criteria>

## Task 2: 创建SKILL.md主文件

<read_first>
- .claude/skills/lyra-course/SKILL.md (参考frontmatter格式和章节结构)
- .planning/phases/15-claude-code-skill-packaging/15-CONTEXT.md (锁定决策D-15-01触发词)
- .planning/phases/15-claude-code-skill-packaging/15-RESEARCH.md (SKILL.md模板推荐)
- CLAUDE.md (项目概述，可复用内容)
</read_first>

<action>
创建SKILL.md文件，包含以下结构（per D-15-01触发词锁定）：

**Frontmatter表格（参考lyra-course格式）：**
```markdown
# uasset-read

| 字段 | 值 |
|------|-----|
| Skill 名称 | uasset-read |
| 版本 | v3.0 |
| 分类 | Unreal Engine 资产解析 |
| 触发词 | uasset、.uasset、蓝图解析、蓝图图、parse_uasset、uasset_read |
```

**章节结构：**
1. Skill 说明
   - 能做什么（6项能力：解析蓝图、提取变量、组件列表、EventGraph流程、输出格式、C++转换参考）
   - 不能做什么（4项限制：不解析Cooked资产、不生成完整C++代码、不反编译字节码、不解析Verse）
2. 快速开始（Python API调用示例，使用parse_uasset）
3. 输出格式说明（output_version: "3.0"，status/grahps_summary/exports字段）
4. 知识库索引（表格列出6个knowledge文件）
5. 示例索引（表格列出4个example文件）

**触发词per D-15-01:** uasset、.uasset、蓝图解析、蓝图图、parse_uasset、uasset_read（必须全部包含）

**内容来源复用：**
- CLAUDE.md项目概述 → Skill说明章节
- Phase 14 VERIFICATION.md → 输出格式说明
- 15-RESEARCH.md → 知识库/示例索引

**文件行数目标：** 80-120行（frontmatter + 5个章节）
</action>

<verify>
grep -c "| Skill 名称 | uasset-read |" .claude/skills/uasset-read/SKILL.md && grep -c "| 触发词 |" .claude/skills/uasset-read/SKILL.md && grep -c "蓝图解析" .claude/skills/uasset-read/SKILL.md
</verify>

<acceptance_criteria>
- SKILL.md文件存在且行数 >= 50
- 包含表格行 "| Skill 名称 | uasset-read |"
- 包含表格行 "| 触发词 |"
- 触发词包含"uasset"、"蓝图解析"、"parse_uasset"（D-15-01锁定）
- 包含"能做什么"章节
- 包含"不能做什么"章节
- 包含"知识库索引"表格
- 包含"示例索引"表格
</acceptance_criteria>

</tasks>

<verification>
## Verification Steps

1. 检查目录结构：
   ```bash
   ls -la .claude/skills/uasset-read/
   ```
   预期：SKILL.md、knowledge/、examples/三项存在

2. 检查SKILL.md frontmatter：
   ```bash
   grep "| Skill 名称 | uasset-read |" .claude/skills/uasset-read/SKILL.md
   grep "| 触发词 |" .claude/skills/uasset-read/SKILL.md
   ```
   预期：两行都存在

3. 检查触发词完整性（D-15-01）：
   ```bash
   grep "uasset" .claude/skills/uasset-read/SKILL.md | head -5
   grep "蓝图解析" .claude/skills/uasset-read/SKILL.md
   grep "parse_uasset" .claude/skills/uasset-read/SKILL.md
   ```
   预期：每个触发词至少出现1次

4. 检查章节结构：
   ```bash
   grep "## 能做什么" .claude/skills/uasset-read/SKILL.md
   grep "## 不能做什么" .claude/skills/uasset-read/SKILL.md
   grep "## 知识库索引" .claude/skills/uasset-read/SKILL.md
   grep "## 示例索引" .claude/skills/uasset-read/SKILL.md
   ```
   预期：四个章节都存在
</verification>

<success_criteria>
## Must-Haves (Goal-Backward)

| Truth | Artifact | Verification |
|-------|----------|--------------|
| 用户可以在.claude/skills/uasset-read/目录中找到SKILL.md | .claude/skills/uasset-read/SKILL.md存在 | `ls`命令验证 |
| SKILL.md包含触发词字段（D-15-01锁定） | frontmatter表格包含触发词行 | `grep`验证 |
| SKILL.md包含能力说明 | "能做什么"/"不能做什么"章节 | `grep`验证 |
| SKILL.md包含索引表格 | 知识库索引/示例索引表格 | `grep`验证 |

**Acceptance:** 所有4项must-haves达成 → Plan完成
</success_criteria>

<output>
完成后创建 `.planning/phases/15-claude-code-skill-packaging/15-01-SUMMARY.md`
</output>