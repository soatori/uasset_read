---
context: phase
phase: 35b-pin-connection-debug
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-05-12T18:28:05Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Direction/FName byte drift — UE5 EdGraphPin Direction field is followed by 4 extra bytes before PinCategory FName

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Byte Drift Accumulation | Incorrect bool/field size reading causes cumulative position misalignment | blocking | Pre-validate field sizes against UE5 source before implementation |
| Missing Version References | Hardcoding UE5 version checks without referencing official engine source leads to incorrect assumptions | advisory | Always cross-check against Unreal Engine source code for serialization logic |


<current_state>
## Where exactly are we?

Phase 35b "Pin Connection Deep Debug & Fix" is partially complete. All 5 planned tasks have been executed:
- ✅ 35b-01: read_bool_ue5() + PinType bools (fixed UE5 1-byte bool serialization)
- ✅ 35b-02: BitField u32 fix (UE5 serializes BitField as 4 bytes instead of 1)
- ✅ 35b-03: FText b_has_culture fix + custom 8-byte mystery data handling
- ✅ 35b-04: Binary trace tool created for field-level position verification
- ⚠️ 35b-05: Integration tests created & Phase 21 unskipped, but linked_to_raw still empty

Current test results: 423 passed, 67 skipped, 7 failed. All failures cascade from linked_to_raw being empty.

The core remaining issue: After PinToolTipLen=0, the raw bytes show:
```
00 00 00 00 ff 00 00 00 00 ff ff ff ff 00 00 00 00 94 00 00 00
```

Our code expects:
- Direction u8 (1 byte) → PinCategory FName (8 bytes)

But actual structure:
- Direction reads correctly as 0 (1 byte)
- Then there are 4 extra bytes before valid FName data appears
- PinCategory reads as garbage index 16711680 = 0x00FF0000
- Hypothesis: UE5 adds padding/marker bytes between Direction and PinCategory FName
</current_state>

<completed_work>

Completed Tasks:
- Task 1: 35b-01 - Add read_bool_ue5() method to FArchive and use for UE5 PinType bools - Done (commit 6363f82)
- Task 2: 35b-02 - Fix BitField to always read u32 for both UE4/UE5 - Done (commit 23ade58)
- Task 3: 35b-03 - Fix FText b_has_culture reading and add 8-byte custom type handling - Done (commit 4302180)
- Task 4: 35b-04 - Create binary_trace_pin.py diagnostic tool - Done (commit 4302180)
- Task 5: 35b-05 - Create UE5 pin integration tests and unskip Phase 21 tests - Partial, tests created but 3/6 failing due to linked_to_raw empty
</completed_work>

<remaining_work>

- Task 6: Investigate Direction/FName byte drift - Investigate why 4 extra bytes appear between Direction and PinCategory
- Task 7: Research UE5 C++ source reference (EdGraphPin.cpp L1838-1964) for exact field boundaries for version 1017
- Task 8: Fix linked_to_raw field population - Resolve the root cause to enable execution_flows and data_flows
</remaining_work>

<decisions_made>

- Decided UE5 bools serialize as uint8 (1 byte) instead of uint32 (4 bytes) — confirmed via binary trace and unit tests
- Decided BitField uses uint32 in UE5 — fixed to read 4 bytes instead of 1
- Decided FText custom types (history_type 131-191) have 8 mystery bytes — detected via position stagnation in FString reading
- Decided to create binary_trace_pin.py for field-level byte position verification — valuable for debugging
</decisions_made>

<blockers>
- Blocker 1: Direction/FName field alignment issue — UE5 EdGraphPin serialization has extra padding/marker bytes not yet identified
- Blocker 2: Missing UE5 C++ source reference — without exact field boundaries, we cannot correctly map PinType fields
</blockers>

## Required Reading (in order)
1. `.planning/phases/35b-pin-connection-debug/35b-SUMMARY.md` — Phase summary and byte drift analysis
2. `.planning/phases/35b-pin-connection-debug/35b-05-SUMMARY.md` — Integration test results and remaining issues
3. `tools/binary_trace_pin.py` — Binary trace tool for field-level position verification
4. `src/uasset_read/serializers/graph.py` — Current pin serialization implementation

## Critical Anti-Patterns (do NOT repeat these)
- [BYTE DRIFT]: Incorrect field size reading causes cumulative position errors — mitigate by cross-referencing engine source
- [VERSIONING]: Hardcoding version checks without official source reference leads to incorrect assumptions

## Infrastructure State
- Python 3.10+ environment with pytest
- Test asset: FirstPersonCharacter.uasset from UE5.7 Samples
- Binary trace tool available at tools/binary_trace_pin.py

## Pre-Execution Critique Required
- Design artifact: Current graph.py pin serialization logic
- Critique focus: Does the current Direction/PinType handling match UE5.7 EdGraphPin::Serialize implementation?
- Gate: Do NOT begin implementation until UE5 source reference is obtained and field boundaries are confirmed

<context>
I'm currently paused in the middle of Phase 35b debugging. The user requested "先研究后执行" (research first, then execute). I've completed all basic byte drift fixes for bools, BitField, and FText, but the remaining Direction/FName alignment issue requires looking at the actual Unreal Engine 5 source code for EdGraphPin serialization. The binary trace clearly shows extra bytes between Direction and PinCategory, but without knowing the exact field layout from UE's source, I can't safely add the correct handling.

The key challenge is that the garbage FName index (16711680 = 0x00FF0000) suggests we're reading the wrong offset for the PinCategory FName — there's either 4 bytes of padding or the Direction field format changed in UE5.
</context>

<next_action>
Start with: Research UE5 EdGraphPin.cpp source code to determine the exact field boundaries for EdGraphPin::Serialize in version 1017 (UE5.7), specifically looking at the pin body structure after PinToolTip.
</next_action>