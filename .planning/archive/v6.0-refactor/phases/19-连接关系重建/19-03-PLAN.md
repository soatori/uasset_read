---
phase: 19-连接关系重建
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - uasset_read.py (新增build_data_flows函数)
  - uasset_read.py (第5060-5086行: format_graphs_json)
  - tests/test_output_formatting.py (新增data_flows测试)
autonomous: true
requirements: [LINK-03]
user_setup: []

must_haves:
  truths:
    - "用户可以在data_flows中看到Pin之间的数据传递关系"
    - "用户可以看到数据流使用name格式的节点和Pin引用"
    - "用户不会在data_flows中看到exec类型的pins"
  artifacts:
    - path: "uasset_read.py"
      provides: "数据流构建函数"
      contains: "def build_data_flows"
      min_lines: 30
    - path: "uasset_read.py"
      provides: "exec类型过滤逻辑"
      contains: "pin_category != \"exec\""
      min_lines: 1
    - path: "uasset_read.py"
      provides: "data_flows输出"
      contains: "\"data_flows\""
      min_lines: 1
  key_links:
    - from: "build_data_flows()"
      to: "format_pin_ref()"
      via: "使用19-01的格式转换函数"
      pattern: "format_pin_ref.*build_data_flows"
    - from: "format_graphs_json()"
      to: "build_data_flows()"
      via: "新增调用"
      pattern: "data_flows.*build_data_flows"
---

<objective>
实现LINK-03需求：新增build_data_flows()函数，构建非exec pins的数据传递关系，输出扁平data_flows数组。

Purpose: 区分执行流和数据流，完整反映蓝图数据传递关系。
Output: data_flows数组，格式为 `{"source": {"node": "...", "pin": "..."}, "target": {...}}`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-连接关系重建/19-CONTEXT.md
@.planning/phases/19-连接关系重建/19-RESEARCH.md
@.planning/phases/19-连接关系重建/19-01-PLAN.md (format_pin_ref依赖)

<interfaces>
<!-- 现有代码和19-01输出接口 -->

From 19-01-PLAN.md Task 2 (format_pin_ref输出):
```python
def format_pin_ref(
    node_guid: str,
    pin_name: str,
    node_name_lookup: Dict[str, str],
    mode: str = "name"
) -> Dict:
    """格式化Pin引用（D-19-02, D-19-05）。"""
    # name模式: {"node": "K2Node_CallFunction_10", "pin": "execute"}
    # guid模式: {"node_guid": "...", "pin_name": "execute"}
```

From uasset_read.py 第5060-5086行:
```python
def format_graphs_json(graphs: List[UEdGraph]) -> List[Dict]:
    """格式化蓝图图数据为 JSON 输出。"""
    for graph in graphs:
        connections, warnings = build_connections_map(graph)
        execution_flows = build_execution_flows(graph)
        
        graph_dict = {
            "graph_name": graph.graph_name,
            "nodes": [asdict(node) for node in graph.nodes],
            "connections": connections,
            "execution_flows": execution_flows,
        }
```

From REQUIREMENTS.md LINK-03示例:
```json
{
  "data_flows": [
    {
      "source": {"node": "K2Node_EnhancedInputAction_3", "pin": "ActionValue_X"},
      "target": {"node": "K2Node_CallFunction_5", "pin": "Left / Right"}
    }
  ]
}
```

From 19-CONTEXT.md D-19-06到D-19-09:
- D-19-06: 数据流包含所有非exec pins（pin_type.category != "exec"）
- D-19-07: data_flows输出结构为 `{source, target}`
- D-19-08: data_flows组织为扁平列表
- D-19-09: 数据流与执行流独立分离
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建build_data_flows()函数</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: build_data_flows()过滤exec类型pins
    - Test 2: build_data_flows()使用format_pin_ref()格式化输出
    - Test 3: build_data_flows()输出扁平数组结构
    - Test 4: build_data_flows()处理linked_to_raw dict格式
  </behavior>
  <action>
    per D-19-06到D-19-09，在build_execution_flows后添加（第5340-5390行）：

    ```python
    def build_data_flows(graph: UEdGraph, mode: str = "name") -> List[Dict]:
        """
        构建数据流图（D-19-06~09, LINK-03）。
        
        从非exec pins提取数据传递关系，构建data_flows数组。
        
        算法：
        1. 构建pin_lookup查找表（pin_id → (node_guid, pin_name)）
        2. 构建node_name_lookup查找表（node_guid → node_name）
        3. 遍历所有output pins（direction=1）
        4. 过滤exec类型pins（pin_type.category != "exec"）
        5. 对每个linked_to_raw中的目标pin，构建数据流关系
        6. 使用format_pin_ref()格式化输出（name或guid模式）
        
        Args:
            graph: UEdGraph对象
            mode: 输出格式模式（"name"或"guid"，默认"name"）
        
        Returns:
            List[Dict]: data_flows数组
            - 格式: [{"source": {"node": "...", "pin": "..."}, "target": {...}}]
        """
        # Step 1: Build pin_lookup
        pin_lookup: Dict[str, Tuple[str, str]] = {}
        for node in graph.nodes:
            for pin in node.pins:
                pin_lookup[pin.pin_id] = (node.node_guid, pin.pin_name)
        
        # Step 2: Build node_name_lookup（复用19-01逻辑）
        node_name_lookup: Dict[str, str] = {}
        for idx, node in enumerate(graph.nodes):
            node_name_lookup[node.node_guid] = _derive_node_name(node, idx)
        
        # Step 3-5: Build data flows（D-19-06/07/08）
        data_flows: List[Dict] = []
        
        for node in graph.nodes:
            for pin in node.pins:
                # D-19-06: 仅处理output pins，排除exec类型
                if pin.direction == 1:  # Output
                    if pin.pin_type and pin.pin_type.pin_category != "exec":
                        # 构建数据流关系
                        for linked_pin_id in pin.linked_to_raw:
                            # Phase 18: linked_to_raw为dict格式 {"pin_guid": str}
                            target_pin_guid = linked_pin_id.get("pin_guid") if isinstance(linked_pin_id, dict) else linked_pin_id
                            
                            if target_pin_guid in pin_lookup:
                                target_node_guid, target_pin_name = pin_lookup[target_pin_guid]
                                # D-19-07: 使用format_pin_ref格式化（依赖19-01）
                                data_flows.append({
                                    "source": format_pin_ref(node.node_guid, pin.pin_name, node_name_lookup, mode),
                                    "target": format_pin_ref(target_node_guid, target_pin_name, node_name_lookup, mode)
                                })
        
        # D-19-08: 返回扁平数组
        return data_flows
    ```

    添加测试验证（tests/test_output_formatting.py）：
    ```python
    def test_build_data_flows_filters_exec_pins():
        """验证build_data_flows过滤exec类型pins（D-19-06）"""
        from uasset_read import build_data_flows, UEdGraph, UEdGraphNode, UEdGraphPin, PinType
        
        # 构造包含exec和data pins的节点
        node1 = UEdGraphNode(
            node_guid="guid-1",
            class_name="K2Node_EnhancedInputAction",
            pins=[
                UEdGraphPin(pin_id="pin-exec", pin_name="execute", direction=1,
                           pin_type=PinType(pin_category="exec"), linked_to_raw=[{"pin_guid": "pin-2"}]),
                UEdGraphPin(pin_id="pin-data", pin_name="ActionValue_X", direction=1,
                           pin_type=PinType(pin_category="float"), linked_to_raw=[{"pin_guid": "pin-3"}]),
            ]
        )
        node2 = UEdGraphNode(
            node_guid="guid-2",
            class_name="K2Node_CallFunction",
            pins=[
                UEdGraphPin(pin_id="pin-2", pin_name="then", direction=0,
                           pin_type=PinType(pin_category="exec")),
                UEdGraphPin(pin_id="pin-3", pin_name="Left", direction=0,
                           pin_type=PinType(pin_category="float")),
            ]
        )
        graph = UEdGraph(graph_name="EventGraph", nodes=[node1, node2])
        
        data_flows = build_data_flows(graph)
        
        # 验证仅包含data flow，排除exec flow
        assert len(data_flows) == 1
        assert data_flows[0]["source"]["pin"] == "ActionValue_X"
        assert data_flows[0]["target"]["pin"] == "Left"
        # 不包含execute → then的exec连接
    
    def test_build_data_flows_name_mode_format():
        """验证build_data_flows使用name模式格式化（D-19-07）"""
        from uasset_read import build_data_flows
        
        # ... 构造测试graph ...
        data_flows = build_data_flows(graph, mode="name")
        
        # 验证name模式格式
        assert "node" in data_flows[0]["source"]
        assert "pin" in data_flows[0]["source"]
        assert "node_guid" not in data_flows[0]["source"]  # name模式不包含guid
    
    def test_build_data_flows_flat_array():
        """验证build_data_flows输出扁平数组（D-19-08）"""
        from uasset_read import build_data_flows
        
        # ... 构造多个数据流的graph ...
        data_flows = build_data_flows(graph)
        
        # 验证扁平数组结构
        assert isinstance(data_flows, list)
        for flow in data_flows:
            assert "source" in flow
            assert "target" in flow
            # 无嵌套结构
    
    def test_build_data_flows_linked_to_raw_dict():
        """验证build_data_flows处理linked_to_raw dict格式"""
        from uasset_read import build_data_flows
        
        # linked_to_raw为dict格式：{"pin_guid": str}
        node1 = UEdGraphNode(
            node_guid="guid-1",
            class_name="K2Node_VariableGet",
            pins=[
                UEdGraphPin(pin_id="pin-1", pin_name="Value", direction=1,
                           pin_type=PinType(pin_category="float"),
                           linked_to_raw=[{"pin_guid": "pin-2", "owning_node": "guid-2"}])
            ]
        )
        # ...
        
        data_flows = build_data_flows(graph)
        
        # 验证正确提取pin_guid字段
        assert len(data_flows) == 1
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_data_flows_filters_exec_pins -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_data_flows_name_mode_format -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_data_flows_flat_array -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_data_flows_linked_to_raw_dict -x</automated>
  </verify>
  <done>
    build_data_flows()函数定义在uasset_read.py第5340-5390行
    函数正确过滤exec类型pins
    使用format_pin_ref()（来自19-01）格式化输出
    输出为扁平数组结构，符合REQUIREMENTS LINK-03格式
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在format_graphs_json()中调用build_data_flows()</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: format_graphs_json()输出包含data_flows字段
    - Test 2: data_flows与execution_flows独立分离（D-19-09）
  </behavior>
  <action>
    per D-19-09，修改format_graphs_json()（第5060-5086行 → 第5060-5090行）：

    **关键修改点**：

    1. **添加build_data_flows()调用**（第5064行后）：
    ```python
    def format_graphs_json(graphs: List[UEdGraph]) -> List[Dict]:
        """格式化蓝图图数据为 JSON 输出。"""
        from dataclasses import asdict
        
        formatted = []
        for graph in graphs:
            # 构建连接映射
            connections, warnings = build_connections_map(graph)
            
            # 构建执行流（Phase 8 Wave 2）
            execution_flows = build_execution_flows(graph)
            
            # 构建数据流（Phase 19 Wave 2, D-19-09）
            data_flows = build_data_flows(graph)
            
            graph_dict = {
                "graph_name": graph.graph_name,
                "graph_class": graph.graph_class,
                "nodes": [asdict(node) for node in graph.nodes],
                "connections": connections,
                "execution_flows": execution_flows,
                "data_flows": data_flows,  # D-19-09: 数据流独立分离
            }
            
            # D-08-04: 添加 warnings（如果有）
            if warnings:
                graph_dict["warnings"] = warnings
            
            # 可选字段
            if graph.graph_guid:
                graph_dict["graph_guid"] = graph.graph_guid
            if graph.schema:
                graph_dict["schema"] = graph.schema
            
            formatted.append(graph_dict)
        
        return formatted
    ```

    添加测试验证（tests/test_output_formatting.py）：
    ```python
    def test_format_graphs_json_contains_data_flows():
        """验证format_graphs_json输出包含data_flows字段"""
        from uasset_read import format_graphs_json, UEdGraph
        
        # 构造包含数据流的graph
        # ...
        
        formatted = format_graphs_json([graph])
        
        # 验证data_flows字段存在
        assert "data_flows" in formatted[0]
        assert isinstance(formatted[0]["data_flows"], list)
    
    def test_format_graphs_json_data_flows_separate_from_execution():
        """验证数据流与执行流独立分离（D-19-09）"""
        from uasset_read import format_graphs_json
        
        formatted = format_graphs_json([graph])
        
        # 验证独立分离
        assert "execution_flows" in formatted[0]
        assert "data_flows" in formatted[0]
        # 无关联关系字段
    
    def test_format_graphs_json_full_structure():
        """验证完整JSON结构包含所有字段"""
        from uasset_read import parse_uasset
        
        # 使用测试资产验证
        result = parse_uasset("BP_FirstPersonCharacter.uasset")
        formatted = format_graphs_json(result.graphs)
        
        # 验证完整结构
        graph_dict = formatted[0]
        assert "graph_name" in graph_dict
        assert "nodes" in graph_dict
        assert "connections" in graph_dict
        assert "execution_flows" in graph_dict
        assert "data_flows" in graph_dict
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_graphs_json_contains_data_flows -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_graphs_json_data_flows_separate_from_execution -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_graphs_json_full_structure -x</automated>
  </verify>
  <done>
    format_graphs_json()修改在第5060-5090行
    新增build_data_flows()调用
    graph_dict包含data_flows字段，与execution_flows独立分离
    测试验证完整JSON结构包含所有字段
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部数据 → 数据流输出 | Pin连接数据转换为用户友好数据流结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Information Disclosure | 数据流构建逻辑 | accept | 无敏感数据，纯蓝图结构分析 |

Phase 19为纯数据结构分析逻辑，无安全敏感操作。
</threat_model>

<verification>
## 整体计划验证

1. **代码验证**：
   - `grep -c "def build_data_flows" uasset_read.py` 应返回 1
   - `grep -c "\"data_flows\"" uasset_read.py` 应返回 ≥ 2（定义和使用）
   - `grep -c "pin_category != \"exec\"" uasset_read.py` 应返回 ≥ 1
   - `grep -c "format_pin_ref.*build_data_flows" uasset_read.py` 应返回 ≥ 1（验证依赖19-01）

2. **功能验证**：
   - 运行测试资产解析：`python -c "from uasset_read import parse_uasset, format_graphs_json; r = parse_uasset('BP_FirstPersonCharacter.uasset'); print(format_graphs_json(r.graphs)[0]['data_flows'])"`
   - 验证data_flows数组包含ActionValue_X/Y数据流

3. **集成验证**：
   - build_data_flows()正确调用format_pin_ref()（来自19-01）
   - format_graphs_json()输出包含connections、execution_flows、data_flows三个数组
   - data_flows与execution_flows独立分离，无关联字段
</verification>

<success_criteria>
## 成功标准

1. ✓ build_data_flows()函数定义在uasset_read.py，正确过滤exec类型pins
2. ✓ build_data_flows()使用format_pin_ref()（来自19-01）格式化输出
3. ✓ build_data_flows()输出为扁平数组结构，符合REQUIREMENTS LINK-03格式
4. ✓ build_data_flows()正确处理linked_to_raw dict格式（提取pin_guid字段）
5. ✓ format_graphs_json()调用build_data_flows()，输出包含data_flows字段
6. ✓ data_flows与execution_flows独立分离（D-19-09）
7. ✓ 测试验证data_flows构建和输出正确
</success_criteria>

<output>
完成后创建：`.planning/phases/19-连接关系重建/19-03-SUMMARY.md`
</output>