---
phase: 19-连接关系重建
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - uasset_read.py (第5180-5225行: build_execution_flows)
  - uasset_read.py (第5228-5315行: _trace_execution_from_event)
  - uasset_read.py (第5318-5330行: _get_event_name → _get_start_event_name)
  - tests/test_output_formatting.py (新增起点类型测试)
autonomous: true
requirements: [LINK-02]
user_setup: []

must_haves:
  truths:
    - "用户可以在execution_flows中看到从EnhancedInputAction节点开始的执行链路"
    - "用户可以在execution_flows中看到从VariableSet节点开始的执行链路"
    - "用户可以在execution_flows中看到从CustomEvent节点开始的执行链路"
    - "用户可以在控制流节点处看到branch_type字段（if_then_else/switch等）"
  artifacts:
    - path: "uasset_read.py"
      provides: "起点类型集合"
      contains: "START_EVENT_TYPES"
      min_lines: 5
    - path: "uasset_read.py"
      provides: "起点识别逻辑"
      contains: "class_name in START_EVENT_TYPES"
      min_lines: 1
    - path: "uasset_read.py"
      provides: "分支类型标记"
      contains: "branch_type"
      min_lines: 1
  key_links:
    - from: "build_execution_flows()"
      to: "_trace_execution_from_event()"
      via: "起点节点识别"
      pattern: "start_nodes.*START_EVENT_TYPES"
    - from: "_trace_execution_from_event()"
      to: "CONTROL_FLOW_NODES"
      via: "分支类型识别"
      pattern: "branch_type.*CONTROL_FLOW_NODES"
---

<objective>
实现LINK-02需求：扩展execution_flows起点类型，从单一K2Node_Event扩展到4种类型（K2Node_Event、K2Node_EnhancedInputAction、K2Node_VariableSet、K2Node_CustomEvent），并添加控制流节点branch_type字段输出。

Purpose: 完整反映蓝图执行流起点，明确标记控制流分支类型。
Output: execution_flows数组，包含多种起点类型的执行链路，控制流节点标记branch_type
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-连接关系重建/19-CONTEXT.md
@.planning/phases/19-连接关系重建/19-RESEARCH.md
@uasset_read.py (第5180-5225行, 第5228-5315行, 第5318-5330行)

<interfaces>
<!-- 现有代码关键接口 -->

From uasset_read.py 第5180-5225行:
```python
def build_execution_flows(graph: UEdGraph) -> List[Dict]:
    """构建执行流路径（D-08-07~11）。"""
    # Step 1: 找到所有 Event 节点
    event_nodes = [n for n in graph.nodes if n.class_name == "K2Node_Event"]
    
    for event_node in event_nodes:
        flow = _trace_execution_from_event(event_node, pin_lookup, node_lookup)
        start_event_name = _get_event_name(event_node)
        execution_flows.append({
            "start_event": start_event_name,
            "nodes": flow
        })
```

From uasset_read.py 第5278-5280行:
```python
# D-08-10: 控制流节点停止
if current_node.class_name in CONTROL_FLOW_NODES:
    flow.append({"stopped_at": "control_flow_node"})
    break
```

From uasset_read.py 第4980-4987行:
```python
CONTROL_FLOW_NODES = frozenset({
    "K2Node_IfThenElse",
    "K2Node_Switch",
    "K2Node_SwitchString",
    "K2Node_SwitchEnum",
    "K2Node_SwitchInteger",
    "K2Node_MacroInstance",
})
```

From 19-CONTEXT.md D-19-10:
- 执行流起点类型扩展：K2Node_Event、K2Node_EnhancedInputAction、K2Node_VariableSet、K2Node_CustomEvent

From 19-CONTEXT.md D-19-14:
- branch_type枚举值：if_then_else/switch/switch_enum/switch_string/switch_integer/macro_instance
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建START_EVENT_TYPES frozenset和BRANCH_TYPE_MAP</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: START_EVENT_TYPES包含4种起点类型
    - Test 2: BRANCH_TYPE_MAP正确映射CONTROL_FLOW_NODES到branch_type枚举
  </behavior>
  <action>
    per D-19-10和D-19-14，在第4980行附近添加：

    1. **START_EVENT_TYPES定义**（第4988-4993行）：
    ```python
    # D-19-10: 执行流起点类型扩展
    START_EVENT_TYPES = frozenset({
        "K2Node_Event",
        "K2Node_EnhancedInputAction",
        "K2Node_VariableSet",
        "K2Node_CustomEvent"
    })
    ```

    2. **BRANCH_TYPE_MAP定义**（第4995-5002行）：
    ```python
    # D-19-14: 控制流节点分支类型映射
    BRANCH_TYPE_MAP = {
        "K2Node_IfThenElse": "if_then_else",
        "K2Node_Switch": "switch",
        "K2Node_SwitchString": "switch_string",
        "K2Node_SwitchEnum": "switch_enum",
        "K2Node_SwitchInteger": "switch_integer",
        "K2Node_MacroInstance": "macro_instance",
    }
    ```

    添加测试验证（tests/test_output_formatting.py）：
    ```python
    def test_start_event_types_contains_four_types():
        """验证START_EVENT_TYPES包含4种起点类型（D-19-10）"""
        from uasset_read import START_EVENT_TYPES
        
        assert "K2Node_Event" in START_EVENT_TYPES
        assert "K2Node_EnhancedInputAction" in START_EVENT_TYPES
        assert "K2Node_VariableSet" in START_EVENT_TYPES
        assert "K2Node_CustomEvent" in START_EVENT_TYPES
        assert len(START_EVENT_TYPES) == 4
    
    def test_branch_type_map_complete():
        """验证BRANCH_TYPE_MAP覆盖所有CONTROL_FLOW_NODES（D-19-14）"""
        from uasset_read import BRANCH_TYPE_MAP, CONTROL_FLOW_NODES
        
        # 每个CONTROL_FLOW_NODES都有对应的branch_type
        for node_type in CONTROL_FLOW_NODES:
            assert node_type in BRANCH_TYPE_MAP
        
        # 验证枚举值命名
        assert BRANCH_TYPE_MAP["K2Node_IfThenElse"] == "if_then_else"
        assert BRANCH_TYPE_MAP["K2Node_SwitchEnum"] == "switch_enum"
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_start_event_types_contains_four_types -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_branch_type_map_complete -x</automated>
  </verify>
  <done>
    START_EVENT_TYPES定义在uasset_read.py第4988-4993行，包含4种起点类型
    BRANCH_TYPE_MAP定义在第4995-5002行，正确映射CONTROL_FLOW_NODES
    测试验证集合完整性和映射正确性
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修改build_execution_flows()起点识别逻辑</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: build_execution_flows()识别所有START_EVENT_TYPES起点
    - Test 2: build_execution_flows()为EnhancedInputAction各触发时机分别追踪（D-19-12）
    - Test 3: build_execution_flows()使用start_event字段标识起点（D-19-11）
  </behavior>
  <action>
    per D-19-10到D-19-12，修改build_execution_flows()（第5180-5225行 → 第5190-5245行）：

    **关键修改点**：

    1. **修改起点节点识别**（第5210-5212行 → 第5210-5212行）：
    ```python
    # Step 1: 找到所有起点节点（D-19-10）
    start_nodes = [n for n in graph.nodes if n.class_name in START_EVENT_TYPES]
    ```

    2. **处理不同起点类型的追踪逻辑**（第5213-5245行）：
    ```python
    for start_node in start_nodes:
        # D-19-12: EnhancedInputAction各触发时机分别追踪
        if start_node.class_name == "K2Node_EnhancedInputAction":
            # 遍历output exec pins，为每个触发时机构建执行流
            for pin in start_node.pins:
                if pin.direction == 1 and pin.pin_type and pin.pin_type.pin_category == "exec":
                    # pin.pin_name即为触发时机（Started/Triggered/Completed）
                    flow = _trace_execution_from_pin(start_node, pin, pin_lookup, node_lookup)
                    execution_flows.append({
                        "start_event": f"{start_node.class_name}.{pin.pin_name}",  # D-19-11
                        "nodes": flow
                    })
        else:
            # 其他起点类型：标准追踪
            flow = _trace_execution_from_event(start_node, pin_lookup, node_lookup)
            start_event_name = _get_start_event_name(start_node)  # 重命名函数
            execution_flows.append({
                "start_event": start_event_name,  # D-19-11
                "nodes": flow
            })
    ```

    3. **新增_trace_execution_from_pin()函数**（第5318行后）：
    ```python
    def _trace_execution_from_pin(
        start_node: UEdGraphNode,
        start_pin: UEdGraphPin,
        pin_lookup: Dict[str, Tuple[str, str]],
        node_lookup: Dict[str, UEdGraphNode]
    ) -> List[Dict]:
        """
        从特定Pin开始追踪执行流（D-19-12）。
        
        用于EnhancedInputAction多触发时机追踪。
        
        Args:
            start_node: 起点节点
            start_pin: 起点output exec pin
            pin_lookup: pin_id → (node_guid, pin_name) 查找表
            node_lookup: node_guid → node 查找表
        
        Returns:
            List[Dict]: 节点信息序列
        """
        # 从start_pin的linked_to_raw查找下一节点
        for linked_pin_id in start_pin.linked_to_raw:
            target_pin_guid = linked_pin_id.get("pin_guid") if isinstance(linked_pin_id, dict) else linked_pin_id
            if target_pin_guid in pin_lookup:
                target_node_guid, _ = pin_lookup[target_pin_guid]
                next_node = node_lookup.get(target_node_guid)
                if next_node:
                    # 从下一节点开始标准追踪
                    return _trace_execution_from_event(next_node, pin_lookup, node_lookup)
        
        return []  # 无连接
    ```

    添加测试验证（tests/test_output_formatting.py）：
    ```python
    def test_build_execution_flows_enhanced_input_action():
        """验证EnhancedInputAction多触发时机追踪（D-19-12）"""
        from uasset_read import build_execution_flows, UEdGraph, UEdGraphNode, UEdGraphPin, PinType
        
        # 构造EnhancedInputAction节点（多个output exec pins）
        node = UEdGraphNode(
            node_guid="guid-1",
            class_name="K2Node_EnhancedInputAction",
            pins=[
                UEdGraphPin(pin_id="pin-1", pin_name="Started", direction=1,
                           pin_type=PinType(pin_category="exec"), linked_to_raw=[{"pin_guid": "pin-2"}]),
                UEdGraphPin(pin_id="pin-2", pin_name="Triggered", direction=1,
                           pin_type=PinType(pin_category="exec"), linked_to_raw=[{"pin_guid": "pin-3"}]),
                UEdGraphPin(pin_id="pin-3", pin_name="Completed", direction=1,
                           pin_type=PinType(pin_category="exec"), linked_to_raw=[{"pin_guid": "pin-4"}]),
            ]
        )
        # ... 构造完整graph（包含目标节点）...
        
        flows = build_execution_flows(graph)
        
        # 验证3个触发时机都有独立执行流
        assert len(flows) == 3
        assert any("Started" in flow["start_event"] for flow in flows)
        assert any("Triggered" in flow["start_event"] for flow in flows)
        assert any("Completed" in flow["start_event"] for flow in flows)
    
    def test_build_execution_flows_variable_set_start():
        """验证VariableSet节点识别为起点"""
        from uasset_read import build_execution_flows
        
        # 构造VariableSet节点
        node = UEdGraphNode(node_guid="guid-1", class_name="K2Node_VariableSet", ...)
        graph = UEdGraph(graph_name="EventGraph", nodes=[node])
        
        flows = build_execution_flows(graph)
        
        # 验证VariableSet为起点
        assert len(flows) >= 1
        assert flows[0]["start_event"] == "VariableSet"  # 或具体变量名
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_execution_flows_enhanced_input_action -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_execution_flows_variable_set_start -x</automated>
  </verify>
  <done>
    build_execution_flows()修改在第5190-5245行
    起点识别从单一K2Node_Event扩展到START_EVENT_TYPES（4种类型）
    EnhancedInputAction各触发时机分别追踪（Started/Triggered/Completed）
    使用start_event字段统一标识起点（D-19-11）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 修改_trace_execution_from_event()添加branch_type字段</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: _trace_execution_from_event()在控制流节点处输出branch_type字段
    - Test 2: branch_type值正确映射节点类型（if_then_else/switch_enum等）
  </behavior>
  <action>
    per D-19-13到D-19-15，修改_trace_execution_from_event()（第5228-5315行 → 第5230-5320行）：

    **关键修改点**：

    1. **修改控制流节点处理**（第5278-5280行 → 第5278-5290行）：
    ```python
    # D-19-13/14: 控制流节点标记停止 + 输出branch_type
    if current_node.class_name in CONTROL_FLOW_NODES:
        branch_type = BRANCH_TYPE_MAP.get(current_node.class_name, "unknown")
        node_info["branch_type"] = branch_type  # D-19-14
        node_info["stopped_at"] = "control_flow_node"  # 保留现有标记
        flow.append(node_info)
        break
    ```

    2. **修改循环检测处理**（第5250-5256行，保持现有逻辑）：
    ```python
    # D-19-15: 循环检测标记停止（节点层级）
    if current_node.node_guid in visited:
        flow.append({
            "node_guid": current_node.node_guid,
            "node_type": current_node.class_name,
            "cycle_detected": True  # 保持节点层级输出
        })
        break
    ```

    3. **重命名_get_event_name为_get_start_event_name**（第5318-5330行 → 第5325-5340行）：
    ```python
    def _get_start_event_name(node: UEdGraphNode) -> str:
        """
        获取起点节点的事件名称（D-19-11）。
        
        支持4种起点类型：
        - K2Node_Event: event_reference.member_name
        - K2Node_EnhancedInputAction: action_name或class_name
        - K2Node_VariableSet: variable_name或"VariableSet"
        - K2Node_CustomEvent: custom_event_name或"CustomEvent"
        
        Args:
            node: 起点节点
        
        Returns:
            str: 事件名称
        """
        if node.class_name == "K2Node_Event":
            if node.node_data and hasattr(node.node_data, 'event_reference'):
                return node.node_data.event_reference.member_name
        elif node.class_name == "K2Node_EnhancedInputAction":
            # 返回action_name（如果有）或class_name
            if node.node_data and hasattr(node.node_data, 'action_name'):
                return node.node_data.action_name
            return node.class_name
        elif node.class_name == "K2Node_VariableSet":
            # 返回变量名或"VariableSet"
            if node.node_data and hasattr(node.node_data, 'variable_name'):
                return node.node_data.variable_name
            return "VariableSet"
        elif node.class_name == "K2Node_CustomEvent":
            # 返回自定义事件名或"CustomEvent"
            if node.node_data and hasattr(node.node_data, 'custom_event_name'):
                return node.node_data.custom_event_name
            return "CustomEvent"
        
        return "Unknown"
    ```

    添加测试验证（tests/test_output_formatting.py）：
    ```python
    def test_trace_execution_branch_type_output():
        """验证控制流节点输出branch_type字段（D-19-14）"""
        from uasset_read import _trace_execution_from_event, UEdGraphNode, BRANCH_TYPE_MAP
        
        # 构造包含IfThenElse节点的执行流
        if_node = UEdGraphNode(
            node_guid="guid-if",
            class_name="K2Node_IfThenElse",
            pins=[]
        )
        # ... 构造完整执行链路 ...
        
        flow = _trace_execution_from_event(start_node, pin_lookup, node_lookup)
        
        # 验证branch_type字段
        if_node_entry = [n for n in flow if n.get("node_guid") == "guid-if"][0]
        assert "branch_type" in if_node_entry
        assert if_node_entry["branch_type"] == "if_then_else"
        assert if_node_entry["stopped_at"] == "control_flow_node"
    
    def test_trace_execution_cycle_detected_node_level():
        """验证循环检测在节点层级输出（D-19-15）"""
        from uasset_read import _trace_execution_from_event
        
        # 构造循环执行流（节点A → B → A）
        # ...
        
        flow = _trace_execution_from_event(start_node, pin_lookup, node_lookup)
        
        # 验证cycle_detected在节点层级
        cycle_entry = [n for n in flow if n.get("cycle_detected")][0]
        assert "node_guid" in cycle_entry
        assert "node_type" in cycle_entry
        assert cycle_entry["cycle_detected"] is True
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_trace_execution_branch_type_output -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_trace_execution_cycle_detected_node_level -x</automated>
  </verify>
  <done>
    _trace_execution_from_event()修改在第5230-5320行
    控制流节点输出branch_type字段（if_then_else/switch_enum等）
    _get_event_name重命名为_get_start_event_name，支持4种起点类型
    循环检测保持节点层级输出（cycle_detected字段）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部数据 → 执行流输出 | 节点追踪数据转换为用户友好执行流结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Information Disclosure | 执行流追踪逻辑 | accept | 无敏感数据，纯蓝图结构分析 |

Phase 19为纯数据结构分析逻辑，无安全敏感操作。
</threat_model>

<verification>
## 整体计划验证

1. **代码验证**：
   - `grep -c "START_EVENT_TYPES" uasset_read.py` 应返回 ≥ 2（定义和使用）
   - `grep -c "BRANCH_TYPE_MAP" uasset_read.py` 应返回 ≥ 2
   - `grep -c "branch_type" uasset_read.py` 应返回 ≥ 1
   - `grep -c "_get_start_event_name" uasset_read.py` 应返回 ≥ 1

2. **功能验证**：
   - 运行测试资产解析：`python -c "from uasset_read import parse_uasset; r = parse_uasset('BP_FirstPersonCharacter.uasset'); print(r.graphs[0].execution_flows)"`
   - 验证execution_flows包含EnhancedInputAction起点（IA_Jump）

3. **集成验证**：
   - format_graphs_json()调用build_execution_flows()生成execution_flows数组
   - execution_flows包含多种起点类型的执行链路
   - 控制流节点标记branch_type字段
</verification>

<success_criteria>
## 成功标准

1. ✓ START_EVENT_TYPES定义在uasset_read.py，包含4种起点类型
2. ✓ BRANCH_TYPE_MAP定义在uasset_read.py，正确映射CONTROL_FLOW_NODES到branch_type枚举
3. ✓ build_execution_flows()修改为识别START_EVENT_TYPES起点
4. ✓ EnhancedInputAction各触发时机分别追踪（Started/Triggered/Completed）
5. ✓ _trace_execution_from_event()在控制流节点处输出branch_type字段
6. ✓ _get_start_event_name()函数支持4种起点类型的名称提取
7. ✓ 测试验证起点类型识别和branch_type输出正确
</success_criteria>

<output>
完成后创建：`.planning/phases/19-连接关系重建/19-02-SUMMARY.md`
</output>