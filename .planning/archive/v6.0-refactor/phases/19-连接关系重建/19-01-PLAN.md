---
phase: 19-连接关系重建
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - uasset_read.py (第4980-5039行: build_connections_map)
  - tests/test_output_formatting.py (新增name模式测试)
autonomous: true
requirements: [LINK-01]
user_setup: []

must_haves:
  truths:
    - "用户可以在connections数组中看到name格式的节点引用（K2Node_CallFunction_1193）"
    - "用户可以在connections数组中看到pin引用使用pin_name而非pin_id"
    - "查找失败时用户可以看到warning字段和原始pin_id fallback"
  artifacts:
    - path: "uasset_read.py"
      provides: "name模式连接输出"
      contains: "FORMAT_CONFIG"
      min_lines: 5
    - path: "uasset_read.py"
      provides: "格式转换函数"
      contains: "def format_pin_ref"
      min_lines: 15
    - path: "uasset_read.py"
      provides: "node_name查找表"
      contains: "node_name_lookup"
      min_lines: 3
  key_links:
    - from: "format_pin_ref()"
      to: "build_connections_map()"
      via: "调用转换函数"
      pattern: "format_pin_ref\\(.*mode\\)"
    - from: "build_connections_map()"
      to: "format_graphs_json()"
      via: "函数调用"
      pattern: "connections.*build_connections_map"
---

<objective>
实现LINK-01需求：构建connections数组，支持name模式输出（默认）和guid模式输出（可选）。

Purpose: 用户友好的连接关系展示，隐藏内部GUID细节。
Output: connections数组，格式为 `{"from": {"node": "节点名", "pin": "pin名"}, "to": {...}}`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-连接关系重建/19-CONTEXT.md
@.planning/phases/19-连接关系重建/19-RESEARCH.md
@uasset_read.py (第4980-5039行)

<interfaces>
<!-- 现有代码关键接口 -->

From uasset_read.py 第4980-4987行:
```python
CONTROL_FLOW_NODES = frozenset({
    "K2Node_IfThenElse",
    "K2Node_Switch",
    "K2Node_SwitchString",
    "K2Node_SwitchEnum",
    "K2Node_SwitchInteger",
    "K2Node_MacroInstance",
})
```

From uasset_read.py 第4990-5039行:
```python
def build_connections_map(graph: UEdGraph) -> Tuple[List[Dict], List[str]]:
    """构建引脚连接映射（D-08-01~06）。"""
    # Step 1: Build pin_lookup: pin_id → (node_guid, pin_name)
    pin_lookup: Dict[str, Tuple[str, str]] = {}
    for node in graph.nodes:
        for pin in node.pins:
            pin_lookup[pin.pin_id] = (node.node_guid, pin.pin_name)
    
    # 当前输出格式（guid模式）:
    connections.append({
        "from": {"node_guid": node.node_guid, "pin_name": pin.pin_name},
        "to": {"node_guid": target_node_guid, "pin_name": target_pin_name}
    })
```

From REQUIREMENTS.md LINK-01示例:
```json
{
  "connections": [
    {
      "from": {"node": "K2Node_EnhancedInputAction_5", "pin": "Started"},
      "to": {"node": "K2Node_CallFunction_1193", "pin": "execute"}
    }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义FORMAT_CONFIG全局配置和节点名派生逻辑</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: FORMAT_CONFIG包含pin_reference_mode字段，默认值为"name"
    - Test 2: _derive_node_name()正确处理同名节点冲突（使用export_idx后缀）
  </behavior>
  <action>
    per D-19-03和D-19-04，在第4980行附近添加：

    1. **FORMAT_CONFIG全局配置**（第4988-4992行）：
    ```python
    # D-19-03: 连接输出格式全局可选配置
    FORMAT_CONFIG = {
        "pin_reference_mode": "name",  # D-19-04: 默认name模式
    }
    ```

    2. **节点名派生函数**（第4994-5008行）：
    ```python
    def _derive_node_name(node: UEdGraphNode, idx: int) -> str:
        """
        从节点派生用户友好的节点名（D-19-02）。
        
        策略：使用 f"{class_name}_{idx}" 格式，避免同名节点冲突。
        
        Args:
            node: UEdGraphNode节点对象
            idx: 导出表索引
        
        Returns:
            str: 用户友好的节点名
        """
        # 使用class_name + idx后缀，避免冲突
        return f"{node.class_name}_{idx}"
    ```

    3. **添加测试验证**（tests/test_output_formatting.py）：
    ```python
    def test_format_config_default_mode():
        """验证FORMAT_CONFIG默认为name模式"""
        from uasset_read import FORMAT_CONFIG
        assert FORMAT_CONFIG["pin_reference_mode"] == "name"
    
    def test_derive_node_name_with_conflict():
        """验证同名节点使用idx后缀区分"""
        from uasset_read import _derive_node_name, UEdGraphNode
        
        node1 = UEdGraphNode(class_name="K2Node_CallFunction", ...)
        node2 = UEdGraphNode(class_name="K2Node_CallFunction", ...)
        
        name1 = _derive_node_name(node1, 10)
        name2 = _derive_node_name(node2, 20)
        
        assert name1 == "K2Node_CallFunction_10"
        assert name2 == "K2Node_CallFunction_20"
        assert name1 != name2  # 无冲突
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_config_default_mode -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_derive_node_name_with_conflict -x</automated>
  </verify>
  <done>
    FORMAT_CONFIG定义在uasset_read.py第4988-4992行
    _derive_node_name()定义在第4994-5008行
    测试验证配置默认值和节点名派生逻辑正确
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建format_pin_ref()格式转换函数</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: format_pin_ref()name模式返回 {"node": "...", "pin": "..."}
    - Test 2: format_pin_ref()guid模式返回 {"node_guid": "...", "pin_name": "..."}
    - Test 3: format_pin_ref()处理node_name_lookup查找失败
  </behavior>
  <action>
    per D-19-02和D-19-05，在_build_connections_map前添加（第5010-5030行）：

    ```python
    def format_pin_ref(
        node_guid: str,
        pin_name: str,
        node_name_lookup: Dict[str, str],
        mode: str = "name"
    ) -> Dict:
        """
        格式化Pin引用（D-19-02, D-19-05）。
        
        Args:
            node_guid: 节点GUID
            pin_name: Pin名称
            node_name_lookup: node_guid → node_name查找表
            mode: "name"或"guid"模式
        
        Returns:
            Dict: 格式化后的Pin引用对象
            - name模式: {"node": "K2Node_CallFunction_10", "pin": "execute"}
            - guid模式: {"node_guid": "...", "pin_name": "execute"}
        
        Raises:
            KeyError: 查找失败时（D-19-05处理）
        """
        if mode == "name":
            # D-19-02: 使用pin_name格式
            if node_guid in node_name_lookup:
                return {
                    "node": node_name_lookup[node_guid],
                    "pin": pin_name
                }
            else:
                # D-19-05: 查找失败fallback
                return {
                    "node_guid": node_guid,  # fallback原始guid
                    "pin": pin_name,
                    "warning": "node_name lookup failed"
                }
        else:
            # guid模式：保留原始格式
            return {
                "node_guid": node_guid,
                "pin_name": pin_name
            }
    ```

    添加测试验证（tests/test_output_formatting.py）：
    ```python
    def test_format_pin_ref_name_mode():
        """验证name模式输出格式"""
        from uasset_read import format_pin_ref
        
        node_name_lookup = {"guid-123": "K2Node_CallFunction_10"}
        result = format_pin_ref("guid-123", "execute", node_name_lookup, "name")
        
        assert result == {"node": "K2Node_CallFunction_10", "pin": "execute"}
    
    def test_format_pin_ref_guid_mode():
        """验证guid模式输出格式"""
        from uasset_read import format_pin_ref
        
        result = format_pin_ref("guid-123", "execute", {}, "guid")
        assert result == {"node_guid": "guid-123", "pin_name": "execute"}
    
    def test_format_pin_ref_lookup_failure():
        """验证查找失败时保留guid fallback（D-19-05）"""
        from uasset_read import format_pin_ref
        
        result = format_pin_ref("guid-unknown", "execute", {}, "name")
        assert result["node_guid"] == "guid-unknown"
        assert result["warning"] == "node_name lookup failed"
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_pin_ref_name_mode -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_pin_ref_guid_mode -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_format_pin_ref_lookup_failure -x</automated>
  </verify>
  <done>
    format_pin_ref()定义在uasset_read.py第5010-5030行
    函数支持name和guid模式切换
    查找失败时保留原始guid作为fallback并添加warning字段
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 修改build_connections_map()支持name模式</name>
  <files>uasset_read.py, tests/test_output_formatting.py</files>
  <behavior>
    - Test 1: build_connections_map()name模式输出符合REQUIREMENTS格式
    - Test 2: build_connections_map()guid模式保持现有格式
    - Test 3: build_connections_map()处理linked_to_raw dict格式（Phase 18输出）
  </behavior>
  <action>
    per D-19-01到D-19-05，修改build_connections_map()（第4990-5039行 → 第5032-5082行）：

    **关键修改点**：

    1. **添加node_name_lookup构建**（第5040-5046行）：
    ```python
    # Step 1a: Build node_name_lookup for name mode (D-19-02)
    node_name_lookup: Dict[str, str] = {}
    for idx, node in enumerate(graph.nodes):
        node_name_lookup[node.node_guid] = _derive_node_name(node, idx)
    ```

    2. **修改pin_lookup构建**（保持现有逻辑）：
    ```python
    # Step 1b: Build pin_lookup: pin_id → (node_guid, pin_name)
    pin_lookup: Dict[str, Tuple[str, str]] = {}
    for node in graph.nodes:
        for pin in node.pins:
            pin_lookup[pin.pin_id] = (node.node_guid, pin.pin_name)
    ```

    3. **修改连接构建逻辑**（第5056-5076行）：
    ```python
    # Step 2-4: Build connections (支持name模式)
    mode = FORMAT_CONFIG["pin_reference_mode"]  # D-19-04
    
    for node in graph.nodes:
        for pin in node.pins:
            if pin.direction == 1:  # EGPD_Output
                for linked_pin_id in pin.linked_to_raw:
                    # Phase 18: linked_to_raw为dict格式 {"pin_guid": str}
                    # 需提取pin_guid字段
                    target_pin_guid = linked_pin_id.get("pin_guid") if isinstance(linked_pin_id, dict) else linked_pin_id
                    
                    if target_pin_guid in pin_lookup:
                        target_node_guid, target_pin_name = pin_lookup[target_pin_guid]
                        # 使用format_pin_ref转换格式（D-19-02）
                        connections.append({
                            "from": format_pin_ref(node.node_guid, pin.pin_name, node_name_lookup, mode),
                            "to": format_pin_ref(target_node_guid, target_pin_name, node_name_lookup, mode)
                        })
                    else:
                        # D-19-05: Warning + raw data
                        warnings.append(f"PinId {target_pin_guid} not found in graph")
                        connections.append({
                            "from": format_pin_ref(node.node_guid, pin.pin_name, node_name_lookup, mode),
                            "to": {"raw_pin_id": target_pin_guid},
                            "warning": "target pin not found"
                        })
    ```

    添加测试验证（tests/test_output_formatting.py）：
    ```python
    def test_build_connections_map_name_mode():
        """验证LINK-01 name模式输出符合REQUIREMENTS格式"""
        from uasset_read import build_connections_map, UEdGraph, UEdGraphNode, UEdGraphPin, FORMAT_CONFIG
        
        # 构造测试graph
        node1 = UEdGraphNode(
            node_guid="guid-1",
            class_name="K2Node_EnhancedInputAction",
            pins=[
                UEdGraphPin(pin_id="pin-1", pin_name="Started", direction=1, 
                           pin_type=PinType(pin_category="exec"), linked_to_raw=[{"pin_guid": "pin-2"}])
            ]
        )
        node2 = UEdGraphNode(
            node_guid="guid-2",
            class_name="K2Node_CallFunction",
            pins=[
                UEdGraphPin(pin_id="pin-2", pin_name="execute", direction=0,
                           pin_type=PinType(pin_category="exec"))
            ]
        )
        graph = UEdGraph(graph_name="EventGraph", nodes=[node1, node2])
        
        connections, warnings = build_connections_map(graph)
        
        # 验证name模式格式（D-19-04）
        assert connections[0]["from"]["node"] == "K2Node_EnhancedInputAction_0"
        assert connections[0]["from"]["pin"] == "Started"
        assert connections[0]["to"]["node"] == "K2Node_CallFunction_1"
        assert connections[0]["to"]["pin"] == "execute"
    
    def test_build_connections_map_guid_mode():
        """验证guid模式保持现有格式"""
        from uasset_read import build_connections_map, FORMAT_CONFIG
        
        # 临时切换为guid模式
        FORMAT_CONFIG["pin_reference_mode"] = "guid"
        
        # ... 构造测试graph（同上）...
        connections, warnings = build_connections_map(graph)
        
        # 验证guid模式格式
        assert connections[0]["from"]["node_guid"] == "guid-1"
        assert connections[0]["from"]["pin_name"] == "Started"
        
        # 恢复name模式
        FORMAT_CONFIG["pin_reference_mode"] = "name"
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_connections_map_name_mode -x</automated>
    <automated>python -m pytest tests/test_output_formatting.py::test_build_connections_map_guid_mode -x</automated>
  </verify>
  <done>
    build_connections_map()修改在第5032-5082行
    输出格式符合REQUIREMENTS LINK-01示例
    支持name和guid模式切换（通过FORMAT_CONFIG）
    正确处理Phase 18的linked_to_raw dict格式
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部数据 → 输出格式 | 查找表数据转换为用户友好JSON格式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | format_pin_ref查找失败 | accept | 低价值数据，保留fallback不影响功能 |
| T-19-02 | Tampering | FORMAT_CONFIG全局配置 | mitigate | 使用常量frozenset，运行时不可修改 |

Phase 19为纯数据结构转换逻辑，无外部输入或安全敏感操作。
</threat_model>

<verification>
## 整体计划验证

1. **代码验证**：
   - `grep -c "FORMAT_CONFIG" uasset_read.py` 应返回 ≥ 1
   - `grep -c "def format_pin_ref" uasset_read.py` 应返回 1
   - `grep -c "def _derive_node_name" uasset_read.py` 应返回 1
   - `grep -c "node_name_lookup" uasset_read.py` 应返回 ≥ 1

2. **功能验证**：
   - 运行测试资产解析：`python -c "from uasset_read import parse_uasset; r = parse_uasset('BP_FirstPersonCharacter.uasset'); print(r.graphs[0].connections)"`
   - 验证输出格式包含name模式节点引用

3. **集成验证**：
   - format_graphs_json()调用build_connections_map()生成connections数组
   - connections数组包含from/to对象，使用name格式
</verification>

<success_criteria>
## 成功标准

1. ✓ FORMAT_CONFIG定义在uasset_read.py，默认pin_reference_mode="name"
2. ✓ _derive_node_name()函数正确派生节点名，处理同名节点冲突
3. ✓ format_pin_ref()函数支持name和guid模式转换，处理查找失败fallback
4. ✓ build_connections_map()修改为使用format_pin_ref()，输出符合REQUIREMENTS格式
5. ✓ 测试验证name模式和guid模式输出正确
6. ✓ linked_to_raw dict格式正确处理（提取pin_guid字段）
</success_criteria>

<output>
完成后创建：`.planning/phases/19-连接关系重建/19-01-SUMMARY.md`
</output>