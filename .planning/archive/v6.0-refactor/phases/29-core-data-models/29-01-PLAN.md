---
phase: "29-core-data-models"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/models/__init__.py
  - src/uasset_read/models/core.py
  - src/uasset_read/__init__.py
autonomous: true
requirements: [MOD-08]
must_haves:
  truths:
    - "FEdGraphPinType 可用正确字段实例化"
    - "UEdGraphPin 可用正确字段实例化"
    - "UEdGraphNode 作为基类可被继承"
    - "UEdGraph 可包含节点列表"
    - "FMemberReference 可正确实例化"
    - "from uasset_read.models import UEdGraph 可正常工作"
  artifacts:
    - path: "src/uasset_read/models/core.py"
      provides: "FEdGraphPinType, UEdGraphPin, UEdGraphNode, UEdGraph, FMemberReference 5个dataclass"
      min_lines: 80
    - path: "src/uasset_read/models/__init__.py"
      provides: "core.py 5个类的扁平导出"
  key_links:
    - from: "src/uasset_read/models/core.py"
      to: "uasset_read.py:1878-1971"
      via: "字段定义对照"
      pattern: "@dataclass class UEd"
---

<objective>
创建 `models/core.py` 定义 5 个核心 UE 蓝图数据类，并建立模块导出。

Purpose: 提供蓝图图解析的基础数据结构 — 引脚类型、引脚、节点基类、图容器、成员引用。所有后续节点类型和图解析依赖这些类。
Output: models/core.py, models/__init__.py（初始版本）, src/uasset_read/__init__.py 更新。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-core-data-models/29-CONTEXT.md
@.planning/phases/29-core-data-models/29-RESEARCH.md
</execution_context>

<context>
@uasset_read.py
@src/uasset_read/__init__.py
@src/uasset_read/serializers/package_summary.py
@src/uasset_read/archive.py
</context>

<interfaces>
<!-- FEdGraphPinType from uasset_read.py (PinType structure, referenced by UEdGraphPin.pin_type) -->
```python
@dataclass
class FEdGraphPinType:
    pin_category: str = ""
    pin_subcategory: str = ""
    pin_subcategory_object: Optional[str] = None
    container_type: int = 0
    is_map_key: bool = False
    is_map_value: bool = False
```

<!-- UEdGraphNode 基类 from uasset_read.py:1922-1938 -->
```python
@dataclass
class UEdGraphNode:
    node_guid: str
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List["UEdGraphPin"] = field(default_factory=list)
    class_name: str = ""
    node_data: Optional[any] = None
```
</interfaces>

<tasks>

<task id="29-01">
  <objective>创建 models/core.py 定义 5 个核心数据类 + 创建 models/__init__.py + 更新 src/uasset_read/__init__.py</objective>
  <read_first>
    - uasset_read.py §1878-1920 — UEdGraphPin 完整定义（FEdGraphPinType 也在附近定义）
    - uasset_read.py §1922-1938 — UEdGraphNode 基类
    - uasset_read.py §1941-1956 — UEdGraph 容器
    - uasset_read.py §1959-1971 — FMemberReference
    - src/uasset_read/serializers/package_summary.py — dataclass + from_archive 模式参考
    - src/uasset_read/archive.py — FArchive 接口
  </read_first>
  <acceptance_criteria>
    - models/core.py 包含 5 个 @dataclass：FEdGraphPinType, UEdGraphPin, UEdGraphNode, UEdGraph, FMemberReference
    - 字段名与 uasset_read.py 完全一致
    - 所有类有 from_archive classmethod（Phase 31 实现）
    - models/__init__.py 导出 core.py 5 个类
    - src/uasset_read/__init__.py 追加导入和 __all__
  </acceptance_criteria>
  <action>
**步骤 A — 创建 `src/uasset_read/models/__init__.py`（初始版本，后续 Plan 扩展）：**
```python
"""
核心数据模型 — UE 蓝图图容器、节点、引脚。

通过扁平导出（D-03），调用者使用 from uasset_read.models import UEdGraph 等。
"""

from .core import (
    FEdGraphPinType,
    UEdGraphPin,
    UEdGraphNode,
    UEdGraph,
    FMemberReference,
)

__all__ = [
    "FEdGraphPinType",
    "UEdGraphPin",
    "UEdGraphNode",
    "UEdGraph",
    "FMemberReference",
]
```

**步骤 B — 创建 `src/uasset_read/models/core.py`，包含 5 个 dataclass：**

文件头部：
```python
"""
核心 UE 蓝图数据模型 — 引脚、节点、图容器、成员引用。

等价覆盖 uasset_read.py 中第 1878-1971 行的数据类定义。
Per D-01: 保持 UE 源码命名。
Per D-06: 数据和序列化解耦，from_archive 为 stub。
Per D-10: Python 3.10+ 严格类型提示。
Per D-12: 静态 from_archive 方法（Phase 31 实现）。
"""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, List, Any, Dict, Self, TYPE_CHECKING

if TYPE_CHECKING:
    from uasset_read.archive import FArchive
```

**1. FEdGraphPinType（Pin 类型结构）：**
参考 uasset_read.py 中 PinType 相关定义。包含 pin_category, pin_subcategory, pin_subcategory_object, container_type, is_map_key, is_map_value 字段。

```python
@dataclass
class FEdGraphPinType:
    """蓝图引脚类型结构。"""
    pin_category: str = ""
    pin_subcategory: str = ""
    pin_subcategory_object: Optional[str] = None
    container_type: int = 0
    is_map_key: bool = False
    is_map_value: bool = False

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**2. UEdGraphPin（20+ 字段，完整）：**
按 uasset_read.py:1878-1920 定义，包含 PIN-01~05 组 + EditorOnly + Legacy 字段。注意：pin_type 字段类型为 FEdGraphPinType。linked_to_raw, sub_pins 类型为 List[dict]，parent_pin 类型为 Optional[dict]。

```python
@dataclass
class UEdGraphPin:
    """UEdGraphPin 蓝图引脚完整结构。"""
    # PIN-01: 基础信息
    pin_id: str
    pin_name: str
    pin_tooltip: str = ""
    direction: int = 0
    # PIN-02: PinType
    pin_type: Optional[FEdGraphPinType] = None
    # PIN-03: 默认值
    default_value: Optional[str] = None
    auto_wildcard_value: Optional[str] = None
    default_object: Optional[int] = None
    default_text_value: Optional[str] = None
    # PIN-04: 连接引用
    linked_to_raw: List[dict] = field(default_factory=list)
    sub_pins: List[dict] = field(default_factory=list)
    parent_pin: Optional[dict] = None
    # PIN-05: 显示属性
    hidden: bool = False
    not_connectable: bool = False
    advanced_view: bool = False
    orphaned_pin: bool = False
    # EditorOnly
    owning_node_index: int = 0
    source_index: Optional[int] = None
    persistent_guid: Optional[str] = None
    # Legacy
    flags: int = 0

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**3. UEdGraphNode 基类（7 字段）：**
按 uasset_read.py:1922-1938 定义。Per D-04/D-05: class_name 用于类型识别，node_data 为 Optional[any] 多态字段。

```python
@dataclass
class UEdGraphNode:
    """UEdGraphNode 蓝图节点基类。"""
    node_guid: str
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List["UEdGraphPin"] = field(default_factory=list)
    class_name: str = ""
    node_data: Optional[Any] = None

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**4. UEdGraph（6 字段）：**
按 uasset_read.py:1941-1956 定义。

```python
@dataclass
class UEdGraph:
    """UEdGraph 蓝图图容器。"""
    graph_name: str
    graph_class: str
    schema: Optional[str] = None
    nodes: List["UEdGraphNode"] = field(default_factory=list)
    graph_guid: Optional[str] = None
    b_editable: bool = True

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**5. FMemberReference（4 字段）：**
按 uasset_read.py:1959-1971 定义。

```python
@dataclass
class FMemberReference:
    """FMemberReference 成员引用结构。"""
    member_parent: Optional[str] = None
    member_name: str = ""
    member_guid: Optional[str] = None
    b_self_context: bool = False

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**步骤 C — 更新 `src/uasset_read/__init__.py`：**
在现有 imports 部分（ParseError 之后）追加：
```python
from .models import (
    FEdGraphPinType,
    UEdGraphPin,
    UEdGraphNode,
    UEdGraph,
    FMemberReference,
)
```
在 __all__ 列表（"ParseError" 之后）追加：
```python
    # 核心数据模型（Phase 29）
    "FEdGraphPinType",
    "UEdGraphPin",
    "UEdGraphNode",
    "UEdGraph",
    "FMemberReference",
```
  </action>
  <verify>
    <automated>python -c "from uasset_read import FEdGraphPinType, UEdGraphPin, UEdGraphNode, UEdGraph, FMemberReference; print('5 core models importable from uasset_read')"</automated>
  </verify>
  <done>
    - models/core.py 存在且包含 5 个 @dataclass（FEdGraphPinType, UEdGraphPin, UEdGraphNode, UEdGraph, FMemberReference）
    - 所有字段名与 uasset_read.py:1878-1971 完全一致
    - 每个类有 from_archive classmethod stub
    - UEdGraphPin.pin_type 类型为 Optional[FEdGraphPinType]
    - UEdGraphNode 有 class_name: str 和 node_data: Optional[Any]（per D-04/D-05/D-11）
    - models/__init__.py 导出 5 个核心类
    - src/uasset_read/__init__.py 向后兼容重导出 5 个核心类
    - from uasset_read import UEdGraph 能正常工作
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UEdGraphPin linked_to_raw | 来自不可信 .uasset 文件的引用数据，此阶段仅定义数据结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | core.py dataclasses | accept | 纯数据类定义，无 I/O；输入验证由解析层负责 |
| T-29-02 | Information Disclosure | FArchive integration | mitigate | from_archive 使用 FArchive 边界验证读取，不越界 |
</threat_model>

<verification>
1. `python -c "from uasset_read.models.core import FEdGraphPinType, UEdGraphPin, UEdGraphNode, UEdGraph, FMemberReference; print('core models OK')"`
2. `python -c "from uasset_read.models import UEdGraph, UEdGraphNode, UEdGraphPin; print('flat exports OK')"`
3. `python -c "from uasset_read import UEdGraph, UEdGraphNode, UEdGraphPin, FMemberReference, FEdGraphPinType; print('backward compat OK')"`
4. `python -c "from dataclasses import asdict; from uasset_read import UEdGraphPin; p = UEdGraphPin(pin_id='test', pin_name='test'); d = asdict(p); assert 'pin_id' in d; print('asdict works')"`
</verification>

<success_criteria>
- [ ] models/core.py 包含 5 个 @dataclass
- [ ] 所有字段名与 uasset_read.py:1878-1971 完全一致
- [ ] 每个类有 from_archive classmethod stub（raise NotImplementedError）
- [ ] models/__init__.py 导出 5 个核心类
- [ ] src/uasset_read/__init__.py 向后兼容重导出 5 个核心类
- [ ] from uasset_read import UEdGraph 等可正常工作
- [ ] asdict() 对所有 dataclass 正常工作
</success_criteria>

<output>
After completion, create `.planning/phases/29-core-data-models/29-01-SUMMARY.md`
</output>
