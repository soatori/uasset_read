---
phase: "29-core-data-models"
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - src/uasset_read/models/result.py
  - src/uasset_read/models/__init__.py
  - src/uasset_read/__init__.py
  - src/uasset_read/models/blueprint.py
autonomous: true
requirements: [MOD-08]
must_haves:
  truths:
    - "ParseResult 可以用正确的字段名和类型实例化"
    - "StatusInfo 可以用正确的字段名和类型实例化"
    - "ParseResult.blueprint 类型为 Optional[BlueprintMetadata]"
    - "BlueprintMetadata/BlueprintVariable/BlueprintFunction/BlueprintEvent 可从 uasset_read 导入"
    - "asdict(ParseResult(...)) 正常工作"
    - "旧版所有测试中 from uasset_read import ParseResult 仍然有效"
  artifacts:
    - path: "src/uasset_read/models/result.py"
      provides: "ParseResult(16字段), StatusInfo(3字段) dataclass 定义"
      min_lines: 50
    - path: "src/uasset_read/models/blueprint.py"
      provides: "BlueprintMetadata, BlueprintVariable, BlueprintFunction, BlueprintEvent, FunctionParameter, MulticastDelegate"
      min_lines: 120
  key_links:
    - from: "src/uasset_read/models/result.py"
      to: "src/uasset_read/models/blueprint.py"
      via: "import BlueprintMetadata"
      pattern: "from .blueprint import BlueprintMetadata"
    - from: "src/uasset_read/models/__init__.py"
      to: "src/uasset_read/models/result.py"
      via: "import 重导出"
      pattern: "from .result import"
---

<objective>
创建 `models/result.py` 定义 ParseResult 和 StatusInfo，创建 `models/blueprint.py` 定义 BlueprintMetadata 等辅助类，并更新所有模块导出和向后兼容。

Purpose: 完成 Phase 29 所有数据模型定义，确保 ParseResult 类型链完整，10 个测试文件的导入路径不受影响。
Output: models/result.py, models/blueprint.py, 更新的 models/__init__.py 和 src/__init__.py。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-core-data-models/29-CONTEXT.md
@.planning/phases/29-core-data-models/29-RESEARCH.md
</execution_context>

<context>
@uasset_read.py
@src/uasset_read/__init__.py
@src/uasset_read/serializers/package_summary.py
@src/uasset_read/serializers/object_resources.py
@tests/test_partial_results.py
@tests/test_phase14_output_formats.py
@tests/test_output_formatting.py
</context>

<interfaces>
<!-- ParseResult from uasset_read.py:2051-2074 -->
```python
@dataclass
class ParseResult:
    summary: Optional[PackageFileSummary] = None
    name_map: List[str] = field(default_factory=list)
    import_map: List[ObjectImport] = field(default_factory=list)
    export_map: List[ObjectExport] = field(default_factory=list)
    errors: List[str] = field(default_factory=list)
    blueprint: Optional["BlueprintMetadata"] = None
    graphs: List["UEdGraph"] = field(default_factory=list)
    is_success: bool = False
    mmap_used: bool = False
    mmap_warning: Optional[str] = None
    warnings: List[str] = field(default_factory=list)
    imports: List[Dict] = field(default_factory=list)
    soft_references: List[Dict] = field(default_factory=list)
    circular_deps: List[List[str]] = field(default_factory=list)
```

<!-- StatusInfo from uasset_read.py:2078-2091 -->
```python
@dataclass
class StatusInfo:
    status: str
    message: Optional[str] = None
    code: Optional[str] = None
```

<!-- BlueprintMetadata from uasset_read.py:1716-1729 -->
```python
@dataclass
class BlueprintMetadata:
    is_blueprint: bool
    parent_class: Optional[str] = None
    variables: List["BlueprintVariable"] = field(default_factory=list)
    detection_warning: Optional[str] = None
    functions: List["BlueprintFunction"] = field(default_factory=list)
    events: List["BlueprintEvent"] = field(default_factory=list)
```

<!-- BlueprintVariable from uasset_read.py:1655-1712 (全部 36 字段) -->
```python
@dataclass
class BlueprintVariable:
    var_name: str
    var_type: "FEdGraphPinType"
    category: str
    property_flags: int
    default_value: Any = None
    friendly_name: str = ""
    is_component: bool = False
    metadata: Dict[str, str] = field(default_factory=dict)
    flags_labels: List[str] = field(default_factory=list)
    edit_condition: str = ""
    meta_class: str = ""
    is_edit_anywhere: bool = False
    is_edit_instance_only: bool = False
    is_visible_anywhere: bool = False
    is_blueprint_read_only: bool = False
    is_blueprint_readable: bool = False
    is_blueprint_writable: bool = False
    is_transient: bool = False
    is_duplicate_transient: bool = False
    is_text_export_transient: bool = False
    is_non_transient: bool = False
    is_export_object: bool = False
    is_save_game: bool = False
    is_no_clear: bool = False
    is_reference_only: bool = False
    is_blueprint_assignable: bool = False
    is_blueprint_callable: bool = False
    is_net: bool = False
    is_replicated: bool = False
    is_rep_notify: bool = False
    is_interp: bool = False
    is_non_pi_ed_duplicate_transient: bool = False
    is_expose_on_spawn: bool = False
    edit_category: str = ""
    edit_widget: str = ""
    meta_data: Dict[str, Any] = field(default_factory=dict)
```

<!-- BlueprintFunction from uasset_read.py:1819-1870 (全部 28 字段) -->
```python
@dataclass
class BlueprintFunction:
    name: str = ""
    return_type: str = ""
    parameters: List[FunctionParameter] = field(default_factory=list)
    function_flags: int = 0
    is_pure: bool = False
    is_blueprint_callable: bool = False
    is_blueprint_event: bool = False
    is_blueprint_implementable_event: bool = False
    is_native: bool = False
    is_const: bool = False
    is_static: bool = False
    is_virtual: bool = False
    is_exec: bool = False
    is_net: bool = False
    is_net_reliable: bool = False
    is_net_server: bool = False
    is_net_client: bool = False
    is_net_multicast: bool = False
    is_blueprint_private: bool = False
    is_blueprint_protected: bool = False
    is_blueprint_public: bool = False
    is_blueprint_pure: bool = False
    is_blueprint_cosmetic: bool = False
    is_editor_only: bool = False
    is_final: bool = False
    is_delegate: bool = False
    is_multicast_delegate: bool = False
    is_has_out_parms: bool = False
    is_has_defaults: bool = False
    access_specifier: str = "Public"
    meta_data: Dict[str, Any] = field(default_factory=dict)
```

<!-- BlueprintEvent from uasset_read.py:1772-1815 (全部 25 字段) -->
```python
@dataclass
class BlueprintEvent:
    name: str = ""
    event_type: str = ""
    function_flags: int = 0
    is_blueprint_event: bool = False
    is_blueprint_implementable_event: bool = False
    is_net: bool = False
    is_net_multicast: bool = False
    is_net_reliable: bool = False
    is_net_client: bool = False
    is_net_server: bool = False
    is_replicated: bool = False
    is_cosmetic: bool = False
    is_static: bool = False
    is_multicast: bool = False
    multicast_delegate: Optional[MulticastDelegate] = None
    is_override: bool = False
    override_parent_class: str = ""
    override_parent_event: str = ""
    is_interface_event: bool = False
    interface_class: str = ""
    parameters: List[FunctionParameter] = field(default_factory=list)
    meta_data: Dict[str, Any] = field(default_factory=dict)
```

<!-- FunctionParameter from uasset_read.py:1737-1757 -->
```python
@dataclass
class FunctionParameter:
    name: str = ""
    param_type: str = ""
    default_value: Any = None
    is_input: bool = True
    is_output: bool = False
    is_optional: bool = False
    property_flags: int = 0
    meta_data: Dict[str, Any] = field(default_factory=dict)
```

<!-- MulticastDelegate from uasset_read.py:1761-1768 -->
```python
@dataclass
class MulticastDelegate:
    delegate_name: str = ""
    signature_function: str = ""
    is_callable_in_blueprint: bool = False
```
</interfaces>

<tasks>

<task id="29-03-T1">
  <objective>创建 models/blueprint.py 定义 6 个蓝图元数据辅助类</objective>
  <read_first>
    - uasset_read.py §1655-1712（BlueprintVariable, 36 字段）
    - uasset_read.py §1716-1729（BlueprintMetadata）
    - uasset_read.py §1737-1757（FunctionParameter）
    - uasset_read.py §1761-1768（MulticastDelegate）
    - uasset_read.py §1772-1815（BlueprintEvent）
    - uasset_read.py §1819-1870（BlueprintFunction）
  </read_first>
  <acceptance_criteria>
    - models/blueprint.py 包含 6 个 @dataclass
    - BlueprintVariable 有 36 个字段（与 uasset_read.py:1655-1712 完全一致）
    - 每个类有 from_archive classmethod stub
  </acceptance_criteria>
  <action>
创建 `src/uasset_read/models/blueprint.py`，包含以下 6 个 dataclass（按定义顺序）：

文件头部：
```python
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, List, Dict, Any, Self, TYPE_CHECKING

if TYPE_CHECKING:
    from uasset_read.archive import FArchive
    from uasset_read.models.core import FEdGraphPinType
```

1. FunctionParameter（8 字段）— name, param_type, default_value, is_input, is_output, is_optional, property_flags, meta_data
2. MulticastDelegate（3 字段）— delegate_name, signature_function, is_callable_in_blueprint
3. BlueprintEvent（25 字段）— name, event_type, function_flags + 布尔标志 + multicast_delegate + override 相关 + interface 相关 + parameters + meta_data
4. BlueprintFunction（28 字段）— name, return_type, parameters + 布尔标志 + access_specifier + meta_data
5. BlueprintVariable（36 字段 — per uasset_read.py:1655-1712）— var_name, var_type, category, property_flags, default_value, friendly_name, is_component, metadata, flags_labels, edit_condition, meta_class, 25 个标志/修饰符字段, edit_category, edit_widget, meta_data。注意：旧版使用 __post_init__ 初始化 meta_data，新版使用 field(default_factory=dict)。
6. BlueprintMetadata（6 字段）— is_blueprint, parent_class, variables, detection_warning, functions, events

每个类都添加：
```python
    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```
  </action>
  <verify>
    <automated>python -c "from uasset_read.models.blueprint import BlueprintMetadata, BlueprintVariable, BlueprintFunction, BlueprintEvent, FunctionParameter, MulticastDelegate; bv_fields = len(BlueprintVariable.__dataclass_fields__); assert bv_fields == 36, f'expected 36, got {bv_fields}'; print(f'blueprint models OK, BlueprintVariable has {bv_fields} fields')"</automated>
  </verify>
  <done>
    - models/blueprint.py 存在且包含 6 个 @dataclass
    - BlueprintMetadata 有 is_blueprint, parent_class, variables, functions, events 字段
    - BlueprintVariable 有全部 36 个字段（与 uasset_read.py:1655-1712 完全一致）
    - BlueprintFunction 有全部 28 个字段（与 uasset_read.py:1819-1870 完全一致）
    - BlueprintEvent 有全部 25 个字段（与 uasset_read.py:1772-1815 完全一致）
    - FunctionParameter 有 8 个字段
    - MulticastDelegate 有 3 个字段
    - 每个类有 from_archive classmethod 签名
    - BlueprintVariable.var_type 类型为 "FEdGraphPinType"（前向引用）
  </done>
</task>

<task id="29-03-T2">
  <objective>创建 models/result.py 定义 ParseResult 和 StatusInfo</objective>
  <read_first>
    - uasset_read.py §2051-2091（ParseResult, StatusInfo 原始定义）
    - src/uasset_read/serializers/package_summary.py（PackageFileSummary 导入参考）
    - src/uasset_read/serializers/object_resources.py（ObjectImport, ObjectExport 导入参考）
  </read_first>
  <acceptance_criteria>
    - models/result.py 包含 ParseResult（16 字段）和 StatusInfo（3 字段）
    - ParseResult 字段名与旧版完全一致
  </acceptance_criteria>
  <action>
创建 `src/uasset_read/models/result.py`。

文件头部：
```python
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, List, Dict, Self, TYPE_CHECKING

if TYPE_CHECKING:
    from uasset_read.archive import FArchive
    from uasset_read.serializers.package_summary import PackageFileSummary
    from uasset_read.serializers.object_resources import ObjectImport, ObjectExport
    from uasset_read.models.core import UEdGraph
    from uasset_read.models.blueprint import BlueprintMetadata
```

**1. ParseResult（16 字段，与旧版完全一致）：**
summary, name_map, import_map, export_map, errors, blueprint, graphs, is_success, mmap_used, mmap_warning, warnings, imports, soft_references, circular_deps

ParseResult 不使用 from_archive（它是聚合结果，不是从二进制直接读取的）。

**2. StatusInfo（3 字段）：**
status, message, code

StatusInfo 不使用 from_archive（它由 build_status_info 函数构建）。
  </action>
  <verify>
    <automated>python -c "from uasset_read.models.result import ParseResult, StatusInfo; pr_fields = len(ParseResult.__dataclass_fields__); assert pr_fields == 16, f'expected 16, got {pr_fields}'; print(f'result models OK, ParseResult has {pr_fields} fields')"</automated>
  </verify>
  <done>
    - models/result.py 存在且包含 ParseResult（16字段）和 StatusInfo（3字段）
    - ParseResult 所有字段名与旧版完全一致
    - ParseResult 使用 TYPE_CHECKING 导入 serializers 类型（避免循环导入）
    - StatusInfo 有 status, message, code 三个字段
    - 不使用 from_archive（ParseResult 是聚合结果）
  </done>
</task>

<task id="29-03-T3">
  <objective>更新所有模块导出（models/__init__.py 和 src/__init__.py）</objective>
  <read_first>
    - src/uasset_read/models/__init__.py（Plan 01/02 创建的文件）
    - src/uasset_read/__init__.py（Plan 01/02 更新的文件）
    - src/uasset_read/models/result.py（刚创建）
    - src/uasset_read/models/blueprint.py（刚创建）
    - tests/test_partial_results.py（验证 `from uasset_read import ParseResult`）
    - tests/test_output_formatting.py（验证 `from uasset_read import BlueprintMetadata`）
    - tests/test_phase14_output_formats.py（验证 `from uasset_read import StatusInfo`）
  </read_first>
  <acceptance_criteria>
    - models/__init__.py 完整导出 16 个类
    - src/uasset_read/__init__.py 向后兼容重导出全部 16 个类
    - `from uasset_read import ParseResult` 能正常工作
  </acceptance_criteria>
  <action>
**步骤 A — 最终更新 `src/uasset_read/models/__init__.py`：**

完整的最终文件内容（包含所有 16 个类的导出）：

```python
"""
核心数据模型 — UE 蓝图图容器、节点、引脚、蓝图元数据及 ParseResult。

通过扁平导出（D-03），调用者使用 from uasset_read.models import UEdGraph 等。
"""

from .core import (
    FEdGraphPinType,
    UEdGraphPin,
    UEdGraphNode,
    UEdGraph,
    FMemberReference,
)
from .node_types import (
    K2NodeCallFunction,
    K2NodeEvent,
    K2NodeKnot,
    EdGraphNodeComment,
    K2NodeEnhancedInputAction,
)
from .result import (
    ParseResult,
    StatusInfo,
)
from .blueprint import (
    BlueprintMetadata,
    BlueprintVariable,
    BlueprintFunction,
    BlueprintEvent,
    FunctionParameter,
    MulticastDelegate,
)

__all__ = [
    # 核心模型（core.py）
    "FEdGraphPinType",
    "UEdGraphPin",
    "UEdGraphNode",
    "UEdGraph",
    "FMemberReference",
    # 节点类型（node_types.py）
    "K2NodeCallFunction",
    "K2NodeEvent",
    "K2NodeKnot",
    "EdGraphNodeComment",
    "K2NodeEnhancedInputAction",
    # 结果（result.py）
    "ParseResult",
    "StatusInfo",
    # 蓝图元数据（blueprint.py）
    "BlueprintMetadata",
    "BlueprintVariable",
    "BlueprintFunction",
    "BlueprintEvent",
    "FunctionParameter",
    "MulticastDelegate",
]
```

**步骤 B — 最终更新 `src/uasset_read/__init__.py`：**

在现有 `__all__` 末尾追加以下导出（在 `ParseError` 之后）：

```python
# 在 imports 部分追加：
from .models import (
    # 核心模型
    FEdGraphPinType,
    UEdGraphPin,
    UEdGraphNode,
    UEdGraph,
    FMemberReference,
    # 节点类型
    K2NodeCallFunction,
    K2NodeEvent,
    K2NodeKnot,
    EdGraphNodeComment,
    K2NodeEnhancedInputAction,
    # 结果
    ParseResult,
    StatusInfo,
    # 蓝图元数据
    BlueprintMetadata,
    BlueprintVariable,
    BlueprintFunction,
    BlueprintEvent,
    FunctionParameter,
    MulticastDelegate,
)

# 在 __all__ 列表追加（在 "ParseError" 之后）：
    # 核心数据模型（Phase 29）
    "FEdGraphPinType",
    "UEdGraphPin",
    "UEdGraphNode",
    "UEdGraph",
    "FMemberReference",
    # 节点类型（Phase 29）
    "K2NodeCallFunction",
    "K2NodeEvent",
    "K2NodeKnot",
    "EdGraphNodeComment",
    "K2NodeEnhancedInputAction",
    # 结果（Phase 29）
    "ParseResult",
    "StatusInfo",
    # 蓝图元数据（Phase 29）
    "BlueprintMetadata",
    "BlueprintVariable",
    "BlueprintFunction",
    "BlueprintEvent",
    "FunctionParameter",
    "MulticastDelegate",
```
  </action>
  <verify>
    <automated>python -c "from uasset_read import ParseResult, StatusInfo, BlueprintMetadata, BlueprintVariable, BlueprintFunction, BlueprintEvent, FunctionParameter, MulticastDelegate, UEdGraph, UEdGraphNode, UEdGraphPin, FMemberReference, FEdGraphPinType, K2NodeCallFunction, K2NodeEvent, K2NodeKnot, EdGraphNodeComment, K2NodeEnhancedInputAction; print('all 18 classes importable')"</automated>
  </verify>
  <done>
    - models/__init__.py 完整导出 16 个类
    - src/uasset_read/__init__.py 向后兼容重导出全部 16 个类
    - `from uasset_read import ParseResult` 能正常工作
    - `from uasset_read import StatusInfo` 能正常工作
    - `from uasset_read import BlueprintMetadata` 等蓝图元数据类能正常工作
    - 现有导出（常量、异常）不受影响
    - __all__ 包含所有 16 个新类名
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParseResult.errors/warnings | 用户提供的 .uasset 文件可能触发错误信息收集，但此阶段仅定义数据结构 |
| BlueprintMetadata 类型链 | 多级嵌套数据结构，asdict() 递归遍历所有层级 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering | result.py ParseResult | accept | 纯数据类定义，无 I/O；解析时的输入验证由 parse_uasset 函数负责 |
| T-29-06 | Information Disclosure | blueprint.py classes | accept | 纯数据类定义，仅存储解析后的蓝图元数据，无敏感信息 |
</threat_model>

<verification>
1. `python -c "from uasset_read.models.result import ParseResult, StatusInfo; print('result models OK')"`
2. `python -c "from uasset_read.models.blueprint import BlueprintMetadata, BlueprintVariable, BlueprintFunction, BlueprintEvent; print('blueprint models OK')"`
3. `python -c "from uasset_read.models import ParseResult, StatusInfo, BlueprintMetadata, BlueprintVariable; print('flat exports OK')"`
4. `python -c "from uasset_read import ParseResult, StatusInfo, BlueprintMetadata, BlueprintVariable, BlueprintFunction, BlueprintEvent; print('backward compat OK')"`
5. `python -c "from dataclasses import asdict; from uasset_read import ParseResult; r = ParseResult(); d = asdict(r); assert 'summary' in d and 'graphs' in d and 'is_success' in d; print('asdict(ParseResult) works')"`
6. `python -c "from uasset_read import StatusInfo; s = StatusInfo(status='success'); assert s.status == 'success'; print('StatusInfo instantiation works')"`
7. `grep -c "BlueprintMetadata" src/uasset_read/models/result.py` 结果 >= 1（确认 ParseResult 引用 BlueprintMetadata）
8. `python -c "from uasset_read import ParseResult; import inspect; fields = [f.name for f in ParseResult.__dataclass_fields__.values()]; assert 'summary' in fields and 'graphs' in fields and 'blueprint' in fields; print('all ParseResult fields present')"`
</verification>

<success_criteria>
- [ ] models/blueprint.py 包含 6 个辅助 @dataclass（BlueprintMetadata, BlueprintVariable, BlueprintFunction, BlueprintEvent, FunctionParameter, MulticastDelegate）
- [ ] BlueprintVariable 有全部 36 个字段（与 uasset_read.py:1655-1712 完全一致）
- [ ] BlueprintFunction 有全部 28 个字段（与 uasset_read.py:1819-1870 完全一致）
- [ ] BlueprintEvent 有全部 25 个字段（与 uasset_read.py:1772-1815 完全一致）
- [ ] models/result.py 包含 ParseResult（16字段）和 StatusInfo（3字段）
- [ ] ParseResult 所有字段名与旧版完全一致
- [ ] ParseResult 使用 TYPE_CHECKING 导入 serializers 类型
- [ ] models/__init__.py 完整导出 16 个类
- [ ] src/uasset_read/__init__.py 向后兼容重导出全部 16 个类
- [ ] `from uasset_read import ParseResult` 等 10 个测试文件的导入路径不受影响
- [ ] asdict() 对 ParseResult 和 StatusInfo 正常工作
- [ ] BlueprintMetadata 可在 ParseResult.blueprint 类型中使用
</success_criteria>

<output>
After completion, create `.planning/phases/29-core-data-models/29-03-SUMMARY.md`
</output>
