---
phase: "29-core-data-models"
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/uasset_read/models/node_types.py
  - src/uasset_read/models/__init__.py
  - src/uasset_read/__init__.py
autonomous: true
requirements: [MOD-08]
must_haves:
  truths:
    - "K2NodeCallFunction 继承 UEdGraphNode 可通过 super() 获取基类字段"
    - "K2NodeEvent 继承 UEdGraphNode"
    - "K2NodeKnot 继承 UEdGraphNode（无额外字段）"
    - "EdGraphNodeComment 继承 UEdGraphNode"
    - "K2NodeEnhancedInputAction 继承 UEdGraphNode"
    - "5 个节点类型均可通过 uasset_read 导入"
  artifacts:
    - path: "src/uasset_read/models/node_types.py"
      provides: "5个UEdGraphNode子类"
      min_lines: 60
  key_links:
    - from: "src/uasset_read/models/node_types.py"
      to: "src/uasset_read/models/core.py"
      via: "from .core import UEdGraphNode"
      pattern: "class.*\\(UEdGraphNode\\)"
---

<objective>
创建 `models/node_types.py` 定义 5 个 UEdGraphNode 子类，并更新模块导出。

Purpose: 为 Phase 31 图解析提供节点类型识别与多态数据结构。Per D-04: 继承结构。Per D-05: class_name 字段 + match/case 类型分派。
Output: models/node_types.py, 更新的 models/__init__.py 和 src/uasset_read/__init__.py。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-core-data-models/29-CONTEXT.md
@.planning/phases/29-core-data-models/29-RESEARCH.md
</execution_context>

<context>
@uasset_read.py
@src/uasset_read/models/core.py
@src/uasset_read/serializers/package_summary.py
</context>

<interfaces>
<!-- UEdGraphNode 基类 from Plan 29-01 (core.py) -->
```python
@dataclass
class UEdGraphNode:
    node_guid: str
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List["UEdGraphPin"] = field(default_factory=list)
    class_name: str = ""
    node_data: Optional[Any] = None
```
<!-- 子类使用 super().__init__(...) 调用基类构造函数 -->
</interfaces>

<tasks>

<task id="29-02">
  <objective>创建 models/node_types.py 定义 5 个 UEdGraphNode 子类 + 更新模块导出</objective>
  <read_first>
    - uasset_read.py §1978-2047 — 5 种节点类型数据类
    - src/uasset_read/models/core.py（Plan 29-01 创建的 UEdGraphNode 基类）
    - src/uasset_read/models/__init__.py（Plan 29-01 创建）
    - src/uasset_read/__init__.py（Plan 29-01 更新）
  </read_first>
  <acceptance_criteria>
    - node_types.py 包含 5 个 @dataclass 继承 UEdGraphNode
    - K2NodeCallFunction: function_reference + b_defaults_to_pure
    - K2NodeEvent: event_reference + b_override_function
    - K2NodeKnot: pass（仅基类字段）
    - EdGraphNodeComment: comment_color + node_width + node_height + font_size
    - K2NodeEnhancedInputAction: input_action_path
    - models/__init__.py 和 src/uasset_read/__init__.py 包含全部导出
  </acceptance_criteria>
  <action>
**创建 `src/uasset_read/models/node_types.py`：**

文件头部：
```python
"""
节点类型特定数据类 — 5 种 K2Node 子类继承 UEdGraphNode。

Per D-04: 节点继承结构，子类通过 super() 获取基类字段。
Per D-05: class_name 字段用于 match/case 类型分派。
Per D-06: 数据和序列化解耦。
"""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Optional, List, Tuple, Self, TYPE_CHECKING

if TYPE_CHECKING:
    from uasset_read.archive import FArchive

from .core import UEdGraphNode, FMemberReference
```

**1. K2NodeCallFunction（2 字段，继承 UEdGraphNode）：**
Per uasset_read.py:1978-1990，function_reference 类型为 FMemberReference。
```python
@dataclass
class K2NodeCallFunction(UEdGraphNode):
    """K2Node_CallFunction 函数调用节点。"""
    function_reference: Optional[FMemberReference] = None
    b_defaults_to_pure: bool = False

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**2. K2NodeEvent（2 字段，继承 UEdGraphNode）：**
Per uasset_read.py:1992-2004，event_reference 类型为 FMemberReference。
```python
@dataclass
class K2NodeEvent(UEdGraphNode):
    """K2Node_Event 事件节点。"""
    event_reference: Optional[FMemberReference] = None
    b_override_function: bool = False

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**3. K2NodeKnot（0 额外字段，继承 UEdGraphNode）：**
Per uasset_read.py:2006-2016，Knot 无额外字段。
```python
@dataclass
class K2NodeKnot(UEdGraphNode):
    """K2Node_Knot 重定向节点，无额外字段。"""

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**4. EdGraphNodeComment（4 字段，继承 UEdGraphNode）：**
Per uasset_read.py:2018-2035。
```python
@dataclass
class EdGraphNodeComment(UEdGraphNode):
    """EdGraphNode_Comment 注释节点。"""
    comment_color: Tuple[float, float, float, float] = (0.05, 0.05, 0.05, 1.0)
    node_width: int = 0
    node_height: int = 0
    font_size: int = 14

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**5. K2NodeEnhancedInputAction（1 字段，继承 UEdGraphNode）：**
Per uasset_read.py:2037-2048。
```python
@dataclass
class K2NodeEnhancedInputAction(UEdGraphNode):
    """K2Node_EnhancedInputAction 输入动作节点。"""
    input_action_path: str = ""

    @classmethod
    def from_archive(cls, archive: FArchive) -> Self:
        raise NotImplementedError("Phase 31")
```

**更新 `src/uasset_read/models/__init__.py`：**
在现有 5 个 core 导入后追加 node_types 导入：
```python
from .core import (...)  # 已有
from .node_types import (
    K2NodeCallFunction,
    K2NodeEvent,
    K2NodeKnot,
    EdGraphNodeComment,
    K2NodeEnhancedInputAction,
)

__all__ = [
    # 核心模型（core.py）
    "FEdGraphPinType", "UEdGraphPin", "UEdGraphNode", "UEdGraph", "FMemberReference",
    # 节点类型（node_types.py）
    "K2NodeCallFunction", "K2NodeEvent", "K2NodeKnot", "EdGraphNodeComment", "K2NodeEnhancedInputAction",
]
```

**更新 `src/uasset_read/__init__.py`：**
追加 5 个节点类型到 imports 和 __all__（在 Phase 29 注释区块内）。
  </action>
  <verify>
    <automated>python -c "from uasset_read import K2NodeCallFunction, K2NodeEvent, K2NodeKnot, EdGraphNodeComment, K2NodeEnhancedInputAction; print('5 node types importable')"</automated>
  </verify>
  <done>
    - node_types.py 存在且包含 5 个继承 UEdGraphNode 的 @dataclass
    - K2NodeCallFunction 有 function_reference: Optional[FMemberReference] 和 b_defaults_to_pure: bool
    - K2NodeEvent 有 event_reference: Optional[FMemberReference] 和 b_override_function: bool
    - K2NodeKot 无额外字段（仅基类字段）
    - EdGraphNodeComment 有 comment_color (Tuple), node_width, node_height, font_size
    - K2NodeEnhancedInputAction 有 input_action_path: str
    - 每个类有 from_archive classmethod stub
    - K2NodeCallFunction 是 UEdGraphNode 子类（isinstance(K2NodeCallFunction(), UEdGraphNode) 为 True）
    - models/__init__.py 导出 5 个节点类型
    - src/uasset_read/__init__.py 向后兼容重导出 5 个节点类型
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node_types.py subclasses | 纯数据类，继承自基类；字段类型安全依赖 dataclass 定义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering | node_types.py classes | accept | 纯数据类定义，无 I/O；解析验证由 Phase 31 负责 |
| T-29-04 | Insecure Reference | FMemberReference type | mitigate | Optional[FMemberReference] 允许 None，避免空引用 |
</threat_model>

<verification>
1. `python -c "from uasset_read.models.node_types import K2NodeCallFunction, K2NodeEvent, K2NodeKnot, EdGraphNodeComment, K2NodeEnhancedInputAction; print('node types OK')"`
2. `python -c "from uasset_read.models import K2NodeCallFunction; from uasset_read.models import UEdGraphNode; n = K2NodeCallFunction(node_guid='test'); assert isinstance(n, UEdGraphNode); print('inheritance OK')"`
3. `python -c "from uasset_read import K2NodeCallFunction; print('backward compat OK')"`
</verification>

<success_criteria>
- [ ] node_types.py 包含 5 个继承 UEdGraphNode 的 @dataclass
- [ ] 继承关系正确（isinstance 测试通过）
- [ ] 每个类有 from_archive classmethod stub
- [ ] models/__init__.py 导出全部 5 个节点类型
- [ ] src/uasset_read/__init__.py 向后兼容重导出
- [ ] match/case 可用于类型分派（基于 class_name 或 isinstance）
</success_criteria>

<output>
After completion, create `.planning/phases/29-core-data-models/29-02-SUMMARY.md`
</output>
