---
phase: 13-component-transform-properties
plan: 03
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
files_modified:
  - uasset_read.py
  - tests/test_phase13_transform.py
autonomous: true
requirements:
  - EXTR-04

must_haves:
  truths:
    - "VectorValue/RotatorValue/ScaleValue dataclass构造正确且可JSON序列化"
    - "parse_vector_value/parse_rotator_value/parse_scale_value转换StructValue正确"
    - "extract_component_transforms从ExportMap提取变换属性完整"
    - "测试覆盖.location/.rotation/.scale精度处理"
  artifacts:
    - path: "tests/test_phase13_transform.py"
      provides: "Transform解析完整测试套件"
      contains: "class TestVectorValue|TestRotatorValue|TestScaleValue|TestPrecision"
    - path: "uasset_read.py"
      provides: "Phase 13 __all__导出列表更新"
      contains: "VectorValue, RotatorValue, ScaleValue, parse_vector_value, parse_rotator_value, parse_scale_value, extract_component_transforms"
  key_links:
    - from: "tests/test_phase13_transform.py"
      to: "uasset_read.py"
      via: "导入Phase 13新增类型和函数"
      pattern: "from uasset_read import.*VectorValue|RotatorValue|ScaleValue"
    - from: "test_transform_values_construct"
      to: "VectorValue/RotatorValue/ScaleValue dataclass"
      via: "直接构造测试"
      pattern: "VectorValue\(x=1,y=2,z=3\)"
    - from: "test_component_transforms_extraction"
      to: "extract_component_transforms"
      via: "集成测试"
      pattern: "extract_component_transforms\(export\.properties\)"
---

<objective>
创建Phase 13完整测试覆盖，验证Transform dataclass构造、StructValue转换、精度处理和集成提取。

Purpose: 验证Phase 13所有功能正确实现（per D-02a, D-03, D-03a）
Output: tests/test_phase13_transform.py + __all__更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-transform-properties/13-CONTEXT.md
@.planning/phases/13-component-transform-properties/13-RESEARCH.md

<interfaces>
<!-- Phase 13 exported symbols -->

From uasset_read.py lines 890+ (Phase 13 additions):
```python
@dataclass
class VectorValue(AdvancedPropertyValue):
    x: float
    y: float
    z: float

@dataclass
class RotatorValue(AdvancedPropertyValue):
    roll: float
    pitch: float
    yaw: float
    unit: str = 'degrees'

@dataclass
class ScaleValue(AdvancedPropertyValue):
    x: float
    y: float
    z: float

def format_transform_value(value: float, precision_type: str) -> Union[int, float]: ...
def parse_vector_value(struct_value: StructValue, precision_type: str = 'location') -> VectorValue: ...
def parse_rotator_value(struct_value: StructValue) -> RotatorValue: ...
def parse_scale_value(struct_value: StructValue) -> ScaleValue: ...
def extract_component_transforms(props: List[PropertyValue]) -> Dict[str, Any]: ...
```

From tests/test_phase12_blueprint_variables.py (Phase 12 Wave 3 test pattern):
```python
FIRST_PERSON_CHARACTER_PATH = "E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/.../BP_FirstPersonCharacter.uasset"

def get_test_asset_path():
    if os.path.exists(FIRST_PERSON_CHARACTER_PATH):
        return FIRST_PERSON_CHARACTER_PATH
    return None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建tests/test_phase13_transform.py测试文件</name>
  <files>tests/test_phase13_transform.py</files>
  <read_first>
    tests/test_phase12_blueprint_variables.py (Phase 12 test pattern)
    tests/__init__.py (fixture导入)
  </read_first>
  <action>
    创建tests/test_phase13_transform.py，遵循Phase 12测试模式。

文件结构：
```python
"""
Phase 13 Transform Properties Extraction Tests

Tests for EXTR-04 requirement:
- VectorValue/RotatorValue/ScaleValue dataclass construction
- StructValue to specialized value conversion
- Transform extraction from ExportMap component properties
- Precision handling (location 3 decimals, rotation 3 decimals, scale 4 decimals)

Created: 2026-05-03 (Phase 13 Wave 3)
"""

import pytest
import os
import json
from uasset_read import (
    VectorValue,
    RotatorValue,
    ScaleValue,
    StructValue,
    parse_vector_value,
    parse_rotator_value,
    parse_scale_value,
    format_transform_value,
    extract_component_transforms,
    parse_uasset,
)
```

导入所有Phase 13新增类型和函数（per 13-01, 13-02）。
    </action>
  <verify>
    <automated>test -f tests/test_phase13_transform.py && head -30 tests/test_phase13_transform.py | grep -c "from uasset_read import"</automated>
    Expected: 文件存在，导入语句包含Phase 13所有符号
  </verify>
  <done>
    tests/test_phase13_transform.py文件创建
    所有Phase 13类型和函数导入正确
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建Transform Values构造测试</name>
  <files>tests/test_phase13_transform.py</files>
  <read_first>
    tests/test_phase13_transform.py (new file)
  </read_first>
  <action>
    在tests/test_phase13_transform.py添加TestTransformValuesConstructor类测试。

测试内容（per D-04, D-04a）：
```python
class TestTransformValuesConstructor:
    """Test Transform dataclass construction (per 13-01, D-04, D-04a)"""

    def test_vector_value_construct(self):
        """VectorValue should construct with x, y, z floats"""
        v = VectorValue(x=1.0, y=2.0, z=3.0)
        assert v.x == 1.0
        assert v.y == 2.0
        assert v.z == 3.0
        assert v.property_type == "StructProperty"  # inherited

    def test_rotator_value_construct(self):
        """RotatorValue should construct with roll, pitch, yaw, unit"""
        r = RotatorValue(roll=100.0, pitch=200.0, yaw=300.0)
        assert r.roll == 100.0
        assert r.pitch == 200.0
        assert r.yaw == 300.0
        assert r.unit == "degrees"  # default value (per D-02a)

    def test_scale_value_construct(self):
        """ScaleValue should construct with x, y, z floats"""
        s = ScaleValue(x=1.5, y=1.5, z=1.5)
        assert s.x == 1.5
        assert s.y == 1.5
        assert s.z == 1.5

    def test_vector_value_json_serializable(self):
        """VectorValue should be JSON serializable"""
        v = VectorValue(x=1.0, y=2.0, z=3.0)
        data = json.dumps(v.__dict__)
        parsed = json.loads(data)
        assert parsed["x"] == 1.0
        assert parsed["y"] == 2.0
        assert parsed["z"] == 3.0

    def test_rotator_value_json_serializable(self):
        """RotatorValue should be JSON serializable with unit field"""
        r = RotatorValue(roll=100.0, pitch=200.0, yaw=300.0)
        data = json.dumps(r.__dict__)
        parsed = json.loads(data)
        assert parsed["unit"] == "degrees"
```
    </action>
  <verify>
    <automated>grep -c "class TestTransformValuesConstructor" tests/test_phase13_transform.py && grep -c "test_vector_value_construct" tests/test_phase13_transform.py</automated>
    Expected: 测试类和方法定义存在
  </verify>
  <done>
    TestTransformValuesConstructor类包含6个测试方法
    VectorValue/RotatorValue/ScaleValue构造测试
    JSON序列化测试
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建StructValue转换测试</name>
  <files>tests/test_phase13_transform.py</files>
  <read_first>
    tests/test_phase13_transform.py (existing)
  </read_first>
  <action>
    添加TestStructValueConversion类测试。

测试内容：
```python
class TestStructValueConversion:
    """Test StructValue to specialized value conversion (per 13-02)"""

    def test_parse_vector_value(self):
        """parse_vector_value should convert StructValue to VectorValue"""
        struct_val = StructValue(
            property_type="StructProperty",
            struct_type="Vector",
            fields={"X": 10.0, "Y": 20.0, "Z": 30.0}
        )
        result = parse_vector_value(struct_val)
        assert isinstance(result, VectorValue)
        assert result.x == 10.0
        assert result.y == 20.0
        assert result.z == 30.0

    def test_parse_rotator_value(self):
        """parse_rotator_value should convert StructValue to RotatorValue"""
        struct_val = StructValue(
            property_type="StructProperty",
            struct_type="Rotator",
            fields={"Roll": 100.0, "Pitch": 200.0, "Yaw": 300.0}
        )
        result = parse_rotator_value(struct_val)
        assert isinstance(result, RotatorValue)
        assert result.roll == 100.0
        assert result.pitch == 200.0
        assert result.yaw == 300.0

    def test_parse_scale_value(self):
        """parse_scale_value should convert StructValue to ScaleValue"""
        struct_val = StructValue(
            property_type="StructProperty",
            struct_type="Vector",  # Scale3D uses same struct type
            fields={"X": 1.5, "Y": 1.5, "Z": 1.5}
        )
        result = parse_scale_value(struct_val)
        assert isinstance(result, ScaleValue)
        assert result.x == 1.5
        assert result.y == 1.5
        assert result.z == 1.5

    def test_parse_rotator_value_includes_unit(self):
        """RotatorValue should have unit='degrees' field"""
        struct_val = StructValue(
            property_type="StructProperty",
            struct_type="Rotator",
            fields={"Roll": 0.0, "Pitch": 0.0, "Yaw": 0.0}
        )
        result = parse_rotator_value(struct_val)
        assert result.unit == "degrees"
```
    </action>
  <verify>
    <automated>grep -c "class TestStructValueConversion" tests/test_phase13_transform.py && grep -c "test_parse_vector_value" tests/test_phase13_transform.py</automated>
    Expected: 测试类和方法定义存在
  </verify>
  <done>
    TestStructValueConversion类包含4个测试方法
    VectorValue/RotatorValue/ScaleValue转换测试
    unit字段验证
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建精度处理测试</name>
  <files>tests/test_phase13_transform.py</files>
  <read_first>
    tests/test_phase13_transform.py (existing)
  </read_first>
  <action>
    添加TestPrecisionHandling类测试（per D-03, D-03a）。

测试内容：
```python
class TestPrecisionHandling:
    """Test transform precision handling (per 13-01, D-03, D-03a)"""

    def test_format_transform_value_location_integer(self):
        """Location: integer value should output as int"""
        result = format_transform_value(10.0, 'location')
        assert result == 10  # int, not float

    def test_format_transform_value_location_decimal(self):
        """Location: decimal value should round to 3 decimal places"""
        result = format_transform_value(10.123456, 'location')
        assert result == 10.123  # 3 decimals

    def test_format_transform_value_rotation(self):
        """Rotation: should round to 3 decimal places"""
        result = format_transform_value(1.23456789, 'rotation')
        assert result == 1.235  # 3 decimals

    def test_format_transform_value_scale(self):
        """Scale: should round to 4 decimal places"""
        result = format_transform_value(1.23456789, 'scale')
        assert result == 1.2346  # 4 decimals

    def test_parse_vector_value_precision(self):
        """parse_vector_value should apply location precision"""
        struct_val = StructValue(
            property_type="StructProperty",
            struct_type="Vector",
            fields={"X": 10.0, "Y": 1.2345678, "Z": 100.9999}
        )
        result = parse_vector_value(struct_val)
        assert result.x == 10  # int
        assert result.y == 1.235  # 3 decimals
        assert result.z == 101.0  # rounds to 101.0 -> 101 as int?
```
    </action>
  <verify>
    <automated>grep -c "class TestPrecisionHandling" tests/test_phase13_transform.py && grep -c "test_format_transform_value" tests/test_phase13_transform.py</automated>
    Expected: 测试类和方法定义存在
  </verify>
  <done>
    TestPrecisionHandling类包含5个测试方法
    format_transform_value各精度类型测试
    parse_*_value精度应用测试
  </done>
</task>

<task type="auto">
  <name>Task 5: 创建组件变换提取测试</name>
  <files>tests/test_phase13_transform.py</files>
  <read_first>
    tests/test_phase13_transform.py (existing)
  </read_first>
  <action>
    添加TestComponentTransforms类测试（per D-01, D-01a）。

测试内容：
```python
class TestComponentTransforms:
    """Test component transform extraction from ExportMap (per 13-02, D-01, D-01a)"""

    def test_extract_component_transforms_empty(self):
        """extract_component_transforms should return empty dict for empty props"""
        result = extract_component_transforms([])
        assert result == {}

    def test_extract_component_transforms_relative_location(self):
        """Should extract RelativeLocation property"""
        props = [
            PropertyValue(
                name="RelativeLocation",
                type="StructProperty",
                value=StructValue(
                    property_type="StructProperty",
                    struct_type="Vector",
                    fields={"X": 100.0, "Y": 200.0, "Z": 0.0}
                )
            )
        ]
        result = extract_component_transforms(props)
        assert "relative_location" in result
        assert result["relative_location"].x == 100.0

    def test_extract_component_transforms_relative_rotation(self):
        """Should extract RelativeRotation property"""
        props = [
            PropertyValue(
                name="RelativeRotation",
                type="StructProperty",
                value=StructValue(
                    property_type="StructProperty",
                    struct_type="Rotator",
                    fields={"Roll": 0.0, "Pitch": 0.0, "Yaw": 90.0}
                )
            )
        ]
        result = extract_component_transforms(props)
        assert "relative_rotation" in result
        assert result["relative_rotation"].yaw == 90.0

    def test_extract_component_transforms_relative_scale(self):
        """Should extract RelativeScale3D property"""
        props = [
            PropertyValue(
                name="RelativeScale3D",
                type="StructProperty",
                value=StructValue(
                    property_type="StructProperty",
                    struct_type="Vector",
                    fields={"X": 1.5, "Y": 1.5, "Z": 1.5}
                )
            )
        ]
        result = extract_component_transforms(props)
        assert "relative_scale" in result
        assert result["relative_scale"].x == 1.5

    def test_extract_component_transforms_all_three(self):
        """Should extract all three transform properties"""
        props = [
            PropertyValue(name="RelativeLocation", type="StructProperty", value=...),
            PropertyValue(name="RelativeRotation", type="StructProperty", value=...),
            PropertyValue(name="RelativeScale3D", type="StructProperty", value=...)
        ]
        result = extract_component_transforms(props)
        assert len(result) == 3
        assert "relative_location" in result
        assert "relative_rotation" in result
        assert "relative_scale" in result

    def test_extract_component_transforms_ignores_other_properties(self):
        """Should ignore non-transform properties"""
        props = [
            PropertyValue(name="SomeOtherProp", type="IntProperty", value=42),
            PropertyValue(name="RelativeLocation", type="StructProperty", value=...)
        ]
        result = extract_component_transforms(props)
        assert "relative_location" in result
        assert len(result) == 1
```
    </action>
  <verify>
    <automated>grep -c "class TestComponentTransforms" tests/test_phase13_transform.py && grep -c "test_extract_component_transforms" tests/test_phase13_transform.py</automated>
    Expected: 测试类和方法定义存在
  </verify>
  <done>
    TestComponentTransforms类包含5个测试方法
    各transform属性提取测试
    空值和忽略其他属性测试
  </done>
</task>

<task type="auto">
  <name>Task 6: 创建集成测试（完整资产解析）</name>
  <files>tests/test_phase13_transform.py</files>
  <read_first>
    tests/test_phase13_transform.py (existing)
  </read_first>
  <action>
    添加IntegrationTests类测试，验证完整资产解析。

测试内容：
```python
class IntegrationTests:
    """Integration tests with actual UAsset files"""

    FIRST_PERSON_CHARACTER_PATH = "E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset"

    def test_parse_uasset_contains_transforms(self):
        """parse_uasset should populate export.transforms for components"""
        if not os.path.exists(self.FIRST_PERSON_CHARACTER_PATH):
            pytest.skip("Test asset not available")

        result = parse_uasset(self.FIRST_PERSON_CHARACTER_PATH)
        
        # 期望至少一个export有transforms
        exports_with_transforms = [exp for exp in result.export_map if hasattr(exp, 'transforms') and exp.transforms]
        assert len(exports_with_transforms) >= 1, "Expected at least one component with transforms"

    def test_transforms_have_expected_fields(self):
        """Extracted transforms should have expected fields"""
        if not os.path.exists(self.FIRST_PERSON_CHARACTER_PATH):
            pytest.skip("Test asset not available")

        result = parse_uasset(self.FIRST_PERSON_CHARACTER_PATH)
        
        for exp in result.export_map:
            if hasattr(exp, 'transforms') and exp.transforms:
                if "relative_location" in exp.transforms:
                    loc = exp.transforms["relative_location"]
                    assert hasattr(loc, 'x')
                    assert hasattr(loc, 'y')
                    assert hasattr(loc, 'z')
                if "relative_rotation" in exp.transforms:
                    rot = exp.transforms["relative_rotation"]
                    assert hasattr(rot, 'roll')
                    assert hasattr(rot, 'pitch')
                    assert hasattr(rot, 'yaw')
                    assert hasattr(rot, 'unit')
                if "relative_scale" in exp.transforms:
                    scale = exp.transforms["relative_scale"]
                    assert hasattr(scale, 'x')
                    assert hasattr(scale, 'y')
                    assert hasattr(scale, 'z')
```
    </action>
  <verify>
    <automated>grep -c "class IntegrationTests" tests/test_phase13_transform.py && grep -c "FIRST_PERSON_CHARACTER_PATH" tests/test_phase13_transform.py</automated>
    Expected: 集成测试类和路径定义存在
  </verify>
  <done>
    IntegrationTests类包含2个测试方法
    完整资产解析集成测试
    transforms字段验证
  </done>
</task>

<task type="auto">
  <name>Task 7: 更新__all__导出列表</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 4930-4950 (__all__导出列表)
    13-01 PLAN (Wave 1 additions)
    13-02 PLAN (Wave 2 additions)
  </read_first>
  <action>
    确认__all__导出列表包含所有Phase 13新增符号：

```python
__all__ = [
    # ... existing exports ...
    
    # Phase 13: Transform Property Parsing (EXTR-04)
    'VectorValue',
    'RotatorValue',
    'ScaleValue',
    'format_transform_value',
    'parse_vector_value',
    'parse_rotator_value',
    'parse_scale_value',
    'extract_component_transforms',
]
```
    </action>
  <verify>
    <automated>grep -c "VectorValue" uasset_read.py && grep -c "RotatorValue" uasset_read.py && grep -c "extract_component_transforms" uasset_read.py</automated>
    Expected: 所有Phase 13函数在__all__中
  </verify>
  <done>
    __all__包含所有Phase 13新增符号
    VectorValue, RotatorValue, ScaleValue
    format_transform_value, parse_vector_value, parse_rotator_value, parse_scale_value
    extract_component_transforms
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试资产路径 | 来自配置路径，需存在性验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | 测试数据构造 | accept | 仅测试内部数据构造，无外部输入 |
| T-13-07 | Information Disclosure | 测试输出 | accept | 无敏感信息，仅解析输出验证 |
| T-13-08 | Repudiation | 缺失测试资产 | accept | pytest.skip跳过，不中断测试套件 |
</threat_model>

<verification>
整体Phase 13验证：
- tests/test_phase13_transform.py创建且包含7测试类
- TestTransformValuesConstructor: 6 tests
- TestStructValueConversion: 4 tests  
- TestPrecisionHandling: 5 tests
- TestComponentTransforms: 5 tests
- IntegrationTests: 2 tests
- pytest tests/test_phase13_transform.py -x -v 全部通过
- pytest tests/ -v 全部通过（回归测试）
</verification>

<success_criteria>
- pytest tests/test_phase13_transform.py -x -v 全部通过
- 相对坐标字段名转换为snake_case（relative_location/relative_rotation/relative_scale）
- VectorValue/RotatorValue/ScaleValue可JSON序列化
- Precision处理：location整数优先/3位，rotation 3位，scale 4位
- export.transforms字段可访问（parse_uasset集成）
</success_criteria>

<output>
完成后创建`.planning/phases/13-component-transform-properties/13-03-SUMMARY.md`
</output>
