---
phase: 13-component-transform-properties
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - uasset_read.py
autonomous: true
requirements:
  - EXTR-04

must_haves:
  truths:
    - "parse_struct_property返回StructValue后，根据struct_type分派到专用解析函数"
    - "Vector字段提取X/Y/Z（大写），Scale使用相同格式"
    - "Rotator字段提取Roll/Pitch/Yaw（大写），unit='degrees'填充"
    - "从ExportMap组件properties筛选RelativeLocation/RelativeRotation/RelativeScale3D"
  artifacts:
    - path: "uasset_read.py"
      provides: "parse_vector_value函数"
      pattern: "def parse_vector_value"
    - path: "uasset_read.py"
      provides: "parse_rotator_value函数"
      pattern: "def parse_rotator_value"
    - path: "uasset_read.py"
      provides: "parse_scale_value函数"
      pattern: "def parse_scale_value"
    - path: "uasset_read.py"
      provides: "extract_component_transforms函数"
      pattern: "def extract_component_transforms"
  key_links:
    - from: "parse_struct_property"
      to: "parse_vector_value/parse_rotator_value/parse_scale_value"
      via: "struct_type分派"
      pattern: "struct_type == \"Vector\"|\"Rotator\""
    - from: "extract_component_transforms"
      to: "format_transform_value"
      via: "精度处理应用"
      pattern: "format_transform_value"
---

<objective>
实现StructProperty到专用VectorValue/RotatorValue/ScaleValue的转换，添加分派逻辑从ExportMap组件properties提取变换属性（per D-01, D-01a）。

Purpose: 从Phase 11的parse_properties_from_export输出中识别并转换变换类型StructProperty（per D-01）
Output: parse_vector_value/parse_rotator_value/parse_scale_value函数 + extract_component_transforms函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-transform-properties/13-CONTEXT.md
@.planning/phases/13-component-transform-properties/13-RESEARCH.md

<interfaces>
<!-- Key interfaces from Phase 11 and Phase 13 Wave 1 -->

From uasset_read.py lines 820-825 (PropertyValue dataclass):
```python
@dataclass
class PropertyValue:
    name: str                         # 属性名
    type: str                         # 属性类型
    value: any = None                 # 解析后的值（int, float, str, list 等）
    array_index: int = 0              # 数组元素索引
```

From 13-01 PLAN (Wave 1 completed):
```python
@dataclass
class VectorValue(AdvancedPropertyValue):
    x: float
    y: float
    z: float

@dataclass
class RotatorValue(AdvancedPropertyValue):
    roll: float
    pitch: float
    yaw: float
    unit: str = 'degrees'

@dataclass
class ScaleValue(AdvancedPropertyValue):
    x: float
    y: float
    z: float
```

From uasset_read.py lines 4079-4185 (parse_properties_from_export返回):
```python
def parse_properties_from_export(...) -> List[PropertyValue]:
    # Returns list of PropertyValue with:
    # - name: "RelativeLocation", "RelativeRotation", "RelativeScale3D"
    # - type: "StructProperty"
    # - value: StructValue struct_type="Vector"/"Rotator"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建parse_vector_value/parse_rotator_value/parse_scale_value函数</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 890-900 (现有AdvancedPropertyValue子类定义后位置)
    13-RESEARCH.md lines 315-390 (Transform extractor pattern)
  </read_first>
  <action>
    创建三个专用解析函数，位置在format_transform_value之后（约line 890附近）：

1. parse_vector_value(struct_value: StructValue, precision_type: str = 'location') -> VectorValue:
   - Input: StructValue with struct_type="Vector", fields={X, Y, Z}
   - Extract x=fields["X"], y=fields["Y"], z=fields["Z"] (大写字母)
   - Apply format_transform_value for each coordinate
   - Return VectorValue(x, y, z)

2. parse_rotator_value(struct_value: StructValue) -> RotatorValue:
   - Input: StructValue with struct_type="Rotator", fields={Roll, Pitch, Yaw}
   - Extract roll=fields["Roll"], pitch=fields["Pitch"], yaw=fields["Yaw"]
   - Apply format_transform_value(precision_type='rotation')
   - Return RotatorValue(roll, pitch, yaw, unit='degrees')

3. parse_scale_value(struct_value: StructValue) -> ScaleValue:
   - Input: StructValue with struct_type="Vector", fields={X, Y, Z} (Scale3D uses same format)
   - Extract x=fields["X"], y=fields["Y"], z=fields["Z"]
   - Apply format_transform_value(precision_type='scale')
   - Return ScaleValue(x, y, z)

实现（参考13-RESEARCH.md Pattern 3）：
```python
def parse_vector_value(struct_value: StructValue, precision_type: str = 'location') -> VectorValue:
    """Parse Vector struct property to VectorValue (Phase 13)."""
    fields = struct_value.fields
    x = format_transform_value(fields["X"], precision_type)
    y = format_transform_value(fields["Y"], precision_type)
    z = format_transform_value(fields["Z"], precision_type)
    return VectorValue(x=x, y=y, z=z)

def parse_rotator_value(struct_value: StructValue) -> RotatorValue:
    """Parse Rotator struct property to RotatorValue (Phase 13)."""
    fields = struct_value.fields
    roll = format_transform_value(fields["Roll"], 'rotation')
    pitch = format_transform_value(fields["Pitch"], 'rotation')
    yaw = format_transform_value(fields["Yaw"], 'rotation')
    return RotatorValue(roll=roll, pitch=pitch, yaw=yaw)

def parse_scale_value(struct_value: StructValue) -> ScaleValue:
    """Parse Scale3D struct property to ScaleValue (Phase 13)."""
    fields = struct_value.fields
    x = format_transform_value(fields["X"], 'scale')
    y = format_transform_value(fields["Y"], 'scale')
    z = format_transform_value(fields["Z"], 'scale')
    return ScaleValue(x=x, y=y, z=z)
```

更新__all__导出列表添加三个新函数。
    </action>
  <verify>
    <automated>grep -c "def parse_vector_value" uasset_read.py && grep -c "def parse_rotator_value" uasset_read.py && grep -c "def parse_scale_value" uasset_read.py</automated>
    Expected: 三个函数定义各返回>=1
  </verify>
  <done>
    parse_vector_value/parse_rotator_value/parse_scale_value函数已定义
    正确提取大写字段名X/Y/Z和Roll/Pitch/Yaw
    format_transform_value应用正确精度类型
    函数已添加到__all__导出列表
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建extract_component_transforms函数</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 4185-4210 (parse_properties_from_export之后位置)
    13-CONTEXT.md D-01, D-01a (Transform extraction rules)
  </read_first>
  <action>
    创建extract_component_transforms(export_properties: List[PropertyValue], component_name: str = None) -> Dict[str, Any]函数，位置在parse_scale_value之后（约line 4210附近）。

函数功能（per D-01, D-01a）：
1. 遍历export_properties列表
2. 筛选条件（D-01）：
   - prop.type == "StructProperty"
   - prop.name in ["RelativeLocation", "RelativeRotation", "RelativeScale3D"]
   - prop.value.struct_type in ["Vector", "Rotator"]
3. 分派到对应解析函数（D-01a）：
   - name == "RelativeLocation" → parse_vector_value(prop.value, 'location')
   - name == "RelativeRotation" → parse_rotator_value(prop.value)
   - name == "RelativeScale3D" → parse_scale_value(prop.value)
4. 返回字典：{"relative_location": VectorValue, "relative_rotation": RotatorValue, "relative_scale": ScaleValue}

实现：
```python
def extract_component_transforms(
    export_properties: List[PropertyValue],
    component_name: str = None
) -> Dict[str, Any]:
    """从组件export的properties中提取变换属性（per D-01, D-01a）。"""
    transforms = {}
    
    for prop in export_properties:
        if prop.type != "StructProperty" or not prop.value:
            continue
        
        struct_val = prop.value
        if not isinstance(struct_val, StructValue):
            continue
        
        prop_name = prop.name
        
        # 从Context D-01:筛选RelativeLocation/RelativeRotation/RelativeScale3D
        if prop_name == "RelativeLocation" and struct_val.struct_type == "Vector":
            transforms["relative_location"] = parse_vector_value(struct_val, 'location')
        elif prop_name == "RelativeRotation" and struct_val.struct_type == "Rotator":
            transforms["relative_rotation"] = parse_rotator_value(struct_val)
        elif prop_name == "RelativeScale3D" and struct_val.struct_type == "Vector":
            transforms["relative_scale"] = parse_scale_value(struct_val)
    
    return transforms
```

更新__all__导出列表添加extract_component_transforms。
    </action>
  <verify>
    <automated>grep -c "def extract_component_transforms" uasset_read.py && grep -c "RelativeLocation" uasset_read.py && grep -c "RelativeRotation" uasset_read.py</automated>
    Expected: extract_component_transforms函数存在，筛选条件包含RelativeLocation/RelativeRotation/RelativeScale3D
  </verify>
  <done>
    extract_component_transforms函数正确筛选并转换变换属性
    相对坐标字段名转换为snake_case（relative_location/relative_rotation/relative_scale）
    函数已添加到__all__导出列表
  </done>
</task>

<task type="auto">
  <name>Task 3: 集成到parse_properties_from_export调用链</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py lines 4110-4185 (parse_properties_from_export循环结构)
    13-RESEARCH.md lines 315-390 (Pattern 4: Transform extractor)
  </read_first>
  <action>
    在parse_properties_from_export函数返回值之前（line 4185 return properties之前），添加组件变换属性提取逻辑。

修改位置：在properties.append之后（line 4154附近）或返回前新增后处理循环。

方案（推荐）：在parse_properties_from_export返回前添加后处理：
```python
# Phase 13: 提取组件变换属性
# 识别主要组件：通过is_component标志或object_name匹配
component_transforms = extract_component_transforms(properties)
```

更完整的方案（两种模式）：
1. 简单版本：返回独立函数extract_component_transforms供调用方使用
2. 增强版本：在parse_uasset中集成（参考Phase 12的extract_blueprint_variable模式）

推荐采用简单版本（Phase 13-02范围），在parse_uasset中提供可选调用：
```python
# In parse_uasset, after parse_properties_from_export:
export.transforms = extract_component_transforms(export.properties)
```

在parse_uasset约line 4299的parse_properties_from_export调用后：
```python
export.properties = parse_properties_from_export(...)
# Phase 13-02: 提取组件变换属性
export.transforms = extract_component_transforms(export.properties)
```

确保导入StructValue, parse_vector_value, parse_rotator_value, parse_scale_value。
    </action>
  <verify>
    <automated>grep -c "export.transforms = extract_component_transforms" uasset_read.py</automated>
    Expected: 在parse_uasset中成功集成extract_component_transforms调用
  </verify>
  <done>
    extract_component_transforms在parse_uasset中调用
    export.transforms字段填充ComponentTransforms结构
    无is_component标志时返回空字典而非None
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StructValue.fields字典访问 | 需验证字段存在性 |
| export.properties列表遍历 | 空列表安全 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information Disclosure | StructValue.fields KeyError | mitigat | 检查"X"/"Y"/"Z"或"Roll"/"Pitch"/"Yaw"存在性 |
| T-13-04 | Tampering | extract_component_transforms输入 | accept | List[PropertyValue]来自已验证解析 |
| T-13-05 | Repudiation |疏异常 | accept | 无外部输入，仅内部数据转换 |
</threat_model>

<verification>
整体Wave 2验证：
- parse_vector_value/parse_rotator_value/parse_scale_value函数实现正确
- 字段提取使用大写命名（X/Y/Z/Roll/Pitch/Yaw）
- extract_component_transforms筛选RelativeLocation/RelativeRotation/RelativeScale3D
- 集成到parse_uasset主流程
- export.transforms字段可用
</verification>

<success_criteria>
- parse_vector_value(StructValue with fields={X:10,Y:20,Z:30})返回VectorValue(x=10,y=20,z=30)
- parse_rotator_value(StructValue with fields={Roll:100,Pitch:200,Yaw:300})返回RotatorValue
- extract_component_transforms筛选RelativeLocation/RelativeRotation/RelativeScale3D
- export.transforms包含relative_location/relative_rotation/relative_scale键
</success_criteria>

<output>
完成后创建`.planning/phases/13-component-transform-properties/13-02-SUMMARY.md`
</output>
