---
phase: 17-property-parsing-fix
plan: 03
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified: [uasset_read.py, tests/test_property_parsing.py]
autonomous: true
requirements: [FIX-07]
user_setup: []

must_haves:
  truths:
    - "PropertyTag Extensions (0x04) 标志正确处理"
    - "UE 5.7 资产 BP_FirstPersonCharacter.uasset 属性解析成功"
    - "69 个导出对象属性全部可解析（当前 53/69 有错误 → 目标 0/69）"
    - "蓝图变量、组件属性数据正确提取"
  artifacts:
    - path: "uasset_read.py"
      function: "read_property_tag"
      contains: "PROP_TAG_HAS_EXTENSIONS"
      pattern: "property_extensions"
    - path: "tests/test_property_parsing.py"
      contains: "test_property_tag_extensions"
  key_links:
    - from: "read_property_tag()"
      to: "PropertyTag.cpp Extensions"
      via: "flags & 0x04 检查"
      pattern: "PROP_TAG_HAS_EXTENSIONS"
---

<objective>
实现 PropertyTag Extensions 处理（D-03）并完成测试验证。

Purpose: PropertyTag flags 包含 HAS_EXTENSIONS (0x04) 时有扩展数据未读取，导致位置错位。
Output: read_property_tag() 正确处理 Extensions，完整修复使 UE 5.7 资产解析成功。

根因验证（RESEARCH.md D-03）:
- UE 源码 PropertyTag.cpp 第 541-544 行：当 flags & HAS_EXTENSIONS 时序列化扩展
- EPropertyTagExtension (u8): 扩展标志
- 条件字段：OverridableInformation (0x02) → override_operation (u8) + experimental_overridable_logic (u8)
- 当前代码第 52 行已定义 PROP_TAG_HAS_EXTENSIONS = 0x04，但注释 "defer to Phase 3" 未实现
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-property-parsing-fix/17-CONTEXT.md
@.planning/phases/17-property-parsing-fix/17-RESEARCH.md
@.planning/phases/17-property-parsing-fix/17-01-SUMMARY.md
@.planning/phases/17-property-parsing-fix/17-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From uasset_read.py (第 49-55 行):
```python
# PropertyTag flags (PropertyTag.h lines 17-26)
PROP_TAG_NONE = 0x00
PROP_TAG_HAS_ARRAY_INDEX = 0x01      # ArrayIndex field present
PROP_TAG_HAS_PROPERTY_GUID = 0x02    # PropertyGuid field present
PROP_TAG_HAS_EXTENSIONS = 0x04       # Extension data (defer to Phase 3)
PROP_TAG_HAS_BINARY_OR_NATIVE = 0x08 # Binary/native serialize
PROP_TAG_BOOL_TRUE = 0x10            # Bool value is true
PROP_TAG_SKIPPED_SERIALIZE = 0x20    # Skipped serialize
```

From uasset_read.py (第 3560-3571 行 - UE5 格式处理):
```python
# 条件字段（基于标志位）
if tag.flags & PROP_TAG_HAS_ARRAY_INDEX:
    tag.array_index = archive.read_i32()

if tag.flags & PROP_TAG_HAS_PROPERTY_GUID:
    tag.property_guid = archive.read(16)

# BoolTrue 标志表示 bool 值为 true
if tag.flags & PROP_TAG_BOOL_TRUE:
    tag.bool_val = 1
```

From PropertyTag.cpp (UE 源码参考):
```cpp
// 第 17-48 行：EPropertyTagFlags 和 EPropertyTagExtension 定义
enum class EPropertyTagFlags : uint8
{
    HasPropertyExtensions      = 0x04,
};

enum class EPropertyTagExtension : uint8
{
    NoExtension           = 0x00,
    ReserveForFutureUse   = 0x01,
    OverridableInformation = 0x02,
};

// 第 155-173 行：SerializePropertyExtensions
static void SerializePropertyExtensions(...)
{
    Slot << SA_ATTRIBUTE(TEXT("PropertyExtensions"), PropertyTagExtensions);
    if (EnumHasAnyFlags(PropertyTagExtensions, EPropertyTagExtension::OverridableInformation))
    {
        Slot << SA_ATTRIBUTE(TEXT("OverriddenPropertyOperation"), Tag.OverrideOperation);
        Slot << SA_ATTRIBUTE(TEXT("ExperimentalOverridableLogic"), Tag.bExperimentalOverridableLogic);
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 read_property_tag() 中添加 Extensions 处理</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py (第 3527-3588 行 read_property_tag 函数)
    - uasset_read.py (第 49-55 行 PropertyTag flags 常量)
    - uasset_read.py (第 810-825 行 PropertyTag dataclass)
    - .planning/phases/17-property-parsing-fix/17-RESEARCH.md (D-03 根因验证)
  </read_first>
  <action>
    在 read_property_tag() 的 UE5 格式分支中，添加 HAS_EXTENSIONS 处理：

    在第 3567 行（HAS_PROPERTY_GUID 处理后）添加：

    ```python
    # D-03: PropertyTag Extensions 处理
    # 参考: PropertyTag.cpp 第 155-173 行、第 541-544 行
    if tag.flags & PROP_TAG_HAS_EXTENSIONS:
        # EPropertyTagExtension (u8)
        property_extensions = archive.read_u8()

        # OverridableInformation 标志 (0x02)
        if property_extensions & 0x02:
            # EOverriddenPropertyOperation (u8)
            tag.override_operation = archive.read_u8()
            # bExperimentalOverridableLogic — 根据研究暂按 u8 处理
            tag.experimental_overridable_logic = archive.read_u8()
    ```

    同时需要更新 PropertyTag dataclass（第 810-825 行），添加扩展字段：

    ```python
    @dataclass
    class PropertyTag:
        name: str
        type: str
        size: int
        array_index: int = 0
        flags: int = 0
        property_guid: Optional[bytes] = None
        bool_val: int = 0
        # D-03: Extensions 字段
        override_operation: Optional[int] = None  # EOverriddenPropertyOperation
        experimental_overridable_logic: Optional[int] = None
    ```

    关键点：
    - PROP_TAG_HAS_EXTENSIONS = 0x04 已在第 52 行定义
    - Extensions 处理位于 HAS_ARRAY_INDEX 和 HAS_PROPERTY_GUID 之后
    - OverridableInformation 标志值为 0x02
    - experimental_overridable_logic 暂按 u8 处理（研究建议）
  </action>
  <acceptance_criteria>
    - uasset_read.py read_property_tag 函数包含 "PROP_TAG_HAS_EXTENSIONS" 检查
    - uasset_read.py 包含 "property_extensions = archive.read_u8()"
    - uasset_read.py 包含 "property_extensions & 0x02" 标志检查
    - PropertyTag dataclass 包含 override_operation 字段
    - PropertyTag dataclass 包含 experimental_overridable_logic 字段
  </acceptance_criteria>
  <verify>
    <automated>python -c "from uasset_read import read_property_tag, PropertyTag; import inspect; src = inspect.getsource(read_property_tag); assert 'PROP_TAG_HAS_EXTENSIONS' in src and 'property_extensions' in src, 'Extensions 处理未添加'"</automated>
  </verify>
  <done>
    - read_property_tag() 正确处理 HAS_EXTENSIONS 标志
    - PropertyTag dataclass 包含扩展字段
    - 代码无语法错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加 PropertyTag Extensions 单元测试</name>
  <files>tests/test_property_parsing.py</files>
  <read_first>
    - tests/test_property_parsing.py (第 98-150 行现有 PropertyTag 测试)
    - uasset_read.py (第 49-55 行 PropertyTag flags 常量)
    - uasset_read.py (第 810-825 行 PropertyTag dataclass)
  </read_first>
  <behavior>
    - Test 1: flags = 0x04 时，Extensions 数据正确读取
    - Test 2: property_extensions = 0x00 时，仅读取 1 byte
    - Test 3: property_extensions = 0x02 时，读取 3 bytes（extension + operation + logic）
  </behavior>
  <action>
    在 tests/test_property_parsing.py 中添加 Extensions 测试：

    ```python
    # ============================================================================
    # Phase 17 D-03: PropertyTag Extensions 测试
    # ============================================================================

    def test_property_tag_has_extensions_flag():
        """D-03: flags & 0x04 时，Extensions 数据正确读取"""
        from uasset_read import PROP_TAG_HAS_EXTENSIONS
        assert PROP_TAG_HAS_EXTENSIONS == 0x04

    def test_property_tag_extensions_no_extension():
        """D-03: property_extensions = 0x00 时，仅读取 1 byte"""
        # 构造 mock PropertyTag 数据（UE5 格式）
        # Name (FName index), Type (FString), Size (i32), Flags (u8), Extensions (u8)
        # 简化：直接构造 flags + extensions 数据
        data = bytes([
            0x04,  # flags = HAS_EXTENSIONS
            0x00,  # property_extensions = NoExtension
        ])
        archive = create_mock_archive_with_data(data)

        flags = archive.read_u8()
        assert flags == 0x04
        assert flags & PROP_TAG_HAS_EXTENSIONS

        property_extensions = archive.read_u8()
        assert property_extensions == 0x00
        assert not (property_extensions & 0x02)
        assert archive.tell() == 2

    def test_property_tag_extensions_with_overridable():
        """D-03: property_extensions = 0x02 时，读取 3 bytes"""
        data = bytes([
            0x04,  # flags = HAS_EXTENSIONS
            0x02,  # property_extensions = OverridableInformation
            0x00,  # override_operation
            0x00,  # experimental_overridable_logic
        ])
        archive = create_mock_archive_with_data(data)

        flags = archive.read_u8()
        assert flags == 0x04
        assert flags & PROP_TAG_HAS_EXTENSIONS

        property_extensions = archive.read_u8()
        assert property_extensions == 0x02
        assert property_extensions & 0x02

        # 读取扩展数据
        override_operation = archive.read_u8()
        experimental_overridable_logic = archive.read_u8()
        assert archive.tell() == 4  # 总共 4 bytes (flags + extensions + operation + logic)
    ```

    注意：
    - 使用 create_mock_archive_with_data 辅助函数
    - 测试验证字节消耗和字段读取
  </action>
  <acceptance_criteria>
    - tests/test_property_parsing.py 包含函数 test_property_tag_has_extensions_flag
    - tests/test_property_parsing.py 包含函数 test_property_tag_extensions_no_extension
    - tests/test_property_parsing.py 包含函数 test_property_tag_extensions_with_overridable
    - 测试文件包含注释 "# Phase 17 D-03"
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_property_parsing.py::test_property_tag_has_extensions_flag -x -v</automated>
  </verify>
  <done>
    - 三个 Extensions 测试函数添加完成
    - 所有测试通过
    - 测试覆盖 NoExtension (0x00) 和 OverridableInformation (0x02) 两种情况
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 17 全部修复已完成：
    1. D-01: 偏移计算修复 (serial_offset + script_serial_offset)
    2. D-02: SerializationControlExtensions 头部处理
    3. D-03: PropertyTag Extensions 处理
  </what-built>
  <how-to-verify>
    运行完整验证：

    1. 运行测试套件：
       ```bash
       python -m pytest tests/test_property_parsing.py -v
       ```

    2. 验证 UE 5.7 资产解析：
       ```bash
       python -c "
       from uasset_read import parse_uasset
       import json
       result = parse_uasset('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/Blueprints/BP_FirstPersonCharacter.uasset')
       print('Status:', result.status)
       print('Export count:', len(result.exports))
       # 检查属性解析错误
       errors = [e for e in result.exports if any(p.type == 'Error' for p in e.properties)]
       print('Exports with errors:', len(errors))
       if errors:
           for e in errors[:3]:
               print(f'  - {e.object_name}: {[p.value for p in e.properties if p.type == \"Error\"]}')
       "
       ```

    3. 验证蓝图变量提取：
       ```bash
       python -c "
       from uasset_read import parse_uasset
       result = parse_uasset('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/Blueprints/BP_FirstPersonCharacter.uasset')
       if result.blueprint and result.blueprint.variables:
           print('Variables extracted:', len(result.blueprint.variables))
           for v in result.blueprint.variables[:5]:
               print(f'  - {v.name}: {v.type}')
       "
       ```

    预期结果：
    - 测试全部通过
    - status = "success"
    - Export count = 69
    - Exports with errors = 0（目标）
    - Variables extracted > 0
  </how-to-verify>
  <resume-signal>
    输入 "approved" 继续完成 Phase，或描述发现的任何问题
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件数据 → 解析器 | Extensions 字节需在有效范围内 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering | property_extensions | mitigate | FArchive.read_u8 已有边界验证 |
| T-17-06 | Denial of Service | Extensions 读取 | accept | 最多读取 3 bytes，无风险 |
</threat_model>

<verification>
1. Extensions 处理代码添加完成
2. 单元测试添加并通过
3. UE 5.7 资产验证成功
4. 全部测试套件通过
</verification>

<success_criteria>
- read_property_tag() 正确处理 HAS_EXTENSIONS 标志
- PropertyTag dataclass 包含扩展字段
- 单元测试通过
- UE 5.7 资产 BP_FirstPersonCharacter.uasset 解析成功
- 69 个导出对象属性全部可解析（0 错误）
- 蓝图变量正确提取
- 实现 FIX-07（完整修复）
</success_criteria>

<output>
完成后创建 `.planning/phases/17-property-parsing-fix/17-03-SUMMARY.md`
</output>