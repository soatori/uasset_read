---
wave: 2
depends_on:
  - 11-01
files_modified:
  - uasset_read.py
  - tests/test_property_parsing.py
requirements_addressed:
  - EXTR-01
autonomous: true
estimated_time: 45min
---

# Plan 11-02: 增强ObjectProperty解析返回可读对象引用

**Objective:** ObjectProperty解析返回解析后的对象引用信息（类名、对象名、包路径），而非原始int32索引

## Context

现有`parse_object_property()`（line 3126-3142）仅返回原始int32值（FPackageIndex编码）。用户需手动查阅ImportMap/ExportMap才能理解引用对象，不符合EXTR-01"用户可读取组件属性值"的目标。

**Goal:** 增强ObjectProperty返回`{"raw_index": int, "resolved": {"type": "import|export|null", "class_name": "...", "object_name": "..."}}`

## Truths

- PackageIndex类已实现FPackageIndex编码解码（lines 478-507）
- ImportMap/ExportMap包含完整的对象引用信息
- 需保持向后兼容：raw_index字段保留原始值

## Tasks

### Task 1: 新增resolve_package_index函数

<read_first>
- E:/Develop/uasset_read/uasset_read.py (lines 478-507: PackageIndex类)
- E:/Develop/uasset_read/uasset_read.py (lines 524-570: ObjectImport定义)
- E:/Develop/uasset_read/uasset_read.py (lines 670-706: ObjectExport定义)
- E:/Develop/uasset_read/uasset_read.py (lines 3711-3801: parse_properties_from_export传递import_map参数)
</read_first>

<action>
新增`resolve_package_index_to_reference()`函数（约line 508附近，PackageIndex类之后）：

```python
def resolve_package_index_to_reference(
    pkg_idx: PackageIndex,
    import_map: List[ObjectImport],
    export_map: List[ObjectExport],
    name_map: List[str]
) -> Optional[Dict[str, Any]]:
    """
    解析FPackageIndex为可读对象引用信息。
    
    Args:
        pkg_idx: PackageIndex对象（已解码的FPackageIndex）
        import_map: ImportMap列表
        export_map: ExportMap列表
        name_map: NameMap列表（用于解析FName索引）
    
    Returns:
        None if pkg_idx.is_null
        {"type": "import", "class_name": str, "object_name": str, "package": str} if import
        {"type": "export", "class_name": str, "object_name": str} if export
    """
    if pkg_idx.is_null:
        return None
    
    if pkg_idx.is_import:
        imp_idx = pkg_idx.to_import_index()
        if 0 <= imp_idx < len(import_map):
            imp = import_map[imp_idx]
            return {
                "type": "import",
                "class_name": name_map[imp.class_name] if isinstance(imp.class_name, int) else imp.class_name,
                "object_name": name_map[imp.object_name] if isinstance(imp.object_name, int) else imp.object_name,
                "package": name_map[imp.class_package] if isinstance(imp.class_package, int) else imp.class_package
            }
    
    elif pkg_idx.is_export:
        exp_idx = pkg_idx.to_export_index()
        if 0 <= exp_idx < len(export_map):
            exp = export_map[exp_idx]
            # 类名可能需要递归解析class_index
            class_name = _resolve_class_name(exp.class_index, import_map, export_map, name_map)
            return {
                "type": "export",
                "class_name": class_name,
                "object_name": name_map[exp.object_name] if isinstance(exp.object_name, int) else exp.object_name
            }
    
    return None  # 索引越界等异常情况


def _resolve_class_name(
    class_index: int,
    import_map: List[ObjectImport],
    export_map: List[ObjectExport],
    name_map: List[str]
) -> str:
    """递归解析class_index获取类名"""
    if class_index == 0:
        return "None"
    
    pkg_idx = PackageIndex(class_index)
    resolved = resolve_package_index_to_reference(pkg_idx, import_map, export_map, name_map)
    if resolved:
        return resolved.get("class_name", "Unknown")
    return "Unknown"
```

位置建议：在PackageIndex类定义之后（line ~508），与其他辅助函数一起。
</action>

<acceptance_criteria>
- uasset_read.py包含resolve_package_index_to_reference()函数
- 函数处理null、import、export三种情况
- import返回包含class_name、object_name、package字段
- export返回包含class_name、object_name字段
- 边界检查防止索引越界
</acceptance_criteria>

### Task 2: 修改parse_object_property返回增强值

<read_first>
- E:/Develop/uasset_read/uasset_read.py (lines 3126-3142: parse_object_property现有实现)
- E:/Develop/uasset_read/uasset_read.py (lines 3804-3862: parse_property_value分派逻辑)
- E:/Develop/uasset_read/uasset_read.py (lines 3711-3801: parse_properties_from_export传递参数)
</read_first>

<action>
修改parse_object_property()函数（line ~3126）：

1. 函数签名不变，但内部调用resolve_package_index_to_reference
2. 需要从parse_properties_from_export传递import_map参数
3. 修改parse_property_value()的调用以传递import_map

**方案A（推荐）：在parse_properties_from_export中解析**
- parse_object_property保持返回int32
- 在parse_properties_from_export调用后，对ObjectProperty类型值调用resolve_package_index_to_reference
- 优点：不改变现有解析器签名，向后兼容

**方案B：修改parse_object_property签名**
- 添加import_map参数到parse_object_property
- 直接返回解析后的Dict
- 缺点：需修改parse_property_value分派逻辑

选择方案A，在parse_properties_from_export中后处理：

修改parse_properties_from_export()（line ~3730附近PropertyValue.append后）：
```python
# 增强ObjectProperty解析
if tag.type == "ObjectProperty" and isinstance(value, int):
    pkg_idx = PackageIndex(value)
    resolved = resolve_package_index_to_reference(pkg_idx, import_map, export_map, name_map)
    value = {"raw_index": value, "resolved": resolved}
```
</action>

<acceptance_criteria>
- ObjectProperty类型PropertyValue.value返回Dict格式
- Dict包含raw_index（int）和resolved（Dict|None）字段
- resolved包含type、class_name、object_name字段
- 现有parse_object_property()函数签名不变
- parse_properties_from_export()包含ObjectProperty后处理逻辑
</acceptance_criteria>

### Task 3: 新增ObjectProperty增强解析测试

<read_first>
- E:/Develop/uasset_read/tests/test_property_parsing.py (现有测试模式)
- E:/Develop/uasset_read/tests/test_blueprint_parsing.py (测试资产路径)
- E:/Develop/uasset_read/.planning/phases/11-exportmap-property-extraction/11-RESEARCH.md (PackageIndex解析逻辑)
</read_first>

<action>
在tests/test_property_parsing.py中新增测试：

1. test_object_property_resolved_import():
   - 创建MockArchive返回import索引（负数如-5）
   - 创建MockImportMap包含对应条目
   - 验证resolve_package_index_to_reference返回正确import信息
   - 验证class_name、object_name、package字段正确

2. test_object_property_resolved_export():
   - 创建MockArchive返回export索引（正数如3）
   - 创建MockExportMap包含对应条目
   - 验证resolve_package_index_to_reference返回正确export信息
   - 验证class_name、object_name字段正确

3. test_object_property_null_reference():
   - 验证索引0返回resolved=None

4. test_object_property_in_exportmap():
   - 使用实际FirstPerson资产
   - 验证export.properties中ObjectProperty类型值包含resolved字段
</action>

<acceptance_criteria>
- tests/test_property_parsing.py包含test_object_property_resolved_import()
- tests/test_property_parsing.py包含test_object_property_resolved_export()
- tests/test_property_parsing.py包含test_object_property_null_reference()
- tests/test_property_parsing.py包含test_object_property_in_exportmap()
- pytest tests/test_property_parsing.py::test_object_property_* -v 全部通过
</acceptance_criteria>

## Verification

**Manual verification:**
```bash
python -c "
from uasset_read import parse_uasset
r = parse_uasset('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/Blueprints/BP_FirstPersonCharacter.uasset')
for exp in r.export_map:
    for prop in exp.properties:
        if prop.type == 'ObjectProperty':
            print(f'{exp.object_name}.{prop.name}: {prop.value}')
"
# 期望输出：{"raw_index": N, "resolved": {"type": "...", "class_name": "...", ...}}
```

**Automated verification:**
```bash
pytest tests/test_property_parsing.py::test_object_property_* -x -v
```

## Must-haves

- [ ] ObjectProperty返回解析后引用（EXTR-01成功标准1）
- [ ] 类名/对象名可读（EXTR-01成功标准2）
- [ ] 向后兼容（raw_index保留）

---

## Summary

**Wave 2** — 增强ObjectProperty解析
- Plan 11-02: 新增resolve_package_index_to_reference()函数
- 修改parse_properties_from_export()后处理ObjectProperty值
- 测试验证import/export/null三种情况

**Files:** 2 (uasset_read.py修改，test_property_parsing.py新增测试)
**Time:** ~45min
**Risk:** MEDIUM（需修改现有解析流程，但保持向后兼容）
**Dependencies:** Plan 11-01（ExportMap属性已填充才能测试）