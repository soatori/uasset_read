---
phase: 09-advanced-properties
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
- uasset_read.py
autonomous: true
requirements: [ADVP-01, ADVP-02, ADVP-03, ADVP-04, ADVP-05, ADVP-06]
user_setup: []

must_haves:
  truths:
    - "AdvancedPropertyValue 基类定义，包含 property_type 字段（D-07a）"
    - "六种高级属性 dataclass 定义：StructValue, MapValue, SetValue, EnumValue, TextValue, DelegateValue"
    - "parse_property_value() type_dispatch 扩展包含六种高级属性处理器"
    - "版本检查模式复用 Phase 2 D-05/D-06 模式（D-08）"
  artifacts:
    - path: "uasset_read.py"
      provides: "AdvancedPropertyValue 基类 + 六种高级属性 dataclass"
      contains: "class AdvancedPropertyValue:"
      min_lines: 80
    - path: "uasset_read.py"
      provides: "type_dispatch 扩展"
      contains: "StructProperty"
      pattern: "type_dispatch.*StructProperty"
  key_links:
    - from: "AdvancedPropertyValue"
      to: "StructValue, MapValue, SetValue, EnumValue, TextValue, DelegateValue"
      via: "继承关系"
      pattern: "class.*Value.*AdvancedPropertyValue"
    - from: "parse_property_value()"
      to: "高级属性解析函数"
      via: "type_dispatch 字典"
      pattern: "type_dispatch\\[.*Property\\]"
---

# Phase 9 Plan 01: 高级属性数据类定义 + type_dispatch 扩展

<objective>
定义高级属性类型的数据模型基础，扩展 parse_property_value() type_dispatch 字典，为 Wave 2 的解析函数实现奠定架构。

Purpose: 实现 D-07a 统一基类设计，定义六种高级属性 dataclass，扩展 type_dispatch 入口点。
Output: AdvancedPropertyValue 基类 + 六种高级属性 dataclass + type_dispatch 扩展
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-advanced-properties/09-CONTEXT.md
@.planning/phases/09-advanced-properties/09-RESEARCH.md

# 现有代码关键接口
From uasset_read.py L698-726:
- PropertyTag dataclass 包含 name, type, size, array_index, flags, property_guid, bool_val
- 用于高级属性类型识别

From uasset_read.py L716-726:
- PropertyValue dataclass 包含 name, type, value, array_index
- 高级属性值将替换 value 字段（D-11）

From uasset_read.py L3034-3054:
- parse_property_value() type_dispatch 字典模式
- 扩展点：添加高级属性处理器

From uasset_read.py L2650-2718:
- read_property_tag() 版本检查模式（Phase 2 D-05/D-06）
- 复用于高级属性版本感知解析（D-08）
</context>

<interfaces>
<!-- Executor 需使用的现有代码接口 -->

From uasset_read.py:
```python
# PropertyTag（Phase 2 已实现）
class PropertyTag:
    name: str              # 属性名称
    type: str              # 类型名称（UE5 完整 TypeName）
    size: int              # 序列化大小
    array_index: int = 0
    flags: int = 0
    property_guid: Optional[bytes] = None
    bool_val: int = 0

# PropertyValue（Phase 2 已实现）
class PropertyValue:
    name: str
    type: str
    value: Any             # 高级属性将替换为 dataclass
    array_index: int = 0

# type_dispatch 模式（Phase 2 L3034-3054）
type_dispatch = {
    "BoolProperty": lambda t, a, n, e: parse_bool_property(t, a),
    "IntProperty": lambda t, a, n, e: parse_int_property(t, a),
    # ... 其他基本类型
    # Phase 9 扩展点：添加高级属性处理器
}

# 版本检查函数（Phase 2 L2650-2655）
def use_complete_type_name(legacy_version: int, ue5_version: int) -> bool:
    """UE5 >= 1000 使用完整 TypeName 格式"""
    if legacy_version <= -8 and ue5_version >= PROPERTY_TAG_COMPLETE_TYPE_NAME:
        return True
    return False
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 定义 AdvancedPropertyValue 基类（D-07a）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L123-155（ErrorContext dataclass 参考）
    - uasset_read.py L698-726（PropertyTag dataclass 参考）
    - uasset_read.py L716-726（PropertyValue dataclass 参考）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L68-70（D-07a 定义）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L313-360（dataclass 设计示例）
  </read_first>
  <acceptance_criteria>
    - grep -c "class AdvancedPropertyValue:" uasset_read.py == 1
    - grep "property_type: str" uasset_read.py | grep -c "AdvancedPropertyValue" >= 1
    - python -c "from uasset_read import AdvancedPropertyValue; print('Import OK')"
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中定义 AdvancedPropertyValue 基类（Phase 9 新增 section，位于 PropertyValue 之后）：

    **AdvancedPropertyValue dataclass**（D-07a）：
    ```python
    @dataclass
    class AdvancedPropertyValue:
        """
        高级属性值基类（D-07a）。

        统一基类设计，包含 property_type 字段用于类型识别。
        所有高级属性 dataclass 继承此基类。

        来自 CONTEXT.md D-07a。
        """
        property_type: str  # 属性类型名（如 "StructProperty", "MapProperty"）
    ```

    位置：在 PropertyValue dataclass 之后、BlueprintVariable 之前（约 L728 附近）。
    添加到 __all__ 导出列表（Phase 9 section）。
  </action>
  <verify>
    <automated>python -c "from uasset_read import AdvancedPropertyValue; from dataclasses import fields; print([f.name for f in fields(AdvancedPropertyValue)])"</automated>
  </verify>
  <done>
    - AdvancedPropertyValue 基类定义完成
    - 包含 property_type: str 字段
    - 可导入且字段结构正确
  </done>
</task>

<task type="auto">
  <name>Task 2: 定义六种高级属性 dataclass（D-01a~D-06a）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L123-155（dataclass 模式参考）
    - uasset_read.py L716-726（PropertyValue 模式参考）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L35-66（D-01a~D-06a 定义）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L313-483（dataclass 示例）
  </read_first>
  <acceptance_criteria>
    - grep -c "class StructValue:" uasset_read.py == 1
    - grep -c "class MapValue:" uasset_read.py == 1
    - grep -c "class SetValue:" uasset_read.py == 1
    - grep -c "class EnumValue:" uasset_read.py == 1
    - grep -c "class TextValue:" uasset_read.py == 1
    - grep -c "class DelegateValue:" uasset_read.py == 1
    - grep "AdvancedPropertyValue" uasset_read.py | grep -c "class.*Value" >= 6
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中定义六种高级属性 dataclass（继承 AdvancedPropertyValue）：

    1. **StructValue dataclass**（D-01a, ADVP-01）：
       ```python
       @dataclass
       class StructValue(AdvancedPropertyValue):
           """
           StructProperty 值容器（D-01a）。

           格式：{struct_type: str, fields: dict}
           嵌套结构体解析，递归深度限制 5（D-01）。

           来自 PropertyStruct.cpp §167-172。
           """
           struct_type: str              # 结构体类型名
           fields: Dict[str, Any]        # 嵌套字段（递归解析）
       ```

    2. **MapValue dataclass**（D-02a, ADVP-02）：
       ```python
       @dataclass
       class MapValue(AdvancedPropertyValue):
           """
           MapProperty 值容器（D-02a）。

           格式：{key_type: str, value_type: str, entries: List[{key: Any, value: Any}]}
           支持基本类型、枚举、Struct、Object 键（D-02）。

           来自 PropertyMap.cpp §267-880。
           """
           key_type: str                 # 键类型名
           value_type: str               # 值类型名
           entries: List[Dict[str, Any]] # 键值对列表
       ```

    3. **SetValue dataclass**（D-03a, ADVP-03）：
       ```python
       @dataclass
       class SetValue(AdvancedPropertyValue):
           """
           SetProperty 值容器（D-03a）。

           格式：{element_type: str, elements: List[Any]}
           解析为 List，不验证唯一性（D-03）。

           来自 PropertySet.cpp §221-427。
           """
           element_type: str             # 元素类型名
           elements: List[Any]           # 元素列表
       ```

    4. **EnumValue dataclass**（D-04a, ADVP-04）：
       ```python
       @dataclass
       class EnumValue(AdvancedPropertyValue):
           """
           EnumProperty 值容器（D-04a）。

           格式：{enum_type: str, value_name: str}
           返回枚举值名（如 'EWalletState::Active'）（D-04）。

           来自 EnumProperty.cpp §279-353。
           """
           enum_type: str                # 枚举类型名
           value_name: str               # 枚举值名（包含类型前缀）
       ```

    5. **TextValue dataclass**（D-05a, ADVP-05）：
       ```python
       @dataclass
       class TextValue(AdvancedPropertyValue):
           """
           TextProperty 值容器（D-05a）。

           格式：{namespace: str, key: str, source_string: str}
           完整 FText 结构返回（D-05）。

           来自 TextProperty.cpp §135-139。
           """
           namespace: str                # 本地化命名空间
           key: str                      # 本地化键
           source_string: str            # 源字符串
       ```

    6. **DelegateValue dataclass**（D-06a, ADVP-06）：
       ```python
       @dataclass
       class DelegateValue(AdvancedPropertyValue):
           """
           DelegateProperty 值容器（D-06a）。

           格式：{object_ref: int, function_name: str}
           原始引用格式，延迟解析（D-06b）。

           来自 PropertyDelegate.cpp §86-89。
           """
           object_ref: int               # FPackageIndex 原始值
           function_name: str            # 函数名（FName）
       ```

    所有 dataclass 继承 AdvancedPropertyValue，property_type 字段在基类中定义。
    位置：在 AdvancedPropertyValue 之后，BlueprintVariable 之前。
    添加到 __all__ 导出列表。
  </action>
  <verify>
    <automated>python -c "from uasset_read import StructValue, MapValue, SetValue, EnumValue, TextValue, DelegateValue; print('All imports OK')" && python -c "from uasset_read import StructValue; from dataclasses import fields; print([f.name for f in fields(StructValue)])"</automated>
  </verify>
  <done>
    - 六种高级属性 dataclass 定义完成
    - 所有类继承 AdvancedPropertyValue
    - 字段结构符合 D-01a~D-06a 定义
    - 所有类可导入
  </done>
</task>

<task type="auto">
  <name>Task 3: 扩展 parse_property_value() type_dispatch（D-08）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L3034-3054（parse_property_value 函数）
    - uasset_read.py L2650-2718（read_property_tag 版本检查）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L73-76（D-08 版本支持）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L130-149（架构图）
  </read_first>
  <acceptance_criteria>
    - grep "type_dispatch" uasset_read.py | grep -c "StructProperty" >= 1
    - grep "type_dispatch" uasset_read.py | grep -c "MapProperty" >= 1
    - grep "type_dispatch" uasset_read.py | grep -c "SetProperty" >= 1
    - grep "type_dispatch" uasset_read.py | grep -c "EnumProperty" >= 1
    - grep "type_dispatch" uasset_read.py | grep -c "TextProperty" >= 1
    - grep "type_dispatch" uasset_read.py | grep -c "DelegateProperty" >= 1
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中扩展 parse_property_value() type_dispatch 字典：

    扩展 type_dispatch（L3034-3054）：
    ```python
    type_dispatch = {
        # ... Phase 2 基本类型 ...

        # Phase 9 高级属性（Wave 2 实现函数）
        "StructProperty": lambda t, a, n, e, s, d: parse_struct_property(t, a, n, e, s, d),
        "MapProperty": lambda t, a, n, e, s: parse_map_property(t, a, n, e, s),
        "SetProperty": lambda t, a, n, e, s: parse_set_property(t, a, n, e, s),
        "EnumProperty": lambda t, a, n, s: parse_enum_property(t, a, n, s),
        "TextProperty": lambda t, a: parse_text_property(t, a),
        "DelegateProperty": lambda t, a, n: parse_delegate_property(t, a, n),
    }
    ```

    注意：
    - 高级属性解析函数需要额外参数（summary, depth）
    - Lambda 需要调整参数传递
    - Wave 2 实现具体函数，Wave 1 仅添加入口点

    参数传递调整：
    - parse_property_value() 需扩展参数列表：添加 summary: PackageFileSummary, depth: int = 0
    - 高级属性 lambda 传递 summary 和 depth
    - 基本属性 lambda 保持原参数（忽略 summary/depth）

    版本检查模式（D-08）：
    - 复用 use_complete_type_name() 函数（Phase 2 L2650-2655）
    - UE5 >= 1000 使用完整 TypeName 格式
    - UE4 使用旧格式分离字段
  </action>
  <verify>
    <automated>grep -A 20 "type_dispatch = {" uasset_read.py | grep -c "Property" >= 12</automated>
  </verify>
  <done>
    - type_dispatch 字典扩展完成
    - 包含六种高级属性处理器入口
    - 参数传递模式正确（summary, depth）
    - 版本检查模式复用 Phase 2
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建高级属性解析函数占位符</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L2721-2900（Phase 2 属性解析函数模式）
    - uasset_read.py L2844-2899（parse_array_property 递归模式）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L151-259（解析模式示例）
  </read_first>
  <acceptance_criteria>
    - grep -c "def parse_struct_property" uasset_read.py == 1
    - grep -c "def parse_map_property" uasset_read.py == 1
    - grep -c "def parse_set_property" uasset_read.py == 1
    - grep -c "def parse_enum_property" uasset_read.py == 1
    - grep -c "def parse_text_property" uasset_read.py == 1
    - grep -c "def parse_delegate_property" uasset_read.py == 1
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中创建六种高级属性解析函数占位符（Wave 2 完整实现）：

    占位符函数签名（Wave 2 实现完整逻辑）：
    ```python
    # Phase 9 高级属性解析函数（Wave 1 占位符）

    def parse_struct_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary,
        depth: int = 0
    ) -> StructValue:
        """解析 StructProperty（ADVP-01）。Wave 2 完整实现。"""
        # Wave 2 实现：PropertyTag 循环递归解析
        raise NotImplementedError("StructProperty parsing - Wave 2 implementation")

    def parse_map_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary
    ) -> MapValue:
        """解析 MapProperty（ADVP-02）。Wave 2 完整实现。"""
        # Wave 2 实现：NumEntries + Key/Value pairs
        raise NotImplementedError("MapProperty parsing - Wave 2 implementation")

    def parse_set_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary
    ) -> SetValue:
        """解析 SetProperty（ADVP-03）。Wave 2 完整实现。"""
        # Wave 2 实现：NumElements + 元素循环
        raise NotImplementedError("SetProperty parsing - Wave 2 implementation")

    def parse_enum_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        summary: PackageFileSummary
    ) -> EnumValue:
        """解析 EnumProperty（ADVP-04）。Wave 2 完整实现。"""
        # Wave 2 实现：FName EnumValueName 序列化
        raise NotImplementedError("EnumProperty parsing - Wave 2 implementation")

    def parse_text_property(
        tag: PropertyTag,
        archive: FArchive
    ) -> TextValue:
        """解析 TextProperty（ADVP-05）。Wave 2 完整实现。"""
        # Wave 2 实现：Flags + Namespace + Key + SourceString
        raise NotImplementedError("TextProperty parsing - Wave 2 implementation")

    def parse_delegate_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str]
    ) -> DelegateValue:
        """解析 DelegateProperty（ADVP-06）。Wave 2 完整实现。"""
        # Wave 2 实现：ObjectRef + FunctionName
        raise NotImplementedError("DelegateProperty parsing - Wave 2 implementation")
    ```

    位置：在 Phase 2 属性解析函数之后（约 L2900+）。
    添加到 __all__ 导出列表。

    注意：Wave 1 仅创建占位符，Wave 2 完整实现解析逻辑。
  </action>
  <verify>
    <automated>python -c "from uasset_read import parse_struct_property, parse_map_property, parse_set_property, parse_enum_property, parse_text_property, parse_delegate_property; print('All function imports OK')" && grep -c "NotImplementedError" uasset_read.py >= 6</automated>
  </verify>
  <done>
    - 六种高级属性解析函数占位符创建完成
    - 函数签名正确（包含 summary, depth 参数）
    - 所有函数可导入
    - 占位符使用 NotImplementedError
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PropertyTag → 高级属性解析 | TypeName 参数解析可能返回异常类型名 |
| type_dispatch → 解析函数 | lambda 参数传递需正确匹配函数签名 |
| AdvancedPropertyValue 基类 | 用户代码访问新基类 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | type_dispatch lambda | mitigate | 参数数量验证，匹配函数签名 |
| T-09-01-02 | Tampering | StructValue.fields | accept | D-01 深度限制，Wave 2 实现 |
| T-09-01-03 | Tampering | MapValue.entries | accept | D-02 键类型分派，Wave 2 实现 |
</threat_model>

<verification>
- 数据类定义验证：导入测试
- type_dispatch 扩展验证：grep 检查
- 函数占位符验证：导入测试
- 回归测试：现有测试套件通过
</verification>

<success_criteria>
- AdvancedPropertyValue 基类定义完成，包含 property_type 字段
- 六种高级属性 dataclass 定义完成，继承基类
- type_dispatch 字典扩展包含六种高级属性处理器
- 六种解析函数占位符创建完成
- 所有新类和函数可导入
- 现有测试无回归
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-properties/09-01-SUMMARY.md`
</output>