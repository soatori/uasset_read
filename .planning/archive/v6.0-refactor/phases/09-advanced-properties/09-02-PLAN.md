---
phase: 09-advanced-properties
plan: 02
type: execute
wave: 2
depends_on: [09-01-PLAN.md]
files_modified:
- uasset_read.py
autonomous: true
requirements: [ADVP-01, ADVP-02, ADVP-03, ADVP-04, ADVP-05, ADVP-06]
user_setup: []

must_haves:
  truths:
    - "StructProperty 递归解析，深度限制 5，返回 StructValue dataclass（ADVP-01, D-01）"
    - "MapProperty 全键类型支持，返回 MapValue dataclass（ADVP-02, D-02）"
    - "SetProperty 解析为 List，返回 SetValue dataclass（ADVP-03, D-03）"
    - "EnumProperty 返回枚举值名，返回 EnumValue dataclass（ADVP-04, D-04）"
    - "TextProperty 完整结构返回，返回 TextValue dataclass（ADVP-05, D-05）"
    - "DelegateProperty 原始引用格式，返回 DelegateValue dataclass（ADVP-06, D-06）"
  artifacts:
    - path: "uasset_read.py"
      provides: "六种高级属性解析函数完整实现"
      contains: "def parse_struct_property"
      exports: ["parse_struct_property", "parse_map_property", "parse_set_property", "parse_enum_property", "parse_text_property", "parse_delegate_property"]
    - path: "uasset_read.py"
      provides: "TypeName 参数解析辅助函数"
      contains: "_extract_.*_from_tag"
      pattern: "_extract.*_from_tag"
  key_links:
    - from: "parse_struct_property()"
      to: "PropertyTag 循环"
      via: "read_property_tag() 递归调用"
      pattern: "read_property_tag.*depth"
    - from: "parse_map_property()"
      to: "键类型分派"
      via: "_dispatch_key_parse()"
      pattern: "_dispatch.*_parse"
---

# Phase 9 Plan 02: 高级属性解析函数实现

<objective>
实现六种高级属性类型的完整解析逻辑，替换 Wave 1 的占位符函数，实现 ADVP-01~ADVP-06 所有需求。

Purpose: 根据 UE 源码 SerializeItem 模式实现高级属性解析，支持 UE4/UE5 双版本格式。
Output: 六种高级属性解析函数完整实现 + TypeName 参数解析辅助函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-advanced-properties/09-CONTEXT.md
@.planning/phases/09-advanced-properties/09-RESEARCH.md

# Wave 1 交付成果
From 09-01-PLAN.md:
- AdvancedPropertyValue 基类已定义
- 六种高级属性 dataclass 已定义
- parse_property_value() type_dispatch 已扩展
- 六种解析函数占位符已创建

# 现有代码关键接口
From uasset_read.py L2657-2718:
- read_property_tag() 版本感知解析（Phase 2 D-05/D-06）
- 复用于 StructProperty 递归解析

From uasset_read.py L2844-2899:
- parse_array_property() 嵌套模式（MAX_DEPTH = 10）
- 参考用于 StructProperty 深度限制（改为 5）

From uasset_read.py L3034-3054:
- type_dispatch 字典模式
- 高级属性 lambda 已添加（Wave 1）
</context>

<interfaces>
<!-- Wave 1 交付的接口 -->

From uasset_read.py (Wave 1):
```python
# AdvancedPropertyValue 基类（Wave 1）
class AdvancedPropertyValue:
    property_type: str

# 高级属性 dataclass（Wave 1）
class StructValue(AdvancedPropertyValue):
    struct_type: str
    fields: Dict[str, Any]

class MapValue(AdvancedPropertyValue):
    key_type: str
    value_type: str
    entries: List[Dict[str, Any]]

class SetValue(AdvancedPropertyValue):
    element_type: str
    elements: List[Any]

class EnumValue(AdvancedPropertyValue):
    enum_type: str
    value_name: str

class TextValue(AdvancedPropertyValue):
    namespace: str
    key: str
    source_string: str

class DelegateValue(AdvancedPropertyValue):
    object_ref: int
    function_name: str

# 占位符函数（Wave 1，Wave 2 替换）
def parse_struct_property(...) -> StructValue:
    raise NotImplementedError(...)
# ... 其他占位符
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现 StructProperty 解析函数（ADVP-01, D-01）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L2844-2899（parse_array_property 递归模式）
    - uasset_read.py L2657-2718（read_property_tag 版本感知）
    - uasset_read.py L3034-3054（parse_property_value type_dispatch）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L151-189（StructProperty 解析模式）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L35-38（D-01 递归深度限制）
    - E:/Develop/uasset_read/UnrealEngine/Engine/Source/Runtime/CoreUObject/Private/UObject/PropertyStruct.cpp（UE 源码参考）
  </read_first>
  <acceptance_criteria>
    - grep -A 30 "def parse_struct_property" uasset_read.py | grep -c "MAX_DEPTH" >= 1
    - grep -A 30 "def parse_struct_property" uasset_read.py | grep -c "read_property_tag" >= 1
    - grep -A 30 "def parse_struct_property" uasset_read.py | grep -c "parse_property_value" >= 1
    - grep -A 30 "def parse_struct_property" uasset_read.py | grep -c "StructValue" >= 1
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中替换 parse_struct_property 占位符，实现完整解析逻辑（ADVP-01）：

    实现逻辑（来自 PropertyStruct.cpp §167-172, RESEARCH.md L151-189）：
    ```python
    def parse_struct_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary,
        depth: int = 0
    ) -> StructValue:
        """
        解析 StructProperty（ADVP-01）。

        递归 PropertyTag 循环解析内部字段。
        最大递归深度 5（D-01）。

        来自 PropertyStruct.cpp §167-172。
        """
        MAX_DEPTH = 5  # D-01 深度限制（不同于 ArrayProperty 的 10）

        if depth > MAX_DEPTH:
            raise ParseError(
                f"StructProperty nesting depth {depth} exceeds maximum {MAX_DEPTH}"
            )

        # 提取结构体类型名（UE5 格式）
        # UE5: tag.type = "StructProperty(/Script/CoreUObject.Vector)"
        struct_type = _extract_struct_type_from_tag(tag)

        fields: Dict[str, Any] = {}
        property_count = 0

        # PropertyTag 循环（直到 Name == "None"）
        while property_count < MAX_PROPERTY_COUNT:
            property_count += 1

            inner_tag = read_property_tag(
                archive, name_map,
                summary.legacy_file_version,
                summary.file_version_ue5
            )

            if inner_tag.name == "None":
                break

            # 递归解析字段值（depth + 1）
            field_value = parse_property_value(
                inner_tag, archive, name_map, export_map,
                summary, depth + 1
            )
            fields[inner_tag.name] = field_value

        return StructValue(
            property_type="StructProperty",
            struct_type=struct_type,
            fields=fields
        )
    ```

    关键点：
    - MAX_DEPTH = 5（D-01，不同于 Phase 2 的 10）
    - PropertyTag 循环直到 Name == "None"
    - 递归调用 parse_property_value（depth + 1）
    - 返回 StructValue dataclass（Wave 1 定义）
  </action>
  <verify>
    <automated>grep -A 40 "def parse_struct_property" uasset_read.py | grep -c "MAX_DEPTH = 5" >= 1 && grep -A 40 "def parse_struct_property" uasset_read.py | grep -c "return StructValue" >= 1</automated>
  </verify>
  <done>
    - parse_struct_property 完整实现完成
    - 递归深度限制 5 正确设置
    - PropertyTag 循环模式正确
    - 返回 StructValue dataclass
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 MapProperty 解析函数（ADVP-02, D-02）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L3034-3054（parse_property_value type_dispatch）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L191-226（MapProperty 解析模式）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L40-44（D-02 全键类型支持）
    - E:/Develop/uasset_read/UnrealEngine/Engine/Source/Runtime/CoreUObject/Private/UObject/PropertyMap.cpp（UE 源码参考）
  </read_first>
  <acceptance_criteria>
    - grep -A 30 "def parse_map_property" uasset_read.py | grep -c "read_i32" >= 1
    - grep -A 30 "def parse_map_property" uasset_read.py | grep -c "num_entries" >= 1
    - grep -A 30 "def parse_map_property" uasset_read.py | grep -c "MapValue" >= 1
    - grep -c "_dispatch_key_parse\|_dispatch_value_parse" uasset_read.py >= 2
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中替换 parse_map_property 占位符，实现完整解析逻辑（ADVP-02）：

    实现逻辑（来自 PropertyMap.cpp §267-880, RESEARCH.md L191-226）：
    ```python
    def parse_map_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary
    ) -> MapValue:
        """
        解析 MapProperty（ADVP-02）。

        支持基本类型、枚举、Struct、Object 键（D-02）。

        来自 PropertyMap.cpp §267-880。
        """
        # 提取 Key/Value 类型（UE5 格式）
        # UE5: tag.type = "MapProperty(IntProperty,StrProperty)"
        key_type, value_type = _extract_map_types_from_tag(tag)

        # 简化格式：NumEntries + Key/Value pairs
        num_entries = archive.read_i32()
        entries: List[Dict[str, Any]] = []

        for _ in range(num_entries):
            # D-02b 键解析分派
            key = _dispatch_key_parse(key_type, archive, name_map, export_map, summary)
            value = _dispatch_value_parse(value_type, archive, name_map, export_map, summary)
            entries.append({"key": key, "value": value})

        return MapValue(
            property_type="MapProperty",
            key_type=key_type,
            value_type=value_type,
            entries=entries
        )

    def _dispatch_key_parse(
        key_type: str,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary
    ) -> Any:
        """
        键类型分派解析（D-02b）。

        支持：基本类型、枚举、Struct、Object。
        """
        # 基本类型分派
        if key_type in ["IntProperty", "Int64Property", "FloatProperty", "DoubleProperty", "StrProperty", "NameProperty", "BoolProperty"]:
            dummy_tag = PropertyTag(name="Key", type=key_type, size=0)
            return parse_property_value(dummy_tag, archive, name_map, export_map, summary, depth=0)

        # ObjectProperty 键
        if key_type == "ObjectProperty":
            return archive.read_i32()  # FPackageIndex

        # EnumProperty 键
        if key_type == "EnumProperty":
            return archive.read_name(name_map)  # FName

        # StructProperty 键（简化处理）
        # 完整实现需要 PropertyTag 循环
        return None  # D-02b: 简化处理，返回 None

    def _dispatch_value_parse(
        value_type: str,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary
    ) -> Any:
        """
        值类型分派解析。

        复用 parse_property_value type_dispatch。
        """
        dummy_tag = PropertyTag(name="Value", type=value_type, size=0)
        return parse_property_value(dummy_tag, archive, name_map, export_map, summary, depth=0)
    ```

    关键点：
    - NumEntries + Key/Value pairs 循环
    - 键类型分派复用 parse_property_value 模式
    - 返回 MapValue dataclass（Wave 1 定义）
  </action>
  <verify>
    <automated>grep -A 40 "def parse_map_property" uasset_read.py | grep -c "num_entries = archive.read_i32" >= 1 && grep -A 40 "def parse_map_property" uasset_read.py | grep -c "return MapValue" >= 1</automated>
  </verify>
  <done>
    - parse_map_property 完整实现完成
    - NumEntries + Key/Value pairs 模式正确
    - 键类型分派函数创建
    - 返回 MapValue dataclass
  </done>
</task>

<task type="auto">
  <name>Task 3: 实现 SetProperty 解析函数（ADVP-03, D-03）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L2844-2899（parse_array_property 元素循环模式）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L230-259（SetProperty 解析模式）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L46-49（D-03 解析为 List）
    - E:/Develop/uasset_read/UnrealEngine/Engine/Source/Runtime/CoreUObject/Private/UObject/PropertySet.cpp（UE 源码参考）
  </read_first>
  <acceptance_criteria>
    - grep -A 30 "def parse_set_property" uasset_read.py | grep -c "read_i32" >= 1
    - grep -A 30 "def parse_set_property" uasset_read.py | grep -c "num_elements" >= 1
    - grep -A 30 "def parse_set_property" uasset_read.py | grep -c "SetValue" >= 1
    - grep -A 30 "def parse_set_property" uasset_read.py | grep -c "不验证唯一性" >= 1
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中替换 parse_set_property 占位符，实现完整解析逻辑（ADVP-03）：

    实现逻辑（来自 PropertySet.cpp §221-427, RESEARCH.md L230-259）：
    ```python
    def parse_set_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        export_map: List[ObjectExport],
        summary: PackageFileSummary
    ) -> SetValue:
        """
        解析 SetProperty（ADVP-03）。

        解析为 List，不验证唯一性（D-03）。
        格式与 ArrayProperty 相似。

        来自 PropertySet.cpp §221-427。
        """
        # 提取元素类型（UE5 格式）
        # UE5: tag.type = "SetProperty(IntProperty)"
        element_type = _extract_set_type_from_tag(tag)

        # 简化格式：NumElements + 元素循环
        num_elements = archive.read_i32()
        elements: List[Any] = []

        for _ in range(num_elements):
            # 元素解析（复用 type_dispatch）
            dummy_tag = PropertyTag(name="Element", type=element_type, size=0)
            element = parse_property_value(dummy_tag, archive, name_map, export_map, summary, depth=0)
            elements.append(element)

        return SetValue(
            property_type="SetProperty",
            element_type=element_type,
            elements=elements
        )
    ```

    关键点：
    - NumElements + 元素循环（与 ArrayProperty 相似）
    - 不验证唯一性（D-03）
    - 返回 SetValue dataclass（Wave 1 定义）
  </action>
  <verify>
    <automated>grep -A 30 "def parse_set_property" uasset_read.py | grep -c "num_elements = archive.read_i32" >= 1 && grep -A 30 "def parse_set_property" uasset_read.py | grep -c "return SetValue" >= 1</automated>
  </verify>
  <done>
    - parse_set_property 完整实现完成
    - NumElements + 元素循环模式正确
    - 返回 SetValue dataclass
    - 不验证唯一性（D-03）
  </done>
</task>

<task type="auto">
  <name>Task 4: 实现 EnumProperty 解析函数（ADVP-04, D-04）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L209-229（read_name 函数）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L398-423（EnumProperty 解析模式）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L51-54（D-04 返回枚举值名）
    - E:/Develop/uasset_read/UnrealEngine/Engine/Source/Runtime/CoreUObject/Private/UObject/EnumProperty.cpp（UE 源码参考）
  </read_first>
  <acceptance_criteria>
    - grep -A 20 "def parse_enum_property" uasset_read.py | grep -c "read_name" >= 1
    - grep -A 20 "def parse_enum_property" uasset_read.py | grep -c "EnumValue" >= 1
    - grep -A 20 "def parse_enum_property" uasset_read.py | grep -c "::" >= 1
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中替换 parse_enum_property 占位符，实现完整解析逻辑（ADVP-04）：

    实现逻辑（来自 EnumProperty.cpp §279-353, RESEARCH.md L398-423）：
    ```python
    def parse_enum_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str],
        summary: PackageFileSummary
    ) -> EnumValue:
        """
        解析 EnumProperty（ADVP-04）。

        FName EnumValueName 序列化（非整数值）。
        返回枚举值名（如 'EWalletState::Active'）（D-04）。

        来自 EnumProperty.cpp §279-353。
        """
        # 提取枚举类型名（UE5 格式）
        # UE5: tag.type = "EnumProperty(/Script/Game.EWalletState)"
        enum_type = _extract_enum_type_from_tag(tag)

        # SerializeItem: FName EnumValueName
        enum_value_name = archive.read_name(name_map)

        # D-04 返回枚举值名（如 "EWalletState::Active"）
        # 格式：EnumType::ValueName
        value_name = f"{enum_type}::{enum_value_name}"

        return EnumValue(
            property_type="EnumProperty",
            enum_type=enum_type,
            value_name=value_name
        )
    ```

    关键点：
    - FName 序列化（非整数值）
    - 枚举值名格式：EnumType::ValueName（D-04）
    - 返回 EnumValue dataclass（Wave 1 定义）
  </action>
  <verify>
    <automated>grep -A 20 "def parse_enum_property" uasset_read.py | grep -c "archive.read_name" >= 1 && grep -A 20 "def parse_enum_property" uasset_read.py | grep -c "return EnumValue" >= 1</automated>
  </verify>
  <done>
    - parse_enum_property 完整实现完成
    - FName 序列化正确
    - 枚举值名格式正确（EnumType::ValueName）
    - 返回 EnumValue dataclass
  </done>
</task>

<task type="auto">
  <name>Task 5: 实现 TextProperty 解析函数（ADVP-05, D-05）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L178-207（read_fstring 函数）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L425-457（TextProperty 解析模式）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L56-59（D-05 完整结构返回）
    - E:/Develop/uasset_read/UnrealEngine/Engine/Source/Runtime/CoreUObject/Private/UObject/TextProperty.cpp（UE 源码参考）
  </read_first>
  <acceptance_criteria>
    - grep -A 20 "def parse_text_property" uasset_read.py | grep -c "read_i32" >= 1
    - grep -A 20 "def parse_text_property" uasset_read.py | grep -c "read_fstring" >= 3
    - grep -A 20 "def parse_text_property" uasset_read.py | grep -c "TextValue" >= 1
    - grep -A 20 "def parse_text_property" uasset_read.py | grep -c "namespace" >= 1
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中替换 parse_text_property 占位符，实现完整解析逻辑（ADVP-05）：

    实现逻辑（来自 TextProperty.cpp §135-139, RESEARCH.md L425-457）：
    ```python
    def parse_text_property(
        tag: PropertyTag,
        archive: FArchive
    ) -> TextValue:
        """
        解析 TextProperty（ADVP-05）。

        FText 序列化格式：
        - Flags (int32)
        - Namespace (FString)
        - Key (FString)
        - SourceString (FString)

        完整结构返回（D-05）。

        来自 TextProperty.cpp §135-139。
        """
        # FText 序列化格式
        flags = archive.read_i32()
        namespace = archive.read_fstring()
        key = archive.read_fstring()
        source_string = archive.read_fstring()

        # D-05 完整结构返回
        return TextValue(
            property_type="TextProperty",
            namespace=namespace or "",
            key=key or "",
            source_string=source_string or ""
        )
    ```

    关键点：
    - Flags + Namespace + Key + SourceString 四字段
    - 空字段处理：返回 ""（D-05）
    - 返回 TextValue dataclass（Wave 1 定义）
  </action>
  <verify>
    <automated>grep -A 25 "def parse_text_property" uasset_read.py | grep -c "flags = archive.read_i32" >= 1 && grep -A 25 "def parse_text_property" uasset_read.py | grep -c "return TextValue" >= 1</automated>
  </verify>
  <done>
    - parse_text_property 完整实现完成
    - FText 四字段序列化正确
    - 空字段处理正确
    - 返回 TextValue dataclass
  </done>
</task>

<task type="auto">
  <name>Task 6: 实现 DelegateProperty 解析函数（ADVP-06, D-06）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L209-229（read_name 函数）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L459-483（DelegateProperty 解析模式）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L61-66（D-06 原始引用格式）
    - E:/Develop/uasset_read/UnrealEngine/Engine/Source/Runtime/CoreUObject/Private/UObject/PropertyDelegate.cpp（UE 源码参考）
  </read_first>
  <acceptance_criteria>
    - grep -A 15 "def parse_delegate_property" uasset_read.py | grep -c "read_i32" >= 1
    - grep -A 15 "def parse_delegate_property" uasset_read.py | grep -c "read_name" >= 1
    - grep -A 15 "def parse_delegate_property" uasset_read.py | grep -c "DelegateValue" >= 1
    - grep -A 15 "def parse_delegate_property" uasset_read.py | grep -c "object_ref" >= 1
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中替换 parse_delegate_property 占位符，实现完整解析逻辑（ADVP-06）：

    实现逻辑（来自 PropertyDelegate.cpp §86-89, RESEARCH.md L459-483）：
    ```python
    def parse_delegate_property(
        tag: PropertyTag,
        archive: FArchive,
        name_map: List[str]
    ) -> DelegateValue:
        """
        解析 DelegateProperty（ADVP-06）。

        FScriptDelegate 序列化：
        - ObjectRef (FPackageIndex = int32)
        - FunctionName (FName)

        原始引用格式，延迟解析（D-06b）。

        来自 PropertyDelegate.cpp §86-89。
        """
        # FScriptDelegate 序列化
        object_ref = archive.read_i32()  # FPackageIndex 原始值
        function_name = archive.read_name(name_map)

        # D-06b 延迟解析 ObjectRef
        # Phase 10 依赖分析时解析为对象名
        return DelegateValue(
            property_type="DelegateProperty",
            object_ref=object_ref,
            function_name=function_name
        )
    ```

    关键点：
    - FPackageIndex 原始值（不解析）
    - FunctionName FName 解析
    - 延迟解析（D-06b，Phase 10）
    - 返回 DelegateValue dataclass（Wave 1 定义）
  </action>
  <verify>
    <automated>grep -A 20 "def parse_delegate_property" uasset_read.py | grep -c "object_ref = archive.read_i32" >= 1 && grep -A 20 "def parse_delegate_property" uasset_read.py | grep -c "return DelegateValue" >= 1</automated>
  </verify>
  <done>
    - parse_delegate_property 完整实现完成
    - FScriptDelegate 序列化正确
    - ObjectRef 原始值保持（延迟解析）
    - 返回 DelegateValue dataclass
  </done>
</task>

<task type="auto">
  <name>Task 7: 实现 TypeName 参数解析辅助函数（D-08）</name>
  <files>uasset_read.py</files>
  <read_first>
    - uasset_read.py L2650-2718（read_property_tag 版本检查）
    - .planning/phases/09-advanced-properties/09-RESEARCH.md L516-543（TypeName 格式）
    - .planning/phases/09-advanced-properties/09-CONTEXT.md L73-76（D-08 版本支持）
    - E:/Develop/uasset_read/UnrealEngine/Engine/Source/Runtime/CoreUObject/Public/UObject/PropertyTag.h（UE 源码参考）
  </read_first>
  <acceptance_criteria>
    - grep -c "_extract_struct_type_from_tag" uasset_read.py == 1
    - grep -c "_extract_map_types_from_tag" uasset_read.py == 1
    - grep -c "_extract_set_type_from_tag" uasset_read.py == 1
    - grep -c "_extract_enum_type_from_tag" uasset_read.py == 1
    - grep -c "use_complete_type_name" uasset_read.py >= 4
  </acceptance_criteria>
  <action>
    在 uasset_read.py 中创建 TypeName 参数解析辅助函数（D-08）：

    实现逻辑（UE5 PropertyTag.h 完整 TypeName 格式）：
    ```python
    # TypeName 参数解析辅助函数

    def _extract_struct_type_from_tag(tag: PropertyTag) -> str:
        """
        从 PropertyTag 提取结构体类型名（D-08）。

        UE5 格式: "StructProperty(/Script/CoreUObject.Vector)"
        UE4 格式: 使用分离字段（简化处理）。
        """
        type_str = tag.type

        # UE5: 提取括号内参数
        if "(" in type_str:
            # 格式: "StructProperty(/Script/CoreUObject.Vector)"
            # 提取括号内内容
            start = type_str.find("(")
            end = type_str.find(")")
            if start != -1 and end != -1:
                return type_str[start+1:end]

        # UE4: 简化处理，返回类型名
        # 完整实现需要从分离字段获取
        return "UnknownStruct"

    def _extract_map_types_from_tag(tag: PropertyTag) -> Tuple[str, str]:
        """
        从 PropertyTag 提取 Map Key/Value 类型（D-08）。

        UE5 格式: "MapProperty(IntProperty,StrProperty)"
        UE4 格式: 使用分离字段（简化处理）。
        """
        type_str = tag.type

        # UE5: 提取括号内参数
        if "(" in type_str:
            start = type_str.find("(")
            end = type_str.find(")")
            if start != -1 and end != -1:
                params = type_str[start+1:end]
                # 分割 Key,Value
                parts = params.split(",")
                if len(parts) == 2:
                    return parts[0], parts[1]

        # UE4: 简化处理，返回默认类型
        return "IntProperty", "IntProperty"

    def _extract_set_type_from_tag(tag: PropertyTag) -> str:
        """
        从 PropertyTag 提取 Set 元素类型（D-08）。

        UE5 格式: "SetProperty(IntProperty)"
        UE4 格式: 使用分离字段（简化处理）。
        """
        type_str = tag.type

        # UE5: 提取括号内参数
        if "(" in type_str:
            start = type_str.find("(")
            end = type_str.find(")")
            if start != -1 and end != -1:
                return type_str[start+1:end]

        # UE4: 简化处理
        return "IntProperty"

    def _extract_enum_type_from_tag(tag: PropertyTag) -> str:
        """
        从 PropertyTag 提取枚举类型名（D-08）。

        UE5 格式: "EnumProperty(/Script/Game.EWalletState)"
        UE4 格式: 使用分离字段（简化处理）。
        """
        type_str = tag.type

        # UE5: 提取括号内参数
        if "(" in type_str:
            start = type_str.find("(")
            end = type_str.find(")")
            if start != -1 and end != -1:
                enum_path = type_str[start+1:end]
                # 提取类型名（去除路径前缀）
                if "." in enum_path:
                    return enum_path.split(".")[-1]
                return enum_path

        # UE4: 简化处理
        return "UnknownEnum"
    ```

    关键点：
    - UE5 完整 TypeName 格式：PropertyType(Parameter)
    - 参数提取使用括号解析
    - UE4 简化处理（完整实现需分离字段）
    - D-08 版本支持
  </action>
  <verify>
    <automated>grep -c "_extract.*_from_tag" uasset_read.py >= 4 && python -c "from uasset_read import _extract_struct_type_from_tag; print('Helper functions OK')"</automated>
  </verify>
  <done>
    - 四个 TypeName 参数解析辅助函数创建完成
    - UE5 格式解析正确
    - UE4 简化处理实现
    - 所有函数可导入
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeName → 参数解析 | 括号解析可能返回异常类型名 |
| StructProperty → 递归解析 | depth 参数防止无限递归 |
| MapProperty → 键类型分派 | 键类型匹配需正确 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | parse_struct_property | mitigate | D-01 MAX_DEPTH = 5，超过抛出 ParseError |
| T-09-02-02 | Denial of Service | PropertyTag 循环 | mitigate | Phase 2 D-08 MAX_PROPERTY_COUNT = 10_000 |
| T-09-02-03 | Tampering | _extract_*_from_tag | mitigate | 参数格式验证，异常时返回默认值 |
| T-09-02-04 | Tampering | MapProperty 键分派 | mitigate | 键类型验证，未知类型返回 None |
</threat_model>

<verification>
- 解析函数实现验证：grep 检查关键模式
- TypeName 辅助函数验证：导入测试
- 递归深度验证：MAX_DEPTH = 5 检查
- 循环限制验证：MAX_PROPERTY_COUNT 检查
</verification>

<success_criteria>
- 六种高级属性解析函数完整实现完成
- StructProperty 递归解析，深度限制 5
- MapProperty 全键类型支持
- SetProperty 解析为 List
- EnumProperty 返回枚举值名
- TextProperty 完整结构返回
- DelegateProperty 原始引用格式
- TypeName 参数解析辅助函数创建完成
- 所有函数可导入且无 NotImplementedError
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-properties/09-02-SUMMARY.md`
</output>