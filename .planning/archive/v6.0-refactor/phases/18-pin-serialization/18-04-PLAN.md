---
phase: 18-pin-serialization
plan: 04
type: execute
wave: 4
depends_on: [18-01, 18-03]
files_modified: [uasset_read.py]
autonomous: true
requirements: [PIN-02]

must_haves:
  truths:
    - "PinType版本条件检查正确"
    - "ContainerType版本依赖处理"
    - "bIsConst版本依赖处理"
  artifacts:
    - path: "uasset_read.py"
      provides: "版本检查修复"
      contains: "framework_version.*PINS_STORE_FNAME"
  key_links:
    - from: "read_ed_graph_pin_type()"
      to: "CustomVersion"
      via: "版本条件判断"
      pattern: "framework_version.*EdGraphPinContainerType"
---

<objective>
修复 read_ed_graph_pin_type() 版本检查，使其正确处理 FName/FString 格式转换和版本依赖字段。

Purpose: 按UE源码FEdGraphPinType::Serialize实现版本条件序列化。
Output: 版本正确的PinType解析
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-pin-serialization/18-RESEARCH.md

<interfaces>
<!-- UE源码验证的PinType版本检查 -->
From EdGraphPin.cpp L163-346 (FEdGraphPinType::Serialize):
```cpp
// 1-2. PinCategory/PinSubCategory (version dependent)
if (Ar.CustomVer(FFrameworkObjectVersion::GUID) >= FFrameworkObjectVersion::PinsStoreFName)
{
    Ar << PinCategory;    // FName
    Ar << PinSubCategory; // FName
}
else
{
    FString PinCategoryStr; Ar << PinCategoryStr;    // FString
    FString PinSubCategoryStr; Ar << PinSubCategoryStr; // FString
}

// 4-5. ContainerType (version dependent)
if (Ar.CustomVer(FFrameworkObjectVersion::GUID) >= FFrameworkObjectVersion::EdGraphPinContainerType)
{
    Ar << ContainerType;  // uint8
    if (IsMap()) Ar << PinValueType;
}
else
{
    // Legacy bool flags: bIsArray, bIsSet, bIsMap
}
```

From uasset_read.py L2497-2575 (当前实现):
```python
# 当前无版本检查，直接读取FName格式
pin_type.pin_category = archive.read_name(name_map)
pin_type.pin_sub_category = archive.read_name(name_map)
pin_type.container_type = archive.read_u8()  # 无版本检查
```

<!-- CustomVersion常量来自18-01 -->
From 18-01-PLAN Task 1 (CustomVersion常量):
```python
FFRAMEWORK_OBJECT_VERSION_GUID = "CFFC743F-43B04480-939114DF-171D2073"
FRELEASE_OBJECT_VERSION_GUID = "9C54D522-A8264FBE-94210746-61B482D0"
FFRAMEWORK_VERSION_PINS_STORE_FNAME = 20
FFRAMEWORK_VERSION_ED_GRAPH_PIN_CONTAINER_TYPE = 15
FRELEASE_VERSION_PIN_TYPE_UOBJECT_WRAPPER = 10
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 read_ed_graph_pin_type() 版本检查</name>
  <files>uasset_read.py</files>
  <read_first>
    - E:\Develop\uasset_read\uasset_read.py (L2497-2575 当前实现)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-RESEARCH.md (L155-250 PinType序列化)
    - E:\Develop\lib\UnrealEngine\Engine\Source\Runtime\Engine\Private\EdGraph\EdGraphPin.cpp (L163-346)
    - E:\Develop\uasset_read\.planning\phases\18-pin-serialization\18-01-PLAN.md (CustomVersion常量)
  </read_first>
  <action>
    修改 read_ed_graph_pin_type() 函数（L2497-2575），添加版本条件检查：

    ```python
    def read_ed_graph_pin_type(
        archive: FArchive,
        name_map: List[str],
        summary: PackageFileSummary
    ) -> FEdGraphPinType:
        """
        Parse FEdGraphPinType with version checks.

        序列化顺序（UE源码 EdGraphPin.cpp L163-346 验证）：
        1. PinCategory (FName/FString) - 版本依赖
        2. PinSubCategory (FName/FString) - 版本依赖
        3. PinSubCategoryObject (FPackageIndex)
        4. ContainerType (uint8) - 版本依赖
        5. PinValueType (if ContainerType==Map) - 版本依赖
        6. bIsReference (bool)
        7. bIsWeakPointer (bool)
        8. PinSubCategoryMemberReference - 版本依赖
        9. bIsConst (bool) - 版本依赖
        10. bIsUObjectWrapper (bool) - 版本依赖
        """
        pin_type = FEdGraphPinType()

        # 版本获取（使用18-01定义的常量）
        framework_version = summary.custom_version.get(FFRAMEWORK_OBJECT_VERSION_GUID, 0)
        release_version = summary.custom_version.get(FRELEASE_OBJECT_VERSION_GUID, 0)
        ue4_version = summary.file_version_ue4

        # 1-2. PinCategory and PinSubCategory (version dependent)
        if framework_version >= FFRAMEWORK_VERSION_PINS_STORE_FNAME:
            pin_type.pin_category = archive.read_name(name_map)
            pin_type.pin_sub_category = archive.read_name(name_map)
        else:
            # Legacy FString format
            pin_type.pin_category = archive.read_fstring()
            pin_type.pin_sub_category = archive.read_fstring()

        # 3. PinSubCategoryObject (FPackageIndex)
        pin_type.pin_sub_category_object = archive.read_i32()

        # 4-5. ContainerType (version dependent)
        if framework_version >= FFRAMEWORK_VERSION_ED_GRAPH_PIN_CONTAINER_TYPE:
            pin_type.container_type = archive.read_u8()
            if pin_type.container_type == 3:  # Map
                # PinValueType (FEdGraphTerminalType)
                archive.read_name(name_map)  # TerminalCategory
                archive.read_name(name_map)  # TerminalSubCategory
                archive.read_i32()           # TerminalSubCategoryObject
        else:
            # Legacy bool flags
            b_is_array = archive.read_bool()
            b_is_set = archive.read_bool()
            b_is_map = archive.read_bool()
            # Convert to ContainerType
            if b_is_map:
                pin_type.container_type = 3
            elif b_is_set:
                pin_type.container_type = 2
            elif b_is_array:
                pin_type.container_type = 1
            else:
                pin_type.container_type = 0

        # 6-7. bIsReference and bIsWeakPointer
        pin_type.is_reference = archive.read_bool()
        pin_type.is_weak_pointer = archive.read_bool()

        # 8. PinSubCategoryMemberReference (version dependent)
        # VER_UE4_MEMBERREFERENCE_IN_PINTYPE (UE4 version check)
        # 现代资产通常有此字段，简化处理：始终读取
        archive.read_i32()  # MemberParent (FPackageIndex)
        archive.read_name(name_map)  # MemberName
        archive.read(16)  # MemberGuid

        # 9. bIsConst (version dependent)
        # VER_UE4_SERIALIZE_PINTYPE_CONST
        # 现代资产通常有此字段，简化处理：始终读取
        pin_type.is_const = archive.read_bool()

        # 10. bIsUObjectWrapper (version dependent)
        # FReleaseObjectVersion::PinTypeIncludesUObjectWrapperFlag
        # 现代资产通常有此字段，简化处理：始终读取
        pin_type.is_uobject_wrapper = archive.read_bool()

        return pin_type
    ```

    关键变更：
    1. PinCategory/PinSubCategory 版本检查（FName vs FString）
    2. ContainerType 版本检查（uint8 vs legacy bool flags）
    3. Map容器时读取 PinValueType
    4. 使用18-01定义的版本阈值常量
  </action>
  <verify>
    <automated>grep -c "framework_version.*PINS_STORE_FNAME" uasset_read.py >= 1 && grep -c "FFRAMEWORK_VERSION_ED_GRAPH_PIN_CONTAINER_TYPE" uasset_read.py >= 1 && grep -c "b_is_array.*read_bool" uasset_read.py >= 1</automated>
  </verify>
  <done>
    - PinCategory/PinSubCategory版本检查存在
    - ContainerType版本检查存在
    - Legacy bool flags处理存在
    - Map容器PinValueType处理存在
    - 使用18-01定义的CustomVersion常量
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 版本检查 | CustomVersion值决定字段格式 |
| Legacy兼容 | 旧版本资产使用不同格式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering | CustomVersion值 | accept | 值由UE资产决定，解析失败抛出异常 |
</threat_model>

<verification>
- grep验证：版本条件检查存在
- grep验证：Legacy格式处理存在
</verification>

<success_criteria>
- PinCategory/PinSubCategory版本检查（FName vs FString）
- ContainerType版本检查（现代enum vs legacy bool）
- 版本阈值使用18-01定义的常量
</success_criteria>

<output>
完成后创建 `.planning/phases/18-pin-serialization\18-04-SUMMARY.md`
</output>