---
phase: 41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/link/result.py
  - tests/test_link_object_instance.py
  - tests/test_link_result.py
autonomous: true
requirements: [LINK-01, LINK-06]

must_haves:
  truths:
    - "LinkerParseResult dataclass can be instantiated with default values"
    - "UObjectInstance has all 15 fields defined in skeleton"
    - "Test suite runs with 0 failures on new test files"
  artifacts:
    - path: "src/uasset_read/link/result.py"
      provides: "LinkerParseResult dataclass definition"
      contains: "class LinkerParseResult"
    - path: "tests/test_link_object_instance.py"
      provides: "UObjectInstance field and method tests"
      min_lines: 20
    - path: "tests/test_link_result.py"
      provides: "LinkerParseResult structure tests"
      min_lines: 15
  key_links:
    - from: "src/uasset_read/link/result.py"
      to: "src/uasset_read/link/linker.py"
      via: "import type hint"
      pattern: "PackageLinker"
    - from: "tests/test_link_object_instance.py"
      to: "src/uasset_read/link/object_instance.py"
      via: "import UObjectInstance"
      pattern: "from uasset_read.link.object_instance import UObjectInstance"
---

<objective>
创建 LinkerParseResult 结果类、测试脚手架、验证 UObjectInstance 骨架完整性。

Purpose: 为 Phase 41 提供返回值数据结构和测试基础设施，后续 PackageLinker 计划依赖此计划产出的测试框架。
Output: result.py 数据类、两个测试文件、UObjectInstance 字段验证
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-link-module-infrastructure/41-CONTEXT.md
@.planning/phases/41-link-module-infrastructure/41-RESEARCH.md
@.planning/milestones/v7.0-OBJECT-GRAPH.md
@src/uasset_read/models/result.py — ParseResult 模式参考
@src/uasset_read/link/object_instance.py — UObjectInstance 骨架（验证目标）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 LinkerParseResult 数据类</name>
  <files>src/uasset_read/link/result.py</files>
  <read_first>
    - src/uasset_read/models/result.py — ParseResult 模式参考（字段顺序、type hints、dataclass 风格）
    - src/uasset_read/link/object_instance.py — UObjectInstance 类型定义（TYPE_CHECKING 模式）
  </read_first>
  <action>
在 src/uasset_read/link/result.py 中创建 LinkerParseResult dataclass，严格遵循 ParseResult 模式：

1. 文件头：`"""Linker 解析结果数据类 — LinkerParseResult。"""`
2. `from __future__ import annotations`
3. `from dataclasses import dataclass, field`
4. `from typing import Optional, List, TYPE_CHECKING`
5. TYPE_CHECKING 块中导入 `PackageLinker`（from uasset_read.link.linker）和 `UObjectInstance`（from uasset_read.link.object_instance），以及 `PackageFileSummary`、`ObjectImport`、`ObjectExport`（from uasset_read.serializers.object_resources）

字段（按此顺序，使用 field(default_factory=...) 用于可变默认值）：
- `summary: Optional["PackageFileSummary"] = None`
- `name_map: List[str] = field(default_factory=list)`
- `import_map: List["ObjectImport"] = field(default_factory=list)`
- `export_map: List["ObjectExport"] = field(default_factory=list)`
- `linker: Optional["PackageLinker"] = None`
- `root_objects: List["UObjectInstance"] = field(default_factory=list)`
- `all_objects: List["UObjectInstance"] = field(default_factory=list)`
- `errors: List[str] = field(default_factory=list)`
- `is_success: bool = False`
- `mmap_used: bool = False`
- `mmap_warning: Optional[str] = None`

不使用任何方法（纯数据容器），与 ParseResult 的 status property 不同，LinkerParseResult 不需要 computed property。
  </action>
  <verify>
    <automated>python -c "from uasset_read.link.result import LinkerParseResult; r = LinkerParseResult(); assert r.is_success == False; assert r.errors == []; assert r.root_objects == []; assert r.linker is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class LinkerParseResult" src/uasset_read/link/result.py` 返回 1 行
    - `grep -c "= field(default_factory=list)" src/uasset_read/link/result.py` >= 6（name_map, import_map, export_map, root_objects, all_objects, errors）
    - `python -c "from uasset_read.link.result import LinkerParseResult; r = LinkerParseResult(); assert hasattr(r, 'summary'); assert hasattr(r, 'name_map'); assert hasattr(r, 'import_map'); assert hasattr(r, 'export_map'); assert hasattr(r, 'linker'); assert hasattr(r, 'root_objects'); assert hasattr(r, 'all_objects'); assert hasattr(r, 'errors'); assert hasattr(r, 'is_success'); assert hasattr(r, 'mmap_used'); assert hasattr(r, 'mmap_warning'); print('OK')"` 返回 OK
    - `grep "from __future__ import annotations" src/uasset_read/link/result.py` 存在
    - `grep "TYPE_CHECKING" src/uasset_read/link/result.py` 存在
  </acceptance_criteria>
  <done>result.py 存在且包含 LinkerParseResult 类，11 个字段，可实例化，默认值正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建测试脚手架 — UObjectInstance 验证</name>
  <files>tests/test_link_object_instance.py</files>
  <read_first>
    - src/uasset_read/link/object_instance.py — UObjectInstance 完整实现
    - tests/test_uasset_read.py — 现有 pytest 风格和导入约定
  </read_first>
  <behavior>
    - 测试 1: UObjectInstance 可实例化，传入 package_index=1, object_name="Test", object_class="TestClass", class_package="/Script/Engine", outer_index=None, is_import=False
    - 测试 2: is_export 属性返回正确值（is_import=True 时 is_export=False）
    - 测试 3: is_null 属性在 package_index=0 时返回 True
    - 测试 4: get_full_name() 返回 object_name 当无 outer 时
    - 测试 5: __repr__ 包含 "Export" 或 "Import" 关键字
  </behavior>
  <action>
创建 tests/test_link_object_instance.py，验证 UObjectInstance 骨架（LINK-01）：

1. 文件头标准 pytest 格式
2. `from uasset_read.link.object_instance import UObjectInstance`
3. 测试类 `TestUObjectInstance`，包含以下方法：
   - `test_creation`: 创建 export 实例，验证 package_index=1, object_name="Test", object_class="TestClass", is_import=False, is_export=True
   - `test_is_null`: 创建 package_index=0 的实例，验证 is_null == True
   - `test_get_full_name_no_outer`: 创建无 outer 的 export，验证 get_full_name() 返回包含 object_name 的字符串
   - `test_repr_export`: 创建 export 实例，验证 repr 包含 "Export"
   - `test_repr_import`: 创建 import 实例 (is_import=True)，验证 repr 包含 "Import"

所有测试必须使用最小参数构造，不依赖 PackageLinker。
  </action>
  <verify>
    <automated>python -m pytest tests/test_link_object_instance.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class TestUObjectInstance" tests/test_link_object_instance.py` 返回 1 行
    - `grep -c "def test_" tests/test_link_object_instance.py` >= 5
    - `grep "from uasset_read.link.object_instance import UObjectInstance" tests/test_link_object_instance.py` 存在
    - `python -m pytest tests/test_link_object_instance.py -x -v` 全部通过（exit code 0）
  </acceptance_criteria>
  <done>5 个测试全部通过，覆盖 UObjectInstance 的核心字段和行为</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建测试脚手架 — LinkerParseResult 验证</name>
  <files>tests/test_link_result.py</files>
  <read_first>
    - src/uasset_read/link/result.py — LinkerParseResult 定义（Task 1 产出）
    - tests/test_uasset_read.py — 现有测试风格和断言模式
  </read_first>
  <behavior>
    - 测试 1: LinkerParseResult 默认实例的 is_success == False, errors == []
    - 测试 2: 设置 is_success=True 后属性正确反映
    - 测试 3: 所有 list 字段默认值为空列表
  </behavior>
  <action>
创建 tests/test_link_result.py，验证 LinkerParseResult 结构（LINK-06）：

1. `from uasset_read.link.result import LinkerParseResult`
2. 测试类 `TestLinkerParseResult`，包含以下方法：
   - `test_default_values`: 默认实例，验证 is_success=False, errors=[], root_objects=[], all_objects=[], name_map=[], import_map=[], export_map=[], linker=None, summary=None, mmap_used=False, mmap_warning=None
   - `test_success_flag`: 创建后设置 is_success=True，验证为 True
   - `test_errors_append`: 向 errors 添加字符串，验证 errors == ["test error"]
   - `test_all_fields_exist`: 使用反射验证所有预期字段存在（summary, name_map, import_map, export_map, linker, root_objects, all_objects, errors, is_success, mmap_used, mmap_warning）
  </action>
  <verify>
    <automated>python -m pytest tests/test_link_result.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class TestLinkerParseResult" tests/test_link_result.py` 返回 1 行
    - `grep -c "def test_" tests/test_link_result.py` >= 4
    - `grep "from uasset_read.link.result import LinkerParseResult" tests/test_link_result.py` 存在
    - `python -m pytest tests/test_link_result.py -x -v` 全部通过（exit code 0）
  </acceptance_criteria>
  <done>4 个测试全部通过，覆盖 LinkerParseResult 所有字段</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → LinkerParseResult | .uasset 文件可能包含恶意构造的偏移/计数值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | LinkerParseResult.errors | mitigate | errors 字段为 List[str]，仅接受字符串追加，不接受任意对象注入 |
| T-41-02 | Denial of Service | UObjectInstance._preloaded | mitigate | _preloaded bool 标志防止 preload 重复执行导致资源浪费 |

</threat_model>

<verification>
- python -m pytest tests/test_link_object_instance.py tests/test_link_result.py -x -v — 全部通过
- python -c "from uasset_read.link.result import LinkerParseResult; r = LinkerParseResult(); assert r.is_success == False" — 默认值正确
- grep -c "class LinkerParseResult" src/uasset_read/link/result.py == 1 — 类定义存在
- grep -c "class TestUObjectInstance" tests/test_link_object_instance.py == 1 — 测试类存在
</verification>

<success_criteria>
- [ ] LinkerParseResult 数据类已创建，11 个字段，默认值正确
- [ ] UObjectInstance 骨架验证通过（LINK-01）：5 个核心字段 + 5 个行为测试
- [ ] LinkerParseResult 结构验证通过（LINK-06）：4 个测试覆盖所有字段
- [ ] 现有 373 测试不受影响（回归检查留到 Wave 2 后）
</success_criteria>

<output>
After completion, create `.planning/phases/41-link-module-infrastructure/41-01-SUMMARY.md`
</output>
