---
phase: 042-integration-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/link/result.py
  - src/uasset_read/parse_uasset.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "parse_uasset_with_linker() is importable from uasset_read.parse_uasset"
    - "parse_uasset() behavior is completely unchanged (same code path)"
    - "LinkerParseResult carries blueprint/graphs/dependency fields after post-processing"
    - "Errors are collected into result.errors without silent fallback or hard throw"
  artifacts:
    - path: "src/uasset_read/link/result.py"
      provides: "LinkerParseResult with post-process fields"
      contains: "blueprint, graphs, imports, soft_references, circular_deps optional fields"
    - path: "src/uasset_read/parse_uasset.py"
      provides: "Shared _post_process() + new entry point"
      exports: ["parse_uasset", "parse_uasset_with_linker", "_post_process"]
  key_links:
    - from: "src/uasset_read/parse_uasset.py"
      to: "src/uasset_read/link/linker.py"
      via: "PackageLinker(archive, summary, name_map, import_map, export_map).link()"
      pattern: "PackageLinker.*\\.link\\(\\)"
    - from: "src/uasset_read/parse_uasset.py"
      to: "src/uasset_read/link/result.py"
      via: "import LinkerParseResult"
      pattern: "from uasset_read.link.* import.*LinkerParseResult"
---

<objective>
Add post-process fields to LinkerParseResult so that the shared `_post_process()` function
can write blueprint/graphs/dependency data onto both ParseResult and LinkerParseResult.

Purpose: D-03 requires shared post-processing between both entry points. LinkerParseResult
must have the same writable fields that parse_uasset() currently sets directly.

Output: Updated LinkerParseResult dataclass with 6 new optional fields.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@src/uasset_read/link/result.py
@src/uasset_read/models/result.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add optional post-process fields to LinkerParseResult</name>
  <files>src/uasset_read/link/result.py</files>
  <action>
In src/uasset_read/link/result.py, add these optional fields to the LinkerParseResult dataclass
(after the existing `errors` field, before `is_success`):

1. `blueprint: Optional["BlueprintMetadata"] = None` — Blueprint metadata from extract_blueprint_metadata
2. `graphs: List["UEdGraph"] = field(default_factory=list)` — Blueprint graphs from extract_blueprint_graphs
3. `warnings: List[str] = field(default_factory=list)` — Warning messages (mirrors ParseResult)
4. `imports: List[Dict] = field(default_factory=list)` — Import list from build_imports_list
5. `soft_references: List[Dict] = field(default_factory=list)` — Soft object paths from read_soft_object_paths
6. `circular_deps: List[List[str]] = field(default_factory=list)` — Circular dependency chains from detect_circular_deps

Add the necessary TYPE_CHECKING imports:
- `from uasset_read.models.blueprint import BlueprintMetadata`
- `from uasset_read.models.core import UEdGraph`

Do NOT remove any existing fields (linker, root_objects, all_objects). These are additive.
Do NOT change field types of existing fields.
  </action>
  <verify>
    <automated>python -c "from uasset_read.link.result import LinkerParseResult; r = LinkerParseResult(); assert hasattr(r, 'blueprint'); assert hasattr(r, 'graphs'); assert hasattr(r, 'imports'); assert hasattr(r, 'soft_references'); assert hasattr(r, 'circular_deps'); assert hasattr(r, 'warnings'); print('OK')"</automated>
  </verify>
  <done>LinkerParseResult has all 6 new optional fields with correct types, imports compile without errors, existing fields unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataclass field additions | New optional fields with safe defaults — no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-042-01 | Integrity | link/result.py | accept | Optional fields with default_factory — no untrusted input, low-value target |
</threat_model>

<verification>
- `python -c "from uasset_read.link import LinkerParseResult"` succeeds
- `python -c "r = LinkerParseResult(); r.blueprint = None; r.graphs = []; r.imports = []"` succeeds
- Existing link/ module tests still pass
</verification>

<success_criteria>
- LinkerParseResult dataclass has 6 new optional fields matching ParseResult post-process fields
- All fields have correct type hints and safe default values
- No breaking changes to existing LinkerParseResult fields
- TYPE_CHECKING imports are correctly scoped
</success_criteria>

<output>
After completion, create `.planning/phases/042-integration-entry/042-01-SUMMARY.md`
</output>
