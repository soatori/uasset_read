---
phase: 045
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/models/core.py
  - tests/test_phase45_from_archive_with_linker.py
autonomous: true
requirements:
  - LINK-05
user_setup: []

must_haves:
  truths:
    - "UEdGraphPin.from_archive_with_linker() 可调用并返回 UEdGraphPin 实例"
    - "UEdGraphNode.from_archive_with_linker() 可调用并返回 UEdGraphNode 实例"
    - "UEdGraph.from_archive_with_linker() 可调用并返回 UEdGraph 实例"
    - "linker 参数正确传递给底层 read_* 函数"
    - "UEdGraphPin 的 default_object 可通过 linker 解析为 UObjectInstance"
    - "现有 from_archive() 方法保持不变，向后兼容"
  artifacts:
    - path: "src/uasset_read/models/core.py"
      provides: "三个 from_archive_with_linker() 类方法 + default_object_ref 字段"
      contains: "def from_archive_with_linker"
    - path: "tests/test_phase45_from_archive_with_linker.py"
      provides: "基本功能验证测试"
      contains: "class TestFromArchiveWithLinker"
  key_links:
    - from: "UEdGraphPin.from_archive_with_linker()"
      to: "serializers/graph.py::read_ue_graph_pin()"
      via: "linker 参数传递"
      pattern: "read_ue_graph_pin.*linker=linker"
    - from: "UEdGraphNode.from_archive_with_linker()"
      to: "serializers/graph.py::read_ue_graph_node()"
      via: "linker 参数传递"
      pattern: "read_ue_graph_node.*linker=linker"
    - from: "UEdGraph.from_archive_with_linker()"
      to: "serializers/graph.py::read_ue_graph()"
      via: "linker 参数传递"
      pattern: "read_ue_graph.*linker=linker"
---

<objective>
为 UEdGraphPin、UEdGraphNode、UEdGraph 三个模型类创建 `from_archive_with_linker()` 类方法，并添加 `UEdGraphPin.default_object_ref` 字段。确保 graph 序列化层能通过 linker 模式入口点完整驱动，所有 FPackageIndex 字段均可解析为 UObjectInstance。

Purpose: 这是 v7.0 里程碑的最后一环——Phase 41-44 已完成 link/ 模块、集成入口、PackageIndex 增强和模型增强，本 phase 补齐模型类 entry point 的 linker 支持。
Output: 3 个新方法 + 1 个新字段 + 基本功能验证测试
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/phases/045-graph-serialization-linker/045-CONTEXT.md
@.planning/phases/045-graph-serialization-linker/045-DISCUSSION-LOG.md
@src/uasset_read/models/core.py
@src/uasset_read/serializers/graph.py
@src/uasset_read/link/linker.py
@src/uasset_read/link/object_instance.py
@tests/test_phase44_linker_objects.py
</context>

<interfaces>
<!-- 关键类型和契约，执行者直接使用。 -->

### 新增方法签名（models/core.py）

```python
# UEdGraphPin — 新增字段
@dataclass
class UEdGraphPin:
    # ... 现有字段不变 ...
    default_object_ref: Optional["UObjectInstance"] = None  # D-04: linker 解析后的对象引用

# 三个新的类方法：
@classmethod
def from_archive_with_linker(
    cls,
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary,
    export_map: List[ObjectExport],
    import_map: List[ObjectImport],
    linker: Optional["PackageLinker"] = None,
) -> Self

@classmethod  # UEdGraphNode
def from_archive_with_linker(
    cls,
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary,
    export_map: List[ObjectExport],
    import_map: List[ObjectImport],
    node_export: ObjectExport,
    linker: Optional["PackageLinker"] = None,
) -> Self

@classmethod  # UEdGraph
def from_archive_with_linker(
    cls,
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary,
    export_map: List[ObjectExport],
    import_map: List[ObjectImport],
    graph_export: ObjectExport,
    graph_class: str,
    graph_export_idx: int = 0,
    linker: Optional["PackageLinker"] = None,
) -> Self
```

### 已有 read_* 函数签名（graph.py — 无需修改，仅供参考）

```python
def read_ue_graph_pin(archive, name_map, summary, export_map, import_map, linker=None) -> UEdGraphPin
def read_ue_graph_node(archive, name_map, summary, export_map, import_map, node_export, linker=None) -> UEdGraphNode
def read_ue_graph(archive, name_map, summary, export_map, import_map, graph_export, graph_class, graph_export_idx=0, linker=None) -> UEdGraph
```

### PackageLinker.resolve_package_index (linker.py:123-142)

```python
def resolve_package_index(self, pkg_idx: PackageIndex) -> Optional[UObjectInstance]:
    """将 PackageIndex 解析为 UObjectInstance，null 或越界返回 None。"""
```

### UEdGraphPin 现有 from_archive 模式（参考延迟导入风格）

```python
@classmethod
def from_archive(cls, archive, name_map, summary, export_map, import_map) -> Self:
    """延迟导入避免循环依赖。"""
    from uasset_read.serializers.graph import read_ue_graph_pin
    return read_ue_graph_pin(archive, name_map, summary, export_map, import_map)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 from_archive_with_linker() 方法 + default_object_ref 字段</name>
  <files>src/uasset_read/models/core.py, tests/test_phase45_from_archive_with_linker.py</files>
  <behavior>
    - Test 1: UEdGraphPin.from_archive_with_linker 方法存在，接受 linker 参数，返回 UEdGraphPin
    - Test 2: UEdGraphNode.from_archive_with_linker 方法存在，接受 linker 参数，返回 UEdGraphNode
    - Test 3: UEdGraph.from_archive_with_linker 方法存在，接受 linker 参数，返回 UEdGraph
    - Test 4: linker=None 时行为与 from_archive() 一致（向后兼容）
    - Test 5: linker 提供时，default_object 非零值解析为 UObjectInstance（default_object_ref 字段）
    - Test 6: default_object=0（null）时，default_object_ref 为 None
    - Test 7: linker 解析失败时，default_object_ref 为 None（不抛异常，D-06）
  </behavior>
  <action>
在 `src/uasset_read/models/core.py` 中执行以下修改（按顺序）：

**步骤 1: 为 UEdGraphPin 添加 default_object_ref 字段（D-04）**

在 `default_object: Optional[int] = None`（第 57 行）之后添加新字段：
```python
default_object_ref: Optional["UObjectInstance"] = None
```
这遵循项目现有 `*_object` 命名模式（如 `linked_to_objects`、`parent_pin_object`）。

**步骤 2: 为 UEdGraphPin 添加 from_archive_with_linker()（D-07, D-08, D-09）**

在 `from_archive()` 方法（第 82-92 行）之后添加：
```python
@classmethod
def from_archive_with_linker(
    cls,
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary,
    export_map: List[ObjectExport],
    import_map: List[ObjectImport],
    linker: Optional["PackageLinker"] = None,
) -> Self:
    """带 linker 的读取入口，支持 PackageIndex → UObjectInstance 解析（D-09）。"""
    from uasset_read.serializers.graph import read_ue_graph_pin
    from uasset_read.serializers.object_resources import PackageIndex
    pin = read_ue_graph_pin(archive, name_map, summary, export_map, import_map, linker)
    # D-04: 解析 default_object 为 UObjectInstance
    if linker is not None and pin.default_object is not None and pin.default_object != 0:
        try:
            pin.default_object_ref = linker.resolve_package_index(PackageIndex(pin.default_object))
        except Exception:
            pin.default_object_ref = None  # D-06: 解析失败存 None
    return pin
```
注意：使用延迟导入避免循环依赖（与现有 `from_archive()` 一致）。`read_ue_graph_pin` 返回的 pin 实例上直接设置 `default_object_ref`（dataclass 允许后赋值）。

**步骤 3: 为 UEdGraphNode 添加 from_archive_with_linker()**

在 `from_archive()` 方法（第 106-118 行）之后添加：
```python
@classmethod
def from_archive_with_linker(
    cls,
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary,
    export_map: List[ObjectExport],
    import_map: List[ObjectImport],
    node_export: ObjectExport,
    linker: Optional["PackageLinker"] = None,
) -> Self:
    """带 linker 的读取入口（D-09）。"""
    from uasset_read.serializers.graph import read_ue_graph_node
    return read_ue_graph_node(archive, name_map, summary, export_map, import_map, node_export, linker)
```

**步骤 4: 为 UEdGraph 添加 from_archive_with_linker()**

在 `from_archive()` 方法（第 131-145 行）之后添加：
```python
@classmethod
def from_archive_with_linker(
    cls,
    archive: FArchive,
    name_map: List[str],
    summary: PackageFileSummary,
    export_map: List[ObjectExport],
    import_map: List[ObjectImport],
    graph_export: ObjectExport,
    graph_class: str,
    graph_export_idx: int = 0,
    linker: Optional["PackageLinker"] = None,
) -> Self:
    """带 linker 的读取入口（D-09）。"""
    from uasset_read.serializers.graph import read_ue_graph
    return read_ue_graph(archive, name_map, summary, export_map, import_map, graph_export, graph_class, graph_export_idx, linker)
```

**步骤 5: 编写基本功能验证测试**

创建 `tests/test_phase45_from_archive_with_linker.py`，参考 `tests/test_phase44_linker_objects.py` 的 `_BytesIOArchive`、`_make_mock_linker()`、`_make_mock_summary()` 等 helper，编写以下测试类：

1. `TestMethodExistence` — 使用 `inspect.signature` 验证三个方法存在且接受 `linker` 参数
2. `TestDefaultObjectRef` — 使用 Phase 44 的 `_build_minimal_ue5_pin()` 模式构建 pin 数据，验证：
   - default_object=非零值 + linker → default_object_ref 为 UObjectInstance
   - default_object=0 → default_object_ref 为 None
   - linker=None → default_object_ref 为 None
3. `TestBackwardCompatibility` — 验证 from_archive() 未被修改（签名不变）

测试文件应复用 Phase 44 的 helper 模式（_BytesIOArchive, _make_mock_linker, _make_mock_summary），不要直接 import Phase 44 测试中的 helper（避免跨测试文件依赖）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase45_from_archive_with_linker.py -x -v</automated>
  </verify>
  <done>
    - UEdGraphPin/Node/Graph 三个类都有 from_archive_with_linker() 方法
    - 方法签名与 from_archive() 一致，额外接受 linker 参数
    - UEdGraphPin 有 default_object_ref 字段
    - 所有测试通过（方法存在、参数传递、default_object 解析、向后兼容）
    - 现有 from_archive() 方法未被修改
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .uasset 文件 → FArchive | 用户提供的二进制文件，可能包含恶意构造的序列化数据 |
| PackageIndex → UObjectInstance | linker 解析时可能遇到越界索引 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-045-01 | Tampering | from_archive_with_linker() | accept | 仅传递 linker 参数给已有 read_* 函数，这些函数已有容错处理；新增的 default_object_ref 解析有 try/except 保护（D-06） |
| T-045-02 | Denial of Service | default_object_ref 解析 | mitigate | 使用 try/except 捕获解析异常，失败时设为 None（D-06），不抛异常中断解析流程 |
</threat_model>

<verification>
1. 运行 `python -m pytest tests/test_phase45_from_archive_with_linker.py -x -v` — 所有测试通过
2. 运行 `python -m pytest tests/ -x --ignore=tests/test_property_parsing.py` — 确保无回归（373 passed, 0 failed）
3. 验证 `grep -c "from_archive_with_linker" src/uasset_read/models/core.py` == 3（三个方法定义）
4. 验证 `grep "default_object_ref" src/uasset_read/models/core.py` 存在（新字段）
</verification>

<success_criteria>
- [ ] UEdGraphPin.from_archive_with_linker() 存在，接受 6 个参数（archive + 4 maps + linker），返回 UEdGraphPin
- [ ] UEdGraphNode.from_archive_with_linker() 存在，接受 7 个参数，返回 UEdGraphNode
- [ ] UEdGraph.from_archive_with_linker() 存在，接受 9 个参数，返回 UEdGraph
- [ ] 三个方法均通过延迟导入调用对应 read_* 函数，linker 参数正确传递
- [ ] UEdGraphPin 新增 default_object_ref 字段，类型 Optional[UObjectInstance]
- [ ] from_archive_with_linker() 中 default_object 非零时通过 linker.resolve_package_index() 解析
- [ ] 解析失败时 default_object_ref = None（不抛异常）
- [ ] 现有 from_archive() 方法签名和行为完全不变
- [ ] Phase 45 测试全部通过
- [ ] 全量测试无回归
</success_criteria>

<output>
After completion, create `.planning/phases/045-graph-serialization-linker/045-01-SUMMARY.md`
</output>
