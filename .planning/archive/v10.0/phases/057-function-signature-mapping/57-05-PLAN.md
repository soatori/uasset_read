---
phase: "057"
plan: "05"
type: execute
wave: 4
depends_on: ["057-02", "057-03", "057-04"]
files_modified:
  - tests/test_cpp_gen.py
autonomous: true
requirements: [FUNC-01, FUNC-02, FUNC-03]
user_setup: []

must_haves:
  truths:
    - "Golden-path test with BP_FirstPersonCharacter-like data produces correct .h output"
    - "Extracted methods match reference node data from reference/蓝图节点文本参考.md"
    - "Extracted call statements match reference CallFunction node data"
    - "End-to-end pipeline: extract → format produces valid UE C++ text"
  artifacts:
    - path: "tests/test_cpp_gen.py"
      provides: "Golden-path integration tests for Phase 57"
      exports: ["TestFunctionSignatureGoldenPath"]
  key_links:
    - from: "tests/test_cpp_gen.py"
      to: "src/uasset_read/cpp_gen/extract_cpp_skeleton.py"
      via: "extract_cpp_functions + extract_cpp_call_statements"
      pattern: "extract_cpp_functions|extract_cpp_call_statements"
    - from: "tests/test_cpp_gen.py"
      to: "src/uasset_read/cpp_gen/formatters/cpp_header_formatter.py"
      via: "format_cpp_header + format_cpp_call_statements"
      pattern: "format_cpp_header|format_cpp_call_statements"

<objective>
Add golden-path integration tests for Phase 57 end-to-end pipeline.

Purpose: Verify the complete extraction → formatting pipeline with data matching BP_FirstPersonCharacter reference nodes (FUNC-01, FUNC-02, FUNC-03).
Output: Golden-path test class in test_cpp_gen.py covering Move/Aim function signatures and Jump/StopJumping call statements.
</objective>

<execution_context>
@.planning/phases/057-function-signature-mapping/57-CONTEXT.md
@.planning/phases/057-function-signature-mapping/57-RESEARCH.md
@reference/蓝图节点文本参考.md
</execution_context>

<context>
@src/uasset_read/cpp_gen/extract_cpp_skeleton.py
@src/uasset_read/cpp_gen/formatters/cpp_header_formatter.py
@src/uasset_read/cpp_gen/formatters/cpp_json_ir.py
@tests/test_cpp_skeleton_e2e.py
@reference/蓝图节点文本参考.md

<interfaces>
Reference data from reference/蓝图节点文本参考.md:

Move FunctionEntry:
```
K2Node_FunctionEntry_0:
  FunctionReference=(MemberName="Move")
  bIsEditable=True
  Pins:
    - "then" (exec, EGPD_Output)
    - "Left / Right" (real/double, EGPD_Output)
    - "Forward / Backward" (real/double, EGPD_Output)
```
→ Expected: `UFUNCTION(BlueprintCallable) void Move(double LeftRight, double ForwardBackward);`

Jump CallFunction:
```
K2Node_CallFunction_1193:
  FunctionReference=(MemberName="Jump",bSelfContext=True)
  Pins:
    - "execute" (exec, Input)
    - "then" (exec, Output)
    - "self" (object, PinSubCategoryObject="/Script/CoreUObject.Class'/Script/Engine.Character'")
```
→ Expected: `this->Jump();`

StopJumping CallFunction:
```
K2Node_CallFunction_9386:
  FunctionReference=(MemberName="StopJumping",bSelfContext=True)
  Pins:
    - "execute" (exec, Input)
    - "then" (exec, Output)
    - "self" (object, PinSubCategoryObject="/Script/CoreUObject.Class'/Script/Engine.Character'")
```
→ Expected: `this->StopJumping();`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add golden-path integration tests for Phase 57 end-to-end</name>
  <files>tests/test_cpp_gen.py</files>
  <read_first>
    - tests/test_cpp_gen.py (existing tests from Plans 01-04)
    - tests/test_cpp_skeleton_e2e.py (golden-path test pattern to reuse — mock_blueprint_metadata fixture, mock_linker_parse_result fixture)
    - reference/蓝图节点文本参考.md (Move FunctionEntry, Jump/StopJumping CallFunction reference data)
  </read_first>
  <action>
Add test class `TestFunctionSignatureGoldenPath` to tests/test_cpp_gen.py:

**Fixtures:**
1. `mock_function_entry_move`: Create mock K2NodeFunctionEntry-like object matching reference data:
   - class_name = "K2Node_FunctionEntry"
   - function_reference = FMemberReference(member_name="Move", b_self_context=False)
   - pins: [exec "then" (direction=1), real "Left / Right" (direction=1, pin_subcategory="double"), real "Forward / Backward" (direction=1, pin_subcategory="double")]
   - b_is_editable = True

2. `mock_event_primary_thumbstick`: Create mock K2NodeEvent-like object:
   - class_name = "K2Node_Event"
   - event_reference = FMemberReference(member_name="Primary Thumbstick", b_self_context=False)
   - b_override_function = True
   - pins: [delegate "OutputDelegate" (direction=1), exec "then" (direction=1), real "Axis_X" (direction=1, pin_subcategory="double"), real "Axis_Y" (direction=1, pin_subcategory="double")]

3. `mock_call_jump`: Create mock K2NodeCallFunction for Jump:
   - class_name = "K2Node_CallFunction"
   - function_reference = FMemberReference(member_name="Jump", b_self_context=True)
   - pins: [exec "execute" (direction=0), exec "then" (direction=1), object "self"]

4. `mock_graph_with_functions`: Create mock UEdGraph with all above nodes in graph.nodes.

**Tests:**
1. `test_golden_move_function_signature`: extract_cpp_functions with mock_graph → verify Move method in results: cpp_name="Move", 2 parameters, ufunction_specifiers=["BlueprintCallable"], return_type="void".

2. `test_golden_event_override`: extract_cpp_functions with mock_graph → verify PrimaryThumbstick method: cpp_name="PrimaryThumbstick", is_override=True, ufunction_specifiers=[], 2 parameters.

3. `test_golden_jump_call_statement`: extract_cpp_call_statements with mock_graph → verify Jump call: method_name="Jump", target="this", args=[].

4. `test_golden_full_header_output`: Full pipeline — extract methods → attach to CppClassIR → format_cpp_header → verify .h output contains:
   - `UFUNCTION(BlueprintCallable)` before `void Move(double LeftRight, double ForwardBackward);`
   - `void PrimaryThumbstick(double Axis_X, double Axis_Y) override;` (no UFUNCTION before it)
   - Constructor, properties, methods all present in correct UE .h structure

5. `test_golden_call_statements_output`: Full pipeline — extract calls → format_cpp_call_statements → verify output contains:
   - `this->Jump();`
   - `this->StopJumping();`

Use pytest fixtures to avoid test code duplication. Mock objects should use real dataclasses (FMemberReference, FEdGraphPinType, UEdGraphPin) where possible, MagicMock only for node wrapper behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cpp_gen.py::TestFunctionSignatureGoldenPath -x</automated>
  </verify>
  <acceptance_criteria>
    - test_golden_move_function_signature passes: Move method has correct name, 2 double parameters, BlueprintCallable specifier
    - test_golden_event_override passes: PrimaryThumbstick has is_override=True, no UFUNCTION, 2 double parameters
    - test_golden_jump_call_statement passes: Jump call has method_name="Jump", target="this", args=[]
    - test_golden_full_header_output passes: .h output contains `UFUNCTION(BlueprintCallable)\n    void Move(...)` and `void PrimaryThumbstick(...) override;`
    - test_golden_call_statements_output passes: Output contains `this->Jump();` and `this->StopJumping();`
    - Full test suite passes: `python -m pytest tests/test_cpp_gen.py -x` (all tests from Plans 01-05)
  </acceptance_criteria>
  <done>Golden-path tests verify end-to-end Phase 57 pipeline produces correct C++ declarations and call statements matching BP_FirstPersonCharacter reference data</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify all Phase 57 tests pass</name>
  <files>tests/test_cpp_gen.py</files>
  <read_first>
    - tests/test_cpp_gen.py (complete file with all Phase 57 tests)
    - tests/test_cpp_skeleton_e2e.py (verify no test conflicts)
  </read_first>
  <action>
Run full test suite to verify:
1. All Phase 57 tests pass: `python -m pytest tests/test_cpp_gen.py -v`
2. No regression in existing tests: `python -m pytest tests/ -x --ignore=tests/test_cpp_gen.py` (verify Phase 56 tests still pass)
3. Full suite: `python -m pytest tests/ -x`

If any test fails, diagnose and fix before declaring Phase 57 complete.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cpp_gen.py -v` shows all Phase 57 tests passing
    - `python -m pytest tests/ -x` full suite passes with no new failures
    - Test count increases from baseline (656 passed) by the number of new Phase 57 tests
  </acceptance_criteria>
  <done>Full test suite green — Phase 57 complete with no regressions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock data → extraction | Test fixtures use synthetic data; no external input |
| Golden-path verification | Tests validate against known reference data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Tampering | Test assertions | mitigate | Assertions use exact string matching and field-by-field comparison, not substring checks |
| T-57-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages — uses existing project modules only |
</threat_model>

<verification>
- Golden-path tests produce correct .h output matching reference data
- Call statement output matches reference CallFunction nodes
- Full test suite passes with no regressions
- All three requirements (FUNC-01, FUNC-02, FUNC-03) covered by tests
</verification>

<success_criteria>
- Move function: `UFUNCTION(BlueprintCallable) void Move(double LeftRight, double ForwardBackward);` appears in .h output
- PrimaryThumbstick: `void PrimaryThumbstick(double Axis_X, double Axis_Y) override;` appears with no UFUNCTION
- Jump call: `this->Jump();` appears in call statement output
- Full test suite passes (no new failures, existing tests unaffected)
- All FUNC-01, FUNC-02, FUNC-03 requirements verified by tests
</success_criteria>

<output>
Create `.planning/phases/057-function-signature-mapping/57-05-SUMMARY.md` when done
</output>
