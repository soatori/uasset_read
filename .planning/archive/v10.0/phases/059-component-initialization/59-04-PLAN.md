---
phase: "059"
plan: "04"
type: execute
wave: 3
depends_on: [59-01, 59-02, 59-03]
files_modified:
  - .planning/phases/059-component-initialization/59-VALIDATION.md
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "VALIDATION.md 定义了 golden-path 测试策略"
    - "包含 CreateDefaultSubobject 调用验证清单"
    - "包含 SetupAttachment 链验证清单"
    - "包含 transform 赋值验证清单"
    - "包含 property 默认值验证清单"
    - "包含 InputAction LoadObject 验证清单"
  artifacts:
    - path: ".planning/phases/059-component-initialization/59-VALIDATION.md"
      provides: "Phase 59 验证策略文档"
      contains: "Validation strategy, golden-path test plan, verification checklist"
  key_links:
    - from: ".planning/phases/059-component-initialization/59-VALIDATION.md"
      to: "src/uasset_read/cpp_gen/cpp_constructor_formatter.py"
      via: "验证生成的构造函数代码"
      pattern: "format_cpp_constructor|build_constructor_sections"
---

<objective>
创建 Phase 59 验证文档，定义 golden-path 测试策略和验证清单，确保生成的构造函数代码与 BP_FirstPersonCharacter 参考实现匹配。

Purpose: 为 execute-phase 提供明确的验证标准，避免功能回归。
Output: 59-VALIDATION.md 文档。
</objective>

<execution_context>
@E:\Develop\uasset_read\.planning\phases\059-component-initialization\59-CONTEXT.md
@E:\Develop\uasset_read\.planning\phases\059-component-initialization\59-01-PLAN.md
@E:\Develop\uasset_read\.planning\phases\059-component-initialization\59-02-PLAN.md
@E:\Develop\uasset_read\.planning\phases\059-component-initialization\59-03-PLAN.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\phases\059-component-initialization\59-CONTEXT.md

<!-- 参考 C++ 实现（来自 59-CONTEXT.md） -->

BP_FirstPersonCharacter 参考构造函数:
```cpp
AFirstPersonCCharacter::AFirstPersonCCharacter()
{
    // 1. Capsule size
    GetCapsuleComponent()->InitCapsuleSize(55.f, 96.0f);

    // 2. FirstPersonMesh 创建
    FirstPersonMesh = CreateDefaultSubobject<USkeletalMeshComponent>(TEXT("First Person Mesh"));
    FirstPersonMesh->SetupAttachment(GetMesh());
    FirstPersonMesh->SetOnlyOwnerSee(true);
    FirstPersonMesh->FirstPersonPrimitiveType = EFirstPersonPrimitiveType::FirstPerson;
    FirstPersonMesh->SetCollisionProfileName(FName("NoCollision"));

    // 3. Camera 创建
    FirstPersonCameraComponent = CreateDefaultSubobject<UCameraComponent>(TEXT("First Person Camera"));
    FirstPersonCameraComponent->SetupAttachment(FirstPersonMesh, FName("head"));
    FirstPersonCameraComponent->SetRelativeLocationAndRotation(
        FVector(-2.8f, 5.89f, 0.0f),
        FRotator(0.0f, 90.0f, -90.0f)
    );
    FirstPersonCameraComponent->bUsePawnControlRotation = true;
    FirstPersonCameraComponent->EnableFirstPersonFieldOfView = true;
    FirstPersonCameraComponent->EnableFirstPersonScale = true;
    FirstPersonCameraComponent->FirstPersonFieldOfView = 70.0f;
    FirstPersonCameraComponent->FirstPersonScale = 0.6f;

    // 4. CharacterMesh 设置
    GetMesh()->SetOwnerNoSee(true);
    GetMesh()->FirstPersonPrimitiveType = EFirstPersonPrimitiveType::WorldSpaceRepresentation;

    // 5. Capsule size (再次设置)
    GetCapsuleComponent()->SetCapsuleSize(34.0f, 96.0f);

    // 6. CharacterMovement 配置
    GetCharacterMovement()->BrakingDecelerationFalling = 1500.0f;
    GetCharacterMovement()->AirControl = 0.5f;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 59-VALIDATION.md 验证文档</name>
  <files>.planning/phases/059-component-initialization/59-VALIDATION.md</files>
  <action>
创建 `59-VALIDATION.md` 验证文档，包含以下章节：

**1. 验证策略**
- Golden-path 测试：使用 BP_FirstPersonCharacter 作为参考实现
- 对比方法：将 `format_cpp_constructor()` 输出与 59-CONTEXT.md 中的已知-good C++ 参考逐行对比
- 容许差异：注释文本、空行数量、缩进风格（4 空格固定）

**2. CreateDefaultSubobject 调用验证**
检查清单：
- [ ] `FirstPersonMesh = CreateDefaultSubobject<USkeletalMeshComponent>(TEXT("First Person Mesh"))`
- [ ] `FirstPersonCameraComponent = CreateDefaultSubobject<UCameraComponent>(TEXT("First Person Camera"))`
- [ ] UInputAction* 类型变量**不**出现在 CreateDefaultSubobject 调用中（D-59-06）

**3. SetupAttachment 链验证**
检查清单：
- [ ] `FirstPersonMesh->SetupAttachment(GetMesh())`（或等价形式）
- [ ] `FirstPersonCameraComponent->SetupAttachment(FirstPersonMesh, FName("head"))`
- [ ] attach 顺序：子组件 attach 在父组件创建之后

**4. Transform 赋值验证**
检查清单：
- [ ] `FirstPersonCameraComponent->SetRelativeLocationAndRotation(FVector(-2.8f, 5.89f, 0.0f), FRotator(0.0f, 90.0f, -90.0f))`
- [ ] FVector 参数顺序：(x, y, z)，float 值有 f 后缀
- [ ] FRotator 参数顺序：(pitch, yaw, roll)，float 值有 f 后缀

**5. Property 默认值验证**
检查清单：
- [ ] `FirstPersonCameraComponent->bUsePawnControlRotation = true`
- [ ] `GetCharacterMovement()->BrakingDecelerationFalling = 1500.0f`
- [ ] float 值有 f 后缀，bool 值为 true/false

**6. InputAction LoadObject 验证**
检查清单：
- [ ] JumpAction, MoveAction, LookAction, MouseLookAction 使用 `LoadObject<UInputAction>(nullptr, TEXT("/Game/..."))` 而非 CreateDefaultSubobject
- [ ] 如果 asset_path 为空，跳过该 InputAction 初始化

**7. Super 调用验证**
检查清单：
- [ ] 构造函数初始化列表包含 `: Super::ClassName()`（无条件，D-59-05）
- [ ] 不是 `: Super()`

**8. 代码段顺序验证**
检查清单：
- [ ] Super 调用 → Component creation → Setup attachments → Transform assignments → Property assignments → InputAction loads

**9. 已知前提**
- `extract_components` 当前不返回 `attach_parent` 字段。如果 attach 关系无法从现有数据获取，需要在验证文档中标注此限制。
</action>
<verify>
  <automated>test -f .planning/phases/059-component-initialization/59-VALIDATION.md</automated>
</verify>
<done>59-VALIDATION.md 存在并包含所有 9 个验证章节和检查清单</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates documentation only, no code execution paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- 59-VALIDATION.md 文件存在且包含所有验证章节
- 检查清单覆盖所有 4 个 Plan 的输出
</verification>

<success_criteria>
- 59-VALIDATION.md 文件存在于 .planning/phases/059-component-initialization/ 目录
- 包含验证策略、golden-path 测试方法、6 个检查清单章节（CreateDefaultSubobject, SetupAttachment, Transform, Property defaults, InputAction, Super call）
- 标注已知前提（extract_components 缺少 attach_parent）
</success_criteria>

<output>
Create `.planning/phases/059-component-initialization/59-04-SUMMARY.md` when done
</output>
