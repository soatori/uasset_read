---
phase: 66-agent-translation-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/uasset_read/agent/writer.py]
autonomous: true
requirements: [TRANSLATE-BP-01]
user_setup: []
must_haves:
  truths:
    - "CppFileWriter generates valid .h file text"
    - "CppFileWriter generates valid .cpp file text"
    - "Generated .h includes UCLASS macro and class declaration"
    - "Generated .cpp includes constructor and method implementations"
  artifacts:
    - path: "src/uasset_read/agent/writer.py"
      provides: "C++ file output formatter"
      min_lines: 150
      exports: ["CppFileWriter", "write_cpp_class_files"]
      contains: "class CppFileWriter"
  key_links:
    - from: "CppClassIR"
      to: ".h/.cpp files"
      via: "write_cpp_class_files(ir, output_dir)"
      pattern: "ir → header_text + cpp_text → file.write()"
    - from: "CppMethodIR.body"
      to: "method implementation"
      via: "_format_method_definition()"
      pattern: "body注入到 .cpp 文件的方法实现中"
---

<objective>
创建 C++ 文件输出模块，将 CppClassIR 转换为 .h 和 .cpp 文件文本。

Purpose: 提供文件级输出能力，生成符合 UE C++ 规范的类文件。
Output: agent/writer.py（文件输出模块）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-66/66-CONTEXT.md

# Phase 59 输出（构造函数生成）
@src/uasset_read/cpp_gen/cpp_constructor_formatter.py
@src/uasset_read/cpp_gen/cpp_constructor_ir_builder.py

# Phase 56 输出（类型映射）
@src/uasset_read/cpp_gen/cpp_type_mapper.py
@src/uasset_read/cpp_gen/cpp_uproperty_mapper.py
</context>

<interfaces>
<!-- Key contracts from cpp_gen -->

From src/uasset_read/cpp_gen/formatters.py:
```python
class CppClassIR:
    name: str
    parent_class: str
    header_meta: CppHeaderMeta
    properties: List[CppProperty]
    methods: List[CppMethodIR]
    constructor: Dict[str, Any]  # {"constructor_text": "...", ...}

class CppProperty:
    cpp_type: str
    name: str
    uproperty_marks: List[str]
    category: str
    default_value: Optional[Any]

class CppMethodIR:
    cpp_name: str
    return_type: str
    parameters: List[CppCallParameter]
    ufunction_specifiers: List[str]
    is_override: bool
    body: Optional[str]  # Added in Phase 66-01
```

From src/uasset_read/cpp_gen/cpp_constructor_formatter.py:
```python
def format_cpp_constructor(ir: CppClassIR) -> str
# Returns constructor text already in ir.constructor["constructor_text"]
```

UE C++ file structure reference:
```cpp
// .h file
#pragma once
#include "CoreMinimal.h"
#include "GameFramework/Character.h"
#include "BP_FirstPersonCharacter.generated.h"

UCLASS()
class ABP_FirstPersonCharacter : public ACharacter
{
    GENERATED_BODY()

public:
    ABP_FirstPersonCharacter();

    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Camera")
    UCameraComponent* CameraComponent;

    UFUNCTION(BlueprintCallable, Category = "Input")
    void Aim(float AxisValue);

protected:
    virtual void BeginPlay() override;

private:
    // Private members
};

// .cpp file
#include "BP_FirstPersonCharacter.h"

ABP_FirstPersonCharacter::ABP_FirstPersonCharacter()
{
    // Constructor implementation (from ir.constructor["constructor_text"])
    CameraComponent = CreateDefaultSubobject<UCameraComponent>("CameraComponent");
    CameraComponent->SetupAttachment(RootComponent);
}

void ABP_FirstPersonCharacter::Aim(float AxisValue)
{
    // Method body (from method.body or Kismet decompiled)
    AddControllerPitchInput(AxisValue);
}
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IR → Writer | CppClassIR input (intermediate representation) |
| Writer → Files | Text output to .h/.cpp files (user review required) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-04 | Information Disclosure | Blueprint logic exposure | accept | intentional — translation exposes logic |
| T-66-05 | Denial of Service | Large blueprint processing | mitigate | timeout on file generation, fail gracefully |
| T-66-SC | Tampering | Python imports | mitigate | use existing tested modules only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2: Create CppFileWriter for .h/.cpp output</name>
  <files>src/uasset_read/agent/writer.py, tests/test_agent_writer.py</files>
  <behavior>
    - Test 1: CppFileWriter importable and callable
    - Test 2: write_cpp_class_files(ir, None) returns dict with .h/.cpp text
    - Test 3: write_cpp_class_files(ir, "dir") writes files to directory
    - Test 4: Generated .h contains #pragma once + UCLASS macro
    - Test 5: Generated .h contains correct class declaration (class X : public Y)
    - Test 6: Generated .h contains UPROPERTY declarations
    - Test 7: Generated .cpp contains constructor implementation
    - Test 8: Generated .cpp contains method implementations (if body present)
    - Test 9: Empty method.body generates declaration only (no implementation in .cpp)
  </behavior>
  <action>
    创建 C++ 文件输出模块，将 CppClassIR 转换为 .h 和 .cpp 文件文本。

    1. 创建 `agent/writer.py` 包含：
       - `CppFileWriter` 类：
         - `__init__(self, ir: CppClassIR)` - 接收 IR
         - `_generate_header_text(self) -> str` - 生成 .h 文件内容
         - `_generate_cpp_text(self) -> str` - 生成 .cpp 文件内容
         - `_format_property_declaration(self, prop: CppProperty) -> str` - 属性声明格式化
         - `_format_method_declaration(self, method: CppMethodIR) -> str` - 方法声明格式化
         - `_format_method_definition(self, method: CppMethodIR) -> str` - 方法实现格式化
         - `write_to_files(self, output_dir: Optional[str] = None) -> Dict[str, str]` - 写入文件或返回文本
       - `write_cpp_class_files(ir, output_dir=None)` 便捷函数

    2. 创建 `tests/test_agent_writer.py` 包含上述 9 个测试

    文件结构（per UE C++ 规范）：
    - .h 文件：
      - #pragma once
      - #include "CoreMinimal.h" + parent class include
      - #include "ClassName.generated.h"
      - UCLASS() macro
      - class declaration (class X : public Y)
      - GENERATED_BODY()
      - public/protected/private sections
      - UPROPERTY declarations
      - UFUNCTION declarations + method signatures
    - .cpp 文件：
      - #include "ClassName.h"
      - Constructor implementation（从 ir.constructor["constructor_text"]）
      - Method implementations（从 method.body，如果存在）

    Per D-66-04: 如果 method.body 为 None，在 .h 中声明，但不生成 .cpp 实现。
    Per D-66-05: 构造函数文本直接使用 `ir.constructor["constructor_text"]`（Phase 59 已生成）。
    Per D-66-06: UPROPERTY 标记从 `prop.uproperty_marks` 提取（Phase 56 已生成）。

    实现顺序：
    - RED: 先写 test_agent_writer.py（9 个测试）
    - GREEN: 实现 writer.py 使测试通过
    - REFACTOR: 清理代码，添加类型注解
  </action>
  <verify>
    <automated>python -m pytest tests/test_agent_writer.py -v --tb=short</automated>
  </verify>
  <done>
    - agent/writer.py exists with CppFileWriter class
    - write_cpp_class_files() function callable
    - All 9 tests pass
    - Generated .h contains UCLASS macro and class declaration
    - Generated .cpp contains constructor and method implementations
    - Empty method.body handled correctly (declaration only)
  </done>
</task>

</tasks>

<verification>
## Manual verification
```bash
# Test import
python -c "from uasset_read.agent.writer import CppFileWriter, write_cpp_class_files; print('Import OK')"

# Test file generation on sample IR
python -c "
import sys; sys.path.insert(0, 'src')
from uasset_read.cpp_gen.formatters import CppClassIR, CppProperty, CppMethodIR
from uasset_read.agent.writer import write_cpp_class_files

# Create sample IR
ir = CppClassIR(
    name='ATestCharacter',
    parent_class='ACharacter',
    header_meta={'includes': ['GameFramework/Character.h']},
    properties=[
        CppProperty(cpp_type='UCameraComponent*', name='CameraComp',
                    uproperty_marks=['VisibleAnywhere', 'BlueprintReadOnly'], category='Camera')
    ],
    methods=[
        CppMethodIR(cpp_name='Aim', return_type='void',
                    parameters=[{'name': 'AxisValue', 'cpp_type': 'float'}],
                    ufunction_specifiers=['BlueprintCallable'], is_override=False,
                    body='AddControllerPitchInput(AxisValue);')
    ],
    constructor={'constructor_text': 'CameraComp = CreateDefaultSubobject<UCameraComponent>(\"CameraComp\");'}
)

# Generate files
files = write_cpp_class_files(ir, None)
print('.h file:')
print(files['.h'][:200])
print('.cpp file:')
print(files['.cpp'][:200])
"
```

## Automated verification
```bash
python -m pytest tests/test_agent_writer.py -v
```
</verification>

<success_criteria>
- [x] CppFileWriter module created and importable
- [x] write_cpp_class_files() function exists and callable
- [x] 9 tests in test_agent_writer.py all pass
- [x] Generated .h contains #pragma once, UCLASS, class declaration
- [x] Generated .h contains UPROPERTY declarations
- [x] Generated .cpp contains constructor implementation
- [x] Generated .cpp contains method implementations (when body present)
- [x] Empty method.body handled (declaration only in .h, no .cpp impl)
- [x] File structure follows UE C++ conventions
- [x] All commits follow git commit message format
</success_criteria>

<output>
Create `.planning/phases/phase-66/66-02-SUMMARY.md` when done
</output>