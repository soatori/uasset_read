---
phase: 66-agent-translation-pipeline
plan: 03
type: execute
wave: 2
depends_on: [66-01, 66-02]
files_modified: [tests/test_agent_translation.py, tests/golden/agent/BP_FirstPersonCharacter.h.expected, tests/golden/agent/BP_FirstPersonCharacter.cpp.expected]
autonomous: true
requirements: [TRANSLATE-BP-02]
user_setup: []
must_haves:
  truths:
    - "Integration test validates BP_FirstPersonCharacter → C++ translation"
    - "Golden files provide reference output for validation"
    - "Test handles empty decompiled_functions (skeleton mode)"
    - "Test handles incomplete graphs (fallback strategy)"
  artifacts:
    - path: "tests/test_agent_translation.py"
      provides: "End-to-end integration tests"
      min_lines: 100
      contains: "test_translate_bp_first_person_character"
    - path: "tests/golden/agent/BP_FirstPersonCharacter.h.expected"
      provides: "Golden .h file reference"
      contains: "UCLASS() class ABP_FirstPersonCharacter"
    - path: "tests/golden/agent/BP_FirstPersonCharacter.cpp.expected"
      provides: "Golden .cpp file reference"
      contains: "ABP_FirstPersonCharacter::ABP_FirstPersonCharacter"
  key_links:
    - from: "parse_uasset_with_linker"
      to: "translate_blueprint_to_cpp"
      via: "integration test"
      pattern: "parse → translate → validate"
    - from: "Generated .h/.cpp"
      to: "Golden files"
      via: "partial match comparison"
      pattern: "关键结构匹配（类名、继承、属性）"
---

<objective>
创建 Agent 翻译管线端到端集成测试，验证 BP_FirstPersonCharacter → C++ 输出质量。

Purpose: 验证整个翻译管线（parse → translate → write）的正确性。
Output: tests/test_agent_translation.py（集成测试） + tests/golden/agent/（Golden file fixtures）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-66/66-CONTEXT.md

# Phase 65 输出（已知 stub）
@.planning/phases/phase-65/65-01-SUMMARY.md
@.planning/phases/phase-65/65-02-SUMMARY.md

# Phase 64 输出（Kismet 反编译）
@.planning/phases/phase-64/64-02-SUMMARY.md

# Wave 1 输出（Agent 翻译管线）
@.planning/phases/phase-66/66-01-SUMMARY.md（预期）
@.planning/phases/phase-66/66-02-SUMMARY.md（预期）

# 测试资产
E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset
</context>

<interfaces>
<!-- Key test dependencies -->

From src/uasset_read/parse_uasset.py:
```python
def parse_uasset_with_linker(path: str, tolerant: bool = True) -> LinkerParseResult
```

From src/uasset_read/agent/translator.py (Wave 1):
```python
def translate_blueprint_to_cpp(result: LinkerParseResult, output_dir: Optional[str] = None) -> CppClassIR
```

From src/uasset_read/agent/writer.py (Wave 1):
```python
def write_cpp_class_files(ir: CppClassIR, output_dir: Optional[str] = None) -> Dict[str, str]
```

Expected test pattern:
```python
# Parse BP_FirstPersonCharacter
result = parse_uasset_with_linker(BP_PATH, tolerant=True)

# Translate to C++ IR
ir = translate_blueprint_to_cpp(result)

# Generate files
files = write_cpp_class_files(ir, None)

# Validate IR structure
assert ir.name == "ABP_FirstPersonCharacter"
assert ir.parent_class == "ACharacter"
assert len(ir.properties) > 0  # Has components
assert len(ir.methods) >= 0  # May be empty if decompiled_functions empty

# Validate generated files
assert "UCLASS()" in files['.h']
assert "class ABP_FirstPersonCharacter : public ACharacter" in files['.h']
assert "ABP_FirstPersonCharacter::ABP_FirstPersonCharacter" in files['.cpp']
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Assets → Test | External .uasset file dependency |
| Test → Golden Files | Reference output comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-06 | Information Disclosure | Blueprint logic in golden files | accept | intentional — golden files expose logic |
| T-66-07 | Denial of Service | Test file I/O | mitigate | use temp directory, cleanup after test |
| T-66-SC | Tampering | Test imports | mitigate | use existing tested modules only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3: Create golden file integration test</name>
  <files>tests/test_agent_translation.py, tests/golden/agent/BP_FirstPersonCharacter.h.expected, tests/golden/agent/BP_FirstPersonCharacter.cpp.expected</files>
  <behavior>
    - Test 1: test_translate_bp_first_person_character() passes
    - Test 2: test_translate_empty_blueprint() handles blueprint=None gracefully
    - Test 3: test_translate_with_no_decompiled_functions() generates skeleton (no method bodies)
    - Test 4: test_translate_with_no_graphs() uses blueprint_functions fallback
    - Test 5: Generated .h matches golden file (key sections: UCLASS, class declaration, properties)
    - Test 6: Generated .cpp matches golden file (key sections: constructor, includes)
    - Test 7: Integration test runs end-to-end without errors
  </behavior>
  <action>
    创建 Agent 翻译管线集成测试，验证 BP_FirstPersonCharacter → C++ 输出质量。

    1. 创建 `tests/test_agent_translation.py` 包含：
       - `test_translate_bp_first_person_character()` - 主集成测试
       - `test_translate_empty_blueprint()` - 空蓝图处理测试
       - `test_translate_with_no_decompiled_functions()` - 无字节码测试
       - `test_translate_with_no_graphs()` - 无图数据测试
       - `_load_bp_first_person_character()` - 测试 fixture helper
       - `_compare_with_golden(generated, golden_path, key_sections)` - Golden file 比较辅助函数

    2. 创建 golden files 目录：`tests/golden/agent/`
    3. 创建 `BP_FirstPersonCharacter.h.expected`（期望的 .h 文件）：
       - 手动编写符合 UE C++ 规范的类声明
       - 包含 UCLASS、继承自 ACharacter、组件属性
       - 示例：
         ```cpp
         #pragma once
         #include "CoreMinimal.h"
         #include "GameFramework/Character.h"
         #include "BP_FirstPersonCharacter.generated.h"

         UCLASS()
         class ABP_FirstPersonCharacter : public ACharacter
         {
             GENERATED_BODY()

         public:
             ABP_FirstPersonCharacter();

             UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Camera")
             UCameraComponent* CameraComponent;

             UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Mesh")
             USkeletalMeshComponent* MeshComponent;
         };
         ```
    4. 创建 `BP_FirstPersonCharacter.cpp.expected`（期望的 .cpp 文件）：
       - 包含构造函数实现
       - 示例：
         ```cpp
         #include "BP_FirstPersonCharacter.h"

         ABP_FirstPersonCharacter::ABP_FirstPersonCharacter()
         {
             CameraComponent = CreateDefaultSubobject<UCameraComponent>("CameraComponent");
             CameraComponent->SetupAttachment(RootComponent);

             MeshComponent = CreateDefaultSubobject<USkeletalMeshComponent>("CharacterMesh");
             MeshComponent->SetupAttachment(CameraComponent);
         }
         ```

    测试逻辑：
    - 调用 `parse_uasset_with_linker('BP_FirstPersonCharacter.uasset', tolerant=True)`
    - 调用 `translate_blueprint_to_cpp(result)`
    - 调用 `write_cpp_class_files(ir, None)`
    - 验证生成的 IR 包含：
      - 类名：ABP_FirstPersonCharacter（或类似）
      - 父类：ACharacter
      - Properties：至少有 CameraComponent, MeshComponent
      - Methods：可能为空（如果 decompiled_functions 为空）
      - Constructor：有 CreateDefaultSubobject 调用
    - 验证生成的 .h 文件包含关键结构（UCLASS, class declaration, UPROPERTY）
    - 验证生成的 .cpp 文件包含关键结构（constructor, includes）
    - 使用 `_compare_with_golden()` 比较关键部分（允许部分匹配）

    Per D-66-05: 如果 decompiled_functions 为空，测试验证骨架生成（不要求方法体）。
    Per D-66-06: 如果 graphs 数据不完整（linked_to_raw stub），测试验证 fallback 策略生效（使用 function_reference）。
    Per D-66-07: Golden file 比较只验证关键部分（类名、继承、属性列表），不要求完全匹配。

    实现顺序：
    - RED: 先创建 golden files 和测试（测试会失败因为管线未完成）
    - GREEN: Wave 1 完成 → 测试应该通过
    - REFACTOR: 清理测试代码，添加更多 edge cases
  </action>
  <verify>
    <automated>python -m pytest tests/test_agent_translation.py -v --tb=short</automated>
  </verify>
  <done>
    - tests/test_agent_translation.py exists
    - tests/golden/agent/ directory created
    - BP_FirstPersonCharacter.h.expected created
    - BP_FirstPersonCharacter.cpp.expected created
    - test_translate_bp_first_person_character passes
    - test_translate_empty_blueprint passes
    - test_translate_with_no_decompiled_functions passes
    - Integration test validates class inheritance (ACharacter)
    - Integration test validates component properties
    - Integration test validates constructor initialization
  </done>
</task>

</tasks>

<verification>
## Manual verification
```bash
# Run integration test
python -m pytest tests/test_agent_translation.py::test_translate_bp_first_person_character -v

# Generate actual output for comparison
python -c "
import sys; sys.path.insert(0, 'src')
from uasset_read import parse_uasset_with_linker
from uasset_read.agent import translate_blueprint_to_cpp
from uasset_read.agent.writer import write_cpp_class_files

result = parse_uasset_with_linker('E:/Develop/lib/UnrealEngine/Samples/FirstPerson/Content/FirstPerson/Blueprints/BP_FirstPersonCharacter.uasset', tolerant=True)
ir = translate_blueprint_to_cpp(result)
files = write_cpp_class_files(ir, 'temp/BP_FirstPersonCharacter_output')

print('Generated files:')
print(files)
"

# Compare generated files with golden
cat temp/BP_FirstPersonCharacter_output/BP_FirstPersonCharacter.h
cat tests/golden/agent/BP_FirstPersonCharacter.h.expected
```

## Automated verification
```bash
python -m pytest tests/test_agent_translation.py -v
```
</verification>

<success_criteria>
- [x] Integration test file created (test_agent_translation.py)
- [x] Golden files directory created (tests/golden/agent/)
- [x] Golden .h file created with correct structure
- [x] Golden .cpp file created with correct structure
- [x] test_translate_bp_first_person_character passes
- [x] Edge case tests pass (empty blueprint, no decompiled, no graphs)
- [x] Test validates class inheritance (ACharacter)
- [x] Test validates component properties
- [x] Test validates constructor initialization
- [x] Golden file comparison validates key sections
- [x] All commits follow git commit message format
</success_criteria>

<output>
Create `.planning/phases/phase-66/66-03-SUMMARY.md` when done
</output>