---
phase: 69-n2c-processor
plan: 03
type: execute
wave: 3
depends_on: ["69-02"]
files_modified:
  - src/uasset_read/graph/flow_builder.py
  - src/uasset_read/n2c/compat.py
  - tests/n2c/test_flow_builder_integration.py
autonomous: true
requirements:
  - PROCESSOR-01
  - PROCESSOR-02
  - PROCESSOR-03

must_haves:
  truths:
    - "_trace_execution_from_event() 使用 Registry 分发而非 if/elif class_name 链"
    - "format_node_dict() 使用 Processor 提取类型特有字段"
    - "现有 JSON 输出格式完全不变（向后兼容）"
    - "现有测试全部通过（1271 tests）"
  artifacts:
    - path: "src/uasset_read/graph/flow_builder.py"
      provides: "迁移后的图流构建函数"
      exports: ["build_execution_flows", "build_connections_map", "build_data_flows", "format_node_dict"]
    - path: "src/uasset_read/n2c/compat.py"
      provides: "向后兼容适配器（Processor 输出 -> 现有 dict 格式）"
      exports: ["definition_to_node_dict"]
  key_links:
    - from: "src/uasset_read/graph/flow_builder.py"
      to: "src/uasset_read/n2c/processor_registry.py"
      via: "import N2CProcessorRegistry"
      pattern: "from uasset_read.n2c.processor_registry import N2CProcessorRegistry"
    - from: "src/uasset_read/n2c/compat.py"
      to: "src/uasset_read/n2c/definitions.py"
      via: "import N2CNodeDefinition"
      pattern: "N2CNodeDefinition.to_dict()"
---

<objective>
将 `flow_builder.py` 中的节点类型分派链替换为 Processor Registry 调用，保持 JSON 输出向后兼容。

Purpose: 实现 Phase 69 的主要重构目标 — 消除 `if node.class_name == "K2Node_X"` 模式，使用 O(1) 字典查找分发。
Output: 重构后的 flow_builder.py + 兼容层 + 集成测试。
</objective>

<execution_context>
@E:\Develop\uasset_read\.claude\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\phases\phase-69\RESEARCH.md
</context>

<interfaces>
<!-- Current dispatch patterns in flow_builder.py that will be replaced -->
From src/uasset_read/graph/flow_builder.py:
```python
# L520-578: _trace_execution_from_event() — 4 if blocks to replace
if current_node.class_name == "K2Node_CallFunction":
    # 提取 function_name, parameters, pure, data_providers
if current_node.class_name == "K2Node_Event":
    # 提取 event_name
if current_node.class_name == "K2Node_FunctionEntry":
    # 提取 function_name
if current_node.class_name in CONTROL_FLOW_NODES:
    # 提取 branch_type
```

<!-- From 69-01 — N2CNodeDefinition -->
```python
@dataclass
class N2CNodeDefinition:
    node_id: str
    node_type: N2CNodeType
    position: Tuple[int, int]
    comment: str = ""
    input_pins: List[Dict] = field(default_factory=list)
    output_pins: List[Dict] = field(default_factory=list)
    extra_data: Dict[str, Any] = field(default_factory=dict)
```

<!-- From 69-02 — Processor Registry interface -->
```python
class N2CProcessorRegistry:
    @classmethod
    def get_instance(cls) -> "N2CProcessorRegistry": ...
    def process_node(self, node, node_type, definition) -> bool: ...
```

<!-- From Phase 68 — assumed existing -->
```python
class N2CNodeTypeRegistry:
    def resolve(self, class_name: str) -> N2CNodeType: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建向后兼容适配器 (n2c/compat.py)</name>
  <files>src/uasset_read/n2c/compat.py</files>
  <action>创建 `n2c/compat.py`，提供 `definition_to_node_dict()` 函数：

该函数接受 `N2CNodeDefinition` 实例和辅助参数（node_name, node_guid, original_class_name, pins），输出与现有 `format_node_dict()` 完全一致的 dict 结构：
```python
{
    "node_name": node_name,
    "node_type": original_class_name,
    "node_guid": node_guid,
    "position": {"x": definition.position[0], "y": definition.position[1]},
    "node_comment": definition.comment,
    "pins": pins,
    "function_reference": {...},     # 如果 extra_data 有 member_name
    "event_reference": {...},        # 如果 extra_data 有 event_name
    "branch_type": ...,              # 如果 extra_data 有
    "pure": ...,                     # 如果 extra_data 有
    "function_name": ...,            # 如果 extra_data 有
    "event_name": ...,               # 如果 extra_data 有
}
```

逻辑：
1. 从 `definition.extra_data` 读取类型特有字段
2. 映射到现有 OUT-01 格式的键名（member_name -> function_reference, event_name -> event_reference）
3. 仅包含非 None 的字段（与现有逻辑一致）

关键：此函数保证 Processor 重构后的输出与重构前完全相同。</action>
  <verify>
    <automated>python -c "from uasset_read.n2c.compat import definition_to_node_dict; print('compat OK')"</automated>
  </verify>
  <done>definition_to_node_dict() 可用，输出包含 function_reference 等字段的 dict</done>
</task>

<task type="auto">
  <name>Task 2: 重构 _trace_execution_from_event() 使用 Processor 分发</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <action>替换 `_trace_execution_from_event()` (L472-584) 中的 4 个 if 块：

**当前模式（L520-578）：**
```python
if current_node.class_name == "K2Node_CallFunction":
    nd = current_node.node_data
    if nd:
        fr = nd.get("function_reference") if isinstance(nd, dict) else getattr(nd, 'function_reference', None)
        if fr:
            node_info["function_name"] = getattr(fr, 'member_name', None)
    # ... pure detection, parameters, data_providers
if current_node.class_name == "K2Node_Event":
    nd = current_node.node_data
    if nd:
        er = nd.get("event_reference") if isinstance(nd, dict) else getattr(nd, 'event_reference', None)
        if er:
            node_info["event_name"] = getattr(er, 'member_name', None)
if current_node.class_name == "K2Node_FunctionEntry":
    nd = current_node.node_data
    if nd:
        fr = nd.get("function_reference") if isinstance(nd, dict) else getattr(nd, 'function_reference', None)
        if fr:
            node_info["function_name"] = getattr(fr, 'member_name', None)
if current_node.class_name in CONTROL_FLOW_NODES:
    branch_type = BRANCH_TYPE_MAP.get(current_node.class_name, "unknown")
    node_info["branch_type"] = branch_type
    node_info["stopped_at"] = "control_flow_node"
```

**新模式：**
在函数顶部添加导入：
```python
from uasset_read.n2c.processor_registry import N2CProcessorRegistry
from uasset_read.n2c.definitions import N2CNodeDefinition
```

在每个节点处理循环中（L519 之后）替换 4 个 if 块为：
```python
# 使用 Processor 分发
node_type = _resolve_node_type(current_node.class_name)  # Phase 68 或临时映射
definition = N2CNodeDefinition(
    node_id=current_guid,
    node_type=node_type,
    position=(current_node.node_pos_x, current_node.node_pos_y),
    comment=current_node.node_comment,
    input_pins=[...],  # 从 node.pins 构建
    output_pins=[...],
)
N2CProcessorRegistry.get_instance().process_node(current_node, node_type, definition)

# 从 extra_data 读取填充 node_info
if definition.extra_data.get("member_name"):
    node_info["function_name"] = definition.extra_data["member_name"]
if definition.extra_data.get("event_name"):
    node_info["event_name"] = definition.extra_data["event_name"]
if definition.extra_data.get("branch_type"):
    node_info["branch_type"] = definition.extra_data["branch_type"]
    node_info["stopped_at"] = "control_flow_node"
if definition.extra_data.get("pure"):
    node_info["pure"] = True
```

**临时类型解析**（Phase 68 未就绪时）：
在 flow_builder.py 中添加 `_resolve_node_type(class_name)` 函数，使用硬编码的 class_name -> N2CNodeType 映射。Phase 68 就绪后替换为 `N2CNodeTypeRegistry.resolve()`。

**保留逻辑**（不在 Processor 中）：
- `_extract_call_function_parameters()` 调用保留（Phase 54 数据流追踪）
- `data_providers` 提取逻辑保留（Phase 54 正向数据流追踪）
- `cycle_detected` 和 `missing node_guid` 处理保留

注意：`node_info["parameters"]` 的提取仍走原有路径（调用 `_extract_call_function_parameters`），这是数据流标注逻辑，与 Processor 的语义提取职责不同。</action>
  <verify>
    <automated>python -m pytest tests/ -k "execution" -x -q --timeout=30</automated>
  </verify>
  <done>_trace_execution_from_event() 使用 Processor 分发，相关测试通过</done>
</task>

<task type="auto">
  <name>Task 3: 重构 format_node_dict() 使用 Processor</name>
  <files>src/uasset_read/graph/flow_builder.py</files>
  <action>**重构 format_node_dict() (L107-175):**

当前模式 (L137-168)：
```python
if node.node_data is not None:
    nd = node.node_data
    def _get(key):
        if isinstance(nd, dict): return nd.get(key)
        return getattr(nd, key, None)
    fr = _get('function_reference')
    if fr is not None:
        result["function_reference"] = {...}
    elif _get('event_reference') is not None:
        result["event_reference"] = {...}
    elif _get('input_action_path') is not None:
        result["input_action_path"] = _get('input_action_path')
```

新模式：
1. 构建 `N2CNodeDefinition` 实例（公共字段：node_id, node_type, position, comment, pins）
2. 调用 `N2CProcessorRegistry.get_instance().process_node()` 填充 extra_data
3. 调用 `definition_to_node_dict()` 输出兼容格式
4. 保留 `parameters` 提取（CallFunction 节点专属的数据流逻辑）

具体修改：
- 替换 L137-168 的 if/elif 链为 Processor 调用 + compat 转换
- 保留 L121-134 的公共字段构建
- 保留 L171-173 的 `_extract_call_function_parameters` 调用

**_get_start_event_name() (L178-247) 保持原样**：
这个函数负责"获取用户友好的事件名"（Event.X, FunctionEntry.X 等格式），属于格式化逻辑而非语义提取。与 Processor 的"提取语义信息到 extra_data"职责不同，延后重构。</action>
  <verify>
    <automated>python -c "from uasset_read.graph.flow_builder import format_node_dict, build_execution_flows; print('imports OK')"</automated>
  </verify>
  <done>format_node_dict() 和 build_execution_flows() 导入正常</done>
</task>

<task type="auto">
  <name>Task 4: 编写集成测试验证向后兼容</name>
  <files>tests/n2c/test_flow_builder_integration.py</files>
  <action>创建集成测试验证 Processor 迁移后输出格式不变：

**test_flow_builder_integration.py:**
- `test_format_node_dict_call_function` — CallFunction 节点输出包含 function_reference
- `test_format_node_dict_event` — Event 节点输出包含 event_reference
- `test_format_node_dict_control_flow` — ControlFlow 节点输出包含 branch_type
- `test_trace_execution_from_event_call_function` — 执行流中 CallFunction 节点有 function_name
- `test_trace_execution_from_event_event` — 执行流中 Event 节点有 event_name
- `test_unknown_node_type_uses_fallback` — 未知类型使用 FallbackProcessor 不崩溃

使用 conftest.py 的 fixtures 创建 mock 节点。每个测试在 setup 中调用 `register_all_processors()` 注册处理器。

注意：这些是集成测试，验证 Processor 与 flow_builder.py 的协作，不是单元测试。</action>
  <verify>
    <automated>python -m pytest tests/n2c/test_flow_builder_integration.py -x -q</automated>
  </verify>
  <done>集成测试全部通过（6 个测试）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow_builder.py → N2CNodeTypeRegistry | Phase 68 尚未就绪时的临时映射必须安全降级 |
| Processor output → JSON formatter | extra_data 字段名必须与 OUT-01 格式键名一致 |
| Legacy code → new code | 过渡期间新旧逻辑并存，不得产生双重处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-08 | Availability | Type resolution fallback | mitigate | Phase 68 不可用时使用硬编码映射 + warning，不静默跳过 |
| T-69-09 | Tampering | JSON output compatibility | mitigate | definition_to_node_dict() 作为唯一输出转换点，确保 OUT-01 格式不变 |
| T-69-10 | Denial of Service | Processor exception in hot path | mitigate | Registry.process_node() try/except 包裹，异常不传播到 flow_builder 调用方 |
| T-69-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `python -m pytest tests/n2c/ -x -q` — 所有 n2c 测试通过（含集成测试）
- `python -m pytest tests/ -x -q --ignore=tests/n2c/` — 前 50 个现有测试通过（无回归）
- `python -m pytest tests/test_blueprint_extraction.py -x -q` — 蓝图提取端到端测试通过
- `python -c "from uasset_read.graph.flow_builder import build_execution_flows, format_node_dict"` — 导入正常
</verification>

<success_criteria>
- `_trace_execution_from_event()` 中的 4 个 if 块被 Processor 分发替代
- `format_node_dict()` 使用 Processor 填充 N2CNodeDefinition
- `_get_start_event_name()` 保持原样（格式化逻辑，非 Processor 职责）
- 现有 JSON 输出格式完全不变（OUT-01 兼容）
- 集成测试验证输出一致性
- 所有 1271 个现有测试继续通过
- `n2c/compat.py` 提供可靠的向后兼容转换
</success_criteria>

<output>
Create `.planning/phases/phase-69/69-03-SUMMARY.md` when done
</output>
