---
phase: 69-n2c-processor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/n2c/__init__.py
  - src/uasset_read/n2c/definitions.py
  - src/uasset_read/n2c/processor_base.py
  - src/uasset_read/n2c/processor_registry.py
  - src/uasset_read/n2c/processors/__init__.py
  - tests/n2c/__init__.py
  - tests/n2c/conftest.py
autonomous: true
requirements:
  - PROCESSOR-01

must_haves:
  truths:
    - "N2CNodeProcessor 抽象基类可被继承并实现 process() 方法"
    - "N2CProcessorRegistry 单例可注册和分发处理器"
    - "N2CNodeDefinition 可承载通用字段和类型特有 extra_data"
    - "注册表对未注册类型返回 Fallback 处理器而非抛异常"
  artifacts:
    - path: "src/uasset_read/n2c/processor_base.py"
      provides: "N2CNodeProcessor 抽象基类"
      exports: ["N2CNodeProcessor"]
    - path: "src/uasset_read/n2c/processor_registry.py"
      provides: "N2CProcessorRegistry 单例注册中心"
      exports: ["N2CProcessorRegistry"]
    - path: "src/uasset_read/n2c/definitions.py"
      provides: "N2CNodeDefinition 数据类"
      exports: ["N2CNodeDefinition"]
    - path: "tests/n2c/conftest.py"
      provides: "共享测试 fixtures"
      exports: ["mock_node", "mock_pin"]
  key_links:
    - from: "src/uasset_read/n2c/processor_registry.py"
      to: "src/uasset_read/n2c/processor_base.py"
      via: "import N2CNodeProcessor"
      pattern: "from.*processor_base import N2CNodeProcessor"
    - from: "src/uasset_read/n2c/processor_registry.py"
      to: "src/uasset_read/n2c/definitions.py"
      via: "import N2CNodeDefinition (TYPE_CHECKING)"
      pattern: "from.*definitions import N2CNodeDefinition"
---

<objective>
建立 N2C 节点处理器架构的核心基础设施：抽象基类、注册表、定义数据结构和测试骨架。

Purpose: 为后续具体 Processor 实现和 flow_builder.py 迁移提供基础层。按 RESEARCH.md Option A（策略模式 + 类装饰器自动注册）方案实现。
Output: `n2c/` 目录下的核心模块文件 + 测试 fixtures。
</objective>

<execution_context>
@E:\Develop\uasset_read\.claude\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md
@E:\Develop\uasset_read\.planning\phases\phase-69\RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/uasset_read/models/core.py:
```python
@dataclass
class UEdGraphNode:
    node_guid: str
    node_pos_x: int = 0
    node_pos_y: int = 0
    node_comment: str = ""
    pins: List["UEdGraphPin"] = field(default_factory=list)
    class_name: str = ""
    node_data: Optional[Any] = None

@dataclass
class UEdGraphPin:
    pin_id: str
    pin_name: str
    pin_type: Optional[FEdGraphPinType] = None
    direction: int = 0
    default_value: Optional[str] = None
    linked_to_raw: List[dict] = field(default_factory=list)
    # ... more fields

@dataclass
class FEdGraphPinType:
    pin_category: str = ""
    pin_subcategory: str = ""
    # ... more fields
```

From src/uasset_read/constants.py:
```python
CONTROL_FLOW_NODES = frozenset({
    "K2Node_IfThenElse", "K2Node_Switch", "K2Node_SwitchString",
    "K2Node_SwitchEnum", "K2Node_SwitchInteger", "K2Node_MacroInstance",
})

START_EVENT_TYPES = frozenset({
    "K2Node_Event", "K2Node_EnhancedInputAction", "K2Node_VariableSet",
    "K2Node_CustomEvent", "K2Node_FunctionEntry",
})
```

From src/uasset_read/graph/flow_builder.py (L137-175):
```python
# node_data access pattern — dict or dataclass
nd = node.node_data
if isinstance(nd, dict):
    fr = nd.get('function_reference')
else:
    fr = getattr(nd, 'function_reference', None)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 N2CNodeDefinition 数据类</name>
  <files>src/uasset_read/n2c/__init__.py, src/uasset_read/n2c/definitions.py</files>
  <action>创建 `src/uasset_read/n2c/` 目录和 `__init__.py`。

在 `definitions.py` 中定义 `N2CNodeDefinition` dataclass，字段包括：
- `node_id: str` — 短 ID 或 GUID
- `node_type: "N2CNodeType"` — 语义类型（字符串类型注解，Phase 68 提供，此处使用 forward ref）
- `position: Tuple[int, int]` — (x, y) 坐标
- `comment: str = ""` — 节点注释
- `input_pins: List[Dict] = field(default_factory=list)` — 输入引脚
- `output_pins: List[Dict] = field(default_factory=list)` — 输出引脚
- `extra_data: Dict[str, Any] = field(default_factory=dict)` — 类型特有字段

提供 `to_dict()` 方法，输出与现有 `format_node_dict()` 兼容的 dict 结构（向后兼容 OUT-01 格式）。

注意：`n2c/__init__.py` 使用惰性导入避免循环依赖：
```python
from uasset_read.n2c.definitions import N2CNodeDefinition
```

不使用 TYPE_CHECKING 块（这是数据类，无外部依赖），确保可独立导入。</action>
  <verify>
    <automated>python -c "from uasset_read.n2c.definitions import N2CNodeDefinition; d = N2CNodeDefinition(node_id='N1', node_type='CallFunction', position=(0,0)); print(d.to_dict())"</automated>
  </verify>
  <done>N2CNodeDefinition 可实例化，to_dict() 返回包含 node_name/node_type/position/node_comment/pins/extra_data 键的 dict</done>
</task>

<task type="auto">
  <name>Task 2: 创建 N2CNodeProcessor 抽象基类</name>
  <files>src/uasset_read/n2c/processor_base.py</files>
  <action>在 `processor_base.py` 中定义 `N2CNodeProcessor` 抽象基类：

```python
class N2CNodeProcessor(ABC):
    @property
    @abstractmethod
    def node_types(self) -> list["N2CNodeType"]:
        """此处理器支持的语义类型列表。"""

    @abstractmethod
    def process(self, node: "UEdGraphNode", definition: "N2CNodeDefinition") -> None:
        """提取节点特有属性到输出定义。"""
```

全部使用 `TYPE_CHECKING` 块 + 字符串类型注解避免循环依赖。

提供 `can_process(node_type: N2CNodeType) -> bool` 辅助方法。

注意：基类不依赖 Phase 68 的具体实现，仅使用字符串类型注解。</action>
  <verify>
    <automated>python -c "from uasset_read.n2c.processor_base import N2CNodeProcessor; print('ABC imported OK')"</automated>
  </verify>
  <done>N2CNodeProcessor 抽象基类可导入，尝试直接实例化会抛 TypeError（ABC 保护）</done>
</task>

<task type="auto">
  <name>Task 3: 创建 N2CProcessorRegistry 单例注册中心</name>
  <files>src/uasset_read/n2c/processor_registry.py</files>
  <action>在 `processor_registry.py` 中定义 `N2CProcessorRegistry` 类：

核心方法：
- `get_instance()` — 单例访问
- `reset()` — 测试用重置（类方法，清空 `_instance`）
- `register(processor: N2CNodeProcessor)` — 注册处理器，检测重复类型抛 ValueError
- `set_fallback(processor: N2CNodeProcessor)` — 设置默认回退处理器
- `get_processor(node_type: N2CNodeType) -> Optional[N2CNodeProcessor]` — 查找处理器
- `process_node(node, node_type, definition) -> bool` — 统一入口，调用对应处理器

使用 `TYPE_CHECKING` 块导入，所有类型注解使用字符串。

使用 `logging.getLogger(__name__)` 记录 warning（未注册类型、重复注册等）。

关键实现细节：
- `_processors: dict[N2CNodeType, N2CNodeProcessor]` 内部字典
- `register()` 遍历 `processor.node_types`，每个类型检查是否已注册
- `get_processor()` 找不到时返回 `_fallback`
- `process_node()` 无处理器时 log warning 返回 False

注意：`N2CNodeType` 来自 Phase 68，使用 `TYPE_CHECKING` + 字符串注解。</action>
  <verify>
    <automated>python -c "from uasset_read.n2c.processor_registry import N2CProcessorRegistry; r = N2CProcessorRegistry.get_instance(); print('Registry OK')"</automated>
  </verify>
  <done>单例可创建，register/get_processor/reset 方法可用，未注册类型返回 None</done>
</task>

<task type="auto">
  <name>Task 4: 创建测试目录结构和共享 fixtures</name>
  <files>tests/n2c/__init__.py, tests/n2c/conftest.py</files>
  <action>创建 `tests/n2c/` 目录和 `__init__.py`。

在 `conftest.py` 中定义共享 pytest fixtures：

```python
@pytest.fixture
def mock_pin():
    """创建模拟 UEdGraphPin。"""
    def _make(pin_id="test_pin", pin_name="TestPin", pin_type=None,
              direction=0, default_value="", linked_to_raw=None):
        return UEdGraphPin(
            pin_id=pin_id, pin_name=pin_name, pin_type=pin_type or FEdGraphPinType(),
            direction=direction, default_value=default_value,
            linked_to_raw=linked_to_raw or []
        )
    return _make

@pytest.fixture
def mock_node():
    """创建模拟 UEdGraphNode。"""
    def _make(class_name="K2Node_TestNode", node_data=None, pins=None,
              node_guid="test-guid-001", pos_x=0, pos_y=0, comment=""):
        return UEdGraphNode(
            node_guid=node_guid, node_pos_x=pos_x, node_pos_y=pos_y,
            node_comment=comment, class_name=class_name,
            node_data=node_data, pins=pins or []
        )
    return _make

@pytest.fixture(autouse=True)
def reset_registry():
    """每个测试前自动重置注册表单例。"""
    yield
    N2CProcessorRegistry.reset()
```

注意：需要在 conftest 顶部导入必要的模块。</action>
  <verify>
    <automated>python -m pytest tests/n2c/ --co -q</automated>
  </verify>
  <done>pytest 收集测试目录，fixtures 可用（暂无测试文件，但目录结构应正确）</done>
</task>

<task type="auto">
  <name>Task 5: 编写核心基础设施测试</name>
  <files>tests/n2c/test_definitions.py, tests/n2c/test_registry.py, tests/n2c/test_processor_base.py</files>
  <action>创建三个测试文件：

**test_definitions.py:**
- `test_definition_creation` — 验证所有字段可正确设置
- `test_definition_defaults` — 验证默认值（extra_data 为空 dict）
- `test_definition_to_dict_basic` — 验证 to_dict() 包含必需键
- `test_definition_to_dict_extra_data` — 验证 extra_data 合并到输出

**test_registry.py:**
- `test_singleton_creation` — get_instance() 返回同一实例
- `test_reset_clears_singleton` — reset() 后 get_instance() 返回新实例
- `test_register_and_get_processor` — 注册后正确返回
- `test_duplicate_registration_raises` — 重复注册抛 ValueError
- `test_get_processor_unknown_type` — 未知类型返回 None（无 fallback）
- `test_fallback_processor_used` — 设置 fallback 后未知类型返回它
- `test_process_node_success` — process_node 正确调用处理器
- `test_process_node_no_processor` — 无处理器时返回 False

**test_processor_base.py:**
- `test_cannot_instantiate_abstract` — ABC 阻止直接实例化
- `test_concrete_implementation_works` — 创建测试用具体处理器验证 ABC 接口

使用 conftest.py 的 fixtures 创建 mock 对象。测试应完全独立，不依赖 Phase 68 的实现。</action>
  <verify>
    <automated>python -m pytest tests/n2c/ -x -q</automated>
  </verify>
  <done>所有核心基础设施测试通过（预计 15+ 个测试）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry → Processor | 注册表调用处理器的 process() 方法，异常不应扩散到调用方 |
| Node Data → Definition | node_data 可能为 dict 或 dataclass，也可能为 None |
| Test Fixtures → Production | 测试 fixture 不应污染生产单例状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Availability | N2CProcessorRegistry.process_node() | mitigate | try/except 包裹处理器调用，记录 warning 返回 False，不抛异常到调用方 |
| T-69-02 | Tampering | N2CProcessorRegistry.register() | mitigate | 重复注册检测，ValueError 明确告知冲突的 Processor 类名 |
| T-69-03 | Information Disclosure | Logging | accept | 仅记录 node_type 和处理结果，不记录 node_data 内容（可能含敏感信息） |
| T-69-04 | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `python -m pytest tests/n2c/ -x -q` — 所有 n2c 测试通过
- `python -c "from uasset_read.n2c import N2CNodeDefinition, N2CNodeProcessor, N2CProcessorRegistry"` — 顶层导入可用
- 现有测试不受影响：`python -m pytest tests/ -x -q --ignore=tests/n2c/` 前 20 个测试通过
</verification>

<success_criteria>
- `n2c/` 目录创建，包含 `__init__.py`, `definitions.py`, `processor_base.py`, `processor_registry.py`
- `tests/n2c/` 目录创建，包含 `conftest.py` 和 3 个测试文件
- N2CNodeDefinition 可实例化且 to_dict() 返回正确结构
- N2CNodeProcessor 抽象基类阻止直接实例化
- N2CProcessorRegistry 单例可注册、查找、分发处理器
- Fallback 机制正确工作
- 15+ 单元测试全部通过
- 现有 1271 个测试不受影响（新目录无冲突）
</success_criteria>

<output>
Create `.planning/phases/phase-69/69-01-SUMMARY.md` when done
</output>
