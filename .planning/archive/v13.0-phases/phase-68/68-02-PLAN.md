---
phase: 68
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - src/uasset_read/n2c/type_registry.py
  - src/uasset_read/n2c/__init__.py
autonomous: true
requirements:
  - REGISTRY-01
must_haves:
  truths:
    - "N2CNodeTypeRegistry 单例可通过 get_instance() 访问"
    - "resolve(class_name) 精确匹配已知类型（K2Node_CallFunction → CallFunction）"
    - "resolve(class_name) 继承回退查找子类（K2Node_CallArrayFunction → 查继承链 → CallFunction）"
    - "resolve(class_name) 未知类型返回 Unknown"
    - "resolve() 使用缓存避免重复查找"
  artifacts:
    - path: "src/uasset_read/n2c/type_registry.py"
      provides: "N2CNodeTypeRegistry 单例类"
      exports: ["N2CNodeTypeRegistry"]
      contains: "class N2CNodeTypeRegistry"
    - path: "src/uasset_read/n2c/__init__.py"
      provides: "模块导出更新"
      contains: "from .type_registry import N2CNodeTypeRegistry"
  key_links:
    - from: "src/uasset_read/n2c/type_registry.py"
      to: "src/uasset_read/n2c/type_data.py"
      via: "import K2NODE_INHERITANCE, K2NODE_ENUM_NAMES"
      pattern: "from.*type_data import"
    - from: "src/uasset_read/n2c/type_registry.py"
      to: "src/uasset_read/n2c/node_types.py"
      via: "import N2CNodeType（延迟导入）"
      pattern: "from.*node_types import N2CNodeType"
    - from: "resolve()"
      to: "_inheritance_map"
      via: "while loop 沿继承链查找"
      pattern: "while current in.*_inheritance_map"
---

<objective>
Wave 2: 实现 N2CNodeTypeRegistry 单例类，提供 class_name → N2CNodeType 的精确匹配和继承链回退查找，使用缓存优化性能。

Purpose: 为 Phase 69 Processor Registry 和 flow_builder._resolve_node_type() 提供权威类型解析服务。
Output: type_registry.py 模块 + 导出到 n2c 包。
</objective>

<execution_context>
@E:\Develop\uasset_read\.claude\get-shit-done\workflows\execute-plan.md
@E:\Develop\uasset_read\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\Develop\uasset_read\.planning\ROADMAP.md
@E:\Develop\uasset_read\.planning\STATE.md
@E:\Develop\uasset_read\.planning\phases\phase-68\68-RESEARCH.md
</context>

<interfaces>
From src/uasset_read/n2c/type_data.py (Wave 1):
```python
K2NODE_TYPES: list[str]
K2NODE_INHERITANCE: dict[str, str]  # class_name → parent_class_name
K2NODE_ENUM_NAMES: dict[str, str]   # class_name → enum_member_name
def get_parent_chain(class_name: str) -> list[str]
```

From src/uasset_read/n2c/node_types.py (Wave 1):
```python
class N2CNodeType(Enum):
    # 126 种枚举值，包含原有的 30 种 + Wave 1 新增的 96 种
    CallFunction = "CallFunction"
    # ... 126 total
    Unknown = "Unknown"
```

From src/uasset_read/n2c/processor_registry.py (Phase 69, existing):
```python
class N2CProcessorRegistry:
    def get_processor(self, node_type: N2CNodeType) -> Optional[N2CNodeProcessor]
    def process_node(self, node, node_type, definition) -> bool
```

resolve() 查找顺序（RESEARCH.md Pattern 1）：
1. 精确匹配 _type_map[K2Node_CallFunction] → CallFunction
2. 缓存命中 _resolve_cache[class_name]
3. 继承链查找：K2Node_CallArrayFunction → K2Node_CallFunction → _type_map → CallFunction
4. 返回 Unknown（所有路径失败）
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 N2CNodeTypeRegistry 单例类</name>
  <files>src/uasset_read/n2c/type_registry.py</files>
  <action>创建 `src/uasset_read/n2c/type_registry.py`，实现 N2CNodeTypeRegistry 单例类。

核心实现：
```python
"""K2Node 类型注册表 — 单例模式 + 继承回退 + 缓存。

将 UE K2Node class_name 解析为 N2CNodeType 语义类型枚举。
支持精确匹配和继承链回退查找。
"""
from __future__ import annotations

import logging
from typing import TYPE_CHECKING, Dict, Optional

from uasset_read.n2c.type_data import K2NODE_ENUM_NAMES, K2NODE_INHERITANCE

if TYPE_CHECKING:
    from uasset_read.n2c.node_types import N2CNodeType

logger = logging.getLogger(__name__)


class N2CNodeTypeRegistry:
    """K2Node 类名 → 语义类型注册表（单例）。"""

    _instance: Optional[N2CNodeTypeRegistry] = None

    def __init__(self) -> None:
        self._type_map: Dict[str, N2CNodeType] = {}
        self._inheritance_map = K2NODE_INHERITANCE
        self._resolve_cache: Dict[str, N2CNodeType] = {}
        self._initialized = False

    @classmethod
    def get_instance(cls) -> N2CNodeTypeRegistry:
        if cls._instance is None:
            cls._instance = cls()
        return cls._instance

    @classmethod
    def reset(cls) -> None:
        cls._instance = None

    def _ensure_initialized(self) -> None:
        if self._initialized:
            return
        from uasset_read.n2c.node_types import N2CNodeType
        for class_name, enum_name in K2NODE_ENUM_NAMES.items():
            try:
                self._type_map[class_name] = N2CNodeType[enum_name]
            except KeyError:
                logger.warning("No enum member '%s' for class '%s'", enum_name, class_name)
        self._initialized = True

    def resolve(self, class_name: str) -> N2CNodeType:
        """解析 class_name 到 N2CNodeType，支持继承回退。

        查找顺序：精确匹配 → 缓存 → 继承链 → Unknown
        """
        self._ensure_initialized()

        # 精确匹配
        if class_name in self._type_map:
            return self._type_map[class_name]

        # 缓存命中
        if class_name in self._resolve_cache:
            return self._resolve_cache[class_name]

        # 继承链查找
        from uasset_read.n2c.node_types import N2CNodeType
        current = class_name
        visited = set()
        max_depth = 10

        while current in self._inheritance_map and len(visited) < max_depth:
            if current in visited:
                break
            visited.add(current)
            parent = self._inheritance_map[current]
            if parent in self._type_map:
                result = self._type_map[parent]
                self._resolve_cache[class_name] = result
                return result
            current = parent

        # Unknown fallback
        self._resolve_cache[class_name] = N2CNodeType.Unknown
        return N2CNodeType.Unknown

    def get_registered_types(self) -> list[str]:
        """返回所有已注册的 class_name 列表（诊断用）。"""
        self._ensure_initialized()
        return sorted(self._type_map.keys())
```

关键设计：
- 延迟初始化：`_ensure_initialized()` 在首次 resolve() 时填充 _type_map，避免导入循环
- 继承回退：沿 K2NODE_INHERITANCE 向上查找，最多 10 层（visited set 循环保护）
- 缓存：_resolve_cache dict 缓存已解析结果（包含 Unknown）
- 诊断方法：get_registered_types() 返回已注册的 class_name 列表
- 测试支持：reset() 清空单例，conftest fixture 可用</action>
  <verify>
    <automated>python -c "from uasset_read.n2c.type_registry import N2CNodeTypeRegistry; r = N2CNodeTypeRegistry.get_instance(); types = r.get_registered_types(); print(f'Registered: {len(types)} types'); assert len(types) > 100, f'Expected 100+, got {len(types)}'"</automated>
  </verify>
  <done>N2CNodeTypeRegistry 可创建，get_registered_types() 返回 100+ 已注册类型</done>
</task>

<task type="auto">
  <name>Task 2: 更新 n2c/__init__.py 导出 TypeRegistry</name>
  <files>src/uasset_read/n2c/__init__.py</files>
  <action>更新 `src/uasset_read/n2c/__init__.py`，导出 N2CNodeTypeRegistry。

当前导出：
```python
__all__ = ["N2CNodeDefinition", "N2CNodeProcessor", "N2CProcessorRegistry", "N2CNodeType"]
```

添加 N2CNodeTypeRegistry：
```python
from uasset_read.n2c.type_registry import N2CNodeTypeRegistry

__all__ = [
    "N2CNodeDefinition",
    "N2CNodeProcessor",
    "N2CProcessorRegistry",
    "N2CNodeType",
    "N2CNodeTypeRegistry",  # Phase 68 新增
]
```

保持与 Phase 69 导出不冲突。</action>
  <verify>
    <automated>python -c "from uasset_read.n2c import N2CNodeTypeRegistry; print('N2CNodeTypeRegistry importable from uasset_read.n2c')"</automated>
  </verify>
  <done>N2CNodeTypeRegistry 可从 uasset_read.n2c 导入</done>
</task>

<task type="auto">
  <name>Task 3: 编写 TypeRegistry 单元测试</name>
  <files>tests/n2c/test_type_registry.py</files>
  <action>创建 `tests/n2c/test_type_registry.py`，测试注册表核心功能。

测试用例：

**单例管理**：
- test_singleton_creation — get_instance() 返回同一实例
- test_reset_clears_singleton — reset() 后 get_instance() 返回新实例

**精确匹配**：
- test_resolve_exact_match_call_function — "K2Node_CallFunction" → CallFunction
- test_resolve_exact_match_event — "K2Node_Event" → Event
- test_resolve_exact_match_variable_get — "K2Node_VariableGet" → VariableGet

**继承回退**：
- test_resolve_inheritance_fallback_call_array — "K2Node_CallArrayFunction" 继承 K2Node_CallFunction，但 CallArrayFunction 本身有枚举值，应返回 CallArrayFunction（精确匹配优先）
- test_resolve_inheritance_fallback_struct_member — "K2Node_StructMemberSet" 有枚举值 StructMemberSet，精确匹配
- test_resolve_deep_inheritance — 3 层继承链回退测试（如某类型无枚举值时正确回退到祖先）

**Unknown 回退**：
- test_resolve_unknown_type — "K2Node_NonExistentType" → Unknown
- test_resolve_unknown_cached — 第二次 resolve 同一未知类型，缓存命中

**缓存**：
- test_resolve_cache_populated — resolve 后 _resolve_cache 包含结果
- test_resolve_cache_hit — 第二次 resolve 同一类型返回相同值

**诊断方法**：
- test_get_registered_types — 返回非空排序列表，包含已知类型
- test_get_registered_types_count — 数量 >= 100

**conftest fixture**：
- 创建 tests/n2c/conftest.py（如不存在），包含 `reset_registry` autouse fixture：
```python
@pytest.fixture(autouse=True)
def reset_registry():
    from uasset_read.n2c.type_registry import N2CNodeTypeRegistry
    N2CNodeTypeRegistry.reset()
    yield
    N2CNodeTypeRegistry.reset()
```</action>
  <verify>
    <automated>python -m pytest tests/n2c/test_type_registry.py -x -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>所有 TypeRegistry 测试通过（12+ 个测试用例）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry → type_data.py | 导入静态数据，无运行时修改 |
| Registry → node_types.py | 延迟导入避免循环，首次 resolve() 时加载 |
| resolve() → _resolve_cache | 内部 dict 缓存，无外部写入接口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | Tampering | 继承链循环 | mitigate | visited set + max_depth=10 双重保护 |
| T-68-05 | Tampering | 缓存污染 | accept | 内部 dict 无外部写入接口，风险极低 |
| T-68-06 | Availability | 导入循环导致启动失败 | mitigate | TYPE_CHECKING 延迟导入 + _ensure_initialized() 延迟填充 |
| T-68-SC | Tampering | pip installs | mitigate | 无外部依赖（纯标准库），slopcheck 不适用 |
</threat_model>

<verification>
- N2CNodeTypeRegistry 单例可创建和重置
- resolve() 精确匹配返回正确枚举值
- resolve() 继承回退沿继承链查找
- resolve() 未知类型返回 Unknown
- _resolve_cache 缓存填充且命中
- get_registered_types() 返回 100+ 已注册类型
- 12+ 测试用例全部通过
</verification>

<success_criteria>
- N2CNodeTypeRegistry 单例可创建，reset() 可重置
- resolve() 方法实现：精确匹配 + 缓存 + 继承回退 + Unknown fallback
- get_registered_types() 返回已注册类型列表
- 从 uasset_read.n2c 可导入 N2CNodeTypeRegistry
- 12+ 单元测试覆盖所有核心路径
- 与 Phase 69 N2CProcessorRegistry 无冲突
</success_criteria>

<output>
Create `.planning/phases/phase-68/68-02-SUMMARY.md` when done
</output>
