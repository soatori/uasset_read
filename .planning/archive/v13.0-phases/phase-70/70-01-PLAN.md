---
phase: 70-n2cstruct-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/uasset_read/n2c/__init__.py
  - src/uasset_read/n2c/schema.py
  - src/uasset_read/n2c/id_mapper.py
  - tests/n2c/__init__.py
  - tests/n2c/conftest.py
  - tests/n2c/test_schema.py
  - tests/n2c/test_id_mapper.py
autonomous: true
requirements: [SCHEMA-01]
must_haves:
  truths:
    - "N2CStruct dataclass 可以表示完整的 Blueprint graph 数据（nodes + flows + metadata）"
    - "GUID 可以被映射为短 ID（N1, N2...）并反向解析"
    - "to_n2c_json() 输出的字段名与 P69 N2CNodeDefinition.extra_data 键名对齐"
    - "JSON Schema 可以验证 N2CStruct 输出"
  artifacts:
    - path: "src/uasset_read/n2c/schema.py"
      provides: "N2CStruct/N2CGraph/N2CNode/N2CPin dataclass 定义"
      min_lines: 80
      exports: ["N2CStruct", "N2CGraph", "N2CNode", "N2CPin"]
    - path: "src/uasset_read/n2c/id_mapper.py"
      provides: "GUID <-> 短 ID 双向映射"
      exports: ["N2CIdMapper"]
    - path: "src/uasset_read/n2c/__init__.py"
      provides: "n2c 模块公共导出"
      exports: ["N2CStruct", "N2CGraph", "N2CNode", "N2CPin", "N2CIdMapper"]
  key_links:
    - from: "src/uasset_read/n2c/schema.py"
      to: "src/uasset_read/n2c/id_mapper.py"
      via: "N2CNode.id 由 id_mapper 生成"
      pattern: "N2CIdMapper"
    - from: "src/uasset_read/n2c/id_mapper.py"
      to: "src/uasset_read/graph/flow_builder.py"
      via: "消费 format_graphs_json 的节点 GUID"
      pattern: "node_guid"
---

<objective>
创建 N2CStruct 数据模型和 ID 映射器。建立 `src/uasset_read/n2c/` 模块骨架，定义核心 dataclass（N2CStruct, N2CGraph, N2CNode, N2CPin）和 GUID→短 ID 双向映射（N2CIdMapper）。

Purpose: 为后续 serializer 和 flow_extractor 提供类型基础。短 ID 是 N2C 格式 token 压缩的核心机制（~60% 节省来自 GUID→"N1" 替换）。
Output: n2c 模块 + schema.py + id_mapper.py + 测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/uasset_read/__init__.py
@src/uasset_read/graph/flow_builder.py (L107-176: format_node_dict, L661-706: build_execution_flows, L788-841: format_graphs_json)
@src/uasset_read/models/core.py (UEdGraphNode, UEdGraphPin, UEdGraph 结构)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/uasset_read/models/core.py:
```python
class UEdGraphNode:
    node_guid: str           # Full GUID like "4A3B2C1D..."
    class_name: str          # e.g. "K2Node_CallFunction"
    node_pos_x: int
    node_pos_y: int
    node_comment: str
    pins: list[UEdGraphPin]
    node_data: dict | dataclass | None

class UEdGraphPin:
    pin_id: str
    pin_name: str
    pin_type: FEdGraphPinType | None
    direction: int           # 0=Input, 1=Output
    linked_to: list[str]     # List of pin_ids
    default_value: str | None

class UEdGraph:
    graph_name: str
    graph_class: str         # e.g. "EdGraph", "UberEdGraph"
    graph_guid: str
    nodes: list[UEdGraphNode]
    schema: str | None
```

From src/uasset_read/graph/flow_builder.py L107-176 (format_node_dict output shape):
```python
{
    "node_name": str,
    "node_type": str,          # class_name raw
    "node_guid": str,          # full GUID
    "position": {"x": int, "y": int},
    "node_comment": str,
    "pins": [dict],            # sanitized pin dicts
    "function_reference": {"member_name", "member_parent", "self_context"},  # optional
    "event_reference": {"member_name", "member_parent", "member_guid"},      # optional
    "parameters": {"input_params": [...], "output_params": [...]},            # CallFunction only
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 n2c 模块骨架 + N2CStruct/N2CGraph/N2CNode/N2CPin dataclass</name>
  <read_first>
    - src/uasset_read/__init__.py (模块导出模式)
    - src/uasset_read/models/core.py (dataclass 定义风格)
    - src/uasset_read/models/node_types.py (K2Node* dataclass 模式)
  </read_first>
  <files>
    src/uasset_read/n2c/__init__.py
    src/uasset_read/n2c/schema.py
    tests/n2c/__init__.py
    tests/n2c/test_schema.py
    tests/n2c/conftest.py
  </files>
  <action>
创建 `src/uasset_read/n2c/` 模块，编写以下 dataclass（使用 Python 3.10+ dataclass，与现有 models/ 风格一致）：

**N2CPin** — 扁平化 Pin 信息，去除冗余字段：
- `pin_name: str` — Pin 名称（如 "Exec", "ReturnValue"）
- `pin_category: str` — 类别（"exec", "wildcard", "object" 等）
- `pin_subcategory: str = ""` — 子类别
- `direction: str` — "input" 或 "output"（非 0/1 整数）
- `default_value: str | None = None` — 默认值

**N2CNode** — 紧凑节点表示：
- `id: str` — 短 ID（"N1", "N2"...），由 id_mapper 生成
- `type: str` — 语义类型（如 "CallFunction", "Event"），假设 P68 N2CNodeTypeRegistry 已提供
- `name: str` — 用户友好名称
- `comment: str = ""` — 节点注释
- `pure: bool = False` — 纯函数标志
- `latent: bool = False` — 异步标志
- `input_pins: list[N2CPin]` — 输入 Pin 列表
- `output_pins: list[N2CPin]` — 输出 Pin 列表
- `extra_data: dict = field(default_factory=dict)` — 类型特有字段（与 P69 N2CNodeDefinition.extra_data 键名对齐：member_name, member_parent, event_name, b_override, branch_type 等）

**N2CGraph** — 单图表示：
- `name: str` — 图名
- `graph_type: str` — 语义化类型（"EventGraph", "Function", "Macro" 等，对齐现有 GRAPH_TYPE_MAP）
- `nodes: list[N2CNode]` — 节点列表
- `flows: dict` — 包含 "execution" (list[str]) 和 "data" (dict[str, str])

**N2CStruct** — 顶层输出容器：
- `version: str = "1.0.0"` — Schema 版本
- `metadata: dict` — Blueprint 元数据（Name, BlueprintType, BlueprintClass）
- `graphs: list[N2CGraph]` — 图列表
- `structs: list[dict] = field(default_factory=list)` — 结构体定义占位
- `enums: list[dict] = field(default_factory=list)` — 枚举定义占位

为每个 dataclass 提供 `to_dict()` 方法用于 JSON 序列化。

**Knot 处理规则：** Knot 节点（class_name 包含 "Knot"）在 N2C 转换中完全省略（穿透连接）。这是 RESEARCH.md 的明确要求。

n2c/__init__.py 导出 N2CStruct, N2CGraph, N2CNode, N2CPin。
</action>
<verify>
  <automated>python -m pytest tests/n2c/test_schema.py -x -q</automated>
</verify>
<behavior>
  - N2CPin.to_dict() 返回包含 pin_name, pin_category, pin_subcategory, direction, default_value 的 dict
  - N2CNode(id="N1", type="CallFunction", name="Print String").to_dict() 返回包含 id, type, name 的 dict
  - N2CStruct().to_dict() 返回包含 version="1.0.0", metadata, graphs, structs, enums 的 dict
  - N2CStruct.to_dict()["graphs"] 中每个 graph 包含 name, graph_type, nodes, flows
  - 空 N2CStruct 的 to_dict() 中 structs 和 enums 为空 list
</behavior>
<done>N2CStruct/N2CGraph/N2CNode/N2CPin dataclass 已定义，to_dict() 方法工作，n2c 模块可导入，测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 N2CIdMapper — GUID ↔ 短 ID 双向映射</name>
  <read_first>
    - src/uasset_read/n2c/schema.py (刚创建的 N2CNode.id 字段)
    - src/uasset_read/graph/flow_builder.py L677-684 (GUID 使用模式)
  </read_first>
  <files>src/uasset_read/n2c/id_mapper.py</files>
  <action>
实现 N2CIdMapper 类，提供 GUID 到短 ID 的双向映射：

```python
class N2CIdMapper:
    """GUID ↔ 短 ID 映射器。

    用法:
        mapper = N2CIdMapper()
        short_id = mapper.to_short("4A3B2C1D-...")  # → "N1"
        guid = mapper.to_guid("N1")                   # → "4A3B2C1D-..."
    """
    def __init__(self):
        self._guid_to_short: dict[str, str] = {}
        self._short_to_guid: dict[str, str] = {}
        self._counter = 0

    def register(self, guid: str) -> str:
        """注册 GUID，返回短 ID。已注册则返回现有映射。"""

    def to_short(self, guid: str) -> str | None:
        """GUID → 短 ID。未注册返回 None。"""

    def to_guid(self, short_id: str) -> str | None:
        """短 ID → GUID。未注册返回 None。"""

    def reset(self) -> None:
        """清空映射（测试用）。"""
```

短 ID 格式：`"N{N}"`（N1, N2, N3...），按注册顺序分配。
</action>
<verify>
  <automated>python -m pytest tests/n2c/test_id_mapper.py -x -q</automated>
</verify>
<behavior>
  - mapper.register("GUID-A") == "N1"
  - mapper.register("GUID-B") == "N2"
  - mapper.register("GUID-A") == "N1" (幂等)
  - mapper.to_short("GUID-A") == "N1"
  - mapper.to_guid("N1") == "GUID-A"
  - mapper.to_short("UNKNOWN") is None
  - mapper.to_guid("N99") is None
  - mapper.reset() 后所有映射清空
</behavior>
<done>N2CIdMapper 类实现完成，双向映射正确，幂等注册，测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUID→short ID mapping | 短 ID 必须双向唯一映射，防止 ID 冲突导致节点数据错乱 |
| N2CStruct serialization | 输入数据来自已解析的 UEdGraph，需防御 malformed node/pin 数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | N2CIdMapper.register() | mitigate | 检测重复 GUID 返回同一短 ID，不允许覆盖已有映射 |
| T-70-02 | Tampering | N2CNode extra_data | mitigate | extra_data 仅由 P69 Processor 填充，schema.py 不信任外部输入 |
| T-70-SC | Tampering | npm/pip/cargo installs | mitigate | 无外部包安装，纯标准库 |
</threat_model>

<verification>
- `python -m pytest tests/n2c/ -x -q` 全部通过
- `python -c "from uasset_read.n2c import N2CStruct, N2CNode, N2CPin, N2CIdMapper"` 无错误
</verification>

<success_criteria>
- N2CStruct/N2CGraph/N2CNode/N2CPin dataclass 在 schema.py 中定义
- 每个 dataclass 有 to_dict() 方法
- N2CIdMapper 提供 register/to_short/to_guid/reset 方法
- n2c/__init__.py 正确导出公共符号
- 测试覆盖率：schema.py 和 id_mapper.py 每个公共方法至少一个测试
- 无外部依赖（仅使用 dataclasses, typing 标准库）
</success_criteria>

<output>
Create `.planning/phases/70-n2cstruct-schema/70-01-SUMMARY.md` when done
</output>
