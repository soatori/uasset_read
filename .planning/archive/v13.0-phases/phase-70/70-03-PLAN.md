---
phase: 70-n2cstruct-schema
plan: 03
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - src/uasset_read/n2c/validation.py
  - src/uasset_read/n2c/__init__.py
  - tests/n2c/test_validation.py
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02]
must_haves:
  truths:
    - "N2CStruct 输出可以被 JSON Schema 验证"
    - "验证失败提供清晰的错误信息（哪个字段缺失/类型错误）"
    - "Schema 版本与 N2CStruct.version 字段对应"
  artifacts:
    - path: "src/uasset_read/n2c/validation.py"
      provides: "JSON Schema 定义 + validate_n2c_json() 函数"
      min_lines: 50
      exports: ["N2C_JSON_SCHEMA", "validate_n2c_json"]
  key_links:
    - from: "src/uasset_read/n2c/validation.py"
      to: "src/uasset_read/n2c/schema.py"
      via: "Schema 字段名对齐 N2CStruct.to_dict() 输出"
      pattern: "N2CStruct.to_dict"
---

<objective>
实现 N2CStruct JSON Schema 定义和验证函数。使用纯 Python 验证（零外部依赖），确保 N2C 输出格式的稳定性和可消费性。

Purpose: 为 LLM/Agent 消费提供结构化约束。Schema 本身也可作为第三方消费者的参考文档。
Output: validation.py + JSON Schema + 验证测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/uasset_read/n2c/schema.py (Wave 1 创建)
@src/uasset_read/n2c/serializer.py (Wave 2 创建)
@src/uasset_read/formatters/helpers.py (build_schema_info 模式参考)
</context>

<interfaces>
<!-- Schema definition that validation.py must enforce -->

Expected N2CStruct JSON output shape (from to_n2c_json):
```json
{
  "version": "1.0.0",
  "metadata": {
    "Name": "BP_FirstPersonCharacter",
    "BlueprintType": "Normal",
    "BlueprintClass": "FirstPersonCharacter"
  },
  "graphs": [{
    "name": "ExecuteUbergraph",
    "graph_type": "EventGraph",
    "nodes": [{
      "id": "N1",
      "type": "CallFunction",
      "name": "Print String",
      "comment": "",
      "pure": false,
      "latent": false,
      "input_pins": [{"pin_name": "Exec", "pin_category": "exec", "pin_subcategory": "", "direction": "input", "default_value": null}],
      "output_pins": [],
      "extra_data": {"member_name": "PrintString", "member_parent": "KismetSystemLibrary"}
    }],
    "flows": {
      "execution": ["N1->N2->N3"],
      "data": {"N1.P0": "N2.P1"}
    }
  }],
  "structs": [],
  "enums": []
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 N2C JSON Schema + validate_n2c_json() 验证函数</name>
  <read_first>
    - src/uasset_read/n2c/schema.py (N2CStruct.to_dict() 输出字段)
    - src/uasset_read/formatters/helpers.py (build_schema_info 模式)
  </read_first>
  <files>
    src/uasset_read/n2c/validation.py
    tests/n2c/test_validation.py
  </files>
  <action>
实现 JSON Schema 验证（纯 Python，零外部依赖）：

**N2C_JSON_SCHEMA** — 定义完整的 JSON Schema dict（与 to_n2c_json() 输出对齐）：
```python
N2C_JSON_SCHEMA = {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "N2CStruct",
    "type": "object",
    "required": ["version", "metadata", "graphs"],
    "properties": {
        "version": {"type": "string", "pattern": r"^\d+\.\d+\.\d+$"},
        "metadata": {
            "type": "object",
            "required": ["Name"],
            "properties": {
                "Name": {"type": "string"},
                "BlueprintType": {"type": "string"},
                "BlueprintClass": {"type": "string"}
            }
        },
        "graphs": {
            "type": "array",
            "items": {
                "type": "object",
                "required": ["name", "graph_type", "nodes", "flows"],
                "properties": {
                    "name": {"type": "string"},
                    "graph_type": {"type": "string", "enum": ["EventGraph", "Function", "Macro", "Animation"]},
                    "nodes": {"type": "array", "items": {"$ref": "#/$defs/n2c_node"}},
                    "flows": {"$ref": "#/$defs/n2c_flows"}
                }
            }
        },
        "structs": {"type": "array"},
        "enums": {"type": "array"}
    },
    "$defs": {
        "n2c_node": {
            "type": "object",
            "required": ["id", "type", "name"],
            "properties": {
                "id": {"type": "string", "pattern": r"^N\d+$"},
                "type": {"type": "string"},
                "name": {"type": "string"},
                "comment": {"type": "string"},
                "pure": {"type": "boolean"},
                "latent": {"type": "boolean"},
                "input_pins": {"type": "array"},
                "output_pins": {"type": "array"},
                "extra_data": {"type": "object"}
            }
        },
        "n2c_pin": {
            "type": "object",
            "required": ["pin_name", "pin_category"],
            "properties": {
                "pin_name": {"type": "string"},
                "pin_category": {"type": "string"},
                "pin_subcategory": {"type": "string"},
                "direction": {"type": "string", "enum": ["input", "output"]},
                "default_value": {}
            }
        },
        "n2c_flows": {
            "type": "object",
            "properties": {
                "execution": {"type": "array"},
                "data": {"type": "object"}
            }
        }
    }
}
```

**validate_n2c_json(data: dict) -> list[str]** — 验证输入 dict 是否符合 Schema：
- 返回错误消息列表（空列表 = 验证通过）
- 逐层验证：顶层 required 字段 → graphs 数组 → nodes → pins
- 类型检查：string, number, boolean, array, object
- 模式检查：version 格式 (semver), node id 格式 (N\d+)
- 不依赖 jsonschema 库（零运行时依赖要求）

**n2c/__init__.py 更新：** 导出 N2C_JSON_SCHEMA 和 validate_n2c_json。
</action>
<verify>
  <automated>python -m pytest tests/n2c/test_validation.py -x -q</automated>
</verify>
<behavior>
  - validate_n2c_json(to_n2c_json(graphs=[g]).to_dict()) 返回 [] (通过)
  - validate_n2c_json({}) 返回至少包含 "Missing required field: version" 的错误
  - validate_n2c_json({"version": "bad"}) 返回 version 格式错误
  - validate_n2c_json({"version": "1.0.0", "metadata": {}, "graphs": []}) 返回 metadata.Name 缺失错误
  - validate_n2c_json({...node with "id": "invalid"}) 返回 node id 格式错误
  - N2C_JSON_SCHEMA 本身是有效的 dict 结构
</behavior>
<done>validate_n2c_json() 实现完成，正确识别有效和无效输入，零外部依赖</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema validation | 验证函数不信任输入，逐层检查所有字段 |
| Schema definition | Schema 本身是常量，不可被运行时修改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-06 | Tampering | validate_n2c_json input | mitigate | 防御性检查：非 dict 输入直接返回 ["Input must be a dict"] |
| T-70-SC | Tampering | pip installs | mitigate | 无外部包安装 |
</threat_model>

<verification>
- `python -m pytest tests/n2c/test_validation.py -x -q` 全部通过
- `python -m pytest tests/n2c/ -x -q` 全部 n2c 测试通过
- `python -c "from uasset_read.n2c import validate_n2c_json, N2C_JSON_SCHEMA"` 无错误
- validate_n2c_json 对 to_n2c_json 输出始终返回 []
</verification>

<success_criteria>
- N2C_JSON_SCHEMA 定义完整，覆盖 N2CStruct 所有字段
- validate_n2c_json() 正确识别有效和无效输入
- 错误消息清晰具体（指出缺失字段/类型错误/格式错误）
- 零外部依赖（纯 Python 实现验证逻辑）
- validate_n2c_json 导出到 n2c 模块和顶层 __init__.py
</success_criteria>

<output>
Create `.planning/phases/70-n2cstruct-schema/70-03-SUMMARY.md` when done
</output>
