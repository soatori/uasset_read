---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: ["04-00"]
files_modified: ["uasset_read.py"]
autonomous: true
requirements: ["OUT-01", "OUT-02", "OUT-03", "OUT-04", "OUT-05"]
user_setup: []
must_haves:
  truths:
    - "Full JSON output contains summary, exports[], blueprint_metadata, errors"
    - "JSON hierarchy follows Package → Exports → Properties structure"
    - "Text output uses YAML-style indented format"
    - "References resolved from FPackageIndex to object names"
    - "Null markers present for missing/unparsed data"
  artifacts:
    - path: "uasset_read.py"
      provides: "format_json_full(), format_json_summary(), format_text_full(), format_text_summary()"
      exports: ["format_json_full", "format_json_summary", "format_text_full", "format_text_summary"]
  key_links:
    - from: "uasset_read.py"
      to: "ParseResult.asdict()"
      via: "format_json_full() calls dataclasses.asdict(summary)"
      pattern: "asdict\\(result\\.summary\\)"
    - from: "uasset_read.py"
      to: "format_exports_list()"
      via: "format_json_full() calls format_exports_list(result)"
      pattern: "format_exports_list\\(result\\)"
    - from: "uasset_read.py"
      to: "format_text_full()"
      via: "YAML-style text with indented hierarchy"
      pattern: "Package:|Exports:|Properties:"
---

# Phase 4 Plan 01: Output Formatters

## Objective

Implement four formatter functions in uasset_read.py:
- `format_json_full()` - full JSON with complete asset data (OUT-01, OUT-03)
- `format_json_summary()` - compact JSON summary (OUT-03)
- `format_text_full()` - YAML-style text output (OUT-02)
- `format_text_summary()` - compact YAML text (OUT-02)

Purpose: Provide output layer converting ParseResult to user-accessible formats. Follows D-01 (tiered output) and D-02 (Package→Exports→Properties hierarchy).

Output: Four formatter functions in uasset_read.py.

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-phase.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/phases/04-output-and-cli/04-CONTEXT.md (locked decisions D-01 to D-28)
@.planning/phases/04-output-and-cli/04-RESEARCH.md (Pattern 2-3: JSON hierarchy, YAML text)
@uasset_read.py (ParseResult, PropertyValue dataclasses with asdict() pattern)
@tests/test_output_formatting.py (Wave 0 test scaffolding)

## Tasks

<task type="auto">
  <name>Implement format_json_full() per D-01/D-02/D-03</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py (lines 458-471: ParseResult dataclass)
    tests/test_output_formatting.py (stub: test_json_full_structure)
  </read_first>
  <action>
Implement format_json_full(result: ParseResult) -> Dict in uasset_read.py after BlueprintMetadata:

1. Per D-03: top-level errors field - include result.errors
2. Per D-02: Package → Exports → Properties hierarchy
3. Per D-04: top-level blueprint_metadata - only for blueprint assets

Output structure:
```python
{
    "summary": {
        "version_ue4": summary.file_version_ue4,
        "version_ue5": summary.file_version_ue5,
        "legacy_version": summary.legacy_file_version,
        "package_flags": summary.package_flags,  # D-08: raw u32
        "package_name": summary.package_name
    },
    "exports": format_exports_list(result),  # nested exports
    "blueprint_metadata": format_blueprint_dict(result.blueprint) if result.blueprint else None,
    "errors": result.errors
}
```

4. Per D-05: raw FPackageIndex values preserved where unresolved
5. Per D-06: name_map excluded from output (already parsed to object names)

Acceptance criteria:
- Function signature matches: format_json_full(result: ParseResult) -> Dict
- Returns dict with keys: summary, exports, blueprint_metadata, errors
- summary.object has keys: version_ue4, version_ue5, legacy_version, package_flags, package_name
- blueprint_metadata is None for non-blueprint, dict for blueprint
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_json_full, ParseResult, PackageFileSummary; r = ParseResult(summary=PackageFileSummary(package_name='test', file_version_ue4=521, file_version_ue5=0, legacy_file_version=-1, package_flags=0)); d = format_json_full(r); assert 'summary' in d and 'exports' in d and 'blueprint_metadata' in d and 'errors' in d"</automated>
  </verify>
  <done>
    - format_json_full() exists in uasset_read.py
    - Returns dict with required keys
    - No external dependencies imported
  </done>
</task>

<task type="auto">
  <name>Implement helper functions: format_exports_list, format_properties_list</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py (lines 378-405: ObjectExport and PropertyValue dataclasses)
    uasset_read.py (lines 458-471: ParseResult dataclass)
  </read_first>
  <action>
Implement two helper functions after format_json_full():

1. format_exports_list(result: ParseResult) -> List[Dict]:
   - Iterate result.export_map
   - For each export: name, class (via get_asset_class), serial_size
   - Add properties array by calling format_properties_list() with export data
   - Per D-11/D-12: ParentClass, SuperIndex resolved in Phase 3, include as-is
   - Include warning field if resolution failed (per D-13)

2. format_properties_list(properties: List[PropertyValue]) -> List[Dict]:
   - Convert PropertyValue to dict
   - Per OUT-05: null for missing/unparsed data (Python None → JSON null)
   - Structure: {name, type, value, array_index}

Acceptance criteria:
- format_exports_list() returns list of dicts with keys: name, class, serial_size, properties
- format_properties_list() returns list of {name, type, value, array_index}
- None values preserved (JSON null per OUT-05)
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_exports_list, PropertyValue, ObjectExport, PackageFileSummary, ParseResult; props = [PropertyValue('Health', 'IntProperty', 100), PropertyValue('DmgMult', 'FloatProperty', None)]; exports = format_exports_list(ParseResult(export_map=[ObjectExport(object_name='Test', class_index=PackageIndex(0), super_index=PackageIndex(0), outer_index=PackageIndex(0), object_flags=0, serial_size=100, serial_offset=0)])); assert len(exports) == 1 and exports[0]['name'] == 'Test'"</automated>
  </verify>
  <done>
    - format_exports_list() and format_properties_list() exist
    - Return correct data structures
    - None values handled correctly (null in JSON)
  </done>
</task>

<task type="auto">
  <name>Implement format_json_summary() per D-01/D-09/D-10</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py (format_json_full() implementation)
    tests/test_output_formatting.py (stub: test_json_hierarchy)
  </read_first>
  <action>
Implement format_json_summary(result: ParseResult) -> Dict in uasset_read.py:

Per D-09: Medium detail - export object names + types + properties list (name+type+value)
Per D-10: Skip low-level details - no name_map, import_map, CustomVersions

Compact structure:
```python
{
    "version": {
        "ue4": summary.file_version_ue4,
        "ue5": summary.file_version_ue5 or legacy_file_version,
        "legacy": summary.legacy_file_version
    },
    "package_name": summary.package_name,
    "exports": [
        {
            "name": exp.object_name,
            "class": get_asset_class(exp),
            "properties": [
                {"name": p.name, "type": p.type, "value": p.value}
                for p in export_properties
            ]
        }
    ],
    "blueprint_metadata": {...} or null,
    "errors": result.errors
}
```

Acceptance criteria:
- Format is compact (no name_map/import_map arrays)
- Properties include only name, type, value (no array_index)
- Structure mirrors full JSON but smaller
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_json_summary, ParseResult, PackageFileSummary; r = ParseResult(summary=PackageFileSummary(package_name='test', file_version_ue4=521, file_version_ue5=0, legacy_file_version=-1, package_flags=0)); d = format_json_summary(r); assert 'version' in d and 'exports' in d and 'version' in d['version']"</automated>
  </verify>
  <done>
    - format_json_summary() exists and returns compact structure
    - Does not include name_map or import_map raw data
  </done>
</task>

<task type="auto">
  <name>Implement format_text_full() per D-16/D-17/D-19</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py (format_json_full() implementation)
    tests/test_output_formatting.py (stub: test_text_summary)
  </read_first>
  <action>
Implement format_text_full(result: ParseResult) -> str in uasset_read.py:

Per D-17: YAML style hierarchy with indentation (2-space indent)
Per D-19: ERRORS block at end

Structure:
```
Package: /Game/Path/AssetName
  Version: UE4=521, UE5=-8
  Flags: 0x00000000
  Imports: N
  Exports: N

Exports:
  - Name: ClassName_C
    Class: Blueprint
    SerialSize: 1024
    Properties:
      - Name: Health
        Type: IntProperty
        Value: 100
      - Name: DmgMult
        Type: FloatProperty
        Value: 1.5

Blueprint:
  ParentClass: /Game/Core/Character
  Variables: 3
  - Name: Health
    Type: Int
    Default: 100
    Category: Replicated

ERRORS:
  - warning message if any
```

Per D-21: Embed blueprint metadata in export's property list
Per D-22: YAML indentation with nested values

Acceptance criteria:
- YAML-style indented output
- Package header with version info
- Exports section with properties
- ERRORS block at end (empty if no errors)
- 2-space indentation
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_text_full, ParseResult, PackageFileSummary; r = ParseResult(summary=PackageFileSummary(package_name='test', file_version_ue4=521, file_version_ue5=0, legacy_file_version=-1, package_flags=0)); t = format_text_full(r); assert 'Package:' in t and 'Exports:' in t and 'Version:' in t"</automated>
  </verify>
  <done>
    - format_text_full() exists and returns YAML-style string
    - Contains Package:, Exports:, Properties: hierarchy
    - ERRORS: block at end
  </done>
</task>

<task type="auto">
  <name>Implement format_text_summary() per D-18/D-22</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py (format_text_full() implementation)
  </read_first>
  <action>
Implement format_text_summary(result: ParseResult) -> str in uasset_read.py:

Per D-18: Compact YAML summary - one line per export: "Name (Type)"
Per D-22: YAML indentation

Structure:
```
Package: AssetName
Exports: 3

  - Character_Default_C (Blueprint)
  - some_array [5 items]
  - another_prop: value

Blueprint:
  Parent: /Game/Core/Character
  Variables: 2
```

Acceptance criteria:
- One line per export in format "Name (Type)"
- Compact output (no nested property details)
- Blueprint summary included
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_text_summary, ParseResult, PackageFileSummary; r = ParseResult(summary=PackageFileSummary(package_name='test', file_version_ue4=521, file_version_ue5=0, legacy_file_version=-1, package_flags=0)); t = format_text_summary(r); assert 'Package:' in t and 'Exports:' in t and '\n' in t"</automated>
  </verify>
  <done>
    - format_text_summary() exists and returns compact YAML string
    - One line format: "Name (Type)" for exports
  </done>
</task>

<task type="auto">
  <name>Update __all__ exports in uasset_read.py</name>
  <files>uasset_read.py</files>
  <read_first>
    uasset_read.py (lines 1755-1815: current __all__ list)
  </read_first>
  <action>
Update __all__ list in uasset_read.py (after current exports) to include new formatter functions:

```python
__all__ = [
    ...existing exports...,
    # Phase 4: Output formatters
    'format_json_full',
    'format_json_summary',
    'format_text_full',
    'format_text_summary',
]
```

Acceptance criteria:
- All four formatter functions in __all__
- No duplicates or syntax errors
  </action>
  <verify>
    <automated>python -c "from uasset_read import format_json_full, format_json_summary, format_text_full, format_text_summary; print('All formatters imported successfully')"</automated>
  </verify>
  <done>
    - All four formatter functions importable from uasset_read
    - No import errors or AttributeError
  </done>
</task>

## Verification

<automated>python -m pytest tests/test_output_formatting.py::test_json_full_structure -x</automated>
<automated>python -m pytest tests/test_output_formatting.py::test_json_hierarchy -x</automated>
<automated>python -m pytest tests/test_output_formatting.py::test_text_summary -x</automated>

## Success Criteria

- All 4 formatter functions exist in uasset_read.py
- Each returns correct format type (dict for JSON, str for text)
- D-01 (tiered), D-02 (hierarchy), D-03 (errors), D-09/D-10 (compact), D-17/D-19 (YAML text) all implemented

## Output

After completion, Wave 2 plans implement CLI integration.
