---
phase: 01-core-parsing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [uasset_read.py, tests/test_uasset_read.py]
autonomous: true
gap_closure: true
requirements: [CORE-01, CORE-03]

must_haves:
  truths:
    - "LegacyFileVersion=-7 文件正确解析（名称表、导入表、导出表完整读取）"
    - "FileVersionUE4 正确识别为 UE4 版本号（而非错位后的值）"
    - "名称表偏移值合理（小于文件大小）"
  artifacts:
    - path: "uasset_read.py"
      provides: "修正后的 read_package_summary() 函数"
      contains: "LegacyUE3Version 读取逻辑"
    - path: "tests/test_uasset_read.py"
      provides: "LegacyFileVersion=-7 测试覆盖"
      min_lines: 50
  key_links:
    - from: "read_package_summary()"
      to: "PackageFileSummary"
      via: "正确的版本字段顺序"
      pattern: "legacy_file_version <= -8"
---

<objective>
修复 LegacyFileVersion=-7 文件头解析 bug，使解析器能正确读取 Lyra 等 UE4 风格的 UE5 文件。

Purpose: UAT 测试发现真实 Lyra 文件解析失败，根因是版本字段读取顺序错误。
Output: 修正后的 read_package_summary() 函数，支持 -2 至 -9 所有 LegacyFileVersion。

Gap being closed:
- truth: "给定任意有效 .uasset 文件，解析器读取文件头后 PackageFileSummary 包含正确的魔术标签、版本号和偏移"
- status: failed
- reason: "Lyra 文件 UE5Version=522 被错误拒绝；修复版本后 NameOffset 解析错误（偏移值异常大1701736270）"
- severity: blocker
- diagnosis: "LegacyFileVersion=-7 格式与 -8 不同：包含 LegacyUE3Version，不包含 FileVersionUE5"

Technical root cause:
- 当前代码 line 393: `if legacy_file_version >= -8` 读取 UE5 版本（条件错误）
- UE 源码 PackageFileSummary.cpp line 138-141: `if LegacyFileVersion <= -8` 才读取 UE5 版本
- 缺少 LegacyUE3Version 字段读取（UE 源码 line 130-134）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md

## UE 源码参考

来自 `PackageFileSummary.cpp` line 130-144（正确顺序）：

```cpp
if (LegacyFileVersion != -4)
{
    int32 LegacyUE3Version = 0;
    Record << SA_VALUE(TEXT("LegacyUE3Version"), LegacyUE3Version);
}

Record << SA_VALUE(TEXT("FileVersionUE4"), Sum.FileVersionUE.FileVersionUE4);

if (LegacyFileVersion <= -8)  // 注意：<= -8，不是 >= -8
{
    Record << SA_VALUE(TEXT("FileVersionUE5"), Sum.FileVersionUE.FileVersionUE5);
}

Record << SA_VALUE(TEXT("FileVersionLicenseeUE4"), Sum.FileVersionLicenseeUE);
```

## 实际 Lyra 文件结构验证

Character_Default.uasset（LegacyFileVersion=-7）：
- Tag: 0x9e2a83c1
- LegacyFileVersion: -7
- LegacyUE3Version: 864（存在！当前代码未读取）
- FileVersionUE4: 521
- FileVersionLicenseeUE: 0
- CustomVersionsCount: 9

当前代码错误读取结果：
- file_version_ue4: 864（错！实际是 LegacyUE3Version）
- file_version_ue5: 521（错！不存在此字段）
- name_offset: 1701736270（错位后的垃圾值）

<interfaces>
<!-- 从现有代码提取的关键接口 -->
From uasset_read.py:
```python
class FArchive:
    def read_i32(self) -> int
    def read_u32(self) -> int

@dataclass
class PackageFileSummary:
    tag: int
    legacy_file_version: int
    file_version_ue4: int
    file_version_ue5: int  # 需处理：-7 版本不存在
    file_version_licensee: int
    # ... 其他字段
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修正 read_package_summary() 版本字段读取逻辑</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: LegacyFileVersion=-7 文件，FileVersionUE4=521，FileVersionUE5=0
    - Test 2: LegacyFileVersion=-8 文件，FileVersionUE5 正确读取
    - Test 3: LegacyFileVersion=-4 文件，无 LegacyUE3Version 读取
    - Test 4: Lyra Character_Default.uasset 解析成功，name_offset < file_size
  </behavior>
  <action>
修改 read_package_summary() 函数（line 385-422 区域）：

1. **添加 LegacyUE3Version 字段读取**（在 line 386 后）：
   ```python
   # LegacyUE3Version（仅在 legacy_file_version != -4 时存在）
   if legacy_file_version != -4:
       legacy_ue3_version = archive.read_i32()
   else:
       legacy_ue3_version = 0
   ```

2. **修正 UE5 版本读取条件**（替换 line 393-399）：
   ```python
   # UE4 版本（所有现代版本都有）
   file_version_ue4 = archive.read_i32()
   
   # UE5 版本（仅在 legacy_file_version <= -8 时存在）
   # 注意：UE 源码使用 <= -8，当前代码错误使用 >= -8
   if legacy_file_version <= -8:
       file_version_ue5 = archive.read_i32()
   else:
       file_version_ue5 = 0
   ```

3. **修正 UE5 版本验证条件**（替换 line 400-402）：
   ```python
   # UE5 版本验证（仅对 -8 及以上版本）
   if legacy_file_version <= -8 and file_version_ue5 < UE5_VERSION_MIN:
       raise VersionError(f"Unsupported UE5 version: {file_version_ue5}")
   ```

4. **更新 PackageFileSummary dataclass**（可选）：
   添加 `legacy_ue3_version: int = 0` 字段用于调试和完整记录。

关键注意事项：
- 条件使用 `<= -8` 而非 `>= -8`（这是 bug 的核心）
- 不要添加不需要的字段读取
- 保持向后兼容：-8 及以上版本的文件仍正确解析
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py -v -k "legacy"</automated>
    <manual>python -c "from uasset_read import parse_uasset; r = parse_uasset('LyraStarterGame/Content/Characters/Character_Default.uasset'); print(f'ue4={r.summary.file_version_ue4}, ue5={r.summary.file_version_ue5}, name_offset={r.summary.name_offset}')"</manual>
  </verify>
  <done>
LegacyFileVersion=-7 文件正确解析：
- file_version_ue4 = 521（正确的 UE4 版本）
- file_version_ue5 = 0（不存在此字段）
- name_offset < file_size（合理的偏移值）
- parse_uasset() 返回 is_success=True
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加 LegacyFileVersion 版本测试覆盖</name>
  <files>tests/test_uasset_read.py</files>
  <behavior>
    - Test 1: 合成 -7 版本文件头测试
    - Test 2: 合成 -8 版本文件头测试
    - Test 3: 合成 -4 版本文件头测试（无 LegacyUE3Version）
    - Test 4: 真实 Lyra 文件集成测试（如果有测试文件）
  </behavior>
  <action>
在 tests/test_uasset_read.py 中添加版本差异测试：

1. **添加 test_package_summary_legacy_version_minus_7**：
   ```python
   def test_package_summary_legacy_version_minus_7():
       """测试 LegacyFileVersion=-7 文件头解析（UE4 风格 UE5 文件）"""
       # 构造测试数据：Tag + LegacyFileVersion(-7) + LegacyUE3Version + UE4Version + Licensee
       data = create_test_package_header(legacy_version=-7, ue4_version=521)
       archive = FArchive.from_bytes(data)
       summary = read_package_summary(archive)
       assert summary.legacy_file_version == -7
       assert summary.file_version_ue4 == 521
       assert summary.file_version_ue5 == 0  # -7 版本无 UE5 版本
       assert summary.legacy_ue3_version == 864  # -7 版本有 LegacyUE3Version
       archive.close()
   ```

2. **添加 test_package_summary_legacy_version_minus_8**：
   ```python
   def test_package_summary_legacy_version_minus_8():
       """测试 LegacyFileVersion=-8 文件头解析（UE5 风格）"""
       data = create_test_package_header(legacy_version=-8, ue4_version=522, ue5_version=1000)
       archive = FArchive.from_bytes(data)
       summary = read_package_summary(archive)
       assert summary.legacy_file_version == -8
       assert summary.file_version_ue4 == 522
       assert summary.file_version_ue5 == 1000  # -8 版本有 UE5 版本
       archive.close()
   ```

3. **添加 test_package_summary_legacy_version_minus_4**：
   ```python
   def test_package_summary_legacy_version_minus_4():
       """测试 LegacyFileVersion=-4 文件头解析（无 LegacyUE3Version）"""
       data = create_test_package_header(legacy_version=-4)
       archive = FArchive.from_bytes(data)
       summary = read_package_summary(archive)
       assert summary.legacy_file_version == -4
       assert summary.legacy_ue3_version == 0  # -4 版本无 LegacyUE3Version
       archive.close()
   ```

4. **添加 Lyra 真实文件测试（可选）**：
   ```python
   @pytest.mark.skipif(not os.path.exists(LYRA_TEST_FILE), reason="Lyra test file not available")
   def test_real_lyra_file():
       """测试真实 Lyra 文件解析"""
       result = parse_uasset(LYRA_TEST_FILE)
       assert result.is_success
       assert result.summary.file_version_ue4 == 521
       assert len(result.name_map) > 0
   ```

辅助函数 create_test_package_header 需根据正确的文件头结构构造二进制数据。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py -v -k "legacy_version"</automated>
  </verify>
  <done>
新增 3-4 个版本差异测试，覆盖 -4、-7、-8 三种 LegacyFileVersion。
所有测试通过。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 真实文件解析验证</name>
  <what-built>修正后的 read_package_summary() 函数，支持 LegacyFileVersion=-7/-8/-4 等版本</what-built>
  <how-to-verify>
执行以下命令验证真实 Lyra 文件解析：

```bash
cd E:/Develop/uasset_read
python -c "
from uasset_read import parse_uasset
result = parse_uasset('LyraStarterGame/Content/Characters/Character_Default.uasset')
print(f'is_success: {result.is_success}')
print(f'legacy_version: {result.summary.legacy_file_version}')
print(f'ue4_version: {result.summary.file_version_ue4}')
print(f'ue5_version: {result.summary.file_version_ue5}')
print(f'name_count: {result.summary.name_count}')
print(f'name_offset: {result.summary.name_offset}')
print(f'name_map entries: {len(result.name_map)}')
if result.errors:
    print(f'errors: {result.errors}')
"
```

预期结果：
- is_success: True
- legacy_version: -7
- ue4_version: 521
- ue5_version: 0
- name_offset < 20154（文件大小）
- name_map entries > 0

可选：测试其他 Lyra 文件验证修复效果。
  </how-to-verify>
  <resume-signal>输入 "approved" 确认修复成功，或描述遇到的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件输入 → 解析器 | 外部文件可能损坏或使用不支持的格式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | read_package_summary() | mitigate | 版本验证拒绝不支持版本；偏移边界检查 |
| T-01-02 | Denial of Service | FArchive.read() | mitigate | 文件大小限制（已有边界检查） |

本修复不引入新安全风险，仅修正现有版本解析逻辑。
</threat_model>

<verification>
Gap closure 验证：

1. **版本字段正确性**：
   - LegacyFileVersion=-7 → file_version_ue4=正确值，file_version_ue5=0
   - LegacyFileVersion=-8 → file_version_ue5=正确值

2. **真实文件解析**：
   - Lyra Character_Default.uasset → is_success=True
   - name_offset < file_size（偏移值合理）

3. **测试覆盖**：
   - 新增版本差异测试通过
   - 现有 13 个测试仍通过
</verification>

<success_criteria>
Gap closure 成功标准：

- [ ] LegacyFileVersion=-7 文件正确解析（name_offset 合理）
- [ ] Lyra Character_Default.uasset is_success=True
- [ ] file_version_ue4 读取正确值（非错位）
- [ ] 新增版本差异测试全部通过
- [ ] 现有 13 个测试仍通过
</success_criteria>

<output>
修复完成后创建：
- `.planning/phases/01-core-parsing/01-02-SUMMARY.md`

更新：
- `.planning/phases/01-core-parsing/01-UAT.md`（标记 Test 1 为 pass）
</output>