---
phase: 01-core-parsing
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [uasset_read.py, tests/test_uasset_read.py]
autonomous: true
gap_closure: true
requirements: [CORE-01, CORE-03]

must_haves:
  truths:
    - "Lyra Character_Default.uasset (legacy=-7, UE4 v521) 解析正确，NameOffset=448"
    - "PackageName FString 字段在 PackageFlags 前读取"
    - "现代 UE4/UE5 文件 (legacy < 0) 总是读取 NameOffset"
  artifacts:
    - path: "uasset_read.py"
      function: "read_package_summary()"
      change: "在 TotalHeaderSize 后添加 PackageName FString 读取；移除 inline names 分支"
    - path: "tests/test_uasset_read.py"
      test: "create_test_uasset()"
      change: "添加 PackageName 字段输出"
    - path: "tests/test_uasset_read.py"
      test: "test_package_name_field_reading"
      verifies: "PackageName 正确解析"
  key_links:
    - from: "TotalHeaderSize"
      to: "PackageName"
      via: "FString format"
      pattern: "TotalHeaderSize 后，PackageFlags 前读取 PackageName FString"
    - from: "NameCount"
      to: "NameOffset"
      via: "legacy < 0 时总是读取"
      pattern: "移除错误的 inline names 分支"
---

<objective>
修复两个阻止真实 UE5 文件解析的阻塞 bug：

1. **缺失 PackageName FString 字段** - 解析器从 TotalHeaderSize 直接跳到 PackageFlags，遗漏所有 UE4/UE5 文件中存在的 PackageName FString。

2. **错误的 inline names 条件** - 条件 `legacy >= -5` 错误触发 legacy=-7 的 inline names 处理，而现代文件 (legacy < 0) 应总是读取 NameOffset。

目的：使 Lyra Character_Default.uasset 等真实 UE5 文件能够解析。
输出：修正文件头字段序列，匹配 UE PackageFileSummary.cpp。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-core-parsing/01-VERIFICATION.md
@.planning/phases/01-core-parsing/01-REVIEW.md

<interfaces>
来自 UE 5.7 PackageFileSummary.cpp（line 258）：
```cpp
Record << SA_VALUE(TEXT("PackageName"), Sum.PackageName);  // FString, NOT FName!
```

当前解析器（uasset_read.py lines 439-461）：
```python
# BUG: TotalHeaderSize 和 PackageFlags 间缺失 PackageName FString
package_flags = archive.read_u32()  # 期望位置 204，但 PackageName 从此开始！

# BUG: 错误的 inline names 条件
name_count = archive.read_i32()
if legacy_file_version >= -5:
    name_offset = archive.read_i32()
else:
    # legacy < -5: inline names - 但这是错误的！
    # legacy=-7 应读取 NameOffset，而非 inline names
    name_offset = archive.tell()
```

Lyra Character_Default.uasset 字节分析（VERIFICATION.md）：
- Position 204-207: TotalHeaderSize = 14620
- Position 208-211: PackageName FString length = 5
- Position 212-216: PackageName = "None\x00"（5 bytes）
- Position 217-220: PackageFlags = 262144
- Position 221-224: NameCount = 129
- Position 225-228: NameOffset = 448（有效！）

UE 源码参考（PackageFileSummary.cpp line 278）：
```cpp
Record << SA_VALUE(TEXT("NameCount"), Sum.NameCount) << SA_VALUE(TEXT("NameOffset"), Sum.NameOffset);
// 无条件 - 现代文件总是序列化 NameOffset
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：添加 PackageName FString 字段读取</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: Lyra 文件解析正确，NameOffset=448（非垃圾值）
    - Test 2: 含 PackageName 字段的合成测试文件正确解析
    - Test 3: PackageName 字段存储在 PackageFileSummary（用于调试）
  </behavior>
  <read_first>
    - uasset_read.py lines 420-440（TotalHeaderSize 附近的文件头解析）
    - uasset_read.py lines 267-301（PackageFileSummary dataclass）
  </read_first>
  <action>
    修改 read_package_summary() 以添加 PackageName FString 读取：

    **步骤 1：向 PackageFileSummary dataclass 添加 package_name 字段（lines 267-301）：**
    ```python
    @dataclass
    class PackageFileSummary:
        # ... 现有字段 ...
        package_name: str = ""  # PackageName FString (UE PackageFileSummary.cpp line 258)
        # ... 其他字段 ...
    ```

    **步骤 2：在 read_package_summary() 中 TotalHeaderSize 条件块后添加 PackageName 读取（around line 428）：**

    在 SavedHash/TotalHeaderSize 条件块（lines 419-427）后添加：

    ```python
    # PackageName (FString) - Reference: UE PackageFileSummary.cpp line 258
    # Note: PackageName 是 FString 类型（int32 length + UTF-8 data），不是 FName
    package_name = archive.read_fstring()

    # PackageFlags（UE 源码 line 265）
    package_flags = archive.read_u32()
    ```

    **关键：** PackageName 是 FString（read_fstring），不是 FName（read_name）。
    FString 格式：int32 length + UTF-8 bytes + null 终止符。
    FName 格式：u32 index + u32 number（引用 NameMap）。

    当前代码从 TotalHeaderSize 直接跳到 PackageFlags，
    遗漏 9 bytes（4 length + 5 "None\x00"），导致所有后续字段读取偏移。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short</automated>
    <manual>解析 Lyra Character_Default.uasset - 验证 NameOffset=448（非垃圾值）</manual>
  </verify>
  <done>PackageName FString 正确读取；PackageFileSummary 包含 package_name 字段；真实 UE5 文件的 NameOffset 变为有效</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：修复 inline names 条件 - legacy < 0 时总是读取 NameOffset</name>
  <files>uasset_read.py</files>
  <behavior>
    - Test 1: legacy=-7 文件正确读取 NameOffset（非 inline names）
    - Test 2: legacy=-8 文件正确读取 NameOffset
    - Test 3: legacy=-5 文件正确读取 NameOffset
  </behavior>
  <read_first>
    - uasset_read.py lines 443-461（inline names handling block）
  </read_first>
  <action>
    修改 lines 443-461 的 inline names 处理块：

    **当前错误代码：**
    ```python
    name_count = archive.read_i32()
    if legacy_file_version >= -5:
        name_offset = archive.read_i32()
    else:
        # BUG: 此分支对 legacy=-7 运行，但不应该！
        name_offset = archive.tell()
        # ... 跳过 inline name 数据 ...
    ```

    **UE 源码的正确行为：**
    - 所有 UE4/UE5 文件（LegacyFileVersion -2 至 -9）有 NameOffset 字段
    - Inline names 格式仅用于 UE3 文件（LegacyFileVersion >= 0）
    - 按 D-04：LegacyFileVersion 必须在 [-2, -9]，所以我们总是有 NameOffset

    **修复后代码：**
    ```python
    # NameCount + NameOffset（UE 源码 line 278）
    # 现代 UE4/UE5 文件 (legacy < 0) 总是存在
    # Inline names 仅用于 UE3 文件 (legacy >= 0)，按 D-04 不支持
    name_count = archive.read_i32()
    name_offset = archive.read_i32()  # legacy < 0 时总是读取
    ```

    **移除整个 inline names 分支（lines 448-461）。**
    Inline names 处理被错误添加给 "legacy < -5" 文件，
    但 UE 源码显示 NameOffset 对现代文件总是序列化。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v</automated>
    <manual>验证 legacy=-7 文件（Lyra）正确解析，NameOffset 有效</manual>
  </verify>
  <done>移除 inline names 分支；legacy < 0 时总是读取 NameOffset；Lyra 文件正确解析</done>
</task>

<task type="auto" tdd="true">
  <name>任务 3：更新 create_test_uasset helper 以输出 PackageName 字段</name>
  <files>tests/test_uasset_read.py</files>
  <behavior>
    - Test 1: create_test_uasset 输出 PackageName FString 字段
    - Test 2: 合成测试文件匹配真实 UE 文件结构
    - Test 3: 所有现有测试仍通过
  </behavior>
  <read_first>
    - tests/test_uasset_read.py lines 85-120（create_test_uasset header construction）
    - tests/test_uasset_read.py lines 118-120（PackageFlags emission）
  </read_first>
  <action>
    修改 create_test_uasset 以在 PackageFlags 前添加 PackageName 字段：

    找到写入 PackageFlags 的部分（around line 118-120）：
    ```python
    # PackageFlags
    f.write(struct.pack(endian_fmt + 'I', package_flags))
    ```

    在 PackageFlags 前添加 PackageName FString：
    ```python
    # PackageName (FString) - 匹配 UE PackageFileSummary.cpp line 258
    # 合成文件的默认包名为 "None"
    package_name_bytes = "None".encode('utf-8') + b'\x00'
    f.write(struct.pack(endian_fmt + 'i', len(package_name_bytes)))
    f.write(package_name_bytes)

    # PackageFlags
    f.write(struct.pack(endian_fmt + 'I', package_flags))
    ```

    同时更新 inline names 部分（lines 128-143） - 由于从解析器移除 inline names，
    简化 test helper 以总是输出 NameOffset：

    ```python
    # NameCount + NameOffset（现代文件总是存在）
    name_count_pos = f.tell()
    f.write(struct.pack(endian_fmt + 'i', len(names)))  # NameCount
    name_offset_pos = f.tell()
    f.write(struct.pack(endian_fmt + 'i', 0))  # NameOffset（占位符）
    ```

    从 create_test_uasset 完全移除 legacy < -5 inline names 分支。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v</automated>
  </verify>
  <done>合成测试文件现在匹配真实 UE 文件结构；输出 PackageName 字段；从 test helper 移除 inline names</done>
</task>

<task type="auto" tdd="true">
  <name>任务 4：添加 Lyra 类文件解析测试</name>
  <files>tests/test_uasset_read.py</files>
  <behavior>
    - Test 1: legacy=-7、UE4=521 文件（类似 Lyra）解析，NameOffset 有效
    - Test 2: NameMap 包含预期名称
    - Test 3: 无 "Cannot read X bytes" 错误
  </behavior>
  <read_first>
    - tests/test_uasset_read.py lines 250-274（test_package_summary_valid）
  </read_first>
  <action>
    在 test_package_summary_valid 后添加新测试：

    ```python
    def test_legacy_minus_seven_ue4_521():
        """
        测试解析类似 Lyra Character_Default.uasset 的文件。

        验证：
        - legacy=-7、UE4=521 文件正确解析
        - NameOffset 有效（非缺失 PackageName 导致的垃圾值）
        - PackageName 字段正确读取

        此测试捕获 01-05 修复的 bug：
        - 缺失 PackageName FString 字段
        - legacy=-7 的错误 inline names 条件
        """
        names = ["TestName", "TestClass", "TestPackage"]
        path = create_test_uasset(
            legacy_version=-7,  # Lyra 使用 -7
            ue4_version=521,    # Lyra 使用 UE4 version 521
            ue5_version=0,      # legacy=-7 无 UE5 version
            names=names
        )

        try:
            result = parse_uasset(path)

            assert result.is_success, f"Parse failed: {result.errors}"
            assert result.summary is not None
            assert result.summary.legacy_file_version == -7
            assert result.summary.file_version_ue4 == 521
            # 验证 NameOffset 有效（小于文件大小）
            assert result.summary.name_offset < result.summary.total_header_size
            # 验证 NameMap 填充
            assert len(result.name_map) == len(names) + 1  # "None" + names
            assert result.name_map[0] == "None"
            assert result.name_map[1] == "TestName"
            # 验证 PackageName 正确读取
            assert result.summary.package_name == "None"
        finally:
            cleanup_test_file(path)
    ```

    此测试验证两个修复共同作用于 Lyra 类文件。
  </action>
  <verify>
    <automated>python -m pytest tests/test_uasset_read.py::test_legacy_minus_seven_ue4_521 -v</automated>
  </verify>
  <done>新测试通过；legacy=-7 UE4=521 文件正确解析，NameOffset 和 NameMap 有效</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|----------|-------------|
| File header -> Parser | 不可信文件头字段（PackageName length、NameOffset） |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | PackageName FString length | accept | read_fstring 已验证 length 与剩余字节 |
| T-01-05-02 | Tampering | NameOffset value | mitigate | seek() 验证 offset 与文件大小（D-14） |
</threat_model>

<verification>
```bash
# 运行所有测试包括新 Lyra 类测试
python -m pytest tests/ -v

# 修复后验证 Lyra 文件解析：
python -c "from uasset_read import parse_uasset; r = parse_uasset('LyraStarterGame/Content/Characters/Character_Default.uasset'); print(f'NameOffset={r.summary.name_offset}, NameCount={r.summary.name_count}')"
```
</verification>

<success_criteria>
1. 全部 16 个测试通过（14 个现有 + 2 个新）
2. legacy=-7 UE4=521 文件解析，NameOffset 有效（非垃圾值）
3. PackageName 字段读取并存储在 PackageFileSummary
4. Lyra Character_Default.uasset 成功解析（如可用于手动测试）
</success_criteria>

<output>
完成后，创建 `.planning/phases/01-core-parsing/01-05-SUMMARY.md`
</output>