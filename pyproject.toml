[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uasset_read"
version = "0.4.2"
description = "Unreal Engine .uasset file parser for AI agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
authors = [
    {name = "uasset_read Contributors"}
]
keywords = ["unreal", "uasset", "parser", "blueprint", "ue5"]
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]
pak = [
    "cryptography>=41.0",
    "lz4>=4.0",
    "zstandard>=0.22",
]

[project.scripts]
uasset-read = "uasset_read.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "integration: 需要外部样本资产或较慢路径的集成测试",
    "quality: C++ 输出质量门禁测试",
    "regression: 真实资产回归测试",
    "slow: 需要大量时间或全量资产扫描的慢速测试",
]
